=== Phase 01 evidence — verification.txt ===

=== Pre-implementation baseline gate (2026-09-19T09:57:18Z) ===
branch=phoenix HEAD=b54b01d18d3fdbdc8faaabf0dcc3239ff420c962
BASELINE OK: approvedPlanningBaselineCommit=3fb619730c8032d4aa121c5b8aa9649901b45f5f
exit status: 0

=== Task 01 — pnpm and WXT Project Bootstrap (2026-09-19) ===

task ID: T01
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: configuration exception (AGENTS.md Section 11) — TDD not required; no test files
base commit: b54b01d18d3fdbdc8faaabf0dcc3239ff420c962
commit range: b54b01d18d3fdbdc8faaabf0dcc3239ff420c962..5b0a2da4fb0102595a928019e33338e7c2d84a68

Step 1 — RED (prove bootstrap absent):
  command: test -f package.json && test -f .npmrc && test -f wxt.config.ts
  expected: exit status 1 (none of the files exist)
  observed exit status: 1

Step 2 — Remove orphan npm lockfile:
  command: rm -f package-lock.json
  observed: package-lock.json removed

Step 3 — .npmrc created with exactly:
  engine-strict=true
  save-exact=true

Step 4 — package.json created (exact pins; no floating ranges).
Step 5 — wxt.config.ts created; manifest permissions exactly ['sidePanel', 'storage'];
          no side_panel, standalone, content_scripts, or host permissions.
Step 6 — tsconfig.json created (extends .wxt/tsconfig.json).
Step 7 — four RGBA PNG icons generated at public/icon/{16,32,48,128}.png:
  128.png: PNG image data, 128 x 128, 8-bit/color RGBA, non-interlaced
  16.png:  PNG image data, 16 x 16, 8-bit/color RGBA, non-interlaced
  32.png:  PNG image data, 32 x 32, 8-bit/color RGBA, non-interlaced
  48.png:  PNG image data, 48 x 48, 8-bit/color RGBA, non-interlaced
Step 8 — src/entrypoints/background.ts scaffold created (build scaffold only; T22 owns behaviour).

Step 9 — Install dependencies and generate the pnpm lockfile:
  command: pnpm install
  exit status: 0
  postinstall: "wxt prepare" ran (WXT 0.21.4; Generating types; Finished in 675 ms; Done)
  result: pnpm-lock.yaml created; package-lock.json removed.

Step 10 — Focused verification (exact block):
  test ! -f package-lock.json                                      => exit 0
  test -f pnpm-lock.yaml                                            => exit 0
  node -e "...wxt...v!=='0.21.4'..."                                => exit 0
  node -e "...vitest...v!=='5.0.1'..."                              => exit 0
  pnpm run build                                                    => exit 0
  test -f .output/chrome-mv3/manifest.json                          => exit 0
  WXT version output:
    WXT 0.21.4
    Building chrome-mv3 for production with Vite 8.3.0
    Built extension in 96 ms
    .output/chrome-mv3/manifest.json  340 B
    .output/chrome-mv3/background.js  508 B
    .output/chrome-mv3/icon/128.png   360 B
    .output/chrome-mv3/icon/16.png    82 B
    .output/chrome-mv3/icon/32.png    104 B
    .output/chrome-mv3/icon/48.png    157 B
    Total size: 1.55 kB
    Finished in 309 ms
  wxt installed version: 0.21.4
  vitest installed version: 5.0.1
  generated manifest: {"manifest_version":3,"name":"NowPilot","description":"Privacy-first Chrome extension for support engineers.","version":"0.1.0","icons":{"16":"icon/16.png","32":"icon/32.png","48":"icon/48.png","128":"icon/128.png"},"permissions":["sidePanel","storage"],"action":{"default_title":"NowPilot"},"background":{"service_worker":"background.js"}}
  (no side_panel and no standalone.html at this stage; expected for T01)

Step 11 — Build output not committed:
  command: git status --porcelain
  observed:
     D package-lock.json
    ?? .npmrc
    ?? package.json
    ?? pnpm-lock.yaml
    ?? src/
    ?? tsconfig.json
    ?? wxt.config.ts
  .output/ and .wxt/ are absent (ignored by .gitignore); git check-ignore confirms both ignored.
  Note: pnpm 12.4.2 auto-created an untracked pnpm-workspace.yaml during the initial
  resolution (minimumReleaseAgeExclude supply-chain policy). Global Constraint 2 forbids
  pnpm-workspace.yaml, and it is not required: `pnpm install --frozen-lockfile` passes with
  the file absent and the pnpm-lock.yaml hash is unchanged, and a subsequent plain
  `pnpm install` does not re-create it while the lockfile/node_modules are current.
  The file was therefore removed and is not part of this task's changes.

Lockfile state:
  pnpm-lock.yaml present (authoritative). package-lock.json absent (deleted).
  package.json has no excluded direct dependency and no floating version range.

Phase 01 verification applicable now: none — the phase-level toolchain scripts
(verify:phase-1 / verify:all) do not exist until T02. Recorded explicitly.

Specification-compliance checklist:
  - exact pins match DESIGN.md Section 3: yes (non-exact pins = none)
  - wxt.config.ts permissions exactly sidePanel + storage: yes
  - no excluded direct dependency present: yes
  - standalone stem rules untouched: yes
  - no content script: yes

Code-quality and security checklist:
  - .output/ and .wxt/ uncommitted (ignored): yes
  - no secrets or local paths committed: yes
  - package-lock.json removed: yes
  - pnpm-lock.yaml present: yes

Completion criteria: Steps 10–11 pass; one atomic task commit exists; working tree
clean; status recorded.

=== Task 02 — Test, Lint, Type-Check, Formatting, and Build Configuration (2026-09-19) ===

task ID: T02
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: configuration exception (AGENTS.md Section 11); RED→GREEN applied to tests/config/toolchain.test.ts
base commit: ba7ace2427755c66914de6462b24cc1619ef6c17

Files created: vitest.config.ts, tests/setup.ts, tests/config/toolchain.test.ts,
  eslint.config.mjs, .prettierrc, .prettierignore
Files modified: package.json (scripts), tsconfig.json (authorised corrections below)

Step 1 — RED:
  command: pnpm exec vitest run tests/config/toolchain.test.ts
  exit status: 1
  result: 4 tests | 2 failed | 2 passed
    FAIL declares every required verification script: missing script typecheck
    FAIL provides the test, lint, type-check, and formatting configuration files: missing vitest.config.ts

Pre-correction defects found and independently reproduced by the controller
(four corrections applied; all controller-verified under the pinned toolchain):
  B1: tsconfig.json "extends": ".wxt/tsconfig.json" → "./.wxt/tsconfig.json"
      (TS6053 File '.wxt/tsconfig.json' not found; TS does not treat a bare ".wxt/..." as relative).
  B2: vitest.config.ts unit project — removed `esbuild: { jsx: 'automatic' }`
      (TS2769; Vite 8.3.0 moved the transform to oxc).
  B3: tests/config/toolchain.test.ts line 5 →
      `const root = resolve(dirname(fileURLToPath(import.meta.url)), '../..') + '/';`
      (jsdom selects Vite's client transform, which rewrites
      `new URL('../../', import.meta.url)` to an http://localhost:3000/@fs/... URL and
      breaks fileURLToPath with ERR_INVALID_URL_SCHEME).
  B4: tsconfig.json compilerOptions + "jsx": "react-jsx"
      (.wxt/tsconfig.json does not set jsx; required for later .tsx typecheck).
  Root-cause proof: probe with "./.wxt/tsconfig.json" resolved config cleanly; a temp
  config with the unit environment set to 'node' passed the plan test 4/4.

Step 8 — focused verification after corrections (sequential):
  pnpm run test                  => exit 0  (unit: 1 file, 4 passed)
  pnpm run typecheck             => exit 0  (tsc --noEmit clean)
  pnpm run lint                  => exit 0  (eslint . clean)
  pnpm exec prettier --check .   => exit 0  (after pnpm run format)
  pnpm run build                 => exit 0  (WXT 0.21.4 chrome-mv3; .output/chrome-mv3/manifest.json 340 B)
  Note: the first post-correction `pnpm run test` hit a transient forks-worker startup
  timeout (Duration 63.79s); an immediate retry passed 4/4 (2.58s) with no code change.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0

Formatting scope:
  pnpm run format changed only tests/config/toolchain.test.ts and vitest.config.ts
  (both T02-authorised). No other file changed; no git checkout -- restore was required.
  .prettierignore is the verbatim brief content; no extra entry needed.

Unit test count: 4 passed, 0 skipped, 0 failed (tests/config/toolchain.test.ts).

Other checks:
  pnpm-workspace.yaml absent before and after; not recreated by any T02 command.
  tsconfig.json existing strict / noFallthroughCasesInSwitch / noUnusedLocals /
  noUnusedParameters and include entries preserved.

Specification-compliance checklist:
  - exact script names and identical verify:phase-1 / verify:all chains: yes
  - Vitest 5 projects only (unit/manifest/isolation), exact include/exclude: yes
    (project-level esbuild option removed per authoritative correction B2)
  - no @eslint/js, globals, or other excluded dependency added: yes
  - no content-script test: yes

Code-quality and security checklist:
  - no secrets, credentials, or sensitive data: yes
  - `test` runs only the unit project and requires no build artefacts: yes
  - test:manifest / test:isolation run separate projects against real build output: yes
  - no test writes outside the workspace: yes

Completion criteria: Step 8 exit 0 for all five commands; phase-applicable chain exit 0;
unit test green; one atomic commit.

=== Task 03 — Operational Error and Diagnostic Registries with debugLog (2026-09-19) ===

task ID: T03
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: code task; TDD RED→GREEN applied
base commit: 986fd64 (T02 atomic commit `chore(phase-01): configure test lint typecheck format and build`)
Files created: src/core/error/errorCodes.ts, src/core/error/debugLog.ts,
  tests/core/error/errorCodes.test.ts, tests/core/error/debugLog.test.ts
Files modified: none (production); test files typed per the pinned tsconfig (see Note B5)

Step 2 — RED:
  command: pnpm run test -- tests/core/error
  exit status: 1
  observed: Test Files 2 failed | 1 passed (3); Tests 4 passed (4)
    FAIL |unit| tests/core/error/debugLog.test.ts
      Error: Failed to resolve import "@/core/error/debugLog" from
      "tests/core/error/debugLog.test.ts". Does the file exist?
      Plugin: vite:import-analysis
    FAIL |unit| tests/core/error/errorCodes.test.ts
      Error: Failed to resolve import "@/core/error/errorCodes" from
      "tests/core/error/errorCodes.test.ts". Does the file exist?
      Plugin: vite:import-analysis
  Expected failure reason confirmed: both registry/logger modules were absent, so Vite
  could not resolve the `@` imports. The single passing file was the pre-existing
  tests/config/toolchain.test.ts (Vitest 5 does not restrict `--` positionals to the
  requested subdirectory under `--project unit`), 4 passed.
  No canonical code/event was missing.

Step 3 — src/core/error/errorCodes.ts implemented verbatim:
  ERROR_CODES has exactly the 13 DESIGN.md Section 5 operational codes in the approved
  order: RUNTIME_ENVELOPE_INVALID, RUNTIME_SENDER_REJECTED, WORKSPACE_INVALID_METADATA,
  WORKSPACE_OWNERSHIP_AMBIGUOUS, WORKSPACE_EPOCH_MISMATCH, WORKSPACE_VERSION_CONFLICT,
  WORKSPACE_STALE_MUTATION, WORKSPACE_REHYDRATION_REQUIRED, WORKSPACE_HANDOFF_FAILED,
  STANDALONE_TAB_INVALID, STANDALONE_OPEN_FAILED, THEME_INVALID_VALUE,
  THEME_PERSIST_FAILED.
  DIAGNOSTIC_EVENTS has exactly the 1 value STANDALONE_ROUTE_FALLBACK.
  ErrorCodeSchema and DiagnosticEventSchema are two separate closed z.enum schemas; no
  `ErrorCode | DiagnosticEvent` union and no arbitrary-string overloads.

Step 4 — src/core/error/debugLog.ts implemented verbatim:
  REDACTED_CONTEXT_KEYS is the exact 12-key list (apiKey, api_key, authorization, body,
  caseContent, clipboard, content, cookie, password, path, prompt, token); key-based
  redaction plus non-primitive replacement with `[REDACTED]`. Every emitted record passes
  through redactContext; non-primitive values are never serialised. No network, storage,
  or side effects. No new dependency (zod only).

Note B5 (type-only test adaptation, no behaviour change):
  Under the T02-pinned `.wxt/tsconfig.json` `noUncheckedIndexedAccess: true`, the verbatim
  test expression `sink.debug.mock.calls[0][0]` is TS2532 (Object is possibly
  'undefined'). `sink.debug.mock.calls[0]![0]` was used in both occurrences in
  tests/core/error/debugLog.test.ts. All assertion values, canonical strings, and
  behaviour are unchanged. tsconfig.json was not modified (out of scope). First
  `pnpm run typecheck` (before the adaptation) exited 2 with the two TS2532 errors at
  lines 35 and 44; after it, exit 0.

Step 5 — focused verification (sequential):
  pnpm run test -- tests/core/error              => exit 0  (3 files, 11 passed)
  pnpm exec vitest run --project unit tests/core/error/errorCodes.test.ts tests/core/error/debugLog.test.ts
                                                 => exit 0  (2 files, 7 passed — T03 tests only)
  pnpm run typecheck                             => exit 0  (tsc --noEmit clean)
  pnpm run lint                                  => exit 0  (eslint . clean)
  pnpm exec prettier --check .                   => exit 0  (All matched files use Prettier code style)

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 3 files, 11 tests passed.

Test counts:
  T03 test files: 2 passed, 7 tests passed (3 errorCodes + 4 debugLog), 0 failed, 0 skipped.
  Unit project total after T03: 3 files, 11 passed.

Specification-compliance checklist:
  - exactly the 13 ErrorCode values in the approved order: yes
  - exactly the 1 DiagnosticEvent value: yes
  - two separate closed schemas, not one union: yes
  - no arbitrary-string overload: yes
  - no invented, renamed, or added codes/events: yes
  - every logged record passes through redactContext: yes
  - non-primitive values never serialised: yes

Code-quality and security checklist:
  - key-based redaction only; exact REDACTED_CONTEXT_KEYS list: yes
  - no raw sensitive content logged or persisted: yes
  - no empty catch blocks (no catch blocks): yes
  - no network, storage, or filesystem side effects: yes
  - no new dependency: yes
  - only the four task files created; nothing else modified: yes

Completion criteria: both test files green; typecheck and lint pass; phase-applicable
chain exit 0; one atomic task commit exists; working tree clean; status recorded.

=== Task 04 — Storage Keys and Validated Chrome-Storage Adapter (2026-09-19T10:28:14Z) ===

task ID: T04
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
base commit: d357adf3d71b181eb9478fa39ce5499adc3f247b
implementation tier: economy

Step 1 — Files created:
  src/core/storage/storageKeys.ts
  src/core/storage/chromeStorage.ts
  tests/helpers/chromeMock.ts
  tests/core/storage/storageKeys.test.ts
  tests/core/storage/chromeStorage.test.ts
Step 6 — Files modified:
  tests/setup.ts (default chrome.storage mock; imported createChromeStorageMock)

Step 2 — RED (prove modules absent):
  command: pnpm run test -- tests/core/storage
  observed exit status: 1
  output: Failed to resolve import "@/core/storage/chromeStorage" from
          tests/core/storage/chromeStorage.test.ts; Failed to resolve import
          "@/core/storage/storageKeys" from tests/core/storage/storageKeys.test.ts.
  expected failure reason confirmed: both target modules do not exist yet.

Step 5 — GREEN:
  command: pnpm run test -- tests/core/storage
  exit status: 0
  output: Test Files 5 passed (5); Tests 21 passed (21)
  note: under Vitest 5 the pnpm-script passthrough runs the whole unit project;
        T04-only count confirmed with explicit paths (2 files, 10 tests).

Step 7 — Focused verification:
  command: pnpm exec vitest run --project unit tests/core/storage
  exit status: 0
  output: Test Files 2 passed (2); Tests 10 passed (10), 0 failed, 0 skipped.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 5 files, 21 tests passed.

Formatting:
  pnpm exec prettier --write src/core/storage/chromeStorage.ts ...   => exit 0
  pnpm exec prettier --check .                                      => exit 0
    (All matched files use Prettier code style!)

Test counts:
  T04 test files: 2 passed, 10 tests passed (2 storageKeys + 8 chromeStorage),
  0 failed, 0 skipped.
  Unit project total after T04: 5 files, 21 passed.

Specification-compliance checklist:
  - exactly the 7 DESIGN.md Section 9 keys, in order: np_workspace_meta,
    np_workspace_version, np_workspace_election, np_workspace_handoff,
    np_standalone_tab, np_theme, np_theme_pack: yes
  - area mapping exact (local/local/session/session/session/sync/sync): yes
  - each key declared exactly once; no invented keys: yes
  - all reads Zod-validated; malformed/missing data fails closed to
    {status:'invalid'} or {status:'missing'}, never throws: yes
  - write validates through the schema before persisting: yes
  - session values are ephemeral coordination state, never labelled durable: yes
  - no IndexedDB, no network, no WXT storage API, no direct chrome.storage call
    outside this adapter, no side effects: yes
  - no new dependency (zod reused): yes

Code-quality and security checklist:
  - read never throws on malformed stored data: yes
  - write rejects invalid values before the underlying set: yes
  - subscribe filters by exact key and mapped area; unsubscribe removes the
    listener and is safe to call once per subscription: yes
  - no raw value or sensitive content logged: yes
  - only the six task files created/modified; toolchain config untouched: yes

Type-level deviations (disclosed):
  (D1) tests/helpers/chromeMock.ts (verbatim from plan) imports `StorageArea`
       from '@/core/storage/chromeStorage'. The plan's chromeStorage.ts did not
       export it. A single additive type re-export line
       `export type { StorageArea, StorageKey } from './storageKeys';` was added;
       no runtime surface changed.
  (D2) The plan's object-literal generic methods (`async read<T>(key, schema)`)
       fail the pinned `strict`/`noUnusedParameters` type-check (TS6133/TS7006
       implicit any; generic contextual typing does not flow into object-literal
       methods). The three methods were given their explicit interface
       signatures; behaviour and values are identical.
  (D3) The plan's `const change = changes[key]` is optional under the pinned
       `noUncheckedIndexedAccess: true`; minimal non-null assertion
       `changes[key]!` applied inside the already-guarded `key in changes` branch.

Completion criteria: storage tests green; typecheck and lint pass; phase-applicable
chain exit 0; one atomic task commit exists; working tree clean; status recorded.

=== Task 05 — Canonical Standalone Route Registry (2026-09-19T10:31:13Z) ===

task ID: T05
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: code task; TDD RED→GREEN applied
implementation tier: economy
base commit: 7a4cee0755659730c1fa7a10276555870faf42f2
Files created: src/core/registry/standaloneRoutes.ts,
  tests/core/registry/standaloneRoutes.test.ts
Files modified: none (production)

Step 1 — RED:
  command: pnpm run test -- tests/core/registry/standaloneRoutes.test.ts
  exit status: 1
  observed: Test Files 1 failed | 5 passed (6); Tests 21 passed (21)
    FAIL |unit| tests/core/registry/standaloneRoutes.test.ts (0 test)
      Error: Failed to resolve import "@/core/registry/standaloneRoutes" from
      "tests/core/registry/standaloneRoutes.test.ts". Does the file exist?
      Plugin: vite:import-analysis
  Expected failure reason confirmed: the registry module did not exist yet, so Vite
  could not resolve the `@/core/registry/standaloneRoutes` import. The five passing
  files were the pre-existing unit tests (Vitest 5 does not restrict `--` positionals
  to the requested subdirectory under `--project unit`). No canonical route was missing.

Step 3 — src/core/registry/standaloneRoutes.ts implemented per the brief:
  STANDALONE_ROUTE_IDS has exactly the 7 DESIGN.md Section 11 identifiers in approved
  order: chat, agent, notes, write, tools, options, diagnostics.
  DEFAULT_STANDALONE_ROUTE_ID = 'chat'.
  StandaloneRouteIdSchema is a closed z.enum over STANDALONE_ROUTE_IDS.
  STANDALONE_ROUTES maps each id to { id, label, placement, order, hash } with labels
  Chat/Agent/Notes/Write/Tools/Options/Diagnostics, order 1..7, and hash `#/<id>`.
  PRIMARY_STANDALONE_ROUTES = chat, agent, notes, write, tools (5).
  FOOTER_STANDALONE_ROUTES = options, diagnostics (2).
  standaloneHashRoute(id) returns `#/${id}`.
  parseStandaloneRouteId validates `^#\/([a-z-]+)$` against the closed id list.
  resolveStandaloneRouteId returns { routeId, fellBack }; unknown/empty → chat + true.
  No React, page components, TeamGQM, ServiceNow, query/path routing, browser-history
  concerns, side effects, or new dependency (zod reused).

Step 4 — GREEN (focused, explicit path):
  command: pnpm run test -- tests/core/registry/standaloneRoutes.test.ts
  exit status: 0
  output: Test Files 6 passed (6); Tests 28 passed (28)
  note: under Vitest 5 the pnpm-script passthrough runs the whole unit project;
        T05-only count confirmed with explicit path.
  command: pnpm exec vitest run --project unit tests/core/registry/standaloneRoutes.test.ts
  exit status: 0
  output: Test Files 1 passed (1); Tests 7 passed (7), 0 failed, 0 skipped.

Step 4 — Focused verification (sequential):
  pnpm run typecheck                             => exit 0  (tsc --noEmit clean)
  pnpm run lint                                  => exit 0  (eslint . clean)
  pnpm exec prettier --check .                   => exit 0  (All matched files use Prettier code style)

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 6 files, 28 tests passed.

Test counts:
  T05 test file: 1 passed, 7 tests passed, 0 failed, 0 skipped.
  Unit project total after T05: 6 files, 28 passed.

Specification-compliance checklist:
  - exactly the 7 DESIGN.md Section 11 route IDs in approved order: yes
  - default route chat: yes
  - 5 primary (chat, agent, notes, write, tools) + 2 footer (options, diagnostics): yes
  - hash form `#/<id>` for every route and standaloneHashRoute: yes
  - closed StandaloneRouteIdSchema; teamgqm and servicenow rejected: yes
  - unknown hash → chat with explicit fellBack=true signal, no user-facing error: yes
  - no invented/extra routes; standalone stem only; no `app` family: yes
  - no path or query routing; no browser-history routing concerns: yes
  - no new dependency (zod reused); no side effects: yes

Code-quality and security checklist:
  - closed schema; no stringly-typed route math in UI: yes
  - fallback is explicit and testable: yes
  - pure module; no network, storage, filesystem, or global state: yes
  - no secrets or sensitive content: yes
  - only the two task files created; no other file modified: yes

Type-level deviations (disclosed):
  (D4) The brief's `const candidate = match[1]` is `string | undefined` under the
       T02-pinned `noUncheckedIndexedAccess: true` (RegExpExecArray is array-like),
       which would fail `.includes(candidate)` (TS2345). Minimal non-null assertion
       `match[1]!` applied inside the existing `if (!match) return undefined;` guard;
       all values, regex, and behaviour unchanged. tsconfig.json not modified.

Completion criteria: route test green; typecheck and lint pass; phase-applicable
chain exit 0; one atomic task commit exists; working tree clean; status recorded.

=== Task 06 — Workspace Types and Durable Metadata Schemas (2026-09-19T10:33:44Z) ===

task ID: T06
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: code task; TDD RED->GREEN applied
implementation tier: economy
base commit: 25ac23ed3e8ffefd9561a121273ca15785e079c1 (T05 atomic commit
  `feat(phase-01): add canonical standalone route registry`)
Files created: src/core/workspace/workspaceTypes.ts,
  tests/core/workspace/workspaceTypes.test.ts
Files modified: none (production)

Step 1 — test file created verbatim from the brief at
tests/core/workspace/workspaceTypes.test.ts (7 tests).

Step 2 — RED:
  command: pnpm run test -- tests/core/workspace/workspaceTypes.test.ts
  exit status: 1
  observed: Test Files 1 failed | 6 passed (7); Tests 28 passed (28)
    FAIL |unit| tests/core/workspace/workspaceTypes.test.ts (0 test)
      Error: Failed to resolve import "@/core/workspace/workspaceTypes" from
      "tests/core/workspace/workspaceTypes.test.ts". Does the file exist?
      Plugin: vite:import-analysis
  Expected failure reason confirmed: the workspace types module did not exist yet,
  so Vite could not resolve the `@/core/workspace/workspaceTypes` import. The six
  passing files were the pre-existing unit tests (Vitest 5 does not restrict `--`
  positionals to the requested subdirectory under `--project unit`). No required
  schema or field was missing.

Step 3 — src/core/workspace/workspaceTypes.ts implemented verbatim per the brief:
  WORKSPACE_SCHEMA_VERSION = 1 (as const).
  WorkspaceWriterTypeSchema = closed z.enum(['sidepanel', 'standalone']).
  HandoffPhaseSchema = closed z.enum(['idle', 'prepared', 'acknowledged',
    'committed']).
  InstanceIdSchema = z.string().min(1).
  createInstanceId() uses the runtime crypto.randomUUID.
  WorkspaceMetadataSchema = { schemaVersion literal, committedVersion int
    nonnegative, updatedAt int nonnegative }.
  WorkspaceVersionRecordSchema = { committedVersion, updatedAt } (int nonnegative).
  ElectionRecordSchema = { writerType, writerInstanceId, epoch, committedVersion,
    handoffPhase, handoffTargetInstanceId nullable, updatedAt } — matches DESIGN.md
    Section 6 election metadata (writer type, instance ID, epoch, committed version,
    handoff state, target ID).
  HandoffRecordSchema = { phase ['prepared','acknowledged'], fromInstanceId,
    fromWriterType, toInstanceId, toWriterType, epoch, baseVersion, preparedAt,
    acknowledgedAt nullable }.
  StandaloneTabRecordSchema = { tabId int nonnegative, openedAt int nonnegative }.
  WorkspaceMutationKindSchema = z.literal('workspace.metadata.set') (Approved
    Interpretation 3: closed single kind only).
  WorkspaceMutationSchema = { mutationId uuid, writerInstanceId, epoch, baseVersion,
    resultingVersion, kind, payload: WorkspaceMetadataSchema } — matches DESIGN.md
    Section 6 mutation protocol (mutation ID, writer instance ID, epoch, base version,
    resulting version, type, schema-valid payload).
  createEmptyWorkspaceMetadata(now) returns schemaVersion/committedVersion 0/updatedAt.
  No IndexedDB, no network, no new dependency (zod reused), no side effects, no store,
  election, handoff, or mutation logic.

Step 4 — GREEN (focused, explicit path):
  command: pnpm run test -- tests/core/workspace/workspaceTypes.test.ts
  exit status: 0
  output: Test Files 7 passed (7); Tests 35 passed (35)
  note: under Vitest 5 the pnpm-script passthrough runs the whole unit project;
        T06-only count confirmed with an explicit file path.
  command: pnpm exec vitest run --project unit tests/core/workspace/workspaceTypes.test.ts
  exit status: 0
  output: Test Files 1 passed (1); Tests 7 passed (7), 0 failed, 0 skipped.

Step 4 — Focused verification (sequential):
  pnpm run typecheck                             => exit 0  (tsc --noEmit clean)
  pnpm run lint                                  => exit 0  (eslint . clean)
  pnpm exec prettier --check .                   => exit 0 after formatting the T06
    test file only (`all matched files use Prettier code style`). Formatting changed
    only the final `expect(...).toBe(false)` line wrapping; all values, schema literals,
    imports, and assertions unchanged. No other file was reformatted.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 7 files, 35 tests passed.

Test counts:
  T06 test file: 1 passed, 7 tests passed, 0 failed, 0 skipped.
  Unit project total after T06: 7 files, 35 passed.

Specification-compliance checklist:
  - WORKSPACE_SCHEMA_VERSION exact (1): yes
  - WorkspaceWriterType exactly sidepanel | standalone: yes
  - handoff phases exactly idle/prepared/acknowledged/committed: yes
  - InstanceId string min 1; createInstanceId uses crypto.randomUUID: yes
  - election metadata fields match DESIGN.md Section 6 (writer type, instance ID,
    epoch, committed version, handoff state, target ID): yes
  - mutation fields match DESIGN.md Section 6 (mutation ID, writer instance ID, epoch,
    base version, resulting version, type, schema-valid payload): yes
  - WorkspaceMutationKind closed literal 'workspace.metadata.set' only; generic/arbitrary
    and note/conversation/memory/provider kinds absent and rejected (tested): yes
  - no invented fields; no store/election/handoff/mutation logic: yes
  - no IndexedDB, no network, no new dependency, no side effects: yes
  - only the two task files created (plus evidence/STATUS); no other file changed: yes

Code-quality and security checklist:
  - closed schemas; no `any`: yes
  - instance IDs generated with the runtime crypto.randomUUID: yes
  - schemas reject negative versions and unknown writer types (tested): yes
  - acknowledgement timestamp required present-or-null (tested): yes
  - pure module; no secrets or sensitive content: yes

Completion criteria: type test green; typecheck and lint pass; phase-applicable
chain exit 0; one atomic task commit exists; working tree clean; status recorded.

=== Task 07 — Runtime Primitives, Payload Schemas, and RuntimeEnvelope (2026-09-19T10:38:23Z) ===

task ID: T07
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: code task; TDD RED->GREEN applied
implementation tier: balanced
base commit: f85718931b80e63751506b9347b4597c045bfbdd (T06 atomic commit
  `feat(phase-01): add workspace types and durable metadata schemas`)
Files created (production):
  src/core/runtime/RuntimeSurface.ts
  src/core/runtime/OperationId.ts
  src/core/runtime/MessageType.ts
  src/core/runtime/messageSchemas.ts
  src/core/runtime/RuntimeEnvelope.ts
Files created (tests):
  tests/core/runtime/runtimePrimitives.test.ts
  tests/core/runtime/messageSchemas.test.ts
  tests/core/runtime/runtimeEnvelope.test.ts
Files modified: none (production or toolchain)

Step 1 — three test files created verbatim from the brief:
  runtimePrimitives.test.ts (3 tests), messageSchemas.test.ts (4 + 1 registry
  completeness test appended per Step 9), runtimeEnvelope.test.ts (5 tests).

Step 2 — RED:
  command: pnpm run test -- tests/core/runtime
  exit status: 1
  observed: Test Files 3 failed | 7 passed (10); Tests 35 passed (35)
    FAIL |unit| tests/core/runtime/messageSchemas.test.ts (0 test)
      Error: Failed to resolve import "@/core/runtime/messageSchemas" from
      "tests/core/runtime/messageSchemas.test.ts". Does the file exist?
      Plugin: vite:import-analysis
    FAIL |unit| tests/core/runtime/runtimeEnvelope.test.ts (0 test)
      Error: Failed to resolve import "@/core/runtime/OperationId" from
      "tests/core/runtime/runtimeEnvelope.test.ts". Does the file exist?
      Plugin: vite:import-analysis
    FAIL |unit| tests/core/runtime/runtimePrimitives.test.ts (0 test)
      Error: Failed to resolve import "@/core/runtime/MessageType" from
      "tests/core/runtime/runtimePrimitives.test.ts". Does the file exist?
      Plugin: vite:import-analysis
  Expected failure reason confirmed: all five runtime modules were absent, so Vite could
  not resolve the `@/core/runtime/*` imports in the three new suites. The seven passing
  files were the pre-existing unit tests (Vitest 5 does not restrict `--` positionals to
  the requested subdirectory under `--project unit`). No canonical identifier, message
  type, or payload name was missing.

Steps 3–7 — implementation verbatim per the brief:
  RuntimeSurface.ts: RUNTIME_SURFACES exactly ['background','sidepanel','standalone'];
    RuntimeSurface; RuntimeSurfaceSchema (closed z.enum); RUNTIME_TARGETS adds '*';
    RuntimeTarget; RuntimeTargetSchema; WorkspaceWriterSurface = Exclude<...,'background'>.
  OperationId.ts: OperationIdSchema = z.string().uuid(); OperationId; createOperationId()
    uses the runtime crypto.randomUUID.
  MessageType.ts: MESSAGE_TYPES exactly the eleven DESIGN.md Section 5 strings in approved
    order; MessageType; MessageTypeSchema (closed z.enum); MESSAGE_TYPE_ALLOWED_SOURCES is
    the single canonical allowed-source registry, explicit for every one of the eleven types,
    with no wildcard, permissive default, or fallback source (Approved Interpretation 1).
  messageSchemas.ts: the eleven canonical payload schemas named exactly per DESIGN.md
    Section 5 (WorkspaceMutationPayload, WorkspaceHandoffPreparePayload,
    WorkspaceHandoffAckPayload, WorkspaceHandoffCommitPayload, WorkspaceRelinquishPayload,
    WorkspaceRehydrateRequestPayload, WorkspaceRehydrateResponsePayload,
    StandaloneOpenPayload, StandaloneFocusPayload, StandaloneClosedPayload,
    RuntimeErrorPayload); WorkspaceRehydrateRequestPayload carries instanceId and writerType
    (Approved Interpretation 2); standalone destinations validated with StandaloneRouteIdSchema
    (T05 registry); RuntimeErrorPayload.code uses ErrorCodeSchema (T03 registry); mutation
    payload reuses WorkspaceMutationSchema (T06); RUNTIME_PAYLOAD_SCHEMAS maps every
    MessageType exactly once; RuntimeMessageSchema is one closed discriminated union; the
    MESSAGE_TYPES re-export exists so RuntimeEnvelope.ts imports both from one place (no
    second message-type list).
  RuntimeEnvelope.ts: envelopeBaseFields exactly DESIGN.md Section 5 (envelopeVersion:1,
    id:OperationId, source:RuntimeSurface, target:RuntimeTarget, timestamp, optional
    correlationId/electionEpoch/workspaceVersion) with one discriminated-union variant per
    the eleven types; RuntimeEnvelope; parseRuntimeEnvelope(input) = safeParse (unknown type
    fails closed).

Step 8 — GREEN (focused, explicit path):
  command: pnpm run test -- tests/core/runtime
  exit status: 0
  output: Test Files 10 passed (10); Tests 48 passed (48)
  note: under Vitest 5 the pnpm-script passthrough runs the whole unit project.
  command: pnpm exec vitest run --project unit tests/core/runtime/runtimePrimitives.test.ts
             tests/core/runtime/messageSchemas.test.ts tests/core/runtime/runtimeEnvelope.test.ts
  exit status: 0
  output: Test Files 3 passed (3); Tests 13 passed (13), 0 failed, 0 skipped (T07-only).

Step 9 — payload-registry completeness test added and run: PASS.
  (Object.keys(RUNTIME_PAYLOAD_SCHEMAS).sort() equals [...MESSAGE_TYPES].sort().)

Focused verification (sequential):
  pnpm run typecheck                             => exit 0  (tsc --noEmit clean)
  pnpm run lint                                  => exit 0  (eslint . clean)
  pnpm exec prettier --check .                   => exit 0 after formatting only the two
    T07 files flagged (src/core/runtime/messageSchemas.ts and
    tests/core/runtime/messageSchemas.test.ts). Formatting changed whitespace/line wrapping
    only; all identifiers, message-type strings, payload-schema names, and assertions are
    unchanged. No other file was reformatted.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 10 files, 48 tests passed.

Test counts:
  T07 test files: 3 passed, 13 tests passed (3 primitives + 5 payload schemas incl. the
  registry completeness test + 5 envelope), 0 failed, 0 skipped.
  Unit project total after T07: 10 files, 48 passed.

Specification-compliance checklist:
  - RuntimeSurface exactly three values background|sidepanel|standalone: yes
  - RUNTIME_TARGETS adds only '*' (RuntimeSurface | '*'): yes
  - eleven canonical message-type strings in approved order; no invented/extra type: yes
  - MESSAGE_TYPE_ALLOWED_SOURCES closed, typed, explicit for all eleven; no wildcard or
    permissive default (Approved Interpretation 1): yes
  - eleven type->payload-schema names match DESIGN.md Section 5 exactly: yes
  - rehydrate request carries instanceId and writerType (Approved Interpretation 2): yes
  - standalone destinations validate against the T05 StandaloneRouteId registry: yes
  - runtime error payload code validates against the T03 ErrorCode registry: yes
  - envelope field names/types match DESIGN.md Section 5; one closed discriminated union;
    unknown message type fails closed: yes
  - one canonical payload registry; no second message-type list: yes
  - no invented identifiers; no dependency change; no side effects; no IndexedDB/network: yes
  - only the five source files, three test files, and evidence/STATUS changed: yes

Code-quality and security checklist:
  - RuntimeMessageSchema and RuntimeEnvelopeSchema are closed discriminated unions: yes
  - Zod strips unknown extra keys rather than accepting them; handlers must still validate
    the sender at the trust boundary (T08): yes
  - no catch blocks; no empty catch; no global mutable state: yes
  - no raw sensitive content, secrets, tokens, or paths logged/persisted/committed: yes
  - pure schema/registry modules; no network, storage, IndexedDB, or filesystem access: yes
  - page/message content is data only; this task grants instruction authority to none: yes

Type-level deviations: none — both production and test code typecheck under the pinned
`strict`/`noUncheckedIndexedAccess` configuration without non-null assertions.

Completion criteria: all three runtime test files green; registry completeness test green;
typecheck and lint pass; phase-applicable chain exit 0; one atomic task commit exists;
working tree clean; status recorded.

=== Task 08 — Sender and Envelope Boundary Validation (2026-09-19T10:42:13Z) ===

task ID: T08
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: code task; TDD RED->GREEN applied
implementation tier: advanced (trust boundary)
base commit: af65f4f2adda54bf2d6f08d2043de5edbec5bfbc (T07 atomic commit
  `feat(phase-01): add runtime envelope primitives and registries`)
Files created (production): none
Files created (tests):
  tests/core/runtime/boundaryValidation.test.ts
Files modified (production):
  src/core/runtime/RuntimeEnvelope.ts (validation exports appended)
Files modified: none (toolchain); no dependency change

Step 1 — test file created verbatim from the brief (11 tests): valid trusted envelope;
RUNTIME_ENVELOPE_INVALID on schema failure; RUNTIME_SENDER_REJECTED on foreign sender,
missing sender id, and disallowed source; background allowed for standalone.focus;
allowed-source registry completeness (exactly one explicit non-empty list per MessageType,
Object.keys(MESSAGE_TYPE_ALLOWED_SOURCES) equals MESSAGE_TYPES); no wildcard/permissive
default; every MessageType accepted from each allowed source; a representative disallowed
source rejected for every type that has one.

Step 2 — RED:
  command: pnpm run test -- tests/core/runtime/boundaryValidation.test.ts
  exit status: 1
  output: Test Files 1 failed | 10 passed (11); Tests 11 failed | 48 passed (59)
  observed: TypeError: validateInboundEnvelope is not a function; getAllowedSources and
    isTrustedExtensionSender also not exported.
  Expected failure reason confirmed: the T08 validation exports did not yet exist on
    RuntimeEnvelope.ts. No canonical identifier, message type, or payload name was missing.

Step 3 — src/core/runtime/RuntimeEnvelope.ts append verbatim per the brief:
  getAllowedSources(type) returns MESSAGE_TYPE_ALLOWED_SOURCES[type] ?? [] (fail closed to
    an empty list for an unregistered type); isSourceAllowed(type, source) membership test;
  SenderIdentity { id?, url? }; isTrustedExtensionSender(sender, extensionId) requires an
    exact extension id match and, when a url is present, a chrome-extension://<id>/ prefix;
  InboundValidationResult union; validateInboundEnvelope(input, sender, extensionId) parses
    the envelope first (RUNTIME_ENVELOPE_INVALID on schema failure, including an unknown type
    string), then validates the sender (RUNTIME_SENDER_REJECTED), then checks the source
    against the closed registry (RUNTIME_SENDER_REJECTED for a disallowed source or an
    unregistered type). Failure paths log the canonical error code only
    ({ reason: 'schema' | 'identity' | 'source' }); no raw payload or sender value is logged.
    Validation never throws to callers.

Step 4 — GREEN (focused, explicit path):
  command: pnpm exec vitest run --project unit tests/core/runtime/boundaryValidation.test.ts
  exit status: 0
  output: Test Files 1 passed (1); Tests 11 passed (11), 0 failed, 0 skipped (T08-only).

Focused verification (sequential):
  pnpm run typecheck                             => exit 0  (tsc --noEmit clean)
  pnpm run lint                                  => exit 0  (eslint . clean)
  pnpm exec prettier --check .                   => exit 0 after formatting only the T08 test
    file (tests/core/runtime/boundaryValidation.test.ts, line wrapping at printWidth 100).
    Formatting changed whitespace/line wrapping only; all identifiers, message-type strings,
    payloads, and assertions are unchanged. RuntimeEnvelope.ts was not reformatted and no
    other file was reformatted.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 11 files, 59 tests passed.

Test counts:
  T08 test file: 1 passed, 11 tests passed, 0 failed, 0 skipped.
  Unit project total after T08: 11 files, 59 passed.

Specification-compliance checklist (verbatim from the brief):
  - interfaces exact: SenderIdentity; isTrustedExtensionSender; getAllowedSources(type):
    readonly RuntimeSurface[]; isSourceAllowed(type, source): boolean; InboundValidationResult;
    validateInboundEnvelope: yes
  - sender validation runs before any use of sender data; only the extension's own origin is
    trusted; URL scheme is validated; no raw sender/payload is logged: yes
  - Approved Interpretation 1: every MessageType and all its allowed sources covered, plus a
    representative disallowed source for each type that has one, plus a completeness test
    asserting exactly one MESSAGE_TYPE_ALLOWED_SOURCES entry per MessageType with no
    wildcard/permissive default; Object.keys(MESSAGE_TYPE_ALLOWED_SOURCES) equals MESSAGE_TYPES:
    yes
  - invalid envelope (including unknown type string) -> RUNTIME_ENVELOPE_INVALID; unregistered
    type or disallowed source -> RUNTIME_SENDER_REJECTED; never silent success: yes
  - only RuntimeEnvelope.ts appended and the one new test file created (plus .planning
    evidence/STATUS); no dependency change: yes

Code-quality and security checklist:
  - sender identity checked before use; foreign extension ids rejected; URL scheme validated:
    yes
  - fail closed on every failure path; no wildcard or permissive default; no arbitrary error
    strings (only the canonical RUNTIME_ENVELOPE_INVALID / RUNTIME_SENDER_REJECTED): yes
  - no catch blocks; no empty catch; no global mutable state; validation never throws: yes
  - error records carry only a fixed reason label; no raw sensitive content, secrets, tokens,
    sender values, or payloads are logged/persisted/committed: yes
  - pure validation module; no network, storage, IndexedDB, or filesystem access: yes
  - source-map observation: the appended block imports createErrorRecord/debugLog (T03) and
    MESSAGE_TYPE_ALLOWED_SOURCES/MessageType (T07); debugLog redacts context and emits a JSON
    string through the supplied sink (default console). No payload body or sender data reaches
    the log; the reason label is the only context field: yes

Type-level deviations: none — production and test code typecheck under the pinned
`strict`/`noUncheckedIndexedAccess` configuration without non-null assertions.

Completion criteria: boundary test green; typecheck and lint pass; phase-applicable chain
exit 0; one atomic task commit exists; working tree clean; status recorded.

================================================================================
Task 09 — Canonical Broadcast Bus
================================================================================

Phase: 01-runtime-shells-workspace
Task: T09 (implementation tier: balanced)
Branch: phoenix
Repository root: /Users/george.li/Documents/workspaces/nowpilot
Files created: src/core/runtime/BroadcastBus.ts, tests/core/runtime/broadcastBus.test.ts
Files modified: none (plus .planning evidence/STATUS)
Atomic commit: feat(phase-01): add canonical broadcast bus (SHA captured post-commit
  in .superpowers/sdd/PLAN/task-09-report.md)
Date: 2026-09-19

Step 1-2 — RED (focused, brief command):
  command: pnpm run test -- tests/core/runtime/broadcastBus.test.ts
  exit status: 1
  output: Error: Failed to resolve import "@/core/runtime/BroadcastBus" from
    "tests/core/runtime/broadcastBus.test.ts". Does the file exist?
    Test Files 1 failed | 11 passed (12); Tests 59 passed (59).
  The intended module-resolution failure was confirmed and is caused by the
  not-yet-created module, not by broken setup or an unrelated defect.

Step 3 — GREEN (explicit focused path):
  command: pnpm exec vitest run --project unit tests/core/runtime/broadcastBus.test.ts
  exit status: 0
  output: Test Files 1 passed (1); Tests 5 passed (5), 0 failed, 0 skipped.
    - sends envelopes through the injected transport
    - delivers only envelopes of the subscribed type
    - ignores messages from an untrusted sender
    - ignores malformed incoming messages without throwing
    - stops delivering after unsubscribe

Focused verification (sequential):
  pnpm run test -- tests/core/runtime/broadcastBus.test.ts  => exit 0
    (unit project: 12 files, 64 tests passed; the explicit-path run above isolates
     the T09 file at 1 file, 5 tests)
  pnpm run typecheck                                        => exit 0 (tsc --noEmit clean)
  pnpm run lint                                             => exit 0 (eslint . clean)
  pnpm exec prettier --check .                              => exit 0 after formatting only
    the two T09 files (src/core/runtime/BroadcastBus.ts and
    tests/core/runtime/broadcastBus.test.ts, line wrapping at printWidth 100).
    Formatting changed whitespace/line wrapping only; all identifiers, message-type
    strings, payloads, and assertions are unchanged. No other file was reformatted.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 12 files, 64 tests passed.

Type-level deviation (one; same class as T03 B5):
  The brief's test line `handler.mock.calls[0][0].type` is TS2532 ("Object is possibly
  'undefined'") under the T02-pinned `noUncheckedIndexedAccess: true`, so
  `handler.mock.calls[0]![0].type` was used. Values and assertions are unchanged and
  tsconfig.json was not modified. The production module required no assertion change
  beyond the two `!` already present in the approved brief (`listeners.get(raw)!` and
  `raw!`); the dispatch cast `sender as SenderIdentity` is verbatim from the brief.

Test counts:
  T09 test file: 1 passed, 5 tests passed, 0 failed, 0 skipped.
  Unit project total after T09: 12 files, 64 passed.

Specification-compliance checklist (verbatim from the brief):
  - all outgoing messages are RuntimeEnvelope; `send` forwards to the injected
    `sendMessage` transport: yes
  - malformed incoming messages fail closed and log RUNTIME_ENVELOPE_INVALID: yes —
    `dispatch` calls `validateInboundEnvelope(message, sender, deps.extensionId)` and
    returns before iterating handlers when the result is not ok; T08 logs the canonical
    error code. No handler ever receives an unvalidated envelope and there is no silent
    success.
  - no invented message types; `on` is keyed by the canonical MessageType: yes

Code-quality and security checklist:
  - no raw message or sender logged; only T08 canonical error codes with fixed reason
    labels reach the log: yes
  - listener cleanup on unsubscribe; the raw transport listener is removed when its last
    handler is removed: yes
  - no duplicate raw listeners per type: yes (one raw listener per distinct subscribed
    MessageType; additional handlers for the same type reuse the existing raw listener)
  - no direct chrome.* reference; no content script; no direct chrome.tabs; no
    IndexedDB/network/filesystem access; no dependency change: yes

Completion criteria: bus test green; typecheck and lint pass; phase-applicable chain
exit 0; one atomic task commit exists; working tree clean; status recorded.

--------------------------------------------------------------------------------
Task 09 FIX — single transport listener (controller review, Critical)
--------------------------------------------------------------------------------

Date: 2026-09-19
Branch: phoenix
Repository root: /Users/george.li/Documents/workspaces/nowpilot
Files changed: src/core/runtime/BroadcastBus.ts, tests/core/runtime/broadcastBus.test.ts
Corrective commit: fix(phase-01): deliver each broadcast message once per subscriber
  (SHA captured post-commit in .superpowers/sdd/PLAN/task-09-report.md)

Defect: the original `on` registered one raw transport listener per distinct
subscribed MessageType, while each raw listener's `dispatch` fanned out over all
listener maps. With N distinct types subscribed, one inbound message was dispatched
N times, each matching handler fired N times, and invalid messages were validated/logged
N times (affects T16, 5 subscriptions, and T22).

Fix (exact corrected implementation from controller review): a SINGLE raw transport
listener created lazily by `ensureListener` and one `handlersByType` map keyed by
MessageType. `on` adds the handler to its type set; unsubscribe removes the handler,
deletes an empty type entry, and removes the raw listener (setting it undefined) only
when no type has any handler. Public interface unchanged.

Step 1 — RED (regression test, against the previous fan-out implementation):
  command: pnpm exec vitest run --project unit tests/core/runtime/broadcastBus.test.ts
  exit status: 1
  output: "delivers each inbound message exactly once across multiple subscribed types"
    FAILED — AssertionError: expected 2 to be 1 (listeners.size), i.e. two raw
    transport listeners were registered for two distinct types. 1 failed | 5 passed.

Step 2 — GREEN (corrected implementation):
  command: pnpm exec vitest run --project unit tests/core/runtime/broadcastBus.test.ts
  exit status: 0
  output: Test Files 1 passed (1); Tests 6 passed (6), 0 failed, 0 skipped.

Regression test (added to tests/core/runtime/broadcastBus.test.ts):
  - subscribes two distinct types (standalone.open, standalone.focus) that are both
    valid from a trusted sender to one bus;
  - asserts listeners.size === 1 after both subscriptions (pins the single-listener
    design);
  - iterates the `listeners` set as the existing tests do, delivering each message to
    every registered listener;
  - asserts openHandler fired exactly once with type standalone.open, focusHandler
    fired exactly once with type standalone.focus, and neither fired for the other type.
  It fails against the previous fan-out implementation and passes with the fix.

Focused verification (sequential):
  pnpm exec vitest run --project unit tests/core/runtime/broadcastBus.test.ts => exit 0
    (1 file, 6 tests)
  pnpm run test -- tests/core/runtime/broadcastBus.test.ts  => exit 0
    (unit project: 12 files, 65 tests)
  pnpm run typecheck                                        => exit 0 (tsc --noEmit clean)
  pnpm run lint                                             => exit 0 (eslint . clean)
  pnpm exec prettier --check .                              => exit 0 (all files formatted;
    the corrected implementation was already Prettier-clean)

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 12 files, 65 tests passed.

Specification-compliance checklist (fix):
  - public interface unchanged (RawMessageListener, BroadcastBusDependencies,
    EnvelopeHandler, BroadcastBus, createBroadcastBus): yes
  - inbound messages still pass through the T08 validateInboundEnvelope boundary with
    the injected extensionId and raw sender before any handler; rejected messages are
    never dispatched: yes (now validated exactly once per inbound message)
  - exactly one raw transport listener regardless of subscribed type count: yes

Code-quality and security checklist (fix):
  - each matching handler fires exactly once per inbound message: yes
  - invalid messages are validated/logged exactly once per inbound message: yes
  - raw listener removed when the last handler across all types is removed: yes
  - no raw message/sender logged; no direct chrome.*; no dependency change: yes

Completion criteria: regression test green; typecheck and lint pass; phase-applicable
chain exit 0; corrective commit exists; working tree clean; status recorded.

--------------------------------------------------------------------------------
Task 10 — Standalone Navigation Request Contract
--------------------------------------------------------------------------------

Date: 2026-09-19
Branch: phoenix
Repository root: /Users/george.li/Documents/workspaces/nowpilot
Files created: src/core/runtime/StandaloneNavigation.ts,
  tests/core/runtime/standaloneNavigation.test.ts
Files modified: none
Task commit: feat(phase-01): add standalone navigation request contract
  (SHA captured post-commit in .superpowers/sdd/PLAN/task-10-report.md)

Step 1 — RED:
  command: pnpm run test -- tests/core/runtime/standaloneNavigation.test.ts
  exit status: 1
  output: Error: Failed to resolve import "@/core/runtime/StandaloneNavigation"
    from "tests/core/runtime/standaloneNavigation.test.ts". Does the file exist?
    (1 failed | 12 passed; the 65 pre-existing tests still passed.)

Step 2 — GREEN:
  command: pnpm run test -- tests/core/runtime/standaloneNavigation.test.ts
  exit status: 0
  output: Test Files 13 passed (13); Tests 71 passed (71), 0 failed, 0 skipped.

Focused verification (sequential):
  pnpm run test -- tests/core/runtime/standaloneNavigation.test.ts => exit 0
    (unit project: 13 files, 71 tests; 6 T10 tests)
  pnpm run typecheck                                          => exit 0 (tsc --noEmit clean)
  pnpm run lint                                               => exit 0 (eslint . clean)
  pnpm exec prettier --check .                                => exit 0 (all files formatted;
    only the two T10 files were reformatted, identifiers/values/assertions unchanged)

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 13 files, 71 tests passed.

Specification-compliance checklist:
  - uses only canonical route IDs (StandaloneRouteId from the T05 registry; no raw
    routing strings built outside the registry): yes
  - uses only canonical message types standalone.open / standalone.focus and the T09
    canonical BroadcastBus; no invented message types: yes
  - destination typed and schema-validated via StandaloneRouteIdSchema.parse before
    envelope creation: yes
  - openStandalone targets background; focusStandalone targets the live standalone: yes
  - no chrome.tabs call and no direct tab API in this module: yes
  - no handoff logic (T16 owns coordination; T21 composes handoff + open): yes
  - no dependency change; no IndexedDB/network; no content script: yes

Type-only adaptation (Low, same class as T03 B5 / T09): the brief's verbatim test
  `bus.send.mock.calls[0][0].type` is TS2532/TS2493 under the pinned
  `noUncheckedIndexedAccess: true` (and the untyped `vi.fn(async () => {})` infers an
  empty args tuple). The mock parameter is typed `_envelope: RuntimeEnvelope` and the
  index uses `bus.send.mock.calls[0]![0].type`; assertions and values are unchanged and
  tsconfig.json was not modified.

Code-quality and security checklist:
  - destination validated before envelope creation: yes
  - no invented destinations; closed StandaloneRouteId union: yes
  - no direct tab API; no raw message/sender logged: yes

Completion criteria: navigation test green; typecheck and lint pass; phase-applicable
chain exit 0; one atomic task commit exists; working tree clean; status recorded.

================================================================================
Task 11 — Singleton Standalone Tab Controller (Create, Focus, Stale-ID Recovery, Close)
================================================================================

Date: 2026-09-19T11:02:07Z
Task ID: T11
Branch: phoenix
Repository root: /Users/george.li/Documents/workspaces/nowpilot
Classification: code task; TDD RED->GREEN applied
Implementation tier: advanced (tab identity and recovery)
Base commit: 9578e5a613bf38b93ca6b9c02c4eea1ac349314f (T10 atomic commit
  `feat(phase-01): add standalone navigation request contract`)
Files modified: src/core/runtime/StandaloneNavigation.ts (controller appended)
Files created: tests/core/runtime/standaloneTabController.test.ts
Atomic commit: feat(phase-01): add singleton standalone tab controller
  (SHA captured post-commit in .superpowers/sdd/PLAN/task-11-report.md; a commit
   cannot embed its own SHA, so the subject is recorded here and the exact SHA is
   recorded in the task report and the current-state STATUS entry.)

Step 1 — test file created verbatim from the brief at
tests/core/runtime/standaloneTabController.test.ts (7 tests).

Step 2 — RED (focused, brief command):
  command: pnpm run test -- tests/core/runtime/standaloneTabController.test.ts
  exit status: 1
  output: |unit| tests/core/runtime/standaloneTabController.test.ts (7 tests | 7 failed)
    TypeError: createStandaloneTabController is not a function
    Test Files 1 failed | 13 passed (14); Tests 7 failed | 71 passed (78).
  Expected failure reason confirmed: the T11 controller export did not yet exist on
  StandaloneNavigation.ts, so every `setup()` call reached an undefined
  `createStandaloneTabController`. The 71 pre-existing tests still passed. No canonical
  identifier, storage key, error code, or message type was missing.

Step 3 — src/core/runtime/StandaloneNavigation.ts appended verbatim per the brief:
  Four imports added to the existing top import block (no self-import, no duplicate
  StandaloneNavigation import): createErrorRecord/debugLog (T03), ValidatedStorage,
  StorageKey, and StandaloneTabRecordSchema/StandaloneTabRecord (T06).
  Interfaces/types exactly as specified: StandaloneTabApi (get/create/update/focusWindow),
  StandaloneTabControllerDependencies (tabs/storage/buildStandaloneUrl/sendFocus/now),
  StandaloneOpenResult (created|focused with tabId, or failed with
  STANDALONE_OPEN_FAILED|STANDALONE_TAB_INVALID), StandaloneTabController
  (open/handleTabRemoved/readRecord), and createStandaloneTabController(deps).
  STANDALONE_TAB_KEY = 'np_standalone_tab' (T04 session-area key).
  open(): reads the stored record; when present validates the stored tab ID with
  `tabs.get` (non-sensitive: compares the returned `id` only, no URL/title read); if live,
  `tabs.update(tabId,{active:true})` + `tabs.focusWindow(tabId)` + a schema-valid
  `standalone.focus` envelope via `sendFocus` and returns focused; if the tab is gone
  (rejected `get`) or has no id, it logs STANDALONE_TAB_INVALID (reason stale), removes the
  stale record, and creates the Standalone entrypoint at
  `buildStandaloneUrl(destination)`. create failure or a missing returned id fails closed
  with STANDALONE_OPEN_FAILED (reasons create / missing-id); a created id is persisted as
  { tabId, openedAt: now() }.
  handleTabRemoved(tabId): reads the record and returns false for an unrelated tab or no
  record; when the stored id matches it removes the record and returns true (authoritative
  close recovery via stored-ID validation; `standalone.closed` is not used).
  No direct chrome.* reference, no chrome.tabs.query, no URL/title/favicon/content read,
  no IndexedDB/network, no dependency change.

Step 4 — GREEN (focused, explicit path):
  command: pnpm exec vitest run --project unit tests/core/runtime/standaloneTabController.test.ts
  exit status: 0
  output: Test Files 1 passed (1); Tests 7 passed (7), 0 failed, 0 skipped (T11-only).

Focused verification (sequential):
  pnpm run test -- tests/core/runtime/standaloneTabController.test.ts => exit 0
    (unit project: 14 files, 78 tests passed)
  pnpm run typecheck                                  => exit 0 (tsc --noEmit clean)
  pnpm run lint                                       => exit 0 (eslint . clean)
  pnpm exec prettier --check .                        => exit 0 after formatting only the
    T11 test file (line wrapping at printWidth 100). Formatting changed whitespace/line
    wrapping only; identifiers, values, and assertions are unchanged. StandaloneNavigation.ts
    was already clean; no other file was reformatted.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 14 files, 78 tests passed.

Test counts:
  T11 test file: 1 passed, 7 tests passed, 0 failed, 0 skipped.
  Unit project total after T11: 14 files, 78 passed.

Type-only adaptations (Low; same class as T03 B5 / T09 / T10; no behaviour change):
  (1) The brief's `sendFocus = vi.fn(async () => undefined)` infers an empty args tuple, so
      `sendFocus.mock.calls[0][0]` is TS2532/TS2493 under the pinned
      `noUncheckedIndexedAccess: true`. The mock parameter is typed
      `_envelope: RuntimeEnvelope` (new type-only import) and the two indices use
      `sendFocus.mock.calls[0]![0]`. Assertions and values are unchanged.
  (2) The brief's `tabs.create = vi.fn(async () => ({ id: 42 }))` infers a return type
      `{ id: number }`, so the fail-closed case `tabs.create.mockResolvedValueOnce({})` is
      TS2345. The mock is annotated `async (): Promise<{ id?: number }> => ({ id: 42 })`,
      matching the production StandaloneTabApi.create signature; the test still supplies
      `{}` with no id. Values and assertions are unchanged.
  tsconfig.json was not modified in either case.

Specification-compliance checklist (verbatim from the brief):
  - interfaces exact: StandaloneTabApi; StandaloneTabControllerDependencies;
    StandaloneOpenResult; createStandaloneTabController: yes
  - singleton identity is the stored tab ID in session storage np_standalone_tab: yes
  - open validates via get (NO URL/title read), focuses via update + focusWindow, clears
    the stale id, and creates the entrypoint at the requested canonical hash route: yes
  - background is the only caller of chrome.tabs; the controller uses the injected
    StandaloneTabApi and contains no direct chrome.*: yes
  - no query-by-URL; no active-page URL/title/favicon/content reads: yes
  - authoritative close recovery uses the stored tab-ID validation (onRemoved); the
    non-authoritative standalone.closed message is not used here: yes
  - destination is the canonical typed StandaloneRouteId; no invented identifiers or
    message types: yes
  - preserve committed version / never authorise two writers / stale-writer recovery:
    out of scope for the tab-identity controller (the T12+ workspace coordinator owns
    writer authorisation); stale-identity recovery is tested: yes
  - no dependency change; no IndexedDB; no network: yes
  - only StandaloneNavigation.ts and the one new test file changed (plus .planning
    evidence/STATUS): yes

Code-quality and security checklist:
  - create failure and missing id both fail closed with the canonical
    STANDALONE_OPEN_FAILED code; no silent success: yes
  - stale recovery is deterministic (rejected/absent id -> remove then create): yes
  - no duplicate tab: a live stored tab is focused and never re-created: yes
  - malformed stored record is treated as absent and logged only as
    STANDALONE_TAB_INVALID with a fixed reason label: yes
  - no URL/title/page content is read, logged, or persisted; only the extension-internal
    built URL string is passed to create: yes
  - both catch blocks set a fail-closed outcome and log a canonical code; no empty catch:
    yes
  - no raw sensitive content, secrets, tokens, or sender values logged/persisted: yes

Completion criteria: controller test green; typecheck and lint pass; phase-applicable
chain exit 0; one atomic task commit exists; working tree clean; status recorded.

================================================================================
Task 12 — Workspace Metadata Store and Durable Version
================================================================================

Date: 2026-09-19T11:05:57Z
Task ID: T12
Branch: phoenix
Repository root: /Users/george.li/Documents/workspaces/nowpilot
Classification: code task; TDD RED->GREEN applied
Implementation tier: balanced
Base commit: 29595fc (T11 atomic commit
  `feat(phase-01): add singleton standalone tab controller`)
Files created: src/core/workspace/WorkspaceStore.ts,
  tests/core/workspace/workspaceStore.test.ts
Files modified: none
Atomic commit: feat(phase-01): persist workspace metadata and version
  (SHA captured post-commit in .superpowers/sdd/PLAN/task-12-report.md; a commit
   cannot embed its own SHA, so the subject is recorded here and the exact SHA is
   recorded in the task report and the current-state STATUS entry.)

Step 1 — test file created verbatim from the brief at
tests/core/workspace/workspaceStore.test.ts (4 tests).

Step 2 — RED (focused, brief command):
  command: pnpm run test -- tests/core/workspace/workspaceStore.test.ts
  exit status: 1
  output: Error: Failed to resolve import "@/core/workspace/WorkspaceStore" from
    "tests/core/workspace/workspaceStore.test.ts". Does the file exist?
    Test Files 1 failed | 14 passed (15); Tests 78 passed (78).
  Expected failure reason confirmed: the T12 store module did not yet exist, so the
  test file failed at Vite import analysis for `@/core/workspace/WorkspaceStore`. The
  78 pre-existing unit tests still passed. No canonical key, schema, error code, or
  adapter export was missing.

Step 3 — src/core/workspace/WorkspaceStore.ts created verbatim per the brief:
  Imports the T03 createErrorRecord/debugLog, the T04 ValidatedStorage type, and the
  T06 WorkspaceMetadataSchema/WorkspaceVersionRecordSchema/WorkspaceMetadata.
  Interfaces/types exactly as specified: WorkspaceMetadataReadResult
  (missing | valid+metadata | invalid), WorkspaceStore
  (readMetadata/writeMetadata/readVersion/writeVersion), and
  createWorkspaceStore(storage: ValidatedStorage): WorkspaceStore.
  readMetadata(): validated read of `np_workspace_meta`; valid -> {status:'valid',
  metadata}; invalid -> logs WORKSPACE_INVALID_METADATA with key np_workspace_meta and
  returns {status:'invalid'}; absent -> {status:'missing'}; never throws.
  writeMetadata(metadata): validated write of `np_workspace_meta` then a validated
  `np_workspace_version` record ({committedVersion, updatedAt}) derived from the
  metadata; metadata and committed version are the only durable values.
  readVersion(): validated read of `np_workspace_version`; valid -> committedVersion;
  invalid -> logs WORKSPACE_INVALID_METADATA with key np_workspace_version and returns
  0; absent -> 0; never throws.
  writeVersion(committedVersion, updatedAt): validated write of
  `np_workspace_version`.
  Both keys are T04 `local` area keys (DESIGN.md Section 9); no direct chrome.*, no
  session values treated as durable, no IndexedDB/network, no new dependency.

Step 4 — GREEN (focused, explicit path):
  command: pnpm exec vitest run --project unit tests/core/workspace/workspaceStore.test.ts
  exit status: 0
  output: Test Files 1 passed (1); Tests 4 passed (4), 0 failed, 0 skipped (T12-only).

Focused verification (sequential):
  pnpm run test -- tests/core/workspace/workspaceStore.test.ts => exit 0
    (unit project: 15 files, 82 tests passed)
  pnpm run typecheck                                  => exit 0 (tsc --noEmit clean)
  pnpm run lint                                       => exit 0 (eslint . clean)
  pnpm exec prettier --check .                        => exit 0 after formatting only
    src/core/workspace/WorkspaceStore.ts (the WorkspaceMetadataReadResult union was
    reflowed onto one line at printWidth 100). Formatting changed whitespace only; the
    type members, identifiers, and behaviour are unchanged. The T12 test file was
    already clean; no other file was reformatted.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 15 files, 82 tests passed.

Test counts:
  T12 test file: 1 passed, 4 tests passed, 0 failed, 0 skipped.
  Unit project total after T12: 15 files, 82 passed.

Specification-compliance checklist (verbatim from the brief):
  - interfaces exact: WorkspaceStore; createWorkspaceStore; WorkspaceMetadataReadResult:
    yes
  - only np_workspace_meta and np_workspace_version written, both in
    chrome.storage.local (T04 mapping): yes
  - durable = metadata + committed version only; session values never treated as
    durable workspace state: yes
  - fail closed: invalid/absent stored metadata returns a canonical missing/invalid
    result; never throws; never silent success: yes
  - invalid metadata logs WORKSPACE_INVALID_METADATA (canonical T03 code): yes
  - no IndexedDB/network/new dependency; no provider/MCP; no direct chrome.* (injected
    T04 validated storage only): yes
  - only WorkspaceStore.ts and the one new test file changed (plus .planning
    evidence/STATUS): yes

Code-quality and security checklist:
  - reads never throw on malformed data (invalid -> canonical result, absent ->
    missing/0): yes
  - no raw stored value or payload is logged; only the canonical error code and a fixed
    key label, passed through the T03 redaction sink: yes
  - writes are schema-validated by the T04 adapter before persistence: yes
  - both catches (none required) absent; no empty catch; no swallowed rejection: yes
  - no secrets, tokens, passwords, or page content read/logged/persisted: yes

Completion criteria: store test green; typecheck and lint pass; phase-applicable
chain exit 0; one atomic task commit exists; working tree clean; status recorded.

================================================================================
Task 13 — Writer Election
================================================================================

Date: 2026-09-19T11:10:19Z
Task ID: T13
Branch: phoenix
Repository root: /Users/george.li/Documents/workspaces/nowpilot
Classification: code task; TDD RED->GREEN applied
Implementation tier: advanced (ownership correctness)
Base commit: ba13aef (T12 atomic commit
  `feat(phase-01): persist workspace metadata and version`)
Files created: src/core/workspace/WorkspaceElection.ts,
  tests/core/workspace/workspaceElection.test.ts
Files modified: none
Atomic commit: feat(phase-01): elect a single workspace writer
  (SHA captured post-commit in .superpowers/sdd/PLAN/task-13-report.md; a commit
   cannot embed its own SHA, so the subject is recorded here and the exact SHA is
   recorded in the task report and the current-state STATUS entry.)

Step 1 — test file created verbatim from the brief at
tests/core/workspace/workspaceElection.test.ts (7 tests).

Step 2 — RED (focused, brief command):
  command: pnpm run test -- tests/core/workspace/workspaceElection.test.ts
  exit status: 1
  output: Error: Failed to resolve import "@/core/workspace/WorkspaceElection" from
    "tests/core/workspace/workspaceElection.test.ts". Does the file exist?
    File: .../tests/core/workspace/workspaceElection.test.ts:5:40
    Test Files  1 failed | 15 passed (16); Tests  82 passed (82).
  Expected failure reason confirmed: the T13 election module did not yet exist, so
  the test file failed at Vite import analysis for `@/core/workspace/WorkspaceElection`.
  The 82 pre-existing unit tests still passed. No canonical key, schema, error code,
  interface, or adapter export was missing.

Step 3 — src/core/workspace/WorkspaceElection.ts created verbatim per the brief:
  Imports the T03 createErrorRecord/debugLog, the T04 ValidatedStorage type, the T12
  WorkspaceStore type, and the T06 ElectionRecordSchema/ElectionRecord/InstanceId/
  WorkspaceWriterType.
  Interfaces/types exactly as specified: WorkspaceElectionDependencies
  (storage/store/writerType/instanceId/now), ElectionReadResult
  (missing | valid+record | invalid), ElectionClaimResult
  (acquired+record+recovered | held+record), WorkspaceElection
  (read/claim/relinquish/isWriter), and
  createWorkspaceElection(deps: WorkspaceElectionDependencies): WorkspaceElection.
  read(): validated read of `np_workspace_election` (T04 session area); valid ->
  {status:'valid', record}; invalid -> logs WORKSPACE_INVALID_METADATA with key
  np_workspace_election and returns {status:'invalid'}; absent -> {status:'missing'};
  never throws.
  isWriter(record): true only when record is defined AND
  writerInstanceId === deps.instanceId AND writerType === deps.writerType — identity
  is never inferred from surface type alone.
  claim(): reads first. If a valid record exists it is ALWAYS returned as
  {status:'held'} and is never overwritten, so a valid existing writer is never
  displaced and a repeated same-instance claim is idempotently held. Only when the
  record is missing or invalid does it read the committed version via
  deps.store.readVersion(), build a fresh idle epoch-0 record carrying that committed
  version and deps.now(), write it through the validated adapter, and return
  {status:'acquired', record, recovered} where recovered is true iff the prior record
  was invalid (stale/invalid recovery). There is no silent writer pick: acquisition
  always writes and returns an explicit record.
  relinquish(): removes `np_workspace_election` then `np_workspace_handoff` through
  the validated adapter.
  Both keys are T04 `session`-area keys (DESIGN.md Section 9); no direct chrome.*, no
  IndexedDB/network, no background broker, no new dependency.

Step 4 — GREEN (focused, explicit path):
  command: pnpm exec vitest run --project unit tests/core/workspace/workspaceElection.test.ts
  exit status: 0
  output: Test Files 1 passed (1); Tests 7 passed (7), 0 failed, 0 skipped (T13-only).
    - acquires the writer role when no valid writer exists
    - preserves the committed version when acquiring
    - does not displace a valid writer held by another instance
    - treats a repeated claim by the same instance as held
    - recovers from invalid election metadata by claiming a fresh epoch
    - relinquishes both election and handoff records
    - reports whether a record belongs to this instance

Focused verification (sequential):
  pnpm run test -- tests/core/workspace/workspaceElection.test.ts => exit 0
    (unit project: 16 files, 89 tests passed)
  pnpm run typecheck                                  => exit 0 (tsc --noEmit clean)
  pnpm run lint                                       => exit 0 (eslint . clean)
  pnpm exec prettier --check .                        => exit 0 after formatting only
    the two T13 files (src/core/workspace/WorkspaceElection.ts and
    tests/core/workspace/workspaceElection.test.ts). Formatting changed whitespace/
    line wrapping only; the type members, interface names, literals, and assertions
    are unchanged. No other file was reformatted.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 16 files, 89 tests passed.

Test counts:
  T13 test file: 1 passed, 7 tests passed, 0 failed, 0 skipped.
  Unit project total after T13: 16 files, 89 passed.

Specification-compliance checklist (verbatim from the brief):
  - interfaces exact: WorkspaceElectionDependencies; ElectionReadResult;
    ElectionClaimResult; WorkspaceElection; createWorkspaceElection: yes
  - single live writer: a valid existing record is always returned as held and is
    never overwritten by any code path: yes
  - a first eligible surface may claim only when no valid writer exists (the record is
    missing or invalid): yes
  - writer identity is never inferred from surface type alone: isWriter requires BOTH
    writerInstanceId === deps.instanceId AND writerType === deps.writerType: yes
  - session storage only for election (`np_workspace_election`, T04 session mapping);
    handoff record (`np_workspace_handoff`) relinquished from the same session area: yes
  - committed version preserved on claim via deps.store.readVersion(): yes
  - invalid election metadata fails closed and logs the canonical T03
    WORKSPACE_INVALID_METADATA code (fixed key label); no silent writer pick: yes
  - no handoff transitions (T14), no mutations (T15), no background broker, owner is
    always a UI surface (sidepanel|standalone): yes
  - no IndexedDB/network/new dependency; no direct chrome.* (injected T04 validated
    storage only): yes
  - only WorkspaceElection.ts and the one new test file changed (plus .planning
    evidence/STATUS): yes

Code-quality and security checklist:
  - reads never throw on malformed data (invalid -> canonical result, absent ->
    missing): yes
  - `claim` returns before writing whenever a valid record exists, so no concurrent or
    repeated path can authorise a second writer: yes
  - no raw stored value, instance id, or payload is logged; only the canonical error
    code and a fixed key label, passed through the T03 redaction sink: yes
  - writes are schema-validated by the T04 adapter before persistence: yes
  - no catch blocks; no empty catch; no swallowed rejection: yes
  - no secrets, tokens, passwords, or page content read/logged/persisted: yes

Limitations / observations (non-blocking, no deviation from the approved brief):
  - The `isWriter` implementation is used verbatim as supplied by the brief
    (`record?.writerInstanceId === deps.instanceId && record.writerType === deps.writerType`).
    It typechecks under the pinned strict TypeScript with no error at that line; the
    STOP/ BLOCKED condition did not trigger.
  - `claim` contains an explicit `if (this.isWriter(current.record))` ownership check
    whose both branches return the same `held` result; this is the brief-prescribed,
    intentional documentation of the ownership check and was kept verbatim. It is not
    a defect: no second write path exists.
  - Epoch is initialised to 0 for a fresh acquisition exactly as the brief specifies;
    epoch advance/transition semantics are owned by later tasks (T14/T16).

Completion criteria: election test green; typecheck and lint pass; phase-applicable
chain exit 0; one atomic task commit exists; working tree clean; status recorded.

=== Task 13 — controller review outcome / execution stop (2026-09-19) ===

Task: T13 (Writer Election)
Commit under review: 7633615 (feat(phase-01): elect a single workspace writer)
Automated verification: PASS (focused 7/7; typecheck, lint, prettier, phase chain exit 0)
Controller task review: FAIL — 1 Critical, 2 Important

Critical: non-atomic claim() read-then-write can authorise two live writers
(WorkspaceElection.ts:61-71); meets the task STOP condition and conflicts with
DESIGN.md Section 6.

Result: Phase 01 execution STOPPED at T13 (BLOCKED) pending an operator decision.
T14–T28 not started. No production file changed during the stop; this section and the
matching STATUS.md/review.md entries are the blocker record. Reproduction: two concurrent
claim() calls both observing a missing/invalid election record each persist and return
'acquired'; there is no CAS or verify-after-write.

=== Task 13C — corrective background-serialised election (ADR-0001) — 2026-09-19 ===

Task: T13C (corrective; supersedes the T13 direct multi-context election write path).
Branch: phoenix. Repository root: /Users/george.li/Documents/workspaces/nowpilot.
Base commit at task start: c608708 (docs(phase-01): record election amendment baseline).

RED (before implementation), focused suites:
  - tests/core/workspace/workspaceElectionArbiter.test.ts failed to resolve
    @/core/workspace/WorkspaceElectionArbiter (module absent).
  - tests/core/workspace/workspaceTypes.test.ts failed: new idempotency schemas absent.
  - tests/core/runtime/messageSchemas.test.ts failed: election payload schemas absent.
  - tests/core/runtime/boundaryValidation.test.ts failed: 13-type registry assertion.
  - tests/core/error/errorCodes.test.ts failed: 15-code list (WORKSPACE_ELECTION_* absent).
  - tests/core/workspace/workspaceElection.test.ts failed: client still wrote storage directly.
  Aggregate RED: Test Files 6 failed | 11 passed; Tests 20 failed | 84 passed.

GREEN (after implementation), fresh output:
  $ pnpm exec vitest run --project unit tests/core/workspace/workspaceElectionArbiter.test.ts
    Test Files 1 passed (1); Tests 41 passed (41)   exit 0
  $ pnpm exec vitest run --project unit tests/core/workspace/workspaceElection.test.ts
    Test Files 1 passed (1); Tests 14 passed (14)   exit 0
  $ pnpm run typecheck                                      exit 0
  $ pnpm run lint                                            exit 0
  $ pnpm exec prettier --check .                             exit 0 (All matched files use Prettier)
  $ pnpm run test                                            exit 0 (17 files, 145 tests passed)
  $ pnpm run typecheck && pnpm run lint && pnpm run test     exit 0

Registry completeness: MESSAGE_TYPES = 13 (runtimePrimitives, boundaryValidation,
messageSchemas); ERROR_CODES = 15 (errorCodes).

Obligation-to-test mapping (PLAN T13C Step 1 items 1-24 + ledger bullets):
  1  two simultaneous claims -> exactly one accepted: 'elects exactly one writer for two simultaneous initial claims'
  2  loser never accepted, persisted names winner: same test
  3  winner persisted before accepted (read-back): 'persists the winning record before returning accepted'
  4  two ownership changes never share an epoch: 'never issues the same epoch...'
  5  monotonic claim -> handoff -> recovery: 'increases the epoch monotonically...'
  6  stale expectedEpoch behind -> rejected: 'rejects a stale claim whose expectedEpoch is behind...'
  7  non-writer relinquish rejected, record unchanged: 'rejects a non-writer relinquish...'
  8  duplicate idempotency, no relinquish/handoff replay: 'returns the persisted authoritative result...',
     'does not replay a handoff-commit...', 'does not replay relinquish and rejects a duplicate relinquish'
  9  idempotency survives restart: 'preserves duplicate-request idempotency across a simulated worker restart'
  10 same requestId different data fails closed: 'fails closed when a requestId is reused with different operation data'
  11 handoff-commit and fallback cannot both succeed: 'never lets a handoff-commit and a fallback claim both succeed'
  12 recovery serialised behind handoff: 'serialises a stale recovery behind a live handoff-commit'
  13 persistence failure -> no writer: 'fails closed without a writer when persistence fails',
     'leaves the existing record unchanged when an ownership change cannot be persisted'
  14 read-back mismatch fails closed: 'fails closed when the persisted-record read-back does not match'
  15 fresh arbiter reconstructs decision: 'reconstructs a fresh decision from persisted state after restart'
  16 only election requests handled; mutation never touches record: listener
     'ignores ordinary workspace.mutation messages...'
  17 listener returns literal true synchronously
  18 response only after queued completion
  19 persistence failure -> exactly one failure response
  20 unexpected exception -> exactly one canonical failure response
  21 never responds twice
  22 untrusted sender / schema-invalid -> no response
  23 correlationId = request envelope id; target = requester only
  24 wildcard/background requests rejected (listener); wildcard/mis-targeted responses ignored (client)
  ledger: newest 32 retained; oldest evicted on 33rd; deterministic oldest->newest, unique requestId;
          duplicate A after B; replay does not increment epoch; restart preserves 32;
          malformed/oversized normalised; non-array ledger recovery.
  invalid-recovery epochs: 'recovers an invalid record to a monotonically higher epoch' (5 -> 6),
          'recovers an invalid record without a numeric prior epoch at epoch one' (-> 1)
  read failure: 'fails closed when the persisted record cannot be read'

Files changed: src/core/error/errorCodes.ts, src/core/runtime/MessageType.ts,
  src/core/runtime/messageSchemas.ts, src/core/runtime/RuntimeEnvelope.ts,
  src/core/workspace/workspaceTypes.ts, src/core/workspace/WorkspaceElection.ts,
  src/core/workspace/WorkspaceElectionArbiter.ts (new),
  tests/core/error/errorCodes.test.ts, tests/core/runtime/messageSchemas.test.ts,
  tests/core/runtime/boundaryValidation.test.ts, tests/core/runtime/runtimePrimitives.test.ts,
  tests/core/workspace/workspaceTypes.test.ts, tests/core/workspace/workspaceElection.test.ts,
  tests/core/workspace/workspaceElectionArbiter.test.ts (new).

No new storage key, permission, dependency, DiagnosticEvent, or message type beyond the two named.
No IndexedDB, provider/MCP, or direct chrome.* in core modules. Ordinary workspace.mutation never
routes through the arbiter. No forbidden file was modified.

Corrected T13 acceptance: the T13 direct read-then-write critical finding is remediated by the
single FIFO arbiter executor; simultaneous claims produce exactly one writer; corrected T13 is
accepted.

=== Task 14 — Prepare, Acknowledge, and Commit Handoff (ADR-0001 amended) — 2026-09-19 ===

Task: T14 (amended by ADR-0001 and binding rulings R14.1-R14.5: commit via the arbiter).
Branch: phoenix. Repository root: /Users/george.li/Documents/workspaces/nowpilot.
Base commit at task start: bcd06326a0b06a829b2856194e0fec314da5d4ed
  (docs(phase-01): preserve historical status line).
Runner date: 2026-09-19 22:11 AEST.

RED (before implementation), focused suite:
  $ pnpm run test -- tests/core/workspace/workspaceHandoff.test.ts
    FAIL tests/core/workspace/workspaceHandoff.test.ts (0 test)
    Error: Failed to resolve import "@/core/workspace/WorkspaceHandoff" from
      "tests/core/workspace/workspaceHandoff.test.ts". Does the file exist?
    Test Files 1 failed | 17 passed; Tests 145 passed; exit 1
  Expected failure: module-resolution error for the not-yet-created module.

GREEN (after implementation), fresh output:
  $ pnpm run test -- tests/core/workspace/workspaceHandoff.test.ts
    Test Files 18 passed (18); Tests 156 passed (156)   exit 0
  $ pnpm run typecheck                                      exit 0
  $ pnpm run lint                                            exit 0
  $ pnpm exec prettier --check .                             exit 0 (All matched files use Prettier)
  $ pnpm run typecheck && pnpm run lint && pnpm exec prettier --check . && pnpm run test
    typecheck exit 0; lint exit 0; prettier exit 0;
    Test Files 18 passed (18); Tests 156 passed (156)   exit 0

Behaviour verified (fresh tests in tests/core/workspace/workspaceHandoff.test.ts):
  - prepare reads np_workspace_election and writes ONLY np_workspace_handoff; the election
    record is byte-identical afterwards (handoffPhase stays 'idle') [R14.2].
  - prepare from a non-writer is rejected WORKSPACE_HANDOFF_FAILED; no handoff record written.
  - acknowledge requires phase 'prepared'; epoch mismatch -> WORKSPACE_EPOCH_MISMATCH;
    baseVersion mismatch -> WORKSPACE_VERSION_CONFLICT; success writes phase 'acknowledged'
    with acknowledgedAt and leaves the election record unchanged.
  - commit before acknowledgement is rejected WORKSPACE_HANDOFF_FAILED with no arbiter call.
  - commit after acknowledgement mints a fresh requestId and submits exactly one
    workspace.election.request with operation 'handoff-commit', requester identity,
    committedVersion, expectedEpoch, targetInstanceId, targetWriterType, and no reason; on
    accepted:true it removes np_workspace_handoff and returns 'committed' while leaving
    np_workspace_election untouched [R14.3/R14.4].
  - commit delegates to a real WorkspaceElectionArbiter: ownership becomes standalone at
    epoch 1 and exactly one authorised writer remains.
  - accepted:false with WORKSPACE_ELECTION_REJECTED / WORKSPACE_ELECTION_FAILED maps to the
    same canonical code and leaves the handoff record; any other rejection code or a thrown
    submission fails closed to WORKSPACE_HANDOFF_FAILED with the handoff record unchanged [R14.3].

Files changed: src/core/workspace/WorkspaceHandoff.ts (new),
  tests/core/workspace/workspaceHandoff.test.ts (new).

No np_workspace_election write in WorkspaceHandoff; the arbiter remains its only writer.
No new storage key, message type, error code, permission, dependency, or DiagnosticEvent.
No forbidden file was modified (WorkspaceElection.ts, WorkspaceElectionArbiter.ts, DESIGN.md,
PLAN.md, config, and T15+ files untouched).

===============================================================================
Task 15 — Mutation Versioning and Idempotency
===============================================================================

Date: 2026-09-19T22:16:00Z (22:16 AEST)
Task ID: T15
Branch: phoenix
Repository root: /Users/george.li/Documents/workspaces/nowpilot
Classification: code task; TDD RED->GREEN applied
Implementation tier: advanced (write correctness)
Base commit at task start: 81bc563 (T14 atomic commit
  `feat(phase-01): implement prepare acknowledge commit handoff`)
Files created: src/core/workspace/WorkspaceMutations.ts,
  tests/core/workspace/workspaceMutations.test.ts
Files modified: none (production or toolchain); only .planning evidence/STATUS
Atomic commit: feat(phase-01): version and idempotently apply workspace mutations

Step 1 — test file created verbatim from the brief at
tests/core/workspace/workspaceMutations.test.ts (7 tests).

Step 2 — RED (focused, brief command):
  command: pnpm run test -- tests/core/workspace/workspaceMutations.test.ts
  exit status: 1
  output: FAIL tests/core/workspace/workspaceMutations.test.ts (0 test)
    Error: Failed to resolve import "@/core/workspace/WorkspaceMutations" from
      "tests/core/workspace/workspaceMutations.test.ts". Does the file exist?
      Plugin: vite:import-analysis
    Test Files 1 failed | 18 passed (19); Tests 156 passed (156).
  Expected failure reason confirmed: the T15 mutation module did not yet exist, so the
  test file failed at Vite import analysis for `@/core/workspace/WorkspaceMutations`.
  The 156 pre-existing unit tests still passed. No canonical mutation kind, error code,
  store method, or schema export was missing.

Step 3 — src/core/workspace/WorkspaceMutations.ts created per the brief:
  Imports the T03 createErrorRecord/debugLog, the T12 WorkspaceStore type, and the T06
  WorkspaceMetadataSchema/InstanceId/WorkspaceMutation.
  Interfaces/types exactly as specified: MutationEngineState (committedVersion, epoch,
  writerInstanceId, appliedMutationIds), MutationRejectionCode (the four canonical T03
  codes WORKSPACE_OWNERSHIP_AMBIGUOUS / WORKSPACE_EPOCH_MISMATCH /
  WORKSPACE_STALE_MUTATION / WORKSPACE_VERSION_CONFLICT), MutationOutcome
  (applied+mutation+state | duplicate+state | rejected+code+state),
  createMutationEngineState(input) (committedVersion default 0, appliedMutationIds
  default []), applyWorkspaceMutation(state, mutation), and the commit dependency
  interface CommitWorkspaceMutationDependencies (store, now) plus
  commitWorkspaceMutation(deps, state, mutation).
  applyWorkspaceMutation order of checks (duplicate detection precedes all others):
    1. duplicate: mutationId already in appliedMutationIds -> {status:'duplicate'} with
       the SAME state (no version/ID change, applies once).
    2. wrong writerInstanceId -> rejected WORKSPACE_OWNERSHIP_AMBIGUOUS.
    3. wrong epoch -> rejected WORKSPACE_EPOCH_MISMATCH.
    4. baseVersion !== committedVersion -> rejected WORKSPACE_STALE_MUTATION.
    5. resultingVersion !== baseVersion + 1 -> rejected WORKSPACE_VERSION_CONFLICT.
    Applied: committedVersion becomes resultingVersion and the mutation id is appended,
    so version increments are strictly monotonic.
  Every rejection logs the canonical error code through the T03 debugLog with only a
  fixed `reason: 'mutation'` label (redacted sink); no payload is logged.
  commitWorkspaceMutation is fail-closed: it calls applyWorkspaceMutation and returns
  the outcome UNCHANGED for duplicate/rejected, so nothing is persisted unless the
  outcome is 'applied'; only then does it parse WorkspaceMetadataSchema from the
  mutation payload with the engine's resultingVersion and deps.now(), and call
  store.writeMetadata. No direct chrome.*, no IndexedDB, no durable journal (Phase 01
  scope: in-memory applied-ID set only), no new dependency.

Step 4 — GREEN (focused, explicit path):
  command: pnpm run test -- tests/core/workspace/workspaceMutations.test.ts
  exit status: 0
  output: Test Files 19 passed (19); Tests 163 passed (163), 0 failed, 0 skipped.
    - applies a valid next mutation and increments the version
    - rejects a mutation from the wrong writer
    - rejects a mutation from the wrong epoch
    - rejects a stale base version
    - rejects a non-monotonic resulting version
    - treats a repeated mutation id as a duplicate and applies it once
    - persists an applied mutation through the workspace store

Focused verification (sequential):
  pnpm run test -- tests/core/workspace/workspaceMutations.test.ts => exit 0
    (unit project: 19 files, 163 tests passed)
  pnpm run typecheck                                  => exit 0 (tsc --noEmit clean)
  pnpm run lint                                       => exit 0 (eslint . clean)
  pnpm exec prettier --check .                        => exit 0 after formatting only
    src/core/workspace/WorkspaceMutations.ts (the three-symbol workspaceTypes import
    was collapsed onto one line at printWidth 100). Formatting changed whitespace only;
    identifiers, values, codes, and behaviour are unchanged. The T15 test file was
    already Prettier-clean; no other file was reformatted.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 19 files, 163 tests passed.

Test counts:
  T15 test file: 1 passed, 7 tests passed, 0 failed, 0 skipped.
  Unit project total after T15: 19 files, 163 passed.

Specification-compliance checklist (verbatim from the brief):
  - one mutation kind only (`workspace.metadata.set`), validated by the T06
    WorkspaceMutationSchema: yes
  - mutation ID, writer instance, epoch, base + resulting version, kind, and
    schema-valid payload are all carried and checked: yes
  - wrong writer / wrong epoch / stale base rejected with canonical codes: yes
  - non-monotonic resulting version rejected (must equal base + 1): yes
  - repeated mutationId treated as a duplicate and applied once: yes
  - version increments are monotonic (committedVersion = resultingVersion): yes
  - persisted applied mutation goes through the T12 WorkspaceStore.writeMetadata, which
    writes np_workspace_meta then np_workspace_version; store.readVersion() reads 1 and
    store.readMetadata() carries { committedVersion: 1, updatedAt: now() }: yes
  - in-memory applied-ID set only; no durable journal, no IndexedDB, no replay: yes
  - no dependency change; no manifest/permission change; no direct chrome.*: yes
  - only WorkspaceMutations.ts and the one new test file created (plus .planning
    evidence/STATUS): yes

Code-quality and security checklist:
  - rejections use canonical T03 codes only; no invented code or message: yes
  - duplicate detection precedes all other checks (including writer/epoch/version): yes
  - no partial persistence on rejection: commitWorkspaceMutation returns the non-applied
    outcome before any store.writeMetadata call: yes
  - monotonic version increments; applied state is immutable (new array/object, no
    mutation of the input state): yes
  - only a fixed `reason: 'mutation'` label is logged through the redacting debugLog; no
    mutation payload or writer id is logged: yes
  - no catch blocks; no empty catch; no swallowed rejection: yes
  - no secrets, tokens, passwords, or page content read/logged/persisted: yes

Completion criteria: mutation test green; typecheck and lint pass; phase-applicable
chain exit 0; one atomic task commit exists; working tree clean; status recorded.

===============================================================================
Task 16 — Mirror Ordering, Gap Rehydration, and Writer Coordination
===============================================================================

Date: 2026-09-19T12:27:45Z (22:27 AEST)
Task ID: T16 (amended by ADR-0001 and binding rulings R16.1-R16.7)
Branch: phoenix
Repository root: /Users/george.li/Documents/workspaces/nowpilot
Classification: code task; TDD RED->GREEN applied
Implementation tier: advanced (convergence and ownership coordination)
Base commit at task start: 1d7afcf6dd7496904a558e516513edd0d35c24ba (T15 atomic
  commit `feat(phase-01): version and idempotently apply workspace mutations`)
Files created: src/core/workspace/WorkspaceSync.ts,
  tests/core/workspace/workspaceSync.test.ts
Files modified: none (production or toolchain); only .planning evidence/STATUS
Atomic commit: feat(phase-01): coordinate writer handoff and mirror convergence

Step 1 — test file written first at tests/core/workspace/workspaceSync.test.ts
(mirror + envelope tests from the brief, plus the redesigned coordinator handshake
tests required by R16.6: a real in-memory bus that routes workspace.election.request
to a real createWorkspaceElectionArbiter and dispatches the workspace.election.response
to registered handlers; the T13C client and T14 handoff are built over that bus/arbiter).

Step 2 — RED (focused, brief command):
  command: pnpm run test -- tests/core/workspace/workspaceSync.test.ts
  exit status: 1
  output: FAIL |unit| tests/core/workspace/workspaceSync.test.ts [0 test]
    Error: Failed to resolve import "@/core/workspace/WorkspaceSync" from
      "tests/core/workspace/workspaceSync.test.ts". Does the file exist?
      Plugin: vite:import-analysis
    Test Files 1 failed | 19 passed (20); Tests 163 passed (163).
  Expected failure reason confirmed: the T16 coordinator module did not yet exist, so
  the test file failed at Vite import analysis for `@/core/workspace/WorkspaceSync`.
  The 163 pre-existing unit tests still passed. No canonical identifier, message type,
  envelope builder, or arbitration contract was missing.

Step 3 — src/core/workspace/WorkspaceSync.ts created per the brief and rulings:
  - Mirror: MirrorState, MirrorDecision, classifyMirrorEnvelope (current-epoch
    next-version -> apply; equal -> duplicate; lower -> stale; wrong epoch -> epoch;
    higher-than-next -> rehydrate gap), applyMirrorEnvelope (state advances only on
    apply).
  - Envelope builders: createRehydrateRequestEnvelope, createRehydrateResponseEnvelope,
    createHandoffPrepareEnvelope, createHandoffAckEnvelope, createHandoffCommitEnvelope.
  - Coordinator: createWorkspaceCoordinator with the exact R16.1 dependency shape
    { bus, surface, instanceId, storage, election (T13C client), handoff (T14), store }.
    handleRehydrateRequest (writer-only; rehydrate response; Side Panel prepares the
    handoff to a Standalone requester); handleHandoffPrepare (R16.3: calls
    deps.handoff.acknowledge(epoch, baseVersion) and fails closed with
    WORKSPACE_HANDOFF_FAILED unless acknowledged, then sends the workspace.handoff.ack
    envelope); handleHandoffAck (R16.4: local-writer check then deps.handoff.commit,
    which submits the handoff-commit election request through the arbiter; reports
    committed only on success, else WORKSPACE_HANDOFF_FAILED); handleMutation (mirror
    apply/ignore/rehydrate; ordinary mutations never touch the arbiter);
    handleRelinquish (R16.4: relinquish only for the validated local writer and report
    relinquished only when accepted). start() (R16.2) subscribes to rehydrate.request,
    handoff.prepare, handoff.ack, mutation, relinquish, and the np_workspace_election
    storage subscription; the storage subscription never writes the record and, for a
    sidepanel, delegates recovery to the T13C client via claim('fallback') for missing
    and claim('stale-recovery') for invalid.
  - No third workspace.election.response subscription (R16.1): request/response
    correlation remains in the T13C client and the handoff's injected
    submitElectionRequest. No direct np_workspace_election access or write.

Step 4 — GREEN:
  command: pnpm exec vitest run --project unit tests/core/workspace/workspaceSync.test.ts
  exit status: 0
  output: Test Files 1 passed (1); Tests 17 passed (17), 0 failed, 0 skipped.
    - workspace mirror: applies next version; ignores duplicate/stale/other-epoch;
      rehydrates on a version gap; advances state only on apply; builds schema-valid
      rehydrate request/response envelopes
    - workspace coordinator handshake (10):
      - hands writer ownership from sidepanel to standalone through prepare/ack/commit
        (persisted record standalone/st/epoch 1)
      - reclaims sidepanel ownership after the election is relinquished (arbiter)
      - routes missing-record recovery through the election client as a fallback claim
      - routes invalid-record recovery through the election client as stale recovery
      - mirrors a current-epoch next-version mutation
      - requests rehydration on a version gap
      - relinquishes only for the current writer; ignores a non-writer
      - acknowledges the handoff record before sending the handoff ack
      - commits ownership through the arbiter on handoff ack

Focused verification (sequential, fresh output):
  pnpm run test -- tests/core/workspace/workspaceSync.test.ts  => exit 0
    (unit project: 20 files, 180 tests passed; T16 file 17 tests)
  pnpm run typecheck                                          => exit 0 (tsc --noEmit clean)
  pnpm run lint                                               => exit 0 (eslint . clean)
  pnpm exec prettier --check .                                => exit 0 after formatting only
    Both new files were formatted with Prettier (printWidth 100). Formatting changed
    whitespace only; identifiers, message-type strings, payloads, and assertions are
    unchanged; no other file was reformatted.

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 20 files, 180 tests passed.

Test counts:
  T16 test file: 1 passed, 17 tests passed, 0 failed, 0 skipped.
  Unit project total after T16: 20 files, 180 passed (T15's 163 + T16's 17).

Specification-compliance checklist (verbatim from the brief, applied per the rulings):
  - ignore duplicate/stale; rehydrate on gaps: yes
  - apply only current-epoch next-version: yes
  - rehydration uses the canonical message pair: yes (workspace.rehydrate.request /
    workspace.rehydrate.response, schema-valid, no new type)
  - handoff prepare/ack/commit never authorises two writers: yes (T14 prepare/ack
    local, commit through the single background arbiter; on acceptance the persisted
    record moves to exactly one new writer at epoch+1)
  - R16.3 handleHandoffPrepare acknowledges the handoff record and fails closed
    otherwise: yes
  - R16.4 handleHandoffAck commits via deps.handoff.commit and reports committed only
    on success; handleRelinquish reports relinquished only when accepted: yes
  - R16.2 start() does not write the election record; non-valid sidepanel records
    delegate to claim('fallback'|'stale-recovery'): yes
  - R16.1 no third workspace.election.response subscription; no direct
    np_workspace_election write: yes
  - R16.7 no new message type, storage key, error code, permission, dependency, or
    DiagnosticEvent; no IndexedDB: yes
  - Only WorkspaceSync.ts and the one new test file created (plus .planning
    evidence/STATUS): yes

Code-quality and security checklist:
  - no mirror state advanced on ignored/rehydrate decisions: yes (applyMirrorEnvelope
    returns the input state unchanged for non-apply decisions)
  - no silent gap skip: yes (a gap emits a rehydrate request and reports rehydrated)
  - coordinator validates writer status before preparing/committing/relinquishing: yes
  - no committed mutation replay: yes (ordinary mutations are mirrored or trigger
    rehydration; they never route through the background arbiter)
  - only canonical error codes (WORKSPACE_HANDOFF_FAILED, WORKSPACE_INVALID_METADATA)
    are returned; no invented code: yes
  - no catch blocks, no empty catch, no swallowed rejection: yes
  - no secrets, tokens, passwords, page content, prompts, or tool I/O read, logged, or
    persisted; no direct chrome.* (storage only through the T04 adapter): yes

Completion criteria: sync test green; typecheck and lint pass; phase-applicable chain
exit 0; one atomic task commit exists; working tree clean; status recorded.

=== Task 17 — Theme Schemas, Ant Design Configuration, Persistence, and Live Propagation (2026-09-19T22:34:45Z) ===

task ID: T17
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: code task; TDD RED->GREEN applied
implementation tier: balanced
base commit: 7aa7e9683ecc17fe9f06c7679cc735c4755d2d3f (T16 atomic commit
  `feat(phase-01): coordinate writer handoff and mirror convergence`)
Files created (production):
  src/core/theme/themeTypes.ts
  src/core/theme/antdConfig.ts
  src/core/theme/ThemeStore.ts
  src/core/theme/useTheme.ts
Files created (tests):
  tests/core/theme/themeTypes.test.ts
  tests/core/theme/antdConfig.test.ts
  tests/core/theme/themeStore.test.ts
  tests/core/theme/useTheme.test.tsx
Files modified: none (production or toolchain); no dependency change

Step 1 — four test files created verbatim from the brief:
  themeTypes.test.ts (3 tests), antdConfig.test.ts (5 tests), themeStore.test.ts
  (5 tests), useTheme.test.tsx (1 test).

Step 2 — RED:
  command: pnpm run test -- tests/core/theme
  exit status: 1
  observed: Test Files 4 failed | 20 passed (24); Tests 180 passed (180)
    FAIL |unit| tests/core/theme/themeTypes.test.ts (0 test)
      Error: Failed to resolve import "@/core/theme/themeTypes" from
      "tests/core/theme/themeTypes.test.ts". Does the file exist?
      Plugin: vite:import-analysis
    FAIL |unit| tests/core/theme/antdConfig.test.ts (0 test)
      Error: Failed to resolve import "@/core/theme/antdConfig" from
      "tests/core/theme/antdConfig.test.ts". Does the file exist?
    FAIL |unit| tests/core/theme/themeStore.test.ts (0 test)
      Error: Failed to resolve import "@/core/theme/ThemeStore" from
      "tests/core/theme/themeStore.test.ts". Does the file exist?
    FAIL |unit| tests/core/theme/useTheme.test.tsx (0 test)
      Error: Failed to resolve import "@/core/theme/useTheme" from
      "tests/core/theme/useTheme.test.tsx". Does the file exist?
  Expected failure reason confirmed: all four theme modules were absent, so Vite could
  not resolve the `@/core/theme/*` imports. The 20 passing files were the pre-existing
  unit tests (Vitest 5 does not restrict `--` positionals to the requested
  subdirectory under `--project unit`). The four pre-existing baseline had 180 tests.
  No canonical token, key, algorithm, or error code was missing; `antd` 6.6.4 exposes
  `theme.defaultAlgorithm`, `theme.darkAlgorithm`, `theme.compactAlgorithm`,
  `ThemeConfig.algorithm`, `token`, `components`, `hashed`, and `cssVar`.

Steps 3–6 — implementation verbatim per the brief:
  themeTypes.ts: THEME_MODES exactly ['auto','light','dark']; ThemeMode; ThemeModeSchema
    (closed z.enum); THEME_PACKS exactly ['default','liquid-glass','claude-warm'];
    ThemePack; ThemePackSchema; DEFAULT_THEME_MODE='auto'; DEFAULT_THEME_PACK='default';
    ResolvedColorScheme='light'|'dark'; resolveColorScheme(mode, prefersDark) resolves
    'auto' from prefersDark and passes explicit modes through.
  antdConfig.ts: NOWPILOT_SEED (primary/success/warning/error/info colours,
    colorTextBase #1F2430, colorBgBase #FCFCFD, borderRadius 8, wireframe false);
    NOWPILOT_COMPONENTS (Card/Button/Layout/Menu/Modal); NOWPILOT_PACK_OVERLAYS
    (default {}, liquid-glass colorBgContainer rgba(255,255,255,0.68), claude-warm
    colorBgBase #FAF7F2); AntdConfigInput; getAntdConfig applies seed -> pack overlay ->
    algorithm (default/dark by resolved scheme, plus compactAlgorithm when compact),
    cssVar { key: 'nowpilot' }, hashed false.
  ThemeStore.ts: ThemePreferences; ThemeStore (read/writeMode/writePack/subscribe);
    createThemeStore(storage) reads np_theme/np_theme_pack through the T04 validated
    adapter, logs canonical THEME_INVALID_VALUE (fixed key label, no raw value) and falls
    back to canonical defaults for invalid/missing values; writeMode/writePack write to
    chrome.storage.sync (T04 area mapping) and fail closed with THEME_PERSIST_FAILED on
    rejection; subscribe combines both key subscriptions and re-reads to emit combined
    preferences. Theme is independent of writer election; no chrome.storage call outside
    the T04 adapter; no @ant-design/x.
  useTheme.ts: UseThemeOptions/UseThemeResult; useTheme(store, options) seeds from
    canonical defaults, subscribes and reads stored preferences in an effect (active
    guard + unsubscribe cleanup), and returns config from getAntdConfig plus mode/pack.

Step 7 — GREEN (focused, explicit path):
  command: pnpm exec vitest run --project unit tests/core/theme
  exit status: 0
  output: Test Files 4 passed (4); Tests 14 passed (14), 0 failed, 0 skipped (T17-only).
  command: pnpm run test -- tests/core/theme
  exit status: 0
  output: Test Files 24 passed (24); Tests 194 passed (194)
  note: under Vitest 5 the pnpm-script passthrough runs the whole unit project.

Focused verification (sequential):
  pnpm run typecheck                             => exit 0  (tsc --noEmit clean)
  pnpm run lint                                  => exit 0  (eslint . clean)
  pnpm exec prettier --check .                   => exit 0  (All matched files use
    Prettier code style; the eight new T17 files were formatted after creation with
    identifiers, values, and assertions unchanged and no other file reformatted)

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 24 files, 194 tests passed.

Test counts:
  T17 test files: 4 passed, 14 tests passed (3 themeTypes + 5 antdConfig + 5 themeStore
  + 1 useTheme), 0 failed, 0 skipped.
  Unit project total after T17: 24 files, 194 passed.

Type-only test adaptations (two; disclosed; supplied implementation unchanged):
  (T17-A) tests/core/theme/themeStore.test.ts: the brief's
    `chromeStorage.sync.set.mockRejectedValueOnce(...)` is TS2339 because
    `StorageAreaMock` extends `ChromeStorageAreaLike` and therefore widens the area
    methods to their plain signatures. Wrapped in the canonical Vitest type helper:
    `vi.mocked(chromeStorage.sync.set).mockRejectedValueOnce(...)`. All identifiers,
    the mock value `new Error('quota')`, and every assertion are unchanged.
  (T17-B) tests/core/theme/useTheme.test.tsx: the brief's mock held a write-only
    `listener` variable, which the pinned `noUnusedLocals` (tsc) and the repo ESLint
    rule `@typescript-eslint/no-unused-vars` reject. The mock now invokes the stored
    listener after updating `prefs` in `writeMode`/`writePack` (`listener?.(prefs)`),
    matching the real `ThemeStore` propagation contract. All identifiers, initial
    values, and assertions are unchanged; the one test performs no writes, so no
    assertion or observed behaviour changes.
  Before these adaptations `pnpm run typecheck` exited 2 (TS2339 at
  themeStore.test.ts:33 and TS6133 at useTheme.test.tsx:7) and `pnpm run lint` exited
  1 (same unused variable). After them both exit 0. tsconfig.json, vitest.config.ts,
  eslint.config.mjs, and tests/helpers/chromeMock.ts were not modified.

Specification-compliance checklist:
  - np_theme and np_theme_pack persist to chrome.storage.sync via the T04 mapping: yes
  - seed -> pack overlay -> algorithm ordering in getAntdConfig: yes
  - default/dark algorithm by resolved mode; compactAlgorithm appended when compact: yes
  - resolveColorScheme resolves 'auto' from prefersDark and passes explicit modes: yes
  - invalid/missing stored values fall back to canonical defaults (auto/default): yes
  - invalid stored values log canonical THEME_INVALID_VALUE with a fixed key label only: yes
  - write failures fail closed and reject with THEME_PERSIST_FAILED: yes
  - Side Panel compact true / Standalone compact false is applied by callers (compact
    is a hook input; no surface-specific hard-coding here): yes
  - closed ThemeModeSchema/ThemePackSchema reject unknown modes/packs: yes
  - no @ant-design/x; no hard-coded hex in components (seed/overlays only): yes
  - no provider configuration; no new storage key, error code, permission, or
    dependency; theme independent of writer election: yes
  - only the eight T17 files created (plus .planning evidence/STATUS); no other file
    changed (except the two disclosed test-only adaptations within the allowed files): yes

Code-quality and security checklist:
  - no raw stored value is logged; THEME_INVALID_VALUE/THEME_PERSIST_FAILED carry only a
    fixed key label through the redacting T03 debugLog: yes
  - write failures fail closed; no empty catch; the canonical code is logged and then
    rethrown as an error bearing the same code: yes
  - live propagation is driven by chrome.storage.onChanged through the T04 subscribe
    path; no remount-dependent switching: yes
  - useTheme seeds canonical defaults before the async read and guards state updates
    after unmount (active flag) and unsubscribes the store subscription: yes
  - no direct chrome.* outside the T04 adapter; no IndexedDB/network/filesystem; no
    content script; pure schema/config modules: yes
  - no secrets, tokens, passwords, page content, or prompts handled or persisted: yes

Completion criteria: theme tests green; typecheck and lint pass; prettier check passes;
phase-applicable chain exit 0; one atomic task commit exists; working tree clean;
status recorded.

=== Task 18 — Standalone Page Registry, Core Registration, and Skeleton Pages (2026-09-19T12:40:09Z) ===

task ID: T18
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: code task; TDD RED->GREEN applied
implementation tier: economy
base commit: aeb287c1d52dd9739a6de55ca4e87a104df08e75 (T17 atomic commit
  `feat(phase-01): add theme schemas config store and hook`)
Files created (production):
  src/core/registry/StandalonePageRegistry.ts
  src/core/registry/registerCorePages.ts
  src/components/standalone/pages/ChatPage.tsx
  src/components/standalone/pages/AgentPage.tsx
  src/components/standalone/pages/NotesPage.tsx
  src/components/standalone/pages/WritePage.tsx
  src/components/standalone/pages/ToolsPage.tsx
  src/components/standalone/pages/DiagnosticsPage.tsx
  src/components/options/OptionsPage.tsx (skeleton; replaced in T20)
Files created (tests):
  tests/core/registry/standalonePageRegistry.test.ts
  tests/core/registry/registerCorePages.test.tsx
Files modified: none (production or toolchain); no dependency change; no
  AppearanceSection.tsx created (deferred to T20)

Step 1 — two test files created verbatim from the brief:
  standalonePageRegistry.test.ts (3 tests), registerCorePages.test.tsx (2 tests).

Step 2 — RED:
  command: pnpm run test -- tests/core/registry
  exit status: 1
  observed: Test Files 2 failed | 24 passed (26); Tests 194 passed (194)
    FAIL |unit| tests/core/registry/standalonePageRegistry.test.ts
      Error: Failed to resolve import "@/core/registry/StandalonePageRegistry"
      from "tests/core/registry/standalonePageRegistry.test.ts". Does the file
      exist? Plugin: vite:import-analysis
    FAIL |unit| tests/core/registry/registerCorePages.test.tsx
      Error: Failed to resolve import "@/core/registry/registerCorePages" from
      "tests/core/registry/registerCorePages.test.tsx". Does the file exist?
      Plugin: vite:import-analysis
  Expected failure reason confirmed: both T18 modules were absent, so Vite could
  not resolve the `@/core/registry/*` imports. The 24 passing files were the
  pre-existing unit tests (Vitest 5 does not restrict `--` positionals to the
  requested subdirectory under `--project unit`). No canonical identifier was
  missing; `@testing-library/react` and `antd` 6.6.4 are already installed.

Steps 3–5 — implementation verbatim per the brief:
  StandalonePageRegistry.ts: StandalonePageComponent = ComponentType;
    StandalonePageRegistry (register/get/has/entries); createStandalonePageRegistry()
    backed by a Map, register throws `Standalone route already registered: <id>`
    on duplicate, entries() preserves registration order.
  Six skeleton pages: ChatPage, AgentPage, NotesPage, WritePage, ToolsPage,
    DiagnosticsPage — each presentational and non-interactive, wrapping an antd
    Typography.Title plus Empty inside a <section> carrying its canonical
    data-testid `standalone-page-<id>` and aria-label.
  registerCorePages.ts: createCorePageRegistry() registers all seven route ids in
    STANDALONE_ROUTE_IDS order (chat, agent, notes, write, tools, options,
    diagnostics); CORE_PAGE_REGISTRY is the module singleton.
  OptionsPage.tsx (skeleton): <section data-testid="standalone-page-options"
    aria-label="Options"> with Typography.Title and Empty; body is replaced in T20.
  Decision recorded: the brief permits either pre-running T20 or creating a
  minimal OptionsPage skeleton in T18. Chosen: create the minimal skeleton in T18
  so this task's commit stays atomic and green; T20 replaces its body. No
  AppearanceSection.tsx was created.

Step 6 — GREEN (focused, explicit path):
  command: pnpm exec vitest run --project unit tests/core/registry/standalonePageRegistry.test.ts tests/core/registry/registerCorePages.test.tsx
  exit status: 0
  output: Test Files 2 passed (2); Tests 5 passed (5), 0 failed, 0 skipped (T18-only).
  command: pnpm run test -- tests/core/registry
  exit status: 0
  output: Test Files 26 passed (26); Tests 199 passed (199)
  note: under Vitest 5 the pnpm-script passthrough runs the whole unit project.

Focused verification (sequential):
  pnpm run typecheck                             => exit 0  (tsc --noEmit clean)
  pnpm run lint                                  => exit 0  (eslint . clean)
  pnpm exec prettier --check .                   => exit 0  (All matched files use
    Prettier code style; only src/core/registry/registerCorePages.ts required a
    reflow of the multi-line StandalonePageRegistry import — identifiers, values,
    and assertions unchanged; no other file reformatted)

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 26 files, 199 tests passed.

Test counts:
  T18 test files: 2 passed, 5 tests passed (3 standalonePageRegistry + 2
  registerCorePages), 0 failed, 0 skipped.
  Unit project total after T18: 26 files, 199 passed.

Specification-compliance checklist:
  - exactly seven registry entries, one per STANDALONE_ROUTE_IDS id: yes (asserted)
  - CORE_PAGE_REGISTRY.entries() length equals STANDALONE_ROUTE_IDS.length: yes
  - every page exposes data-testid="standalone-page-<id>": yes (rendered and
    asserted for all seven ids)
  - primary/footer placement is owned by standaloneRoutes.ts, not this task: yes
  - labels match the canonical route labels (Chat/Agent/Notes/Write/Tools/Options/
    Diagnostics): yes
  - no route duplication in createCorePageRegistry: yes (registry rejects duplicates)
  - interfaces produced exactly as specified (StandalonePageComponent,
    StandalonePageRegistry, createStandalonePageRegistry, createCorePageRegistry,
    CORE_PAGE_REGISTRY): yes
  - no AppearanceSection.tsx; no later-phase page logic, TeamGQM, ServiceNow, mock
    services, inactive controls, dependency, permission, key, or error code: yes
  - only the brief's allowed files created (plus .planning evidence/STATUS): yes

Code-quality and security checklist:
  - registry rejects duplicate route ids by throwing: yes
  - entries() returns a fresh array preserving registration order; no internal Map
    reference escapes: yes
  - skeleton pages are presentational and non-interactive; no controls, no event
    handlers, no state, no side effects: yes
  - stable data-testids for the T24 isolation inspection: yes
  - no direct chrome.*, IndexedDB, network, filesystem, content-script, or
    host-page access; no secrets, tokens, passwords, prompts, or page content: yes
  - no `any`; strong typing through StandaloneRouteId and ComponentType: yes

Completion criteria: registry tests green; typecheck and lint pass; prettier check
passes; phase-applicable chain exit 0; one atomic task commit exists; working tree
clean; status recorded.

=== Task 19 — Standalone Shell, Router, and Sider (2026-09-19T19:48:02Z) ===

task ID: T19
branch: phoenix
repository root: /Users/george.li/Documents/workspaces/nowpilot
classification: code task; TDD RED->GREEN applied
implementation tier: balanced
base commit: f146e79e8ebb4b21283bf054c524da50113cd5d4 (T18 atomic commit
  `feat(phase-01): register standalone pages and skeletons`)
Files created (production):
  src/components/standalone/StandaloneSider.tsx
  src/components/standalone/StandaloneRouter.tsx
  src/components/standalone/StandaloneShell.tsx
Files created (tests):
  tests/components/standaloneSider.test.tsx
  tests/components/standaloneRouter.test.tsx
  tests/components/standaloneShell.test.tsx
Files modified: .planning/evidence/phase-01/verification.txt,
  .planning/evidence/phase-01/review.md, .planning/STATUS.md only.
  No dependency, permission, storage key, message type, error code, or
  DiagnosticEvent change.

Step 1 — three test files created from the brief.

Step 2 — RED:
  command: pnpm run test -- tests/components
  exit status: 1
  observed: Test Files 3 failed | 26 passed (29); Tests 199 passed (199)
    FAIL |unit| tests/components/standaloneSider.test.tsx
      Error: Failed to resolve import
      "@/components/standalone/StandaloneSider" ... Does the file exist?
      Plugin: vite:import-analysis
    FAIL |unit| tests/components/standaloneRouter.test.tsx
      Error: Failed to resolve import
      "@/components/standalone/StandaloneRouter" ... Does the file exist?
      Plugin: vite:import-analysis
    FAIL |unit| tests/components/standaloneShell.test.tsx
      Error: Failed to resolve import
      "@/components/standalone/StandaloneShell" ... Does the file exist?
      Plugin: vite:import-analysis
  Expected failure reason confirmed: all three T19 modules were absent, so Vite
  could not resolve the `@/components/standalone/*` imports. The 26 passing files
  (199 tests) were the pre-existing unit tests.

Steps 3–5 — implementation per the brief:
  StandaloneSider.tsx: antd Layout.Sider (width 240, theme light,
    aria-label="Workspace navigation") wrapping an inline Menu whose items are
    built by mapping PRIMARY_STANDALONE_ROUTES then FOOTER_STANDALONE_ROUTES —
    five primary plus two footer labels come from the T05 registry, with no
    hard-coded Sider array; selectedKeys={[activeRoute]}; onClick maps the antd
    key back to StandaloneRouteId and calls onNavigate.
  StandaloneRouter.tsx: StandaloneRouter renders registry.get(routeId) or null;
    useStandaloneRoute seeds routeId from initialHash ?? window.location.hash via
    resolveStandaloneRouteId, invokes onRouteFallback once for a fallback, applies
    a focusSubscription destination to route state, and writes the hash with
    window.history.replaceState (no hashchange/popstate listener, no back/forward
    traversal, no history entries).
  StandaloneShell.tsx: Layout with StandaloneSider and a Layout.Content holding
    the StandaloneRouter, wired to useStandaloneRoute.

Authorised correction applied:
  Removed the unused DEFAULT_STANDALONE_ROUTE_ID import from
  StandaloneRouter.tsx (the brief's import list included it but the module never
  referenced it, which fails the pinned noUnusedLocals/eslint). Every other import
  and all code were transcribed verbatim.

Necessary test-only correction (disclosed in review.md):
  The repository runs vitest without `globals: true` and imports vitest APIs
  explicitly, so no global `afterEach` exists and `@testing-library/react` cannot
  auto-register cleanup. The brief's `standaloneSider.test.tsx` renders in two
  `it` blocks; without cleanup the second `getByText('Notes')` matched two mounted
  Siders (`getMultipleElementsFoundError`). Added `cleanup`/`afterEach` imports
  and `afterEach(cleanup)` to the three component test files. All test bodies,
  identifiers, values, and assertions are unchanged.

Step 6 — GREEN:
  command: pnpm run test -- tests/components
  exit status: 0
  output: Test Files 29 passed (29); Tests 206 passed (206), 0 failed, 0 skipped.

Focused verification (sequential):
  pnpm run typecheck                             => exit 0  (tsc --noEmit clean)
  pnpm run lint                                  => exit 0  (eslint . clean)
  pnpm exec prettier --check .                   => exit 0  (All matched files use
    Prettier code style; the T19 files required a formatter reflow of long lines
    in StandaloneRouter.tsx and the router/shell test files — identifiers, values,
    and assertions unchanged; no other file reformatted)

Phase 01 verification applicable now:
  command: pnpm run typecheck && pnpm run lint && pnpm run test
  exit status: 0
  output: typecheck clean; lint clean; unit project 29 files, 206 tests passed.

Test counts:
  T19 test files: 3 passed, 7 tests passed (2 standaloneSider, 3
  standaloneRouter including useStandaloneRoute, 2 standaloneShell), 0 failed,
  0 skipped.
  Unit project total after T19: 29 files, 206 passed.

Specification-compliance checklist:
  - default route is `chat`; an unknown hash resolves to `chat` and reports the
    raw hash through onRouteFallback: yes (asserted via resolveStandaloneRouteId
    and the harness test)
  - Sider lists the five primary plus two footer routes from the T05 registry:
    yes (asserted for Chat/Agent/Notes/Write/Tools/Options/Diagnostics)
  - Sider navigation emits the clicked route id: yes (asserted onNavigate('notes'))
  - StandaloneRouter renders the registered page for the active route and nothing
    for an unregistered route: yes (asserted both)
  - focus destination applied by the live router: implemented via
    focusSubscription and applied to route state
  - interfaces produced exactly as specified (StandaloneSiderProps,
    StandaloneRouterProps, UseStandaloneRouteOptions, StandaloneRouteController,
    StandaloneShellProps; StandaloneSider, StandaloneRouter, useStandaloneRoute,
    StandaloneShell): yes
  - no hashchange/popstate listener, no browser-history traversal, no query/path
    routing; only replaceState: yes
  - no hard-coded Sider route array; labels/hashes come from standaloneRoutes.ts:
    yes
  - no Side Panel navigation UI; only the six created files (plus .planning
    evidence/STATUS) changed: yes

Code-quality and security checklist:
  - accessible navigation label (aria-label="Workspace navigation") on the Sider:
    yes
  - no route strings built outside the registry: yes (standaloneHashRoute is used)
  - no history entries (replaceState only); no direct chrome.*, storage,
    IndexedDB, network, filesystem, content-script, or host-page access: yes
  - no secrets, tokens, cookies, passwords, prompts, page content, or customer
    data read, logged, persisted, exported, or committed: yes
  - no `any`; strong typing through StandaloneRouteId and the registry types: yes

Completion criteria: component tests green; typecheck and lint pass; prettier check
passes; phase-applicable chain exit 0; one atomic task commit exists; working tree
clean; status recorded.
