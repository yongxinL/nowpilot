---
phase: 05-context-adaptive-execution
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tests/core/context/TokenBudget.test.ts
  - tests/core/context/ContextCompressor.test.ts
  - tests/core/context/ContextOptimizer.test.ts
  - package.json
autonomous: true
requirements: []   # infra phase — no spec-native v1 IDs (ROADMAP Phase 5 note); acceptance = §18 DONE-when + D-69..D-78
user_setup: []

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tier boundary fixtures pass verbatim (DONE-1): classifyModelContext(4096)='tiny', (4097)='small', (16384)='small', (16385)='medium', (131072)='medium', (131073)='large', (200000)='large'; contextTierCaps(tiny)={1,1}, (small)={2,1}, (medium)={3,2}, (large)={5,3}"
    - "Budget math passes (DONE-1): computeBudgets(10000)={inputBudget:7000, outputBudget:2000, safetyMargin:1000} (floor); each tier's DISTRIBUTION sums to 100 with the §2.2 values verbatim; budgetForCategory(tiny, 7000, 'context')=1400"
    - "Heuristic counter passes (D-71): ceil(len/4) for English, ceil(len/3) for CJK-heavy text at >= 0.30 density; English text with a stray CJK token stays len/4 (Pitfall 8); a supplementary-plane char (U+20000) counts as 1 code point, not 2 (Pitfall 7)"
    - "Context overflow degrades stepwise in §2.4 order; every ok:true OptimizedContext has provenance.totalTokens <= inputBudget — never oversized (DONE-2); the CONTEXT_TOO_LARGE terminal is RETURNED (ok:false, code:'CONTEXT_TOO_LARGE', message present, totalTokens, inputBudget, minimalMode, truncatedSources), never thrown"
    - "isFeatureAllowedInMinimalMode asserts the §2.5 blocked set verbatim (the 7 kebab-case literals return false) and allowed behaviors/unknown strings return true; tiny tier => minimalMode always true; minimal mode is the penultimate degradation step (DONE-3, D-74)"
    - "Every OptimizedContext carries a §2.6-verbatim ContextProvenanceManifest that zod-parses; truncatedSources derives from the manifest's truncated section sourceIds (excluding the by-design 'system'/'task' omission records) (DONE-4, D-77)"
    - "verify:phase-5 is re-pointed at tests/core/context and goes GREEN at the end of this plan (D-78 — the mis-pointed gate currently exits 1 with 'No test files found')"
  artifacts:
    - path: "tests/core/context/TokenBudget.test.ts"
      provides: "DONE-1: tier boundary fixtures + 70/20/10 budget formula + per-tier distribution sums-to-100 + heuristic counter (EN/CJK/density/surrogate) + contextTierCaps §1.4 table"
      exports: []
    - path: "tests/core/context/ContextCompressor.test.ts"
      provides: "D-75: compressStructural / reduceTopK / trimToolSchemas strategy behavior + summarizeHistory summarizer-seam fallback (drop-older-turns, truncated:true, never silent) + CompressionType closed union"
      exports: []
    - path: "tests/core/context/ContextOptimizer.test.ts"
      provides: "DONE-2/3/4: degradation ladder §2.4 order, never-oversized guarantee, CONTEXT_TOO_LARGE terminal returned, minimal-mode predicate verbatim, tiny=>minimalMode, manifest-on-every-context (schema-parsed), truncatedSources derivation, §1.3 canonical order + totalTokens tally via assemble (D-76)"
      exports: []
    - path: "package.json"
      provides: "verify:phase-5 re-pointed from the mis-pointed Phase-8/9 dirs to 'tsc --noEmit && vitest run tests/core/context' (D-78, spec 3609 canonical)"
      exports: []
  key_links:
    - from: "tests/core/context/ContextOptimizer.test.ts"
      to: "src/core/context/ContextOptimizer.ts"
      via: "assemble() happy-path + overflow fixtures assert the §2.3 contract and §2.4 ladder through the returned AssembleResult — the DONE-2/3/4 proofs"
      pattern: "assemble|AssembleResult"
    - from: "tests/core/context/ContextOptimizer.test.ts"
      to: "src/core/context/ContextProvenanceManifest.ts"
      via: "ContextProvenanceManifestSchema.parse(context.provenance) — every OptimizedContext carries a §2.6-verbatim manifest (DONE-4)"
      pattern: "ContextProvenanceManifestSchema"
    - from: "package.json"
      to: "tests/core/context"
      via: "verify:phase-5 = 'tsc --noEmit && vitest run tests/core/context' — the D-78 re-point (D-68 precedent, 04-PATTERNS.md:358-361)"
      pattern: "verify:phase-5"
  prohibitions:
    - "No module edits in this plan — the three test files + package.json only; modules were proven in 05-01 and any failing test must surface a module bug for a 05-02 task fix, not a silent test weakening"
    - "No new error codes / no StreamErrorCodeSchema edits (D-38)"
    - "No NP-STRICT markers in the three test files (STATE.md decision 17)"
    - "No live pipeline wiring (D-69) — tests call assemble() directly; no AgentOrchestrator / useChatStreaming imports"
---

<objective>
Expansion plan: prove the §18 create-only context layer with the three required test files (`tests/core/context/TokenBudget.test.ts`, `ContextCompressor.test.ts`, `ContextOptimizer.test.ts` — verbatim §18, spec 2596-2598) and make the phase gate GREEN by re-pointing `verify:phase-5` at `tests/core/context` (D-78 — the gate currently exits 1 with 'No test files found' because it mis-points at Phase-8/9 dirs).

Purpose: pin the DONE-when criteria — DONE-1 (tier tests), DONE-2 (stepwise degradation + truncatedSources), DONE-3 (minimal-mode predicate verbatim), DONE-4 (manifest on every OptimizedContext) — and close the D-78 gate re-point (the exact Phase-4 D-68 precedent). Wave-0 role per 05-VALIDATION.md: the three test files ARE this plan's wave-0 scaffold, written against the modules created in 05-01.

Output: three green §18 test files + `verify:phase-5` GREEN.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md  # §2.1-§2.6 (425-544), §1.3 (331-350), §1.4 (352-361), §18 Phase 5 (2580-2607), §19.2/§19.3 (2991-3004), §21.6 (3402-3454)
@.planning/phases/05-context-adaptive-execution/05-CONTEXT.md  # D-71..D-78 (the behaviors under test)
@.planning/phases/05-context-adaptive-execution/05-RESEARCH.md  # Validation Architecture (DONE-1..4 test map), Common Operations 1-6 (verbatim fixture values), Pitfalls 3/5/7/8/9
@.planning/phases/05-context-adaptive-execution/05-PATTERNS.md  # the three test-file sections (analog: OutcomeVerifier.test.ts input-builder, PromptCacheManager.test.ts canonical-order asserts, TrajectoryTracker.test.ts closed-union asserts)
@.planning/phases/05-context-adaptive-execution/05-VALIDATION.md  # per-task verification map (05-01-01..05-01-05) + Wave 0 checklist
@src/core/context/ModelContextTier.ts  # classifyModelContext + contextTierCaps under test
@src/core/context/TokenBudget.ts  # computeBudgets + DISTRIBUTION + heuristicTokenCounter under test
@src/core/context/ContextCompressor.ts  # the four strategies under test
@src/core/context/ContextPack.ts  # CANONICAL_SECTION_ORDER + pack (exercised through assemble)
@src/core/context/ContextProvenanceManifest.ts  # ContextProvenanceManifestSchema + MANIFEST_KIND_MAP under test
@src/core/context/ContextOptimizer.ts  # assemble + AssembleResult + isFeatureAllowedInMinimalMode under test
@src/core/context/types.ts  # PageContext / RetrievedMemory / ToolSchemaRef fixture shapes
@tests/core/ai/PromptCacheManager.test.ts  # canonical-order assertion pattern (36-47)
@tests/core/ai/OutcomeVerifier.test.ts  # input-builder fixture pattern (22-43)
@tests/core/ai/trajectory/TrajectoryTracker.test.ts  # closed-union completeness assertion pattern (64-81)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TokenBudget.test.ts — DONE-1 tier boundaries + budget math + distribution + CJK heuristic</name>
  <files>tests/core/context/TokenBudget.test.ts</files>
  <read_first>@src/core/context/ModelContextTier.ts (classifyModelContext + contextTierCaps under test) · @src/core/context/TokenBudget.ts (computeBudgets + DISTRIBUTION + budgetForCategory + countTokensHeuristic under test) · 05-RESEARCH.md Common Operation 1 (verbatim classify, spec 427-435) + Common Operation 2 (verbatim budgets + distribution, spec 446-461) + Pitfalls 5/7/8 + Assumption A1 (CJK ranges + 0.30 density) · 05-PATTERNS.md TokenBudget.test.ts section (coverage list) · @tests/core/ai/PromptCacheManager.test.ts (deterministic math assertion style)</read_first>
  <action>Create `tests/core/context/TokenBudget.test.ts` — pure unit tests, no chrome mocks, no providers (PromptCacheManager.test.ts style). Import from '../..' — no — use the '@/' alias: `import { classifyModelContext, contextTierCaps } from '@/core/context/ModelContextTier'` and `import { computeBudgets, DISTRIBUTION, budgetForCategory, heuristicTokenCounter } from '@/core/context/TokenBudget'`. Coverage: (1) tier boundaries verbatim — classifyModelContext(4096)==='tiny', (4097)==='small', (16384)==='small', (16385)==='medium', (131072)==='medium', (131073)==='large', (200000)==='large' (DONE-1, spec 430-434); (2) §1.4 caps table — contextTierCaps('tiny')==={plannerCap:1, toolCap:1}, ('small')==={2,1}, ('medium')==={3,2}, ('large')==={5,3}; (3) budget formula — computeBudgets(10000)==={inputBudget:7000, outputBudget:2000, safetyMargin:1000} and computeBudgets(4096)==={2867, 819, 409} (floor semantics, Pitfall 5); (4) distribution verbatim — DISTRIBUTION.tiny==={system:15, tools:20, memory:10, context:20, history:15, user:20}, small {10,15,10,25,20,20}, medium {8,12,10,30,25,15}, large {5,10,10,35,25,15}, and each tier's six values sum to 100 (Pitfall 6); (5) budgetForCategory — budgetForCategory('tiny', 7000, 'context')===1400 (floor(7000*0.20)) and budgetForCategory('large', 100000, 'system')===5000; (6) heuristic counter English — heuristicTokenCounter.count('hello world')===Math.ceil(11/4)===3 (11 ASCII chars); (7) heuristic counter CJK — a CJK-heavy fixture (e.g. a 12-char string of U+4E00-U+9FFF chars with no spaces) returns Math.ceil(12/3)===4 (density 1.0 >= 0.30); (8) density gate (Pitfall 8) — English-heavy text with a single stray CJK char stays on the len/4 formula (density below 0.30); (9) code-point awareness (Pitfall 7) — a supplementary-plane char U+20000 (e.g. '\u{20000}') counts as ONE code point: heuristicTokenCounter.count('\u{20000}')===1 (Math.ceil(1/4)); (10) empty string returns 0. No module edits — if any assertion fails, the module has a bug; fix the module in Task 3's allowed-minimal correction window (documented in verification) rather than weakening the test.</action>
  <acceptance_criteria>`npx vitest run tests/core/context/TokenBudget.test.ts` passes with all ten coverage groups green (tier boundaries verbatim, §1.4 caps, 70/20/10 floor budgets, per-tier distribution sums-to-100 with verbatim values, budgetForCategory, EN ceil(len/4), CJK ceil(len/3), density gate, surrogate-pair=1, empty=0); `grep -c "@ts-expect-error NP-STRICT" tests/core/context/TokenBudget.test.ts` equals 0.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/context/TokenBudget.test.ts</automated>
  </verify>
  <done>TokenBudget.test.ts exists and is green — DONE-1's tier/budget/distribution/heuristic proofs pinned (ModelContextTier resolver + TokenBudget per §18 DONE-when).</done>
</task>

<task type="auto">
  <name>Task 2: ContextCompressor.test.ts — D-75 strategies + summarizer-seam drop-not-silence fallback</name>
  <files>tests/core/context/ContextCompressor.test.ts</files>
  <read_first>@src/core/context/ContextCompressor.ts (compressStructural / reduceTopK / trimToolSchemas / summarizeHistory under test) · @src/core/context/types.ts (CompressionType union + Summarizer seam) · @src/core/ai/types.ts (PromptSection A8 shape — kind/text/stable/tokens, int tokens >= 0) · 05-RESEARCH.md Pattern 2 (Summarizer seam shape) + Pitfall 5 (tokens integer) + Assumption A2 (section text conventions) · 05-PATTERNS.md ContextCompressor.test.ts section (required coverage) · @tests/core/ai/trajectory/TrajectoryTracker.test.ts (closed-union completeness assertion style 64-81)</read_first>
  <action>Create `tests/core/context/ContextCompressor.test.ts` — pure module tests, no chrome mocks (TrajectoryTracker.test.ts style). Fixtures are crafted PromptSection objects (A8 shape: kind/text/stable/tokens) following the LOCKED section text conventions: [MEMORY] lines '<id>\t<content>', [TOOL SCHEMAS] lines '<name>\t<description>' name-sorted, [CONTEXT] 'URL: <url>\nTITLE: <title>\n<body>'. Coverage: (1) `compressStructural` — a [CONTEXT] section with a long body returns a PromptSection with kind 'CONTEXT', text containing 'URL:' and 'TITLE:' and only the first 40% of body chars (STRUCTURAL_COMPRESS_RATIO 0.4), tokens recomputed and integer >= 0; (2) `reduceTopK` — a 5-line [MEMORY] section with k=2 keeps exactly the first 2 lines; k=0 yields an empty-text section; (3) `trimToolSchemas` — a 3-tool [TOOL SCHEMAS] section filtered to inScopeTools ['toolB'] keeps only the toolB line and preserves name order; (4) `summarizeHistory` WITH a mock Summarizer — the history portion of [CONTEXT] is replaced by the summarizer's { text, tokens } and truncated:true is returned; (5) `summarizeHistory` WITHOUT a summarizer — older turns are dropped (keeps the last 2 lines), truncated:true is returned — drop-not-silence (D-75, never a silent drop); (6) CompressionType closed union — 'summarise' | 'structural' | 'topk' asserted via a type-level compile check or a const-array equivalence so no fourth value can be added silently; (7) every strategy output validates against the A8 shape (tokens non-negative integers). No module edits in this task; a failing assertion is a module bug for Task 3's correction window (documented in verification).</action>
  <acceptance_criteria>`npx vitest run tests/core/context/ContextCompressor.test.ts` passes with all seven coverage groups green (structural ratio + tokens, top-k first-k semantics + k=0, in-scope filter + order preservation, summarizer-seam replacement, drop-not-silence fallback, CompressionType closed union, A8-valid outputs); `grep -c "@ts-expect-error NP-STRICT" tests/core/context/ContextCompressor.test.ts` equals 0.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/context/ContextCompressor.test.ts</automated>
  </verify>
  <done>ContextCompressor.test.ts exists and is green — D-75's pure strategies + summarizer-seam fallback pinned (compressionApplied types 'summarise'|'structural'|'topk' asserted closed).</done>
</task>

<task type="auto">
  <name>Task 3: ContextOptimizer.test.ts — DONE-2/3/4 degradation + terminal + minimal mode + manifest + verify:phase-5 re-point (D-78)</name>
  <files>tests/core/context/ContextOptimizer.test.ts, package.json</files>
  <read_first>@src/core/context/ContextOptimizer.ts (assemble / AssembleResult / isFeatureAllowedInMinimalMode / BLOCKED_IN_MINIMAL_MODE under test) · @src/core/context/ContextProvenanceManifest.ts (ContextProvenanceManifestSchema parse) · @src/core/context/types.ts (fixture shapes) · 05-RESEARCH.md Pattern 1 (AssembleResult union shape) + Pattern 4 (predicate + blocked literals verbatim) + Common Operation 4 (§2.6 manifest verbatim) + Common Operation 6 (§2.5 lists verbatim) + Pitfall 9 (truncatedSources derivation) · 05-PATTERNS.md ContextOptimizer.test.ts section (input-builder pattern from OutcomeVerifier.test.ts:22-43, canonical-order pattern from PromptCacheManager.test.ts:36-47) · 05-VALIDATION.md per-task map (05-01-02/03/04 rows) · package.json line 23 (the mis-pointed verify:phase-5 — CURRENT: 'tsc --noEmit && vitest run tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts')</read_first>
  <action>First edit package.json line 23 per D-78 (the D-68 precedent, 04-PATTERNS.md:358-361): change `"verify:phase-5": "tsc --noEmit && vitest run tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts"` to `"verify:phase-5": "tsc --noEmit && vitest run tests/core/context"` (spec 3609 canonical). Do NOT touch verify:phase-8 (it legitimately references tests/core/memory — Phase 8 territory) or any other script. Then create `tests/core/context/ContextOptimizer.test.ts` — pure unit tests, no chrome mocks (OutcomeVerifier.test.ts style). Build a `makeInput(overrides?: Partial<ContextOptimizerInput>): ContextOptimizerInput` fixture builder (mirroring OutcomeVerifier.test.ts:23-32) with REQUIRED defaults: operationId 'op-test', model 'fixture-model', modelContextWindow 16384, userInput 'Summarize the current incident', conversationId 'conv-1', workspaceId 'ws-1', activeSurface 'sidepanel', selectedToolSchemas [ToolSchemaRef {name:'toolA', description:'a', jsonSchema:{}, dangerous:false, source:'builtin'}], memoryHints [RetrievedMemory {id:'m1', content:'user prefers concise', type:'preference', tags:[], score:0.9}], preferences {} (UserPreferences). Import assemble, isFeatureAllowedInMinimalMode, BLOCKED_IN_MINIMAL_MODE from '@/core/context/ContextOptimizer' and ContextProvenanceManifestSchema from '@/core/context/ContextProvenanceManifest'.

Coverage (DONE-2/3/4): (1) happy path — makeInput() returns ok:true; context.tier==='small'; context.inputBudget===floor(16384*0.70)===11468; context.outputBudget===floor(16384*0.20)===3276; context.sections.map(s=>s.kind) equals ['TOOL SCHEMAS','USER PREFERENCES','MEMORY','CONTEXT','USER INPUT'] (the sourced five in §1.3 canonical order — D-76 exercised through assemble; Pitfall 4: not alphabetical); context.minimalMode===false; context.provenance.totalTokens===sum of section tokens; (2) manifest on every context (DONE-4) — ContextProvenanceManifestSchema.parse(context.provenance) succeeds; provenance.sections has 7 records (5 shipped + the 'system'/'task' by-design omission records with truncated:true and tokens 0); provenance.workspaceId==='ws-1' and provenance.activeSurface==='sidepanel' (Q6 fields flow through); the shipped records' kinds are the lowercase manifest kinds ('tool_schemas','preferences','memory','context','user_input') via MANIFEST_KIND_MAP; (3) trace surface — happy path context.truncated===false and context.truncatedSources equals [] (the 'system'/'task' omission records are EXCLUDED from the degradation trace per the locked derivation); context.contextTier==='small'; (4) tiny tier forces minimal mode (DONE-3) — makeInput({modelContextWindow: 4096}) returns ok:true with context.minimalMode===true (mandatory, spec 506); (5) minimal-mode predicate verbatim (D-74) — for each of the seven BLOCKED_IN_MINIMAL_MODE literals ['multi-step-agent','mcp-chaining','code-search-skill','full-note-graph-injection','large-research-synthesis','llm-wiki-bulk','llm-wiki-rag'], isFeatureAllowedInMinimalMode(literal)===false; allowed behaviors ('compact-system-prompt','compact-preference-profile','top-3-memories','conversation-summary-200','last-1-2-turns','one-safe-tool-schema') and unknown strings ('anything-else') return true; (6) stepwise degradation — a pageContext fixture with a large body (sized so the five-section total exceeds inputBudget at 16384 but fits after structural compression) returns ok:true with the CONTEXT manifest record truncated:true and compressionApplied==='structural', and truncatedSources includes the page URL — the rung-4 observable (DONE-2); a memoryHints fixture with many hints (sized to overflow) triggers the topk rung — the MEMORY record has compressionApplied==='topk'; (7) minimal mode is the PENULTIMATE rung — a fixture that overflows through rungs 4/5/6 returns ok:true with context.minimalMode===true (entered before the terminal) and the memory section reduced to at most 3 lines and tool schemas to at most 1 safe (dangerous:false) tool per §2.5; (8) never oversized (DONE-2) — for every ok:true result in the suite, context.provenance.totalTokens <= context.inputBudget (assert in a loop over all success fixtures); (9) CONTEXT_TOO_LARGE terminal RETURNED, never thrown (DONE-2/Q4) — makeInput({modelContextWindow: 4096, userInput: <a string sized so its ceil(len/4) tokens alone exceed the tiny inputBudget 2867>}) returns an object with ok:false, code==='CONTEXT_TOO_LARGE', a non-empty message that does NOT contain the userInput text, totalTokens >= inputBudget, minimalMode===true, and truncatedSources as an array — assert with a plain expect, no try/catch (the terminal is a return value, not an exception; the message is the fixed user-facing explanation naming budgets only — never raw content, T-05-03); (10) pack fidelity through assemble — context.sections stay in §1.3 order and provenance.totalTokens equals the sum of section tokens (D-76 tally). No module edits in this task unless a test exposes a genuine module bug — in that case fix the module minimally and re-run; never weaken a test to force green.
<!-- planner-discipline-allow: tests/core/memory -->
</action>
  <acceptance_criteria>`node -e "const p=require('./package.json'); if (p.scripts['verify:phase-5'] !== 'tsc --noEmit && vitest run tests/core/context') process.exit(1)"` passes (D-78 re-point exact — old mis-pointed targets gone from the script); `npx vitest run tests/core/context/ContextOptimizer.test.ts` passes all ten coverage groups (happy path order/budgets, schema-parsed manifest with 7 records + workspaceId/activeSurface, empty trace on happy path, tiny=>minimalMode, predicate verbatim both lists, structural/topk rung observables, penultimate minimal mode, never-oversized loop, CONTEXT_TOO_LARGE returned with non-content message, pack tally); `pnpm run verify:phase-5` passes GREEN (tsc strict-clean + all three tests/core/context files); `grep -c "@ts-expect-error NP-STRICT" tests/core/context/ContextOptimizer.test.ts` equals 0; `grep -c "tests/core/memory" package.json` is 1 (only verify:phase-8's legit Phase-8 reference remains).</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-5</automated>
  </verify>
  <done>verify:phase-5 GREEN for the first time (D-78 closed — the gate stops being RED); all three §18-required test files exist and pass; DONE-1..4 pinned by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests ← assemble() | Malicious-shaped fixtures (oversized pageContext bodies, huge userInput, hostile memory content) cross into the degradation pipeline — the cost-abuse / never-oversized boundary under test |
| tests ← manifest/trace | Test assertions read provenance records — must prove sourceIds-only (no raw bodies leak into truncatedSources or the CONTEXT_TOO_LARGE message) |
| package.json ← verify:phase-5 | The gate script — a mis-edit could silently drop coverage (the reason the re-point is asserted exactly via node -e) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | DoS | assemble() overflow fixtures | high | mitigate | Test (8) asserts provenance.totalTokens <= inputBudget for every ok:true fixture and test (9) asserts the CONTEXT_TOO_LARGE terminal is RETURNED (never thrown, never an oversized context) — the never-oversized guarantee is regression-pinned |
| T-05-02 | Spoofing | isFeatureAllowedInMinimalMode | medium | mitigate | Test (5) asserts the §2.5 blocked set verbatim (7 literals => false) AND allowed behaviors/unknowns => true — the closed-set predicate is regression-pinned (D-74) |
| T-05-03 | Information Disclosure | CONTEXT_TOO_LARGE.message / truncatedSources | high | mitigate | Test (9) asserts the message does NOT contain the userInput text; test (3)/(6) assert truncatedSources carries sourceIds only (page URL / memory ids), never section bodies — the V6/TraceRedactor discipline is regression-pinned |
| T-05-04 | Tampering | package.json verify:phase-5 | medium | mitigate | The acceptance criteria assert the exact script string via node -e (old mis-pointed targets gone; verify:phase-8's legit reference preserved) — a scope-reducing gate edit is caught |
| T-05-SC | Tampering | package installs | high | mitigate | Package legitimacy gate N/A — zero new dependencies this phase; no install commands exist in this plan (RESEARCH Standard Stack verified) |
</threat_model>

<verification>
- `npx vitest run tests/core/context` green after every task commit (targeted file per task; full dir at close).
- `pnpm run lint` green after every task commit (strict-clean — the gate runs tsc --noEmit first).
- Grep guard: zero `@ts-expect-error NP-STRICT` markers in the three test files (STATE.md decision 17).
- D-78 re-point asserted EXACTLY via `node -e` (not a loose grep — grep -c "tests/core/memory" would count verify:phase-8's legit reference and self-invalidate).
- Module-fix window (documented acceptance): Tasks 1-2 are test-only; if a test exposes a genuine module bug, the fix lands in Task 3's window (minimal module edit + re-run both the failing test and the full gate) — a module bug is fixed, a test is never weakened. Task 3's acceptance includes the full `pnpm run verify:phase-5` so any Task 1/2 module fix is re-proven by the whole dir.
- Gate progression: 05-01 closed with the modules type-clean; this plan closes with `verify:phase-5` GREEN for the first time (D-78) — the phase's §18 DONE-when is then fully automated.
</verification>

<success_criteria>
1. The three §18-required test files exist under `tests/core/context/` and pass (spec 2596-2598).
2. DONE-1 pinned: tier boundary fixtures verbatim + budget math + distribution + CJK heuristic + §1.4 caps table.
3. DONE-2 pinned: overflow degrades stepwise in §2.4 order; never oversized; CONTEXT_TOO_LARGE terminal RETURNED with the locked union fields and a content-free message.
4. DONE-3 pinned: minimal-mode predicate asserts the §2.5 blocked/allowed lists verbatim; tiny => minimalMode always true.
5. DONE-4 pinned: every OptimizedContext carries a §2.6-verbatim schema-parsed manifest; truncatedSources derives from manifest truncated sections.
6. `pnpm run verify:phase-5` GREEN — the D-78 re-point closes the mis-pointed gate (currently RED, 'No test files found').
</success_criteria>

<output>
Create `.planning/phases/05-context-adaptive-execution/05-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-5 inventory — continuation of 05-01's table. `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/context/types.ts | PageContext / RetrievedMemory / ToolSchemaRef supersession shapes, CompressionType, Summarizer seam | 05-01 |
| src/core/context/ModelContextTier.ts | ModelContextTier + classifyModelContext + contextTierCaps (unwired) | 05-01 |
| src/core/context/TokenBudget.ts | TokenCounter + heuristicTokenCounter + computeBudgets + DISTRIBUTION + budgetForCategory | 05-01 |
| src/core/context/ContextCompressor.ts | compressStructural / reduceTopK / trimToolSchemas / summarizeHistory | 05-01 |
| src/core/context/ContextPack.ts | CANONICAL_SECTION_ORDER + pack | 05-01 |
| src/core/context/ContextProvenanceManifest.ts | ManifestKindSchema + ContextProvenanceManifestSchema + buildManifest + MANIFEST_KIND_MAP | 05-01 |
| src/core/context/ContextOptimizer.ts | ContextOptimizerInput + OptimizedContext + AssembleResult + assemble + isFeatureAllowedInMinimalMode + PromptSection re-export + trace surface | 05-01 |
| tests/core/context/TokenBudget.test.ts | DONE-1 proofs: tier boundaries, budgets, distribution, CJK heuristic, §1.4 caps | **[this plan]** |
| tests/core/context/ContextCompressor.test.ts | D-75 proofs: strategies + summarizer-seam drop-not-silence fallback + closed CompressionType | **[this plan]** |
| tests/core/context/ContextOptimizer.test.ts | DONE-2/3/4 proofs: degradation ladder, terminal, minimal mode, manifest, order, never-oversized | **[this plan]** |
| package.json | verify:phase-5 re-pointed to tests/core/context (D-78) — the only config change of the phase | **[this plan]** |

Phase close: `pnpm run verify:phase-5` GREEN = the §18 DONE-when list (tier tests pass · overflow degrades stepwise with truncatedSources · minimal mode blocks MCP chaining/LLM-Wiki RAG via the typed predicate · manifest on every OptimizedContext) is fully automated.