---
phase: 05-context-adaptive-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/context/types.ts
  - src/core/context/ModelContextTier.ts
  - src/core/context/TokenBudget.ts
  - src/core/context/ContextCompressor.ts
  - src/core/context/ContextPack.ts
  - src/core/context/ContextProvenanceManifest.ts
  - src/core/context/ContextOptimizer.ts
autonomous: true
requirements: []   # infra phase — no spec-native v1 IDs (ROADMAP Phase 5 note); acceptance = §18 DONE-when + D-69..D-78
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - statement: "classifyModelContext hits the §2.1 boundaries verbatim (4096→tiny, 16384→small, 131072→medium, else large) and ModelContextTier is a closed 4-value union — distinct from the Phase-3 ModelTier fast/balanced axis (D-70, DONE-1)"
      verification: backstop
    - statement: "TokenBudget computes inputBudget=floor(window*0.70), outputBudget=floor(window*0.20), safetyMargin=floor(window*0.10); the six-category per-tier distribution is the §2.2 table verbatim (each tier sums to 100); budgetForCategory = floor(inputBudget*pct/100); heuristic counter is ceil(len/4) English / ceil(len/3) CJK with a 0.30 density gate, code-point-aware, integer tokens only (D-71, DONE-1)"
      verification: backstop
    - statement: "assemble() is a pure function returning the AssembleResult discriminated union — the ok:false variant carries code:'CONTEXT_TOO_LARGE' (canonical §21.6 literal, spec 3435/5079), a user-facing message without raw content, totalTokens, inputBudget, minimalMode, truncatedSources; never a throw; StreamErrorCodeSchema untouched (D-38, DONE-2)"
      verification: backstop
    - statement: "ContextOptimizer re-exports PromptSection from src/core/ai/types (D-72) — single source of truth; PromptCacheAdapter's spec import-target note resolves"
      verification: backstop
    - statement: "Every OptimizedContext carries a §2.6-verbatim ContextProvenanceManifest (schema-parsed) with the 7-entry kind mapping (uppercase-spaced A8 kind → lowercase manifest kind), workspaceId/activeSurface REQUIRED with no defaults (D-77, DONE-4, Q6)"
      verification: backstop
    - statement: "minimalMode is mandatory for tiny tiers (spec 506) and entered as the penultimate §2.4 degradation rung; isFeatureAllowedInMinimalMode asserts the §2.5 blocked set verbatim (7 kebab-case literals) and defaults unknown features to allowed (D-74, DONE-3)"
      verification: backstop
    - statement: "The degradation ladder walks the §2.4 order (drop debug → drop secondary notes → summarise history → structural-compress page/case → trim tool schemas → reduce memory top-k → minimal mode → CONTEXT_TOO_LARGE); no ok:true context ever exceeds inputBudget; truncatedSources derives from manifest truncated section sourceIds excluding the by-design 'system'/'task' omission records (D-73/D-77, DONE-2)"
      verification: backstop
  artifacts:
    - path: "src/core/context/types.ts"
      provides: "Discretion-sanctioned sibling: minimal PageContext/RetrievedMemory/ToolSchemaRef supersession shapes (verbatim spec 4346-4357 / 4572-4578 / 4601-4607 with Phase-6/8 supersession comments), CompressionType, Summarizer seam"
      exports: ["PageContext", "RetrievedMemory", "ToolSchemaRef", "CompressionType", "Summarizer"]
    - path: "src/core/context/ModelContextTier.ts"
      provides: "§2.1 classifyModelContext + ModelContextTierSchema + unwired §1.4 contextTierCaps helper (tiny 1/1 · small 2/1 · medium 3/2 · large 5/3)"
      exports: ["ModelContextTier", "ModelContextTierSchema", "classifyModelContext", "contextTierCaps"]
    - path: "src/core/context/TokenBudget.ts"
      provides: "§2.2 budget formula + six-category dynamic distribution + TokenCounter seam (D-71) with the CJK-aware heuristicTokenCounter default"
      exports: ["TokenCounter", "heuristicTokenCounter", "countTokensHeuristic", "computeBudgets", "DISTRIBUTION", "BudgetCategory", "budgetForCategory"]
    - path: "src/core/context/ContextCompressor.ts"
      provides: "D-75 pure strategies — compressStructural / reduceTopK / trimToolSchemas — plus the summarizeHistory summarizer-seam wrapper with drop-older-turns fallback (never silent)"
      exports: ["compressStructural", "reduceTopK", "trimToolSchemas", "summarizeHistory"]
    - path: "src/core/context/ContextPack.ts"
      provides: "D-76 deterministic §1.3 canonical-order assembly (kind→index table, never alphabetical sort) + totalTokens tally"
      exports: ["CANONICAL_SECTION_ORDER", "pack"]
    - path: "src/core/context/ContextProvenanceManifest.ts"
      provides: "D-77 §2.6-verbatim manifest zod schema + buildManifest builder + 7-entry MANIFEST_KIND_MAP"
      exports: ["ManifestKind", "ManifestKindSchema", "ContextProvenanceManifestSchema", "ContextProvenanceManifest", "buildManifest", "MANIFEST_KIND_MAP"]
    - path: "src/core/context/ContextOptimizer.ts"
      provides: "§2.3 contract + assemble() pure function + §2.4 degradation ladder + D-74 minimal-mode predicate + AssembleResult union + D-77 trace surface + A8 PromptSection re-export (D-72)"
      exports: ["ContextOptimizerInput", "OptimizedContext", "AssembleResult", "assemble", "isFeatureAllowedInMinimalMode", "BlockedFeature", "BLOCKED_IN_MINIMAL_MODE", "PromptSection"]
  key_links:
    - from: "src/core/context/ContextOptimizer.ts"
      to: "src/core/ai/types.ts"
      via: "export type { PromptSection } from '../ai/types' — D-72 re-export; resolves PromptCacheAdapter.ts:6-7 spec import-target note ('../context/ContextOptimizer')"
      pattern: "export type \\{ PromptSection \\} from '../ai/types'"
    - from: "src/core/context/ContextOptimizer.ts"
      to: "src/core/context/ContextProvenanceManifest.ts"
      via: "OptimizedContext.provenance built by buildManifest; truncatedSources derived from manifest truncated sections (D-77, Pitfall 9)"
      pattern: "buildManifest"
    - from: "src/core/context/ContextPack.ts"
      to: "src/core/ai/PromptCacheManager.ts"
      via: "§1.3 canonical order + stable-first byte-stability — the ordering contract PromptCacheAdapter.hashStableSections (FNV-1a) hashes; ContextPack emits in hard-coded §1.3 order, never alphabetical (Pitfall 4)"
      pattern: "CANONICAL_SECTION_ORDER"
    - from: "src/core/context/ContextOptimizer.ts"
      to: "src/core/context/ContextCompressor.ts"
      via: "the §2.4 degradation ladder invokes compressStructural / reduceTopK / trimToolSchemas / summarizeHistory; compression type recorded per section as compressionApplied ('summarise'|'structural'|'topk', D-75)"
      pattern: "compressStructural|reduceTopK|trimToolSchemas|summarizeHistory"
  prohibitions:
    - "No edit to any src/core/ai/* file (D-69/D-70/D-72): the PromptSection re-export is additive inside the NEW ContextOptimizer.ts; AgentOrchestrator, TierResolver, PromptCacheManager, PromptCacheAdapter, types.ts stay byte-identical — git-diff-assertable"
    - "No new error codes (D-38): CONTEXT_TOO_LARGE is the canonical §21.6 literal (spec 3435/5079); StreamErrorCodeSchema is untouched"
    - "No barrel index.ts in src/core/context/ — mirror the src/core/ai layout convention (direct path imports only)"
    - "No pipeline wiring (D-69): zero imports of src/core/context/* from src/components or src/core/ai/* in this plan"
    - "Zero @ts-expect-error NP-STRICT markers in all seven modules (STATE.md decision 17 — new code strict-clean)"
    - "No silent truncation (D-73/D-77, Pitfall 9): every degraded/compressed section is recorded in the manifest with truncated:true; system/task are recorded as by-design omission records (truncated:true, 0 tokens) but excluded from the truncatedSources trace"
---

<objective>
TRACER plan: build the complete §18 context-layer inventory — `src/core/context/types.ts`, `ModelContextTier.ts`, `TokenBudget.ts`, `ContextCompressor.ts`, `ContextPack.ts`, `ContextProvenanceManifest.ts`, `ContextOptimizer.ts` — wired so ONE end-to-end `assemble(ContextOptimizerInput)` call traverses every layer the phase introduces: tier classification → budget computation → per-section token counting → (overflow → §2.4 degradation ladder) → §1.3 canonical packing → §2.6 provenance manifest → OptimizedContext with the derived trace surface.

Purpose: prove the context spine type-checks end-to-end (ContextOptimizerInput → AssembleResult → OptimizedContext → manifest; the D-72 PromptSection re-export resolves against the Phase-3 A8 contract) and that every §18 module exists with the locked D-69..D-78 semantics, before the §18 test files (plan 05-02) pin the behavior. This is the phase's thinnest vertical slice: the pipeline is linear, so the slice IS the full module set — there is no thinner path through every layer. No pipeline wiring (D-69): zero imports from `src/components` or `src/core/ai/*` beyond the type-only re-export.

Output: the seven `src/core/context/` modules, strict-clean, with the six research open questions locked as decisions (Q1 types.ts home · Q2 budget→section mapping · Q3 system/task recorded as omitted · Q4 CONTEXT_TOO_LARGE returned union fields · Q5 §1.4 caps helper shipped unwired · Q6 workspaceId/activeSurface required).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  # Phase 5 block (168-180): goal, success criteria, gate
@.planning/STATE.md  # decision 17: strict-clean, zero NP-STRICT markers
@.planning/PRODUCT_SPEC_v0_1.md  # §2.1 (425-443), §2.2 (444-461), §2.3 (463-489), §2.4 (491-502), §2.5 (504-524), §2.6 (526-544), §1.3 (331-350), §1.4 (352-361), §19.2 (2991-2997), §19.3 (2999-3004), PromptTrace (732-756), §21.6 (3402-3454), Appendix C PageContext/RetrievedMemory/ToolSchemaRef (4345-4357, 4572-4578, 4601-4607), §18 Phase 5 (2580-2607)
@.planning/phases/05-context-adaptive-execution/05-CONTEXT.md  # D-69..D-78 locked decisions + discretion items
@.planning/phases/05-context-adaptive-execution/05-RESEARCH.md  # Patterns 1-4, Common Operations 1-6, Pitfalls 1-10, Assumptions A1-A7, Open Questions 1-6, Security Domain
@.planning/phases/05-context-adaptive-execution/05-PATTERNS.md  # all seven module sections + shared patterns (closed unions, supersession points, stable-first, returned results)
@src/core/ai/types.ts  # A8 PromptSectionSchema (95-101) — the D-72 re-export target; ModelTierSchema (27-28) — the z.enum pattern; StreamErrorCodeSchema (55-64) — must stay untouched (D-38)
@src/core/ai/PromptCacheManager.ts  # buildSystemPrompt §1.3 assembly (93-133) + estimateTokens (34-36) — the ordering/stable contract ContextPack preserves; kind convention uppercase-spaced
@src/core/ai/PromptCacheAdapter.ts  # import-target note (6-7) + stableFirst (77-80) — cache-adaptation sort, NOT canonical order (Pitfall 4)
@src/core/ai/AgentOrchestrator.ts  # AgentTier (48-55) — the fast/balanced axis D-70 keeps separate; DO NOT modify
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Context spine — the seven §18 modules wired end-to-end through one assemble() happy path</name>
  <files>src/core/context/types.ts, src/core/context/ModelContextTier.ts, src/core/context/TokenBudget.ts, src/core/context/ContextCompressor.ts, src/core/context/ContextPack.ts, src/core/context/ContextProvenanceManifest.ts, src/core/context/ContextOptimizer.ts</files>
  <read_first>05-CONTEXT.md D-69..D-78 (locked) + the agent's Discretion items · 05-RESEARCH.md Patterns 1-4 + Common Operations 1-6 (verbatim contracts) + Pitfalls 3/4/5/6/7/8/9/10 + Assumptions A2/A3/A5/A6/A7 + Open Questions 1-6 recommendations · 05-PATTERNS.md all seven module sections (analog excerpts) · PRODUCT_SPEC §2.1-§2.6 (425-544) + §1.3 (331-350) + §1.4 (352-361) + §19.2/§19.3 (2991-3004) + Appendix C shapes (4345-4357, 4572-4578, 4601-4607) + §21.6 closed codes (3402-3454) · @src/core/ai/types.ts (PromptSection 95-101 — the re-export target; ModelTierSchema 27-28 — z.enum pattern; StreamErrorCodeSchema 55-64 — untouched) · @src/core/ai/PromptCacheManager.ts (buildSystemPrompt 93-133 — §1.3 order + stable/tokens tagging; estimateTokens 34-36) · @src/core/ai/UserPreferences.ts (supersession-point header pattern 1-14) · @src/core/ai/trajectory.ts (closed-union throw-on-illegal pattern 29-75)</read_first>
  <action>Create the seven modules in dependency order (types.ts → ModelContextTier.ts → TokenBudget.ts → ContextCompressor.ts → ContextPack.ts → ContextProvenanceManifest.ts → ContextOptimizer.ts). All modules are pure TypeScript: zero React, zero chrome API imports, zero storage access, strict-clean (zero @ts-expect-error NP-STRICT markers). Locked decision Q1: no barrel index.ts — direct path imports only (mirror src/core/ai).

1. `src/core/context/types.ts` — header comment in the UserPreferences.ts supersession-point style naming the owning phases: minimal PageContext (Phase 6 replaces in place at src/core/content/PageContext.ts), RetrievedMemory (Phase 8 replaces at src/core/memory/types.ts), ToolSchemaRef (Phase 18 / src/core/ai/toolSchemas.ts owns the real registry type). Declare verbatim from spec: PageContext { url, origin, hostname, title, html?, markdown?, meta: Record<string,string>, extractedAt: number, addonId?, addonFields? } (spec 4346-4357); RetrievedMemory { id, content, type: 'fact'|'preference'|'pattern', tags: string[], score: number } (spec 4572-4578); ToolSchemaRef { name, description, jsonSchema: unknown, dangerous: boolean, source: 'builtin'|'mcp'|'skill'|'servicenow' } (spec 4601-4607). Also declare `CompressionType = 'summarise' | 'structural' | 'topk'` (the §2.6 compressionApplied union, spec 537) and the `Summarizer` seam interface `{ summarize(sections: PromptSection[]): { text: string; tokens: number } }` (D-75) — import PromptSection type-only from '../ai/types'. These are plain TS interfaces (Phase 6/8 own the zod versions; the UserPreferences precedent).

2. `src/core/context/ModelContextTier.ts` — `ModelContextTierSchema = z.enum(['tiny', 'small', 'medium', 'large'])` + inferred type (mirror ModelTierSchema at types.ts:27-28). `classifyModelContext(contextWindow: number): ModelContextTier` verbatim §2.1 (spec 429-434): contextWindow <= 4096 → 'tiny'; <= 16384 → 'small'; <= 131072 → 'medium'; else 'large'. Export `contextTierCaps(tier: ModelContextTier): { plannerCap: number; toolCap: number }` per the §1.4 table (spec 356-359): tiny {1,1}, small {2,1}, medium {3,2}, large {5,3} — an UNWIRED helper with a comment: Appendix I owns enforcement; AgentOrchestrator receives caps as caller-supplied AgentTier (D-70); zero production call sites this phase. Header comment: ModelContextTier is the context-window axis, DISTINCT from the Phase-3 ModelTier fast/balanced runtime axis — do not import ModelTier here, do not touch AgentOrchestrator/TierResolver (D-70).

3. `src/core/context/TokenBudget.ts` — `TokenCounter` interface `{ count(text: string): number }` (D-71 seam). `countTokensHeuristic(text: string): number` — code-point-aware (Array.from(text)), CJK-density-gated: named constant `CJK_DENSITY_THRESHOLD = 0.30`; CJK ranges constant covering U+4E00–U+9FFF, U+3400–U+4DBF, U+3040–U+309F, U+30A0–U+30FF, U+AC00–U+D7AF, U+F900–U+FAFF; density = CJK code points / non-whitespace code points (/\s/u); density >= threshold → Math.ceil(codePoints/3), else Math.ceil(codePoints/4); empty text → 0; always an integer >= 0 (Pitfalls 5/7/8). `heuristicTokenCounter: TokenCounter = { count: countTokensHeuristic }` (D-71 default). `computeBudgets(modelContextWindow: number): { inputBudget: number; outputBudget: number; safetyMargin: number }` — verbatim §2.2 (spec 447-449): floor(window*0.70), floor(window*0.20), floor(window*0.10). `BudgetCategory = 'system' | 'tools' | 'memory' | 'context' | 'history' | 'user'`; `DISTRIBUTION: Record<ModelContextTier, Record<BudgetCategory, number>>` verbatim §2.2 table (spec 456-459): tiny {system:15, tools:20, memory:10, context:20, history:15, user:20}, small {10,15,10,25,20,20}, medium {8,12,10,30,25,15}, large {5,10,10,35,25,15} — each tier sums to 100. `budgetForCategory(tier: ModelContextTier, inputBudget: number, category: BudgetCategory): number` = Math.floor(inputBudget * pct / 100). Comment the LOCKED budget→section mapping (Assumption A2): system→[SYSTEM] (Phase-7 caller's buildSystemPrompt output), tools→[TOOL SCHEMAS], memory→[MEMORY], context→[CONTEXT] (page/case), history→older turns INSIDE [CONTEXT] (the manifest has no 'history' kind), user→[USER PREFERENCES]+[USER INPUT]. Phase-5 assemble enforces the AGGREGATE inputBudget (never oversized, §19.3); per-category enforcement is Phase-7 caller policy.

4. `src/core/context/ContextCompressor.ts` — D-75 pure strategies operating on the A8 sections assemble passes them, each recomputing tokens via heuristicTokenCounter and returning valid PromptSection(s) (int tokens >= 0). Section text conventions (LOCKED, shared with ContextOptimizer): [MEMORY] = one '<id>\t<content>' line per memory hint; [TOOL SCHEMAS] = one '<name>\t<description>' line per tool, lines name-sorted (stable tool-name sort, §1.3); [CONTEXT] = 'URL: <url>\nTITLE: <title>\n<body>' (+ history turns as 'TURN <ts>: <text>' lines in Phase 7). Exports: `compressStructural(section: PromptSection): PromptSection` — structural-compresses a [CONTEXT] section to its structured fields (URL/TITLE + the first 40% of body chars, named constant STRUCTURAL_COMPRESS_RATIO = 0.4) and recomputes tokens; `reduceTopK(sections: PromptSection[], k: number): PromptSection[]` — keeps the first k lines of the [MEMORY] section, drops the rest (memory top-k, §2.4 rung 6); `trimToolSchemas(sections: PromptSection[], inScopeTools: readonly string[]): PromptSection[]` — filters the [TOOL SCHEMAS] section to lines whose tab-prefixed name is in inScopeTools; `summarizeHistory(sections: PromptSection[], summarizer?: Summarizer): { sections: PromptSection[]; truncated: boolean }` — when a Summarizer is supplied, replaces the history portion of [CONTEXT] with its { text, tokens }; when absent, DROPS older turns (keeps the last 2) and returns truncated:true — drop-not-silence (D-75). Header comment: Phase 5 never calls the LLM; the summarizer seam is declare-now/populate-later (D-46/D-64 precedent).

5. `src/core/context/ContextPack.ts` — D-76. `CANONICAL_SECTION_ORDER: readonly string[] = ['SYSTEM', 'TOOL SCHEMAS', 'USER PREFERENCES', 'MEMORY', 'CONTEXT', 'TASK', 'USER INPUT']` (verbatim §1.3, spec 331-339). `pack(sections: PromptSection[]): { prompt: string; totalTokens: number }` — deterministic: reorders sections by kind→index from CANONICAL_SECTION_ORDER (never alphabetical sort — Pitfall 4; the stableFirst sort in PromptCacheAdapter is cache adaptation, NOT canonical order), throws Error on a section kind outside the table (closed-union discipline, mirrors trajectory.ts illegal-transition throw), joins section texts with '\n\n', totalTokens = Σ section.tokens. The prompt string is the materialization seam: Phase-7 consumers call pack(context.sections) at the caching boundary (PromptCacheManager/PromptCacheAdapter stable-first, Appendix K).

6. `src/core/context/ContextProvenanceManifest.ts` — D-77, §2.6 verbatim (spec 530-544). `ManifestKindSchema = z.enum(['system', 'tool_schemas', 'preferences', 'memory', 'context', 'task', 'user_input'])` + type. `ContextProvenanceManifestSchema = z.object({ sections: z.array(z.object({ kind: ManifestKindSchema, sourceId: z.string(), tokens: z.number().int().nonnegative(), truncated: z.boolean(), compressionApplied: CompressionTypeSchema.optional() })), totalTokens: z.number().int().nonnegative(), minimalMode: z.boolean(), workspaceId: z.string(), activeSurface: z.enum(['sidepanel', 'standalone']) })` (CompressionTypeSchema = z.enum(['summarise','structural','topk']) local to this module). `MANIFEST_KIND_MAP: Record<string, ManifestKind>` — the LOCKED 7-entry casing table (Pitfall 3): 'SYSTEM'→'system', 'TOOL SCHEMAS'→'tool_schemas', 'USER PREFERENCES'→'preferences', 'MEMORY'→'memory', 'CONTEXT'→'context', 'TASK'→'task', 'USER INPUT'→'user_input'. `buildManifest(input: { sectionRecords: ManifestSectionRecord[]; totalTokens: number; minimalMode: boolean; workspaceId: string; activeSurface: 'sidepanel' | 'standalone' }): ContextProvenanceManifest` where ManifestSectionRecord = { kind: ManifestKind; sourceId: string; tokens: number; truncated: boolean; compressionApplied?: CompressionType } — the builder maps the shipped-section records and ALWAYS appends the two by-design omission records for 'system' and 'task' (truncated: true, tokens: 0, sourceId 'system' / 'task') so the manifest is the complete 7-kind receipt (Q3 LOCKED: assemble emits only the sourced five kinds; system/task have no input source in the §2.3 verbatim contract and are recorded as omitted — Phase-7 CTX-03 'inclusion/omission' semantics). Do NOT add sourceId to the A8 PromptSection (that would edit the Phase-3 file).

7. `src/core/context/ContextOptimizer.ts` — the spine. Line 1: `export type { PromptSection } from '../ai/types';` (D-72 re-export — the ONLY cross-module dependency on Phase 3). Declare `ContextOptimizerInput` verbatim §2.3 (spec 466-478) — operationId, model, modelContextWindow, userInput, conversationId, workspaceId, activeSurface: 'sidepanel'|'standalone', pageContext?: PageContext, selectedToolSchemas: ToolSchemaRef[], memoryHints: RetrievedMemory[], preferences: UserPreferences — with workspaceId and activeSurface REQUIRED, no defaults (Q6 LOCKED, Assumption A5: empty-string defaults would poison provenance records). Declare `OptimizedContext` verbatim §2.3 (spec 479-486) — tier, inputBudget, outputBudget, sections, provenance, minimalMode — PLUS the additive D-77 trace surface: contextTier: ModelContextTier, truncated: boolean, truncatedSources: string[], minimalMode: boolean (contextTier mirrors tier — the Phase-11 PromptTrace field name, spec 744; document the redundancy). Declare `BlockedFeature` = the §2.5 blocked set verbatim (spec 519-524) as kebab-case literals: 'multi-step-agent' | 'mcp-chaining' | 'code-search-skill' | 'full-note-graph-injection' | 'large-research-synthesis' | 'llm-wiki-bulk' | 'llm-wiki-rag'; `BLOCKED_IN_MINIMAL_MODE: readonly BlockedFeature[]` with all seven; `isFeatureAllowedInMinimalMode(feature: string): boolean` = NOT blocked; unknown features default to ALLOWED (D-74, closed-set discipline). Declare `AssembleResult` (Q4 LOCKED — returned discriminated union, never a throw): `{ ok: true; context: OptimizedContext } | { ok: false; code: 'CONTEXT_TOO_LARGE'; message: string; totalTokens: number; inputBudget: number; minimalMode: boolean; truncatedSources: string[] }` — the code literal is the canonical §21.6 member (spec 3435/5079), do NOT touch StreamErrorCodeSchema.

`assemble(input: ContextOptimizerInput): AssembleResult` — pure, synchronous. Step sequence: (1) tier = classifyModelContext(input.modelContextWindow); (2) budgets = computeBudgets(input.modelContextWindow); (3) minimalMode = tier === 'tiny' (mandatory, spec 506); (4) build the five sourced sections in §1.3 canonical order: [TOOL SCHEMAS] from selectedToolSchemas (one '<name>\t<description>' line each, name-sorted, stable:true — tool schemas are cache-eligible §1.3), [USER PREFERENCES] compact rendering mirroring PromptCacheManager.prefsCompact style (stable:false), [MEMORY] from memoryHints (one '<id>\t<content>' line each, all hints initially, stable:false), [CONTEXT] from pageContext when present ('URL: <url>\nTITLE: <title>\n<body>' from markdown ?? html-stripped ?? title, stable:false), [USER INPUT] = input.userInput (stable:false, never cached §1.3); (5) estimate each section's tokens via heuristicTokenCounter; (6) totalTokens = Σ; if totalTokens > budgets.inputBudget, walk the §2.4 degradation ladder (D-73) in EXACT order, re-tallying after each rung: rung 1 'drop debug-only context' = RESERVED no-op rung (no debug source in the §2.3 verbatim input; code comment names the Phase-7 caller), rung 2 'drop secondary notes / optional metadata' = RESERVED no-op rung (no notes source; comment), rung 3 'summarise older history' = summarizeHistory on the history portion of [CONTEXT] (empty in Phase 5 → skips; the seam + fallback are unit-tested in 05-02), rung 4 'compress page/case context into structured fields' = compressStructural on [CONTEXT] when pageContext present (record compressionApplied:'structural'), rung 5 'trim tool schemas to the tools currently in scope' = trimToolSchemas halving the tool list each pass (keep the first ceil(n/2) name-sorted entries, minimum 1; 'in scope' in Phase 5 = the caller's selected schemas), rung 6 'reduce memory injection top-k' = reduceTopK halving k each pass (minimum 1), rung 7 'enter minimal mode' = set minimalMode true and apply the §2.5 minimal constraints (memory top-3, at most one safe tool schema — the first name-sorted tool with dangerous:false, else empty, and compact preference profile), rung 8 'if still too large' = return the CONTEXT_TOO_LARGE variant (never throws; message is a fixed user-facing explanation naming inputBudget and totalTokens and suggesting a larger-context model or a shorter request — NO raw user/page/memory content); (7) pass the surviving sections through ContextPack.pack (canonical order + totalTokens tally — 'ContextOptimizer decides, ContextPack packs', D-76); (8) build the manifest via buildManifest with per-section records (kind mapped via MANIFEST_KIND_MAP, sourceId = the section's source identity — CONTEXT: pageContext.url else 'context'; MEMORY: the hint ids joined ','; TOOL SCHEMAS: the tool names joined ','; USER PREFERENCES: 'preferences'; USER INPUT: 'user_input' — truncated:true + compressionApplied for every rung-affected section); (9) derive the trace surface: truncatedSources = manifest.sections.filter(s => s.truncated && s.sourceId !== 'system' && s.sourceId !== 'task').map(s => s.sourceId) (the two by-design omission records are excluded — the trace records DEGRADATION truncation for §19.3/PromptTrace, while the manifest retains the omission records for the Phase-7 receipt); truncated = truncatedSources.length > 0; (10) return { ok: true, context: { tier, inputBudget: budgets.inputBudget, outputBudget: budgets.outputBudget, sections: packedSections, provenance: manifest, minimalMode, contextTier: tier, truncated, truncatedSources } }.

No file outside the seven is touched. Do NOT add any import of the context layer into src/core/ai/*, src/components/*, or AgentOrchestrator (D-69/D-70). Do NOT add the section kinds 'SYSTEM'/'TASK' to OptimizedContext.sections.
<!-- planner-discipline-allow: 'CONTEXT_TOO_LARGE' -->
</action>
  <acceptance_criteria>`pnpm run lint` (tsc --noEmit) passes STRICT-CLEAN across the whole project (all seven modules type-check; the D-72 re-export `export type { PromptSection } from '../ai/types'` resolves against the Phase-3 A8 schema); `grep -rn "NP-STRICT" src/core/context/ | wc -l` equals 0; `grep -n "export type { PromptSection } from '../ai/types'" src/core/context/ContextOptimizer.ts` shows the re-export line; `grep -c "'CONTEXT_TOO_LARGE'" src/core/context/ContextOptimizer.ts` is >= 1 (closed-set literal used, code NOT added to src/core/ai/types.ts StreamErrorCodeSchema — `grep -n "CONTEXT_TOO_LARGE" src/core/ai/types.ts | wc -l` equals 0); `grep -n "contextTierCaps" src/core/context/ModelContextTier.ts` shows the helper and `grep -rn "contextTierCaps" src/core/ai/ src/components/ | wc -l` equals 0 (unwired, D-70); `git status --porcelain src/core/ai/ src/components/ | wc -l` equals 0 (no Phase-3 file modified — D-69/D-72 create-only boundary holds); `ls src/core/context/` lists exactly the seven files (no barrel index.ts).</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && test "$(grep -rn "NP-STRICT" src/core/context/ | wc -l)" -eq 0 && grep -q "export type { PromptSection } from '../ai/types'" src/core/context/ContextOptimizer.ts && grep -q "CONTEXT_TOO_LARGE" src/core/context/ContextOptimizer.ts && test "$(grep -rn "contextTierCaps" src/core/ai/ src/components/ 2>/dev/null | wc -l)" -eq 0 && test "$(git status --porcelain src/core/ai/ src/components/ | wc -l)" -eq 0</automated>
  </verify>
  <done>All seven modules exist, strict-clean, with the locked D-69..D-78 semantics; the end-to-end type-level slice compiles (input → result union → OptimizedContext → manifest); the D-72 re-export resolves; zero Phase-3 files modified; the six research open questions are locked as decisions in the module comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assemble() ← ContextOptimizerInput | Untrusted caller-supplied content (pageContext.text, memoryHints[].content, selectedToolSchemas[].description, userInput) crosses here — never trusted as instructions (the structural boundary that Phase-7 CTX-02 completes) |
| ContextOptimizer → AssembleResult | The budget-safe outcome exits the layer — the "never sends an oversized prompt" boundary (roadmap SC#2 / §19.3) |
| OptimizedContext.provenance / trace surface → consumers | Provenance records (sourceIds, token counts, flags) exit for Phase-7 receipts / Phase-11 PromptTrace — must never carry raw section bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | DoS | assemble() degradation terminal | high | mitigate | Budget math is floor-based and the ladder re-tallies after every rung; an ok:true OptimizedContext can never exceed inputBudget — if still over after minimal mode, assemble RETURNS the typed CONTEXT_TOO_LARGE variant (spec 502, roadmap SC#2). Callers are compile-time-forced to handle it (result union, not a throw) |
| T-05-02 | Spoofing | isFeatureAllowedInMinimalMode + OptimizedContext.minimalMode | medium | mitigate | Typed closed-set predicate over the §2.5 blocked literals verbatim (D-74); unknown feature strings default to allowed (closed-set discipline); tiny tiers always set minimalMode true (spec 506); 05-02 asserts both §2.5 lists verbatim |
| T-05-03 | Information Disclosure | manifest / trace / CONTEXT_TOO_LARGE.message | high | mitigate | truncatedSources carries sourceIds ONLY (never section text — V6/TraceRedactor discipline); the CONTEXT_TOO_LARGE message is a fixed user-facing explanation naming budgets only, never raw content; the manifest stores token counts + flags, not bodies (spec 528: "display provenance without the raw body") |
| T-05-04 | Tampering | untrusted page/memory/tool content | medium | mitigate | Untrusted content is confined to the data kinds (context/memory/tool_schemas); SYSTEM/TASK sections are never synthesized from caller input — the by-design omission records are constant literals; no content slot can redefine policy (the structural substrate Phase-7 CTX-02 hardens with trust metadata) |
| T-05-05 | — | error-code set | medium | mitigate | 'CONTEXT_TOO_LARGE' is a canonical §21.6 closed-set member (spec 3435/5079); no addition to StreamErrorCodeSchema, no new codes (D-38); the plan's grep gates assert the code set stays untouched |
| T-05-SC | Tampering | package installs | high | mitigate | Package legitimacy gate N/A — zero new dependencies this phase (RESEARCH Standard Stack: none; verified installed tree). No install commands exist in this plan. |
</threat_model>

<verification>
- `pnpm run lint` green after the task commit (strict-clean across the project — tsc --noEmit).
- Grep guard: zero `@ts-expect-error NP-STRICT` markers in the seven new modules (STATE.md decision 17).
- Create-only boundary: `git status --porcelain src/core/ai/ src/components/` empty — no Phase-3 file modified (D-69/D-70/D-72); the only Phase-3 contact is the type-only re-export inside the new ContextOptimizer.ts.
- Grep guard: `contextTierCaps` appears in ModelContextTier.ts only (unwired §1.4 helper, D-70) — zero production call sites.
- Tracer discipline: Task 1 creates the full vertical slice (all seven layers) because the pipeline is linear — there is no thinner path that still traverses every §18 module; each module is small (spec-verbatim pure functions).
- Nyquist / test-first ordering (documented acceptance — NOT an executor error): the §18 test files (`tests/core/context/TokenBudget.test.ts`, `ContextCompressor.test.ts`, `ContextOptimizer.test.ts`) land in plan 05-02, one plan after the modules they cover. This is deliberate tracer discipline: Task 1's automated verify (tsc strict-clean + the grep gates) is the tracer's runnable end-to-end proof that the seven-module slice compiles and the locked decisions hold at the type level; the behavioral proofs are the immediately-following §18 tests in 05-02. Strict RED-first TDD is intentionally NOT applied inside this plan — it would invert the tracer's purpose (the modules cannot compile RED against each other), and every task carries an automated `<verify>`, so Nyquist holds.
</verification>

<success_criteria>
1. The seven §18 modules exist under `src/core/context/` with the locked D-69..D-78 semantics and strict-clean typing (STATE.md decision 17).
2. The six research open questions are locked as decisions: Q1 types.ts home · Q2 budget→section mapping (history lives inside [CONTEXT]) · Q3 SYSTEM/TASK recorded as by-design omission records (truncated:true, 0 tokens) · Q4 CONTEXT_TOO_LARGE returned union `{ ok:false, code, message, totalTokens, inputBudget, minimalMode, truncatedSources }` · Q5 §1.4 contextTierCaps helper SHIPPED unwired · Q6 workspaceId/activeSurface REQUIRED with no defaults.
3. `ContextOptimizer.assemble` is a pure function returning the AssembleResult union — never a throw; the ok:false variant uses the canonical CONTEXT_TOO_LARGE literal; StreamErrorCodeSchema untouched (D-38).
4. ContextOptimizer re-exports PromptSection from `src/core/ai/types` (D-72) — PromptCacheAdapter's spec import-target note resolves; zero Phase-3 files modified.
5. The degradation ladder walks the §2.4 order against inputBudget; the manifest records every truncated/compressed section; the trace surface derives truncatedSources from the manifest (excluding the two by-design omissions) — the "never sends an oversized prompt" spine (roadmap SC#2) is structurally guaranteed.
</success_criteria>

<output>
Create `.planning/phases/05-context-adaptive-execution/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-5 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/context/types.ts | PageContext / RetrievedMemory / ToolSchemaRef supersession shapes (verbatim spec 4346-4357 / 4572-4578 / 4601-4607), CompressionType, Summarizer seam (D-75) | **[this plan]** |
| src/core/context/ModelContextTier.ts | ModelContextTier + ModelContextTierSchema + classifyModelContext (§2.1) + contextTierCaps (§1.4, unwired — D-70) | **[this plan]** |
| src/core/context/TokenBudget.ts | TokenCounter + heuristicTokenCounter (CJK-aware, D-71) + computeBudgets (§2.2 70/20/10) + DISTRIBUTION + budgetForCategory | **[this plan]** |
| src/core/context/ContextCompressor.ts | compressStructural / reduceTopK / trimToolSchemas + summarizeHistory (summarizer seam, drop-not-silence fallback — D-75) | **[this plan]** |
| src/core/context/ContextPack.ts | CANONICAL_SECTION_ORDER (§1.3) + pack (canonical-order assembly + totalTokens tally — D-76) | **[this plan]** |
| src/core/context/ContextProvenanceManifest.ts | ManifestKindSchema + ContextProvenanceManifestSchema (§2.6 verbatim) + buildManifest + MANIFEST_KIND_MAP (7-entry) | **[this plan]** |
| src/core/context/ContextOptimizer.ts | ContextOptimizerInput + OptimizedContext + AssembleResult + assemble + isFeatureAllowedInMinimalMode + BlockedFeature + BLOCKED_IN_MINIMAL_MODE (§2.5) + PromptSection re-export (D-72) + trace surface (D-77) | **[this plan]** |
| tests/core/context/TokenBudget.test.ts | DONE-1 tier boundaries + budget math + distribution + heuristic counter | 05-02 |
| tests/core/context/ContextCompressor.test.ts | D-75 strategy + summarizer-seam fallback tests | 05-02 |
| tests/core/context/ContextOptimizer.test.ts | DONE-2/3/4 degradation ladder, CONTEXT_TOO_LARGE terminal, minimal-mode predicate verbatim, manifest-on-every-context, §1.3 order | 05-02 |
| package.json | verify:phase-5 re-pointed to tests/core/context (D-78) | 05-02 |

Layout note (locked): no barrel `index.ts` — direct path imports only, mirroring the existing `src/core/ai/` layout (verified: `src/core/ai/index.ts` does not exist). The `types.ts` sibling is the discretion-sanctioned seventh file, declared here for reviewability against the §18 create-only boundary (RESEARCH A6).