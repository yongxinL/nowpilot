---
phase: 08-knowledge-base-memory-minisearch-notes
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - src/core/notes/NoteGraph.ts
  - src/components/notes/BacklinksPanel.tsx
  - src/components/notes/WikilinkAutocomplete.tsx
  - src/components/notes/NoteGraphView.tsx
  - tests/core/notes/NoteGraph.test.ts
  - tests/components/notes/BacklinksPanel.test.tsx
  - tests/components/notes/WikilinkAutocomplete.test.tsx
  - tests/components/notes/NoteGraphView.test.tsx
autonomous: true
requirements: []
user_setup: []

# Infra plan — no spec-native v1 IDs (RICH-R-05 landed in 08-01/08-02).
# Acceptance = §22.3 verbatim cosine + backlinks (D-111) + the three thin
# components' core logic per 08-UI-SPEC contracts.

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - statement: "D-111: NoteGraph.topKSimilar(note, k=5) implements §22.3 verbatim (spec 3508-3514) — bag-of-words cosine over content.toLowerCase().match(/[a-z0-9]{3,}/g) tokenisation, inline fixed 50-word English stop-word list (spec 3511: count + inline location; vocabulary is executor discretion bounded by a length-50 test), per-note term-frequency map, cosine = dot(a,b)/(||a||*||b||), ties broken by updated desc then id asc"
      verification: backstop
    - statement: "D-111: NoteGraph exposes a backlinks reverse index over links[] (computeBacklinks: noteId → referencing note IDs); WIKI-ID-04 deletion demotion is respected — backlink/links membership is computed against the live note set"
      verification: backstop
    - statement: "The three .tsx files ship their core logic per 08-UI-SPEC data contracts — BacklinksPanel (BacklinkEntry sorted updated desc), WikilinkAutocomplete (WikilinkSuggestion via MiniSearch title matching, top-k ≤10, WIKI-ID-02 tie-break; NO LLM suggestions — D-04), NoteGraphView (GraphNode/GraphEdge adjacency data from topKSimilar + backlinks); all render through React JSX text nodes only — zero dangerouslySetInnerHTML (CTX-02; grep-assertable across src/components/notes/)"
      verification: backstop
    - statement: "Full NotesWorkspace UI integration is Phase 15 (scope fence) — these are thin, testable core-logic components consumed later; no wiring into the existing NotesWorkspace.tsx scaffold in this phase"
      verification: backstop
  artifacts:
    - path: "src/core/notes/NoteGraph.ts"
      provides: "§22.3 verbatim graph core — STOP_WORDS (exactly 50), tokenise, term-frequency map, cosine, topKSimilar(note, k=5) with updated-desc/id-asc tie-break; computeBacklinks(notes) → Map<noteId, referencing-note-ids>"
      exports: ["STOP_WORDS", "topKSimilar", "computeBacklinks", "SimilarNote"]
    - path: "src/components/notes/BacklinksPanel.tsx"
      provides: "D-111 core logic + thin list — BacklinkEntry { noteId, title, updated } sorted updated desc; props { noteId, onSelect, maxItems? }; header 'BACKLINKS' + count Tag; empty state 'No backlinks yet' / 'Notes that link to this one will appear here.' (UI-SPEC copywriting); React JSX only"
      exports: ["BacklinksPanel", "BacklinkEntry"]
    - path: "src/components/notes/WikilinkAutocomplete.tsx"
      provides: "D-111/D-04 core logic + thin popover — WikilinkSuggestion { noteId, title }; query = text after '[['; MiniSearch title search via MiniSearchIndex, top-k ≤10, WIKI-ID-02 tie-break; props { query, open, onSelect, onClose }; role=listbox/option, arrow-key navigation, Enter accept, Esc dismiss; empty state 'No matching notes' / 'Create the note first, then link to it.'; NO LLM suggestions"
      exports: ["WikilinkAutocomplete", "WikilinkSuggestion", "AUTOCOMPLETE_MAX"]
    - path: "src/components/notes/NoteGraphView.tsx"
      provides: "D-111 core logic + thin scaffold — GraphNode { id, title, kind: 'current'|'similar'|'backlink' }, GraphEdge { source, target, type: 'similar'|'backlink' }; adjacency from topKSimilar(k=5) + computeBacklinks; props { noteId, onSelect }; renders adjacency legibly (SVG or container scaffold); empty state 'No connections yet'; d3-force layout is Phase 15"
      exports: ["NoteGraphView", "GraphNode", "GraphEdge", "buildGraphAdjacency"]
    - path: "tests/core/notes/NoteGraph.test.ts"
      provides: "§22.3 — STOP_WORDS length-50 assertion, cosine ordering, tie-break, backlinks reverse index, demotion vs live set"
      exports: []
    - path: "tests/components/notes/BacklinksPanel.test.tsx"
      provides: "UI-SPEC — sorted updated-desc rows, count Tag, empty state copy, onSelect on click, no-dangerouslySetInnerHTML structural gate"
      exports: []
    - path: "tests/components/notes/WikilinkAutocomplete.test.tsx"
      provides: "UI-SPEC — suggestion top-k ≤10, WIKI-ID-02 tie-break, keyboard nav, Enter accept, Esc dismiss, empty state, no LLM calls"
      exports: []
    - path: "tests/components/notes/NoteGraphView.test.tsx"
      provides: "UI-SPEC — current/similar/backlink node kinds, edge types, empty state, onSelect"
      exports: []
  key_links:
    - from: "src/components/notes/WikilinkAutocomplete.tsx"
      to: "src/core/search/MiniSearchIndex.ts"
      via: "MiniSearchIndex title-matching query for the autocomplete suggestions (D-04 — no LLM suggestions in v0.1); ties broken WIKI-ID-02"
      pattern: "MiniSearchIndex"
    - from: "src/components/notes/NoteGraphView.tsx"
      to: "src/core/notes/NoteGraph.ts"
      via: "topKSimilar + computeBacklinks feed the GraphNode/GraphEdge adjacency (D-111, UI-SPEC data contract)"
      pattern: "topKSimilar"
    - from: "src/components/notes/BacklinksPanel.tsx"
      to: "src/core/notes/NoteGraph.ts"
      via: "computeBacklinks reverse index → BacklinkEntry rows sorted updated desc (D-111, UI-SPEC contract 1)"
      pattern: "computeBacklinks"
  prohibitions:
    - "No dangerouslySetInnerHTML / innerHTML in any of the three components — note titles/content are untrusted (CTX-02); render through React JSX text nodes only (grep-assertable across src/components/notes/)"
    - "No LLM wikilink autocomplete suggestions in v0.1 (D-04) — title matching only"
    - "No d3-force graph layout / clustering — Phase 8 ships adjacency data + a legible thin render (Phase 15 owns the full graph view)"
    - "No wiring into the existing NotesWorkspace.tsx scaffold (Phase 15 scope fence)"
    - "STOP_WORDS is EXACTLY 50 entries (spec 3511) — pinned by expect(STOP_WORDS).toHaveLength(50); no lemmatisation/POS filtering (Pitfall 8)"
    - "Zero new @ts-expect-error NP-STRICT markers (STATE.md decision 17)"
    - "Breadcrumb (PROHIB_ABSENT fallback): canon-security items (OWASP/prototype-pollution/generic injection) dropped — covered by threat model; component rendering is the CTX-02 untrusted-data surface"

---

<objective>
NoteGraph + component core-logic plan: the §22.3 verbatim graph algorithm (`NoteGraph` — cosine similarity + inline 50-word stop-list + backlinks) and the three thin `.tsx` components' core logic (`BacklinksPanel`, `WikilinkAutocomplete`, `NoteGraphView`) per the 08-UI-SPEC data contracts.

Purpose: the knowledge-model connective web. topKSimilar proves related-note discovery; the components carry the data derivation the Phase-15 NotesWorkspace UI will consume — backlink listings, MiniSearch-title autocomplete (no LLM, D-04), and graph adjacency — all untrusted-data-safe (React JSX only).

Output: the eight files in frontmatter, strict-clean; NoteGraph.test.ts + three component tests; the full component set carries zero dangerouslySetInnerHTML (grep-assertable).
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  # Phase 8 block (253-267)
@.planning/STATE.md  # decision 11 (WIKI-ID UUID), decision 17 (strict-clean)
@.planning/PRODUCT_SPEC_v0_1.md  # §22.3 (3506-3514: cosine + stop-list + tie-break), §27.7a WIKI-ID-04 (3904: dangling edges demote), §16.1 (PortableMarkdown — Phase 15 body rendering; components use JSX text only)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-CONTEXT.md  # D-111 + discretion
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-RESEARCH.md  # Pattern 5 (§22.3 verbatim values), Pitfall 8 (50-word stop-list vocabulary discretion), Security Domain (XSS via note content — React JSX only)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-PATTERNS.md  # NoteGraph (353-365), components (383-397), component skeleton (NotesWorkspace.tsx:194-205 analog)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-UI-SPEC.md  # Component Interaction Contracts 1/2/3 (118-171), Copywriting table, cross-component security contract (168-171)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-VALIDATION.md  # 08-04-01 (NoteGraph + components)
@src/core/notes/NoteGraph.ts  # (created in this plan — no prior read needed beyond PATTERNS)
@src/core/search/MiniSearchIndex.ts  # from 08-03 — the autocomplete query surface
@src/components/notes/NotesWorkspace.tsx  # scaffold (194-205) — the component-skeleton precedent; NOT wired in this phase
@src/types/notes.ts  # canonical Note (08-01)
@tests/components/OptionsPage.test.tsx  # @testing-library/react + renderWithAntd conventions (1-56)
@tests/setup.ts  # ResizeObserver/matchMedia mocks (32-53)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — NoteGraph §22.3 cosine core: STOP_WORDS(50) + topKSimilar + computeBacklinks</name>
  <files>src/core/notes/NoteGraph.ts, tests/core/notes/NoteGraph.test.ts</files>
  <read_first>08-PATTERNS.md NoteGraph section (353-365 — verbatim cosine, STOP_WORDS length-50 pin, backlinks reverse index) · 08-RESEARCH.md Pattern 5 (§22.3 verbatim 291-292) + Pitfall 8 (stop-list vocabulary is executor discretion bounded by count; a length-50 test pins it) + Pitfall 9 (deletion demotes edges — links membership vs live set) · 08-CONTEXT.md D-111 · PRODUCT_SPEC §22.3 (3508-3514 verbatim) · @src/types/notes.ts (canonical Note shape — topKSimilar operates on Note[]) · @src/core/context/trust/contextItems.ts (136-139 — tie-break sort precedent)</read_first>
  <action>Build the graph core per §22.3 verbatim. Strict-clean.

1. `src/core/notes/NoteGraph.ts` (NEW) — pure module (TokenBudget.ts pure-fn style):
   a. `export const STOP_WORDS: readonly string[] = [ /* EXACTLY 50 common English stop-words — e.g. a, an, and, are, as, at, be, but, by, for, from, has, have, he, her, his, i, in, is, it, its, of, on, or, she, that, the, their, them, they, this, to, was, we, were, what, when, where, which, who, will, with, you, your, not, so, do, no, if */ ]` — exactly 50 entries (Pitfall 8: count is the contract; vocabulary is standard English stop-words, no lemma/POS filtering).
   b. Tokeniser (spec 3508): `const tokenise = (content: string): string[] => content.toLowerCase().match(/[a-z0-9]{3,}/g) ?? []` then filter STOP_WORDS.
   c. Term-frequency map: `buildTf(tokens) → Map<string, number>` (count / total tokens — normalised TF).
   d. `export interface SimilarNote { note: Note; score: number }` and `export function topKSimilar(note: Note, allNotes: Note[], k = 5): SimilarNote[]` — compute cosine = dot(a,b) / (||a|| * ||b||) between the target's TF map and every other note's TF map (skip the target itself; zero-norm guard → score 0), sort score desc, ties broken by `updated` desc then `id` asc (spec 3508-3514 verbatim; contextItems.ts:136-139 sort precedent), take top-k.
   e. `export function computeBacklinks(notes: Note[]): Map<string, string[]>` — reverse index: for each note, for each id in note.links, map id → referencing note ids (deduplicated). WIKI-ID-04 respect: membership is computed against the LIVE note set passed in — notes not in the set contribute nothing (their dangling links are not re-added; demotion lives in LinkParser.demoteDangling from 08-03).
   Header comment: D-111 + §22.3 (spec 3508-3514) + Pitfall 8 note.
2. `tests/core/notes/NoteGraph.test.ts` (NEW) — (a) `expect(STOP_WORDS).toHaveLength(50)` (Pitfall 8 pin); (b) tokenise drops stop words (content 'the quick brown fox' → 'quick','brown','fox'); (c) COSINE ORDERING: 3 notes sharing topic words → topKSimilar returns the most overlapping note first; (d) TIE-BREAK: two notes with identical cosine → newer updated first; identical updated → lower id first; (e) ZERO-NORM: empty content note → cosine 0, no crash; (f) backlinks: notes A(l→B), C(l→B), D(l→none) → computeBacklinks.get('B') = ['A','C'], get('D') = [] ; (g) LIVE-SET: a note whose links reference an id absent from the passed allNotes set → that edge does not appear in backlinks (WIKI-ID-04 semantics); (h) k default 5 and explicit k respected.</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `npx vitest run tests/core/notes/NoteGraph.test.ts` passes; `grep -c "STOP_WORDS" src/core/notes/NoteGraph.ts` >= 2 (declaration + comment); `grep -n "match(/[a-z0-9]{3,}/g)" src/core/notes/NoteGraph.ts` shows the verbatim tokeniser; `grep -n "topKSimilar" src/core/notes/NoteGraph.ts` shows the export; `grep -n "computeBacklinks" src/core/notes/NoteGraph.ts` shows the reverse index; `grep -rn "NP-STRICT" src/core/notes/NoteGraph.ts tests/core/notes/NoteGraph.test.ts | wc -l` equals 0</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/notes/NoteGraph.test.ts && test "$(grep -rn 'NP-STRICT' src/core/notes/NoteGraph.ts tests/core/notes/NoteGraph.test.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>NoteGraph ships the §22.3 verbatim cosine core: exactly-50-word inline stop-list (length-pinned), [a-z0-9]{3,} tokenisation with stop-word filtering, per-note TF maps, cosine scoring with the updated-desc/id-asc tie-break, and the backlinks reverse index over links[] — all proven by NoteGraph.test.ts; strict-clean.</done>
  <!-- planner-discipline-allow: STOP_WORDS -->
  <reversibility rating="reversible">D-111 — core-logic module; Phase 15 consumes it (CONTEXT.md rating).</reversibility>
</task>

<task type="auto">
  <name>Task 2: BacklinksPanel + NoteGraphView — core-logic components (data contracts per UI-SPEC)</name>
  <files>src/components/notes/BacklinksPanel.tsx, src/components/notes/NoteGraphView.tsx, tests/components/notes/BacklinksPanel.test.tsx, tests/components/notes/NoteGraphView.test.tsx</files>
  <read_first>08-UI-SPEC.md Component Contracts 1 (BacklinksPanel 118-133) + 3 (NoteGraphView 153-166) + Copywriting table + cross-component security contract (168-171) + Color/Typography/Spacing tokens · 08-PATTERNS.md components section (383-397 — NotesWorkspace.tsx:194-205 skeleton: useToken, App.useApp, React.FC) · 08-CONTEXT.md D-111 (components carry core logic; Phase-15 NotesWorkspace integration deferred) · @src/core/notes/NoteGraph.ts (from Task 1) · @src/core/search/MiniSearchIndex.ts (08-03 — not needed by these two, but the shared query surface for Task 3) · @src/components/notes/NotesWorkspace.tsx (1-49 imports convention: React, antd, @ant-design/icons, useExtensionStore) · @tests/components/OptionsPage.test.tsx (renderWithAntd convention 1-56) · @tests/setup.ts (antd jsdom mocks 32-53)</read_first>
  <action>Build the two data-driven core-logic components per the UI-SPEC contracts. Strict-clean, React JSX text nodes only (CTX-02 — the untrusted-data surface).

1. `src/components/notes/BacklinksPanel.tsx` (NEW) — D-111 core logic + thin list. `export interface BacklinkEntry { noteId: string; title: string; updated: number }` (UI-SPEC Contract 1 data contract). `export function useBacklinks(notes: Note[], noteId: string): BacklinkEntry[]` (or a pure helper `computeBacklinkEntries(notes, noteId)` used by the component) — reverse index via computeBacklinks over the LIVE note set, rows `{ noteId, title, updated }` sorted `updated` desc (dangling IDs excluded — WIKI-ID-04). Component: `export const BacklinksPanel: React.FC<{ noteId: string; onSelect: (noteId: string) => void; maxItems?: number }>` — header row 'BACKLINKS' (11 px uppercase colorTextTertiary per UI-SPEC) + count Tag; scrollable list (colorBgContainer, radius 12); each row is a real `<button>` (keyboard-focusable, 2 px colorPrimary focus ring, aria-label `Open backlink ${title}`, hit target ≥ 32 px) with the title (colorPrimary, hover underline, single-line ellipsis, `title` attribute for the full text) + relative time (colorTextTertiary 12 px); row click → onSelect(noteId); empty state heading 'No backlinks yet' + body 'Notes that link to this one will appear here.' (UI-SPEC copywriting verbatim); never a blank surface.
2. `src/components/notes/NoteGraphView.tsx` (NEW) — D-111 core logic + thin scaffold. `export interface GraphNode { id: string; title: string; kind: 'current' | 'similar' | 'backlink' }`, `export interface GraphEdge { source: string; target: string; type: 'similar' | 'backlink' }`, and `export function buildGraphAdjacency(notes: Note[], noteId: string, k?: number): { nodes: GraphNode[]; edges: GraphEdge[] }` — current note = center node (kind 'current'); topKSimilar(note, k=5) → 'similar' nodes + 'similar' edges (source = noteId); computeBacklinks → 'backlink' nodes + 'backlink' edges (target = noteId). Component: `export const NoteGraphView: React.FC<{ noteId: string; onSelect: (noteId: string) => void }>` — renders the adjacency data legibly (SVG or a container scaffold: current node colorPrimary fill; similar/backlink nodes colorBgContainer with colorBorder; title in colorPrimary on select); node click → onSelect(noteId), hover highlight, hit target ≥ 32 px; empty state 'No connections yet' + 'Related and linking notes appear here as you add wikilinks.'; the d3-force layout is Phase 15 (comment it).
3. `tests/components/notes/BacklinksPanel.test.tsx` (NEW) — @testing-library/react + renderWithAntd (ConfigProvider wrapper — OptionsPage.test.tsx:55-57 precedent). (a) renders the 'BACKLINKS' header + count Tag for 2 backlinks; (b) rows sorted updated desc (assert row order via getAllByRole('button') text sequence); (c) dangling-id exclusion: a note whose links reference an absent id → no row; (d) empty state copy 'No backlinks yet' when zero; (e) click row → onSelect called with the noteId; (f) STRUCTURAL XSS gate: `grep -c "dangerouslySetInnerHTML" src/components/notes/BacklinksPanel.tsx` equals 0 (assert via fs-read in the test OR as an acceptance grep — prefer the acceptance grep + a test that renders a note titled with HTML-ish text like '<img src=x onerror=alert(1)>' and asserts it appears as plain text (screen.getByText with the literal string, no <img> element in the document)).
4. `tests/components/notes/NoteGraphView.test.tsx` (NEW) — (a) current node rendered (kind 'current'); (b) similar nodes from topKSimilar (k=5) + 'similar' edges; (c) backlink nodes + 'backlink' edges; (d) empty state 'No connections yet' when the note has no similar/backlinks; (e) node click → onSelect(noteId); (f) buildGraphAdjacency unit: node kinds + edge types + no self-edges.</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `npx vitest run tests/components/notes/BacklinksPanel.test.tsx tests/components/notes/NoteGraphView.test.tsx` passes; `grep -n "export interface BacklinkEntry" src/components/notes/BacklinksPanel.tsx` shows the contract; `grep -n "computeBacklinks" src/components/notes/BacklinksPanel.tsx` shows the reverse-index consumption; `grep -n "export function buildGraphAdjacency" src/components/notes/NoteGraphView.tsx` shows the data contract; `grep -n "topKSimilar" src/components/notes/NoteGraphView.tsx` shows the similar-node source; `grep -c "dangerouslySetInnerHTML\|innerHTML" src/components/notes/BacklinksPanel.tsx src/components/notes/NoteGraphView.tsx | wc -l` equals 0 (combined — the XSS gate); `grep -rn "NP-STRICT" src/components/notes/BacklinksPanel.tsx src/components/notes/NoteGraphView.tsx tests/components/notes/BacklinksPanel.test.tsx tests/components/notes/NoteGraphView.test.tsx | wc -l` equals 0</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/components/notes/BacklinksPanel.test.tsx tests/components/notes/NoteGraphView.test.tsx && test "$(grep -c 'dangerouslySetInnerHTML\|innerHTML' src/components/notes/BacklinksPanel.tsx src/components/notes/NoteGraphView.tsx | wc -l)" -eq 0 && test "$(grep -rn 'NP-STRICT' src/components/notes/BacklinksPanel.tsx src/components/notes/NoteGraphView.tsx tests/components/notes/BacklinksPanel.test.tsx tests/components/notes/NoteGraphView.test.tsx | wc -l)" -eq 0</automated>
  </verify>
  <done>BacklinksPanel ships the UI-SPEC backlink listing core (entries sorted updated desc, count Tag, empty state, keyboard-focusable rows, onSelect) and NoteGraphView ships the graph adjacency core (current/similar/backlink nodes + edges from topKSimilar + computeBacklinks, empty state, onSelect) — both proven by component tests with the HTML-ish-title plain-text assertion; zero dangerouslySetInnerHTML; strict-clean.</done>
  <!-- planner-discipline-allow: BacklinkEntry -->
  <!-- planner-discipline-allow: dangerouslySetInnerHTML -->
  <!-- planner-discipline-allow: MiniSearchIndex -->
  <reversibility rating="reversible">D-111 — core-logic components; Phase 15 consumes them (CONTEXT.md rating).</reversibility>
</task>

<task type="auto">
  <name>Task 3: WikilinkAutocomplete — MiniSearch title-matching core (D-04, no LLM)</name>
  <files>src/components/notes/WikilinkAutocomplete.tsx, tests/components/notes/WikilinkAutocomplete.test.tsx</files>
  <read_first>08-UI-SPEC.md Component Contract 2 (WikilinkAutocomplete 135-151 — WikilinkSuggestion { noteId, title }, query = text after '[[', MiniSearch title search, top-k ≤10, WIKI-ID-02 tie-break, keyboard/a11y) + Copywriting table + security contract · 08-PATTERNS.md components section (383-397) + MiniSearchIndex section (274-325 — the query surface) · 08-CONTEXT.md D-111 + D-04 (no LLM suggestions in v0.1) · @src/core/search/MiniSearchIndex.ts (08-03 — title-matching query + NoteHit) · @tests/components/OptionsPage.test.tsx (renderWithAntd) · @tests/setup.ts (antd mocks)</read_first>
  <action>Build the autocomplete core per UI-SPEC Contract 2 — MiniSearch title matching only (D-04: NO LLM suggestions in v0.1). Strict-clean.

1. `src/components/notes/WikilinkAutocomplete.tsx` (NEW) — D-111/D-04 core logic + thin popover. `export interface WikilinkSuggestion { noteId: string; title: string }` (UI-SPEC Contract 2). `export const AUTOCOMPLETE_MAX = 10;` (top-k cap). Core function: `export function searchSuggestions(hits: NoteHit[], query: string, now?: number): WikilinkSuggestion[]` — filter hits whose title contains the query (case-insensitive), deduplicate by noteId, sort by title match quality (exact prefix first, then WIKI-ID-02 tie-break: updated desc → id asc — NoteHit carries the stored fields incl. `updated` per 08-03's NoteDoc/storeFields contract, so the tie-break is satisfiable), cap at AUTOCOMPLETE_MAX. Component: `export const WikilinkAutocomplete: React.FC<{ query: string; open: boolean; onSelect: (s: WikilinkSuggestion) => void; onClose: () => void }>` — when open and query non-empty, run the title search via MiniSearchIndex.query (the per-surface singleton — import the module, call its query fn with the notes DB handle the surface owns; keep the db-handle acquisition as a thin prop/DI seam `getNotesDb?: () => Promise<IDBPDatabase<NotesDBV1>>` so the component stays testable without a live DB — default: lazily resolve via openNotesDB); render a popover (colorBgElevated, E2 elevation, radius 12, max-height 240 px + scroll) with rows role=listbox/role=option + aria-selected; active row colorPrimaryBg fill + colorPrimaryText; keyboard ↑/↓ move the active row, Enter accepts, Esc dismisses (call onClose); click accepts; empty state 'No matching notes' + 'Create the note first, then link to it.' (UI-SPEC verbatim — D-04: no LLM suggestions); focus remains in the editor (Phase-15 wiring — comment it); 2 px colorPrimary focus ring. The `[[` trigger + caret anchoring is Phase-15 editor integration (this component consumes query + open — comment the seam).
2. `tests/components/notes/WikilinkAutocomplete.test.tsx` (NEW) — (a) searchSuggestions unit: query 'sql' matches titles 'MySQL Guide' and 'SQL Basics' — prefix/exact wins; tie-break updated desc then id asc; cap at 10; case-insensitive; (b) component: open + query → suggestions rendered as options (role=option); (c) keyboard: ArrowDown moves the active row (aria-selected), Enter calls onSelect with the active suggestion; (d) Esc calls onClose; (e) click calls onSelect; (f) empty state 'No matching notes' when no title matches; (g) closed or empty query → nothing rendered; (h) NO-LLM structural: the component and its core import no AI modules (`grep -c "from '../../core/ai/\|fetch(" src/components/notes/WikilinkAutocomplete.tsx` equals 0 — D-04).</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `npx vitest run tests/components/notes/WikilinkAutocomplete.test.tsx` passes; `grep -n "AUTOCOMPLETE_MAX = 10" src/components/notes/WikilinkAutocomplete.tsx` shows the top-k cap; `grep -n "searchSuggestions" src/components/notes/WikilinkAutocomplete.tsx` shows the core; `grep -c "MiniSearchIndex" src/components/notes/WikilinkAutocomplete.tsx` >= 1 (the query surface); `grep -c "from '../../core/ai/\|fetch(" src/components/notes/WikilinkAutocomplete.tsx` equals 0 (D-04 no-LLM); `grep -c "dangerouslySetInnerHTML\|innerHTML" src/components/notes/WikilinkAutocomplete.tsx` equals 0; `grep -rn "NP-STRICT" src/components/notes/WikilinkAutocomplete.tsx tests/components/notes/WikilinkAutocomplete.test.tsx | wc -l` equals 0</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/components/notes/WikilinkAutocomplete.test.tsx && test "$(grep -c 'from .*core/ai/\|fetch(' src/components/notes/WikilinkAutocomplete.tsx)" -eq 0 && test "$(grep -rn 'NP-STRICT' src/components/notes/WikilinkAutocomplete.tsx tests/components/notes/WikilinkAutocomplete.test.tsx | wc -l)" -eq 0</automated>
  </verify>
  <done>WikilinkAutocomplete ships the MiniSearch title-matching core (top-k ≤10, WIKI-ID-02 tie-break, keyboard nav, listbox/option a11y, UI-SPEC empty state) with zero AI-module imports (D-04) and zero dangerouslySetInnerHTML; the test proves suggestions, navigation, accept/dismiss, and the empty state; strict-clean.</done>
  <!-- planner-discipline-allow: WikilinkSuggestion -->
  <!-- planner-discipline-allow: from '../../core/ai/\|fetch( -->
  <reversibility rating="reversible">D-111/D-04 — core-logic component; Phase-15 editor wiring is a caller edit (CONTEXT.md rating).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note titles/content → component props | Untrusted stored content (CTX-02) enters the React render tree — JSX text nodes only, never HTML injection |
| MiniSearchIndex hits → WikilinkAutocomplete suggestions | Search results carry stored note fields — rendered as plain text rows, never markup |
| NoteGraph adjacency → NoteGraphView | Cosine-derived similarity + backlinks from untrusted note metadata — plain text + SVG shapes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-11 | Tampering / XSS | BacklinksPanel / WikilinkAutocomplete / NoteGraphView | high | mitigate | Zero dangerouslySetInnerHTML / innerHTML across src/components/notes/ (grep-asserted); titles/content render via React JSX text nodes only; the BacklinksPanel test renders an HTML-ish title ('<img src=x onerror=alert(1)>') and asserts it appears as literal text with no element injected (CTX-02/UI-SPEC cross-component security contract) |
| T-8-12 | Tampering | WikilinkAutocomplete suggestion source | low | mitigate | Suggestions are MiniSearch TITLE matches only (D-04) — no LLM-generated content enters the suggestion list in v0.1 (grep: zero AI-module imports); ties broken deterministically (WIKI-ID-02) |
| T-8-13 | Denial of Service | NoteGraphView large-graph render | low | accept | Phase 8 renders adjacency as-is (topKSimilar k=5 + backlinks — bounded); d3-force layout + clustering is Phase 15 (UI-SPEC overflow backstop); no unbounded layout computation ships here |
| T-8-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO installs this phase (Package Legitimacy Audit vacuous — all deps pre-installed; @testing-library/react + antd already present); executor must not add any dependency |

</threat_model>

<verification>
- **Per-task commits:** `pnpm run lint` + the affected test file (08-VALIDATION.md sampling rate).
- Grep guards: zero dangerouslySetInnerHTML/innerHTML across src/components/notes/ (the CTX-02 XSS gate); zero NP-STRICT markers; zero AI-module imports in WikilinkAutocomplete (D-04).
- Consumer-regression guard: `npx vitest run tests/components` stays green (the existing OptionsPage/OnboardingModal/MirrorBanner tests unaffected).
- Wave-3 merge check: `npx vitest run tests/core/notes/NoteGraph.test.ts tests/components/notes` green.
</verification>

<success_criteria>
1. NoteGraph implements §22.3 verbatim: exactly-50-word inline stop-list (length-pinned), [a-z0-9]{3,} tokenisation, TF-map cosine with the updated-desc/id-asc tie-break, and the computeBacklinks reverse index (D-111).
2. The three components ship their UI-SPEC core logic: BacklinksPanel (updated-desc backlink rows + count + empty state), WikilinkAutocomplete (MiniSearch title matching, top-k ≤10, WIKI-ID-02 tie-break, keyboard/a11y, no LLM), NoteGraphView (current/similar/backlink adjacency) — all React-JSX-only with zero dangerouslySetInnerHTML (CTX-02).
</success_criteria>

<output>
Create `.planning/phases/08-knowledge-base-memory-minisearch-notes/08-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-8 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/notes/NoteGraph.ts | STOP_WORDS(50) / topKSimilar / computeBacklinks / SimilarNote — §22.3 verbatim cosine (D-111) | **[this plan]** |
| src/components/notes/BacklinksPanel.tsx | BacklinksPanel / BacklinkEntry / computeBacklinkEntries — backlink listing core (D-111, UI-SPEC) | **[this plan]** |
| src/components/notes/WikilinkAutocomplete.tsx | WikilinkAutocomplete / WikilinkSuggestion / searchSuggestions / AUTOCOMPLETE_MAX=10 — MiniSearch title matching (D-04/D-111, UI-SPEC) | **[this plan]** |
| src/components/notes/NoteGraphView.tsx | NoteGraphView / GraphNode / GraphEdge / buildGraphAdjacency — graph adjacency core (D-111, UI-SPEC) | **[this plan]** |
| tests/core/notes/NoteGraph.test.ts | STOP_WORDS length-50 pin, cosine ordering, tie-break, backlinks, live-set semantics | **[this plan]** |
| tests/components/notes/BacklinksPanel.test.tsx | sorted rows, count Tag, empty state, onSelect, HTML-title plain-text XSS assertion | **[this plan]** |
| tests/components/notes/WikilinkAutocomplete.test.tsx | suggestions, keyboard nav, accept/dismiss, empty state, no-LLM structural gate | **[this plan]** |
| tests/components/notes/NoteGraphView.test.tsx | node kinds, edge types, empty state, onSelect, buildGraphAdjacency unit | **[this plan]** |