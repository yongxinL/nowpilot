---
phase: 08-knowledge-base-memory-minisearch-notes
plan: 05
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - tests/core/search/notes-search-e2e.test.ts
  - package.json
autonomous: true
requirements: [RICH-R-05]
user_setup: []

# NOTE: this plan's verify task touches package.json (D-114 gate re-point).
# Files_modified overlap with other Wave-3 plans: none (08-04 touches
# components/NoteGraph only; 08-05 touches tests/core/search E2E + package.json).

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - statement: "D-105/§18 DONE-when: the end-to-end path 'Page → PageContentService → Note → MiniSearch' works — proven by a service-level test (PageContext → Note creation → NotesDB.put → index upsert → query), NOT a shipped UI call-site (create-only discipline)"
      verification: backstop
    - statement: "D-114: verify:phase-8 is re-pointed to the §18 canonical gate string (spec 3612 verbatim): 'tsc --noEmit && vitest run tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts' — asserted via node -e exact-string check (D-92/D-103 precedent)"
      verification: backstop
    - statement: "RICH-R-05 + §18 DONE-when: pnpm run verify:phase-8 passes green with all five §18-required test files + the E2E test + the perf gate (MemoryEngine/MemoryScorer/UserMemoryStore/MiniSearchIndex/LinkParser)"
      verification: backstop
  artifacts:
    - path: "tests/core/search/notes-search-e2e.test.ts"
      provides: "§18 DONE-when E2E — PageContext fixture → canonical Note creation → LinkParser.parseLinks/resolveLinks → saveNote (NotesDB.put + note:saved) → MiniSearchIndex upsert → query returns the note; plus the RICH-R-05 surface check (buildPreferenceProfile includes persona overrides)"
      exports: []
    - path: "package.json"
      provides: "D-114 — verify:phase-8 script re-pointed to spec 3612 (replacing the stale Phase-6/17 target tests/core/content tests/addons tests/isolation)"
      exports: []
  key_links:
    - from: "tests/core/search/notes-search-e2e.test.ts"
      to: "src/core/notes/save.ts"
      via: "saveNote drives the Flow-3-minus-LLM path the E2E proves (parse → resolve → put → emit note:saved)"
      pattern: "saveNote"
    - from: "tests/core/search/notes-search-e2e.test.ts"
      to: "src/core/search/MiniSearchIndex.ts"
      via: "the note:saved emission upserts the index — the E2E asserts query() finds the note after the save"
      pattern: "note:saved"
    - from: "package.json"
      to: "tests/core/{memory,search}"
      via: "verify:phase-8 gate string runs the §18 dirs verbatim (D-114, spec 3612)"
      pattern: "vitest run"
  prohibitions:
    - "The E2E test must NOT open a real browser/extension surface — service-level only (create-only D-105; chrome mocks from tests/setup.ts suffice)"
    - "package.json edits are limited to the verify:phase-8 script string — no dependency additions, no other script changes (Package Legitimacy Audit vacuous — no installs)"
    - "The gate string is spec 3612 VERBATIM — no extra dirs appended, none removed (D-114; the Phase-6/17 dirs keep their own gates: verify:phase-6/17 untouched)"
    - "Zero new @ts-expect-error NP-STRICT markers (STATE.md decision 17)"
    - "Breadcrumb (PROHIB_ABSENT fallback): canon-security items dropped — covered by threat model + the Phase-7 trust layer"

---

<objective>
Phase-verification plan: the §18 DONE-when end-to-end proof and the gate re-point — `tests/core/search/notes-search-e2e.test.ts` (Page → PageContentService → Note → MiniSearch) and the `verify:phase-8` re-point to the spec 3612 canonical string (D-114).

Purpose: close the phase — prove the full save→index→query path works at the service level (the §18 DONE-when), and fix the mis-pointed verification gate so `pnpm run verify:phase-8` actually runs the §18-required tests (memory, search, LinkParser) instead of the stale Phase-6/17 targets.

Output: the E2E test + the package.json gate re-point, verified by `pnpm run verify:phase-8` passing green.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  # Phase 8 block (253-267): SC#5 (E2E path) + verification gate
@.planning/STATE.md  # decision 17 (strict-clean); D-92/D-103 gate-re-point precedents (line 173)
@.planning/PRODUCT_SPEC_v0_1.md  # §18 Phase 8 DONE-when (2675), §24 gate (3612 verbatim), Flow 3 (1690), spec 3481 (perf gate)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-CONTEXT.md  # D-105 (E2E via service-level test), D-114 (gate re-point)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-RESEARCH.md  # Validation Architecture (545-580: quick/full commands, Wave 0 gaps), Pitfall 7 (gate mis-pointing)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-PATTERNS.md  # package.json edit (431, node -e exact-string assert precedent per 05-02-PLAN.md:130), E2E test conventions (450)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-VALIDATION.md  # 08-05-01 (verify:phase-8 gate + E2E)
@package.json  # verify:phase-8 currently 'tsc --noEmit && vitest run tests/core/content tests/addons tests/isolation'
@src/core/notes/save.ts  # saveNote seam (08-03)
@src/core/search/MiniSearchIndex.ts  # index (08-03)
@src/core/memory/MemoryEngine.ts  # buildPreferenceProfile (08-02) for the RICH-R-05 surface check
@src/core/memory/PreferenceMemoryStore.ts  # np_persona (08-01)
@src/core/content/PageContext.ts  # PageContext fixture source (Phase 6)
@src/core/storage/NotesDB.ts  # openNotesDB + canonical Note (08-01)
@tests/setup.ts  # chrome mocks + fake-indexeddb reset
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — E2E service-level test: Page → Note → NotesDB.put → index upsert → query</name>
  <files>tests/core/search/notes-search-e2e.test.ts</files>
  <read_first>08-RESEARCH.md Validation Architecture Wave 0 (E2E service-level test row 577) + Code Examples (save path 169-184 architecture diagram) · 08-PATTERNS.md E2E test conventions (450) · 08-CONTEXT.md D-105 (E2E proven by service-level test, not a shipped UI call-site) · PRODUCT_SPEC §18 Phase 8 DONE-when (2675) + Flow 3 (1690) · @src/core/notes/save.ts (saveNote signature) · @src/core/search/MiniSearchIndex.ts (query/upsert/__test__) · @src/core/storage/NotesDB.ts (openNotesDB + canonical Note) · @src/core/content/PageContext.ts (Phase-6 fixture shape) · @src/core/memory/MemoryEngine.ts (buildPreferenceProfile for the RICH-R-05 surface) · @src/core/memory/PreferenceMemoryStore.ts (np_persona) · @tests/setup.ts (chrome mocks + __resetIndexedDB)</read_first>
  <action>Create the §18 DONE-when E2E service-level test (D-105: proven by tests, not a UI call-site). Strict-clean. This is the phase's end-to-end tracer — it wires EVERY layer the phase touched into one runnable path.

`tests/core/search/notes-search-e2e.test.ts` (NEW):
1. Setup: `(globalThis as any).__resetIndexedDB()` + `(globalThis as any).__chromeStorageMap.clear()` in beforeEach (tests/setup.ts conventions). Re-seat the MiniSearchIndex __test__ (`indexTest.reset()`) so the module-load note:saved subscription is re-wired (Pitfall 6 — the re-invocable seam).
2. THE PATH (spec 2675 DONE-when): (a) build a Phase-6 `PageContext` fixture (url 'https://instance.service-now.com/incident/INC0012345', title 'High CPU on prod db', tabId 7 — the PageContentService-shaped input); (b) derive a canonical Note from it (title 'Incident: High CPU on prod db', content 'The database showed [[MySQL Guide]] high CPU. See the [[Runbook]] for the fix.', tags ['incident','perf'], source { kind: 'page-export', conversationId: undefined }, aiMeta { suggestedLinks: [], concepts: ['cpu'] }, version 1) — Note creation is the service-level step (the real NoteTagger/LLM pipeline is Phase 9; the seam is the canonical Note construction); (c) seed a 'MySQL Guide' note so the [[MySQL Guide]] wikilink RESOLVES; (d) `saveNote(db, note)` → the saved note.links contains the MySQL Guide id, unresolvedLinks carries '[[Runbook]]' (no such note — WIKI-ID-03); (e) the note:saved emit fires → MiniSearchIndex.upsert runs (await a microtask/tick — EventBus emit is synchronous per EventBus.ts:27-37, so the handler runs synchronously; assert via indexTest.buildCount or a direct query); (f) `MiniSearchIndex.query(db, 'mysql')` returns the saved note (title boost) — the MiniSearch leg of the DONE-when; (g) assert the full path produced exactly one hit with the note's id.
3. RICH-R-05 SURFACE CHECK (the phase requirement): PreferenceMemoryStore setPersonaOverrides({ tone: 'concise' }) → MemoryEngine.buildPreferenceProfile() JSON includes the override — the persona persistence surface works end-to-end (never touches the fact store — the profile reads np_persona only).
4. Assert the test itself is service-level: no chrome.extension / window / document access beyond the setup mocks (structural: the file imports only src/core modules + tests/setup mocks — no component imports).</action>
  <acceptance_criteria>`npx vitest run tests/core/search/notes-search-e2e.test.ts` passes; `pnpm run lint` passes strict-clean; `grep -c "saveNote" tests/core/search/notes-search-e2e.test.ts` >= 1 (the Flow-3 seam drives the path); `grep -c "MiniSearchIndex" tests/core/search/notes-search-e2e.test.ts` >= 1 (the query leg); `grep -c "buildPreferenceProfile" tests/core/search/notes-search-e2e.test.ts` >= 1 (the RICH-R-05 surface check); `grep -c "from '../components/\|from '../../components/" tests/core/search/notes-search-e2e.test.ts` equals 0 (service-level — no component imports); `grep -rn "NP-STRICT" tests/core/search/notes-search-e2e.test.ts | wc -l` equals 0</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/search/notes-search-e2e.test.ts && test "$(grep -c 'from .*components/' tests/core/search/notes-search-e2e.test.ts)" -eq 0 && test "$(grep -rn 'NP-STRICT' tests/core/search/notes-search-e2e.test.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>The §18 DONE-when E2E path is proven end-to-end: PageContext fixture → canonical Note → saveNote (parse → resolve → NotesDB.put → note:saved) → MiniSearchIndex upsert → query returns the note; unresolved wikilinks tracked (WIKI-ID-03); buildPreferenceProfile includes persona overrides (RICH-R-05); the test is service-level with zero component imports; strict-clean.</done>
  <!-- planner-discipline-allow: saveNote -->
  <!-- planner-discipline-allow: buildPreferenceProfile -->
  <!-- planner-discipline-allow: MiniSearchIndex -->
  <reversibility rating="reversible">D-105 — additive test-only proof; live wiring later (Phase 9 NMEM / Phase 15 RICH) is a caller edit (CONTEXT.md rating).</reversibility>
</task>

<task type="auto">
  <name>Task 2: verify:phase-8 re-point to the §18 gate (D-114, spec 3612 verbatim) + full-gate verification</name>
  <files>package.json</files>
  <read_first>08-PATTERNS.md package.json edit (431 — D-92/D-103 precedent, node -e exact-string assert per 05-02-PLAN.md:130) · 08-RESEARCH.md Pitfall 7 (gate mis-pointing: package.json:26 targets Phase-6/17 dirs) + Validation Architecture (555: full gate command) · 08-CONTEXT.md D-114 · PRODUCT_SPEC §24 (3612 verbatim) · @package.json (line 26 verify:phase-8 current value)</read_first>
  <action>Re-point the verify:phase-8 gate (D-114). This is a package.json script edit ONLY — no dependency changes (Package Legitimacy Audit vacuous; Phase 8 installs nothing).

1. Edit `package.json` line 26: replace `"verify:phase-8": "tsc --noEmit && vitest run tests/core/content tests/addons tests/isolation"` with `"verify:phase-8": "tsc --noEmit && vitest run tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts"` — the spec 3612 canonical string VERBATIM (D-114). The old target (tests/core/content, tests/addons, tests/isolation) belongs to Phase 6/17 — those keep their own gates (verify:phase-6/verify:phase-17 untouched; do NOT add dirs or reorder tokens).
2. Verify the exact string programmatically (05-02-PLAN.md:130 precedent): `node -e "const s=require('./package.json').scripts['verify:phase-8']; if (s !== 'tsc --noEmit && vitest run tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts') { console.error('MISMATCH: '+s); process.exit(1) }"` — exact-match, no tolerance.
3. Run the full phase gate: `pnpm run verify:phase-8` — must pass GREEN with all §18-required test files + the E2E test + the perf gate (all created by plans 08-01…08-05). Then run the full suite `pnpm test` to confirm no regressions in the Phase-6/17 dirs that the gate no longer covers (they still run under `pnpm test`).</action>
  <acceptance_criteria>`node -e` exact-string assert passes (script === spec 3612 verbatim); `pnpm run verify:phase-8` exits 0 (tsc strict-clean + all §18 memory/search/LinkParser tests green, incl. the E2E + perf gate); `pnpm test` full suite exits 0 (no regression in tests/core/content tests/addons tests/isolation — they still run under the full suite); `grep -c "tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts" package.json` >= 1 (the re-pointed gate string present); `grep -c "tests/core/content" package.json` equals 0 for the verify:phase-8 script (old target gone from THIS script only)</acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts['verify:phase-8']; if (s !== 'tsc --noEmit && vitest run tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts') { console.error('MISMATCH: '+s); process.exit(1) }" && pnpm run verify:phase-8</automated>
  </verify>
  <done>verify:phase-8 is re-pointed to the spec 3612 canonical gate string (D-114) and passes GREEN — tsc strict-clean + all §18-required tests (MemoryEngine/MemoryScorer/UserMemoryStore/MiniSearchIndex/LinkParser) + the E2E + the perf gate; the full suite still passes; the Phase-6/17 dirs remain covered by their own gates and `pnpm test`.</done>
  <!-- planner-discipline-allow: verify:phase-8 -->
  <!-- planner-discipline-allow: tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts -->
  <!-- planner-discipline-allow: tests/core/content -->
  <reversibility rating="reversible">D-114 — package.json script edit (D-68/D-78/D-92/D-103 precedent; CONTEXT.md rating).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E fixtures → service modules | Synthetic PageContext/Note data flows through the real save→index→query path — fixture-only, no real user data |
| verify:phase-8 script → test runner | The gate string decides which tests ship in CI — a drift here silently drops the §18 coverage |
| phase gate → full suite | The re-pointed gate must not orphan the Phase-6/17 dirs (they keep verify:phase-6/17 + `pnpm test`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-14 | Tampering | package.json verify:phase-8 script | medium | mitigate | Exact-string node -e assert against spec 3612 verbatim (D-114); no token reordering/append; Phase-6/17 dirs keep their own gates (verify:phase-6/17) + run under `pnpm test` |
| T-8-15 | Information Disclosure | E2E test fixtures | low | accept | Fixtures are synthetic (service-now.com-shaped URL, invented note titles) — no real customer data, no real secrets; chrome mocks are Map-backed (tests/setup.ts) |
| T-8-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO installs this phase — the only package.json change is the script string; Package Legitimacy Audit vacuous (08-RESEARCH.md); executor must not add any dependency |

</threat_model>

<verification>
- **Per-task commits:** `pnpm run lint` + the E2E test / the gate command (08-VALIDATION.md sampling rate).
- Gate correctness: exact-string node -e assert (D-114) — a mismatch fails the task before any test run.
- Full-suite regression: `pnpm test` green after the re-point (the Phase-6/17 dirs still run under the full suite even though verify:phase-8 no longer targets them).
- Phase close: `pnpm run verify:phase-8` green is the §18 DONE gate (08-VALIDATION.md 08-05-01).
</verification>

<success_criteria>
1. The §18 DONE-when E2E path works end-to-end at the service level: PageContext → Note → saveNote → MiniSearchIndex → query (SC#5); buildPreferenceProfile includes persona overrides (RICH-R-05).
2. verify:phase-8 is re-pointed to the spec 3612 canonical string (D-114) and passes green with all five §18-required test files + the E2E + the perf gate; the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/08-knowledge-base-memory-minisearch-notes/08-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-8 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| tests/core/search/notes-search-e2e.test.ts | §18 DONE-when E2E — Page→Note→saveNote→index→query + RICH-R-05 profile surface check | **[this plan]** |
| package.json | verify:phase-8 re-point to spec 3612 verbatim (D-114) | **[this plan]** |