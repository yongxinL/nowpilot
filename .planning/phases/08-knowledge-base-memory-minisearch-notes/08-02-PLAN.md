---
phase: 08-knowledge-base-memory-minisearch-notes
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/core/memory/UserMemoryStore.ts
  - src/core/memory/MemoryScorer.ts
  - src/core/memory/ConversationMemoryStore.ts
  - src/core/memory/MemoryExtractor.ts
  - src/core/memory/WorkingMemory.ts
  - src/core/memory/MemoryEngine.ts
  - src/core/storage/WriteJournal.ts
  - tests/core/memory/UserMemoryStore.test.ts
  - tests/core/memory/MemoryScorer.test.ts
  - tests/core/memory/ConversationMemoryStore.test.ts
  - tests/core/memory/MemoryExtractor.test.ts
  - tests/core/memory/WorkingMemory.test.ts
  - tests/core/memory/MemoryEngine.test.ts
autonomous: true
requirements: [RICH-R-05]
user_setup: []

# scope-sanity note (checker advisory): 13 files but cohesive — one concern
# (the §18 memory subsystem); per-task commit discipline keeps each task's
# diff bounded to its own concern (T1: 4 files, T2: 5 files, T3: 4 files).

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - statement: "D-104: UserMemoryStore facts persist to MemoryDB.userFacts (§3.4 canonical shape) as bodies; chrome.storage.local np_facts (max 500, LRU) holds the metadata index (ids + recency + useCount); ConversationMemoryStore bodies live in MemoryDB.messages, summaries in MemoryDB.conversationSummaries, np_conversation_meta (LRU 10 active / 100 archived) in chrome.storage.local; PreferenceMemoryStore owns np_persona (plan 08-01)"
      verification: backstop
    - statement: "D-113: MemoryScorer implements the §3.4 formula verbatim (spec 618-628): keywordScore*0.45 + tagScore*0.25 + recencyScore*0.15 + useCountScore*0.10 + confidenceScore*0.05 with keyword = matchedQueryTerms/totalQueryTerms, tag = matchedTags/max(1,memoryTags.length), recency = clamp(1-(now-updatedAt)/(30*DAY),0,1), useCount = min(1,useCount/20); every sub-score normalized to [0,1] (ROADMAP SC#3)"
      verification: backstop
    - statement: "D-106: §15.3 compactor verbatim — fires at messageCount % 12 === 0 → keep head (system + first 2) + summary of middle + tail (last 4); archive after 30 min idle; LRU max 10 active / 100 archived; evict-conversation via WriteJournal (op already in the union); summariser is a pluggable seam proven by a deterministic test stub"
      verification: backstop
    - statement: "D-105: MemoryEngine is create-only — retrieveConversationMemory(), retrieveUserMemory() (top-5 / top-3 tiny / ≤1000 tokens / never secrets, §3.4 spec 630-635), buildPreferenceProfile() (compact JSON incl. persona overrides, RICH-R-05 DONE-when), retrieveMemoryHints() → RetrievedMemory[] matching the Phase-7 [MEMORY] trust-builder contract (contextItems.ts:70-93: hint.id + hint.content, trust 'retrieved', authority false); zero ContextOptimizer/AgentOrchestrator edits"
      verification: backstop
    - statement: "RICH-R-05 DONE-when (plan 02): MemoryEngine.buildPreferenceProfile() returns compact JSON including persona overrides read from np_persona (PreferenceMemoryStore) — never the fact store (R2); asserted in MemoryEngine.test.ts"
      verification: backstop
    - statement: "O.10/Pitfall 1: WorkingMemory uses redactSensitiveValue (src/core/security/redactSensitive.ts:68-71) with a TODO(Phase 11) swap comment — NOT the nonexistent TraceRedactor import; block cap ≤ 300 tokens; single-writer (isPrimaryWriter gate); §3.6 template verbatim"
      verification: backstop
    - statement: "D-113: MemoryExtractor ships only the memoryFacts schema + parse seam (mirrors NoteTagResultSchema/ConfidentFact, spec 4764-4773); the LLM extraction call + NMEM-02 wiring is Phase 9 — no LLM import/call in this module"
      verification: backstop
  artifacts:
    - path: "src/core/memory/UserMemoryStore.ts"
      provides: "D-104 user-facts store — MemoryDB.userFacts bodies (§3.4 canonical shape), np_facts LRU ≤500 metadata (ids + recency + useCount), redaction before IDB write, single-writer-gated upserts"
      exports: ["useUserMemoryStore", "UserMemoryStore", "NP_FACTS_MAX"]
    - path: "src/core/memory/MemoryScorer.ts"
      provides: "D-113 §3.4 verbatim scoring — scoreMemory(memory, query, now) + exported weight constants (MEMORY_SCORE_KEYWORD=0.45 etc.); scores ∈ [0,1]"
      exports: ["scoreMemory", "MEMORY_SCORE_KEYWORD", "MEMORY_SCORE_TAG", "MEMORY_SCORE_RECENCY", "MEMORY_SCORE_USE_COUNT", "MEMORY_SCORE_CONFIDENCE", "MEMORY_RECENCY_WINDOW_DAYS"]
    - path: "src/core/memory/ConversationMemoryStore.ts"
      provides: "D-104/D-106 conversation memory — MemoryDB.messages bodies (compound key [conversationId, seq]), np_conversation_meta LRU 10/100, 12-message compactor (head+summary+tail-4) with pluggable Summarizer seam, archive-after-30-min, evict-conversation via WriteJournal"
      exports: ["ConversationMemoryStore", "CONVERSATION_COMPACTOR_MODULO", "CONVERSATION_ACTIVE_MAX", "CONVERSATION_ARCHIVED_MAX", "CONVERSATION_IDLE_ARCHIVE_MS"]
    - path: "src/core/memory/MemoryExtractor.ts"
      provides: "D-113 extraction schema + parse seam — memoryFacts zod schema (content + confidence [0,1] + type), parseMemoryFacts(output); NO LLM wiring"
      exports: ["memoryFactsSchema", "parseMemoryFacts", "MemoryFact"]
    - path: "src/core/memory/WorkingMemory.ts"
      provides: "O.10 working memory — initWorkingMemory/updateWorkingMemory, MAX_WORKING_MEMORY_TOKENS=300, redactSensitiveValue redaction (TODO Phase 11 swap), truncateToTokens, single-writer gate"
      exports: ["initWorkingMemory", "updateWorkingMemory", "MAX_WORKING_MEMORY_TOKENS"]
    - path: "src/core/memory/MemoryEngine.ts"
      provides: "D-105 create-only orchestrator — retrieveConversationMemory / retrieveUserMemory (top-5/top-3 tiny/≤1000 tokens) / buildPreferenceProfile (compact JSON incl. persona overrides) / retrieveMemoryHints → RetrievedMemory[]"
      exports: ["MemoryEngine", "MEMORY_HINTS_TOP_K", "MEMORY_HINTS_TINY_K", "MEMORY_HINTS_MAX_TOKENS"]
    - path: "src/core/storage/WriteJournal.ts"
      provides: "D-106 — register 'evict-conversation' JournalSteps (curried factory createEvictConversationSteps, delete conversation rows + drop np_conversation_meta LRU entry); op already in the union (src/types/storage.ts:48)"
      exports: ["createEvictConversationSteps"]
    - path: "tests/core/memory/UserMemoryStore.test.ts"
      provides: "§18 required — LRU ≤500 eviction, redaction, np_facts metadata ↔ MemoryDB.userFacts body consistency"
      exports: []
    - path: "tests/core/memory/MemoryScorer.test.ts"
      provides: "§18 required — verbatim weights, sub-scores ∈ [0,1], recency window, keyword/tag matching"
      exports: []
    - path: "tests/core/memory/ConversationMemoryStore.test.ts"
      provides: "compactor 12-rule, LRU caps, archive-after-30-min, evict-conversation steps, deterministic stub summariser"
      exports: []
    - path: "tests/core/memory/MemoryExtractor.test.ts"
      provides: "memoryFacts parse seam — valid output → facts, invalid → typed failure, confidence bounds"
      exports: []
    - path: "tests/core/memory/WorkingMemory.test.ts"
      provides: "O.10 — template init, update redacts, ≤300-token cap, truncation"
      exports: []
    - path: "tests/core/memory/MemoryEngine.test.ts"
      provides: "§18 required + RICH-R-05 DONE-when — conversation summary + recent turns, user memory top-5/top-3 tiny, ≤1000-token budget, buildPreferenceProfile incl. persona overrides, retrieveMemoryHints shape"
      exports: []
  key_links:
    - from: "src/core/memory/MemoryEngine.ts"
      to: "src/core/context/trust/contextItems.ts"
      via: "retrieveMemoryHints() → RetrievedMemory[] — the Phase-7 [MEMORY] builder (D-94: trust 'retrieved', authority false) consumes hint.id + hint.content; MemoryEngine produces plain data, never instructions (CTX-02)"
      pattern: "memoryHints"
    - from: "src/core/memory/ConversationMemoryStore.ts"
      to: "src/core/storage/WriteJournal.ts"
      via: "evict-conversation runs through runJournaled with createEvictConversationSteps — §15.3 eviction spans IDB + chrome.storage.local; journal makes it atomic-on-recovery (O.11)"
      pattern: "evict-conversation"
    - from: "src/core/memory/MemoryEngine.ts"
      to: "src/core/memory/PreferenceMemoryStore.ts"
      via: "buildPreferenceProfile reads np_persona via PreferenceMemoryStore (getState) — persona overrides flow into the compact profile (RICH-R-05/R2, never the fact store)"
      pattern: "buildPreferenceProfile"
    - from: "src/core/memory/WorkingMemory.ts"
      to: "src/core/security/redactSensitive.ts"
      via: "redactSensitiveValue(value) — §4.4 never store secrets; TODO(Phase 11): swap to TraceRedactor (the module does not exist yet, Pitfall 1)"
      pattern: "redactSensitiveValue"
  prohibitions:
    - "Zero ContextOptimizer/AgentOrchestrator/useChatStreaming edits — MemoryEngine is create-only (D-105/D-69/D-81)"
    - "Memory bodies never store raw secrets — redactSensitiveValue before every IDB/chrome.storage.local write (O.10 swap comment; §3.4 never inject secrets/raw customer data)"
    - "No real LLM calls anywhere in this plan — summariser is a test stub seam (D-106); MemoryExtractor parses only (D-113, Phase 9 wiring)"
    - "No MemoryRecord/MemoryKind/MEM-01…05 constructs — Phase 10 scope (spec 4903-4915)"
    - "WriteJournalOperation union is NOT edited — 'evict-conversation' already exists (src/types/storage.ts:48); only steps are registered"
    - "Zero new @ts-expect-error NP-STRICT markers (STATE.md decision 17)"
    - "Breadcrumb (PROHIB_ABSENT fallback): canon-security items (OWASP/prototype-pollution/generic injection) dropped — covered by threat model + Phase-7 trust layer (CTX-02, memory is untrusted retrieved data)"

---

<objective>
Memory-subsystem plan: the three §18 memory stores + scoring + extraction + the create-only MemoryEngine — `UserMemoryStore` (np_facts LRU ≤500 + MemoryDB.userFacts), `MemoryScorer` (§3.4 verbatim), `ConversationMemoryStore` (compactor seam + LRU 10/100 + WriteJournal evict), `MemoryExtractor` (schema + parse seam), `WorkingMemory` (O.10), and `MemoryEngine` (the producers the Phase-7 trust layer consumes, incl. buildPreferenceProfile for RICH-R-05).

Purpose: this is the phase's core value — "Memory is system-owned; the LLM does not own it" (§3.1). The stores write through the Phase-2 foundations (MemoryDB, chrome.storage.local, WriteJournal, WorkspaceElection) so the memory layer is real, tested, single-writer-disciplined, and privacy-redacted — while live chat/AgentOrchestrator adoption stays deferred (D-105 create-only).

Output: the thirteen files in frontmatter, strict-clean, with all five §18-required memory-side behaviors proven: conversation summary + recent turns returned, user memory top-5 (top-3 tiny), scores in [0,1], persona overrides in the preference profile (RICH-R-05), and never secrets.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  # Phase 8 block (253-267): SC#1-3 + gate
@.planning/STATE.md  # decision 19 (isPrimaryWriter delegation), decision 17 (strict-clean)
@.planning/PRODUCT_SPEC_v0_1.md  # §3.1-3.6 (546-694: principle, scoring formula 618-628, injection 630-635, working memory 678-684), §15.1 (np_facts max 500 LRU, 1938-1962), §15.3 (2005-2010: compactor 12-rule, LRU 10/100, archive 30min), §23 (3554: metadata local / bodies IDB), O.10 (6596-6622), O.11 (evict via journal)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-CONTEXT.md  # D-104/D-105/D-106/D-113 + discretion (WorkingMemory module vs folded; store layout)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-RESEARCH.md  # Patterns 1/2/5, Pitfalls 1/2, Code Examples (§3.6 O.10 389-417), Security Domain rows, Standard Stack
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-PATTERNS.md  # ConversationMemoryStore (109-135), UserMemoryStore (138-154), MemoryScorer (189-208), MemoryExtractor (210-230), MemoryEngine (233-251), WorkingMemory (253-271), WriteJournal edit (423-429)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-VALIDATION.md  # 08-02-01 (MemoryEngine.test.ts + UserMemoryStore.test.ts)
@src/core/storage/MemoryDB.ts  # messages/userFacts/conversationSummaries stores + compound-key constraint
@src/core/storage/WriteJournal.ts  # registerJournalSteps (78-83), createChatTurnSteps factory (297-329) — the evict factory precedent
@src/core/workspace/WorkspaceStore.ts  # isPrimaryWriter (23-25)
@src/core/context/TokenBudget.ts  # countTokensHeuristic (44-55)
@src/core/security/redactSensitive.ts  # redactSensitiveValue (68-71)
@src/core/memory/types.ts  # canonical types from 08-01 (RetrievedMemory/UserPreferences/UserMemoryFact)
@src/core/ai/persona/PersonaProfile.ts  # DEFAULT_PERSONA (25) for buildPreferenceProfile
@src/core/context/trust/contextItems.ts  # [MEMORY] builder contract (70-93)
@tests/setup.ts  # __resetIndexedDB (9-11) + __chromeStorageMap (56-80)
@tests/core/storage/IndexedDBMigrator.test.ts  # DB-test conventions (43-48)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — user-memory retrieval spine: UserMemoryStore (np_facts LRU + MemoryDB.userFacts) + MemoryScorer (§3.4)</name>
  <files>src/core/memory/UserMemoryStore.ts, src/core/memory/MemoryScorer.ts, tests/core/memory/UserMemoryStore.test.ts, tests/core/memory/MemoryScorer.test.ts</files>
  <read_first>08-PATTERNS.md UserMemoryStore section (138-154 — LRU eviction shape, partialize analog) + MemoryScorer section (189-208 — pure-function module + verbatim weights) · 08-RESEARCH.md Pattern 2 (metadata-local/bodies-IDB 252-254) + Pattern 5 (spec-verbatim values: scoring formula 279-293) + Pitfall 2 (bootstrap shape ≠ §3.4) + Assumption A6 (np_facts metadata shape) · 08-CONTEXT.md D-104/D-113 · PRODUCT_SPEC §3.4 (601-635: fact shape, scoring, injection top-5/top-3/≤1000) + §15.1 (np_facts max 500, 1938-1962) · @src/core/memory/types.ts (UserMemoryFact canonical) · @src/core/storage/MemoryDB.ts (openMemoryDB, userFacts store) · @src/core/security/redactSensitive.ts (redactSensitiveValue 68-71) · @src/core/workspace/WorkspaceStore.ts (isPrimaryWriter 23-25) · @tests/setup.ts (reset conventions)</read_first>
  <action>Build the user-memory retrieval spine end-to-end — the thinnest vertical slice through store → metadata → score → return. Strict-clean. Header comments: `// <Module> — D-104/D-113 <purpose>` + spec refs.

1. `src/core/memory/MemoryScorer.ts` (NEW) — pure-function module (TokenBudget.ts:44-52 pattern). Export the §3.4 verbatim weights as named constants: `MEMORY_SCORE_KEYWORD = 0.45`, `MEMORY_SCORE_TAG = 0.25`, `MEMORY_SCORE_RECENCY = 0.15`, `MEMORY_SCORE_USE_COUNT = 0.10`, `MEMORY_SCORE_CONFIDENCE = 0.05`, `MEMORY_RECENCY_WINDOW_DAYS = 30`. `export function scoreMemory(memory: Pick<UserMemoryFact,'tags'|'updatedAt'|'useCount'|'confidence'>, queryTerms: string[], now: number): number` implementing spec 618-628 exactly: keywordScore = matchedQueryTerms / max(1, totalQueryTerms) (match = query term substring-contained in a lowercase content/tag scan — use the fact's tags + a contentMatch boolean passed in, or accept a `matches: number` param; keep the sub-scores independently normalised to [0,1]); tagScore = matchedTags / max(1, memoryTags.length); recencyScore = clamp(1 - (now - updatedAt) / (30 * DAY_MS), 0, 1); useCountScore = min(1, useCount / 20); confidenceScore = confidence (0..1); score = weighted sum. NO invented fields/weights (D-38).
2. `src/core/memory/UserMemoryStore.ts` (NEW) — module singleton (PageContentService.ts:1-45 pattern) owning: (a) BODIES → `openMemoryDB()` → `db.put('userFacts', fact)` with the §3.4 canonical UserMemoryFact shape from @/core/memory/types (Pitfall 2 — the MemoryDB re-export from 08-01 is the value shape); (b) METADATA → chromeStorageAdapter.setItem('np_facts', JSON metadata array capped at `NP_FACTS_MAX = 500` with per-fact `{ id, updatedAt, useCount }` (A6 — ids + recency + useCount per D-104), LRU eviction: when full, drop the lowest (useCount, updatedAt) entries; (c) REDACTION — every fact body passes through `redactSensitiveValue` before the IDB put (`// TODO(Phase 11): swap to TraceRedactor` — Pitfall 1); (d) SINGLE-WRITER — upsert/evict paths gate on `isPrimaryWriter()` (WorkspaceStore.ts:23-25); non-primary surfaces are read-only; (e) API: `upsertFact(fact)`, `getTopFacts(query, { k, now })` scoring via scoreMemory then returning top-k with bodies fetched from IDB only for the winners (A6 perf — scoring reads metadata without opening IDB where feasible; if bodies are needed for keyword scoring, fetch on demand and document). debugLog('MEMORY_FACT_UPSERT', ...) / 'MEMORY_FACT_EVICTED' on the LRU path.
3. `tests/core/memory/MemoryScorer.test.ts` (NEW, §18 required) — (a) verbatim weights: assert the five constants and that a perfect keyword+tag+recency+useCount+confidence hit scores ≈ 1.0 and a zero hit ≈ 0.0; (b) sub-score bounds: any input → score ∈ [0,1] (ROADMAP SC#3); (c) recency: updatedAt = now-1d → recency ≈ 1, updatedAt = now-40d → 0 (30-day window); (d) useCount: 0 → 0, 20 → 1, 50 → clamped 1; (e) tag matching math; (f) determinism: same inputs → same score.
4. `tests/core/memory/UserMemoryStore.test.ts` (NEW, §18 required) — reset __resetIndexedDB() + __chromeStorageMap.clear() in beforeEach. (a) upsertFact → body in MemoryDB.userFacts (read via openMemoryDB) AND metadata entry in the np_facts chromeStorageMap blob; (b) redaction — a fact whose content contains an apiKey-shaped secret round-trips through the store with the secret redacted in the persisted body (assert the persisted value differs from the raw input); (c) LRU — upsert 505 facts → np_facts metadata length ≤ 500 and the lowest-scored/oldest evicted (assert eviction debugLog or metadata count); (d) top-k retrieval: 10 facts, getTopFacts(query,{k:5}) returns 5 in score-desc order; (e) single-writer — when isPrimaryWriter() is false (mock the module seam), upsertFact is a no-op (assert no new IDB row + no metadata growth).</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `npx vitest run tests/core/memory/MemoryScorer.test.ts tests/core/memory/UserMemoryStore.test.ts` passes; `grep -n "MEMORY_SCORE_KEYWORD = 0.45" src/core/memory/MemoryScorer.ts` shows the verbatim weight; `grep -n "MEMORY_RECENCY_WINDOW_DAYS = 30" src/core/memory/MemoryScorer.ts` shows the window; `grep -c "NP_FACTS_MAX = 500" src/core/memory/UserMemoryStore.ts` >= 1; `grep -n "redactSensitiveValue" src/core/memory/UserMemoryStore.ts` shows the redaction seam; `grep -c "TraceRedactor" src/core/memory/UserMemoryStore.ts` >= 1 (the TODO swap comment); `grep -rn "NP-STRICT" src/core/memory/MemoryScorer.ts src/core/memory/UserMemoryStore.ts tests/core/memory/MemoryScorer.test.ts tests/core/memory/UserMemoryStore.test.ts | wc -l` equals 0; `git status --porcelain src/core/memory/UserMemoryStore.ts src/core/memory/MemoryScorer.ts tests/core/memory/UserMemoryStore.test.ts tests/core/memory/MemoryScorer.test.ts | wc -l` >= 4</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/memory/MemoryScorer.test.ts tests/core/memory/UserMemoryStore.test.ts && test "$(grep -rn 'NP-STRICT' src/core/memory/MemoryScorer.ts src/core/memory/UserMemoryStore.ts tests/core/memory/MemoryScorer.test.ts tests/core/memory/UserMemoryStore.test.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>The user-memory spine is green and committed: UserMemoryStore persists §3.4-shaped facts to MemoryDB.userFacts with a ≤500-entry np_facts LRU metadata index, redacts bodies before IDB writes, gates writes on isPrimaryWriter(), and returns top-k scored results; MemoryScorer implements the §3.4 formula verbatim with exported constants; both §18-required tests pass with all scores in [0,1].</done>
  <!-- planner-discipline-allow: redactSensitiveValue -->
  <!-- planner-discipline-allow: NP_FACTS_MAX -->
  <!-- planner-discipline-allow: NP_FACTS_MAX = 500 -->
  <!-- planner-discipline-allow: TraceRedactor -->
  <!-- planner-discipline-allow: MAX_WORKING_MEMORY_TOKENS = 300 -->
  <reversibility rating="reversible">D-104/D-113 — additive store + pure scorer; moving the LRU metadata shape later is a local edit (CONTEXT.md ratings).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Conversation memory + working block — ConversationMemoryStore (compactor seam) + WriteJournal evict steps + WorkingMemory (O.10)</name>
  <files>src/core/memory/ConversationMemoryStore.ts, src/core/storage/WriteJournal.ts, src/core/memory/WorkingMemory.ts, tests/core/memory/ConversationMemoryStore.test.ts, tests/core/memory/WorkingMemory.test.ts</files>
  <read_first>08-PATTERNS.md ConversationMemoryStore (109-135 — MemoryDB write-through + compactor seam) + WorkingMemory (253-271 — O.10 with redaction swap) + WriteJournal edit (423-429 — createEvictConversationSteps factory) · 08-RESEARCH.md Pattern 5 (§15.3 verbatim values) + Pitfall 1 (TraceRedactor missing — use redactSensitiveValue) + Code Example §3.6/O.10 (389-417) + Open Question on evict (delete path: direct API, no new event) · 08-CONTEXT.md D-104/D-106 · PRODUCT_SPEC §3.3 (conversation memory 555-600), §15.3 (2005-2010), O.10 (6596-6622), O.11 (evict via WriteJournal) · @src/core/storage/MemoryDB.ts (messages compound key [conversationId, seq] — the value MUST carry both fields, 12-15; byConversation index) · @src/core/storage/WriteJournal.ts (registerJournalSteps 78-83, createChatTurnSteps 297-329, isSupportedOperation 99-101) · @src/core/context/types.ts (Summarizer interface 52-54 — the seam shape) · @src/core/security/redactSensitive.ts (redactSensitiveValue 68-71) · @src/types/harness.ts (WorkingMemory + WORKING_MEMORY_TEMPLATE from 08-01) · @src/core/workspace/WorkspaceStore.ts (isPrimaryWriter 23-25)</read_first>
  <action>Build conversation memory with the §15.3 compactor seam, the WriteJournal evict-conversation steps, and the O.10 working-memory block. Strict-clean.

1. `src/core/memory/ConversationMemoryStore.ts` (NEW) — module singleton owning: (a) BODIES → `openMemoryDB()` → `db.put('messages', { conversationId, seq, role, content, timestamp })` (compound-key constraint: the value MUST carry conversationId + seq — MemoryDB.ts:12-15) and `db.put('conversationSummaries', { conversationId, summary, keyPoints, updatedAt })`; (b) METADATA → chromeStorageAdapter.setItem('np_conversation_meta', LRU map capped `CONVERSATION_ACTIVE_MAX = 10` active / `CONVERSATION_ARCHIVED_MAX = 100` archived (spec §15.3 verbatim); (c) COMPACTOR SEAM (D-106) — `appendMessage(msg)` counts messages per conversation; when `messageCount % CONVERSATION_COMPACTOR_MODULO === 0` (12), compact: keep head (system message + first 2 turns) + `summarizer.summarize(middleSections)` (the pluggable Summarizer seam — context/types.ts:52-54 interface shape, but here it takes the middle messages and returns { text, tokens }) + tail (last 4 turns verbatim); store the summary via `db.put('conversationSummaries', ...)`; the summarizer is a CONSTRUCTOR/INJECTED dependency (`createConversationMemoryStore({ summarizer })` factory — tests inject a deterministic stub, e.g. `summarize: (msgs) => ({ text: 'STUB: ' + msgs.length + ' messages', tokens: 8 })`); (d) ARCHIVE — when a conversation is idle > `CONVERSATION_IDLE_ARCHIVE_MS` (30 * 60 * 1000), move it active → archived; (e) EVICT — when archived exceeds 100, evict the oldest via `runJournaled` with the registered `evict-conversation` steps (O.11 atomicity — eviction spans IDB rows + np_conversation_meta entry); (f) SINGLE-WRITER — append/compact/archive/evict gate on `isPrimaryWriter()`; (g) API: `appendMessage`, `getRecentTurns(conversationId, k)`, `getSummary(conversationId)`, `archiveIdleConversations(now)`, `evictConversation(conversationId)`. debugLog on compactor fire ('CONVERSATION_COMPACTED'), archive ('CONVERSATION_ARCHIVED'), evict ('CONVERSATION_EVICTED').
2. `src/core/storage/WriteJournal.ts` (EDIT) — add the D-106 evict steps. `export interface EvictConversationDeps { deleteMessages: (conversationId: string) => Promise<void>; deleteSummary: (conversationId: string) => Promise<void>; dropMeta: (conversationId: string) => Promise<void>; }` and `export function createEvictConversationSteps(deps: EvictConversationDeps): (conversationId: string) => JournalStep[]` returning steps that (apply) delete MemoryDB.messages rows for the conversation + conversationSummaries row + drop the np_conversation_meta entry, (rollback) no-op (eviction is idempotent — deleting already-deleted rows is safe). Mirror the createChatTurnSteps curried-factory precedent (297-329). Register via `registerJournalSteps('evict-conversation', ...)` at module load or document that ConversationMemoryStore registers at boot (D-32 gating: isSupportedOperation('evict-conversation') must return true after registration). Do NOT edit the WriteJournalOperation union (already contains 'evict-conversation' — src/types/storage.ts:48).
3. `src/core/memory/WorkingMemory.ts` (NEW) — Appendix O.10 verbatim (spec 6596-6622) with the Pitfall-1 redaction swap: import `{ WORKING_MEMORY_TEMPLATE, type WorkingMemory } from '@/types/harness'` (canonical home, 08-01) + `{ redactSensitiveValue } from '../security/redactSensitive'` with `// TODO(Phase 11): swap to TraceRedactor`. `MAX_WORKING_MEMORY_TOKENS = 300` (§3.6). `initWorkingMemory(resourceId)` → `{ resourceId, markdown: WORKING_MEMORY_TEMPLATE, tokens: estimate(WORKING_MEMORY_TEMPLATE), updatedAt: Date.now() }`. `updateWorkingMemory(cur, patch: Partial<Record<'Name'|'Role / Team'|'Environment'|'Preferences'|'Long-term Goals', string>>)` — redact each value via redactSensitiveValue BEFORE the markdown replace (`md.replace(new RegExp(\`(- \\*\\*${field}\\*\\*:).*\`), \`$1 ${safe}\`)`), re-estimate (`estimate = (s) => Math.ceil(s.length / 4)` — the countTokensHeuristic len/4 analog), truncate via `truncateToTokens(md, MAX_WORKING_MEMORY_TOKENS)` when over cap. Gate writes on `isPrimaryWriter()` (§13 single-writer); non-primary update returns cur unchanged + debugLog('WORKING_MEMORY_NON_PRIMARY_SKIP', ...). Header comment: O.10 + Pitfall 1 + Phase-11 swap point.
4. `tests/core/memory/ConversationMemoryStore.test.ts` (NEW) — reset DB + chrome mocks in beforeEach. (a) append 12 messages → compactor fires: MemoryDB.messages keeps head (system + first 2) + tail (last 4), conversationSummaries has the stub summary (assert stub text); (b) 11 messages → no compaction; (c) LRU: 11 active conversations → oldest archived (active ≤ 10); (d) archive-after-30-min: conversation idle > CONVERSATION_IDLE_ARCHIVE_MS (inject a fake now) → archived; (e) evict: register evict-conversation steps via createEvictConversationSteps + registerJournalSteps, evictConversation → messages rows + summary + np_conversation_meta entry gone; isSupportedOperation('evict-conversation') true; (f) single-writer: isPrimaryWriter false → appendMessage no-op.
5. `tests/core/memory/WorkingMemory.test.ts` (NEW) — (a) initWorkingMemory produces the §3.6 template with tokens = estimate; (b) updateWorkingMemory replaces the **Name:** value and re-estimates tokens; (c) a secret-shaped value (apiKey-style) is redacted in the resulting markdown (persisted block never contains the raw secret); (d) a huge patch truncates to ≤ 300 tokens; (e) non-primary gate: update returns cur unchanged when isPrimaryWriter() false.</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `npx vitest run tests/core/memory/ConversationMemoryStore.test.ts tests/core/memory/WorkingMemory.test.ts` passes; `grep -n "CONVERSATION_COMPACTOR_MODULO = 12" src/core/memory/ConversationMemoryStore.ts` shows the verbatim rule; `grep -n "CONVERSATION_ACTIVE_MAX = 10\|CONVERSATION_ARCHIVED_MAX = 100" src/core/memory/ConversationMemoryStore.ts` shows both LRU caps; `grep -n "CONVERSATION_IDLE_ARCHIVE_MS" src/core/memory/ConversationMemoryStore.ts` shows the 30-min constant (30*60*1000); `grep -n "createEvictConversationSteps" src/core/storage/WriteJournal.ts` shows the factory; `grep -n "'evict-conversation'" src/core/storage/WriteJournal.ts` shows the registration; `grep -c "MAX_WORKING_MEMORY_TOKENS = 300" src/core/memory/WorkingMemory.ts` >= 1; `grep -n "redactSensitiveValue" src/core/memory/WorkingMemory.ts` shows the redaction seam; `grep -rn "NP-STRICT" src/core/memory/ConversationMemoryStore.ts src/core/memory/WorkingMemory.ts src/core/storage/WriteJournal.ts tests/core/memory/ConversationMemoryStore.test.ts tests/core/memory/WorkingMemory.test.ts | wc -l` equals 0</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/memory/ConversationMemoryStore.test.ts tests/core/memory/WorkingMemory.test.ts && test "$(grep -rn 'NP-STRICT' src/core/memory/ConversationMemoryStore.ts src/core/memory/WorkingMemory.ts src/core/storage/WriteJournal.ts tests/core/memory/ConversationMemoryStore.test.ts tests/core/memory/WorkingMemory.test.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>ConversationMemoryStore ships the §15.3 compactor (12-message rule, head+summary+tail-4, archive-after-30-min, LRU 10/100) with a pluggable Summarizer seam proven by a deterministic stub; evict-conversation runs atomically through registered WriteJournal steps (O.11); WorkingMemory implements O.10 with redactSensitiveValue redaction and the Phase-11 swap comment; both tests green, strict-clean.</done>
  <!-- planner-discipline-allow: evict-conversation -->
  <!-- planner-discipline-allow: CONVERSATION_COMPACTOR_MODULO -->
  <reversibility rating="reversible">D-104/D-106 — seam + deterministic tests; swapping the stub summariser for an LLM call later is a wiring edit (CONTEXT.md rating).</reversibility>
</task>

<task type="auto">
  <name>Task 3: MemoryExtractor schema seam + MemoryEngine create-only producers (RICH-R-05 DONE-when)</name>
  <files>src/core/memory/MemoryExtractor.ts, src/core/memory/MemoryEngine.ts, tests/core/memory/MemoryExtractor.test.ts, tests/core/memory/MemoryEngine.test.ts</files>
  <read_first>08-PATTERNS.md MemoryExtractor (210-230 — zod schema module, ConfidentFact mirror) + MemoryEngine (233-251 — PageContentService singleton analog, typed-result facade, object-form namespace export) · 08-RESEARCH.md Pattern 4 (create-only producer seam 269-273) + Pitfall 2 (schema shape) + Code Example MemoryExtractor (461-466) + RetrievalMetadata shape (469-477) + Open Question 2 (store + producers only) · 08-CONTEXT.md D-105/D-113 + RICH-R-05 · PRODUCT_SPEC spec 4764-4773 (ConfidentFact mirror) + spec 4572-4578 (RetrievedMemory) + §3.4 injection (630-635: top-5/top-3/≤1000/never secrets) + §3.5 (preference profile) · @src/core/memory/UserMemoryStore.ts (from Task 1) · @src/core/memory/ConversationMemoryStore.ts (from Task 2) · @src/core/memory/PreferenceMemoryStore.ts (from 08-01) · @src/core/context/trust/contextItems.ts (70-93 [MEMORY] contract) · @src/core/context/TokenBudget.ts (countTokensHeuristic 44-55) · @src/core/ai/persona/PersonaProfile.ts (DEFAULT_PERSONA 25)</read_first>
  <action>Build the extraction seam and the create-only orchestrator. Strict-clean. THE RICH-R-05 DONE-WHEN PROOF LIVES IN MemoryEngine.test.ts.

1. `src/core/memory/MemoryExtractor.ts` (NEW) — D-113 schema + parse seam ONLY (no LLM call, no NMEM-02 wiring — Phase 9, spec 3876). Export `memoryFactsSchema = z.object({ content: z.string(), type: z.enum(['fact','preference','pattern']), confidence: z.number().min(0).max(1), tags: z.array(z.string()).default([]) })` (mirrors ConfidentFact at spec 4764-4773) and `export type MemoryFact = z.infer<typeof memoryFactsSchema>`. `export function parseMemoryFacts(output: string): { ok: true; facts: MemoryFact[] } | { ok: false; code: 'MEMORY_FACT_PARSE_FAILED'; message: string }` — tolerant JSON-array extraction (strip code fences, find the first `[...]` block, safeParse each entry, drop invalid entries with debugLog('MEMORY_FACT_DROPPED', ...); return ok:false only when NO valid fact parses). Header comment: D-113 + Phase-9 wiring point.
2. `src/core/memory/MemoryEngine.ts` (NEW) — create-only orchestrator facade (PageContentService.ts:1-45 singleton pattern; object-form namespace export `export const MemoryEngine = { ... }` — ProviderRegistry/PageIndexBuilder:254-261 convention). Methods:
   a. `retrieveConversationMemory(conversationId)` → `{ summary: string | null; recentTurns: MemoryMessage[] }` — delegates to ConversationMemoryStore.getSummary + getRecentTurns.
   b. `retrieveUserMemory(query, opts?: { tier?: ModelContextTier })` → `RetrievedMemory[]` — top-5 default / top-3 when tier === 'tiny' (`MEMORY_HINTS_TOP_K = 5`, `MEMORY_HINTS_TINY_K = 3`), cumulative `countTokensHeuristic` budget ≤ `MEMORY_HINTS_MAX_TOKENS = 1000` (spec 630-635), never injects secrets (the store already redacted at write; engine additionally filters any fact whose content contains a redaction marker if the redaction primitive exposes one — otherwise document the write-time guarantee), maps each scored fact to `{ id, content, type: fact.type, tags: fact.tags, score }` (spec 4572-4578).
   c. `buildPreferenceProfile()` → compact JSON string — RICH-R-05 DONE-WHEN: reads PreferenceMemoryStore state (personaId + persona + personaOverrides) and returns a compact serialization incl. the overrides (e.g. JSON.stringify({ personaId, name, tone, brevity, overrides }) — the prefsCompact rendering precedent at contextItems.ts:143-152; keep it small — the memory budget is 1000 tokens). NEVER reads the fact store (R2).
   d. `retrieveMemoryHints(query, opts?)` → `RetrievedMemory[]` — the seam the Phase-7 [MEMORY] trust builder consumes (contextItems.ts:70-93: `hint.id\t hint.content`, trust 'retrieved', authority false — engine only produces plain data; it must never attach instructionAuthority). Implementation = retrieveUserMemory results in the RetrievedMemory[] shape (same top-k/token budget). Header comment: D-105 create-only — live AgentOrchestrator/useChatStreaming adoption is Phase 15/9 (NMEM).
   All retrieval paths typed — no silent empty results (D-91 typed-result-union precedent); failures surface as typed `{ ok: false; code: ... }` variants or logged debugLog('MEMORY_RETRIEVAL_FAILED', ...) with empty fallback per contract.
3. `tests/core/memory/MemoryExtractor.test.ts` (NEW) — (a) valid fenced JSON array → ok:true with parsed facts; (b) partially invalid array → valid entries kept, invalid dropped with debugLog; (c) garbage/no-array → ok:false MEMORY_FACT_PARSE_FAILED; (d) confidence bounds: 1.5 rejected, 0.5 accepted; (e) no LLM import: `grep -c "from '../ai/\|fetch(" src/core/memory/MemoryExtractor.ts` equals 0.
4. `tests/core/memory/MemoryEngine.test.ts` (NEW, §18 required) — reset all mocks. (a) CONVERSATION: seed ConversationMemoryStore with a stub-summarised conversation → retrieveConversationMemory returns the summary + recent turns (ROADMAP SC#1); (b) USER MEMORY: seed UserMemoryStore with 7 facts → retrieveUserMemory returns 5 (default) / 3 (tier tiny) in score-desc order, all score ∈ [0,1] (SC#3); (c) TOKEN BUDGET: facts whose total exceeds 1000 tokens → truncated to ≤1000 via countTokensHeuristic; (d) RICH-R-05 DONE-WHEN: PreferenceMemoryStore setPersonaOverrides({ tone: 'concise' }) → buildPreferenceProfile() JSON includes the override AND reads np_persona (not userFacts — assert no userFacts read needed to produce the profile); (e) retrieveMemoryHints returns RetrievedMemory[] matching the Phase-7 shape ({ id, content, type, tags, score }) — the exact shape contextItems.ts consumes; (f) CREATE-ONLY: `grep -c "ContextOptimizer\|AgentOrchestrator" src/core/memory/MemoryEngine.ts` equals 0 (no live wiring imports).</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `npx vitest run tests/core/memory/MemoryExtractor.test.ts tests/core/memory/MemoryEngine.test.ts` passes; `grep -n "MEMORY_HINTS_TOP_K = 5\|MEMORY_HINTS_TINY_K = 3" src/core/memory/MemoryEngine.ts` shows both caps; `grep -n "MEMORY_HINTS_MAX_TOKENS = 1000" src/core/memory/MemoryEngine.ts` shows the budget; `grep -n "buildPreferenceProfile" src/core/memory/MemoryEngine.ts` shows the RICH-R-05 producer; `grep -c "memoryHints" src/core/memory/MemoryEngine.ts` >= 1 (the Phase-7 seam); `grep -c "ContextOptimizer\|AgentOrchestrator" src/core/memory/MemoryEngine.ts` equals 0 (create-only); `grep -n "memoryFactsSchema" src/core/memory/MemoryExtractor.ts` shows the schema; `grep -rn "NP-STRICT" src/core/memory/MemoryEngine.ts src/core/memory/MemoryExtractor.ts tests/core/memory/MemoryEngine.test.ts tests/core/memory/MemoryExtractor.test.ts | wc -l` equals 0; `git status --porcelain src/core/memory/MemoryEngine.ts src/core/memory/MemoryExtractor.ts | wc -l` >= 2</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/memory/MemoryExtractor.test.ts tests/core/memory/MemoryEngine.test.ts && test "$(grep -c 'ContextOptimizer\|AgentOrchestrator' src/core/memory/MemoryEngine.ts)" -eq 0 && test "$(grep -rn 'NP-STRICT' src/core/memory/MemoryEngine.ts src/core/memory/MemoryExtractor.ts tests/core/memory/MemoryEngine.test.ts tests/core/memory/MemoryExtractor.test.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>MemoryEngine is the real, tested, create-only producer: conversation summary + recent turns (SC#1), user memory top-5/top-3-tiny/≤1000 tokens (SC#2), scores in [0,1] (SC#3), buildPreferenceProfile including persona overrides from np_persona (RICH-R-05 DONE-when, R2), retrieveMemoryHints feeding the Phase-7 [MEMORY] trust builder; MemoryExtractor ships only the schema + parse seam; zero live-wiring imports; strict-clean.</done>
  <!-- planner-discipline-allow: buildPreferenceProfile -->
  <!-- planner-discipline-allow: MEMORY_HINTS_MAX_TOKENS -->
  <!-- planner-discipline-allow: from '../ai/\|fetch( -->
  <!-- planner-discipline-allow: ContextOptimizer\|AgentOrchestrator -->
  <reversibility rating="reversible">D-105/D-113 — additive producer modules; wiring later (Phase 9 NMEM / Phase 15 RICH) is a caller edit (CONTEXT.md rating).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input / page content → userFacts/messages bodies | Untrusted content enters MemoryDB — redacted at write (§4.4), never raw secrets |
| np_facts / np_conversation_meta / working block → chrome.storage.local | Small metadata + ≤300-token working block persist locally — redacted, single-writer |
| MemoryEngine → RetrievedMemory[] → [MEMORY] trust builder | Retrieved memory is untrusted data — trust 'retrieved', authority FALSE (Phase-7 CTX-02 contract) |
| compactor/evict → WriteJournal | Multi-store eviction (IDB + chrome.storage.local) must be atomic-on-recovery (O.11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-02 | Information Disclosure | MemoryDB.userFacts / messages / np_facts / working block | high | mitigate | redactSensitiveValue before EVERY persistence write (O.10/Pitfall-1 swap comment; TODO Phase 11 TraceRedactor); §3.4 'never inject secrets or raw customer data'; secret-shaped fixture proves the persisted body is redacted (UserMemoryStore.test.ts + WorkingMemory.test.ts) |
| T-8-04 | Elevation of Privilege | MemoryEngine.retrieveMemoryHints → [MEMORY] section | high | mitigate | MemoryEngine produces PLAIN data only — the Phase-7 trust layer (contextItems.ts:70-93, D-94) tags [MEMORY] trust:'retrieved', instructionAuthority:false; engine never attaches authority; create-only (no ContextOptimizer/AgentOrchestrator edits — grep-asserted) |
| T-8-05 | Tampering | single-writer discipline (fact upserts, compactor, working block) | medium | mitigate | isPrimaryWriter() gate on every write path; non-primary surfaces are read-only + debugLog skip (UserMemoryStore/ConversationMemoryStore/WorkingMemory tests assert the no-op) |
| T-8-06 | Denial of Service | np_facts LRU / conversation LRU / compactor | medium | mitigate | NP_FACTS_MAX=500 LRU caps chrome.storage.local growth; CONVERSATION_ACTIVE_MAX=10/ARCHIVED_MAX=100 + 12-message compactor bound message bodies; evict-conversation is journaled (O.11) |
| T-8-07 | Tampering | WriteJournal evict-conversation steps | medium | mitigate | createEvictConversationSteps registered via registerJournalSteps; apply is idempotent (delete-already-deleted is safe — replay-safe per O.11); union untouched (op already declared, src/types/storage.ts:48) |
| T-8-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO installs this phase (Package Legitimacy Audit vacuous — deps pre-installed; 08-RESEARCH.md); executor must not add any dependency |

</threat_model>

<verification>
- **Per-task commits:** `pnpm run lint` + the affected memory test file (08-VALIDATION.md sampling rate).
- Grep guards: zero NP-STRICT markers (STATE.md decision 17); zero ContextOptimizer/AgentOrchestrator imports in MemoryEngine (create-only D-105); zero LLM imports in MemoryExtractor (schema seam only); zero edits to the WriteJournalOperation union.
- Consumer-regression guard: `npx vitest run tests/core/context tests/core/ai tests/core/storage` stays green (memory types from 08-01 unchanged).
- RICH-R-05 DONE-when proof: MemoryEngine.test.ts buildPreferenceProfile includes persona overrides from np_persona.
- Wave-2 merge check: `npx vitest run tests/core/memory` green (all six memory test files).
- The verify:phase-8 re-point (D-114) lands in plan 08-05; this plan creates the §18-required memory tests the gate will run.
</verification>

<success_criteria>
1. UserMemoryStore persists §3.4 facts to MemoryDB.userFacts with a ≤500 np_facts LRU metadata index, redacted bodies, single-writer gates; MemoryScorer implements the §3.4 formula verbatim with scores ∈ [0,1] (SC#3).
2. ConversationMemoryStore ships the §15.3 compactor (12-message, head+summary+tail-4, archive-30-min, LRU 10/100) with a pluggable Summarizer seam + journaled evict-conversation (D-106).
3. WorkingMemory implements O.10 with redactSensitiveValue (Pitfall-1 swap) and a 300-token cap.
4. MemoryEngine is the create-only producer: conversation summary + recent turns (SC#1), user memory top-5/top-3-tiny/≤1000 tokens (SC#2), buildPreferenceProfile incl. persona overrides (RICH-R-05), retrieveMemoryHints → RetrievedMemory[] for the Phase-7 trust layer; MemoryExtractor is schema+parse only (D-113).
</success_criteria>

<output>
Create `.planning/phases/08-knowledge-base-memory-minisearch-notes/08-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-8 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/memory/UserMemoryStore.ts | useUserMemoryStore / NP_FACTS_MAX=500 — np_facts LRU metadata + MemoryDB.userFacts bodies + redaction + single-writer (D-104/D-113) | **[this plan]** |
| src/core/memory/MemoryScorer.ts | scoreMemory + verbatim §3.4 weight constants + MEMORY_RECENCY_WINDOW_DAYS=30 (D-113) | **[this plan]** |
| src/core/memory/ConversationMemoryStore.ts | ConversationMemoryStore / CONVERSATION_COMPACTOR_MODULO=12 / ACTIVE_MAX=10 / ARCHIVED_MAX=100 / IDLE_ARCHIVE_MS — compactor seam + LRU + summaries (D-104/D-106) | **[this plan]** |
| src/core/memory/MemoryExtractor.ts | memoryFactsSchema / parseMemoryFacts — schema + parse seam only (D-113) | **[this plan]** |
| src/core/memory/WorkingMemory.ts | initWorkingMemory / updateWorkingMemory / MAX_WORKING_MEMORY_TOKENS=300 — O.10 with redaction swap (D-104) | **[this plan]** |
| src/core/memory/MemoryEngine.ts | MemoryEngine — retrieveConversationMemory / retrieveUserMemory / buildPreferenceProfile / retrieveMemoryHints + caps (D-105, RICH-R-05 DONE-when) | **[this plan]** |
| src/core/storage/WriteJournal.ts | + createEvictConversationSteps + 'evict-conversation' registration (D-106) | **[this plan]** |
| tests/core/memory/UserMemoryStore.test.ts | §18 — LRU ≤500, redaction, metadata/body consistency, single-writer | **[this plan]** |
| tests/core/memory/MemoryScorer.test.ts | §18 — verbatim weights, scores ∈ [0,1], recency/useCount math | **[this plan]** |
| tests/core/memory/ConversationMemoryStore.test.ts | compactor 12-rule, LRU caps, archive, journaled evict, stub summariser | **[this plan]** |
| tests/core/memory/MemoryExtractor.test.ts | parse seam — valid/partial/garbage, confidence bounds | **[this plan]** |
| tests/core/memory/WorkingMemory.test.ts | O.10 — template, redaction, 300-token cap, truncation | **[this plan]** |
| tests/core/memory/MemoryEngine.test.ts | §18 + RICH-R-05 DONE-when — summary+turns, top-5/top-3, ≤1000 tokens, buildPreferenceProfile, retrieveMemoryHints shape | **[this plan]** |