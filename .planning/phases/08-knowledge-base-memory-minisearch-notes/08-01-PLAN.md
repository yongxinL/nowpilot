---
phase: 08-knowledge-base-memory-minisearch-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/notes.ts
  - src/core/storage/NotesDB.ts
  - src/core/memory/types.ts
  - src/types/harness.ts
  - src/core/context/types.ts
  - src/core/storage/MemoryDB.ts
  - src/core/ai/UserPreferences.ts
  - src/core/memory/PreferenceMemoryStore.ts
  - tests/core/memory/PreferenceMemoryStore.test.ts
  - tests/core/notes/note-canonical.test.ts
autonomous: true
requirements: [RICH-R-05]
user_setup: []

# assumption-delta (D-108): Note.type?: string is an ADDITIVE OKF declaration,
# populated/serialized in Phase 9 — it is NOT an identity anchor. Note identity
# stays the immutable UUID `id` (WIKI-ID-01, ADR-NOTE-01). No-change decision
# recorded per the assumption-delta protocol.
# Tracer discipline note: this plan's tracer is the canonical-type spine
# (notes.ts → NotesDB re-export → put/get round-trip) — the thinnest slice
# through the type homes + storage layer the whole phase builds on.
# scope-sanity note (checker advisory): 10 files but cohesive — one concern
# (canonical type homes + the RICH-R-05 persona store); per-task commit
# discipline keeps each task's diff bounded to its own concern (T1: 3 files,
# T2: 4 files, T3: 3 files).

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - statement: "RICH-R-05 (D-112): PreferenceMemoryStore persists np_persona { personaId, persona: PersonaProfile, personaOverrides? } to chrome.storage.local — NEVER MemoryDB.userFacts (R2, spec 121/664); hydration on boot re-reads np_persona; verified by PreferenceMemoryStore.test.ts + the buildPreferenceProfile block of MemoryEngine.test.ts (plan 08-02)"
      verification: backstop
    - statement: "RICH-R-05 idempotency (resolved, explicit): hydrate() re-reading np_persona over an already-hydrated store is idempotent — the persisted config matches the in-memory state after repeat hydration; PreferenceMemoryStore never writes a duplicate persona row"
      verification: backstop
    - statement: "RICH-R-05 concurrency (resolved, explicit): persona config writes are single-writer on the primary surface (gated by isPrimaryWriter()); a non-primary surface never persists np_persona"
      verification: backstop
    - statement: "D-107: canonical Note lives at src/types/notes.ts (spec 4721-4741 verbatim: links[]=resolved IDs, unresolvedLinks[], source{kind,...}, aiMeta{suggestedLinks,concepts,lastWikiRunAt?}, summary?, categoryPath?, summaryGeneratedAt?, tagsGeneratedAt?, type?: string, version) + OKF_NOTE_DEFAULT_TYPE + OkfNoteFrontmatter + LLM-WIKI-11 constants; NotesDB re-exports the canonical type (D-72 precedent, no parallel copy); no DB migration (v4 is Phase 9, spec 3156)"
      verification: backstop
    - statement: "D-108: Note.type?: string is declaration-only in Phase 8 — the field type-checks but no reader/writer consumes it, no serialization, no migration (grep-assertable: zero src references outside notes.ts)"
      verification: backstop
    - statement: "D-112: full §3.5 UserPreferences (responseStyle/preferredLanguage/preferStructuredOutput/allowCloudFallbackFromLocal/defaultProviderId?/toolAutonomy/defaultSurface/personaId?/personaOverrides? + additive fastModel?/balancedModel? per D-54) is canonical at src/core/memory/types.ts (spec 4579-4595); ai/UserPreferences.ts re-exports so PersonaInjector/ContextOptimizer/PromptCacheManager/AgentOrchestrator keep resolving with zero edits (Pitfall 3: initialPreferences gains defaults for required fields, partialize decision per Open Q3 grep)"
      verification: backstop
    - statement: "D-104: canonical UserMemoryFact (§3.4, spec 601-612) lives at src/core/memory/types.ts and MemoryDB imports/re-exports it (write-empty stores = zero migration); WorkingMemory + WORKING_MEMORY_TEMPLATE appended to src/types/harness.ts (C.1 spec 4839, Appendix O.10)"
      verification: backstop
  artifacts:
    - path: "src/types/notes.ts"
      provides: "Canonical Phase-8 note-type home — §21.2 Note verbatim (spec 4721-4741, incl. summaryGeneratedAt?/tagsGeneratedAt? staleness fields per LLM-WIKI-08) + OKF_NOTE_DEFAULT_TYPE + OkfNoteFrontmatter + LLM-WIKI-11 constants (spec 4743-4762), declared for Phase 9; Note.type?: string declaration-only (D-108)"
      exports: ["Note", "OKF_NOTE_DEFAULT_TYPE", "OkfNoteFrontmatter", "NOTE_SUGGESTION_DISPLAY_THRESHOLD"]
    - path: "src/core/storage/NotesDB.ts"
      provides: "D-107 re-export — canonical Note imported from @/types/notes (put/get value shape becomes canonical); local placeholder deleted; Concept/openNotesDB/getNoteByTitle untouched (getNoteByTitle stays a first-hit helper, Pitfall 4)"
      exports: ["Note (re-export)", "Concept", "openNotesDB", "getNoteByTitle"]
    - path: "src/core/memory/types.ts"
      provides: "Canonical memory-type home (D-112/D-113): RetrievedMemory (spec 4572-4578), full §3.5 UserPreferences + schema (spec 4579-4595 + D-54 fastModel/balancedModel), §3.4 UserMemoryFact (spec 601-612); tone/brevity enums + personaOverridesSchema moved here (single declaration, re-exported by ai/UserPreferences.ts)"
      exports: ["RetrievedMemory", "UserPreferences", "UserPreferencesSchema", "UserMemoryFact", "PersonaTone", "PersonaBrevity", "PersonaOverrides", "personaOverridesSchema"]
    - path: "src/types/harness.ts"
      provides: "D-104/C.1 — WorkingMemory + WORKING_MEMORY_TEMPLATE appended (spec 4839 row, Appendix O.10 shape); existing reliability/trust blocks untouched"
      exports: ["WorkingMemory", "WORKING_MEMORY_TEMPLATE"]
    - path: "src/core/context/types.ts"
      provides: "D-112 supersession — local RetrievedMemory (25-32) replaced by re-export from ../memory/types (D-72/D-83 precedent); ToolSchemaRef (34-41) stays (Phase 18)"
      exports: ["RetrievedMemory (re-export)"]
    - path: "src/core/storage/MemoryDB.ts"
      provides: "D-104 — UserMemoryFact superseded to the §3.4 canonical shape via import/re-export from @/core/memory/types (write-empty store = zero migration); MemoryMessage/ConversationSummary/schema untouched"
      exports: ["UserMemoryFact (re-export)", "MemoryMessage", "ConversationSummary", "openMemoryDB"]
    - path: "src/core/ai/UserPreferences.ts"
      provides: "D-112/Pitfall 3 — re-exports UserPreferences + schema + enums from @/core/memory/types; zustand store updated: initialPreferences gains defaults for the new REQUIRED §3.5 fields, partialize keeps fastModel/balancedModel (personaOverrides dropped ONLY if Open Q3 grep shows zero live readers of the persisted np_preferences personaOverrides in src/ non-test code)"
      exports: ["useUserPreferencesStore", "UserPreferences (re-export)", "PersonaTone (re-export)", "PersonaBrevity (re-export)"]
    - path: "src/core/memory/PreferenceMemoryStore.ts"
      provides: "RICH-R-05/D-112 — zustand + persist + chromeStorageAdapter store owning np_persona { personaId, persona: PersonaProfile, personaOverrides? }; zod-validated hydrate (PersonaProfileSchema + personaOverridesSchema); get/setPersona, setPersonaOverrides, hydrate; R2: zero MemoryDB imports"
      exports: ["usePreferenceMemoryStore", "npPersonaSchema", "PreferenceMemoryStore"]
    - path: "tests/core/memory/PreferenceMemoryStore.test.ts"
      provides: "RICH-R-05 proof — np_persona round-trip (set → hydrate → get), R2 (np_persona never lands in MemoryDB.userFacts), idempotent repeat-hydration, single-writer gate"
      exports: []
    - path: "tests/core/notes/note-canonical.test.ts"
      provides: "TRACER proof — canonical Note round-trips through NotesDB put/get with links/unresolvedLinks/aiMeta/source/type fields intact (the type spine end-to-end)"
      exports: []
  key_links:
    - from: "src/core/storage/NotesDB.ts"
      to: "src/types/notes.ts"
      via: "import type { Note } from '../../types/notes' + re-export — NotesDB value shape becomes canonical (D-72/D-83 precedent, context/types.ts:22-23)"
      pattern: "re-export"
    - from: "src/core/ai/UserPreferences.ts"
      to: "src/core/memory/types.ts"
      via: "re-export of UserPreferences/UserPreferencesSchema/enums — PersonaInjector (import type { UserPreferences } from '../UserPreferences') and PromptCacheManager (prefsCompact, lines 160-171) keep resolving unchanged (D-112, Pitfall 3)"
      pattern: "import type { UserPreferences }"
    - from: "src/core/memory/PreferenceMemoryStore.ts"
      to: "src/core/ai/persona/PersonaProfile.ts"
      via: "PersonaProfileSchema + DEFAULT_PERSONA imported for the np_persona payload + defaults (RICH-R-01 code-seeded; Phase 8 persists them, D-112)"
      pattern: "PersonaProfileSchema"
  prohibitions:
    - "np_persona is NEVER written to MemoryDB.userFacts (R2) — PreferenceMemoryStore imports zero storage/memory DB modules (flagged-unverified)"
    - "No new MemoryRecord/MemoryKind declarations anywhere (Phase 10 scope fence, spec 4903-4915)"
    - "Note.type?: string is declaration-only — no reader/writer consumes it in Phase 8 (D-108); no serialization of type/categoryPath (Phase 9)"
    - "No DB migration in Phase 8 — v4 migration (tags/summary/type population) is Phase 9 (spec 3156)"
    - "No parallel copy of Note/RetrievedMemory/UserPreferences — re-export supersession only (D-72/D-83/D-107/D-112)"
    - "Zero new @ts-expect-error NP-STRICT markers in all new modules (STATE.md decision 17)"
    - "No dangerouslySetInnerHTML / innerHTML anywhere in this plan's files (CTX-02 surface discipline)"
    - "Breadcrumb (PROHIB_ABSENT fallback): canon-security items (OWASP top-10 / prototype-pollution / generic injection) dropped from this prohibition list — covered by the threat model below + Phase-7 trust layer (CTX-02/CTX-03)"

---

<objective>
TRACER-led type-spine plan: establish the canonical Phase-8 type homes and the RICH-R-05 persona persistence — `src/types/notes.ts` (canonical `Note`, D-107/D-108), `src/core/memory/types.ts` (RetrievedMemory + full §3.5 UserPreferences + §3.4 UserMemoryFact, D-112/D-113), `harness.ts` WorkingMemory (D-104), the re-export supersessions (NotesDB, MemoryDB, context/types, ai/UserPreferences — D-72/D-83 precedent), and `PreferenceMemoryStore` owning `np_persona` (RICH-R-05).

Purpose: every later plan (memory stores, search, notes core, components, E2E) imports from these homes. Proving the canonical-Note spine end-to-end first (declare → re-export → NotesDB round-trip) catches type-drift before any store logic builds on it; the persona store lands RICH-R-05 — the phase's only v1 requirement — at the earliest possible point.

Output: the ten files in frontmatter, strict-clean, with the D-107/D-108/D-112/D-104 semantics locked; the canonical Note round-trip proven by a test; np_persona persistence proven by PreferenceMemoryStore.test.ts.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  # Phase 8 block (253-267): goal, success criteria SC#1-5, gate
@.planning/STATE.md  # decision 11 (WIKI-ID UUID, ADR-NOTE-01), decision 17 (strict-clean zero NP-STRICT), decision 19 (isPrimaryWriter delegation)
@.planning/PRODUCT_SPEC_v0_1.md  # §21.2 Note (3287-3322), spec 4720-4774 (canonical Note + OKF), §3.5 UserPreferences (4579-4595), §3.4 UserMemoryFact (601-612), spec 4571-4578 (RetrievedMemory), C.1 canonical-home rule (4833-4845), §15.1 np_persona key (1957), R2 persona-not-fact (664/2384)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-CONTEXT.md  # D-104/D-107/D-108/D-112 locked + the agent's Discretion items
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-RESEARCH.md  # Patterns 2/3, Pitfalls 1/2/3, Open Questions 1/3, Assumptions A3, Code Examples (canonical Note 434-458), Security Domain
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-PATTERNS.md  # notes.ts (42-82), memory/types.ts (86-107), PreferenceMemoryStore (158-187), harness edit (401-403), NotesDB/MemoryDB/UserPreferences/context re-export edits (405-421)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-VALIDATION.md  # per-task map 08-01-01 (PreferenceMemoryStore.test.ts) + 08-01-02 (Note.type lint)
@src/types/harness.ts  # canonical-home convention (1-102) — WorkingMemory appends here
@src/core/storage/NotesDB.ts  # Note placeholder (26-37) superseded; byTitle/byUpdated indexes (69-70); getNoteByTitle (88-93)
@src/core/storage/MemoryDB.ts  # UserMemoryFact bootstrap shape (37-45) superseded; messages/userFacts/conversationSummaries schema
@src/core/context/types.ts  # RetrievedMemory supersession point (25-32) + D-83 re-export precedent (22-23)
@src/core/ai/UserPreferences.ts  # Phase-3 minimal store (21-105) — the Pitfall-3 supersession target
@src/core/ai/persona/PersonaProfile.ts  # PersonaProfileSchema (7-23) + DEFAULT_PERSONA (25) — np_persona payload sources
@src/core/ai/PromptCacheManager.ts  # UserPreferences consumer (26, 160-171) — must keep resolving
@src/core/ai/AgentOrchestrator.ts  # UserPreferences consumer (24) — must keep resolving
@src/core/theme/chromeStorageAdapter.ts  # StateStorage adapter (210) — np_persona persistence target
@tests/setup.ts  # __chromeStorageMap reset + fake-indexeddb conventions (9-11, 56-80)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — canonical Note spine: src/types/notes.ts + NotesDB re-export + put/get round-trip</name>
  <files>src/types/notes.ts, src/core/storage/NotesDB.ts, tests/core/notes/note-canonical.test.ts</files>
  <read_first>08-PATTERNS.md notes.ts section (42-82, verbatim shape + file-header convention) + NotesDB edit (405-410) · 08-RESEARCH.md Code Example 'Canonical Note' (434-458) + Pattern 3 (canonical home + re-export, 256-267) + Pitfall 2 (supersede-in-place) · 08-CONTEXT.md D-107/D-108 + assumption-delta no-change decision · PRODUCT_SPEC §21.2 (3287-3322) + spec 4720-4774 (canonical Note + OKF frontmatter + suggestion-gating constants) + spec 3156 (v4 migration is Phase 9 — no migration here) · @src/core/storage/NotesDB.ts (26-37 placeholder, 61-81 openNotesDB) · @src/types/harness.ts (header-convention precedent 3-19) · @tests/core/storage/IndexedDBMigrator.test.ts (db-test conventions 43-48) · @tests/setup.ts (__resetIndexedDB 9-11)</read_first>
  <action>Create the canonical note-type home and prove the type spine end-to-end through the real NotesDB. Strict-clean (STATE.md decision 17). File headers follow the repo convention `// <Module> — <D-ID> <purpose>` + spec line refs.

1. `src/types/notes.ts` (NEW) — declare the canonical `Note` interface VERBATIM from spec 4721-4741 (D-107): id/title/content/created/updated (numbers for the timestamps), tags: string[], links: string[] (RESOLVED note IDs per WIKI-ID-02), unresolvedLinks: string[] (raw [[Title]] targets per WIKI-ID-03), source: { kind: 'manual'|'voice'|'chat-export'|'template'|'page-export'; conversationId?: string; templateId?: string }, aiMeta: { suggestedLinks: Array<{ targetId: string; confidence: number; reason: string }>; concepts: string[]; lastWikiRunAt?: number }, summary?: string, categoryPath?: string, summaryGeneratedAt?: number, tagsGeneratedAt?: number (staleness detection per LLM-WIKI-08, spec 3312-3313 / RESEARCH 452-453 — declared in Phase 8, populated Phase 9), type?: string (D-108 declaration-only — do NOT add any reader/writer), version: number. Also export `OKF_NOTE_DEFAULT_TYPE = 'Note'` (const), `OkfNoteFrontmatter` interface (spec 4743-4762 shape: type, description?, id, generated { by, at }, status 'draft'|'stable' — declared for Phase 9), and the LLM-WIKI-11 suggestion-gating constant `NOTE_SUGGESTION_DISPLAY_THRESHOLD = 0.60` (REQUIREMENTS LLM-WIKI-11). Header comment citing D-107/D-108 + the assumption-delta no-change decision (identity = immutable UUID id, WIKI-ID-01/ADR-NOTE-01; type is additive OKF declaration, NOT an identity anchor). No zod here (harness.ts C.1 convention — TS interfaces only). Path alias `@/types/notes` resolves via tsconfig.
2. `src/core/storage/NotesDB.ts` (EDIT) — DELETE the local placeholder `Note` interface (lines 26-37). Import + re-export the canonical type: `export type { Note } from '../../types/notes';` with a `/** D-107: canonical Note re-exported from the Phase-8 home (spec 4721-4741). */` comment (D-83 precedent, context/types.ts:22-23). Keep `Concept`, `openNotesDB`, `NotesDBV1`, `getNoteByTitle` untouched — getNoteByTitle stays a first-hit helper (Pitfall 4: it is NOT the resolution primitive; resolveLinks in plan 08-03 does its own getAllFromIndex + sort).
3. `tests/core/notes/note-canonical.test.ts` (NEW) — the TRACER's end-to-end proof: build a fixture canonical Note (id = crypto.randomUUID()-shaped string, title 'Tracer Note', content with a [[Wikilink Target]], tags ['t'], links ['note-1'], unresolvedLinks ['[[No Such Note]]'], source { kind: 'manual' }, aiMeta { suggestedLinks: [], concepts: ['c'] }, type: undefined (omitted), version 1); openNotesDB → put → get → assert deep equality of the canonical fields (links/unresolvedLinks/aiMeta/source survive the round-trip); assert `OKF_NOTE_DEFAULT_TYPE === 'Note'` and `NOTE_SUGGESTION_DISPLAY_THRESHOLD === 0.60`. Use the IndexedDBMigrator.test.ts conventions: `(globalThis as any).__resetIndexedDB()` in beforeEach. Do NOT assert on type at all (D-108 declaration-only — presence or absence both valid).</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `npx vitest run tests/core/notes/note-canonical.test.ts` passes (round-trip + constants); `grep -c "unresolvedLinks" src/types/notes.ts` >= 1; `grep -c "OKF_NOTE_DEFAULT_TYPE" src/types/notes.ts` >= 1; `grep -c "type?: string" src/types/notes.ts` >= 1 (D-108 declared); `grep -c "summaryGeneratedAt" src/types/notes.ts` >= 1 (LLM-WIKI-08 staleness field declared); `grep -c "tagsGeneratedAt" src/types/notes.ts` >= 1 (LLM-WIKI-08 staleness field declared); `grep -rn "OKF_NOTE_DEFAULT_TYPE" src --include="*.ts" | grep -v "src/types/notes.ts" | wc -l` equals 0 (declaration-only — no consumer in Phase 8); `grep -n "export type { Note }" src/core/storage/NotesDB.ts` shows the re-export; `grep -c "interface Note" src/core/storage/NotesDB.ts` equals 0 (placeholder deleted); `grep -rn "NP-STRICT" src/types/notes.ts src/core/storage/NotesDB.ts tests/core/notes/note-canonical.test.ts | wc -l` equals 0; `git status --porcelain src/types/notes.ts src/core/storage/NotesDB.ts tests/core/notes/note-canonical.test.ts | wc -l` >= 3</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/notes/note-canonical.test.ts && test "$(grep -rn 'NP-STRICT' src/types/notes.ts src/core/storage/NotesDB.ts tests/core/notes/note-canonical.test.ts | wc -l)" -eq 0 && test "$(grep -rn 'OKF_NOTE_DEFAULT_TYPE' src --include='*.ts' | grep -v 'src/types/notes.ts' | wc -l)" -eq 0</automated>
  </verify>
  <done>The canonical Note (spec 4721-4741 verbatim, incl. summaryGeneratedAt?/tagsGeneratedAt? staleness fields) lives at src/types/notes.ts with OKF constants; NotesDB re-exports it with the local placeholder deleted; the round-trip test proves links/unresolvedLinks/aiMeta/source persist through NotesDB put/get; Note.type?: string is declared with zero consumers (D-108); lint strict-clean.</done>
  <!-- planner-discipline-allow: [[ -->
  <!-- planner-discipline-allow: OKF_NOTE_DEFAULT_TYPE -->
  <!-- planner-discipline-allow: unresolvedLinks -->
  <!-- planner-discipline-allow: type?: string -->
  <!-- planner-discipline-allow: summaryGeneratedAt -->
  <!-- planner-discipline-allow: tagsGeneratedAt -->
  <reversibility rating="costly">D-107 — type-home move + NotesDB value-shape change touches every NotesDB consumer/test fixture once, then converges (D-83 precedent cost; CONTEXT.md rating).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Memory type homes — memory/types.ts + harness WorkingMemory + supersession re-exports</name>
  <files>src/core/memory/types.ts, src/types/harness.ts, src/core/context/types.ts, src/core/storage/MemoryDB.ts</files>
  <read_first>08-PATTERNS.md memory/types.ts section (86-107, verbatim shapes + scope fence) + harness edit (403) + MemoryDB edit (412) + context re-export (416-421) · 08-RESEARCH.md Pattern 3 (re-export supersession) + Pitfall 2 (UserMemoryFact bootstrap ≠ §3.4) + Open Question 1 (UserMemoryFact home) + Code Example 'RetrievalMetadata → RetrievedMemory' (469-477) · 08-CONTEXT.md D-104/D-112/D-113 + discretion (memory/types.ts hosts §3.4 fact shape, not idb row schemas) · PRODUCT_SPEC spec 4571-4595 (RetrievedMemory + UserPreferences) + spec 601-612 (§3.4 UserMemoryFact) + spec 4833-4845 (canonical-home rule) + spec 4839 (WorkingMemory → @/types/harness) · @src/core/context/types.ts (22-32) · @src/core/storage/MemoryDB.ts (37-45) · @src/types/harness.ts (1-102, append-only convention) · @src/core/ai/UserPreferences.ts (21-44, the enums/schema being moved)</read_first>
  <action>Declare the canonical memory types and wire the supersession re-exports. Strict-clean. All header comments follow the repo convention + D-ID citations + spec refs.

1. `src/core/memory/types.ts` (NEW) — canonical memory-type home (D-112/D-113): (a) `RetrievedMemory` verbatim from spec 4572-4578: `{ id: string; content: string; type: 'fact' | 'preference' | 'pattern'; tags: string[]; score: number }`; (b) the full §3.5 `UserPreferences` + `UserPreferencesSchema` verbatim from spec 4579-4595: responseStyle, preferredLanguage, preferStructuredOutput, allowCloudFallbackFromLocal, defaultProviderId? (string), toolAutonomy, defaultSurface, personaId? (string), personaOverrides? — plus the additive D-54 fields fastModel?/balancedModel? (strings); use zod v4 (`z.object`, `.safeParse` — PersonaProfile.ts:7-23 pattern) and reference the existing `personaOverridesSchema`; (c) the canonical §3.4 `UserMemoryFact` from spec 601-612: `{ id, content, type: 'fact'|'preference'|'pattern', tags: string[], confidence: number, source: 'explicit'|'inferred'|'system', createdAt: number, updatedAt: number, lastUsedAt?: number, useCount: number }`; (d) MOVE the tone/brevity enums (PERSONA_TONE_ENUM/PersonaTone, PERSONA_BREVITY_ENUM/PersonaBrevity) + personaOverridesSchema + PersonaOverrides from ai/UserPreferences.ts into THIS file (single declaration — ai/UserPreferences re-exports them in Task 3; grep importers first and confirm they keep resolving via the re-export). SCOPE FENCE: do NOT declare MemoryKind/MemoryRecord (Phase 10, canonical home @/types/harness, spec 4903-4915). Header comment citing D-112/D-113 + the canonical-home rule (spec 4833-4845).
2. `src/types/harness.ts` (EDIT) — APPEND the C.1 `WorkingMemory` + `WORKING_MEMORY_TEMPLATE` block (D-104, spec 4839 row + Appendix O.10): `export interface WorkingMemory { resourceId: string; markdown: string; tokens: number; updatedAt: number }` and `export const WORKING_MEMORY_TEMPLATE = '# User Profile\n- **Name**:\n- **Role / Team**:\n- **Environment**:\n- **Preferences**:\n- **Long-term Goals**:'` (spec 678-684 verbatim). Do not touch the Phase-4/7 blocks.
3. `src/core/context/types.ts` (EDIT) — REPLACE the local `RetrievedMemory` (lines 25-32) with a re-export: `export type { RetrievedMemory } from '../memory/types';` + `/** D-112: RetrievedMemory re-exported from the canonical Phase-8 home (spec 4571). */` (D-83 precedent). ToolSchemaRef (34-41) stays.
4. `src/core/storage/MemoryDB.ts` (EDIT) — DELETE the local `UserMemoryFact` interface (37-45) and re-export the canonical shape: `export type { UserMemoryFact } from '../memory/types';` + D-104 comment. Zero migration (write-empty store). Keep MemoryMessage/ConversationSummary/schema untouched.</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean (all consumers resolve via re-exports — the real end-to-end check of the supersession); `grep -c "RetrievedMemory" src/core/memory/types.ts` >= 1 and `grep -c "export type { RetrievedMemory }" src/core/context/types.ts` >= 1; `grep -c "responseStyle" src/core/memory/types.ts` >= 1 (full §3.5 shape); `grep -c "useCount" src/core/memory/types.ts` >= 1 (§3.4 UserMemoryFact); `grep -c "WORKING_MEMORY_TEMPLATE" src/types/harness.ts` >= 1; `grep -c "interface UserMemoryFact" src/core/storage/MemoryDB.ts` equals 0 (local superseded); `grep -rn "MemoryRecord\|MemoryKind" src/core/memory/types.ts | wc -l` equals 0 (Phase-10 fence); `npx vitest run tests/core/storage/IndexedDBMigrator.test.ts tests/core/context/ContextOptimizer.test.ts` passes (existing consumers + DB conventions stay green); `grep -rn "NP-STRICT" src/core/memory/types.ts src/types/harness.ts src/core/context/types.ts src/core/storage/MemoryDB.ts | wc -l` equals 0</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/storage/IndexedDBMigrator.test.ts tests/core/context/ContextOptimizer.test.ts && test "$(grep -rn 'MemoryRecord\|MemoryKind' src/core/memory/types.ts | wc -l)" -eq 0 && test "$(grep -rn 'NP-STRICT' src/core/memory/types.ts src/types/harness.ts src/core/context/types.ts src/core/storage/MemoryDB.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>The four canonical memory types (RetrievedMemory, UserPreferences §3.5 + D-54, UserMemoryFact §3.4, WorkingMemory) live in their spec-mandated homes; context/types and MemoryDB re-export with local copies deleted; the full consumer graph (ContextOptimizer, IndexedDBMigrator) stays green; zero MemoryRecord/MemoryKind (Phase-10 fence); lint strict-clean.</done>
  <!-- planner-discipline-allow: MemoryRecord -->
  <!-- planner-discipline-allow: RetrievedMemory -->
  <!-- planner-discipline-allow: export type { RetrievedMemory } -->
  <!-- planner-discipline-allow: responseStyle -->
  <!-- planner-discipline-allow: useCount -->
  <!-- planner-discipline-allow: WORKING_MEMORY_TEMPLATE -->
  <reversibility rating="costly">D-112 — supersession touches the Phase-3 UserPreferences contract + consumers; the D-72 re-export keeps it converging, but the shape move is the locked target per spec 4579 (CONTEXT.md rating).</reversibility>
</task>

<task type="auto">
  <name>Task 3: UserPreferences store supersession (Pitfall 3) + PreferenceMemoryStore np_persona (RICH-R-05)</name>
  <files>src/core/ai/UserPreferences.ts, src/core/memory/PreferenceMemoryStore.ts, tests/core/memory/PreferenceMemoryStore.test.ts</files>
  <read_first>08-PATTERNS.md PreferenceMemoryStore section (158-187 — EXACT analog: zustand + persist + chromeStorageAdapter + immer) + UserPreferences edit (414, Pitfall 3) · 08-RESEARCH.md Pitfall 3 (full-shape supersession breaks the store typecheck) + Open Question 2 (store + producers only; live injection re-point is Phase 15) + Open Question 3 (personaOverrides two homes — grep live readers before dropping from partialize) + Assumption A3 (np_persona payload shape) · 08-CONTEXT.md D-112 (np_persona owner, R2 never fact store) + RICH-R-05 · PRODUCT_SPEC §15.1 np_persona key (1957) + R2 (664/2384) · @src/core/ai/UserPreferences.ts (21-105 — the store being superseded) · @src/core/ai/persona/PersonaProfile.ts (7-40) · @src/core/theme/chromeStorageAdapter.ts (210 StateStorage) · @src/core/workspace/WorkspaceStore.ts (23-25 isPrimaryWriter) · @tests/core/ai (existing Phase-3 store tests, if any — run them after the edit) · @tests/setup.ts (__chromeStorageMap reset 56-80)</read_first>
  <action>Supersede the Phase-3 UserPreferences store per D-112 and build the np_persona owner. Strict-clean. THIS TASK IMPLEMENTS RICH-R-05 (the phase's only v1 requirement).

1. `src/core/ai/UserPreferences.ts` (EDIT) — (a) delete the local `UserPreferencesSchema`/`UserPreferences` declarations (36-44) and the tone/brevity enums + personaOverridesSchema (21-34) — they moved to @/core/memory/types in Task 2; re-export: `export type { UserPreferences, PersonaTone, PersonaBrevity, PersonaOverrides } from '../memory/types';` + `export { personaOverridesSchema, PERSONA_TONE_ENUM, PERSONA_BREVITY_ENUM } from '../memory/types';` (D-112). (b) PITFALL 3: `interface UserPreferencesStore extends UserPreferences` now includes REQUIRED §3.5 fields — update `initialPreferences` (54-58) with explicit defaults for every required field (responseStyle, preferredLanguage, preferStructuredOutput, allowCloudFallbackFromLocal, toolAutonomy, defaultSurface — pick the zod schema's defaults from the spec 4579-4595 shape) plus the optional ones (fastModel/balancedModel/personaOverrides undefined). (c) Open Question 3 grep BEFORE touching partialize: `grep -rn "personaOverrides" src --include="*.ts" --include="*.tsx" | grep -v "UserPreferences.ts"` — if any NON-TEST file reads `useUserPreferencesStore` state's personaOverrides in a live path (e.g. useChatStreaming.ts:214 passing `prefs: useUserPreferencesStore.getState()`), KEEP personaOverrides in partialize and add a `// Phase 15: consolidate personaOverrides → np_persona (Open Q3)` comment; if zero live readers, drop it from partialize (np_persona is canonical). Grep result must be recorded in the commit message. (d) Keep setFastModel/setBalancedModel/setPersonaOverrides/hydrate signatures unchanged.
2. `src/core/memory/PreferenceMemoryStore.ts` (NEW) — zustand + persist + immer + chromeStorageAdapter store (UserPreferences.ts:66-105 template) with `name: 'np_persona'`, `version: 1`. State: `personaId: string`, `persona: PersonaProfile`, `personaOverrides: PersonaOverrides | undefined`, initialized from DEFAULT_PERSONA (import from @/core/ai/persona/PersonaProfile) + crypto.randomUUID() personaId. Export `npPersonaSchema = z.object({ personaId: z.string().min(1), persona: PersonaProfileSchema, personaOverrides: personaOverridesSchema.optional() })` and validate on hydrate (`api.persist.rehydrate()` then safeParse the persisted blob — zod v4 `.safeParse` per PersonaProfile.ts:7-23). Actions: `setPersona(p: PersonaProfile)`, `setPersonaOverrides(o: PersonaOverrides | undefined)`, `hydrate()` (boot re-read per D-112). IDEMPOTENCY (probe): hydrate() is a single-key overwrite — re-hydrating over an already-hydrated store must leave the persisted config === in-memory config (no duplicate rows — np_persona is one chrome.storage.local key). CONCURRENCY (probe): wrap setPersona/setPersonaOverrides with the single-writer gate — when `!isPrimaryWriter()` (import { isPrimaryWriter } from '../workspace/WorkspaceStore'), skip the persist and debugLog('NP_PERSONA_NON_PRIMARY_WRITE_SKIPPED', ...) — non-primary surfaces never persist np_persona (spec §13/D-106 single-writer discipline). R2: ZERO imports from @/core/storage (no MemoryDB, no userFacts) — comment `// R2 (spec 664): persona is user CONFIG — never a fact; np_persona never touches MemoryDB.userFacts`. NOTE (Open Q2): the live PersonaInjector re-point (reading overrides from np_persona at injection time) is Phase 15 — this phase ships the store + producers only; add a comment marking the Phase-15 swap point.
3. `tests/core/memory/PreferenceMemoryStore.test.ts` (NEW) — reset `(globalThis as any).__chromeStorageMap.clear()` in beforeEach (tests/setup.ts:56-80). Assert: (a) RICH-R-05 round-trip — setPersonaOverrides({ tone: 'concise' }) → persist → hydrate() → getState().personaOverrides.tone === 'concise'; np_persona key present in the chromeStorageMap mock; (b) R2 — after the write, the mock storage has NO 'np_persona' entry inside any MemoryDB store and the test imports no MemoryDB module (structural: assert the store module source contains no '../storage/ import via fs read or by construction — prefer construction: the test never opens MemoryDB); (c) IDEMPOTENCY — hydrate() twice: getState() deep-equals after both, and the persisted blob is unchanged (single-key overwrite, no duplicate); (d) CONCURRENCY — mock isPrimaryWriter to return false (vi.mock or a module-level seam if the store reads it directly — if mocking is impractical, assert via the exported gate helper), setPersonaOverrides is a no-op persist (np_persona unchanged) and debugLog path is hit; (e) zod validation — hydrate with a tampered blob (personaId: 123) → safeParse rejects, store keeps DEFAULT_PERSONA state.</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean (PersonaInjector/PromptCacheManager/AgentOrchestrator/ContextOptimizer resolve the re-exported full shape with zero edits); `npx vitest run tests/core/memory/PreferenceMemoryStore.test.ts` passes; `npx vitest run tests/core/ai tests/core/context/ContextOptimizer.test.ts tests/core/context/trust/contextItems.test.ts` passes (Phase-3/7 consumers stay green — create-only, D-105); `grep -n "name: 'np_persona'" src/core/memory/PreferenceMemoryStore.ts` shows the persist key; `grep -rn "np_persona" src/core/memory/PreferenceMemoryStore.ts | wc -l` >= 2 (key + R2 comment); `grep -c "from '../storage/\|from '../../core/storage/" src/core/memory/PreferenceMemoryStore.ts` equals 0 (R2 — zero storage imports); `grep -c "NP-STRICT" src/core/memory/PreferenceMemoryStore.ts src/core/ai/UserPreferences.ts | wc -l` equals 0; the Open Q3 grep result is recorded in the commit message; `git status --porcelain src/core/ai/UserPreferences.ts src/core/memory/PreferenceMemoryStore.ts tests/core/memory/PreferenceMemoryStore.test.ts | wc -l` >= 3</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/memory/PreferenceMemoryStore.test.ts tests/core/ai tests/core/context/ContextOptimizer.test.ts tests/core/context/trust/contextItems.test.ts && test "$(grep -c 'from .*storage/' src/core/memory/PreferenceMemoryStore.ts)" -eq 0 && test "$(grep -rn 'NP-STRICT' src/core/memory/PreferenceMemoryStore.ts src/core/ai/UserPreferences.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>RICH-R-05 is implemented: PreferenceMemoryStore owns np_persona (chrome.storage.local), zod-validated on hydrate, single-writer-gated, idempotent repeat-hydration, and provably never touches the fact store (R2); the Phase-3 UserPreferences store survives the full-§3.5 supersession with initialPreferences defaults and a documented partialize decision (Open Q3); all existing consumers keep resolving with zero edits; lint strict-clean.</done>
  <!-- planner-discipline-allow: np_persona -->
  <!-- planner-discipline-allow: isPrimaryWriter -->
  <reversibility rating="costly">D-112 — np_persona ownership + full-shape supersession is the locked target (spec 4579); the store edit is the converging supersession point (CONTEXT.md rating).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Options/Persona UI → np_persona | Persona config crosses into chrome.storage.local — user-owned config, NOT memory facts (R2) |
| np_persona → hydrate → in-memory store | Persisted persona blob re-enters the surface — must be zod-validated (tampered blob cannot inject fields) |
| userFacts store ↔ np_persona | The R2 boundary: persona config must never land in MemoryDB.userFacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-01 | Tampering | np_persona blob (chrome.storage.local) | high | mitigate | npPersonaSchema zod safeParse on every hydrate (PersonaProfileSchema + personaOverridesSchema); tampered/legacy blobs fall back to DEFAULT_PERSONA state, never crash; single-writer gate: non-primary surfaces skip np_persona persist (isPrimaryWriter(), debugLog instrumentation) |
| T-8-02 | Information Disclosure | PreferenceMemoryStore / ai/UserPreferences.ts | low | accept | np_persona holds only non-secret persona config by design (§3.6 'must not contain secrets'; persona is user config per R2); secrets remain in encrypted np_providers (Phase 2); no new crypto in Phase 8 |
| T-8-03 | Tampering | re-export supersession (NotesDB/MemoryDB/context/UserPreferences) | medium | mitigate | Single canonical declaration per type (D-107/D-112) + re-export only — no parallel copies that could drift; lint (tsc) + existing consumer tests gate every supersession edit |
| T-8-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO package installs this phase — Package Legitimacy Audit is vacuous (all deps pre-installed: minisearch 7.2.0 / idb 8.0.3 / zod 4.4.3 / fake-indexeddb, 08-RESEARCH.md); executor must not add any dependency; no checkpoint needed |

</threat_model>

<verification>
- **Per-task commits:** `pnpm run lint` + the affected test file (08-VALIDATION.md sampling rate).
- Grep guards: zero NP-STRICT markers in all new/modified files (STATE.md decision 17); zero consumers of OKF_NOTE_DEFAULT_TYPE in Phase 8 (D-108 declaration-only); zero MemoryRecord/MemoryKind in memory/types.ts (Phase-10 fence); zero storage imports in PreferenceMemoryStore.ts (R2).
- Consumer-regression guard: `npx vitest run tests/core/ai tests/core/context tests/core/storage` must stay green after the supersessions (PersonaInjector/PromptCacheManager/AgentOrchestrator/ContextOptimizer resolve via re-exports).
- RICH-R-05 full proof spans this plan (PreferenceMemoryStore.test.ts — np_persona round-trip/R2/idempotency/concurrency) + plan 08-02 (MemoryEngine.test.ts — buildPreferenceProfile includes persona overrides).
- The verify:phase-8 re-point (D-114) lands in plan 08-05; this plan ensures the type spine + persona store exist and are tested.
</verification>

<success_criteria>
1. The canonical Note (spec 4721-4741) is the single declaration at src/types/notes.ts; NotesDB re-exports it (D-107); Note.type?: string is declaration-only (D-108) — grep-proven zero consumers; the put/get round-trip test passes.
2. RetrievedMemory + full §3.5 UserPreferences + §3.4 UserMemoryFact live at src/core/memory/types.ts (D-112/D-113); WorkingMemory at src/types/harness.ts (D-104); context/types and MemoryDB re-export (D-72/D-83); zero MemoryRecord/MemoryKind (Phase-10 fence).
3. RICH-R-05: PreferenceMemoryStore owns np_persona — zod-validated hydrate, single-writer-gated, idempotent, R2-never-fact-store; the Phase-3 store survives the supersession with initialPreferences defaults + a documented partialize decision; all consumers keep resolving with zero edits.
</success_criteria>

<output>
Create `.planning/phases/08-knowledge-base-memory-minisearch-notes/08-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-8 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/types/notes.ts | Canonical Note (§21.2 verbatim spec 4721-4741) + OKF_NOTE_DEFAULT_TYPE + OkfNoteFrontmatter + NOTE_SUGGESTION_DISPLAY_THRESHOLD; Note.type?: string declaration-only (D-108) | **[this plan]** |
| src/core/memory/types.ts | RetrievedMemory + full §3.5 UserPreferences(+schema, D-54 additive) + §3.4 UserMemoryFact + tone/brevity enums + personaOverridesSchema (D-112/D-113) | **[this plan]** |
| src/types/harness.ts | + WorkingMemory + WORKING_MEMORY_TEMPLATE (C.1/O.10, D-104) | **[this plan]** |
| src/core/storage/NotesDB.ts | Note re-export from @/types/notes (placeholder deleted, D-107) | **[this plan]** |
| src/core/storage/MemoryDB.ts | UserMemoryFact re-export from @/core/memory/types (bootstrap shape superseded, D-104) | **[this plan]** |
| src/core/context/types.ts | RetrievedMemory re-export from ../memory/types (D-112) | **[this plan]** |
| src/core/ai/UserPreferences.ts | Full-shape re-export + initialPreferences defaults + partialize decision (D-112/Pitfall 3) | **[this plan]** |
| src/core/memory/PreferenceMemoryStore.ts | usePreferenceMemoryStore / npPersonaSchema — np_persona owner (RICH-R-05, R2, single-writer, idempotent hydrate) | **[this plan]** |
| src/core/memory/UserMemoryStore.ts | np_facts LRU ≤500 metadata + MemoryDB.userFacts bodies + redaction (D-104/D-113) | 08-02 |
| src/core/memory/MemoryScorer.ts | §3.4 verbatim scoring weights (D-113) | 08-02 |
| src/core/memory/ConversationMemoryStore.ts | np_conversation_meta LRU 10/100 + compactor seam + summaries (D-104/D-106) | 08-02 |
| src/core/memory/MemoryExtractor.ts | memoryFacts schema + parse seam (D-113) | 08-02 |
| src/core/memory/WorkingMemory.ts | O.10 module (redaction swap, ≤300 tokens, single-writer) | 08-02 |
| src/core/memory/MemoryEngine.ts | retrieveConversationMemory / retrieveUserMemory / buildPreferenceProfile / retrieveMemoryHints (D-105, RICH-R-05 DONE-when) | 08-02 |
| src/core/storage/WriteJournal.ts | + evict-conversation steps (D-106, §15.3) | 08-02 |
| src/core/search/MiniSearchIndex.ts | lazy/memoized persistent notes index, note:saved upsert, remove (D-109) | 08-03 |
| src/core/notes/LinkParser.ts | parseLinks/resolveLinks + WIKI-ID-02 tie-break (D-110) | 08-03 |
| src/core/notes/save.ts | NOTE_SAVED_EVENT + saveNote seam — Flow 3 minus LLM (D-110) | 08-03 |
| src/core/notes/NoteGraph.ts | topKSimilar §22.3 cosine + 50-word stop list + backlinks (D-111) | 08-04 |
| src/components/notes/BacklinksPanel.tsx | backlink listing core + thin list (D-111, UI-SPEC) | 08-04 |
| src/components/notes/WikilinkAutocomplete.tsx | MiniSearch title matching (D-04/D-111, UI-SPEC) | 08-04 |
| src/components/notes/NoteGraphView.tsx | graph adjacency/rendering data (D-111, UI-SPEC) | 08-04 |
| tests/core/memory/{MemoryEngine,MemoryScorer,UserMemoryStore}.test.ts | §18 required memory tests | 08-02 |
| tests/core/memory/{PreferenceMemoryStore,ConversationMemoryStore,MemoryExtractor,WorkingMemory}.test.ts | memory subsystem tests | 08-01 (PMS) / 08-02 |
| tests/core/search/MiniSearchIndex.test.ts | §18 required + perf gate (<50 ms/1,000 notes) | 08-03 |
| tests/core/notes/LinkParser.test.ts | §18 required wikilink tests | 08-03 |
| tests/core/notes/NoteGraph.test.ts | §22.3 cosine + stop-list-50 + backlinks | 08-04 |
| tests/core/search/notes-search-e2e.test.ts | §18 DONE-when E2E Page→Note→MiniSearch | 08-05 |
| package.json | verify:phase-8 re-point to spec 3612 (D-114) | 08-05 |
| tests/components/notes/*.test.tsx | three component core-logic tests | 08-04 |