---
phase: 08-knowledge-base-memory-minisearch-notes
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/core/search/MiniSearchIndex.ts
  - src/core/notes/LinkParser.ts
  - src/core/notes/save.ts
  - tests/core/search/MiniSearchIndex.test.ts
  - tests/core/notes/LinkParser.test.ts
autonomous: true
requirements: []
user_setup: []

# Infra plan — no spec-native v1 IDs (ROADMAP Phase 8 lists only RICH-R-05,
# landed in 08-01/08-02). Acceptance = §18 DONE-when (wikilink tie-break,
# MiniSearch <50 ms/1,000 notes) + D-109/D-110 + the note:saved save seam.

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - statement: "D-110: LinkParser.parseLinks extracts [[Title]] targets via the WIKILINK_RE regex; resolveLinks maps each to a note ID via the WIKI-ID-02 resolution order (exact title match → updated desc → id asc) implemented with getAllFromIndex('byTitle') + explicit sort — NEVER getNoteByTitle (Pitfall 4: it returns an arbitrary first hit); resolved targets go to links[] (IDs), raw unmatched targets to unresolvedLinks[] (WIKI-ID-03)"
      verification: backstop
    - statement: "D-110: the Flow-3-minus-LLM save core exists — saveNote = parseLinks → resolveLinks → NotesDB.put → EventBus.emit('note:saved', { noteId }) (spec 1690); a note:saved subscriber upserts MiniSearchIndex (D-109); deletion does NOT rewrite source bodies — dangling edges demote back to unresolvedLinks[] at the next save/graph rebuild (WIKI-ID-04)"
      verification: backstop
    - statement: "D-109: MiniSearchIndex is a per-surface lazy/memoized persistent notes index over NotesDB with searchable fields title+content+tags(+summary seam) per the search-notes contract (spec 1608); storeFields additionally carry `updated` (the WIKI-ID-02 tie-break sort key consumed by plan 08-04's searchSuggestions); built lazily on first query; upserted incrementally on note:saved; remove(noteId) via discard; NEVER persisted (zero storage-area imports — grep-assertable, §26.5 spec 3774); perf gate <50 ms over 1,000 notes (spec 3481) asserted by test"
      verification: backstop
    - statement: "The note:saved event is a declared typed constant + payload (NOTE_SAVED_EVENT + NoteSavedPayload exported from the notes module) — EventBus.ts itself is NOT edited (Pitfall 6: string-keyed, error-swallowing emit; handler registered at module load, re-invocable via __test__ reset)"
      verification: backstop
  artifacts:
    - path: "src/core/notes/LinkParser.ts"
      provides: "D-110 pure parser — WIKILINK_RE = /\\[\\[([^\\]]+)\\]\\]/g; parseLinks(content) → raw targets; resolveLinks(db, targets) → { links: string[]; unresolvedLinks: string[] } via getAllFromIndex + exact-title filter + updated-desc-then-id-asc sort (WIKI-ID-02, Pitfall 4)"
      exports: ["WIKILINK_RE", "parseLinks", "resolveLinks", "LinkResolution"]
    - path: "src/core/notes/save.ts"
      provides: "D-110 save seam — NOTE_SAVED_EVENT = 'note:saved'; NoteSavedPayload { noteId }; saveNote(db, note) = parseLinks → resolveLinks → NotesDB.put → emit(NOTE_SAVED_EVENT, { noteId }) (Flow 3 minus LLM, spec 1690)"
      exports: ["NOTE_SAVED_EVENT", "NoteSavedPayload", "saveNote"]
    - path: "src/core/search/MiniSearchIndex.ts"
      provides: "D-109 persistent notes index — NoteDoc { id, title, content, tags (joined), summary ('' seam), updated (stored tie-break key) }; lazy memoized module-level MiniSearch (searchable fields title+content+tags+summary verbatim, storeFields + 'updated' per the 08-04 contract); query(q) → NoteHit[]; upsert(note); remove(noteId) via discard; note:saved subscription wired at module load; __test__ reset seam; zero storage imports"
      exports: ["MiniSearchIndex", "NoteDoc", "NoteHit", "NOTE_SEARCH_FIELDS", "__test__"]
    - path: "tests/core/notes/LinkParser.test.ts"
      provides: "§18 required — parseLinks extraction; resolveLinks tie-break (two same-title notes → updated desc then id asc winner); unresolvedLinks for no-match; deletion demotion via rebuild (WIKI-ID-04)"
      exports: []
    - path: "tests/core/search/MiniSearchIndex.test.ts"
      provides: "§18 required — lazy build (buildCount===1), note:saved upsert, remove/discard, <50 ms/1,000-notes perf gate (spec 3481), zero storage imports structural assertion"
      exports: []
  key_links:
    - from: "src/core/notes/save.ts"
      to: "src/core/events/EventBus.ts"
      via: "emit(NOTE_SAVED_EVENT, { noteId }) — the Flow-3 emit; EventBus is string-keyed + error-swallowing (Pitfall 6), handler registered at MiniSearchIndex module load"
      pattern: "note:saved"
    - from: "src/core/search/MiniSearchIndex.ts"
      to: "src/core/storage/NotesDB.ts"
      via: "seedFromNotesDB(db) reads all notes (db.getAll('notes')) on lazy build — the index is rebuilt from NotesDB on surface boot, never persisted"
      pattern: "getAll('notes')"
    - from: "src/core/notes/LinkParser.ts"
      to: "src/core/storage/NotesDB.ts"
      via: "db.getAllFromIndex('notes', 'byTitle', title) + exact filter + sort — the WIKI-ID-02 tie-break; getNoteByTitle is NOT used (Pitfall 4)"
      pattern: "getAllFromIndex('notes', 'byTitle'"
  prohibitions:
    - "MiniSearchIndex NEVER persists — zero chrome.storage / idb imports of any kind (grep-assertable; §26.5/D-109); the notes index never shares storage with the Phase-6 per-tab page index (spec 3774)"
    - "getNoteByTitle is never the resolution primitive — resolveLinks does its own getAllFromIndex + sort (Pitfall 4)"
    - "No edits to src/core/events/EventBus.ts — the event is a typed constant in the notes module (Pitfall 6)"
    - "No LLM in the save path — no NoteTagger/NoteQA/NoteFileSync (Phase 9); save core is parse→resolve→put→emit only (D-110)"
    - "No note-body rewrites on delete (WIKI-ID-04) — demotion happens at next save/rebuild, never by rewriting sources"
    - "Zero new @ts-expect-error NP-STRICT markers (STATE.md decision 17)"
    - "Breadcrumb (PROHIB_ABSENT fallback): canon-security items (OWASP/prototype-pollution/generic injection) dropped — covered by threat model; note content is untrusted data (CTX-02) rendered only via React JSX in plan 08-04"

---

<objective>
Notes core plan: the persistent notes search index and the wikilink save path — `MiniSearchIndex` (per-surface lazy/memoized persistent notes index, D-109), `LinkParser` (parseLinks/resolveLinks with the WIKI-ID-02 tie-break, D-110), and the `save.ts` seam (Flow 3 minus the LLM pipeline: parse → resolve → NotesDB.put → emit note:saved).

Purpose: this is the knowledge-model core — atomic notes + wikilinks + tags. The save core proves the §18 DONE-when "wikilinks resolve with tie-break rule" and the `<50 ms over 1,000 notes` perf gate; the note:saved event is the seam plan 08-04's components and plan 08-05's E2E build on.

Output: the five files in frontmatter, strict-clean; LinkParser.test.ts (§18 required) + MiniSearchIndex.test.ts (§18 required, incl. the perf gate).
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  # Phase 8 block (253-267): SC#4-5
@.planning/STATE.md  # decision 11 (WIKI-ID UUID identity, ADR-NOTE-01), decision 17 (strict-clean)
@.planning/PRODUCT_SPEC_v0_1.md  # §21.2 knowledge model (3287-3322), §22.3 (3506-3514), §26.5 two-index rule (3774), §27.7a WIKI-ID-01…04 (3899-3904), Flow 3 save pipeline (1690), spec 1608 (search-notes contract), spec 3481 (perf gate)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-CONTEXT.md  # D-109/D-110 + discretion (save.ts seam; NOTE_SAVED_EVENT declared now; MiniSearchIndex doc shape)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-RESEARCH.md  # Pattern 1 (MiniSearch wrapper 229-248), Pitfalls 4/5/6, Open Question 4 (remove API, no new event), Assumption A2 (NoteDoc encoding), Standard Stack (minisearch 7.2.0)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-PATTERNS.md  # LinkParser (330-351), save.ts (369-379), MiniSearchIndex (274-325), NotesDB tie-break (341-348)
@.planning/phases/08-knowledge-base-memory-minisearch-notes/08-VALIDATION.md  # 08-03-01 (MiniSearchIndex.test.ts + LinkParser.test.ts)
@src/core/extraction/PageIndexBuilder.ts  # the Phase-6 MiniSearch pattern to mirror (145-180, 219-261)
@src/core/storage/NotesDB.ts  # canonical Note re-export (08-01) + byTitle/byUpdated indexes + openNotesDB
@src/core/events/EventBus.ts  # on/emit/off (16-37) — READ-ONLY
@src/types/notes.ts  # canonical Note (08-01)
@tests/core/extraction/PageIndexBuilder.test.ts  # test conventions for the MiniSearch wrapper (1-100)
@tests/setup.ts  # __resetIndexedDB (9-11)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — save-path core: LinkParser (WIKI-ID-02 tie-break) + save.ts seam (parse → resolve → put → emit)</name>
  <files>src/core/notes/LinkParser.ts, src/core/notes/save.ts, tests/core/notes/LinkParser.test.ts</files>
  <read_first>08-PATTERNS.md LinkParser (330-351 — pure-fn module, WIKILINK_RE, resolveLinks tie-break) + save.ts (369-379 — NOTE_SAVED_EVENT + NoteSavedPayload + saveNote) · 08-RESEARCH.md Pitfall 4 (getNoteByTitle is first-hit — resolveLinks must getAllFromIndex + sort) + Pitfall 6 (EventBus string-keyed, declare the event here) + Pattern 5 (WIKI-ID-02 verbatim resolution order) · 08-CONTEXT.md D-110 + discretion (save.ts seam satisfies Flow 3) · PRODUCT_SPEC §27.7a WIKI-ID-01…04 (3899-3904) + Flow 3 (1690) · @src/core/storage/NotesDB.ts (byTitle index 69-70, getNoteByTitle 88-93 — DO NOT rely on it) · @src/core/events/EventBus.ts (16-37) · @src/types/notes.ts (canonical Note) · @tests/core/extraction/PageIndexBuilder.test.ts (pure-fn test style)</read_first>
  <action>Build the wikilink save-path core end-to-end — the phase's signature vertical slice (Flow 3 minus the LLM pipeline, D-110). Strict-clean.

1. `src/core/notes/LinkParser.ts` (NEW) — pure-function module (PageIndexBuilder.ts:59-61 constant pattern). `export const WIKILINK_RE = /\[\[([^\]]+)\]\]/g;` (the [[Title]] syntax per WIKI-ID-02). `export function parseLinks(content: string): string[]` — return every `[[...]]` target in document order, deduplicated, trimmed (no nested-bracket support — the spec syntax is flat [[Title]]). `export interface LinkResolution { links: string[]; unresolvedLinks: string[] }` and `export async function resolveLinks(db: IDBPDatabase<NotesDBV1>, targets: string[]): Promise<LinkResolution>` — for each target: `const hits = await db.getAllFromIndex('notes', 'byTitle', target)`; filter EXACT title matches (`n.title === target`); sort by `b.updated - a.updated` desc then `a.id < b.id ? -1 : a.id > b.id ? 1 : 0` (WIKI-ID-02 verbatim — Pitfall 4: do NOT use getNoteByTitle); take the winner's id into links[]; no match → push the raw target string into unresolvedLinks[] (WIKI-ID-03). Export a `demoteDangling(links: string[], liveIds: Set<string>, unresolvedLinks: string[]): { links: string[]; unresolvedLinks: string[] }` helper — WIKI-ID-04: a save/rebuild that recomputes links[] membership against the live note set moves dangling IDs back to unresolvedLinks[] (raw title string), WITHOUT rewriting any source body. Header comment: D-110 + WIKI-ID-02/03/04.
2. `src/core/notes/save.ts` (NEW) — the Flow-3-minus-LLM seam (D-110, spec 1690): `export const NOTE_SAVED_EVENT = 'note:saved';` and `export interface NoteSavedPayload { noteId: string }` (typed event declaration — Pitfall 6: EventBus.ts itself is NOT edited). `export async function saveNote(db: IDBPDatabase<NotesDBV1>, note: Note): Promise<{ note: Note; emitted: boolean }>` — (a) parseLinks(note.content) → resolveLinks(db, targets) → write `note.links = resolution.links; note.unresolvedLinks = resolution.unresolvedLinks` (the canonical Note carries both arrays — 08-01); (b) `await db.put('notes', note)` (NotesDB value shape is the canonical Note); (c) `emit(NOTE_SAVED_EVENT, { noteId: note.id } as NoteSavedPayload)` (EventBus.emit — error-swallowing by design, Pitfall 6); return emitted:true. Single-writer note: the save path itself is caller-owned (Phase-15 NotesWorkspace gates); saveNote is the pure seam — no election gate inside (document this in a comment: the primary-surface gate lives at the UI layer, D-110).
3. `tests/core/notes/LinkParser.test.ts` (NEW, §18 required) — pure + DB tests. (a) parseLinks: 'See [[Alpha]] and [[Beta]] and [[Alpha]] again' → ['Alpha','Beta'] deduplicated; no wikilinks → []; whitespace-only target trimmed. (b) RESOLUTION TIE-BREAK (Pitfall 4): seed NotesDB with two notes titled 'Same' — older (updated=100) and newer (updated=200) — resolveLinks(['Same']) returns the NEWER id (updated desc); seed two with identical updated → id-asc winner; (c) exact-title: '[[Same]]' does not match 'SAME'/'Same ' (exact match only); (d) unresolved: '[[No Such Note]]' → unresolvedLinks ['No Such Note'], links empty (WIKI-ID-03); (e) DEMOTION (WIKI-ID-04): a note with links ['deleted-id'] + a rebuild using demoteDangling against a live set without 'deleted-id' → links empty, unresolvedLinks gains the raw title; assert NO NotesDB write happened during demotion (pure function — no db.put call in the helper); (f) saveNote end-to-end: build a canonical Note with content '[[Alpha]]' + an existing Alpha note → saveNote → the saved note.links === [alphaId], note.unresolvedLinks === [], and EventBus has a 'note:saved' listener (hasListeners) or emit was observed via a test subscriber; (g) saveNote with an unmatched target → unresolvedLinks carries the raw target.</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `npx vitest run tests/core/notes/LinkParser.test.ts` passes; `grep -n "WIKILINK_RE = /\[\[" src/core/notes/LinkParser.ts` shows the regex; `grep -n "getAllFromIndex('notes', 'byTitle'" src/core/notes/LinkParser.ts` shows the tie-break query; `grep -c "getNoteByTitle" src/core/notes/LinkParser.ts` equals 0 (Pitfall 4 — first-hit helper never used); `grep -n "NOTE_SAVED_EVENT = 'note:saved'" src/core/notes/save.ts` shows the typed event; `grep -n "emit(NOTE_SAVED_EVENT" src/core/notes/save.ts` shows the Flow-3 emit; `grep -n "saveNote" src/core/notes/save.ts` shows the seam; `grep -rn "NP-STRICT" src/core/notes/LinkParser.ts src/core/notes/save.ts tests/core/notes/LinkParser.test.ts | wc -l` equals 0</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/notes/LinkParser.test.ts && test "$(grep -c 'getNoteByTitle' src/core/notes/LinkParser.ts)" -eq 0 && test "$(grep -rn 'NP-STRICT' src/core/notes/LinkParser.ts src/core/notes/save.ts tests/core/notes/LinkParser.test.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>The save-path core is green and committed: parseLinks extracts [[Title]] targets, resolveLinks applies the WIKI-ID-02 tie-break via getAllFromIndex + explicit sort (never getNoteByTitle), unresolved targets land in unresolvedLinks[] (WIKI-ID-03), demoteDangling handles WIKI-ID-04 without rewriting bodies, and saveNote runs the full Flow-3-minus-LLM path (parse → resolve → NotesDB.put → emit note:saved) — all proven by LinkParser.test.ts.</done>
  <!-- planner-discipline-allow: [[ -->
  <!-- planner-discipline-allow: note:saved -->
  <!-- planner-discipline-allow: getNoteByTitle -->
  <reversibility rating="reversible">D-110 — pure functions + graph ops; the save-wiring later (Phase-15 UI) is a caller edit (CONTEXT.md rating).</reversibility>
</task>

<task type="auto">
  <name>Task 2: MiniSearchIndex — persistent per-surface notes index (lazy/memoized, note:saved upsert, perf gate)</name>
  <files>src/core/search/MiniSearchIndex.ts, tests/core/search/MiniSearchIndex.test.ts</files>
  <read_first>08-PATTERNS.md MiniSearchIndex section (274-325 — EXACT PageIndexBuilder analog: buildIndex fields, lazy singleton, __test__ seam, object-form export) · 08-RESEARCH.md Pattern 1 (MiniSearch wrapper 229-248) + Pitfall 5 (stored-fields cast) + Pitfall 6 (module-load subscription, re-invocable via __test__) + Assumption A2 (NoteDoc encoding: tags joined, summary '' seam) + Open Question 4 (remove(noteId) via discard — no new event) · 08-CONTEXT.md D-109 · PRODUCT_SPEC spec 1608 (search-notes fields: title + content + tags + summary) + spec 3481 (perf gate) + §26.5 (3774: never shared with page index) · @src/core/extraction/PageIndexBuilder.ts (145-180 buildIndex/query, 219-230 wire hook, 237-261 __test__ + namespace export) · @src/core/notes/save.ts (NOTE_SAVED_EVENT + NoteSavedPayload from Task 1) · @src/core/storage/NotesDB.ts (openNotesDB + canonical Note) · @src/core/events/EventBus.ts (on 16-25 — returns unsubscribe) · @tests/core/extraction/PageIndexBuilder.test.ts (test conventions incl. __test__.reset) · @tests/setup.ts (__resetIndexedDB)</read_first>
  <action>Build the persistent notes index (D-109) — distinct instance from the Phase-6 ephemeral page index (spec 3774). Strict-clean.

1. `src/core/search/MiniSearchIndex.ts` (NEW) — mirror PageIndexBuilder.ts:145-261:
   a. `export interface NoteDoc { id: string; title: string; content: string; tags: string; summary: string; updated: number }` (A2: tags = note.tags.join(' '), summary = note.summary ?? '' — the Phase-9 seam; updated = note.updated — STORED so the WIKI-ID-02 tie-break in plan 08-04 has its sort key; the searchable fields set stays spec-1608 verbatim) and `export type NoteHit = { id: string; score: number } & NoteDoc`.
   b. `buildIndex(docs: NoteDoc[]): MiniSearch<NoteDoc>` — `new MiniSearch({ fields: ['title','content','tags','summary'], storeFields: ['title','content','tags','summary','updated'], searchOptions: { boost: { title: 3 }, prefix: true, fuzzy: 0.2 } })` + addAll (spec 1608 fields — the searchable `fields` set stays verbatim title+content+tags+summary; `storeFields` adds 'updated' so NoteHit carries the WIKI-ID-02 tie-break key for plan 08-04's searchSuggestions; the PageIndexBuilder boost/prefix/fuzzy pattern — RESEARCH Pattern 1).
   c. LAZY/MEMOIZED singleton: module-level `let index: MiniSearch<NoteDoc> | null = null` + `let lazyBuildCount = 0`; `getIndex(db)` builds once from `db.getAll('notes')` mapped to NoteDoc[] (seedFromNotesDB — the mapping carries `updated: note.updated`), memoized thereafter; `query(db, q)` → `getIndex(db).search(q) as unknown as NoteHit[]` (Pitfall 5: stored fields spread at runtime — the PageIndexBuilder.ts:179 cast precedent).
   d. INCREMENTAL UPSERT: `upsert(note: Note)` — `getIndex(...)` (build if needed) then `index.upsert(noteToDoc(note))` (noteToDoc carries note.updated — the tie-break key stays fresh on every save) — no full rebuild on save (D-109). DELETE: `remove(noteId)` → `index.discard(noteId)` (by-ID lazy vacuum — RESEARCH Open Q4, direct API, NO new note:deleted event).
   e. note:saved SUBSCRIPTION at module load (Pitfall 6): `let noteSavedUnsubscribe` + `wireNoteSaved()` calling `on(NOTE_SAVED_EVENT, (payload: NoteSavedPayload) => { void upsert by fetching the note from NotesDB by id })` — the handler is re-invocable via the __test__ reset seam (PageIndexBuilder.ts:219-230 pattern).
   f. __test__ seam: `export const __test__ = { reset(): void { index = null; lazyBuildCount = 0; wireNoteSaved(); }, get buildCount(): number { return lazyBuildCount; } }` and the object-form namespace export `export const MiniSearchIndex = { query, upsert, remove, __test__ }` + named exports (ProviderRegistry convention).
   g. ZERO storage-area imports (D-109/§26.5 — never persisted): the only chrome/idb-adjacent import is the type-only IDBPDatabase for the db param + openNotesDB used by the SEEDER (the seeder opens the DB handle passed in by the caller — match PageIndexBuilder: the module never imports a storage adapter; grep-assertable).
2. `tests/core/search/MiniSearchIndex.test.ts` (NEW, §18 required) — copy PageIndexBuilder.test.ts structure (1-38: `import { __test__ as indexTest }`, `beforeEach(() => indexTest.reset())`, reset __resetIndexedDB). (a) LAZY: no query yet → buildCount 0; first query → 1; second query → still 1 (memoized); (b) FIELDS: seed 3 notes with distinct title/content/tags → query('titleword') ranks the title match above a content-only match (boost); query('tagword') matches the tags field; summary seam: a note with summary populated is hit via its summary text; STORED UPDATED (the 08-04 contract): a query hit for a seeded note carries `hit.updated === note.updated` (the storeField plan 08-04's searchSuggestions WIKI-ID-02 tie-break sorts on — assert the exact value round-trips through search); (c) UPSERT: seed → query misses a new note → emit note:saved via the save.ts event (or call upsert directly) → query now hits it WITHOUT a rebuild (buildCount unchanged); (d) REMOVE: discard a note's id → query no longer returns it; (e) PERF GATE (spec 3481): build the index over 1,000 synthetic notes (loop-generated NoteDocs), `const t0 = Date.now(); query('needle'); expect(Date.now() - t0).toBeLessThan(50)` (assert < 50 ms — SC#4); (f) ZERO-PERSIST structural assertion (secrets-inspection style): read MiniSearchIndex.ts source via fs and assert it contains no 'chrome.storage' and no 'openNotesDB' outside the seeder doc-block (or assert the file's import lines include no storage adapter import — mirror the PageIndexBuilder never-persisted posture).</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `npx vitest run tests/core/search/MiniSearchIndex.test.ts` passes (incl. the <50 ms perf gate); `grep -n "fields: \['title', 'content', 'tags', 'summary'\]" src/core/search/MiniSearchIndex.ts` shows the spec-1608 field set; `grep -n "storeFields" src/core/search/MiniSearchIndex.ts` shows storeFields incl. 'updated' (the WIKI-ID-02 tie-break key plan 08-04 sorts on); `grep -n "boost: { title: 3 }" src/core/search/MiniSearchIndex.ts` shows the title boost; `grep -n "NOTE_SAVED_EVENT" src/core/search/MiniSearchIndex.ts` shows the subscription; `grep -n "discard(noteId)" src/core/search/MiniSearchIndex.ts` shows the delete path; `grep -c "chrome.storage" src/core/search/MiniSearchIndex.ts` equals 0 (never persisted — D-109); `grep -rn "NP-STRICT" src/core/search/MiniSearchIndex.ts tests/core/search/MiniSearchIndex.test.ts | wc -l` equals 0</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/search/MiniSearchIndex.test.ts && test "$(grep -c 'chrome.storage' src/core/search/MiniSearchIndex.ts)" -eq 0 && test "$(grep -rn 'NP-STRICT' src/core/search/MiniSearchIndex.ts tests/core/search/MiniSearchIndex.test.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>MiniSearchIndex is the real, tested, never-persisted per-surface notes index: lazy/memoized build, spec-1608 fields with the summary seam, incremental note:saved upsert (no rebuild), remove via discard, the <50 ms/1,000-notes perf gate asserted, and the zero-storage-import posture grep-verified; the module-load note:saved subscription is re-invocable via __test__.reset.</done>
  <!-- planner-discipline-allow: NoteDoc -->
  <!-- planner-discipline-allow: chrome.storage -->
  <reversibility rating="reversible">D-109 — additive wrapper; rebuilding strategy is a local edit (CONTEXT.md rating).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note content (markdown body) → LinkParser | Untrusted user-authored content is parsed — the regex extracts [[Title]] targets only, never interprets HTML/script |
| saved note → NotesDB.put → MiniSearchIndex | Note content flows into the in-memory index — search results carry stored fields, rendered later via React JSX only (plan 08-04) |
| note:saved event → MiniSearchIndex subscriber | In-surface EventBus pub/sub — same-module handlers only, error-swallowing emit (Pitfall 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-08 | Tampering | LinkParser regex on untrusted note bodies | medium | mitigate | WIKILINK_RE matches only [[...]] targets — no HTML/script interpretation; resolution maps targets to canonical Note IDs via typed NotesDB reads; unresolved targets stay raw strings in unresolvedLinks[] (WIKI-ID-03) — never executed/rendered as markup in Phase 8 |
| T-8-09 | Denial of Service | MiniSearchIndex lazy build / incremental upsert | medium | mitigate | Lazy + memoized build (once per surface); incremental upsert — no full rebuild on save; <50 ms/1,000-notes perf gate asserted in test (spec 3481); index never persisted (zero storage imports) so no disk-growth vector |
| T-8-10 | Tampering | note:saved event subscription | low | mitigate | Typed NoteSavedPayload { noteId }; handler re-invocable via __test__ reset (Pitfall 6); EventBus is same-surface in-memory pub/sub — no cross-context injection surface; handler errors swallowed by design (never crash the emitter) |
| T-8-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO installs this phase (Package Legitimacy Audit vacuous — minisearch 7.2.0 pre-installed, 08-RESEARCH.md); executor must not add any dependency |

</threat_model>

<verification>
- **Per-task commits:** `pnpm run lint` + the affected test file (08-VALIDATION.md sampling rate).
- Grep guards: zero NP-STRICT markers; zero chrome.storage imports in MiniSearchIndex (D-109 never-persisted); zero getNoteByTitle in LinkParser (Pitfall 4); zero EventBus.ts edits (Pitfall 6).
- Wave-2 merge check: `npx vitest run tests/core/search tests/core/notes/LinkParser.test.ts` green.
- Perf gate: the MiniSearchIndex.test.ts 1,000-note query must assert < 50 ms (spec 3481, SC#4).
- The E2E (Page → Note → MiniSearch) lands in plan 08-05; this plan provides its save core + index.
</verification>

<success_criteria>
1. LinkParser + save.ts ship the Flow-3-minus-LLM save core: parse → resolve (WIKI-ID-02 tie-break via getAllFromIndex + sort, never getNoteByTitle) → NotesDB.put → emit note:saved; unresolved links tracked (WIKI-ID-03); dangling edges demote without rewriting bodies (WIKI-ID-04). SC#5 (wikilink tie-break rule) proven.
2. MiniSearchIndex is the lazy/memoized, never-persisted per-surface notes index with spec-1608 fields, incremental note:saved upsert, remove-by-discard, and the <50 ms/1,000-notes perf gate asserted (SC#4).
</success_criteria>

<output>
Create `.planning/phases/08-knowledge-base-memory-minisearch-notes/08-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-8 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/notes/LinkParser.ts | WIKILINK_RE / parseLinks / resolveLinks / LinkResolution / demoteDangling — WIKI-ID-02 tie-break (D-110) | **[this plan]** |
| src/core/notes/save.ts | NOTE_SAVED_EVENT / NoteSavedPayload / saveNote — Flow 3 minus LLM (D-110) | **[this plan]** |
| src/core/search/MiniSearchIndex.ts | MiniSearchIndex / NoteDoc (incl. stored `updated` — the 08-04 tie-break key) / NoteHit / __test__ — lazy persistent notes index, note:saved upsert, remove (D-109) | **[this plan]** |
| tests/core/notes/LinkParser.test.ts | §18 — parse/extract, tie-break fixtures, unresolvedLinks, demotion, saveNote end-to-end | **[this plan]** |
| tests/core/search/MiniSearchIndex.test.ts | §18 — lazy build, upsert, remove, <50 ms/1,000-notes perf gate, zero-storage assertion | **[this plan]** |