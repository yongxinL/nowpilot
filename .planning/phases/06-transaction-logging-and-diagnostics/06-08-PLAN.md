---
phase: 06-transaction-logging-and-diagnostics
plan: 08
type: execute
wave: 4
depends_on:
  - 06-06
  - 06-07
files_modified:
  - src/components/options/DiagnosticsPanel.tsx
  - src/components/options/DiagnosticsSection.tsx
  - src/components/diagnostics/TransactionTable.tsx
  - src/components/diagnostics/TraceDetailPanel.tsx
  - src/components/diagnostics/ProviderTimeline.tsx
  - src/components/diagnostics/ToolCallDescriptions.tsx
  - src/components/diagnostics/CacheStats.tsx
  - src/components/options/OptionsRoot.tsx
  - src/entrypoints/fullapp/App.tsx
autonomous: false
requirements:
  - TELE-06
  - TELE-07

must_haves:
  truths:
    - "DiagnosticsPanel renders in Full App Options Diagnostics section with master-detail layout — transaction table on left, detail panel on right (D-12)"
    - "Filter bar supports type, status, provider, severity, date range, and free-text search (D-13)"
    - "Selecting a transaction shows ProviderTimeline (AntD Timeline), ToolCallDescriptions (AntD Descriptions), CacheStats (AntD Statistic), and Copyable operationIds (D-15)"
    - "Error toast in Side Panel includes Open Diagnostics button linking to app.html?page=options&section=diagnostics&operationId=<id> (D-14, TELE-07)"
    - "Full App parses operationId query param and auto-selects matching trace (D-14)"
  artifacts:
    - src/components/options/DiagnosticsPanel.tsx
    - src/components/options/DiagnosticsSection.tsx
    - src/components/diagnostics/TransactionTable.tsx
    - src/components/diagnostics/TraceDetailPanel.tsx
    - src/components/diagnostics/ProviderTimeline.tsx
    - src/components/diagnostics/ToolCallDescriptions.tsx
    - src/components/diagnostics/CacheStats.tsx
  key_links:
    - "DiagnosticsSection injected into OptionsRoot via renderSectionContent('diagnostics') (D-16)"
    - "Error toast uses WorkspaceRouter.openFullApp() to construct deep-link URL"
---

<objective>
Build the DiagnosticsPanel UI in Full App Options with master-detail layout, filter bar, AntD component mapping, and deep-linking. Add "Open Diagnostics" link to error toasts in the Side Panel and query param parsing in Full App entry point.

Purpose: Deliver the user-facing diagnostics experience — TELE-06 (DiagnosticsPanel) and TELE-07 (error toast deep-linking).
Output: 7 new React component files, modified OptionsRoot.tsx, modified Full App App.tsx
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-transaction-logging-and-diagnostics/06-CONTEXT.md (D-12 through D-16, D-37, D-38, D-39)
@.planning/phases/06-transaction-logging-and-diagnostics/06-RESEARCH.md (lines 963-1059, DiagnosticsPanel architecture; lines 1079-1102, error toast integration)
@.planning/phases/06-transaction-logging-and-diagnostics/06-PATTERNS.md (DiagnosticsPanel and sub-component sections)
@src/components/options/OptionsRoot.tsx (renderSectionContent prop, diagnostics section registered at line 35)
@src/entrypoints/fullapp/App.tsx (Full App entry point, URL parsing)
@src/core/stores/diagnosticsStore.ts (created in 06-06)
@src/core/telemetry/types.ts (all trace types)
@src/core/telemetry/export.ts (exportSingleTrace, exportTraces, downloadBlob from 06-07)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DiagnosticsPanel master-detail layout with filter bar</name>
  <files>src/components/options/DiagnosticsPanel.tsx, src/components/options/DiagnosticsSection.tsx</files>
  <read_first>
@src/components/options/OptionsRoot.tsx (layout pattern, renderSectionContent prop)
@src/core/stores/diagnosticsStore.ts (filter state, mode toggles, transaction data)
@.planning/phases/06-transaction-logging-and-diagnostics/06-RESEARCH.md (lines 967-1002, component tree and layout diagram)
@.planning/phases/06-transaction-logging-and-diagnostics/06-CONTEXT.md (D-12 through D-16)
  </read_first>
  <action>
Create DiagnosticsSection.tsx — thin wrapper that renders DiagnosticsPanel. Acts as the renderSectionContent('diagnostics') target. Props: none. Returns <DiagnosticsPanel />.

Create DiagnosticsPanel.tsx — main master-detail layout component using AntD Row/Col or Flex:

1. **Filter Bar (top):** AntD Space wrapping:
   - Select for filterType (TransactionType options: chat, planner, tool, agent, renderer, system)
   - Select for filterStatus (TransactionStatus: started, streaming, completed, failed, aborted)
   - Select for filterProvider (populated dynamically from transaction data or hardcoded common providers)
   - Select for filterSeverity (Severity: DEBUG, INFO, WARNING, ERROR, CRITICAL)
   - DatePicker.RangePicker for filterDateRange
   - Input.Search for searchQuery (searches operationId, model, providerId, errorCode)
   - Switch for diagnosticMode (label: "Diagnostic Mode")
   - Switch for privacyMode (label: "Privacy Mode")
   - Button "Export" (triggers export dialog — for now, calls exportTraces with current filters)
   - Button "Clear Filters" (resets all filters)

2. **Layout:** Split into left (TransactionTable, ~40% width) and right (TraceDetailPanel, ~60% width) using AntD Row/Col with gutter.

3. On mount: call useDiagnosticsStore.refreshTransactions() to load initial data.

4. Filter changes call setFilter and then refreshTransactions.

5. Diagnostic/Privacy mode toggles call setDiagnosticMode/setPrivacyMode. When privacyMode switches on, show AntD Alert info: "Privacy Mode active — content fields hidden."

Use useDiagnosticsStore() hook for all state. Import AntD components: Row, Col, Flex, Select, DatePicker, Input, Switch, Button, Space, Alert, Typography.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/options/DiagnosticsPanel.tsx && npx tsc --noEmit src/components/options/DiagnosticsSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DiagnosticsPanel.tsx renders filter bar with 8 controls (5 selects, date range, search, 2 switches, 2 buttons)
    - Master-detail layout with TransactionTable (left) and TraceDetailPanel (right)
    - All filter changes refresh transactions via diagnosticsStore
    - Mode toggles update diagnosticsStore state
    - Privacy Mode shows alert banner
    - DiagnosticsSection.tsx wraps DiagnosticsPanel for OptionsRoot injection
    - npx tsc --noEmit passes for both files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build sub-components — TransactionTable, TraceDetailPanel, ProviderTimeline, ToolCallDescriptions, CacheStats</name>
  <files>src/components/diagnostics/TransactionTable.tsx, src/components/diagnostics/TraceDetailPanel.tsx, src/components/diagnostics/ProviderTimeline.tsx, src/components/diagnostics/ToolCallDescriptions.tsx, src/components/diagnostics/CacheStats.tsx</files>
  <read_first>
@src/components/options/OptionsRoot.tsx (sidebar list pattern for table rows)
@.planning/phases/06-transaction-logging-and-diagnostics/06-RESEARCH.md (lines 980-1002, component tree with AntD props; lines 1050-1059, AntD component mapping table)
@.planning/phases/06-transaction-logging-and-diagnostics/06-CONTEXT.md (D-15 AntD mappings; D-37 hidden by default)
@src/core/telemetry/types.ts (AITransaction, TraceTree, ProviderAttempt, ToolTrace, CacheTrace types)
@src/core/stores/diagnosticsStore.ts (selectedOperationId, traceTree, selectTransaction)
  </read_first>
  <action>
Create 5 sub-component files:

**TransactionTable.tsx:**
AntD Table with columns: Status (icon: CheckCircleOutlined green/CloseCircleOutlined red/WarningOutlined orange), Type (Tag), Provider+Model, operationId (Typography.Text copyable code ellipsis), Duration (formatted ms), Severity (Tag with color), Timestamp (formatted), Actions (Button "Export Trace" — calls exportSingleTrace + downloadBlob). Row click sets selectedOperationId. Table uses pagination (pageSize=20). Data from useDiagnosticsStore.transactions. Highlight selected row.

**TraceDetailPanel.tsx:**
Conditional render: if no selectedOperationId, show AntD Empty "Select a transaction to view details". If loading, show Spin. If traceTree exists, render sections:
- TransactionHeader: Typography.Title with operationId (copyable), Tag for status, duration, severity
- ProviderTimeline component
- ToolCallDescriptions component (only if toolTraces.length > 0)
- CacheStats component (only if cacheTraces.length > 0)
- Prompt section: Typography.Text copyable for promptHash, tokenBreakdown as Progress bars (one per section), contextTier as Tag
- Memory section: summary of factsRetrieved/factsExtracted (only if memoryTraces.length > 0)
- WriteJournal section: summary (only if writeJournalTraces.length > 0)
- Collapse "Show redacted details" (D-37): renders redacted content when expanded. Only visible in diagnostic mode; in normal mode, shows "Enable Diagnostic Mode to view details"

**ProviderTimeline.tsx:**
AntD Timeline with items from ProviderTrace.attempts[]. Each Timeline.Item: color based on outcome (green=success, red=error, orange=timeout/circuit_open), dot icon per outcome, children shows providerId, model, durationMs, errorCode if failed. ProviderTrace at top shows resolvedProviderId + totalDurationMs.

**ToolCallDescriptions.tsx:**
AntD Descriptions bordered column=1. One Descriptions.Item per ToolTrace: label=toolName, children shows: source badge, permissionDecision tag, status tag, durationMs, errorMessage if present. Dangerous tools show red tag.

**CacheStats.tsx:**
AntD Row with Statistic cards: "Cache Hits" (count of hit events), "Cache Misses" (count of miss events), "Tokens Saved" (sum of estimatedTokenSavings), "Invalidations" (count of invalidation events). Use AntD Statistic with prefix icons.

All components import from useDiagnosticsStore for data. No direct DB calls in UI components — use store actions. Follow existing React patterns: functional components, AntD imports from 'antd', icons from '@ant-design/icons'.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/diagnostics/TransactionTable.tsx && npx tsc --noEmit src/components/diagnostics/TraceDetailPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 5 sub-component files created in src/components/diagnostics/
    - TransactionTable renders filterable row list with status icons, copyable IDs, export action
    - TraceDetailPanel shows transaction header, provider timeline, tool descriptions, cache stats conditionally
    - ProviderTimeline uses AntD Timeline with color-coded outcomes
    - ToolCallDescriptions uses AntD Descriptions bordered with permission + status tags
    - CacheStats uses AntD Statistic cards with hit/miss/saved/invalidation counts
    - "Show redacted details" hidden by default, expandable in diagnostic mode (D-37)
    - npx tsc --noEmit passes for all files
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify DiagnosticsPanel UI and Error Toast deep-linking</name>
  <files></files>
  <action>
The agent has built the DiagnosticsPanel with master-detail layout, filter bar, TransactionTable, TraceDetailPanel, ProviderTimeline, ToolCallDescriptions, CacheStats, deep-linking via operationId query param, and error toast "Open Diagnostics" link. Human verification required because UI layout, visual rendering, and cross-surface navigation cannot be fully verified by automated tests.
  </action>
  <what-built>
DiagnosticsPanel with master-detail layout, filter bar, transaction table, trace detail panel with Timeline/Descriptions/Statistic components. Deep-linking via operationId query param integration in Full App entry point.
  </what-built>
  <how-to-verify>
1. Open Full App, navigate to Options, select "Diagnostics" section from the sidebar
2. Verify the filter bar renders with all dropdowns, date picker, search, and mode toggles
3. Verify Diagnostic Mode and Privacy Mode toggles work and persist state
4. Click a transaction row — verify the detail panel shows provider timeline, tool call descriptions, cache stats
5. Verify "Export Trace" button triggers file download with valid JSON
6. Trigger an error in Side Panel (e.g., configure an invalid provider and attempt a chat)
7. Verify the error toast shows "Open Diagnostics" button
8. Click "Open Diagnostics" — verify Full App opens to Diagnostics section with the correct trace auto-selected
  </how-to-verify>
  <verify>
    <human-check>Verify DiagnosticsPanel renders correctly in Full App Options with working filter bar, trace detail, and deep-linking from error toast</human-check>
  </verify>
  <done>DiagnosticsPanel renders in Full App Options with working master-detail layout, filter bar filters transactions, detail panel shows traces, export works, and error toast deep-linking navigates correctly</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | DiagnosticsPanel rendering trace content | medium | mitigate | AntD components auto-escape React content. HARD-10 prohibits dangerouslySetInnerHTML. All trace data already redacted at write time |
| T-06-02 | Information Disclosure | Export button in DiagnosticsPanel | medium | mitigate | Export passes through TraceRedactor and Privacy Mode enforcement (06-07). UI shows Privacy Mode active banner |
</threat_model>

<verification>
## Quick Verify
npx tsc --noEmit

## UI Build Verify
pnpm build (WXT build for extension — verifies all components compile in extension context)
</verification>

<success_criteria>
- [ ] DiagnosticsPanel renders in Full App Options Diagnostics section
- [ ] Filter bar with 8 controls works end-to-end — filter changes update transaction list
- [ ] TransactionTable shows status icons, copyable operationIds, export action per row
- [ ] TraceDetailPanel conditionally renders ProviderTimeline, ToolCallDescriptions, CacheStats
- [ ] AntD Timeline shows provider attempts with correct color coding
- [ ] AntD Descriptions shows tool calls with permission and status tags
- [ ] AntD Statistic cards show cache hit/miss/saved invalidation counts
- [ ] "Show redacted details" Collapse hidden in normal mode, expandable in diagnostic mode
- [ ] Diagnostic/Privacy mode toggles work and persist
- [ ] Export button triggers download via export.ts functions
- [ ] Error toast "Open Diagnostics" link deep-links correctly
- [ ] Full App parses operationId query param and auto-selects trace
- [ ] Human checkpoint approved
</success_criteria>

<output>
Create .planning/phases/06-transaction-logging-and-diagnostics/06-08-SUMMARY.md when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| DiagnosticsPanel | React component | src/components/options/DiagnosticsPanel.tsx |
| DiagnosticsSection | React component | src/components/options/DiagnosticsSection.tsx |
| TransactionTable | React component | src/components/diagnostics/TransactionTable.tsx |
| TraceDetailPanel | React component | src/components/diagnostics/TraceDetailPanel.tsx |
| ProviderTimeline | React component | src/components/diagnostics/ProviderTimeline.tsx |
| ToolCallDescriptions | React component | src/components/diagnostics/ToolCallDescriptions.tsx |
| CacheStats | React component | src/components/diagnostics/CacheStats.tsx |
