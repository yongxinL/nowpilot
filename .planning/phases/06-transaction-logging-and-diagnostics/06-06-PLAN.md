---
phase: 06-transaction-logging-and-diagnostics
plan: 06
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-04
files_modified:
  - src/core/ai/pipeline/AgentOrchestrator.ts
  - src/core/ai/pipeline/PlannerService.ts
  - src/core/ai/pipeline/ExecutorService.ts
  - src/core/ai/pipeline/RendererService.ts
  - src/core/ai/router/ProviderRouter.ts
  - src/core/ai/cache/PromptCacheManager.ts
  - src/core/memory/MemoryEngine.ts
  - src/core/storage/WriteJournal.ts
  - src/core/stores/diagnosticsStore.ts
autonomous: true
requirements:
  - TELE-01
  - TELE-02
  - TELE-03
  - TELE-04
  - TELE-07

must_haves:
  truths:
    - "AgentOrchestrator.runWithContext() creates an ExecutionContext with DefaultTraceCollector and wraps the pipeline in AITransactionLog.start()/complete()/fail() (D-01, D-02)"
    - "PlannerService.plan() emits trace events through execCtx.traceCollector.onPlannerCall() with promptHash, tokenBreakdown, contextTier, cacheStats (D-05)"
    - "ExecutorService.execute() emits onToolExecution() with toolName, permissionDecision, status, durationMs (D-05)"
    - "ProviderRouter.selectModel() emits onProviderAttempt() per retry/fallback with attemptNumber, providerId, outcome, durationMs (D-04, D-05, D-23)"
    - "diagnosticsStore exists with filter state, mode toggles, transaction list, and traceTree — persisted via Zustand persist with chrome.storage.local adapter"
  artifacts:
    - src/core/stores/diagnosticsStore.ts
  key_links:
    - "PlannerService/RendererService accept optional ExecutionContext parameter (backward compatible — existing callers without execCtx skip tracing)"
    - "ExecutorService accept optional ExecutionContext for ToolTrace emission"
    - "ProviderRouter accepts ExecutionContext for ProviderAttempt recording"
---

<objective>
Wire the AITransactionLog lifecycle into AgentOrchestrator.runWithContext() — creating ExecutionContext with DefaultTraceCollector, starting the transaction, passing execCtx through all pipeline services, and closing the transaction on complete/fail. Add trace event emission to PlannerService, ExecutorService, RendererService, ProviderRouter, MemoryEngine, PromptCacheManager, and WriteJournal. Create the diagnosticsStore for UI filter/mode state.

Purpose: Close the loop — every AI operation from Planner through Renderer generates typed trace events that flow through TraceCollector → AITransactionLog → TraceRedactor → IndexedDB.
Output: Modified AgentOrchestrator (ExecutionContext lifecycle), modified 6 pipeline/support services (trace emission), new diagnosticsStore.ts
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-transaction-logging-and-diagnostics/06-CONTEXT.md (D-01, D-02, D-05, D-07, D-21, D-22, D-23)
@.planning/phases/06-transaction-logging-and-diagnostics/06-RESEARCH.md (lines 395-428, ExecutionContext code; lines 1061-1078, integration touchpoints table)
@.planning/phases/06-transaction-logging-and-diagnostics/06-PATTERNS.md (AgentOrchestrator, PlannerService, etc. sections)
@src/core/ai/pipeline/AgentOrchestrator.ts (runWithContext at line 79, constructor DI at line 34)
@src/core/ai/pipeline/PlannerService.ts (plan method signature)
@src/core/ai/pipeline/ExecutorService.ts (execute with permission check)
@src/core/ai/pipeline/RendererService.ts (render with streaming)
@src/core/ai/router/ProviderRouter.ts (selectModel retry chain)
@src/core/ai/cache/PromptCacheManager.ts (identifyStableSections, invalidateCacheKey)
@src/core/memory/MemoryEngine.ts (assemble, extract)
@src/core/storage/WriteJournal.ts (begin/markCompleted)
@src/core/telemetry/types.ts (ExecutionContext, TraceCollector, DefaultTraceCollector types)
@src/core/telemetry/AITransactionLog.ts (aiTransactionLog singleton, created 06-04)
@src/core/stores/workspaceStore.ts (Zustand store analog with chrome.storage.local persist)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate AITransactionLog lifecycle into AgentOrchestrator.runWithContext</name>
  <files>src/core/ai/pipeline/AgentOrchestrator.ts</files>
  <read_first>
@src/core/ai/pipeline/AgentOrchestrator.ts (lines 79-142, runWithContext method)
@src/core/telemetry/types.ts (ExecutionContext, DefaultTraceCollector, TraceVerbosity)
@src/core/telemetry/AITransactionLog.ts (aiTransactionLog singleton)
@.planning/phases/06-transaction-logging-and-diagnostics/06-RESEARCH.md (lines 395-428, ExecutionContext creation code example)
  </read_first>
  <action>
Modify AgentOrchestrator.runWithContext() at line 79 to add AITransactionLog lifecycle:

1. Add imports: ExecutionContext, DefaultTraceCollector, TraceVerbosity from ../../telemetry/types; aiTransactionLog from ../../telemetry/AITransactionLog

2. After operationId extraction (line 83), create ExecutionContext:
   - traceCollector = new DefaultTraceCollector()
   - operationId = optimizedContext.provenance.operationId
   - abortSignal = abortManager.signal (already created at line 83)
   - verbosity = TraceVerbosity.NORMAL (hardcoded for now; diagnostic mode toggle will be wired via diagnosticsStore in Phase 7)
   - privacyMode = false (hardcoded; will be wired via diagnosticsStore in Phase 7)

3. Call await aiTransactionLog.start(operationId, execCtx) before the try block

4. In the try block, pass execCtx to executePlannerLoop and executeRenderer calls. Update private method signatures to accept optional ExecutionContext parameter.

5. In the try block success path (after renderer completes), call await aiTransactionLog.complete(operationId, execCtx.traceCollector)

6. In each catch block (ContextTooLargeError at line 108, AbortError at line 120, generic at line 123), call await aiTransactionLog.fail(operationId, err, execCtx.traceCollector) before yielding the error event

7. In the finally block, keep existing memory extraction (lines 131-141) unchanged

Update executePlannerLoop signature: private async *executePlannerLoop(..., execCtx?: ExecutionContext)
Update executeRenderer signature: private async *executeRenderer(..., execCtx?: ExecutionContext)

Pass execCtx to this.planner.plan(...), this.executor.execute(...), this.renderer.render(...), and this.router.selectModel(...) calls within these methods. Use optional chaining or conditional passing — existing callers without execCtx (run() method at line 42) skip tracing.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/core/ai/pipeline/AgentOrchestrator.ts && npx vitest run --run</automated>
  </verify>
  <acceptance_criteria>
    - AgentOrchestrator.runWithContext creates ExecutionContext with DefaultTraceCollector
    - aiTransactionLog.start() called before pipeline execution
    - aiTransactionLog.complete() called on success path
    - aiTransactionLog.fail() called in all 3 error catch blocks
    - execCtx passed through to executePlannerLoop and executeRenderer
    - Backward compatible: run() method (no execCtx) still works unchanged
    - npx tsc --noEmit passes
    - All existing tests pass unaffected
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Emit trace events from pipeline and support services</name>
  <files>src/core/ai/pipeline/PlannerService.ts, src/core/ai/pipeline/ExecutorService.ts, src/core/ai/pipeline/RendererService.ts, src/core/ai/router/ProviderRouter.ts, src/core/ai/cache/PromptCacheManager.ts, src/core/memory/MemoryEngine.ts, src/core/storage/WriteJournal.ts</files>
  <read_first>
@src/core/ai/pipeline/PlannerService.ts (plan method: prompt assembly, generateText call)
@src/core/ai/pipeline/ExecutorService.ts (execute: permission check at line 25, tool execution at line 40)
@src/core/ai/pipeline/RendererService.ts (render: streaming loop, text-complete)
@src/core/ai/router/ProviderRouter.ts (selectModel: retry/fallback chain)
@src/core/ai/cache/PromptCacheManager.ts (identifyStableSections, invalidateCacheKey)
@src/core/memory/MemoryEngine.ts (assemble, extract)
@src/core/storage/WriteJournal.ts (begin, markCompleted)
@src/core/telemetry/types.ts (TraceCollector interface with on* method signatures)
@.planning/phases/06-transaction-logging-and-diagnostics/06-RESEARCH.md (lines 1062-1078, hook points table)
  </read_first>
  <action>
For each service, add optional ExecutionContext parameter and emit trace events through execCtx.traceCollector when available. Use crypto.randomUUID() for trace IDs. All emission is synchronous — push to TraceCollector (D-35). Existing methods continue working without execCtx (backward compatible).

**PlannerService.plan():**
Add execCtx?: ExecutionContext to plan() parameter list. Before generateText: emit onPlannerCall with promptHash (use DJB2 from PromptCacheManager or compute inline using existing simpleHash), tokenBreakdown (use TokenEstimator if available, else rough estimate), contextTier from provided tier, cacheStats (sectionsMarked count, estimatedSavings 0), truncated: false, source: 'planner', timestamp: Date.now(). After response: if execCtx present, update trace with actual token counts from response.usage.

**ExecutorService.execute():**
Add execCtx?: ExecutionContext parameter. After permission check (existing line 25): emit onToolExecution with toolName, source: 'built-in' (hardcoded for v0.1 — MCP/skill sources in Phase 8), dangerous: false (hardcoded — Phase 7 hooks into permission registry), permissionDecision, status: 'success' | 'failed', durationMs (computed from start/end timestamps), timestamp.

**RendererService.render():**
Add execCtx?: ExecutionContext parameter. On text-complete event (when stream ends): emit onRendererCall with same shape as PromptTrace but source: 'renderer', promptHash, tokenBreakdown from response.usage, timestamp.

**ProviderRouter.selectModel():**
Add execCtx?: ExecutionContext parameter. Inside retry loop: for each attempt, emit onProviderAttempt with attemptNumber (loop counter), providerId, model, startedAt/endedAt timestamps, durationMs, outcome ('success'|'timeout'|'error'|'circuit_open'), errorCode if failed, circuitBreakerTriggered (from circuit breaker state). Record all attempts in order.

**PromptCacheManager:**
In identifyStableSections(): if execCtx present, emit onCacheEvent with event: 'key_generated', sections found. In invalidateCacheKey(): emit onCacheEvent with event: 'invalidation', providerId, cacheKey. Pattern: accept optional execCtx parameter.

**MemoryEngine:**
In assemble(): emit onMemoryEvent with phase: 'assemble', factsRetrieved count, conversationId. In extract(): emit onMemoryEvent with phase: 'extract', factsExtracted count, extractionAttempt, summarized. Pass execCtx through existing method calls from AgentOrchestrator.

**WriteJournal:**
In begin(): emit onWriteJournalEvent with journal entry details (journalId, operation, stepsCount, recovered: false). In markCompleted(): emit onWriteJournalEvent with status: 'completed'. Accept optional execCtx parameter.

IMPORTANT: All execCtx parameters are optional. Services check `if (execCtx?.traceCollector)` before emitting. Existing callers without execCtx (e.g., AgentOrchestrator.run() method, tests) skip tracing entirely — no behavior change.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --run</automated>
  </verify>
  <acceptance_criteria>
    - All 7 services accept optional execCtx?: ExecutionContext parameter
    - Trace events emitted through execCtx.traceCollector.on*() when execCtx provided
    - All existing callers without execCtx work unchanged (backward compatible)
    - PlannerService emits onPlannerCall with promptHash and tokenBreakdown
    - ExecutorService emits onToolExecution with permissionDecision and outcome
    - RendererService emits onRendererCall on text-complete
    - ProviderRouter emits onProviderAttempt per retry/fallback
    - Cache/Memory/WriteJournal emit their respective events
    - All events use crypto.randomUUID() for id fields
    - npx tsc --noEmit passes
    - All existing tests pass (npx vitest run exits 0)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create diagnosticsStore for UI filter and mode state</name>
  <files>src/core/stores/diagnosticsStore.ts</files>
  <read_first>
@src/core/stores/workspaceStore.ts (Zustand persist pattern with chrome.storage.local)
@.planning/phases/06-transaction-logging-and-diagnostics/06-RESEARCH.md (lines 1004-1038, diagnosticsStore interface and mode persistence)
@src/core/telemetry/types.ts (TransactionType, TransactionStatus, Severity, TraceTree types)
  </read_first>
  <action>
Create diagnosticsStore.ts using Zustand v5 create() pattern following workspaceStore.ts analog. State shape:

interface DiagnosticsState {
  // Filter state (D-13)
  filterType: TransactionType | undefined;
  filterStatus: TransactionStatus | undefined;
  filterProvider: string | undefined;
  filterSeverity: Severity | undefined;
  filterDateRange: [number, number] | undefined;
  searchQuery: string;
  // Selection
  selectedOperationId: string | undefined;
  // Modes (D-38, D-39)
  diagnosticMode: boolean;
  privacyMode: boolean;
  // Data
  transactions: AITransaction[];
  traceTree: TraceTree | undefined;
  loading: boolean;
  // Actions
  setFilter: (key: string, value: unknown) => void;
  selectTransaction: (operationId: string) => Promise<void>;
  setDiagnosticMode: (enabled: boolean) => void;
  setPrivacyMode: (enabled: boolean) => void;
  refreshTransactions: () => Promise<void>;
  clearFilters: () => void;
}

Use Zustand persist middleware with chrome.storage.local storage for np_diagnostics_mode and np_privacy_mode keys. The filter/search/selection state is UI-ephemeral (not persisted). Mode toggles persist across sessions.

Import aiTransactionLogDB from ../storage/stores/AITransactionLogDB for selectTransaction (calls getTraceTree) and refreshTransactions (calls queryTransactions). Keep these async actions within the store.

selectTransaction sets selectedOperationId, sets loading=true, calls aiTransactionLogDB.getTraceTree(operationId), sets traceTree, sets loading=false.

refreshTransactions builds query from filter state, calls aiTransactionLogDB.queryTransactions(filters), sets transactions array.

setDiagnosticMode and setPrivacyMode call Zustand set() and persist to chrome.storage.local via the persist middleware partialize option.

Export: export const useDiagnosticsStore = create<DiagnosticsState>()(...);
  </action>
  <verify>
    <automated>npx tsc --noEmit src/core/stores/diagnosticsStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/core/stores/diagnosticsStore.ts exists with Zustand create() store
    - All filter fields (type, status, provider, severity, dateRange, searchQuery) present
    - diagnosticMode and privacyMode persisted via chrome.storage.local
    - selectTransaction calls getTraceTree and sets traceTree state
    - refreshTransactions calls queryTransactions with current filters
    - npx tsc --noEmit passes
    - Import pattern matches existing stores (workspaceStore, providerStore)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Pipeline services emitting raw data to TraceCollector | high | mitigate | Services emit metadata (hash, counts, timing) not raw content in NORMAL mode. Raw content fields (prompt bodies, tool I/O) populated only when verbosity=DIAGNOSTIC, and even then pass through TraceRedactor at close() time |
</threat_model>

<verification>
## Quick Verify
npx tsc --noEmit && npx vitest run --run

## AgentOrchestrator Integration Verify
npx vitest run tests/core/ai/pipeline/AgentOrchestrator.test.ts --run
</verification>

<success_criteria>
- [ ] AgentOrchestrator.runWithContext wraps pipeline with AITransactionLog lifecycle
- [ ] ExecutionContext created with DefaultTraceCollector per D-02
- [ ] All 7 services emit typed trace events through TraceCollector (D-05)
- [ ] Backward compatible: services work without execCtx (optional parameter)
- [ ] diagnosticsStore created with filter state, mode toggles, and async actions
- [ ] diagnosticMode/privacyMode persisted to chrome.storage.local
- [ ] All existing tests pass (npx vitest run exits 0)
- [ ] TypeScript compiles cleanly (npx tsc --noEmit)
</success_criteria>

<output>
Create .planning/phases/06-transaction-logging-and-diagnostics/06-06-SUMMARY.md when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| useDiagnosticsStore | Zustand hook | src/core/stores/diagnosticsStore.ts |
| DiagnosticsState | interface | src/core/stores/diagnosticsStore.ts |
| execCtx parameter (7 services) | optional parameter | 7 pipeline/support files |
| trace event emission (7 on* calls) | method calls | 7 pipeline/support files |
