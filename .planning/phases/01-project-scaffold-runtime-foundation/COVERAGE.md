No external API integration: Phase 1 exclusively uses built-in Chrome browser APIs (chrome.storage.local, BroadcastChannel, chrome.tabs, chrome.runtime, chrome.sidePanel, window.matchMedia). These are platform-native browser capabilities available to any extension page sharing the `chrome-extension://<id>` origin — not external third-party services requiring API keys, rate limits, or capability-gap decisions.
