---
phase: 05-knowledge-base
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/memory/MemoryRecord.ts
  - src/core/memory/MemoryScorer.ts
  - src/core/memory/ConversationMemoryStore.ts
  - src/core/memory/UserMemoryStore.ts
  - src/core/memory/PreferenceMemoryStore.ts
  - src/core/memory/MemoryEngine.ts
  - src/core/memory/types.ts
  - tests/core/memory/MemoryScorer.test.ts
  - tests/core/memory/UserMemoryStore.test.ts
  - tests/core/memory/ConversationMemoryStore.test.ts
  - tests/core/memory/MemoryEngine.test.ts
autonomous: true
requirements:
  - MEM-01
  - MEM-02

must_haves:
  truths:
    - "MemoryScorer.scoreFact(fact, ['query','terms'], Date.now()) returns a number in [0,1] computed as keywordMatch*0.35 + tagMatch*0.25 + recency*0.20 + confidence*0.10 + useCount*0.10 per D-08"
    - "MemoryScorer.getTopFacts(facts, query, 'tiny') returns at most 3 facts; for 'small'/'medium'/'large' returns at most 5 facts — all with score ≥ 0.30 (D-09)"
    - "ConversationMemoryStore.getContext(conversationId, tier) returns { summary: ConversationSummary|null, recentMessages: Message[] } — recent message count: 4 for tiny, 8 for small, 12 for medium/large"
    - "ConversationMemoryStore.appendMessage() triggers compactConversation() when messageCount % 12 === 0 after append (D-10)"
    - "UserMemoryStore.upsert(fact) validates via Zod, sets confidence from source per D-07 mapping (explicit-user=1.0, verified-state=0.8, previous-explicit=0.7, inferred=0.5); confidence is never modified after creation"
    - "UserMemoryStore.incrementUseCount(id) increments useCount by 1 and updates lastUsedAt — confidence field untouched (D-07: immutable confidence)"
    - "PreferenceMemoryStore.get('np_persona') returns the stored persona configuration object or null if not set"
    - "MemoryEngine.retrieve({conversationId, query, tier}) returns ContextItem[] — conversation summary+turns first, then scored user facts, then preferences — each item has sourceId format 'memory.{store}.{type}.{id}' (D-18 Phase 4)"
    - "MemoryEngine.write(record) rejects with error when !isPrimarySurface() — secondary surfaces are read-only (MEM-02)"
    - "MemoryEngine.write(record) wraps in WriteJournal entry with operation matching record.memoryType and broadcasts WORKSPACE_UPDATED on commit"
    - "Retrieving memory twice with the same query and tier returns identical ContextItem[] — deterministic scoring produces identical results; writing same MemoryRecord.id twice overwrites (no duplicates)"
    - "MemoryEngine.retrieve() returns { facts: [], summary: null } when no memories of requested type exist — downstream renders appropriate empty state (UI-SPEC)"
    - "All memory write operations return discriminated union types ({ success: true } | { success: false, error: string }) — no thrown exceptions for operational errors"
    - "Memory writes on secondary surfaces are blocked before any IndexedDB mutation, verified: backstop"
    - "If the primary surface disconnects, the election is re-run and a surviving surface becomes primary — memory writes resume on the new primary without data loss, verified: backstop"
  key_links:
    - "MemoryEngine.write() must check isPrimarySurface() BEFORE calling WriteJournal.createEntry — early rejection prevents journal entries from being created on secondary surfaces"
    - "UserMemoryStore.upsert confidence assignment is the single D-07 trust-gate — wrong mapping here corrupts Phase 5b conflict resolution"
    - "MemoryEngine.retrieve() ContextItem[] must carry trust/sensitivity metadata per Phase 4b ContextTrustPolicy contract — items without trust assessment are rejected by ContextOptimizer"
---

<objective>
Memory Foundation + Engine: Build the complete memory subsystem — MemoryRecord schemas, MemoryScorer (D-08 weighted formula), three memory stores (Conversation/User/Preference), and MemoryEngine orchestrator with retrieval pipeline, tier-gating (D-09), single-writer enforcement (MEM-02), and ContextItem generation for the AI pipeline.

Purpose: Establish the persistent memory layer that feeds context into every AI turn. Conversation memory provides summaries + tier-gated recent turns. User memory provides scored, confidence-weighted facts. Preference memory stores persona config (np_persona). MemoryEngine orchestrates retrieval across all stores and enforces single-writer semantics.

Output: MemoryRecord (Zod schemas), MemoryScorer, ConversationMemoryStore, UserMemoryStore, PreferenceMemoryStore, MemoryEngine — all with passing vitest tests. Uses the v4 MemoryDB stores created by Plan 01's MigrationRunner migration.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-knowledge-base/05-CONTEXT.md
@.planning/phases/05-knowledge-base/05-RESEARCH.md
@.planning/phases/05-knowledge-base/05-UI-SPEC.md
@src/core/ai/types.ts
@src/core/ai/AgentTurnInput.ts
@src/core/context/ContextItem.ts
@src/core/storage/MigrationRunner.ts
@src/core/storage/WriteJournal.ts
@src/core/runtime/BroadcastBus.ts
@src/core/events/EventBus.ts
@.planning/phases/04b-trust-aware-context-receipts/04b-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemoryRecord schemas (Zod) + MemoryScorer (D-08 weighted formula) — pure compute layer</name>
  <files>src/core/memory/MemoryRecord.ts, src/core/memory/MemoryScorer.ts, src/core/memory/types.ts, tests/core/memory/MemoryScorer.test.ts</files>
  <read_first>
    - src/core/context/ContextItem.ts — ContextItemSchema shape (kind, text, tokens, stable, sourceId, relevance, freshness, trust, sensitivity, instructionAuthority), Sensitivity enum, InstructionAuthority enum
    - src/core/ai/types.ts — ContextItem interface, PromptSection interface
    - .planning/phases/05-knowledge-base/05-CONTEXT.md — D-04 (memoryType taxonomy: working|episodic|semantic|preference|procedural), D-07 (confidence source-based immutable: explicit-user=1.0, verified-state=0.8, previous-explicit=0.7, inferred=0.5), D-08 (retrieval scoring: keywordMatch 35% + tagMatch 25% + recency 20% + confidence 10% + useCount 10%), D-09 (tier-gated: top-3 tiny, top-5 others, min threshold 0.30), D-06 (no cross-store consolidation)
    - .planning/phases/05-knowledge-base/05-RESEARCH.md — §Retrieval Scoring Formula: WEIGHTS constant, MIN_SCORE=0.30, USE_COUNT_CAP=20, keywordMatch as proportion of query terms in content, tagMatch as proportion of fact tags matched, recency linear decay over 30 days, useCount capped at 20, tokenizeQuery helper, scoreFact signature; §Memory Architecture: MemoryRecordSchema with id/uuid, content min(1), memoryType enum, tags, confidence 0-1, source enum, useCount nonnegative int, sensitivity public|private|confidential, createdAt/updatedAt timestamps, lastUsedAt/verifiedAt optional
    - .planning/phases/05-knowledge-base/05-UI-SPEC.md — MemoryRecord interface (id, memoryType, source, confidence, content, createdAt, updatedAt, useCount, sensitivity, tags), ConversationSummary interface, RetrievedMemory interface (record, retrievalScore, relevanceReasons), Retrieval Contract (tier-gated counts, min score threshold, scoring formula, immutable confidence)
    - .planning/phases/04b-trust-aware-context-receipts/04b-CONTEXT.md — D-06 (ContextTrustPolicy split ownership: MemoryEngine computes relevance/freshness), D-08 (relevance is query-aware, recomputed per turn), D-09 (sensitivity inheritance from MemoryRecord.sensitivity)
  </read_first>
  <behavior>
    - Test 1: scoreFact(fact, ['keyword','match'], now) where fact.content='keyword match test' and fact.tags=['keyword'] returns score where keywordScore=1.0, tagScore=1.0, confidence=0.8, recency≈1.0, useCountScore=0.0
    - Test 2: scoreFact with empty query terms returns keywordScore=0, tagScore=0 — composite score driven only by recency+confidence+useCount
    - Test 3: recencyScore for fact updatedAt=now is 1.0; for fact updatedAt=now-15days is ~0.5; for fact updatedAt=now-31days is 0
    - Test 4: useCountScore for useCount=0 is 0; for useCount=10 is 0.5; for useCount=20 is 1.0; for useCount=999 is 1.0 (capped)
    - Test 5: getTopFacts with 10 scored facts for tier='tiny' returns at most 3; for tier='small' returns at most 5 — all with score ≥ 0.30
    - Test 6: MemoryRecordSchema.parse rejects missing id, empty content, invalid memoryType, confidence outside [0,1]
    - Test 7: ConfidenceSourceSchema maps 'explicit-user'→1.0, 'verified-state'→0.8, 'previous-explicit'→0.7, 'inferred'→0.5 — unknown source rejected by Zod
  </behavior>
  <action>
    Per D-04/D-07/D-08/D-09 — create the pure-compute foundation for memory, no IndexedDB, no side effects.

    **A. src/core/memory/MemoryRecord.ts (Zod schemas):**
    Export `MemoryType` type = 'working'|'episodic'|'semantic'|'preference'|'procedural'.
    Export `MemoryTypeSchema` = z.enum([...]).
    Export `ConfidenceSource` type = 'explicit-user'|'verified-state'|'previous-explicit'|'inferred'.
    Export `ConfidenceSourceSchema` = z.enum([...]).
    Export `CONFIDENCE_MAP: Record<ConfidenceSource, number>` = { 'explicit-user': 1.0, 'verified-state': 0.8, 'previous-explicit': 0.7, 'inferred': 0.5 }.
    Export `MemoryRecordSchema` = z.object({ id: z.string().uuid(), content: z.string().min(1), memoryType: MemoryTypeSchema, tags: z.array(z.string()), confidence: z.number().min(0).max(1), source: ConfidenceSourceSchema, useCount: z.number().int().nonnegative().default(0), sensitivity: z.enum(['public','private','confidential']), createdAt: z.number(), updatedAt: z.number(), lastUsedAt: z.number().optional(), verifiedAt: z.number().optional() }).
    Export `MemoryRecord` type = z.infer<typeof MemoryRecordSchema>.
    Export `UserMemoryFact` type = MemoryRecord & { memoryType: 'semantic' } — user facts are always semantic by taxonomy (D-04). Export `UserMemoryFactSchema` = MemoryRecordSchema.extend({ memoryType: z.literal('semantic') }).
    Export `ConversationSummarySchema` = z.object({ id: z.string().uuid(), conversationId: z.string(), summary: z.string().min(1), messageRange: z.object({ start: z.number().int().nonnegative(), end: z.number().int().nonnegative() }), createdAt: z.number() }).
    Export `ConversationSummary` type.
    Export `PreferenceRecordSchema` = z.object({ key: z.string().min(1), value: z.unknown(), updatedAt: z.number() }).
    Export `PreferenceRecord` type.
    Export `RetrievedMemorySchema` = z.object({ record: MemoryRecordSchema, retrievalScore: z.number().min(0).max(1), relevanceReasons: z.array(z.string()) }).
    Export `RetrievedMemory` type.
    Export `ConversationContextSchema` = z.object({ summary: ConversationSummarySchema.nullable(), recentMessages: z.array(z.object({ role: z.enum(['user','assistant','tool']), content: z.string(), timestamp: z.number() })) }).
    Export `ConversationContext` type.

    **B. src/core/memory/types.ts (discriminated unions):**
    Export `MemoryRetrievalResult` = { success:true, items:ContextItem[] } | { success:false, error:string, code:'STORE_ERROR'|'SCORING_ERROR' }.
    Export `MemoryWriteResult` = { success:true, recordId:string } | { success:false, error:string, code:'VALIDATION_ERROR'|'NOT_PRIMARY_SURFACE'|'DB_ERROR'|'JOURNAL_ERROR' }.
    Export `RetrievalOptions` interface = { conversationId:string, query:string, tier:'tiny'|'small'|'medium'|'large' }.
    Export `MemoryStoreWriteOp` type = 'update-user-memory'|'write-preference'|'compact-conversation' (matching WriteJournalOperation union from Phase 2).

    **C. src/core/memory/MemoryScorer.ts (pure functions):**
    Export `WEIGHTS` const = { keyword:0.35, tag:0.25, recency:0.20, confidence:0.10, useCount:0.10 }.
    Export `MIN_SCORE` = 0.30.
    Export `USE_COUNT_CAP` = 20.
    Export `TIER_LIMITS: Record<string, number>` = { tiny:3, small:5, medium:5, large:5 }.
    Export `tokenizeQuery(query:string):string[]` — lowercase, split on whitespace, filter empty strings, minimum 2 chars per term.
    Export `scoreFact(fact:UserMemoryFact, queryTerms:string[], now?:number):number` — implements D-08 formula: keywordScore = matchedQueryTerms.length / queryTerms.length (0 if empty), tagScore = matchedTags.length / fact.tags.length (0 if empty), recencyScore = max(0, min(1, 1 - (now - fact.updatedAt) / 30_DAYS_MS)), useCountScore = min(1, fact.useCount / USE_COUNT_CAP), confidenceScore = fact.confidence. Returns weighted composite. All sub-scores normalized [0,1].
    Export `getTopFacts(facts:UserMemoryFact[], query:string, tier:string):RetrievedMemory[]` — scores all facts via scoreFact, filters score < MIN_SCORE, sorts descending, slices to TIER_LIMITS[tier], maps to RetrievedMemory with relevanceReasons.
    These are pure functions with no side effects.

    **D. tests/core/memory/MemoryScorer.test.ts:**
    Covers: all 5 sub-score computations (keyword, tag, recency, useCount, confidence) independently; composite weight verification (e.g., changing confidence from 0.5 to 1.0 changes overall by exactly 0.05); edge cases (empty query terms, empty tags, score floor at 0.30 threshold, useCount beyond cap, recency beyond 30 days); tier-gating (tiny returns ≤3, small returns ≤5); deterministic output (same input same output); MemoryRecordSchema validation (valid, missing id, empty content, invalid memoryType, confidence out of range).

    Commit after tests pass: `feat(05-02): MemoryRecord schemas + MemoryScorer — D-07 confidence mapping, D-08 weighted scoring, D-09 tier-gating`
  </action>
  <verify>
    <automated>npx vitest run tests/core/memory/MemoryScorer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All MemoryScorer sub-score computations produce expected values for known inputs
    - scoreFact with all-max inputs (keyword=1, tag=1, recency=1, useCount=1, confidence=1) returns exactly 1.0
    - scoreFact with all-zero inputs returns 0.0
    - getTopFacts for 'tiny' tier returns at most 3 facts, all with score ≥ 0.30
    - MemoryRecordSchema rejects records with invalid memoryType, confidence outside [0,1], empty content
    - CONFIDENCE_MAP['explicit-user'] === 1.0, ['inferred'] === 0.5
  </acceptance_criteria>
  <reversibility rating="one-way">D-07 confidence mapping is the foundation for Phase 5b conflict resolution — changing the scale or immutability invalidates existing conflict decisions</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConversationMemoryStore + UserMemoryStore + PreferenceMemoryStore — persistent stores with IndexedDB</name>
  <files>src/core/memory/ConversationMemoryStore.ts, src/core/memory/UserMemoryStore.ts, src/core/memory/PreferenceMemoryStore.ts, tests/core/memory/UserMemoryStore.test.ts, tests/core/memory/ConversationMemoryStore.test.ts</files>
  <read_first>
    - src/core/memory/MemoryRecord.ts (Task 1) — MemoryRecordSchema, UserMemoryFactSchema, UserMemoryFact type, ConversationSummarySchema, PreferenceRecordSchema, CONFIDENCE_MAP
    - src/core/memory/MemoryScorer.ts (Task 1) — scoreFact, getTopFacts, WEIGHTS, MIN_SCORE, TIER_LIMITS, tokenizeQuery
    - src/core/storage/MigrationRunner.ts — Plan 01 created v4 migration with memory_messages (compound key [conversationId, seq]), user_facts (keyPath id, indexes by-tag multiEntry, by-confidence), conversation_summaries (keyPath conversationId) object stores
    - src/core/storage/WriteJournal.ts — createEntry/commitEntry/rollbackEntry, WriteJournalOperation union (already includes 'update-user-memory', 'compact-conversation')
    - .planning/phases/05-knowledge-base/05-CONTEXT.md — D-05 (AI writes limited to conversation summaries only; user facts via explicit user action; preferences via explicit settings), D-06 (no automatic cross-store consolidation), D-10 (LLM summary at 12-msg boundary, 2-3 sentences, context assembly head+summary+tail), D-11 (LRU eviction: max 10 active, max 100 archived, archive after 30 min idle), D-04 (memoryType field)
    - .planning/phases/05-knowledge-base/05-RESEARCH.md — §Conversation Memory Implementation: compaction trigger messageCount % 12 === 0, context assembly formula (HEAD: system+first 2 key messages, SUMMARY: LLM 2-3 sentence, TAIL: tier-gated last N), summary prompt template; §IndexedDB Schema Design: memory_messages compound key, user_facts schema, conversation_summaries schema; §Architecture Patterns: MemoryEngine retrieval pipeline order (conversation → user facts → preferences)
    - .planning/phases/05-knowledge-base/05-UI-SPEC.md — ConversationSummary interface (id, conversationId, summary, messageRange, createdAt), RetrievedMemory (record, retrievalScore, relevanceReasons)
  </read_first>
  <behavior>
    - Test 1: ConversationMemoryStore.appendMessage() increments message count; at count 12 triggers compact flag; at count 13 does not re-trigger
    - Test 2: ConversationMemoryStore.getContext(cId, 'tiny') returns summary (if exists) + 4 recent messages; 'medium' returns 12 recent messages
    - Test 3: UserMemoryStore.upsert(fact) with source='explicit-user' assigns confidence=1.0; with source='inferred' assigns confidence=0.5
    - Test 4: UserMemoryStore.incrementUseCount(id) increments useCount by 1 and updates lastUsedAt — confidence field unchanged
    - Test 5: UserMemoryStore.findByTag('preferences') returns facts with that tag; getAll() returns all facts
    - Test 6: PreferenceMemoryStore.set('np_persona', {name:'Test', tone:'casual'}) persists; get('np_persona') returns stored value; get('nonexistent') returns null
    - Test 7: MemoryRecords written to UserMemoryStore pass Zod validation at boundary; invalid records rejected with VALIDATION_ERROR
  </behavior>
  <action>
    Per D-05/D-06/D-10/D-11 — implement the three persistent memory stores. Each store is a class with its own IndexedDB access to the NotesDB (which Plan 01 renamed/migrated to v4 with memory stores). Use the same idb wrapper pattern as WriteJournal.

    **A. src/core/memory/ConversationMemoryStore.ts:**
    Export `ConversationMemoryStore` class. Not a singleton — instantiated by MemoryEngine.
    Constructor takes no args. Uses `openDB('NotesDB', 4)` internally for IndexedDB access. Methods:
    - `getContext(conversationId:string, tier:string):Promise<ConversationContext>` — reads summary from 'conversation_summaries' store, reads recent messages from 'memory_messages' store via index on conversationId, returns {summary, recentMessages}. Recent message count per D-10 tail: tiny=4, small=8, medium/large=12. If no summary exists, summary is null.
    - `appendMessage(conversationId:string, message:{role:'user'|'assistant'|'tool', content:string, timestamp:number}):Promise<void>` — auto-increments seq (getAll from memory_messages for this conversationId, count = next seq), puts to memory_messages store. After put, calls `getMessageCount(conversationId)` — if count % 12 === 0, returns { shouldCompact: true, messageCount: count }. Does NOT invoke LLM summarization — returns the signal; MemoryEngine invokes summarization.
    - `getMessageCount(conversationId:string):Promise<number>` — counts messages for conversation via index.
    - `saveSummary(summary:ConversationSummary):Promise<void>` — puts to conversation_summaries store.
    - `getSummaries(conversationId:string):Promise<ConversationSummary[]>` — gets all summaries for conversation (currently one, but array for future multiple summary ranges).
    - `evictConversation(conversationId:string):Promise<void>` — removes all messages and summaries for this conversation. Used for LRU eviction (D-11).

    **B. src/core/memory/UserMemoryStore.ts:**
    Export `UserMemoryStore` class. Not a singleton — instantiated by MemoryEngine.
    - `upsert(fact:Omit<UserMemoryFact, 'id'|'confidence'|'createdAt'|'updatedAt'|'useCount'> & {id?:string, source:ConfidenceSource}):Promise<MemoryWriteResult>` — generates id if missing via crypto.randomUUID(), sets confidence from CONFIDENCE_MAP[source], sets createdAt/updatedAt to Date.now(), validates via UserMemoryFactSchema.parse, puts to 'user_facts' store via idb. If id exists, update instead (version counter pattern). Returns {success:true, recordId} on success.
    - `getAll():Promise<UserMemoryFact[]>` — getAll from 'user_facts' store.
    - `get(id:string):Promise<UserMemoryFact|null>` — get by key.
    - `remove(id:string):Promise<void>` — delete from store.
    - `incrementUseCount(id:string):Promise<void>` — read fact, increment useCount by 1, set lastUsedAt to Date.now(), put back. Confidence field MUST NOT be modified (D-07). Throws if id not found.
    - `findByTag(tag:string):Promise<UserMemoryFact[]>` — uses 'by-tag' index via idb.

    **C. src/core/memory/PreferenceMemoryStore.ts:**
    Export `PreferenceMemoryStore` class. Not a singleton — instantiated by MemoryEngine.
    - `get(key:string):Promise<unknown|null>` — reads from 'user_facts' store where memoryType='preference' and content contains the key. Actually, preferences are stored in a separate mechanism. Per spec: preferences are stored as MemoryRecords with memoryType='preference' in the same 'user_facts' store. Use `getAll()` filtered by memoryType='preference', then map to key→value pairs. `get(key)` returns the value for the specific preference key, or null.
    - `set(key:string, value:unknown):Promise<MemoryWriteResult>` — upserts a preference record with memoryType='preference', content=JSON.stringify({key, value}), source='explicit-user', confidence=1.0. Tags=['preference', key].
    - `getAll():Promise<Record<string, unknown>>` — returns all preferences as {[key]:value}.
    - `getPersona():Promise<Record<string, unknown>|null>` — convenience: `get('np_persona')`.

    **D. tests/core/memory/UserMemoryStore.test.ts:**
    Covers: upsert with source→confidence mapping (all 4 sources), upsert generates UUID if no id, upsert updates existing record, incrementUseCount increases by 1 and updates lastUsedAt, incrementUseCount does NOT modify confidence, findByTag returns matches, getAll returns all, remove deletes, schema validation rejects invalid records.

    **E. tests/core/memory/ConversationMemoryStore.test.ts:**
    Covers: appendMessage increments seq, message count tracking, compact signal at count 12 (no signal at 11 or 13), getContext returns correct tier-gated recent message count (tiny=4, small=8, medium=12, large=12), getContext returns summary when one exists, saveSummary+getContext round-trip, evictConversation removes all messages+summaries.

    No LLM summarization call in this task — that's Plan 03. The compact signal is just a boolean return.
  </action>
  <verify>
    <automated>npx vitest run tests/core/memory/UserMemoryStore.test.ts tests/core/memory/ConversationMemoryStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/core/memory/UserMemoryStore.test.ts` passes — all 4 confidence sources mapped correctly, useCount increment leaves confidence untouched
    - `npx vitest run tests/core/memory/ConversationMemoryStore.test.ts` passes — correct tier-gated message counts, compact signal at boundary 12
    - UserMemoryStore.upsert with source='inferred' produces record with confidence=0.5
    - ConversationMemoryStore.getContext for 'tiny' tier returns at most 4 recent messages
    - PreferenceMemoryStore.get('np_persona') returns stored config or null
  </acceptance_criteria>
  <reversibility rating="one-way">D-05 AI write boundary (conversation summaries only) and D-07 immutable confidence are trust contracts — relaxing them requires re-auditing memory integrity</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MemoryEngine orchestrator — retrieval pipeline, tier-gating, single-writer enforcement, ContextItem generation</name>
  <files>src/core/memory/MemoryEngine.ts, tests/core/memory/MemoryEngine.test.ts</files>
  <read_first>
    - src/core/memory/MemoryRecord.ts (Task 1) — all schemas and types, CONFIDENCE_MAP
    - src/core/memory/MemoryScorer.ts (Task 1) — scoreFact, getTopFacts, tokenizeQuery, WEIGHTS, MIN_SCORE, TIER_LIMITS
    - src/core/memory/ConversationMemoryStore.ts (Task 2) — ConversationMemoryStore class, getContext, appendMessage, saveSummary, message counting
    - src/core/memory/UserMemoryStore.ts (Task 2) — UserMemoryStore class, getAll, upsert, remove, incrementUseCount, findByTag
    - src/core/memory/PreferenceMemoryStore.ts (Task 2) — PreferenceMemoryStore class, get, set, getAll, getPersona
    - src/core/memory/types.ts (Task 1) — MemoryRetrievalResult, MemoryWriteResult, RetrievalOptions, MemoryStoreWriteOp
    - src/core/runtime/BroadcastBus.ts — subscribe/publish, BroadcastChannelEntry, PRECEDENT: current surface detection pattern
    - src/core/storage/WriteJournal.ts — createEntry/commitEntry/rollbackEntry, WriteJournalOperation, StoredStep
    - src/core/context/ContextItem.ts — ContextItemSchema, ContextItem type (relevance, freshness, trust, sensitivity, instructionAuthority, kind, text, tokens, stable, sourceId), Sensitivity type, InstructionAuthority type
    - .planning/phases/05-knowledge-base/05-CONTEXT.md — D-04 (taxonomy on all records), D-05 (AI writes limited to summaries), D-06 (no cross-store consolidation), D-09 (tier-gating with MemoryEngine owning scoring/threshold/top-K), D-18 Phase 4 (sourceId format: memory.user.fact.{uuid}, memory.conversation.summary.{id}, memory.preference.{key})
    - .planning/phases/04b-trust-aware-context-receipts/04b-CONTEXT.md — D-06 (ContextTrustPolicy split: MemoryEngine computes relevance/freshness), D-09 (sensitivity inheritance from MemoryRecord.sensitivity), MemoryEngine produces ContextItem[]
  </read_first>
  <behavior>
    - Test 1: MemoryEngine.retrieve({conversationId, query, tier:'small'}) returns ContextItem[] with items from conversation store (summary+turns), user store (scored facts), and preference store — in that order
    - Test 2: MemoryEngine.retrieve for tier='tiny' returns at most 3 user facts; tier='small' returns at most 5
    - Test 3: ContextItems have sourceId format 'memory.conversation.summary.{id}', 'memory.user.fact.{uuid}', 'memory.preference.{key}'
    - Test 4: ContextItems have kind='memory', trust≥0, sensitivity from MemoryRecord.sensitivity
    - Test 5: MemoryEngine.write(record) succeeds when isPrimarySurface() returns true; rejects with 'NOT_PRIMARY_SURFACE' error when false
    - Test 6: MemoryEngine.write wraps in WriteJournal entry with operation matching record.memoryType and broadcasts WORKSPACE_UPDATED on commit
    - Test 7: MemoryEngine.write for memoryType='semantic' with source='inferred' assigns confidence=0.5
    - Test 8: MemoryEngine.getPreferences() returns Record<string,unknown> from PreferenceMemoryStore
    - Test 9: MemoryEngine.retrieve() with no conversation summary returns summary=null in conversation context — downstream handles empty state
  </behavior>
  <action>
    Per D-04/D-05/D-06/D-09 — implement MemoryEngine as module-level singleton (pattern: ContextOptimizer, PromptCacheManager). MemoryEngine is the single entry point for all memory operations.

    **File: src/core/memory/MemoryEngine.ts**

    Export `MemoryEngine` class with private constructor. Module-scoped `_instance` variable. Export `getMemoryEngine():MemoryEngine` and `resetMemoryEngine():void` (for test isolation).

    Constructor instantiates: ConversationMemoryStore, UserMemoryStore, PreferenceMemoryStore.

    **Public methods:**

    `async retrieve(options:RetrievalOptions):Promise<MemoryRetrievalResult>` — D-06: queries all stores independently, combines results, no cross-store consolidation:
    1. Get conversation context: `conversationStore.getContext(options.conversationId, options.tier)` → wrap as ContextItem[] with kind='memory', sourceId='memory.conversation.summary.{summaryId}' for summary, 'memory.conversation.turn.{n}' for recent messages. sensitivity='public' for summary, 'private' for message content. trust=0.9, relevance=1.0 (always included for context continuity).
    2. Get user facts: `userStore.getAll()` → score via `getTopFacts(facts, options.query, options.tier)` → wrap as ContextItem[] with sourceId='memory.user.fact.{id}', sensitivity from record.sensitivity, trust=record.confidence, relevance=retrievalScore, freshness computed from updatedAt (same 30-day linear decay as MemoryScorer).
    3. Get preferences: `preferenceStore.getAll()` → compact JSON representation → single ContextItem with kind='memory', sourceId='memory.preference', sensitivity='private', trust=1.0, relevance=0.5.
    4. Return all items combined in order: conversation → user facts → preferences. Return success with items array.

    `async write(record:Omit<MemoryRecord,'id'|'useCount'|'confidence'> & {source:ConfidenceSource}):Promise<MemoryWriteResult>` — MEM-02 gate:
    1. Check `isPrimarySurface()` — if false, return {success:false, error:'Memory writes only allowed on primary surface', code:'NOT_PRIMARY_SURFACE'}. Do NOT create a WriteJournal entry.
    2. Map memoryType to WriteJournal operation: 'semantic'→'update-user-memory', 'preference'→'write-preference', 'working'|'episodic'→'compact-conversation'.
    3. Create WriteJournal entry, commit with step {name:'write-memory-record', executor: ()=>userStore.upsert(record)}, plus broadcast step {'broadcast-workspace-update', executor: ()=>publish('WORKSPACE_UPDATED',{source:'memory'})}.
    4. On success, increment useCount via userStore.incrementUseCount. Return {success:true, recordId}.

    `async getPreferences():Promise<Record<string,unknown>>` — delegates to preferenceStore.getAll().
    `async getPersona():Promise<Record<string,unknown>|null>` — delegates to preferenceStore.getPersona().

    **Private helpers:**
    `toContextItems(records, sourcePrefix, kind):ContextItem[]` — maps memory records to ContextItem instances, computing relevance/freshness per Phase 4b D-06/D-08. Uses tokenizeQuery for relevance.
    `isPrimarySurface():boolean` — reads from BroadcastBus primary election. Pattern: `BroadcastBus.getPrimarySurfaceId() === currentSurfaceId`. currentSurfaceId is set at construction time or passed via config. For Phase 5: use `globalThis.__NOWPILOT_SURFACE_ID__` or a simple module-level variable set by the entrypoint. For now, implement as a method that returns true by default with a `// TODO: wire BroadcastBus primary election (Plan 03)` comment — the actual wiring test proves the guard works correctly.

    **tests/core/memory/MemoryEngine.test.ts:**
    Covers: retrieve returns items from all 3 stores in correct order, tier-gating limits user facts to 3 (tiny) or 5 (other), ContextItem sourceId format correctness, ContextItem carries sensitivity from record, write blocked on secondary surface (mock isPrimarySurface→false), write succeeds on primary surface, write creates WriteJournal entry with correct operation, deterministic retrieval (same query same tier → same results), write assigns correct confidence from source mapping, retrieve with no stored facts returns empty facts array not an error.

    Mock WriteJournal, BroadcastBus, and IndexedDB stores via fake-indexeddb (already in setup.ts). Use `resetMemoryEngine()` in beforeEach for test isolation.

    Commit after tests pass: `feat(05-02): MemoryEngine orchestrator — retrieval pipeline, tier-gating, single-writer enforcement`
  </action>
  <verify>
    <automated>npx vitest run tests/core/memory/MemoryEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/core/memory/MemoryEngine.test.ts` passes — all retrieval, write-guard, and ContextItem generation tests green
    - MemoryEngine.retrieve() for tier='tiny' returns ≤3 user facts
    - MemoryEngine.write() rejects on secondary surface before any IndexedDB mutation
    - ContextItems have correct sourceId prefixes (memory.conversation.summary.*, memory.user.fact.*, memory.preference.*)
    - WriteJournal entry created with correct operation type matching memoryType
    - `pnpm tsc --noEmit` succeeds with no new errors in src/core/memory/
  </acceptance_criteria>
  <reversibility rating="reversible">D-09 tier-gating limits (3/5) and MIN_SCORE threshold (0.30) are local constants in MemoryEngine — tuning does not change schema or persisted data</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-authored fact content → MemoryScorer | Untrusted user text enters scoring; scoring is pure token matching — no code execution |
| MemoryEngine.retrieve() → ContextItem[] → ContextOptimizer | Memory data enters AI prompt pipeline; ContextTrustPolicy gates trust/sensitivity (Phase 4b contract) |
| MemoryEngine.write() → IndexedDB | Write guarded by BroadcastBus primary election; WriteJournal ensures crash consistency |
| LLM summarization → ConversationMemoryStore | LLM output stored as summary text; treated as untrusted data (per D-05: summaries only, not facts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Spoofing | MemoryEngine.isPrimarySurface() | high | mitigate | BroadcastBus primary election verified before every write; secondary surfaces reject with NOT_PRIMARY_SURFACE error before any IndexedDB mutation. Surface identity validated via chrome.runtime.id. |
| T-05-06 | Tampering | UserMemoryStore — LLM writes facts | high | mitigate | D-05 write boundary enforced in MemoryEngine.write(): memoryType must be 'working' for LLM summaries (conversation); 'semantic'/'preference' writes only via explicit user action (called from UI, not AI pipeline). Store-level guard prevents LLM code path from writing user facts. |
| T-05-07 | Information Disclosure | MemoryRecord sensitivity → ContextItem | medium | mitigate | MemoryRecord.sensitivity inherits directly to ContextItem.sensitivity (Phase 4b D-09). 'secret' records must not become ContextItem instances — ContextItemSchema.refine rejects sensitivity:'secret'. 'confidential' records excluded from cloud provider prompts by ContextOptimizer. |
| T-05-08 | Tampering | Confidence mutation during retrieval | medium | mitigate | D-07: confidence is source-based immutable. MemoryScorer reads confidence, never writes it. UserMemoryStore.incrementUseCount updates only useCount and lastUsedAt. Test assertion: confidence field unchanged after retrieval. |
| T-05-09 | Repudiation | Memory writes without WriteJournal | medium | mitigate | MemoryEngine.write() wraps all IndexedDB mutations in WriteJournal createEntry/commitEntry. Failed writes leave audit trail as 'failed' or 'rolled-back'. Broadcast WORKSPACE_UPDATED on successful commit. |
| T-05-SC | Tampering | npm installs (minisearch, idb, zustand, zod) | high | mitigate | All packages verified via Package Legitimacy Audit (RESEARCH.md). No new packages. Already installed with [ASSUMED] verdict. |
</threat_model>

<verification>
All test suites green: `npx vitest run tests/core/memory/MemoryScorer.test.ts tests/core/memory/UserMemoryStore.test.ts tests/core/memory/ConversationMemoryStore.test.ts tests/core/memory/MemoryEngine.test.ts`
`pnpm tsc --noEmit` succeeds with no new errors
</verification>

<success_criteria>
- MemoryScorer computes D-08 weighted scores correctly (all 5 components independently verified)
- ConversationMemoryStore returns tier-gated recent turns and compact signal at 12-message boundary
- UserMemoryStore assigns immutable confidence from source (D-07 mapping)
- MemoryEngine.retrieve() returns ordered ContextItem[] with correct sourceId prefixes
- MemoryEngine.write() enforced single-writer via BroadcastBus primary election
- MemoryEngine writes use WriteJournal for crash consistency
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-base/05-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `MemoryType` | type (union) | src/core/memory/MemoryRecord.ts |
| `MemoryTypeSchema` | Zod schema | src/core/memory/MemoryRecord.ts |
| `ConfidenceSource` | type (union) | src/core/memory/MemoryRecord.ts |
| `ConfidenceSourceSchema` | Zod schema | src/core/memory/MemoryRecord.ts |
| `CONFIDENCE_MAP` | const Record | src/core/memory/MemoryRecord.ts |
| `MemoryRecordSchema` | Zod schema | src/core/memory/MemoryRecord.ts |
| `MemoryRecord` | type (z.infer) | src/core/memory/MemoryRecord.ts |
| `UserMemoryFact` | type | src/core/memory/MemoryRecord.ts |
| `UserMemoryFactSchema` | Zod schema | src/core/memory/MemoryRecord.ts |
| `ConversationSummarySchema` | Zod schema | src/core/memory/MemoryRecord.ts |
| `ConversationSummary` | type (z.infer) | src/core/memory/MemoryRecord.ts |
| `PreferenceRecordSchema` | Zod schema | src/core/memory/MemoryRecord.ts |
| `PreferenceRecord` | type (z.infer) | src/core/memory/MemoryRecord.ts |
| `RetrievedMemorySchema` | Zod schema | src/core/memory/MemoryRecord.ts |
| `RetrievedMemory` | type (z.infer) | src/core/memory/MemoryRecord.ts |
| `ConversationContextSchema` | Zod schema | src/core/memory/MemoryRecord.ts |
| `ConversationContext` | type (z.infer) | src/core/memory/MemoryRecord.ts |
| `MemoryRetrievalResult` | discriminated union | src/core/memory/types.ts |
| `MemoryWriteResult` | discriminated union | src/core/memory/types.ts |
| `RetrievalOptions` | interface | src/core/memory/types.ts |
| `MemoryStoreWriteOp` | type (union) | src/core/memory/types.ts |
| `WEIGHTS` | const | src/core/memory/MemoryScorer.ts |
| `MIN_SCORE` | const (0.30) | src/core/memory/MemoryScorer.ts |
| `USE_COUNT_CAP` | const (20) | src/core/memory/MemoryScorer.ts |
| `TIER_LIMITS` | const Record | src/core/memory/MemoryScorer.ts |
| `tokenizeQuery` | function | src/core/memory/MemoryScorer.ts |
| `scoreFact` | function | src/core/memory/MemoryScorer.ts |
| `getTopFacts` | function | src/core/memory/MemoryScorer.ts |
| `ConversationMemoryStore` | class | src/core/memory/ConversationMemoryStore.ts |
| `ConversationMemoryStore.getContext` | async method | src/core/memory/ConversationMemoryStore.ts |
| `ConversationMemoryStore.appendMessage` | async method | src/core/memory/ConversationMemoryStore.ts |
| `ConversationMemoryStore.getMessageCount` | async method | src/core/memory/ConversationMemoryStore.ts |
| `ConversationMemoryStore.saveSummary` | async method | src/core/memory/ConversationMemoryStore.ts |
| `ConversationMemoryStore.evictConversation` | async method | src/core/memory/ConversationMemoryStore.ts |
| `UserMemoryStore` | class | src/core/memory/UserMemoryStore.ts |
| `UserMemoryStore.upsert` | async method | src/core/memory/UserMemoryStore.ts |
| `UserMemoryStore.getAll` | async method | src/core/memory/UserMemoryStore.ts |
| `UserMemoryStore.get` | async method | src/core/memory/UserMemoryStore.ts |
| `UserMemoryStore.remove` | async method | src/core/memory/UserMemoryStore.ts |
| `UserMemoryStore.incrementUseCount` | async method | src/core/memory/UserMemoryStore.ts |
| `UserMemoryStore.findByTag` | async method | src/core/memory/UserMemoryStore.ts |
| `PreferenceMemoryStore` | class | src/core/memory/PreferenceMemoryStore.ts |
| `PreferenceMemoryStore.get` | async method | src/core/memory/PreferenceMemoryStore.ts |
| `PreferenceMemoryStore.set` | async method | src/core/memory/PreferenceMemoryStore.ts |
| `PreferenceMemoryStore.getAll` | async method | src/core/memory/PreferenceMemoryStore.ts |
| `PreferenceMemoryStore.getPersona` | async method | src/core/memory/PreferenceMemoryStore.ts |
| `MemoryEngine` | class (singleton) | src/core/memory/MemoryEngine.ts |
| `getMemoryEngine` | function | src/core/memory/MemoryEngine.ts |
| `resetMemoryEngine` | function | src/core/memory/MemoryEngine.ts |
| `MemoryEngine.retrieve` | async method | src/core/memory/MemoryEngine.ts |
| `MemoryEngine.write` | async method | src/core/memory/MemoryEngine.ts |
| `MemoryEngine.getPreferences` | async method | src/core/memory/MemoryEngine.ts |
| `MemoryEngine.getPersona` | async method | src/core/memory/MemoryEngine.ts |
| `isPrimarySurface` | private method | src/core/memory/MemoryEngine.ts |
