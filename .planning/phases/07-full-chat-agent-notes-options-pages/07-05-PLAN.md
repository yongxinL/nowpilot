---
phase: 07-full-chat-agent-notes-options-pages
plan: 05
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - src/core/notes/LinkParser.ts
  - src/core/notes/NoteGraph.ts
  - src/core/pages/NotesPage.tsx
  - src/components/notes/NoteList.tsx
  - src/components/notes/NoteEditor.tsx
  - src/components/notes/NotePreview.tsx
  - src/components/notes/BacklinksPanel.tsx
  - src/components/notes/WikilinkAutocomplete.tsx
  - src/components/notes/NoteGraphView.tsx
  - src/components/notes/SaveToNoteDialog.tsx
  - src/components/patterns/SkillMessageRenderer.tsx
  - src/components/patterns/SourceCard.tsx
  - tests/core/notes/LinkParser.test.ts
  - tests/components/NotesPage.test.tsx
autonomous: true
requirements:
  - NOTE-01
  - NOTE-02
  - NOTE-03
  - NOTE-04
  - NOTE-05
  - NOTE-06
  - NOTE-07

must_haves:
  truths:
    - LinkParser.parseLinks('See [[note1]] and [[note2|alias]]') returns two ParsedLink entries (D-21)
    - LinkParser.resolve() pipeline: exact match → case-insensitive → MiniSearch fuzzy → ambiguous → create-or-link (D-21)
    - LinkParser.buildBacklinks() returns Map<noteId, BacklinkEntry[]> computed from all notes (D-23)
    - NoteGraph.buildGraphData() returns { nodes: NoteGraphNode[], links: NoteGraphLink[] } from notes + wikilinks (D-24)
    - NotesPage renders flat NoteList with search bar (D-25, D-30)
    - NoteEditor is split-pane: left textarea with wikilink autocomplete, right XMarkdown preview (D-22)
    - BacklinksPanel shows referencing notes with context snippets (D-23)
    - WikilinkAutocomplete triggers on [[, shows MiniSearch-ranked dropdown, "Create note" option (D-28)
    - NoteGraphView renders d3-force visualization with draggable nodes, zoom/pan, click navigation (D-24)
    - SaveToNoteDialog opens from assistant message context menu, pre-fills content (D-26)
    - Auto-version on save: previous content stored in chrome.storage.local np_note_versions (D-27)
    - Undo restores most recent version, creates new version entry with pre-undo content (D-27)
    - MiniSearch index: full build on page load, incremental updates on CRUD, debounced 150ms search (D-30, D-32)
    - Full-text search returns ranked results with highlighted snippets (D-29, D-30)
    - NotesPage registered only on Full App (standalone) — Side Panel excluded
  artifacts:
    - src/core/notes/LinkParser.ts
    - src/core/notes/NoteGraph.ts
    - src/core/pages/NotesPage.tsx
    - src/components/notes/NoteList.tsx
    - src/components/notes/NoteEditor.tsx
    - src/components/notes/NotePreview.tsx
    - src/components/notes/BacklinksPanel.tsx
    - src/components/notes/WikilinkAutocomplete.tsx
    - src/components/notes/NoteGraphView.tsx
    - src/components/notes/SaveToNoteDialog.tsx
    - src/components/patterns/SkillMessageRenderer.tsx
    - src/components/patterns/SourceCard.tsx
    - tests/core/notes/LinkParser.test.ts
    - tests/components/NotesPage.test.tsx
  key_links:
    - LinkParser → MiniSearch (full-text index for resolution + search)
    - LinkParser → NotesDB (read all notes for backlink computation)
    - NoteGraph → d3-force (force simulation data model)
    - NotesPage → NotesDB (CRUD)
    - NotesPage → LinkParser (wikilink resolution in editor/preview)
    - NoteEditor → WikilinkAutocomplete ([[ triggers dropdown)
    - NotePreview → XMarkdown (rendered markdown with resolved wikilinks)
    - SaveToNoteDialog → NotesDB (direct save from chat)
    - NoteGraphView → d3-force / react-force-graph-2d (canvas rendering)
    - NotesPage → shell renderActivePage (Registration unchanged: registerOn: ['standalone'])
---

<objective>
Build the complete Notes feature (Full App only): `LinkParser` (wikilink parsing, MiniSearch-powered resolution, backlink computation), `NoteGraph` (d3-force graph data model), `NotesPage` (split-pane editor + list + backlinks + graph), and supporting UI components. Notes are entirely independent of the Chat/Agent hook hierarchy.

Purpose: Deliver the knowledge management feature — linked notes with wikilink resolution, backlinks, graph visualization, full-text search, and save-from-chat integration.
Output: 12 source files + 2 test files
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-full-chat-agent-notes-options-pages/07-CONTEXT.md
@.planning/phases/07-full-chat-agent-notes-options-pages/07-RESEARCH.md
@.planning/phases/07-full-chat-agent-notes-options-pages/07-PATTERNS.md

# Reference files
@src/core/storage/stores/NotesDB.ts
@src/core/search/MiniSearchIndex.ts
@src/core/navigation/navigationTypes.ts
@src/core/registries/registerNowPilotCorePages.ts
@src/core/ai/tools/ToolRegistry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LinkParser core service</name>
  <files>src/core/notes/LinkParser.ts, tests/core/notes/LinkParser.test.ts</files>
  <read_first>
    - src/core/storage/stores/NotesDB.ts (Note type shape: id, title, content, created, updated, tags)
    - src/core/search/MiniSearchIndex.ts (MiniSearch constructor pattern, add/remove/rebuild/search methods)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-RESEARCH.md (Notes Engine Architecture lines 1045-1084; Pattern 5: Notes Wikilink Resolution, lines 639-697)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-CONTEXT.md (D-21 through D-28, D-29 through D-32)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-PATTERNS.md (LinkParser pattern, lines 519-568)
  </read_first>
  <behavior>
    - Test 1: parseLinks('[[hello]]') returns [{ title: 'hello', alias: 'hello', raw: '[[hello]]' }]
    - Test 2: parseLinks('[[hello|world]]') returns [{ title: 'hello', alias: 'world', raw: '[[hello|world]]' }]
    - Test 3: parseLinks('[[a]] and [[b|c]]') returns 2 links
    - Test 4: parseLinks('no wikilinks here') returns []
    - Test 5: parseLinks handles edge cases: empty brackets [[]], nested brackets, malformed input → no match or graceful handling
    - Test 6: resolve() exact title match returns { found: true, noteId }
    - Test 7: resolve() case-insensitive match returns { found: true, noteId }
    - Test 8: resolve() with MiniSearch fuzzy match (single result, score > 0.5) returns { found: true, noteId }
    - Test 9: resolve() with multiple fuzzy matches returns { found: false, ambiguous: true, candidates }
    - Test 10: resolve() no match returns { found: false, ambiguous: false }
    - Test 11: buildBacklinks() computes correct referencing notes for each note
    - Test 12: rebuildIndex() populates MiniSearch from NotesDB
    - Test 13: search() returns ranked results with highlighted snippets
  </behavior>
  <action>
    Create `LinkParser` class following the MiniSearchIndex class+singleton pattern.

    **Constants:** `WIKILINK_REGEX = /\[\[([^\]]+?)(?:\|([^\]]+?))?\]\]/g`.

    **MiniSearch config:** follow PATTERNS.md line 555: `new MiniSearch({ fields: ['title', 'content'], storeFields: ['id', 'title', 'updatedAt'], searchOptions: { boost: { title: 3 }, prefix: true, fuzzy: 0.2 } })`.

    **Methods:**
    - `parseLinks(content: string): ParsedLink[]` — regex extraction returning `{ title, alias, raw }` array per D-21.
    - `async resolve(title: string, allNotes: Note[]): Promise<ResolutionResult>` — pipeline: exact title match → case-insensitive (toLowerCase) → MiniSearch.search(title, { fuzzy: 0.2, prefix: true }) with threshold 0.5 → single result → found; multiple results → ambiguous; no results → not found (D-21).
    - `async buildBacklinks(allNotes: Note[]): Promise<Map<string, BacklinkEntry[]>>` — for each note, scan all other notes' content with parseLinks(). If a link's resolved title matches the note's title, add entry `{ noteId, title, snippet }` (snippet = 60 chars around the wikilink) (D-23).
    - `rebuildIndex(notes: Note[]): void` — calls `this.index.removeAll()` then `this.index.addAll(notes.map(n => ({ id: n.id, title: n.title, content: n.content, updatedAt: n.updatedAt })))` (D-32).
    - `search(query: string): SearchResult[]` — delegates to `this.index.search(query, { prefix: true, fuzzy: 0.2, boost: { title: 3 } })` (D-29, D-30).
    - `addToIndex(note: Note): void`, `removeFromIndex(id: string): void` — incremental index updates (D-32).

    **Types:** `ParsedLink { title: string; alias: string; raw: string }`, `ResolutionResult { found: boolean; noteId?: string; ambiguous?: boolean; candidates?: Array<{ id: string; title: string }> }`, `BacklinkEntry { noteId: string; title: string; snippet: string }`, `SearchResult { id: string; title: string; score: number; snippet?: string }`.

    Export class `LinkParser` + singleton `linkParser`.
  </action>
  <acceptance_criteria>
    - `src/core/notes/LinkParser.ts` exists with class + singleton export
    - `tests/core/notes/LinkParser.test.ts` exists with ≥13 tests, all passing
    - `grep 'WIKILINK_REGEX' src/core/notes/LinkParser.ts` returns match
    - `grep 'MiniSearch' src/core/notes/LinkParser.ts` returns match
    - `grep 'buildBacklinks' src/core/notes/LinkParser.ts` returns match
    - `grep 'caseInsensitive\|toLowerCase' src/core/notes/LinkParser.ts` returns match (case-insensitive resolution)
    - All tests pass: `npx vitest run tests/core/notes/LinkParser.test.ts`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/notes/LinkParser.test.ts</automated>
  </verify>
  <done>LinkParser with wikilink regex parsing, four-stage resolution pipeline (exact → case-insensitive → fuzzy → create-or-link), backlink computation, and MiniSearch integration. Class + singleton export. All 13+ tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NoteGraph and NoteGraphView</name>
  <files>src/core/notes/NoteGraph.ts, src/components/notes/NoteGraphView.tsx</files>
  <read_first>
    - src/core/notes/LinkParser.ts (parseLinks, buildBacklinks for wikilink extraction)
    - src/core/storage/stores/NotesDB.ts (Note type shape)
    - src/core/stores/themeStore.ts (theme.useToken for color consistency)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-RESEARCH.md (d3-force Graph Integration lines 1072-1097; Pitfall 5: d3-force SVG/Canvas Confusion in React lines 764-769)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-CONTEXT.md (D-24: separate d3-force Graph view)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-PATTERNS.md (NoteGraph pattern lines 572-595, NoteGraphView pattern lines 779-789)
    - package.json (verify d3-force@3.0.0 is installed)
  </read_first>
  <behavior>
    - Test 1: NoteGraph.buildGraphData([note1, note2, note3], links) returns { nodes: [{id,title}...], links: [{source,target}...] }
    - Test 2: Empty notes array returns { nodes: [], links: [] }
    - Test 3: Notes without wikilinks → nodes exist but links empty
    - Test 4: NoteGraphView renders canvas element when graphData has ≥3 nodes (D-24: minimum 3)
    - Test 5: NoteGraphView shows placeholder message when < 3 notes exist
  </behavior>
  <action>
    **A. NoteGraph.ts (core service):**
    Create `NoteGraph` class with `buildGraphData(notes: Note[], links: ParsedLink[]): { nodes: NoteGraphNode[]; links: NoteGraphLink[] }` method per RESEARCH.md. Nodes: `{ id: string; title: string }`. Links: derived from wikilinks where source note's content contains `[[target-title]]`. Each link: `{ source: noteId, target: resolvedTargetId }`. Filter out self-links (source === target) and unresolved links. Class + singleton export.

    Types: `NoteGraphNode { id: string; title: string }`, `NoteGraphLink { source: string; target: string }`. Export singleton `noteGraph`.

    **B. NoteGraphView.tsx (UI component):**
    Renders interactive d3-force graph visualization. Per D-24: only shows when ≥3 notes exist; otherwise shows a descriptive placeholder.

    **Approach: raw d3-force with canvas rendering** (not react-force-graph-2d, to keep dependencies minimal). Use `useRef<HTMLCanvasElement>` for canvas access.

    Implementation:
    1. Import `forceSimulation, forceLink, forceManyBody, forceCenter, forceCollide` from 'd3-force'.
    2. Create simulation with standard forces: forceLink(links).distance(100), forceManyBody().strength(-300), forceCenter(width/2, height/2), forceCollide().radius(30).
    3. On simulation tick, clear canvas and draw: circles for nodes (radius 8, fill token.colorPrimary), text labels for titles (fontSize 12), lines for edges (stroke token.colorBorderSecondary, strokeWidth 1, opacity 0.6).
    4. Interaction: mousedown on node → start drag (reheat simulation with alpha 0.3, fix node position during drag). mouseup → release. mousemove → hover detection (enlarge node, show tooltip with title). Double-click → navigate to note. Wheel → zoom (transform canvas scale). Track transforms with d3-zoom or manual scale/translate state.
    5. Window resize: recalculate width/height, restart simulation.
    6. Use `useEffect` for simulation lifecycle — create on mount, clean up on unmount (stop simulation, remove listeners).
    7. Wrap canvas in a container div with `width:100%; height:100%`.

    Props: `{ notes: Note[]; linkParser: LinkParser; onNavigateNote: (noteId: string) => void }`. Determines ≥3 notes internally via notes.length.

    Use `theme.useToken()` for colors to stay consistent with Ant Design theme.
  </action>
  <acceptance_criteria>
    - `src/core/notes/NoteGraph.ts` exists with class + singleton export
    - `src/components/notes/NoteGraphView.tsx` exists
    - `grep 'buildGraphData' src/core/notes/NoteGraph.ts` returns match
    - `grep 'forceSimulation\|d3-force' src/components/notes/NoteGraphView.tsx` returns match
    - `grep 'canvas' src/components/notes/NoteGraphView.tsx` returns match (lowercase ok — canvas ref)
    - `tests/core/notes/LinkParser.test.ts` still passes (if graph tests are separate or combined)
    - `npx tsc --noEmit` passes
    - `npx vitest run` — all existing + new tests pass
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/notes/</automated>
  </verify>
  <done>NoteGraph builds d3-force compatible graph data from notes and wikilinks. NoteGraphView renders interactive canvas-based force graph with drag, zoom, hover, click navigation. Only shown when ≥3 notes exist.</done>
</task>

<task type="auto">
  <name>Task 3: Create Notes UI components and replace NotesPage stub</name>
  <files>src/core/pages/NotesPage.tsx, src/components/notes/NoteList.tsx, src/components/notes/NoteEditor.tsx, src/components/notes/NotePreview.tsx, src/components/notes/BacklinksPanel.tsx, src/components/notes/WikilinkAutocomplete.tsx, src/components/notes/SaveToNoteDialog.tsx, src/components/patterns/SkillMessageRenderer.tsx, src/components/patterns/SourceCard.tsx, tests/components/NotesPage.test.tsx</files>
  <read_first>
    - src/core/notes/LinkParser.ts (linkParser singleton, parseLinks, buildBacklinks, search, rebuildIndex)
    - src/core/notes/NoteGraph.ts (noteGraph singleton, buildGraphData)
    - src/core/storage/stores/NotesDB.ts (notesDB singleton, CRUD methods, Note type)
    - src/components/notes/NoteGraphView.tsx (created in Task 2)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-CONTEXT.md (D-21 through D-28 for Notes UX, D-29 through D-32 for Search)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-RESEARCH.md (Notes MiniSearch Index Setup lines 892-920, Notes Engine Architecture lines 1045-1084)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-PATTERNS.md (NotesPage pattern lines 337-352, BacklinksPanel lines 729-744, WikilinkAutocomplete lines 747-776)
  </read_first>
  <action>
    **A. NotesPage.tsx (replace stub):**
    Replace existing stub with functional Notes page (Full App only per registerOn: ['standalone']).

    Layout per D-22, D-23, D-25 (conceptual layout lines 254-267):
    - Flex row: NoteList (240px, collapsible) | NoteEditor (flex:1, split-pane) | BacklinksPanel (260px, collapsible)
    - State: notes[], selectedNoteId, isGraphView (boolean toggle)
    - On mount: load `notesDB.getAllNotes()`, initialize `linkParser.rebuildIndex(notes)`, compute backlinks via `linkParser.buildBacklinks(notes)`.
    - Graph button in toolbar toggles NoteGraphView instead of split-pane editor.

    Wire: NoteList (search + sortable list), NoteEditor (textarea + preview), BacklinksPanel (computed backlinks), NoteGraphView (d3-force graph), SaveToNoteDialog (context menu integration).

    Surface restriction: NotesPage registered only on 'standalone' (already configured in registerNowPilotCorePages.ts). No Side Panel rendering.

    **B. NoteList.tsx (new):**
    Implements D-25 (flat list) + D-30 (persistent search bar). Props: `{ notes: Note[]; selectedNoteId: string | null; onSelect: (id: string) => void; onNew: () => void; onDelete: (id: string) => void; linkParser: LinkParser }`.
    - Search bar at top: AntD `<Input.Search>` with 150ms debounced MiniSearch query (D-30). Search results replace the note list.
    - Sort dropdown: Updated Date (default), Created Date, Title (D-25).
    - Flat list of notes rendered as AntD `<List>` items with title, snippet, timestamp.
    - "+ New Note" button at top.
    - Delete via Popconfirm (D-11 pattern).

    **C. NoteEditor.tsx (new):**
    Implements D-22 (split-pane editor). Props: `{ note: Note | null; onSave: (note: Note) => void; linkParser: LinkParser; onUndo: () => void }`.
    - Left pane: `<Input.TextArea>` for markdown editing with `WikilinkAutocomplete` integration (D-22, D-28). Auto-save debounced (2s after last change) → auto-version (D-27).
    - Right pane: `NotePreview` component (D-22). In Side Panel: toggle between edit and preview (Notes is Full App only, but keep the preview toggle pattern for future).
    - Toolbar: Save button, Undo button (D-27).

    **D. NotePreview.tsx (new):**
    Renders note content as XMarkdown with resolved wikilinks. Props: `{ content: string; notes: Note[]; linkParser: LinkParser }`. Before rendering: replace `[[title]]` and `[[title|alias]]` with resolved links → render via XMarkdown. Unresolved links shown as clickable "Create note?" text.

    **E. BacklinksPanel.tsx (new):**
    Implements D-23. Props: `{ backlinks: BacklinkEntry[]; onNavigateNote: (id: string) => void }`. Renders collapsible panel (AntD `<Card>` or custom) with list of referencing notes. Each entry: note title (clickable → navigate) + context snippet around wikilink. Empty state: "No notes link here."

    **F. WikilinkAutocomplete.tsx (new):**
    Implements D-28. Embedded inside NoteEditor's textarea. Triggers on `[[` typed. Shows dropdown (AntD Dropdown or custom positioned popover) with note suggestions ranked by: exact prefix match → title-starts-with → title-contains → recently updated. Uses `linkParser.search()` for MiniSearch results. Arrow keys navigate, Enter/Tab selects and completes `[[title]]` syntax. "Create note" option for new titles.

    **G. SaveToNoteDialog.tsx (new):**
    Implements D-26. Props: `{ content: string; onSave: (noteId?: string) => void; onClose: () => void }`. AntD Modal with: option to create new note (title input, content pre-filled) or append to existing note (AntD Select dropdown of existing notes, append content). Saves via `notesDB.createNote()` or `notesDB.updateNote()`.

    **H. Auto-versioning (D-27):**
    On every save in NoteEditor, store previous version in chrome.storage.local key `np_note_versions` as `Record<noteId, Array<{ content: string; timestamp: number }>>`. Keep last 10 versions per note. Undo: restore most recent version, push current content as new version on the stack. Implement as a simple helper in NoteEditor or as a utility class `NoteVersioner`.

    **I. SkillMessageRenderer.tsx (new):**
    Props: `{ content: string; type: 'skill' | 'macro' }`. Renders skill/macro output — same as NotePreview but with a badge/tag indicating the source (e.g., "Skill: Write - Rewrite"). Uses XMarkdown for rendering.

    **J. SourceCard.tsx (new):**
    Props: `{ title: string; url?: string; snippet: string }`. AntD Card with source metadata display. Clickable title (opens URL in new tab if provided).

    **Testing:** Component tests render NotesPage with mocked notesDB, verify list renders, editor shows selected note, search filters.
  </action>
  <acceptance_criteria>
    - `src/core/pages/NotesPage.tsx` no longer has "Coming soon" text
    - `grep 'NoteList' src/core/pages/NotesPage.tsx` returns match
    - `grep 'NoteEditor' src/core/pages/NotesPage.tsx` returns match (or in layout)
    - `grep 'BacklinksPanel' src/core/pages/NotesPage.tsx` returns match (or conditional)
    - `src/components/notes/NoteList.tsx` exists with Input.Search + List
    - `src/components/notes/NoteEditor.tsx` exists with TextArea + preview split
    - `src/components/notes/NotePreview.tsx` exists with XMarkdown
    - `src/components/notes/BacklinksPanel.tsx` exists
    - `src/components/notes/WikilinkAutocomplete.tsx` exists
    - `src/components/notes/NoteGraphView.tsx` exists (from Task 2)
    - `src/components/notes/SaveToNoteDialog.tsx` exists
    - `src/components/patterns/SkillMessageRenderer.tsx` exists
    - `src/components/patterns/SourceCard.tsx` exists
    - `grep 'np_note_versions' src/components/notes/NoteEditor.tsx` or related file returns match (auto-versioning)
    - `tests/components/NotesPage.test.tsx` exists with ≥3 tests passing
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/components/NotesPage.test.tsx tests/core/notes/LinkParser.test.ts</automated>
  </verify>
  <done>NotesPage renders full Notes UI: searchable flat NoteList, split-pane NoteEditor with wikilink autocomplete, XMarkdown NotePreview with resolved wikilinks, BacklinksPanel, d3-force NoteGraphView. SaveToNoteDialog for chat integration. Auto-versioning with Undo. SkillMessageRenderer and SourceCard for cross-cutting use. Notes accessible only in Full App. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → NoteEditor | Markdown content with wikilinks; parsed by regex, rendered by XMarkdown (DOMPurify-sanitized) |
| User input → MiniSearch | Search queries from debounced input; no SQL injection risk (in-memory index) |
| Note content → XMarkdown | Rendered through @ant-design/x-markdown; DOMPurify sanitization per RESEARCH.md |
| chrome.storage.local → np_note_versions | Versioned note content; trusted storage |
| NotesDB → NoteGraphView | Note titles rendered as graph node labels; no XSS vector (plain text) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Tampering | XMarkdown rendering of note content | low | accept | @ant-design/x-markdown internal DOMPurify sanitization. No raw HTML rendering. |
| T-07-14 | Spoofing | Wikilink resolution | low | mitigate | Wikilink titles validated against `/^[\w\s-]+$/` before rendering as links per RESEARCH.md threat analysis. |
| T-07-15 | Information Disclosure | Note content → ContextOptimizer | low | accept | Note content fed to ContextOptimizer as notes_metadata section, separated from system_prompt — low injection risk. |
| T-07-16 | Denial of Service | d3-force simulation | low | mitigate | Force simulation alpha decay prevents infinite loops. Canvas rendering uses requestAnimationFrame. Simulation stopped on unmount. |
| T-07-17 | Information Disclosure | SaveToNoteDialog | low | accept | Dialog creates/updates notes in NotesDB — trusted source. No cross-origin data exposure. |
</threat_model>

<verification>
LinkParser tests: `npx vitest run tests/core/notes/LinkParser.test.ts`
Component tests: `npx vitest run tests/components/NotesPage.test.tsx`
Full suite: `npx vitest run`
TypeScript: `npx tsc --noEmit`
</verification>

<success_criteria>
LinkParser resolves wikilinks through four-stage pipeline. NoteGraph produces d3-force compatible data. NotesPage renders full Notes UI with search, editor, preview, backlinks, and graph. SaveToNoteDialog enables chat-to-note flow. Auto-versioning with undo works. All tests pass.
</success_criteria>

<output>
Create `.planning/phases/07-full-chat-agent-notes-options-pages/07-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `ParsedLink` | Type | src/core/notes/LinkParser.ts |
| `ResolutionResult` | Type | src/core/notes/LinkParser.ts |
| `BacklinkEntry` | Type | src/core/notes/LinkParser.ts |
| `SearchResult` | Type | src/core/notes/LinkParser.ts |
| `LinkParser` | Class | src/core/notes/LinkParser.ts |
| `linkParser` | Singleton | src/core/notes/LinkParser.ts |
| `LinkParser.parseLinks` | Method | src/core/notes/LinkParser.ts |
| `LinkParser.resolve` | Method | src/core/notes/LinkParser.ts |
| `LinkParser.buildBacklinks` | Method | src/core/notes/LinkParser.ts |
| `LinkParser.rebuildIndex` | Method | src/core/notes/LinkParser.ts |
| `LinkParser.search` | Method | src/core/notes/LinkParser.ts |
| `NoteGraphNode` | Type | src/core/notes/NoteGraph.ts |
| `NoteGraphLink` | Type | src/core/notes/NoteGraph.ts |
| `NoteGraph` | Class | src/core/notes/NoteGraph.ts |
| `noteGraph` | Singleton | src/core/notes/NoteGraph.ts |
| `NoteGraph.buildGraphData` | Method | src/core/notes/NoteGraph.ts |
| `NotesPage` | Component | src/core/pages/NotesPage.tsx |
| `NoteList` | Component | src/components/notes/NoteList.tsx |
| `NoteEditor` | Component | src/components/notes/NoteEditor.tsx |
| `NotePreview` | Component | src/components/notes/NotePreview.tsx |
| `BacklinksPanel` | Component | src/components/notes/BacklinksPanel.tsx |
| `WikilinkAutocomplete` | Component | src/components/notes/WikilinkAutocomplete.tsx |
| `NoteGraphView` | Component | src/components/notes/NoteGraphView.tsx |
| `SaveToNoteDialog` | Component | src/components/notes/SaveToNoteDialog.tsx |
| `SkillMessageRenderer` | Component | src/components/patterns/SkillMessageRenderer.tsx |
| `SourceCard` | Component | src/components/patterns/SourceCard.tsx |
