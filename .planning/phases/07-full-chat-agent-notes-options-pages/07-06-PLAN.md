---
phase: 07-full-chat-agent-notes-options-pages
plan: 06
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - src/components/options/ProvidersSection.tsx
  - src/components/options/ModelsSection.tsx
  - src/components/options/MCPSection.tsx
  - src/components/options/PromptsSection.tsx
  - src/components/options/SlashSection.tsx
  - src/components/options/MemorySection.tsx
  - src/components/options/ImportExportSection.tsx
  - src/components/options/FeatureFlagsSection.tsx
  - src/components/options/AddonSettingsSection.tsx
  - src/components/options/AppearanceSection.tsx
  - src/components/options/AboutSection.tsx
  - src/core/pages/OptionsPage.tsx
  - src/core/routing/workspaceRouter.ts
  - tests/components/OptionsPage.test.tsx
autonomous: true
requirements:
  - OPT-01
  - OPT-02
  - OPT-03
  - OPT-04
  - OPT-05
  - OPT-06
  - OPT-07
  - OPT-08
  - OPT-09
  - OPT-10
  - OPT-11

must_haves:
  truths:
    - All 11 Options sections render via OptionsRoot.renderSectionContent (D-09, D-12)
    - ProvidersSection has encrypted key input and inline "Test Connection" button with loading + inline result (D-10)
    - ModelsSection uses Form.List per provider with context window override (D-12 layout exception)
    - All delete actions across sections use AntD Popconfirm (D-11)
    - Prompt templates section supports rich editor with variable preview (D-12 layout exception)
    - Import/Export section supports file upload/merge workflow (D-12 layout exception)
    - ProvidersSection, ImportExportSection, PromptsSection diverge from standard form; all others follow D-09 pattern
    - OptionsPage renders as switch statement mapping sectionId to component per PATTERNS.md
    - Options accessible only from Full App (sidebarpanel not in registerOn for Options)
    - workspaceRouter openStandaloneWithParams() supports page/context params for deep linking (D-37, D-38)
    - Deep links focus existing tab or create new one (D-39)
    - StandaloneApp reads query params and navigates to correct page (already partially done — extend)
    - DiagnosticsSection already built in Phase 6; rendered by OptionsPage switch
  artifacts:
    - src/components/options/ProvidersSection.tsx
    - src/components/options/ModelsSection.tsx
    - src/components/options/MCPSection.tsx
    - src/components/options/PromptsSection.tsx
    - src/components/options/SlashSection.tsx
    - src/components/options/MemorySection.tsx
    - src/components/options/ImportExportSection.tsx
    - src/components/options/FeatureFlagsSection.tsx
    - src/components/options/AddonSettingsSection.tsx
    - src/components/options/AppearanceSection.tsx
    - src/components/options/AboutSection.tsx
    - src/core/pages/OptionsPage.tsx
    - tests/components/OptionsPage.test.tsx
  key_links:
    - OptionsPage → OptionsRoot.renderSectionContent (section injection)
    - ProvidersSection → providerStore (API keys, provider configs)
    - ModelsSection → providerStore (modelEntries per provider)
    - MemorySection → MemoryDB (read-only facts display)
    - DiagnosticsSection → existing Phase 6 component (already rendered)
    - ImportExportSection → jszip (already installed)
    - PromptsSection → PromptManager + TemplateEngine (created in Plan 07-01)
    - SlashSection → SlashCommandRegistry (created in Plan 07-01)
    - workspaceRouter → StandaloneApp (deep-link query params)
    - Options accessible only via openOptionsPage / Full App nav (OPT-11)
---

<objective>
Implement all 11 Options sub-sections plus OptionsPage routing and cross-surface deep linking. The Options page is the configuration hub — Providers (with inline test connection), Models, MCP Servers, Prompt Templates, Slash Commands, Memory, Diagnostics (already built), Import/Export, Feature Flags, Add-on Settings, Appearance, and About. Plus deep linking extension to workspaceRouter (D-37 through D-40).

Purpose: Complete the settings surface for user configuration of all AI providers, models, prompts, commands, and system behavior. The Options page is the configuration backbone of the extension.
Output: 11 section components + 1 page modification + 1 router modification + 1 test file
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-full-chat-agent-notes-options-pages/07-CONTEXT.md
@.planning/phases/07-full-chat-agent-notes-options-pages/07-RESEARCH.md
@.planning/phases/07-full-chat-agent-notes-options-pages/07-PATTERNS.md

# Prior wave files
@src/core/prompts/PromptManager.ts
@src/core/prompts/TemplateEngine.ts
@src/core/prompts/builtinTemplates.ts
@src/core/slash/SlashCommandRegistry.ts

# Reference files
@src/components/options/OptionsRoot.tsx
@src/components/options/DiagnosticsSection.tsx
@src/components/options/DiagnosticsPanel.tsx
@src/core/pages/OptionsPage.tsx
@src/core/stores/providerStore.ts
@src/core/stores/themeStore.ts
@src/core/stores/workspaceStore.ts
@src/core/storage/stores/MemoryDB.ts
@src/core/routing/workspaceRouter.ts
@src/entrypoints/standalone/App.tsx
@src/entrypoints/options/App.tsx
@src/core/navigation/navigationTypes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core form-based Options sections (4 sections: Models, MCP, Memory, Appearance)</name>
  <files>src/components/options/ModelsSection.tsx, src/components/options/MCPSection.tsx, src/components/options/MemorySection.tsx, src/components/options/AppearanceSection.tsx</files>
  <read_first>
    - src/components/options/DiagnosticsSection.tsx (canonical options section pattern: data-options-section attribute, padding)
    - src/components/options/OptionsRoot.tsx (OPTIONS_SECTIONS array, section IDs, renderSectionContent)
    - src/core/stores/providerStore.ts (modelEntries, providerConfigs for ModelsSection)
    - src/core/stores/themeStore.ts (mode, setMode for AppearanceSection)
    - src/core/storage/stores/MemoryDB.ts (UserMemoryFact type, getAllUserFacts for MemorySection)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-CONTEXT.md (D-09 through D-12 for Options form patterns)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-RESEARCH.md (Options Form Implementation Strategy lines 992-1011)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-PATTERNS.md (Options Section Components pattern lines 652-726, Shared Form Pattern lines 669-705, Popconfirm deletion pattern lines 712-726)
  </read_first>
  <action>
    **Shared pattern for all standard sections (D-09):**
    Each section component is a named export function. Root div with `data-options-section="section-id"` attribute. Standard sections follow the AntD Form pattern:
    - AntD `<Form>` with `layout="horizontal"`, `labelAlign="left"`, maxWidth: 720
    - AntD `<Title level={4}>` heading
    - Form state managed by `Form.useForm()`
    - Save button with `loading` state, success/error via `App.useApp().message`
    - Collection sections use `Form.List` or table/list management

    **1. ModelsSection.tsx (OPT-02):**
    Read from `useProviderStore(s => s.modelEntries)`. Display per-provider model list with enable/disable toggles and context window override input. Uses AntD `<Table>` or grouped `<List>`. Each model row: checkbox for enabled, model name (read-only), context window numeric input, delete button with Popconfirm (D-11). "Add Model" button per provider. Save writes to providerStore.

    **2. MCPSection.tsx (OPT-03):**
    Manages external MCP server configurations. Table with columns: server name, URL/endpoint, enabled toggle, actions. Add/edit via AntD Modal with form fields (name, url, transport type). Delete via Popconfirm (D-11). Data stored in chrome.storage.local key `np_mcp_servers`.

    **3. MemorySection.tsx (OPT-06):**
    Read-only display of user memory facts from `MemoryDB.getAllUserFacts()`. AntD Table with columns: fact content, category, confidence score, source, last updated. Per RESEARCH.md, read-only in v0.1 — no edit UI (deferred to future phase). Enable/disable toggle for memory system.

    **4. AppearanceSection.tsx (no specific OPT ID — uses existing theme):**
    AntD Form with theme mode selector (light/dark/auto via Radio.Group) and density selector (compact/default). Reads/writes themeStore. Follows standard D-09 pattern.

    All sections export named functions (not default exports). Use `data-options-section="section-id"` attribute on root div per PATTERNS.md convention.
  </action>
  <acceptance_criteria>
    - All 4 section files exist in `src/components/options/`
    - `grep 'data-options-section=' src/components/options/ModelsSection.tsx` returns match
    - Same for MCPSection, MemorySection, AppearanceSection
    - `grep 'Popconfirm' src/components/options/ModelsSection.tsx` returns match (D-11 pattern)
    - `grep 'Form' src/components/options/ModelsSection.tsx` returns match
    - `grep 'np_mcp_servers' src/components/options/MCPSection.tsx` returns match
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>4 core form-based Options sections created: Models (Form.List per provider with context-window override), MCP (table with enable/disable and Modal-based CRUD), Memory (read-only facts table), Appearance (theme mode + density selector). All follow D-09 standard pattern with data-options-section attributes. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create settings and info Options sections (3 sections: Feature Flags, Addons, About)</name>
  <files>src/components/options/FeatureFlagsSection.tsx, src/components/options/AddonSettingsSection.tsx, src/components/options/AboutSection.tsx</files>
  <read_first>
    - src/components/options/DiagnosticsSection.tsx (canonical options section pattern)
    - src/components/options/OptionsRoot.tsx (OPTIONS_SECTIONS array, section IDs)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-CONTEXT.md (D-09 for standard form pattern)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-PATTERNS.md (Shared Form Pattern lines 669-705)
  </read_first>
  <action>
    **All three sections follow the standard D-09 pattern** — AntD Form with horizontal layout, left-aligned labels, maxWidth 720. Named export functions with `data-options-section` attribute on root div.

    **1. FeatureFlagsSection.tsx (OPT-09):**
    List of P2 feature toggles with AntD `<Switch>` components. Each toggle: flag name, description, enabled state. Stored in chrome.storage.local key `np_feature_flags` as `Record<string, boolean>`. Examples: enableExperimentalGraph, enableMarkdownExport.

    **2. AddonSettingsSection.tsx (OPT-10):**
    Namespaced settings per registered add-on. Reads from add-on registry (if available) or placeholder for future Phase 8 add-ons. For v0.1: renders a descriptive placeholder with AntD Empty state "No add-ons installed. Add-ons will appear here when registered." Prepares the namespacing structure (key `np_addon_settings` with addonId → settings mapping).

    **3. AboutSection.tsx (OPT-11 area):**
    Static display with package.json version, build date, credits. Reads version via `import pkg from '../../../../package.json'` or a constant. Renders as AntD `<Descriptions>` component.
  </action>
  <acceptance_criteria>
    - All 3 section files exist in `src/components/options/`
    - `grep 'data-options-section=' src/components/options/FeatureFlagsSection.tsx` returns match
    - Same for AddonSettingsSection, AboutSection
    - `grep 'np_feature_flags' src/components/options/FeatureFlagsSection.tsx` returns match
    - `grep 'np_addon_settings\|Addon' src/components/options/AddonSettingsSection.tsx` returns match
    - `grep 'package.json\|version' src/components/options/AboutSection.tsx` returns match
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>3 settings/info Options sections created: FeatureFlags (toggle list in chrome.storage.local), AddonSettings (placeholder for Phase 8), About (static version/credits display). All follow D-09 standard pattern. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Create layout exception Options sections and Slash section (4 sections: Providers, Prompts, Import/Export, Slash)</name>
  <files>src/components/options/ProvidersSection.tsx, src/components/options/PromptsSection.tsx, src/components/options/ImportExportSection.tsx, src/components/options/SlashSection.tsx</files>
  <read_first>
    - src/components/options/DiagnosticsSection.tsx (canonical section pattern)
    - src/core/stores/providerStore.ts (apiKeys, addProvider, updateProvider, testConnection pattern)
    - src/core/prompts/PromptManager.ts (promptManager singleton, CRUD API — created in Plan 07-01)
    - src/core/prompts/TemplateEngine.ts (templateEngine singleton, render/extractVariables/validate — created in Plan 07-01)
    - src/core/prompts/builtinTemplates.ts (builtinTemplates array)
    - src/core/slash/SlashCommandRegistry.ts (slashCommandRegistry, register/unregister/list — created in Plan 07-01)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-CONTEXT.md (D-09 through D-12 for pattern exceptions)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-RESEARCH.md (Options Form Implementation Strategy lines 992-1011)
  </read_first>
  <action>
    **These four sections are layout exceptions per D-12:**

    **1. ProvidersSection.tsx (OPT-01, D-12 exception):**
    AntD Form managing provider configurations. Each provider: name, provider type (select: openai/anthropic/google/ollama/openai-compatible), API key (AntD Input.Password), base URL (optional, for openai-compatible). Inline "Test Connection" button per D-10: shows AntD Button with loading state, on click calls provider adapter to validate connection. Inline result appears beneath the provider card — AntD Alert success (green) or Alert error (red) with expandable diagnostic details (error code, endpoint, response summary, copy button for error details). Add provider via "Add Provider" button, remove via Popconfirm (D-11). API keys stored via EncryptedStorage (existing providerStore pattern — no changes needed to encryption).

    **2. PromptsSection.tsx (OPT-04, D-12 exception):**
    Rich editor section for prompt templates. Layout: left side shows list of templates (builtin + custom), right side shows template editor with: name input, description textarea, template content `<Input.TextArea>` (large, monospace font), variable management panel (list of {{variables}} extracted by templateEngine.extractVariables), and preview panel showing rendered template with sample variable values via templateEngine.render(). CRUD operations: create (new template), edit, delete (Popconfirm — D-11), clone from builtin. Templates persisted via promptManager (already created in Plan 07-01). Builtin templates shown as non-editable (isBuiltin: true) with clone option.

    **3. ImportExportSection.tsx (OPT-08, D-12 exception):**
    File-based workflow, NOT a form. Layout: two AntD Cards side by side. Left: Export — scope selection checkboxes (Chat History, Notes, Memory, Settings, All), "Export" button that generates sanitized JSON/ZIP via jszip (already installed v3.10.1). Progress bar during export. Download link on completion. Right: Import — file upload area (AntD Upload with drag-and-drop), preview of import contents, "Merge" button with conflict resolution options. Import must validate JSON structure before writing to stores. Reject files with unexpected keys.

    **4. SlashSection.tsx (OPT-05):**
    AntD Form.List managing slash command to template mappings. Table/list with columns: slash command name (e.g., /write), display label, linked template (AntD Select from promptManager.getAllTemplates()), description. Add/edit via Form.List add/remove. Delete via Popconfirm (D-11). Data persisted via slashCommandRegistry (Plan 07-01). Pre-populated with built-in commands (/write, /ask, /research) from slashCommandRegistry on mount.

    Each section follows the same `data-options-section="section-id"` attribute convention and named function export pattern.
  </action>
  <acceptance_criteria>
    - All 4 section files exist in `src/components/options/`
    - `grep 'data-options-section=' src/components/options/ProvidersSection.tsx` returns match
    - Same for PromptsSection, ImportExportSection, SlashSection
    - `grep 'Test Connection\|testConnection\|test-connection' src/components/options/ProvidersSection.tsx` returns match (D-10)
    - `grep 'Input.Password' src/components/options/ProvidersSection.tsx` returns match
    - `grep 'promptManager' src/components/options/PromptsSection.tsx` returns match
    - `grep 'templateEngine' src/components/options/PromptsSection.tsx` returns match
    - `grep 'jszip\|JSZip' src/components/options/ImportExportSection.tsx` returns match
    - `grep 'slashCommandRegistry' src/components/options/SlashSection.tsx` returns match
    - `grep 'Popconfirm' src/components/options/ProvidersSection.tsx` returns match (D-11)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>4 layout exception Options sections created: Providers (encrypted key input + inline Test Connection with diagnostics per D-10), Prompts (rich editor + variable preview per D-12), Import/Export (file upload/merge workflow per D-12), Slash (Form.List command mapping). All follow D-11 Popconfirm deletion. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 4: Wire OptionsPage routing and extend workspaceRouter for deep linking</name>
  <files>src/core/pages/OptionsPage.tsx, src/core/routing/workspaceRouter.ts, tests/components/OptionsPage.test.tsx</files>
  <read_first>
    - src/core/pages/OptionsPage.tsx (current stub — switch statement on sectionId renders DiagnosticsSection or placeholder)
    - src/components/options/OptionsRoot.tsx (renderSectionContent prop, OPTIONS_SECTIONS array with section IDs)
    - src/components/options/DiagnosticsSection.tsx (existing Phase 6 section)
    - src/core/routing/workspaceRouter.ts (openStandaloneWithParams exists, openStandalone exists)
    - src/entrypoints/standalone/App.tsx (deep-link query param parsing: page, operationId already parsed; need to extend)
    - src/entrypoints/options/App.tsx (OptionRoot wiring with renderSectionContent)
    - .planning/phases/07-full-chat-agent-notes-options-pages/07-CONTEXT.md (D-37 through D-40 for deep linking)
  </read_first>
  <action>
    **A. OptionsPage.tsx (replace stub):**
    Replace the current switch statement with full section routing. The OptionsPage receives `sectionId` prop from OptionsRoot.renderSectionContent callback (wired in options/App.tsx). Update the switch to map all 11 section IDs to their components:

    ```typescript
    switch (sectionId) {
      case 'providers': return <ProvidersSection />;
      case 'models': return <ModelsSection />;
      case 'mcp': return <MCPSection />;
      case 'prompts': return <PromptsSection />;
      case 'slash': return <SlashSection />;
      case 'memory': return <MemorySection />;
      case 'appearance': return <AppearanceSection />;
      case 'diagnostics': return <DiagnosticsSection />; // existing Phase 6
      case 'import-export': return <ImportExportSection />;
      case 'feature-flags': return <FeatureFlagsSection />;
      case 'addons': return <AddonSettingsSection />;
      case 'about': return <AboutSection />;
      default: return <DefaultSectionPlaceholder id={sectionId} />;
    }
    ```

    Import all 11 section components. The `DefaultSectionPlaceholder` renders the existing fallback Card with sectionId display. Remove the old placeholder card and "Section: {sectionId}" text from the default case of the switch.

    **B. workspaceRouter.ts (extend for deep linking D-37 through D-40):**
    The `openStandaloneWithParams()` method already exists. Verify it works correctly for Phase 7 deep links:
    - Chat deep link: `openStandaloneWithParams({ page: 'chat', conversationId: 'abc123' })` → opens `standalone.html?page=chat&conversationId=abc123`
    - Agent deep link: `{ page: 'agent', conversationId: 'abc123' }`
    - Notes deep link: `{ page: 'notes', noteId: 'def456' }`
    - Options deep link: `{ page: 'options', section: 'providers' }` (note: Options page might not be accessible through standalone — confirm whether Options has its own entry point in standalone page registry. If not, use `chrome.runtime.openOptionsPage()`)
    - Diagnostics deep link: `{ page: 'diagnostics', operationId: 'ghi789' }` (already partially supported by existing diagnostics deep-link pattern)

    **C. StandaloneApp.tsx deep-link extension (D-38, D-39):**
    The existing deep-link code reads `page` and `operationId` params. Extend to also read: `conversationId` and `noteId`. These context params should be stored in workspaceStore or passed to the page component:
    - `conversationId`: set `workspaceStore.setConversationId(conversationId)` on navigation to Chat/Agent page
    - `noteId`: navigate to Notes page and select the note with matching ID
    - Clean params from URL after consuming (existing pattern already does this)

    **D. Focus existing tab + navigate (D-39):**
    The existing `openOrFocusTab` function in workspaceRouter.ts already implements this: queries existing tabs matching the base URL, updates/focuses if found, creates new tab if not. This satisfies D-39 without changes.

    **E. No special back indicator (D-40):**
    No implementation needed — D-40 is a design decision to NOT add back buttons. The existing "Open Side Panel" action in the sider header (already present) serves as the return path.

    **Testing:** `tests/components/OptionsPage.test.tsx` renders OptionsPage with various sectionId props, verifies correct section component renders for each sectionId. Tests deep-link query param parsing.

    **Surface restriction (OPT-11):** Verify Options page is NOT registered on Side Panel. Check `registerNowPilotCorePages.ts` — if 'options' is registered on 'sidepanel', remove it. Options should only be accessible via the Full App nav sider or `chrome.runtime.openOptionsPage()`.
  </action>
  <acceptance_criteria>
    - `src/core/pages/OptionsPage.tsx` has switch statement with all 11 section cases
    - `grep 'ProvidersSection' src/core/pages/OptionsPage.tsx` returns match
    - `grep 'AboutSection' src/core/pages/OptionsPage.tsx` returns match
    - `grep 'DiagnosticsSection' src/core/pages/OptionsPage.tsx` returns match
    - `src/core/routing/workspaceRouter.ts` deep link verification — existing `openStandaloneWithParams` is sufficient
    - `grep 'conversationId' src/entrypoints/standalone/App.tsx` returns match (if extending)
    - `tests/components/OptionsPage.test.tsx` exists with ≥3 tests passing
    - Options not registered on Side Panel — verify `registerOn` does not include 'sidepanel' for options
    - All tests pass: `npx vitest run tests/components/OptionsPage.test.tsx`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/components/OptionsPage.test.tsx</automated>
  </verify>
  <done>OptionsPage routes all 11 sections via switch statement. workspaceRouter deep linking extended for conversationId/noteId context parameters. StandaloneApp reads new params and navigates. Options restricted to Full App only. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → ProvidersSection API key | Encrypted via EncryptedStorage (AES-GCM-256, existing Phase 2) |
| User input → Prompt template content | Template strings with {{variables}} — no code execution |
| File upload → ImportExportSection | Untrusted JSON/ZIP files; validated before writing to stores |
| chrome.storage.local → Options sections | Configuration data (np_mcp_servers, np_feature_flags, etc.) trusted storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-18 | Information Disclosure | ProvidersSection API key display | medium | mitigate | API keys shown in Input.Password (masked). Encrypted at rest via EncryptedStorage. No plaintext in DOM. |
| T-07-19 | Tampering | ImportExportSection file upload | high | mitigate | Import files validated: must be valid JSON, must match expected schema structure (known keys only). Non-JSON content rejected. Unknown keys rejected. File size limited to 10MB. |
| T-07-20 | Spoofing | Prompt template variables | low | accept | TemplateEngine substitutes registered variables only. Unknown {{vars}} rendered as literal text — no injection. |
| T-07-21 | Information Disclosure | Deep link URL params | low | accept | conversationId/noteId in URL query params — IDs are UUIDs, not secrets. No API keys or tokens in URL. |
| T-07-22 | Information Disclosure | Export debug bundle | low | mitigate | Export uses existing TraceRedactor (Phase 6) to sanitize trace data. No API keys or raw bodies in export. |
</threat_model>

<verification>
Component tests: `npx vitest run tests/components/OptionsPage.test.tsx`
Full suite: `npx vitest run`
TypeScript: `npx tsc --noEmit`
Accessibility: Options page only in Full App — manually verify Side Panel nav does not show Options.
</verification>

<success_criteria>
All 11 Options sections render with functional forms. Providers has encrypted key input + inline test connection. Prompts has rich editor + variable preview. Import/Export has file upload/merge workflow. All delete actions use Popconfirm. OptionsPage routes all sections. workspaceRouter supports deep linking with conversation/note context. Options restricted to Full App only. All tests pass.
</success_criteria>

<output>
Create `.planning/phases/07-full-chat-agent-notes-options-pages/07-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `ProvidersSection` | Component | src/components/options/ProvidersSection.tsx |
| `ModelsSection` | Component | src/components/options/ModelsSection.tsx |
| `MCPSection` | Component | src/components/options/MCPSection.tsx |
| `PromptsSection` | Component | src/components/options/PromptsSection.tsx |
| `SlashSection` | Component | src/components/options/SlashSection.tsx |
| `MemorySection` | Component | src/components/options/MemorySection.tsx |
| `ImportExportSection` | Component | src/components/options/ImportExportSection.tsx |
| `FeatureFlagsSection` | Component | src/components/options/FeatureFlagsSection.tsx |
| `AddonSettingsSection` | Component | src/components/options/AddonSettingsSection.tsx |
| `AppearanceSection` | Component | src/components/options/AppearanceSection.tsx |
| `AboutSection` | Component | src/components/options/AboutSection.tsx |
| `OptionsPage` (replaced stub) | Component | src/core/pages/OptionsPage.tsx |
