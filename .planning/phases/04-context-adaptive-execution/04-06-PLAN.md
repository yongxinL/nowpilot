---
phase: 04-context-adaptive-execution
plan: 06
type: execute
wave: 4
depends_on: ["04-04", "04-05"]
files_modified:
  - src/components/pages/useStreamingLLM.ts
  - src/core/ai/AgentOrchestrator.ts
  - src/core/ai/contextHelper.ts
  - src/core/i18n/strings.ts
  - tests/core/ai/AgentOrchestrator.test.ts
  - tests/components/pages/useStreamingLLM.test.tsx
  - tests/core/context/ContextOptimizer.test.ts
  - tests/core/ai/persona/PersonaInjector.test.ts
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04]
must_haves:
  truths:
    - "src/components/pages/useStreamingLLM.ts (MODIFY — the ONE behavioral-change surface, D-04-07) replaces the hardcoded medium/16K·1K buildOptimizedContext block (L131-141) with per-stage optimization: resolve both stages upfront via the StageResolver closure, read `modelContextWindow` from each StageInvocation, run ContextOptimizer.optimize once per stage (planner + renderer), pass the planner context as runAgentTurn's `context`, `tier: capsForTier(plannerCtx.tier)` (D-04-05 loop caps from the PLANNER-stage tier), and `contextForStage` returning the per-stage pack. The old DEFAULT_* budget constants become the D-04-04 documented pre-resolution fallback (never the primary source), kept internally consistent: `DEFAULT_CONTEXT_TIER = 'medium'` with `FALLBACK_MODEL_CONTEXT_WINDOW = 131_072` — `classifyModelContext(131_072) === 'medium'` — so the fallback window's derived tier always equals the retained tier constant (a 16_384 fallback would derive 'small' and contradict it)."
    - "src/core/ai/AgentOrchestrator.ts (MODIFY) gains the D-04-05 input-only seam `contextForStage?: (stage: 'planner' | 'renderer') => OptimizedContext` on AgentTurnInput (mirrors invocation/onStreamDelta — direct call, never an event bus, L1); planOnce resolves `contextForStage('planner') ?? input.context` as the section base; RendererService.render at finish resolves the renderer-stage context the same way. Existing call sites stay green via the default."
    - "The estimateTokens import at AgentOrchestrator.ts L33 re-points to @/core/context/TokenBudget (Pitfall 1 — the SAME wave as the contextHelper deletion); usage L278 stays untouched."
    - "src/core/ai/contextHelper.ts is DELETED (D-04-08) — zero imports of it remain anywhere in src/ OR tests/ (grep gate); its packing/counting migrated to ContextPack/TokenBudget (04-01/04-02) and its builder to ContextOptimizer (04-04)."
    - "src/core/i18n/strings.ts (MODIFY) gains `chat.messageTooLong` (D-04-15 verbatim STR copy); useStreamingLLM maps a caught ContextTooLargeError (isContextTooLargeError) to the failed state with that surface BEFORE classifyProviderError — never silently truncates user input (P4-10)."
    - "tests updated: useStreamingLLM.test.tsx gains per-stage (two distinct optimizer contexts, planner-tier caps) + CONTEXT_TOO_LARGE→failed + drop-in-identity regression tests; AgentOrchestrator.test.ts gains a contextForStage seam test (planner vs renderer resolution + default fallback); ContextOptimizer.test.ts (04-04) replaces its live `buildOptimizedContext` import with the hardcoded Phase-3 snapshot (drop-in regression survives the deletion); PersonaInjector.test.ts re-points estimateTokens to TokenBudget and its buildOptimizedContext call sites to ContextOptimizer.optimize."
  artifacts:
    - "src/components/pages/useStreamingLLM.ts"
    - "src/core/ai/AgentOrchestrator.ts"
    - "src/core/i18n/strings.ts"
    - "src/core/ai/contextHelper.ts (deleted)"
    - "tests/core/ai/AgentOrchestrator.test.ts"
    - "tests/components/pages/useStreamingLLM.test.tsx"
    - "tests/core/context/ContextOptimizer.test.ts (import replaced with snapshot)"
    - "tests/core/ai/persona/PersonaInjector.test.ts (import re-pointed)"
  key_links:
    - "Pitfall 1 (WAVE TRAP): the TokenBudget import swap + hook swap + contextHelper deletion all land in THIS wave — tsc stays green at every commit; grep zero imports of the deleted module across src/ and tests/."
    - "The hook reads StageInvocation.modelContextWindow (04-05) — the mandated ProviderRouter-before-packs dependency is satisfied."
    - "Golden Rule 3 (Pitfall 7): the hook imports ContextOptimizer + capsForTier — it NEVER assembles prompts or computes budgets; compact prompt text lives only in src/core/prompts/index.ts (04-04)."
    - "R-3: nothing in this plan touches the background SW or content scripts."
  flagged_assumptions:
    - "CTX-01 [unresolved — spec-less probe #1110]: per-stage budget enforcement in the real runtime lands here; the probe edge seals at the phase gate."
    - "CTX-02 [unresolved — spec-less probe #1110]: no runAgentTurn-level consumer — the typed seam is optimizer-side (04-04); page/state-change triggers arrive with Phase 4a/7 (deferred, D-04-02)."
    - "CTX-03 [unresolved — spec-less probe #1110]: minimal mode reaches the runtime via the optimizer's compact-prompt selection (04-04) — this plan's hook passes through per-stage optimizer outputs unchanged."
    - "CTX-04 [unresolved — spec-less probe #1110]: the CONTEXT_TOO_LARGE→failed mapping is the honest surface; never truncation."
    - "A11 [04-04, ASSUMED]: conversationId = 'default' constant in the hook's optimizer inputs (no conversation store — Phase 7)."
    - "Loop-caps derivation [D-04-05, locked]: capsForTier(plannerCtx.tier) — planner and renderer may resolve DIFFERENT tiers; the planner tier governs the loop caps, the renderer carries its own budget."
  prohibitions:
    - "No prompt assembly in the hook (GR-3, Pitfall 7): no PROMPTS import, no compact-constant text, no budget math in useStreamingLLM.ts — only ContextOptimizer/capsForTier calls."
    - "No contextHelper remnants: zero imports of the deleted module anywhere in src/ or tests/ (grep gate — the literal import specifier must appear nowhere)."
    - "No event bus for contextForStage (L1 — direct input-only callback, mirrors invocation/onStreamDelta)."
    - "No silent truncation of user input anywhere in the rewire (D-04-15, P4-10)."
    - "No changes to PlannerService/RendererService/ExecutorService (import-swap-only consumers — D-04-07)."
    - "No new runtime packages (R-9)."
---

<!-- 04-06 (2026-08-12): Wave-4 cutover — the Pitfall-1 wave. The hook rewire
     (per-stage optimizer calls, D-04-04/05/07), the orchestrator's contextForStage
     input-only seam, the STR.messageTooLong surface (D-04-15), the estimateTokens
     import re-point, and the contextHelper DELETION (D-04-08) land TOGETHER so tsc
     stays green at every commit. -->

<objective>
Cut the Phase-3 wiring over to the Phase-4 stack: rewire `useStreamingLLM` to per-stage optimization (D-04-04/05), add the `contextForStage` input-only seam to `AgentOrchestrator` (D-04-05), surface the honest CONTEXT_TOO_LARGE terminal (D-04-15), and DELETE `contextHelper.ts` in the SAME wave as its last consumer migrations (D-04-08, Pitfall 1). Planner/Renderer stay import-swap-only consumers (D-04-07).

Purpose: This is the phase's only behavioral-change surface. It makes real models actually budgeted as their tier — a tiny local model is budgeted tiny, loop caps come from the planner-stage tier, and an over-cap message fails honestly instead of truncating. The drop-in constraint (byte-identical default path) is what keeps prompt-cache wins alive.

Output: the rewired hook, the orchestrator seam, the STR copy, the deleted contextHelper, and the extended hook/orchestrator suites.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@src/components/pages/useStreamingLLM.ts (the rewire target — L28 import, L131-141 build block, L144-151 resolver, L186-207 error mapping)
@src/core/ai/AgentOrchestrator.ts (L33 estimateTokens import, L82-118 AgentTurnInput, planOnce L384-418, finish L337-348 renderer call)
@src/core/context/ContextOptimizer.ts (04-04 — optimize + isContextTooLargeError)
@src/core/i18n/strings.ts (chat.* block L7-24 — contextReduced precedent L12)
@src/core/ai/contextHelper.ts (the deletion target)
@.planning/phases/04-context-adaptive-execution/04-PATTERNS.md L307-395 (hook + orchestrator analogs) + L356-395 (rewire sketch)
</context>

## Artifacts this phase produces

- **MODIFY** `src/components/pages/useStreamingLLM.ts`: per-stage optimizer calls, `contextForStage` threading, `capsForTier(plannerCtx.tier)`, CONTEXT_TOO_LARGE→failed mapping; DEFAULT_* constants → documented pre-resolution fallback.
- **MODIFY** `src/core/ai/AgentOrchestrator.ts`: `AgentTurnInput.contextForStage?` seam; planOnce/finish per-stage context resolution; estimateTokens import → TokenBudget.
- **MODIFY** `src/core/i18n/strings.ts`: `chat.messageTooLong` (verbatim).
- **DELETE** `src/core/ai/contextHelper.ts`.
- **MODIFY** `tests/core/ai/AgentOrchestrator.test.ts` (contextForStage seam test) + `tests/components/pages/useStreamingLLM.test.tsx` (per-stage / terminal / drop-in tests).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the contextForStage seam + re-point estimateTokens in AgentOrchestrator</name>
  <files>src/core/ai/AgentOrchestrator.ts, tests/core/ai/AgentOrchestrator.test.ts</files>
  <read_first>src/core/ai/AgentOrchestrator.ts L82-118 (AgentTurnInput — add after `invocation`), L384-418 (planOnce — the planner context resolution), L304-363 (finish → RendererService.render context); src/core/context/TokenBudget.ts (04-01 — the new estimateTokens home)</read_first>
  <behavior>
    - with no contextForStage supplied, planOnce and finish behave exactly as today (default → input.context) — all existing tests stay green
    - with contextForStage supplied, planOnce's section base is contextForStage('planner') and the renderer receives contextForStage('renderer')
    - estimateTokens resolves from TokenBudget (no import from the deleted module)
  </behavior>
  <action>Add to AgentTurnInput (after `invocation`, documenting D-04-05): `contextForStage?: (stage: 'planner' | 'renderer') => OptimizedContext` — input-only seam, defaults keep every existing call site green (RESEARCH Pattern 1). In planOnce, resolve the base sections from `input.contextForStage ? input.contextForStage('planner').sections : input.context.sections` (replan sections still append). In finish, pass `context: input.contextForStage ? input.contextForStage('renderer') : input.context` to RendererService.render. Re-point L33: `import { estimateTokens } from '@/core/context/TokenBudget'` (usage L278 untouched). Add the seam test: (a) default path — no contextForStage, planner/renderer receive input.context (existing suite covers); (b) with contextForStage — planner stage's sections base equals contextForStage('planner'), renderer invocation carries contextForStage('renderer') (assert via the mock RendererService call args).</action>
  <acceptance_criteria>
    - AgentTurnInput.contextForStage exists with the exact signature; the interface comment cites D-04-05 and L1 (no event bus).
    - No import of the deleted context-helper module remains in AgentOrchestrator.ts (grep the import specifier — zero matches).
    - `pnpm vitest run tests/core/ai/AgentOrchestrator.test.ts` green incl. the new seam test.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/ai/AgentOrchestrator.test.ts && pnpm typecheck</automated>
  </verify>
  <done>Seam + import re-point in place; default behavior unchanged (drop-in); seam behavior tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire the hook to per-stage optimization + honest CONTEXT_TOO_LARGE mapping</name>
  <files>src/components/pages/useStreamingLLM.ts, src/core/i18n/strings.ts, tests/components/pages/useStreamingLLM.test.tsx</files>
  <read_first>src/components/pages/useStreamingLLM.ts L28 (import swap), L44-48 (DEFAULT_* constants), L131-151 (the block to replace), L152-160 (runAgentTurn args), L186-207 (catch — add the terminal mapping); src/core/context/ContextOptimizer.ts (optimize signature + isContextTooLargeError); src/core/i18n/strings.ts L7-24 (chat.* — add beside contextReduced); tests/components/pages/useStreamingLLM.test.tsx L110-126 (the send-path test — the stale `tier: 'medium'` assertion at L122 breaks at THIS task boundary: the rewire derives the tier from the hoisted mock's modelContextWindow, updated to 200_000 in 04-05 T2, so classifyModelContext(200_000) = 'large'); .planning/phases/04-context-adaptive-execution/04-PATTERNS.md L356-395</read_first>
  <behavior>
    - the hook calls ContextOptimizer.optimize TWICE per send (planner + renderer), each with its own stage's modelContextWindow from the invocation
    - runAgentTurn receives tier: capsForTier(plannerCtx.tier) and contextForStage returning the per-stage pack
    - a thrown ContextTooLargeError lands the surface state 'failed' with the STR.chat.messageTooLong surface (before classifyProviderError)
    - the existing send-path test stays green at THIS boundary: input.context.tier equals the optimizer-derived tier from the hoisted mock window (200_000 → 'large'), and input.tier equals the mocked capsForTier constant (L43)
  </behavior>
  <action>Replace the buildOptimizedContext block (L131-141): drop the import of the Phase-3 builder; import `ContextOptimizer` + `isContextTooLargeError` from '@/core/context/ContextOptimizer'; keep `capsForTier` from AgentOrchestrator. Build a shared optimizer input base { operationId, userInput: trimmed, personaBlock, conversationId: 'default', workspaceId, activeSurface, toolSchemaRefs: [], memoryHints: [], preferences: prefs, pageContext: undefined } and run optimize per stage with the stage's { model: inv.model.modelId, modelContextWindow: inv.modelContextWindow, stage }. Thread `context: plannerCtx`, `tier: capsForTier(plannerCtx.tier)`, `contextForStage: (stage) => (stage === 'planner' ? plannerCtx : rendererCtx)` into runAgentTurn. Repurpose L45-48: keep `DEFAULT_CONTEXT_TIER: ModelContextTier = 'medium'` and add `FALLBACK_MODEL_CONTEXT_WINDOW = 131_072` documented as the D-04-04 pre-resolution fallback ONLY — the window value is chosen so the constant set is internally consistent: `classifyModelContext(131_072) === 'medium' === DEFAULT_CONTEXT_TIER` (a 16_384 fallback would derive 'small' and contradict the retained 'medium' constant — never have the fallback window and the fallback tier disagree; budgets come from TokenBudget now — DEFAULT_INPUT/OUTPUT_BUDGET are removed). IN THE SAME TASK, update the stale test assertions in tests/components/pages/useStreamingLLM.test.tsx L110-126 (the suite goes red at THIS boundary if left stale): the context is now OPTIMIZER-built, not contextHelper-built — update the test title + L120-121 comment accordingly, and change L122's `tier: 'medium'` expectation to the tier derived from the hoisted mock's modelContextWindow (200_000, added in 04-05 T2) → `classifyModelContext(200_000)` = 'large', with a comment tying the expectation to that mock constant; leave L124 as-is (the mocked capsForTier constant at L43 returns the same shape regardless of tier) or parametrize both assertions on the mock window so they track a future window change. The per-stage/terminal/drop-in EXTENSIONS remain Task 3's scope — Task 2 only repairs the stale assertions the rewire breaks. In the catch, BEFORE classifyProviderError: `if (isContextTooLargeError(e)) { setState({ state: 'failed', operationId }); return; }` — comment: D-04-15 honest terminal, never truncate. In strings.ts add `chat.messageTooLong: 'This message is too long for the selected model.'` (verbatim, Appendix B style). Golden Rule 3: no PROMPTS import, no budget math in the hook (Pitfall 7).</action>
  <acceptance_criteria>
    - useStreamingLLM.ts contains zero references to the deleted builder module; exactly two optimize() calls in send(); no PROMPTS import.
    - The catch maps isContextTooLargeError before any other classification; STR.chat.messageTooLong exists verbatim.
    - `FALLBACK_MODEL_CONTEXT_WINDOW = 131_072` and `DEFAULT_CONTEXT_TIER = 'medium'` are consistent: `classifyModelContext(FALLBACK_MODEL_CONTEXT_WINDOW) === DEFAULT_CONTEXT_TIER` (asserted by a one-line vitest or verified by inspection — the constants cannot disagree).
    - `pnpm vitest run tests/components/pages/useStreamingLLM.test.tsx` green at THIS boundary with the REPAIRED assertions (tier 'large' from the 200_000 mock window); the per-stage/terminal/drop-in extensions land in Task 3.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/components/pages/useStreamingLLM.test.tsx && pnpm typecheck</automated>
  </verify>
  <done>Hook rewired to per-stage optimization with the honest terminal; STR copy added; stale send-path assertions repaired (tier derived from the mock window → 'large'); default-path bytes preserved (drop-in regression lands in Task 3's extension).</done>
</task>

<task type="auto">
  <name>Task 3: Delete contextHelper + re-point its test imports + extend the hook tests + zero-import gate</name>
  <files>src/core/ai/contextHelper.ts, tests/components/pages/useStreamingLLM.test.tsx, tests/core/context/ContextOptimizer.test.ts, tests/core/ai/persona/PersonaInjector.test.ts</files>
  <read_first>tests/components/pages/useStreamingLLM.test.tsx (whole file — extend, don't rewrite; hoisted mock updated in 04-05); tests/core/context/ContextOptimizer.test.ts (04-04 Task 3 — the drop-in-identity test imports buildOptimizedContext; replace that import with the hardcoded snapshot BEFORE deleting the module); tests/core/ai/persona/PersonaInjector.test.ts (L30 imports buildOptimizedContext + estimateTokens from the deleted module); .planning/phases/04-context-adaptive-execution/04-PATTERNS.md L518-548 (test extension notes)</read_first>
  <behavior>
    - the deleted module's import specifier appears in ZERO src/ or tests/ files (grep gate)
    - ContextOptimizer.test.ts's drop-in-identity comparison no longer imports the deleted module — it compares optimize's default path against a hardcoded Phase-3 [SYSTEM] snapshot (persona-block-only shape), preserving the byte-identity regression
    - PersonaInjector.test.ts imports estimateTokens from TokenBudget and calls ContextOptimizer.optimize instead of buildOptimizedContext
    - useStreamingLLM.test.tsx proves: (a) two optimizer contexts with per-stage windows/tiers; (b) tier: capsForTier(planner tier) passed to runAgentTurn; (c) ContextTooLargeError → 'failed' state; (d) drop-in identity — the default path section bytes equal the pre-04-06 snapshot
  </behavior>
  <action>Update the two test files BEFORE the deletion (the drop-in identity comparison in ContextOptimizer.test.ts and the persona-injection context builder in PersonaInjector.test.ts both import the module being deleted): (1) ContextOptimizer.test.ts — capture the Phase-3 buildOptimizedContext output for the fixed test input (run it once while the module still exists), hardcode that byte-identical [SYSTEM] + section snapshot as a local literal, and replace the live import with the snapshot comparison (assert optimize's default-path sections deep-equal the snapshot; keep the comment citing D-04-07 cache-stability + the 04-04 Task-3 handoff); (2) PersonaInjector.test.ts — re-point `estimateTokens` to `@/core/context/TokenBudget` and migrate the buildOptimizedContext({ ...baseInput, ... }) call sites to `optimize({ modelContextWindow: FIXED_MODEL_CONTEXT_WINDOWS[FIXED_MODEL], personaBlock, stage: 'planner', ...same inputs }, ...)` from '@/core/context/ContextOptimizer' (drop-in: the optimizer's default path is byte-identical, so the §2.3 determinism/injection-safety assertions keep their meaning; the fixture window comes from tests/fixtures/optimizedContext). Then delete src/core/ai/contextHelper.ts (git rm). Run the grep gate over src/ AND tests/: the module's import specifier must match zero files (the migrations in Tasks 1-2 + this task re-pointed every consumer — this is the Pitfall-1 seal). Extend useStreamingLLM.test.tsx: (a) assert createStageInvocation called twice and optimize produced distinct planner/renderer contexts (the mock windows from 04-05: planner 200_000, renderer 200_000 → large; override a window to tiny in one test to prove per-stage tier divergence — planner small/renderer large → capsForTier(planner tier) governs runAgentTurn.tier); (b) runAgentTurnMock receives tier matching capsForTier(plannerCtx.tier) + contextForStage; (c) a rejected optimize (over-cap user input) → state 'failed' (not 'offline', not 'completed'); (d) drop-in regression — compare the default-path [SYSTEM] section text against a hardcoded snapshot of the Phase-3 persona-block-only shape. Run the FULL context + AI + hook suites.</action>
  <acceptance_criteria>
    - `src/core/ai/contextHelper.ts` no longer exists (glob).
    - Grep gate: zero src/ or tests/ files import the deleted module (exact specifier match, zero counts) — includes ContextOptimizer.test.ts and PersonaInjector.test.ts.
    - `pnpm vitest run tests/core/context tests/core/ai tests/components/pages tests/fixtures` green — the drop-in regression still proves byte-identity via the hardcoded snapshot.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context tests/core/ai tests/components/pages tests/fixtures && pnpm typecheck</automated>
  </verify>
  <done>contextHelper deleted with zero survivors; per-stage/terminal/drop-in hook tests green — the Pitfall-1 wave is sealed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → hook → optimizer | The rewire moves every turn's context construction onto the per-stage optimizer path — the honest terminal is the only lossy outcome |
| stage contexts → orchestrator | contextForStage is a direct-call input — the orchestrator must never be able to assemble or mutate prompts |
| optimizer errors → UI | A typed ContextTooLargeError crosses into the surface state machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-25 | DoS | oversized user input at runtime | high | mitigate | isContextTooLargeError → honest 'failed' + messageTooLong surface (D-04-15) — never a truncated prompt sent; test pins the mapping (success criterion 2) |
| T-04-26 | Tampering | contextForStage seam | medium | mitigate | Input-only typed callback (L1, mirrors invocation/onStreamDelta); the orchestrator resolves contexts, never assembles; GR-3 keeps prompt assembly in core builders only (Pitfall 7) |
| T-04-27 | Elevation of Privilege | loop caps from planner tier | medium | mitigate | capsForTier(plannerCtx.tier) (D-04-05) — a tiny planner caps the loop (1/1, mcpChaining false); minimal-mode tool reduction from 04-04 bounds the surface |
| T-04-28 | Information Disclosure | hook debugLog payloads | medium | mitigate | CONTEXT_TOO_LARGE path returns BEFORE debugLog noise (no section/user text logged); other failures keep the R-10 redaction contract (error objects only) |
| T-04-29 | Tampering | contextHelper deletion | low | mitigate | Pitfall-1 wave ordering: import re-points land in the SAME wave as the deletion; grep gate zero-survivors + tsc green seal it |
| T-04-30 | Tampering | npm installs | low | mitigate | No packages installed (R-9) |
</threat_model>

<verification>
- `pnpm vitest run tests/core/context tests/core/ai tests/components/pages tests/fixtures` — the full Phase-4 surface.
- `pnpm typecheck` — zero errors at every task boundary (Pitfall 1: no commit where the deletion outlives a consumer).
- Grep gate: zero src/ or tests/ imports of the deleted module; no `.slice(` on user_input in the rewire; no PROMPTS import in the hook.
</verification>

<success_criteria>
- Hook performs per-stage optimization; loop caps derive from the planner-stage tier; renderer carries its own budget.
- CONTEXT_TOO_LARGE maps to a failed state with the messageTooLong STR surface — honest terminal, never truncation.
- contextForStage seam is input-only and default-compatible; contextHelper deleted with zero survivors (Pitfall 1 sealed).
- All CTX-01..04 behaviors reach the runtime; default-path bytes preserved (cache-stability regression green).
</success_criteria>

<output>
Create `.planning/phases/04-context-adaptive-execution/04-06-SUMMARY.md` when done.
</output>
