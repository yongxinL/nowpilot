---
phase: 04-context-adaptive-execution
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/core/context/ContextProvenanceManifest.ts
  - src/core/ai/contextHelper.ts
  - tests/fixtures/optimizedContext.ts
  - tests/core/context/ContextProvenanceManifest.test.ts
autonomous: true
requirements: [CTX-01]
must_haves:
  truths:
    - "src/core/context/ContextProvenanceManifest.ts (extended IN PLACE, P-3b seed never re-declared — R-1) gains the D-04-17 fields: `tier: ModelContextTier`, `model: string`, `window: number`, `counterMethod: 'native' | 'heuristic'`, `stepsFired: Array<...>` (the §2.4 ladder steps that fired), keeping the existing sections[]/totalTokens/minimalMode/workspaceId/activeSurface contract incl. the 03a-01 'tool_result' kind member."
    - "A co-located `ContextProvenanceManifestSchema` (zod, GR-4 — ProviderConfigSchema precedent in src/core/ai/types.ts L89-103) validates the manifest at the public boundary; `sections[].kind` is a z.enum mirroring the FULL PromptSection kind union incl. 'tool_result'."
    - "tests/core/context/ContextProvenanceManifest.test.ts (NEW) contains the D-04-18 lockstep guard: the manifest kind enum's options set deep-equals the PromptSection kind union (runtime union-member parity, 03a-01 precedent) — drift between the two unions fails the test."
    - "tests/fixtures/optimizedContext.ts provenance builder is extended to emit the new required fields (deterministic constants) so every existing consumer fixture compiles and deep-equals."
    - "src/core/ai/contextHelper.ts (the surviving Phase-3 producer, deletion target 04-06) stamps deterministic placeholder manifest values in its provenance literal so the required fields never break `pnpm typecheck` at any task boundary until the 04-06 deletion."
  artifacts:
    - "src/core/context/ContextProvenanceManifest.ts"
    - "src/core/ai/contextHelper.ts"
    - "tests/fixtures/optimizedContext.ts"
    - "tests/core/context/ContextProvenanceManifest.test.ts"
  key_links:
    - "The manifest is stamped on EVERY OptimizedContext by ContextOptimizer (04-04) and Zod-validated there — success criterion 4 ('Every OptimizedContext carries a ContextProvenanceManifest') is satisfied by this contract + 04-04."
    - "The lockstep guard prevents the 03a-01 drift failure mode: a new PromptSection kind landing without a manifest-schema update must fail CI (D-04-18)."
    - "Manifest lifetime: in-memory per-turn, redacted via TraceRedactor if ever logged, NOT persisted (D-04-19 — durable trace is Phase 6 AITransactionLog)."
  flagged_assumptions:
    - "CTX-01 [unresolved — spec-less probe #1110]: the manifest contract half of the §2.3 output is implemented here; stamping/validation completes in 04-04."
    - "D-04-17 field naming [planner discretion]: `tier`/`model`/`window`/`counterMethod`/`stepsFired` follow the D-04-17 enumeration verbatim (tier, model, window, per-section token counts, steps fired, counter-method-used) — no extra fields beyond the locked list are invented."
    - "stepsFired value set [planner discretion]: mirrors the 8 §2.4 ladder steps (drop-debug, drop-secondary, summarise-history, compress-page, trim-tools, reduce-topk, minimal-mode, too-large) — same vocabulary as ContextCompressor.LADDER_STEPS (04-02) so the manifest and the ladder stay in lockstep."
    - "counterMethod default: 'heuristic' in P4 (the provider-native counter does not exist in ai@4.3.19 — verified) — the enum reserves 'native' for a future SDK surface (D-04-10)."
  prohibitions:
    - "No second kind declaration: the manifest kind union must not be re-declared by hand — the lockstep test + Zod enum ARE the guard (D-04-18; 03a-01 'tool_result' precedent)."
    - "No manifest persistence and no raw-body logging (D-04-19, R-10)."
    - "No re-declaration of the seeded interface fields (R-1, P-3b)."
    - "No new runtime packages (R-9 — zod is already in the approved stack)."
---

<!-- 04-03 (2026-08-12): Wave-2 provenance contract. The seeded ContextProvenanceManifest
     (P-3b) is extended IN PLACE with the D-04-17 fields + a co-located Zod schema;
     the D-04-18 kind-lockstep guard ships as the runtime union-parity test (03a-01
     precedent). BOTH surviving producers (the D-08 fixture builder + the Phase-3
     contextHelper provenance literal) are synced IN THE SAME TASK so the REQUIRED
     fields never break typecheck — contextHelper gets placeholder stamps because it
     is DELETED in 04-06 (D-04-08). Every later plan's fixture relies on this
     extended shape. -->

<objective>
Extend the seeded `ContextProvenanceManifest` with the full D-04-17 enumeration (tier, model, window, per-section token counts, §2.4 steps fired, counter-method) plus a co-located Zod boundary schema (GR-4), and pin the D-04-18 kind-lockstep invariant with the runtime union-parity test. Sync the D-08 fixture's provenance builder so the new required fields keep every existing consumer green.

Purpose: Success criterion 4 ("Every OptimizedContext carries a ContextProvenanceManifest") depends on a manifest contract rich enough to be a faithful provenance record (PromptInspector-ready) and a schema that validates at the public boundary. The lockstep guard closes the 03a-01 drift failure mode permanently.

Output: extended `ContextProvenanceManifest.ts`, `ContextProvenanceManifestSchema`, the lockstep-guard test, and the fixture sync.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md (§2.6 L516-534 — the manifest contract verbatim)
@src/core/context/ContextProvenanceManifest.ts (the 30-line P-3b seed)
@src/core/ai/types.ts (PromptSection kind union L134-148 — the lockstep source; ProviderConfigSchema L89-103 — the co-located-schema precedent)
@tests/fixtures/optimizedContext.ts (provenance builder L144-155 — the sync target)
@.planning/phases/04-context-adaptive-execution/04-PATTERNS.md L77-108 (manifest analog + notes)
</context>

## Artifacts this phase produces

- **EXTEND IN PLACE** `src/core/context/ContextProvenanceManifest.ts`: `tier`, `model`, `window`, `counterMethod`, `stepsFired` fields + co-located `ContextProvenanceManifestSchema` (zod).
- **STAMP (placeholder)** `src/core/ai/contextHelper.ts`: provenance literal gains the required fields with deterministic placeholder values (04-06 deletion target — D-04-08).
- **EXTEND** `tests/fixtures/optimizedContext.ts`: provenance builder emits the new required fields.
- **NEW** `tests/core/context/ContextProvenanceManifest.test.ts`: Zod round-trip + kind-lockstep guard.

<tasks>

<task type="auto">
  <name>Task 1: Extend the manifest IN PLACE + co-locate the Zod schema + sync the D-08 fixture</name>
  <files>src/core/context/ContextProvenanceManifest.ts, tests/fixtures/optimizedContext.ts, src/core/ai/contextHelper.ts</files>
  <read_first>src/core/context/ContextProvenanceManifest.ts (the seed — keep every existing field byte-compatible); .planning/PRODUCT_SPEC_v0_1.md §2.6 (L516-534); src/core/ai/types.ts L89-103 (ProviderConfigSchema co-location precedent) + L134-148 (PromptSection kinds); .planning/phases/04-context-adaptive-execution/04-CONTEXT.md D-04-17/18/19; tests/fixtures/optimizedContext.ts L144-155 (provenance builder — add the new fields with fixed deterministic constants); src/core/ai/contextHelper.ts L90-108 (the surviving Phase-3 producer — stamp the placeholder values IN THE SAME TASK as the interface extension so typecheck never breaks)</read_first>
  <behavior>
    - ContextProvenanceManifestSchema parses a valid manifest and rejects one with an unknown section kind (e.g. an invented 'history' kind)
    - schema.shape.sections.element.shape.kind.options (the enum) contains all 8 PromptSection kinds incl. 'tool_result'
    - the interface's kind union is unchanged (still the 8-member union incl. 'tool_result')
    - the D-08 fixture provenance builder emits the new required fields so every existing consumer fixture compiles
    - contextHelper.ts's provenance literal still typechecks against the extended interface (placeholder values) — `pnpm typecheck` passes at THIS task boundary
  </behavior>
  <action>Add to the interface: `tier: ModelContextTier` (imported type from './ModelContextTier'), `model: string`, `window: number`, `counterMethod: 'native' | 'heuristic'`, `stepsFired: ReadonlyArray<LadderStepName>` where LadderStepName is the 8-step §2.4 vocabulary (drop-debug | drop-secondary | summarise-history | compress-page | trim-tools | reduce-topk | minimal-mode | too-large). Keep the existing sections[].kind union and all existing fields untouched (R-1). Below the interface, co-locate `export const ContextProvenanceManifestSchema = z.object({...})` mirroring every field: sections as z.array(z.object({ kind: z.enum([...all 8 kinds verbatim...]), sourceId: z.string(), tokens: z.number().int().nonnegative(), truncated: z.boolean(), compressionApplied: z.enum(['summarise','structural','topk']).optional() })), totalTokens, minimalMode, workspaceId, activeSurface, tier, model, window, counterMethod (z.enum), stepsFired (z.array of the 8 steps). Header comment: §2.6 source, D-04-17/18/19, 03a-01 'tool_result' note. Import zod + the ModelContextTier type only. THEN sync BOTH surviving producers of the manifest IN THE SAME TASK (the new fields are REQUIRED — without both syncs, `pnpm typecheck` fails at every boundary until the 04-06 deletion): (1) the D-08 fixture provenance builder — add `tier` (the fixture's tier), `model: FIXED_MODEL`, `window: FIXED_MODEL_CONTEXT_WINDOWS[FIXED_MODEL]` (from 04-01's fixture set — deep-link, do not hardcode a second value), `counterMethod: 'heuristic'`, `stepsFired: []` (empty — no degradation in the default fixture); (2) src/core/ai/contextHelper.ts L95-106 provenance literal — stamp deterministic PLACEHOLDER values so the Phase-3 module compiles against the extended interface until its 04-06 deletion: `tier: input.tier` (the builder already receives the tier), `model: ''`, `window: 0`, `counterMethod: 'heuristic'`, `stepsFired: []`, with a comment: "placeholder values — contextHelper is DELETED in 04-06 (D-04-08); the optimizer stamps the real manifest" (D-04-04: this Phase-3 seed never derives tiers — the tier was passed in).</action>
  <acceptance_criteria>
    - The interface contains exactly the 8-member kind union (unchanged) and all D-04-17 fields.
    - `ContextProvenanceManifestSchema` exists as an exported zod object; `schema.shape.sections.element.shape.kind.options` is an 8-element array containing 'tool_result'.
    - Fixture provenance emits the new required fields with deterministic constants; contextHelper.ts's provenance literal compiles with the placeholder stamp.
    - `pnpm typecheck` passes at THIS task boundary (both surviving producers synced; parsing the new schema is asserted in Task 2's suite).
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm vitest run tests/fixtures/fixtures.test.ts tests/core/ai/AgentOrchestrator.test.ts</automated>
  </verify>
  <done>Manifest interface + Zod schema extend the seed with the D-04-17 fields; the fixture AND contextHelper.ts compile with the new required fields; the behavior block passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write ContextProvenanceManifest.test.ts — Zod round-trip + lockstep guard</name>
  <files>tests/core/context/ContextProvenanceManifest.test.ts</files>
  <read_first>.planning/phases/04-context-adaptive-execution/04-RESEARCH.md L361-378 (the lockstep-guard snippet verbatim); src/core/context/ContextProvenanceManifest.ts (the Task-1 schema — the parse target)</read_first>
  <behavior>
    - buildOptimizedContextFixture returns a manifest whose Zod parse succeeds for every tier override
    - the lockstep test asserts: new Set(schema kind enum options) deep-equals new Set of the 8 PromptSection kinds (RESEARCH L369-377 verbatim shape)
  </behavior>
  <action>Create `tests/core/context/ContextProvenanceManifest.test.ts`: (1) Zod round-trip — the fixture manifest (synced in Task 1) parses for tiny/small/medium/large tiers; a hand-built manifest with an invalid kind (e.g. a made-up kind string) fails schema.safeParse; (2) the D-04-18 lockstep guard exactly per RESEARCH L369-377 (new Set of schema enum options vs new Set of PromptSection kinds incl. 'tool_result'); (3) stepsFired vocabulary matches the D-04-12 8-step vocabulary — compare the schema's stepsFired enum options against the LITERAL 8-step array (drop-debug, drop-secondary, summarise-history, compress-page, trim-tools, reduce-topk, minimal-mode, too-large; do NOT import `LADDER_STEPS` from ContextCompressor — 04-02 runs in the same wave in parallel and the file may not exist yet; the literal array mirrors it and Task 3 of 04-02 pins the same vocabulary). Deterministic only.</action>
  <acceptance_criteria>
    - Fixture provenance parses under the schema for all four tiers (Zod round-trip green).
    - The lockstep test compares SETS (order-independent) of the manifest enum options vs the 8 PromptSection kinds — 'tool_result' present on both sides.
    - No import from any 04-02 module (no same-wave cross-plan dependency); `pnpm vitest run tests/core/context/ContextProvenanceManifest.test.ts` passes green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context/ContextProvenanceManifest.test.ts && pnpm typecheck</automated>
  </verify>
  <done>Fixture synced; Zod boundary + lockstep guard tests green — a future PromptSection kind addition without a schema update now fails CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optimizer output → schema | The manifest crosses the public boundary of the context layer — Zod validation is the GR-4 gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Tampering | manifest kind union | medium | mitigate | D-04-18 runtime union-parity lockstep test — a new PromptSection kind without a schema update fails CI (03a-01 precedent) |
| T-04-12 | Information Disclosure | manifest content if logged | medium | mitigate | D-04-19: manifest is in-memory per-turn, redacted via TraceRedactor before any logging (R-10), never persisted — enforced in 04-04/04-06 |
| T-04-13 | Spoofing | Zod boundary | low | mitigate | ContextProvenanceManifestSchema validates every stamped manifest at the public boundary (GR-4); schema rejects unknown kinds (tested) |
| T-04-14 | Tampering | npm installs | low | mitigate | No packages installed (R-9 — zod already approved) |
</threat_model>

<verification>
- `pnpm vitest run tests/core/context/ContextProvenanceManifest.test.ts` — manifest + lockstep suite.
- `pnpm typecheck` — zero errors (the fixture sync keeps every existing consumer compiling).
- `pnpm vitest run tests/core/ai/AgentOrchestrator.test.ts tests/fixtures/fixtures.test.ts` — no fixture regression from the new required fields.
</verification>

<success_criteria>
- Manifest extended IN PLACE with D-04-17 fields; seed fields untouched (R-1).
- ContextProvenanceManifestSchema validates at the boundary; kind-lockstep guard green.
- D-08 fixture provenance compiles + parses for all tiers.
- Success criterion 4's contract is in place for the optimizer stamping in 04-04.
</success_criteria>

<output>
Create `.planning/phases/04-context-adaptive-execution/04-03-SUMMARY.md` when done.
</output>
