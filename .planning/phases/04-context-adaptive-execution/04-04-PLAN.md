---
phase: 04-context-adaptive-execution
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/core/prompts/index.ts
  - src/core/error/errorCodes.ts
  - src/core/ai/types.ts
  - src/core/context/ContextOptimizer.ts
  - tests/core/context/ContextOptimizer.test.ts
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04]
must_haves:
  truths:
    - "src/core/context/ContextOptimizer.ts (NEW) exports `optimize(input: ContextOptimizerInput): OptimizedContext` implementing the §2.3 contract: classifyModelContext(modelContextWindow) → tier; TokenBudget.computeBudgets(window) → input/output budgets; ContextPack.packSections → PromptSection[]; totalTokens over inputBudget → run the LADDER_STEPS in D-04-12 order (real steps: drop-debug, trim-tool-schemas, minimal-mode; no-ops pass through); still over after minimal mode → throw typed `ContextTooLargeError` (code 'CONTEXT_TOO_LARGE', reason 'minimal_mode_exceeded', totalTokens, inputBudget — co-located with an isContextTooLargeError guard, StructuredOutput.ts precedent); else stamp a Zod-validated ContextProvenanceManifest (D-04-17 fields incl. stepsFired + counterMethod) and return the drop-in §2.3 output."
    - "Minimal mode (D-04-14): entered when tier==tiny (mandatory §2.5) OR escalated by the ladder; selects the compact system prompt constants (D-04-11) into [SYSTEM], keeps ≤1 safe tool schema, reduces non-system sections; marks minimalMode in the output + manifest. MCP-chaining/LLM-Wiki RAG blocking are NOT enforced here — the optimizer only marks minimalMode (D-04-14); TIER_CAPS.mcpChaining (tiny/small false) is enforced via capsForTier in the hook (04-06) and RAG is a Phase-5a consumer concern."
    - "src/core/prompts/index.ts (MODIFY) gains canonical per-role COMPACT constants `planner.compact.system` / `renderer.compact.system` (Appendix A addendum style — cacheable:true, tier 'haiku'/'flash') — the ONLY consumer is ContextOptimizer minimal mode; the default path stays byte-identical (Open Q2 — canonical prompts are NOT wired into the default path in P4)."
    - "src/core/error/errorCodes.ts (MODIFY) gains `CONTEXT_TOO_LARGE: 'CONTEXT_TOO_LARGE'` IN PLACE (mirror of spec Appendix C.2 L3512/L5040 — already canonical; W-1 gate re-verifies in 04-07)."
    - "src/core/ai/types.ts (MODIFY) extends ContextOptimizerInput IN PLACE with the D-04-05/07 inputs (`personaBlock: string`, `stage: 'planner' | 'renderer'`) and the D-04-02 CTX-02 seam (`contextUpdate?: ContextUpdate` — a typed input-only re-pack signal, StageEvent stays a TYPE, never an event bus, L1)."
    - "tests/core/context/ContextOptimizer.test.ts (NEW, §18-required) proves: tier/budgets derived from window per §2.1/§2.2 boundaries; ladder runs in §2.4 order; minimal mode selects the compact constants + ≤1 tool schema; CONTEXT_TOO_LARGE throws for absurd input; drop-in identity — the default-path [SYSTEM] text is byte-identical to the Phase-3 contextHelper output for identical inputs (cache-stability, D-04-07/P4-8); manifest stamped + Zod-valid on every return; CTX-02 seam accepted with zero output change (typed input, no consumer)."
  artifacts:
    - "src/core/context/ContextOptimizer.ts"
    - "src/core/prompts/index.ts"
    - "src/core/error/errorCodes.ts"
    - "src/core/ai/types.ts"
    - "tests/core/context/ContextOptimizer.test.ts"
  key_links:
    - "ContextOptimizer is the D-04-08 replacement for contextHelper.buildOptimizedContext — the hook swaps imports in 04-06 (Pitfall 1); the optimizer keeps the §2.3 output shape so Planner/Renderer stay import-swap-only consumers (D-04-07)."
    - "ProviderRouter (04-05) stamps modelContextWindow; the hook reads it and feeds optimize()'s modelContextWindow — this plan's input contract is what makes per-stage optimization possible (D-04-04/05)."
    - "The manifest is Zod-validated at THIS public boundary (GR-4) and the lockstep guard (04-03) pins the kind unions."
    - "Minimal mode's '≤1 safe tool schema' is the access-control surface (ASVS V4 partial) — the optimizer's tool-schema reduction is the mechanism; capsForTier enforcement follows in the hook (04-06)."
  flagged_assumptions:
    - "CTX-01 [unresolved — spec-less probe #1110]: optimizer tier/budget derivation + drop-in identity are proven by this plan's suite; the probe edge seals at the phase gate."
    - "CTX-02 [unresolved — spec-less probe #1110]: the seam is `contextUpdate?: ContextUpdate` on ContextOptimizerInput (planner discretion — 'typed input-only re-pack seam', D-04-02); no consumer in P4 (page/state-change triggers arrive with Phase 4a/7); StageEvent remains a TYPE — no subscribe/publish anywhere (L1). The exact field shape is a flagged-planner-discretion choice, NOT spec text."
    - "CTX-03 [unresolved — spec-less probe #1110]: minimal-mode compact-prompt selection is implemented here per D-04-03 — NOT a new prompting subsystem."
    - "CTX-04 [unresolved — spec-less probe #1110]: the ladder is driven here end-to-end; the CONTEXT_TOO_LARGE honest terminal never truncates user input (D-04-15)."
    - "Input extension [planner discretion, FLAGGED]: the §2.3 ContextOptimizerInput (types.ts) is extended IN PLACE with `personaBlock: string` (the spec input omits it, but F-5's byte-stable [SYSTEM] persona section requires it — Phase-3 bridged via ContextHelperInput.personaBlock) and `stage: 'planner' | 'renderer'` (needed for per-role compact-prompt selection, D-04-11). Both are additive; the output shape is untouched (D-04-07)."
    - "Compact constant text [planner discretion, FLAGGED]: the verbatim compact planner/renderer prompts are drafted by this plan following Appendix A style (agent's-discretion item) and mirrored into spec Appendix A in 04-07 (W-1 gate)."
    - "Minimal-mode [SYSTEM] join [planner discretion]: minimal mode emits the compact constant with the persona block appended (persona-aware from day one, Phase-3 precedent); the DEFAULT path stays persona-block-only (byte-identical to Phase 3 — cache-stability)."
    - "A11 [research, ASSUMED]: conversationId uses the constant 'default' in P4 (no conversation store — Phase 7 threads the real id)."
    - "A12 [research]: StructuredOutput.ts L137's inline chars/4 stays as-is (English-only repair text — correct behavior, no churn)."
  prohibitions:
    - "No prompt assembly in the hook or any component — compact prompt text lives ONLY in src/core/prompts/index.ts; ContextOptimizer selects constants, never authors them (D-04-11, GR-3, Pitfall 7)."
    - "No event bus: the CTX-02 seam is an input-only typed field; StageEvent stays a TYPE (L1) — zero subscribe/publish/emit APIs in the context layer."
    - "No silent truncation of user input — over-cap input walks the ladder to the typed terminal; never a text slice on user_input (D-04-15, P4-10)."
    - "No new prompting subsystem (D-04-03 — constant selection only)."
    - "No MCP-chaining/RAG enforcement inside the optimizer — mark minimalMode only (D-04-14); enforcement is capsForTier (04-06) + Phase-5a consumer."
    - "No error strings beyond canonical codes (GR-9 — CONTEXT_TOO_LARGE from errorCodes.ts; every catch debugLogs)."
    - "No new runtime packages (R-9)."
---

<!-- 04-04 (2026-08-12): Wave-3 optimizer orchestration. ContextOptimizer drives the
     §2.4 ladder over the 04-01/04-02 primitives, selects the compact constants in
     minimal mode (D-04-11/14), stamps the Zod-validated manifest (D-04-17), and
     throws the typed CONTEXT_TOO_LARGE terminal (D-04-15). The CTX-02 typed
     input-only re-pack seam (D-04-02) and the optimizer input contract extensions
     (personaBlock/stage — flagged) land in types.ts. -->

<objective>
Create `ContextOptimizer` — the §2.3 drop-in orchestrator that replaces `contextHelper.buildOptimizedContext` (D-04-08): classify tier from the resolved window, budget via TokenBudget, pack via ContextPack, degrade via the ContextCompressor ladder in §2.4 order, enforce minimal mode (compact prompts + ≤1 tool schema), and stamp a Zod-validated `ContextProvenanceManifest` on every output. Also land the compact prompt constants (D-04-11), the CONTEXT_TOO_LARGE canonical code, the CTX-02 typed input-only seam, and the §18-required ContextOptimizer test suite.

Purpose: This is the heart of Phase 4 — every locked behavior (D-04-04..19) converges here. It must be a deterministic drop-in (D-04-07): same §2.3 output shape, byte-stable default path, zero model calls, honest CONTEXT_TOO_LARGE terminal (never a data-loss lie).

Output: `ContextOptimizer.ts` + `ContextTooLargeError`, compact prompt constants, CONTEXT_TOO_LARGE error code, the extended optimizer input contract (types.ts), `ContextOptimizer.test.ts`.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md (§2.1 L415-433, §2.3 L453-477, §2.4 L481-492, §2.5 L494-514, §2.6 L516-534, §1.4 caps L227-351)
@src/core/context/ModelContextTier.ts, TokenBudget.ts, ContextPack.ts, ContextCompressor.ts, ContextProvenanceManifest.ts
@src/core/ai/types.ts (L150-171 — ContextOptimizerInput/OptimizedContext contract)
@src/core/ai/StructuredOutput.ts (L72-83/L150-154 — the typed-error-carrier precedent)
@src/core/prompts/index.ts, src/core/error/errorCodes.ts
@.planning/phases/04-context-adaptive-execution/04-PATTERNS.md L187-247 (optimizer analog + notes)
</context>

## Artifacts this phase produces

- **NEW** `src/core/context/ContextOptimizer.ts`: `optimize(input)` → OptimizedContext (throws `ContextTooLargeError`), `ContextTooLargeError` interface + `isContextTooLargeError` guard (co-located, StructuredOutput.ts precedent).
- **MODIFY** `src/core/prompts/index.ts`: `PROMPTS.planner.compact.system` + `PROMPTS.renderer.compact.system` (canonical compact constants, Appendix A addendum).
- **MODIFY** `src/core/error/errorCodes.ts`: `CONTEXT_TOO_LARGE` (IN PLACE, mirror of spec C.2).
- **MODIFY** `src/core/ai/types.ts`: `ContextUpdate` type + `ContextOptimizerInput.contextUpdate?` (D-04-02 seam), `personaBlock: string`, `stage: 'planner' | 'renderer'` (flagged input extension).
- **NEW** `tests/core/context/ContextOptimizer.test.ts`.

<tasks>

<task type="auto">
  <name>Task 1: Add compact prompt constants + the CONTEXT_TOO_LARGE canonical code</name>
  <files>src/core/prompts/index.ts, src/core/error/errorCodes.ts</files>
  <read_first>src/core/prompts/index.ts (PROMPTS shape L5-73 — planner.system/renderer.system L6-17); .planning/PRODUCT_SPEC_v0_1.md Appendix A (L4072+ — verbatim style) + Appendix C.2 (L3512 CONTEXT_TOO_LARGE); src/core/error/errorCodes.ts (Phase-3/3a block pattern L63-86)</read_first>
  <behavior>
    - PROMPTS.planner.compact.system exists with cacheable:true, tier:'haiku'; PROMPTS.renderer.compact.system with cacheable:true, tier:'flash'
    - ERROR_CODES.CONTEXT_TOO_LARGE === 'CONTEXT_TOO_LARGE'
  </behavior>
  <action>In prompts/index.ts, add a canonical compact sibling block beside planner/renderer: `planner.compact.system` and `renderer.compact.system` — per-role compact system prompts drafted in Appendix A style (short, directive, JSON-only for planner; concise-context-only for renderer), each `{ cacheable: true, tier: 'haiku' | 'flash' }`. Header note: D-04-11 — minimal-mode-only consumer (ContextOptimizer), default path byte-identical. In errorCodes.ts, extend the registry IN PLACE with a Phase-4 block: `CONTEXT_TOO_LARGE: 'CONTEXT_TOO_LARGE'` + a comment citing spec C.2 L3512/L5040 and the W-1 canonicalization gate (verified present in the spec). Do NOT alter any existing code.</action>
  <acceptance_criteria>
    - `PROMPTS.planner.compact.system` + `PROMPTS.renderer.compact.system` exist and are byte-stable string constants; no runtime import of them outside ContextOptimizer (grep src for 'compact' consumers — only ContextOptimizer).
    - `ERROR_CODES.CONTEXT_TOO_LARGE` === `'CONTEXT_TOO_LARGE'`; the literal also exists in spec C.2 (scoped grep — it does, L3512/L5040).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context/ContextOptimizer.test.ts -x && pnpm typecheck</automated>
  </verify>
  <done>Compact constants + canonical code exist; nothing else consumes the compact constants yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the optimizer input contract (CTX-02 seam) + create ContextOptimizer</name>
  <files>src/core/ai/types.ts, src/core/context/ContextOptimizer.ts</files>
  <read_first>src/core/ai/types.ts L150-171 (ContextOptimizerInput — extend IN PLACE); .planning/phases/04-context-adaptive-execution/04-CONTEXT.md D-04-02/04/05/07/11/14/15/17; .planning/PRODUCT_SPEC_v0_1.md §2.3-§2.6 (L453-534); src/core/ai/StructuredOutput.ts L72-83 (typed-error carrier)</read_first>
  <behavior>
    - optimize({modelContextWindow: 4096, ...tiny inputs}) returns tier 'tiny', minimalMode true, [SYSTEM] text = compact constant + persona block, ≤1 tool schema
    - optimize({modelContextWindow: 200_000, ...}) returns tier 'large', minimalMode false, [SYSTEM] text byte-identical to the Phase-3 persona-block-only shape
    - optimize with an absurd userInput (> window even after minimal mode) throws ContextTooLargeError with code 'CONTEXT_TOO_LARGE'
    - optimize with contextUpdate present returns the same output as without it (typed input, no consumer in P4)
  </behavior>
  <action>Extend `ContextOptimizerInput` in types.ts (IN PLACE, additive — the output shape untouched per D-04-07): add required `personaBlock: string`, required `stage: 'planner' | 'renderer'`, and the D-04-02 seam `contextUpdate?: ContextUpdate` with `export type ContextUpdate = { type: 'page' | 'memory' | 'history' | 'state' }` declared beside it (documented: typed input-only re-pack signal, StageEvent stays a TYPE — never an event bus, L1; no consumer in P4). Then create ContextOptimizer.ts: `optimize(input): OptimizedContext` — classifyModelContext(input.modelContextWindow) → tier; TokenBudget.computeBudgets(window) → budgets; minimalMode = tier==='tiny' || ladderEscalated; ContextPack.packSections (personaBlock + stage-aware system text: default = personaBlock-only byte-identical to Phase 3; minimal = compact constant from PROMPTS + persona block appended); if totalTokens > inputBudget, iterate LADDER_STEPS in order — real steps act (drop-debug, trim-tool-schemas to ≤1 safe schema in minimal mode, minimal-mode section reduction per §2.5), no-ops pass through — re-checking the budget; if still over after minimal mode, throw the typed `ContextTooLargeError` (interface extends Error, code 'CONTEXT_TOO_LARGE', reason 'minimal_mode_exceeded', totalTokens, inputBudget; guard fn `isContextTooLargeError` — StructuredOutput.ts precedent); else stamp ContextProvenanceManifest (tier/model/window/counterMethod:'heuristic'/stepsFired/totalTokens/sections with per-section tokens+truncated+compressionApplied) and validate it via ContextProvenanceManifestSchema (safeParse → debugLog(SCHEMA_INVALID, ...) + throw on failure, GR-4/GR-9); return OptimizedContext. Header: §2.3-§2.6 source, D-04-07 drop-in, D-04-12 real-vs-noop, D-04-15 honest terminal, zero model calls.</action>
  <acceptance_criteria>
    - types.ts: ContextUpdate + the three input fields exist; `OptimizedContext` and `PromptSection` are untouched.
    - ContextOptimizer imports ONLY from @/core/context/*, @/core/ai/types, @/core/prompts, @/core/error/* — no provider/SDK imports, no React.
    - No `text.slice(`/`substring(` on any section text; the user_input section is never modified (asserted by the suite).
    - The default (non-minimal) [SYSTEM] text equals the Phase-3 persona-block-only text (byte-identity — drop-in regression in the test).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context/ContextOptimizer.test.ts -x && pnpm typecheck</automated>
  </verify>
  <done>ContextOptimizer implements the behavior block; input contract extended; typed terminal + manifest stamping wired.</done>
</task>

<task type="auto">
  <name>Task 3: Write ContextOptimizer.test.ts — tiers, ladder, minimal mode, drop-in identity, terminal, CTX-02 seam</name>
  <files>tests/core/context/ContextOptimizer.test.ts</files>
  <read_first>tests/fixtures/optimizedContext.ts (extended D-08 fixture — FIXED_MODEL_CONTEXT_WINDOWS + OVER_BUDGET_SECTIONS + CJK samples); .planning/phases/04-context-adaptive-execution/04-RESEARCH.md L475-496 (req→test map); src/core/ai/contextHelper.ts L59-108 (the byte-identity comparison target)</read_first>
  <behavior>
    - every tier boundary (4096/16384/131072) produces the correct tier + budgets
    - a tiny-window optimize emits minimalMode:true with compact [SYSTEM] + ≤1 tool schema; a large-window one emits minimalMode:false
    - drop-in identity: optimize default path deep-equals buildOptimizedContext (Phase-3 contextHelper) output for equivalent inputs — same section texts, same byte-stable [SYSTEM]
    - over-window input throws ContextTooLargeError (code/reason/totalTokens/inputBudget present); isContextTooLargeError(that error) === true
    - contextUpdate present → identical output to absent (typed seam, no consumer)
  </behavior>
  <action>Create the §18-required suite (deterministic; drive the extended fixture). Cover: (1) tier classification boundaries per §2.1 for windows 4096/16384/131072/200_000 with budgets per §2.2; (2) minimal mode mandatory at tiny — [SYSTEM] starts with the compact constant text, tool_schemas ≤1; (3) ladder order — an over-budget input steps through LADDER_STEPS order (assert via manifest.stepsFired + totalTokens dropping); (4) drop-in identity + cache-stability — for equivalent inputs, optimize's default-path sections deep-equal contextHelper.buildOptimizedContext's sections (import the still-present Phase-3 module for the comparison; deleted in 04-06); (5) CONTEXT_TOO_LARGE terminal — an absurd-length userInput throws the typed error and isContextTooLargeError guard passes; (6) CTX-02 seam — optimize with/without contextUpdate deep-equal; (7) manifest present + Zod-valid on every return (schema.safeParse ok).</action>
  <acceptance_criteria>
    - All behavior-block cases asserted with exact values; the drop-in-identity test imports BOTH optimize and buildOptimizedContext for the byte comparison.
    - No nondeterminism (no Date.now/crypto).
    - `pnpm vitest run tests/core/context/ContextOptimizer.test.ts -x` passes green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context/ContextOptimizer.test.ts -x && pnpm typecheck</automated>
  </verify>
  <done>ContextOptimizer suite green — every CTX edge (01/02/03/04) proven: tiers, ladder, minimal mode, drop-in identity, honest terminal, typed seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → optimizer | Unbounded user text crosses into budget/degradation — the only 'lossy' outcome must be the typed terminal, never a silent slice |
| tool schemas → minimal mode | The ≤1-safe-tool reduction is an access-control surface (ASVS V4) for tiny models |
| optimizer output → providers | The OptimizedContext (with stamped manifest) crosses to PlannerService/RendererService |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | DoS | optimize() over-window input | high | mitigate | §2.4 ladder at section granularity + typed CONTEXT_TOO_LARGE terminal (D-04-15) — an oversized prompt is NEVER sent (success criterion 2); user_input is never sliced (P4-10); test pins the terminal |
| T-04-16 | Elevation of Privilege | minimal-mode tool reduction | medium | mitigate | tier==tiny ⇒ minimalMode mandatory (§2.5) with ≤1 tool schema (D-04-14); the optimizer marks minimalMode; capsForTier (tiny/small mcpChaining:false) enforcement lands in the hook (04-06) — agent capability surface bounded on small models |
| T-04-17 | Tampering | prompt constant selection | medium | mitigate | Compact constants live ONLY in src/core/prompts/index.ts (D-04-11, GR-3) — the optimizer selects, never authors; default path byte-identical (drop-in test) so the anthropic prompt cache is never invalidated |
| T-04-18 | Information Disclosure | debugLog payloads | medium | mitigate | Every catch debugLogs a canonical C.2 code (GR-9) — never section text or user input; manifest redaction via TraceRedactor is enforced at the hook (04-06, D-04-19/R-10) |
| T-04-19 | Spoofing | manifest boundary validation | low | mitigate | ContextProvenanceManifestSchema.safeParse at the public boundary (GR-4); failure → SCHEMA_INVALID debugLog + typed throw (never an unvalidated manifest escapes) |
| T-04-20 | Tampering | npm installs | low | mitigate | No packages installed by this plan (R-9) |
</threat_model>

<verification>
- `pnpm vitest run tests/core/context -x` — the FULL Wave-3 context suite (TokenBudget + ContextCompressor + Manifest + Optimizer).
- `pnpm typecheck` — zero errors.
- Grep gate: no `.slice(`/`.substring(` on section text in src/core/context/; no event-bus APIs (subscribe/publish/emit) in src/core/context/ or types.ts for the seam (L1).
</verification>

<success_criteria>
- ContextOptimizer delivers the §2.3 drop-in with per-stage window input; default path byte-identical (cache-stable).
- Minimal mode selects compact constants + ≤1 tool schema; CONTEXT_TOO_LARGE terminal is honest and typed.
- Manifest stamped + Zod-validated on every OptimizedContext; CTX-02 seam is a typed input, no consumer.
- CTX-01/02/03/04 proven by the optimizer suite; ready for the Router window stamp (04-05) and the hook rewire (04-06).
</success_criteria>

<output>
Create `.planning/phases/04-context-adaptive-execution/04-04-SUMMARY.md` when done.
</output>
