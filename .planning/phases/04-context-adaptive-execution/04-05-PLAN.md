---
phase: 04-context-adaptive-execution
plan: 05
type: execute
wave: 3
depends_on: ["04-01"]
files_modified:
  - src/core/ai/ProviderRouter.ts
  - tests/core/ai/AgentOrchestrator.test.ts
  - tests/core/ai/AgentOrchestrator.budget.test.ts
  - tests/components/pages/useStreamingLLM.test.tsx
autonomous: true
requirements: [CTX-01]
must_haves:
  truths:
    - "src/core/ai/ProviderRouter.ts (MODIFY) `StageInvocation` gains REQUIRED `modelContextWindow: number` (Pitfall 2 — optional would silently degrade to the fallback tier forever, inverting D-04-04); `buildInvocation` stamps it via `resolveModelContextWindow(cand.model).contextWindow` from @/core/context/ModelContextTier — deterministic, synchronous, zero model calls."
    - "The three existing StageInvocation fixture builders are mechanically updated with deterministic windows (the compiler does the inventory — Pitfall 2): AgentOrchestrator.test.ts stageInvocation() L55-63, AgentOrchestrator.budget.test.ts makeResolver path (createStageInvocation gets the window from the canonical map for 'deepseek-chat'), useStreamingLLM.test.tsx hoisted createStageInvocation mock L20-34."
    - "Every existing test stays green — the ONLY breaking change is the required field, and it is mechanical (RESEARCH Pitfall 2: tsc errors ARE the inventory)."
  artifacts:
    - "src/core/ai/ProviderRouter.ts"
    - "tests/core/ai/AgentOrchestrator.test.ts"
    - "tests/core/ai/AgentOrchestrator.budget.test.ts"
    - "tests/components/pages/useStreamingLLM.test.tsx"
  key_links:
    - "The stamped window is the D-04-04 per-stage resolution source the hook reads in 04-06 (per-stage optimizer input) — ProviderRouter window BEFORE the hook's per-stage packs (the mandated hook-rewire dependency order)."
    - "resolveModelContextWindow (04-01) is the single window source (R-1) — ProviderRouter imports it, never re-declares a table."
    - "Unavailable-error shape precedent (ProviderRouter L309-340) is untouched — only the StageInvocation bundle grows."
  flagged_assumptions:
    - "Wave 3 is a DELIBERATE conservative ordering, not the mechanical max(depends_on)+1 = 2: Task 2's full-repo `pnpm typecheck` must not race wave-2 04-03's repo-wide breaking change (required manifest fields + contextHelper stamp inside T1). See the header comment for the full rationale; downstream waves are unaffected (04-06 = max(3,3)+1 = 4)."
    - "CTX-01 [unresolved — spec-less probe #1110]: the window now rides every stage invocation; per-stage budget enforcement completes in the hook rewire (04-06)."
    - "A2..A6 [04-01, ASSUMED — user confirmation gate]: the stamped window values inherit the canonical-map values; if a value is wrong, every downstream tier/cap shifts (mitigated by the 70% budget + ladder)."
    - "The budget test's 'deepseek-chat' fixture resolves to 65_536 via the canonical map — the test does NOT assert a hardcoded budget that the map value would contradict (planner discretion: keep budget assertions relative, e.g. capsForTier-derived, not window-derived)."
  prohibitions:
    - "No optional `modelContextWindow` — REQUIRED field (Pitfall 2; D-04-04 'never the primary source' inverted by optionality)."
    - "No SDK/network window lookup in buildInvocation (getModels is a throwing stub; the map is synchronous — zero model calls)."
    - "No changes to createStageInvocation's resolver/ledger/breaker logic — this plan only grows the StageInvocation bundle (scope discipline, G0)."
    - "No new runtime packages (R-9)."
---

<!-- 04-05 (2026-08-12): Wave-3 Router seam. StageInvocation gains the REQUIRED
     modelContextWindow (D-04-04) stamped from the canonical map by buildInvocation —
     the ProviderRouter window stamp MUST land before the hook's per-stage packs
     (04-06). The three fixture builders are mechanically updated (Pitfall 2: the
     compiler inventories them).
     WAVE RATIONALE (deliberate conservative ordering — wave 3, NOT the mechanical
     max(depends_on)+1 = 2): Task 2's verify runs a FULL-REPO `pnpm typecheck` and
     `tests/core/ai` + useStreamingLLM suites; if 04-05 ran in wave 2 parallel with
     04-03, that full typecheck could race 04-03's repo-wide breaking change (the
     REQUIRED manifest fields + contextHelper stamp land inside 04-03 T1 — the tree
     is typecheck-broken until both syncs are in). Keeping 04-05 one wave behind
     guarantees the wave-2 context foundation is fully committed before any
     full-repo typecheck boundary here. Downstream waves are unaffected:
     04-06 stays wave 4 (max(wave 04-04=3, wave 04-05=3)+1). -->

<objective>
Make the resolved model context window travel with every stage invocation: `StageInvocation` gains a required `modelContextWindow` stamped by `buildInvocation` from the canonical `resolveModelContextWindow` map (D-04-04/06). Update the three fixture builders mechanically so the whole suite compiles with the required field.

Purpose: The hook (04-06) must read each stage's resolved window from its `StageInvocation` to run per-stage optimization (D-04-04/05). Making the field REQUIRED (Pitfall 2) guarantees the pre-resolution `medium` fallback can never silently become the primary source. This plan is the mandated dependency for the hook rewire.

Output: `ProviderRouter.ts` window stamping + the three fixture updates.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@src/core/ai/ProviderRouter.ts (StageInvocation L118-124, buildInvocation L582-599)
@src/core/context/ModelContextTier.ts (resolveModelContextWindow — the stamp source, 04-01)
@tests/core/ai/AgentOrchestrator.test.ts (stageInvocation fixture L55-63)
@tests/core/ai/AgentOrchestrator.budget.test.ts (makeResolver L55-75)
@tests/components/pages/useStreamingLLM.test.tsx (hoisted mock L19-34)
@.planning/phases/04-context-adaptive-execution/04-PATTERNS.md L269-306 (ProviderRouter analog) + L518-548 (fixture updates)
</context>

## Artifacts this phase produces

- **MODIFY** `src/core/ai/ProviderRouter.ts`: `StageInvocation.modelContextWindow: number` (required) + `buildInvocation` stamp via `resolveModelContextWindow(cand.model).contextWindow`.
- **MODIFY** `tests/core/ai/AgentOrchestrator.test.ts` / `AgentOrchestrator.budget.test.ts` / `tests/components/pages/useStreamingLLM.test.tsx`: fixture builders supply deterministic windows.

<tasks>

<task type="auto">
  <name>Task 1: Stamp modelContextWindow on StageInvocation in buildInvocation</name>
  <files>src/core/ai/ProviderRouter.ts</files>
  <read_first>src/core/ai/ProviderRouter.ts L118-124 (StageInvocation) + L582-599 (buildInvocation — the stamp site); src/core/context/ModelContextTier.ts (resolveModelContextWindow); .planning/phases/04-context-adaptive-execution/04-PATTERNS.md L269-303</read_first>
  <behavior>
    - a StageInvocation returned by buildInvocation carries modelContextWindow === resolveModelContextWindow(cand.model).contextWindow
    - no other behavior of buildInvocation/createStageInvocation changes
  </behavior>
  <action>Add `modelContextWindow: number` to the StageInvocation interface (documented: D-04-04 required field — the resolved model's window from the canonical map, stamped so per-stage optimization reads it without network/SDK). In `buildInvocation`, import `resolveModelContextWindow` from '@/core/context/ModelContextTier' and include `modelContextWindow: resolveModelContextWindow(cand.model).contextWindow` in the returned bundle. Do NOT touch createStageInvocation's traversal, the breaker, the ledger, or the F-4/F-5 closures. Header comment: D-04-04/06, Pitfall-2 required-field rationale, zero-model-calls (map is synchronous).</action>
  <acceptance_criteria>
    - `StageInvocation` has exactly the four pre-existing fields + `modelContextWindow: number` (required — no `?`).
    - `buildInvocation` imports resolveModelContextWindow from the canonical home and stamps `cand.model`'s window.
    - No SDK/network call anywhere in the stamp path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/ai/ProviderRouter.test.ts</automated>
  </verify>
  <done>StageInvocation carries the required window; the resolver/ledger/breaker logic is byte-unchanged. (Verify scoped to ProviderRouter.test.ts — full `pnpm typecheck` runs in Task 2, after the three fixture builders supply the required field; the field's blast radius is exactly those fixtures.)</done>
</task>

<task type="auto">
  <name>Task 2: Update the three fixture builders with deterministic windows</name>
  <files>tests/core/ai/AgentOrchestrator.test.ts, tests/core/ai/AgentOrchestrator.budget.test.ts, tests/components/pages/useStreamingLLM.test.tsx</files>
  <read_first>tests/core/ai/AgentOrchestrator.test.ts L50-70 (stageInvocation builder); tests/core/ai/AgentOrchestrator.budget.test.ts L55-88 (makeResolver + turn input); tests/components/pages/useStreamingLLM.test.tsx L19-45 (hoisted mock); .planning/phases/04-context-adaptive-execution/04-PATTERNS.md L518-548</read_first>
  <behavior>
    - AgentOrchestrator.test.ts stageInvocation() returns modelContextWindow: 200_000 by default (overridable)
    - useStreamingLLM.test.tsx hoisted createStageInvocation mock returns modelContextWindow: 200_000 in its object
    - the budget test's resolver still passes the window through the real createStageInvocation path (no hardcoded literal needed — the canonical map resolves 'deepseek-chat')
  </behavior>
  <action>Mechanically satisfy the required field (let tsc point at every site — Pitfall 2): (1) AgentOrchestrator.test.ts `stageInvocation()` adds `modelContextWindow: 200_000` before `...overrides` (deterministic window — overridable per-test); (2) useStreamingLLM.test.tsx hoisted mock adds `modelContextWindow: 200_000` to the returned object; (3) AgentOrchestrator.budget.test.ts — verify whether the resolver path (real createStageInvocation) already stamps the window from the map for 'deepseek-chat' (65_536); if the test constructs a StageInvocation literal anywhere, add the matching deterministic window there. Do NOT change any assertion semantics; if a budget assertion was window-derived, keep it relative (capsForTier-based) per the flagged assumption.</action>
  <acceptance_criteria>
    - `pnpm typecheck` passes with zero StageInvocation-completeness errors across the three files.
    - No assertion in any of the three suites changed meaning (diff shows fixture-only additions).
    - `pnpm vitest run tests/core/ai tests/components/pages/useStreamingLLM.test.tsx` passes green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/ai tests/components/pages/useStreamingLLM.test.tsx && pnpm typecheck</automated>
  </verify>
  <done>All fixture builders supply the required window; the existing suites stay green (drop-in).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver candidates → buildInvocation | The chosen (providerId, model) crosses into the window stamp — an unknown model must never inflate the window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-21 | Spoofing | window stamp for unknown models | medium | mitigate | resolveModelContextWindow returns conservative tiny (4096) + windowKnown:false for unlisted models (D-04-06) — never assume large; the manifest flag surfaces the fallback (04-04) |
| T-04-22 | Tampering | StageInvocation optionality | medium | mitigate | REQUIRED field (Pitfall 2) — optionality would silently keep the pre-resolution fallback forever (D-04-04 inverted); tsc enforces every construction site |
| T-04-23 | Denial of Service | oversized prompt path | high | mitigate | The stamped window feeds TokenBudget caps → §2.4 ladder → CONTEXT_TOO_LARGE terminal (04-04) — an oversized prompt is never sent; this plan delivers the window the enforcement runs on |
| T-04-24 | Tampering | npm installs | low | mitigate | No packages installed (R-9) |
</threat_model>

<verification>
- `pnpm vitest run tests/core/ai tests/components/pages/useStreamingLLM.test.tsx` — full AI + hook test surface green after the mechanical updates.
- `pnpm typecheck` — zero errors at Task-2 completion (Task 1 defers the full typecheck: the required field breaks the three fixture builders until Task 2 updates them — scoped to ProviderRouter.test.ts at the Task-1 boundary).
- Grep gate: `modelContextWindow` is declared exactly once in the interface (no duplicate/optional variant); no getModels/network call added.
</verification>

<success_criteria>
- StageInvocation carries the required modelContextWindow stamped from the canonical map.
- All three fixture builders supply deterministic windows; the existing suites stay green.
- The hook (04-06) can read per-stage windows — the mandated dependency order is satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-context-adaptive-execution/04-05-SUMMARY.md` when done.
</output>
