---
phase: 04-context-adaptive-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/context/ModelContextTier.ts
  - src/core/context/TokenBudget.ts
  - tests/fixtures/optimizedContext.ts
  - tests/core/context/TokenBudget.test.ts
autonomous: true
requirements: [CTX-01]
must_haves:
  truths:
    - "src/core/context/ModelContextTier.ts (extended IN PLACE, P-3b seed never re-declared — R-1) gains `MODEL_CONTEXT_WINDOWS: Readonly<Record<string, number>>` keyed by the five canonical Appendix-D modelIds (`claude-haiku-4-latest`, `deepseek-chat`, `gemini-2.5-flash`, `llama3.2:3b`, `qwen2.5:7b`) and `resolveModelContextWindow(modelId): { contextWindow: number; windowKnown: boolean }` returning `{ 4096, false }` for unknown models (D-04-06: conservative tiny, never large)."
    - "src/core/context/TokenBudget.ts (NEW) exports `estimateTokens` (migrated from contextHelper L28-30 + D-04-10 CJK rule: Unicode-range ratio >= 0.3 → divisor 3, else 4; mixed script → higher-cost divisor; zero-length → 0), `computeBudgets(contextWindow)` (70/20/10: inputBudget/outputBudget/safetyMargin), the §2.2 per-tier distribution table, and a canonical column→kind cap mapping (Pitfall 3: System→system+preferences, Tools→tool_schemas, Memory→memory, Context→context, History→reserved-unfilled D-04-16, User→user_input+task; `tool_result` uncapped-but-counted in totalTokens) — all pure, deterministic, zero model calls."
    - "tests/fixtures/optimizedContext.ts (EXTENDED, not duplicated — P4-15/WR-13) carries a `FIXED_MODEL_CONTEXT_WINDOWS` set, over-budget section samples (token counts exceeding per-tier caps — the §2.4 ladder trigger material), and CJK + mixed-script samples for the estimateTokens heuristic; the determinism header (L1-12) is untouched."
    - "tests/core/context/TokenBudget.test.ts (NEW) proves: 70/20/10 formula exactness, per-tier per-section caps from distribution×inputBudget, CJK/mixed/English estimateTokens boundaries (incl. the 0.3 ratio edge), conservative-unknown-window resolution, and cap values never truncate (caps are numbers that DRIVE degradation, not text slices)."
  artifacts:
    - "src/core/context/ModelContextTier.ts"
    - "src/core/context/TokenBudget.ts"
    - "tests/fixtures/optimizedContext.ts"
    - "tests/core/context/TokenBudget.test.ts"
  key_links:
    - "`resolveModelContextWindow` is the D-04-04 window source stamped onto StageInvocation.modelContextWindow in a later plan (04-05) — the canonical map is the single declaration (R-1)."
    - "`TokenBudget.estimateTokens` becomes the ONLY token counter — AgentOrchestrator L33/L278 re-points its import here in the SAME wave as the contextHelper deletion (Pitfall 1, 04-06); StructuredOutput.ts L137 inline chars/4 is deliberately left (A12)."
    - "ModelContextTier/TokenBudget are pure core modules — no imports of ai/, storage, or the background SW (R-3)."
  flagged_assumptions:
    - "CTX-01 [unresolved — spec-less probe #1110]: tier derivation + budget enforcement are implemented by this plan (ModelContextTier + TokenBudget) and completed by 04-04/04-05/04-06; the probe edge stays unresolved until the phase gate seals it."
    - "A2 [research, ASSUMED — user confirmation gate]: claude-haiku-4-latest window = 200K (large). If actually smaller, tier/caps change — mitigated by the 70% budget + ladder."
    - "A3 [research, ASSUMED — user confirmation gate]: deepseek-chat window = 64K (medium). If 32K → small (caps 2/1)."
    - "A4 [research, ASSUMED — user confirmation gate]: gemini-2.5-flash window = 1M (large at any >=200K — only the budget scales)."
    - "A5 [research, ASSUMED — user confirmation gate]: Ollama llama3.2:3b default num_ctx = 4K (tiny)."
    - "A6 [research, ASSUMED — user confirmation gate]: Ollama qwen2.5:7b default num_ctx = 4K (tiny)."
    - "A7 [research, ASSUMED]: unknown custom models → tiny (4096) + windowKnown:false (D-04-06 locked — conservative by design)."
    - "A8 [research, ASSUMED]: CJK ratio threshold 0.3 selects the higher-cost divisor (testable/adjustable)."
    - "A9 [research, ASSUMED]: distribution-column→section-kind mapping (System→system+preferences etc.) is a planner resolution, not spec text — encoded as a TESTED constant in TokenBudget."
    - "A10 [research, ASSUMED]: column percentages apply to inputBudget (0.70×window), not the raw window."
    - "Minor count note: RESEARCH says 'six Appendix-D candidates' but the VERIFIED TierResolver (src/core/ai/TierResolver.ts L19-31) enumerates five modelIds — the map keys the five; anything else falls to the tiny fallback."
  prohibitions:
    - "No invented model-window source: the SDK exposes no window and getModels() is a throwing stub (verified @ai-sdk/provider 1.1.3) — the canonical map is the only source; never an async/network lookup (zero model calls in optimization)."
    - "No custom tokenizer / BPE — the heuristic IS the counter (D-04-10); provider-native counters are consumed only if the SDK exposes one (it does not in ai@4.3.19)."
    - "No hard truncation as cap enforcement — per-section caps DRIVE the §2.4 degradation ladder; a cap is never a license to slice section text (D-04-13, CTX-04)."
    - "No invented PromptSection kind for the history slice — History is a budget-column reservation (D-04-16), never a new section kind (R-1/R-2)."
    - "No re-declaration of ModelContextTier/classifyModelContext or ContextProvenanceManifest — seeded homes are extended IN PLACE only (R-1, P-3b)."
    - "No new runtime packages — everything here is already in the approved stack (R-9)."
---

<!-- 04-01 (2026-08-12): Wave-1 context foundation. The canonical model→window map
     (D-04-04/06), the token counter + budget math (D-04-09/10), and the test
     fixture material (P4-15) every later plan builds on. Context tiers are
     DERIVED from the resolved model window — the assumption-delta decision on
     ROADMAP criterion #1 ('selectable') is recorded below: no user-facing
     override setting is invented. -->

<objective>
Build the deterministic core-context foundation for Phase 4: the canonical model→context-window map (D-04-04/06), the token counter + 70/20/10 budget math + per-section cap table (D-04-09/10, §2.2), and the extended test-fixture material (P4-15). This plan creates the two pure modules every later plan imports — nothing here touches the hook, the Router, or the orchestrator.

Purpose: CTX-01's "tiers selectable with budget enforcement" requires a single, deterministic source of truth for model windows (the SDK exposes none — verified) and for token budgets (the spec's 70/20/10 + distribution table). All subsequent plans (ContextPack, ContextCompressor, ContextOptimizer, the StageInvocation window stamp, the hook rewire) consume these modules. Zero model calls in optimization is the through-line constraint this plan enforces by construction.

Output: `MODEL_CONTEXT_WINDOWS` + `resolveModelContextWindow` (extended IN PLACE in ModelContextTier.ts), `TokenBudget` (estimateTokens + computeBudgets + distribution table + cap mapping), the extended D-08 fixture, and `TokenBudget.test.ts`.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md
@src/core/context/ModelContextTier.ts
@src/core/ai/contextHelper.ts
@src/core/ai/TierResolver.ts
@tests/fixtures/optimizedContext.ts
</context>

## Assumption-Delta Decision (recorded)

- **Noun:** `ModelContextTier`
- **Decision:** `no-change`
- **Rationale:** Per 04-CONTEXT D-04-04, the tier is DERIVED from the resolved model's context window via `classifyModelContext` (per-stage), with the per-surface default (`medium`) as a pre-resolution fallback only. ROADMAP success criterion #1 "Context tiers are selectable" is satisfied by following the model selection — it is NOT a new user-facing tier-override setting (a second source of truth could mismatch the actual window — cost landmine). This plan and 04-04/04-06 implement derivation only.

## Artifacts this phase produces

- **EXTEND IN PLACE** `src/core/context/ModelContextTier.ts`: `MODEL_CONTEXT_WINDOWS` (Readonly<Record<string, number>> — five Appendix-D modelIds), `resolveModelContextWindow(modelId)` → `{ contextWindow, windowKnown }`.
- **NEW** `src/core/context/TokenBudget.ts`: `estimateTokens(text)` (CJK-aware), `computeBudgets(contextWindow)` (70/20/10), `PER_TIER_DISTRIBUTION` (the §2.2 6-column table), `SECTION_CAP_MAPPING` (column→kind, Pitfall 3), `computeSectionCaps(tier, inputBudget)` (per-kind caps that drive degradation).
- **EXTEND** `tests/fixtures/optimizedContext.ts`: `FIXED_MODEL_CONTEXT_WINDOWS`, over-budget section samples, CJK + mixed-script text samples.
- **NEW** `tests/core/context/TokenBudget.test.ts`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ModelContextTier with the canonical window map + conservative resolver</name>
  <files>src/core/context/ModelContextTier.ts, tests/core/context/TokenBudget.test.ts</files>
  <read_first>src/core/context/ModelContextTier.ts (the 14-line P-3b seed — extend, never re-declare); src/core/ai/TierResolver.ts L19-31 (the canonical static-map precedent); .planning/PRODUCT_SPEC_v0_1.md §2.1 (L415-433)</read_first>
  <behavior>
    - resolveModelContextWindow('claude-haiku-4-latest') → { contextWindow: 200_000, windowKnown: true }
    - resolveModelContextWindow('some-custom-model') → { contextWindow: 4096, windowKnown: false } (D-04-06 conservative tiny)
    - classifyModelContext(4096) → 'tiny'; classifyModelContext(200_000) → 'large' (seed unchanged)
  </behavior>
  <action>tdd scaffold FIRST (this task OWNS the suite file's creation): create `tests/core/context/TokenBudget.test.ts` with this module's behavior cases (resolver known/unknown, classifyModelContext seed) — write the tests (RED), then implement to green. The suite file is shared across this plan's tasks: Task 2 extends it with counter/budget cases, Task 3 completes the fixture-driven cases. Then append to the existing seed (keep `ModelContextTier` type and `classifyModelContext` byte-identical — R-1): a `MODEL_CONTEXT_WINDOWS: Readonly<Record<string, number>>` constant keyed by the five verified Appendix-D modelIds — `claude-haiku-4-latest: 200_000`, `deepseek-chat: 65_536`, `gemini-2.5-flash: 1_048_576`, `llama3.2:3b: 4_096`, `qwen2.5:7b: 4_096` (values flagged [ASSUMED] A2..A6 in the header comment) — and `resolveModelContextWindow(modelId: string): { contextWindow: number; windowKnown: boolean }` that returns the known entry or `{ contextWindow: 4096, windowKnown: false }` for any unlisted model (never `large` — D-04-06). Header comment: cite §2.1 + §18 create-list, the TierResolver static-map precedent, and the D-04-06 conservative-fallback rule. Do NOT import anything from the ai SDK or providers (zero model calls; synchronous).</action>
  <acceptance_criteria>
    - `src/core/context/ModelContextTier.ts` contains exactly one declaration each of `ModelContextTier`, `classifyModelContext`, `MODEL_CONTEXT_WINDOWS`, `resolveModelContextWindow` (grep each name, count == 1).
    - The five modelIds from TierResolver L19-31 are all present as keys; no other keys are added.
    - `MODEL_CONTEXT_WINDOWS` is `Readonly` (as const); `resolveModelContextWindow` returns `windowKnown: false` for an arbitrary unknown string.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context/TokenBudget.test.ts && pnpm typecheck</automated>
  </verify>
  <done>ModelContextTier.ts compiles with the seed intact; `resolveModelContextWindow` resolves known + unknown cases per the behavior block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TokenBudget with the CJK-aware counter + 70/20/10 formula + per-section caps</name>
  <files>src/core/context/TokenBudget.ts, tests/core/context/TokenBudget.test.ts</files>
  <read_first>src/core/ai/contextHelper.ts L27-30 (estimateTokens migration seed); .planning/PRODUCT_SPEC_v0_1.md §2.2 (L434-451, formula + distribution table); .planning/phases/04-context-adaptive-execution/04-RESEARCH.md L313-331 + L380-395 (verified snippets); src/core/ai/types.ts L134-148 (PromptSection kinds — the cap mapping target)</read_first>
  <behavior>
    - estimateTokens('') → 0; estimateTokens(English 4-char string) → 1; estimateTokens of a 60% CJK string → Math.ceil(len/3); estimateTokens of a 10% CJK string → Math.ceil(len/4)
    - computeBudgets(200_000) → { inputBudget: 140_000, outputBudget: 40_000, safetyMargin: 20_000 }
    - computeSectionCaps('medium', 16_384) yields user-input cap ≈ 0.15 × inputBudget (medium User column 15%)
  </behavior>
  <action>tdd scaffold: EXTEND the suite file created in Task 1 (`tests/core/context/TokenBudget.test.ts`) with this module's behavior cases (estimateTokens empty/CJK/English/mixed, computeBudgets formula, distribution rows, cap derivation) — add the tests (RED), then implement to green; Task 3 completes the fixture-driven cases. Then create a pure, deterministic module. Export: (1) `estimateTokens(text: string): number` — migrate contextHelper's function and extend per D-04-10: count characters matching the CJK unicode-range regex (U+3040–U+30FF, U+3400–U+4DBF, U+4E00–U+9FFF, U+AC00–U+D7AF, U+F900–U+FAFF, U+FF00–U+FFEF); divisor = 3 when cjk/len >= 0.3 else 4; return Math.ceil(len/divisor); zero-length returns 0. (2) `computeBudgets(contextWindow: number)` returning `{ inputBudget, outputBudget, safetyMargin }` = floor(window×0.70/0.20/0.10) — §2.2 verbatim. (3) `PER_TIER_DISTRIBUTION: Record<ModelContextTier, { system; tools; memory; context; history; user }>` mirroring the §2.2 table rows exactly (tiny 15/20/10/20/15/20, small 10/15/10/25/20/20, medium 8/12/10/30/25/15, large 5/10/10/35/25/15). (4) `SECTION_CAP_MAPPING` — the canonical column→kind mapping (System→system+preferences, Tools→tool_schemas, Memory→memory, Context→context, History→reserved-unfilled, User→user_input+task) with `tool_result` documented as uncapped-but-counted in totalTokens (Pitfall 3 resolution, tested). (5) `computeSectionCaps(tier, inputBudget)` deriving per-kind caps = column% × inputBudget. Header comment: source §2.2, D-04-10 counting rule, D-04-13 (caps drive degradation, never truncation), D-04-16 (history reserved-unfilled). No imports beyond `ModelContextTier` type — no ai SDK, no network.</action>
  <acceptance_criteria>
    - All five exported symbols exist with the signatures above; `PER_TIER_DISTRIBUTION` rows match §2.2 L446-449 values exactly.
    - `estimateTokens` uses no regex `test` on the full string beyond the per-char CJK class (no tokenizer); returns integers.
    - No `text.slice`/`substring` call anywhere in the module (caps never truncate — grep-gate).
    - No import of any @ai-sdk module or provider code.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context/TokenBudget.test.ts && pnpm typecheck</automated>
  </verify>
  <done>TokenBudget.ts implements the behavior block; the module is pure (no side effects, no async, no model imports).</done>
</task>

<task type="auto">
  <name>Task 3: Extend the D-08 fixture + write TokenBudget.test.ts</name>
  <files>tests/fixtures/optimizedContext.ts, tests/core/context/TokenBudget.test.ts</files>
  <read_first>tests/fixtures/optimizedContext.ts (whole file — determinism header L1-12, TIER_BUDGETS L62-67); tests/core/ai/AgentOrchestrator.budget.test.ts (pure-math test style); .planning/phases/04-context-adaptive-execution/04-PATTERNS.md L466-495 (test skeleton + lockstep precedent)</read_first>
  <action>Extend the fixture IN PLACE (no new fixture file, no duplication): add (a) `FIXED_MODEL_CONTEXT_WINDOWS: Readonly<Record<string, number>>` mirroring the five-map keys; (b) an `OVER_BUDGET_SECTIONS` sample (PromptSection[] whose per-kind token counts exceed medium/large caps — the §2.4 ladder trigger material); (c) `CJK_TEXT` (>=0.3 ratio), `ENGLISH_TEXT`, and `MIXED_TEXT` constants for the heuristic. Preserve the determinism header and existing exports byte-identical where possible. Then COMPLETE the shared suite file created in Task 1 / extended in Task 2 (`tests/core/context/TokenBudget.test.ts` — extend, don't duplicate what already exists): deterministic (no Date.now/crypto) tests for — 70/20/10 formula exactness (computeBudgets(200_000) == the behavior-block values); per-tier distribution rows; per-kind cap derivation for each tier (cap == column% × inputBudget, User→user_input+task; tool_result uncapped-but-counted in a totalTokens assertion); estimateTokens boundaries (empty→0, English→len/4, >=0.3 CJK→len/3, mixed→higher divisor); resolveModelContextWindow known/unknown (unknown → 4096 + windowKnown:false); and an assertion that NO cap value is ever applied as a truncation (caps exist and degrade-drive, not slice — assert computeSectionCaps returns finite numbers and the compressor plan consumes them).</action>
  <acceptance_criteria>
    - `tests/fixtures/optimizedContext.ts` exports the three new fixture additions; all pre-existing exports (buildOptimizedContextFixture, buildOptimizedContextFixtureBundle, FIXED_*) remain importable by existing tests.
    - `TokenBudget.test.ts` contains no `Date.now`, `crypto`, or other nondeterminism.
    - `pnpm vitest run tests/core/context/TokenBudget.test.ts` passes green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context/TokenBudget.test.ts && pnpm typecheck</automated>
  </verify>
  <done>Fixture extended and TokenBudget suite green; the CJK/mixed heuristic and conservative-unknown-window behaviors are pinned by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config data → optimizer | Model window values (MODEL_CONTEXT_WINDOWS) are developer-authored constants consumed by every stage budget — a wrong value mis-sizes every downstream prompt |
| unknown-model input → tier | An unlisted modelId crosses from user provider config into classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | DoS | computeSectionCaps / budgets | high | mitigate | Per-section caps + §2.4 ladder (later plans) + honest CONTEXT_TOO_LARGE terminal never send an oversized prompt (success criterion 2); this plan pins the cap MATH, the ladder consumes it in 04-02/04-04 |
| T-04-02 | Tampering | MODEL_CONTEXT_WINDOWS | medium | mitigate | Readonly record + single declaration (R-1); values flagged [ASSUMED] A2..A6 gated behind user confirmation; unknown → conservative tiny + windowKnown flag (D-04-06) — never assume large |
| T-04-03 | Information Disclosure | estimateTokens inputs | low | accept | The counter only reads string lengths/unicode classes — it never logs or persists section text; R-10 redaction is enforced at the logging boundary (04-04/04-06) |
| T-04-04 | Elevation of Privilege | TokenBudget exports | low | accept | Pure math module with no I/O — no privilege surface; tests pin every output |
| T-04-05 | Tampering | npm installs | low | mitigate | No packages installed by this plan (R-9; verified package.json) — nothing new enters the dependency tree |
</threat_model>

<verification>
- `pnpm vitest run tests/core/context/TokenBudget.test.ts` — the Wave-1 core suite (also covers ModelContextTier via the resolver tests).
- `pnpm typecheck` — compiles with zero errors after the extensions.
- Grep gate: `ModelContextTier`/`TokenBudget` contain no `.slice(`/`.substring(` on any text, no @ai-sdk import.
</verification>

<success_criteria>
- ModelContextTier.ts extended IN PLACE with the canonical map + conservative resolver; seed declarations untouched (R-1).
- TokenBudget.ts ships estimateTokens/computeBudgets/PER_TIER_DISTRIBUTION/SECTION_CAP_MAPPING/computeSectionCaps — pure, deterministic, zero model calls.
- D-08 fixture extended with window set / over-budget / CJK+mixed material; TokenBudget.test.ts green.
- CTX-01's budget-math half is proven; tier/budget derivation is ready for the StageInvocation window stamp (04-05) and the optimizer (04-04).
</success_criteria>

<output>
Create `.planning/phases/04-context-adaptive-execution/04-01-SUMMARY.md` when done.
</output>
