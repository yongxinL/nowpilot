---
phase: 04-context-adaptive-execution
plan: 07
type: verify
wave: 5
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05", "04-06"]
files_modified:
  - package.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PRODUCT_SPEC_v0_1.md
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04]
must_haves:
  truths:
    - "package.json gains `verify:phase-4` = the §24 pattern chain: `eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run && node tests/isolation/check-content-bundle.mjs` (mirrors verify:phase-1/2/3/3a, L19-22)."
    - ".planning/REQUIREMENTS.md CTX rows updated per D-04-01 with an AI-07-style note: CTX-02 → typed input-only re-pack seam on the optimizer/runAgentTurn path with NO consumer in P4 (page/state-change triggers land with Phase 4a/7); CTX-03 → minimal-mode compact-prompt selection, NOT a new prompting subsystem; the Phase-4 CTX ids are DISAMBIGUATED from the spec §28.3 trust-aware CTX-01..06 namespace (Phase 4b). §18 is authoritative over these rows."
    - ".planning/ROADMAP.md Phase 4 'Plans: TBD' is replaced with the 04-01..04-07 plan list; the §18 master phase order and the success criteria are untouched."
    - ".planning/PRODUCT_SPEC_v0_1.md W-1 canonicalizations land: Appendix A gains the compact planner/renderer prompt constants (D-04-11, mirroring src/core/prompts/index.ts); Appendix B gains chat.messageTooLong (D-04-15, mirroring strings.ts); CONTEXT_TOO_LARGE is verified already present in Appendix C.2 (L3512/L5040 — scoped-regex re-verified, no new code needed)."
    - "verify:phase-4 passes green: eslint, prettier --check, tsc --noEmit, wxt build, the full vitest suite (tests/core/context/** + tests/core/ai/** + tests/components/** + tests/fixtures/**), and the content-bundle isolation check."
    - "The R-3 isolation scan still passes: the phase added NO imports to the background SW or content scripts (AI + IndexedDB stay in Side Panel/Standalone)."
    - "The deletion gate holds: zero src/ or tests/ imports of the deleted context-helper module (04-06 re-pointed every consumer incl. ContextOptimizer.test.ts and PersonaInjector.test.ts before the git rm)."
    - "§18 'DONE when' for Phase 4 is satisfied: tier tests pass; context overflow degrades instead of failing; minimal mode blocks MCP chaining (via capsForTier tiny/small mcpChaining:false — the optimizer marks minimalMode, RAG enforcement is the 5a consumer) ; ContextProvenanceManifest is attached to every OptimizedContext."
  artifacts:
    - "package.json"
    - ".planning/REQUIREMENTS.md"
    - ".planning/ROADMAP.md"
    - ".planning/PRODUCT_SPEC_v0_1.md"
  key_links:
    - "verify:phase-4 mirrors verify:phase-3a (package.json L22) + the isolation check (tests/isolation/check-content-bundle.mjs)."
    - "The CTX re-map note follows the AI-07 (L44) + AGT-02/05 (L56-58) precedents — §18 remains authoritative over the REQUIREMENTS.md rows."
    - "The W-1 scoped-regex verify asserts the canonical additions inside the spec slices (03-01 precedent) — compact constants in Appendix A, messageTooLong in Appendix B, CONTEXT_TOO_LARGE already in C.2."
  flagged_assumptions:
    - "CTX-01..04 [unresolved — spec-less probe #1110, all four surfaced]: every probe edge was implemented + proven across 04-01..06 (tiers/budgets 04-01+04-04; CTX-02 seam 04-04; CTX-03 compact selection 04-04; CTX-04 ladder+terminal 04-02/04-04/04-06); this plan's verify:phase-4 run is the aggregate seal. The rows stay unresolved in the probe ledger (never auto-dismissed — flagged-unverified disposition)."
    - "A2..A7 [04-01, ASSUMED — user confirmation gate]: MODEL_CONTEXT_WINDOWS values; if a value is wrong the tier/caps shift (mitigated by 70% budget + ladder). Open Q1 (provider-declared window consult) is deferred per research recommendation — revisit when Settings owns per-model windows."
    - "Open Q2 [research]: the canonical planner/renderer prompts stay UNCONSUMED in the default path (byte-identical, cache-stability) — only minimal mode selects the compact constants; wiring the default path is a Phase-4.1-style decision, not §18."
    - "A8..A12 [04-01/04-04, ASSUMED]: CJK ratio threshold, column→kind mapping, percentages-on-inputBudget, conversationId 'default', StructuredOutput inline chars/4 left as-is."
    - "RAG synthesis fallback enforcement remains a Phase-5a consumer concern (D-04-14) — P4 marks minimalMode only; §18 'DONE when' bullet reads 'minimal mode blocks MCP chaining (and LLM-Wiki RAG synthesis)' — the P4 deliverable is the minimalMode MARK + capsForTier mcpChaining block; RAG enforcement ships with its 5a consumer per the locked decision."
  prohibitions:
    - "No verify:phase-4 without the isolation check (R-3 — the rewire must not leak AI/vault tokens into background/content bundles)."
    - "No new REQUIREMENTS.md CTX id and no §28.3 namespace collision (D-04-01 disambiguation — Phase 4b owns the trust-aware CTX-01..06 ids)."
    - "No §18 master phase-order change (canonical order untouched)."
    - "No exact test-count assertions in the gate (P-5 — full-suite run, documentation subset markers only)."
    - "No skip of eslint/prettier/tsc/wxt-build/vitest/isolation in the chain (§24 pattern)."
    - "No new codes/strings outside the spec (W-1 — every canonical addition mirrors a spec slice; CONTEXT_TOO_LARGE already canonical at C.2 L3512/L5040)."
---

<!-- 04-07 (2026-08-12): the phase gate (Golden Rule 10 — a phase is not done until
     verify:phase-4 passes). verify:phase-4 script (§24 pattern), the REQUIREMENTS.md
     CTX re-map notes (D-04-01), the ROADMAP plan list, the spec W-1 canonicalizations
     (compact constants Appendix A, messageTooLong Appendix B, CONTEXT_TOO_LARGE
     re-verified in C.2), and the full-suite green seal. -->

<objective>
Seal Phase 4: add the `verify:phase-4` script (§24 chain), record the scope reconciliation in REQUIREMENTS.md/ROADMAP.md (D-04-01), canonicalize the phase's additions into the spec (W-1 gate: compact constants Appendix A, messageTooLong Appendix B, CONTEXT_TOO_LARGE re-verified C.2), and run the full chain green — the aggregate proof that CTX-01..04 (the four spec-less probe edges) are satisfied.

Purpose: A phase is not done until its gate passes (Golden Rule 10). The doc updates keep the planning record honest (D-04-01's re-map + namespace disambiguation), the W-1 gate keeps canonical codes/strings single-sourced, and the full-suite green run proves the drop-in rewire (04-06) left zero regressions while the R-3 isolation boundary held.

Output: verify:phase-4 script, REQUIREMENTS.md/ROADMAP.md reconciliation, spec canonicalizations, and a green full-suite gate seal — Phase 4 DONE per §18.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 4 block L211-223 — 'Plans: TBD')
@.planning/REQUIREMENTS.md (CTX-01..04 rows L60-65; AI-07 precedent L44)
@.planning/PRODUCT_SPEC_v0_1.md (Appendix A L4072+, Appendix B L4139+, Appendix C.2 L3512/L5040, §18 Phase-4 L2676-2703)
@package.json (verify:phase-3a script L22 — the copy template)
@.planning/phases/03a-agent-reliability-and-evidence/03a-05-PLAN.md (the phase-gate plan precedent)
</context>

## Artifacts this phase produces

- **MODIFY** `package.json`: `verify:phase-4` script.
- **MODIFY** `.planning/REQUIREMENTS.md`: CTX-01..04 re-map note (D-04-01) + §28.3 namespace disambiguation.
- **MODIFY** `.planning/ROADMAP.md`: Phase 4 plan list (replaces 'Plans: TBD').
- **MODIFY** `.planning/PRODUCT_SPEC_v0_1.md`: Appendix A compact constants + Appendix B chat.messageTooLong (W-1).

<tasks>

<task type="auto">
  <name>Task 1: Add verify:phase-4 + the REQUIREMENTS.md CTX re-map note + ROADMAP plan list</name>
  <files>package.json, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>package.json scripts (L14-23 — copy verify:phase-3a L22 verbatim, rename); .planning/REQUIREMENTS.md CTX-01..04 rows (L60-65) + the AI-07 re-map note precedent (L44); .planning/ROADMAP.md Phase 4 block (L211-223)</read_first>
  <action>Add `"verify:phase-4": "eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run && node tests/isolation/check-content-bundle.mjs"` to package.json scripts (identical chain to verify:phase-3a — §24 pattern). In REQUIREMENTS.md, add an AI-07-style block-quote note under the CTX-01..04 rows: CTX-02 = typed input-only re-pack seam on the optimizer/runAgentTurn path (onStreamDelta precedent), NO consumer in P4 (page/state-change triggers land with Phase 4a/7); CTX-03 = minimal-mode compact-prompt selection, NOT a new prompting subsystem; §18 is authoritative over these rows; the Phase-4 CTX ids are DISAMBIGUATED from the spec §28.3 trust-aware CTX-01..06 namespace (Phase 4b). In ROADMAP.md, replace 'Plans: TBD' (L224) with the plan list: 04-01 foundation (window map + TokenBudget) · 04-02 degradation primitives (ContextPack + ContextCompressor) · 04-03 provenance manifest + lockstep · 04-04 ContextOptimizer + compact prompts + CTX-02 seam · 04-05 Router window stamp · 04-06 hook rewire + contextHelper deletion · 04-07 phase gate. Use Edit (scoped) — never a whole-file Write (ROADMAP/REQUIREMENTS edits are scoped replacements only).</action>
  <acceptance_criteria>
    - `package.json` contains `verify:phase-4` with the exact §24 chain string.
    - REQUIREMENTS.md has the D-04-01 note (CTX-02/CTX-03 re-map + §28.3 disambiguation) in the AI-07 block-quote style; no CTX id is added or renamed.
    - ROADMAP.md Phase 4 block lists 04-01..04-07; the success criteria and §18 master phase order are untouched.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "verify:phase-4" package.json && grep -v "^#" .planning/REQUIREMENTS.md | grep -c "typed input-only re-pack seam" && grep -v "^#" .planning/ROADMAP.md | grep -c "04-07 phase gate"</automated>
  </verify>
  <done>Script + re-map note + plan list land; the docs record matches what was built (D-04-01).</done>
</task>

<task type="auto">
  <name>Task 2: W-1 canonicalize the phase's additions into the spec</name>
  <files>.planning/PRODUCT_SPEC_v0_1.md</files>
  <read_first>.planning/PRODUCT_SPEC_v0_1.md Appendix A (L4072+ — insert the compact constants beside planner/renderer, verbatim style); Appendix B (L4139+ — insert chat.messageTooLong beside contextReduced L4149); Appendix C.2 (L3512/L5040 — CONTEXT_TOO_LARGE already present, scoped-regex re-verify); src/core/prompts/index.ts + src/core/i18n/strings.ts (the mirrors)</read_first>
  <action>Edit the spec IN PLACE (scoped, never whole-file): (1) Appendix A — add the compact per-role constants `planner.compact.system` + `renderer.compact.system` mirroring the src/core/prompts/index.ts text verbatim (D-04-11, Appendix A addendum); (2) Appendix B — add `messageTooLong: 'This message is too long for the selected model.'` beside contextReduced (D-04-15, mirroring strings.ts verbatim); (3) re-verify CONTEXT_TOO_LARGE is present in Appendix C.2 (it is — L3512/L5040; if the scoped grep unexpectedly fails, add the /^CONTEXT_TOO_LARGE$/m line in the C.2 block). No other spec sections change.</action>
  <acceptance_criteria>
    - Scoped grep: the compact constant lines exist in the Appendix A slice; the messageTooLong line exists in the Appendix B slice; the CONTEXT_TOO_LARGE line exists in the C.2 slice.
    - The constants/strings in the spec byte-match the source mirrors (src/core/prompts/index.ts, src/core/i18n/strings.ts).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "compact.system" .planning/PRODUCT_SPEC_v0_1.md && grep -n "messageTooLong" .planning/PRODUCT_SPEC_v0_1.md && grep -c "^CONTEXT_TOO_LARGE$" .planning/PRODUCT_SPEC_v0_1.md</automated>
  </verify>
  <done>Spec canonicalized (W-1); every shipped code/string mirrors a spec slice.</done>
</task>

<task type="auto">
  <name>Task 3: Run verify:phase-4 — the full-suite green seal</name>
  <files>package.json</files>
  <read_first>(none — gate run only; see execution_context)</read_first>
  <action>Run `pnpm run verify:phase-4`. Fix any eslint/prettier/tsc/wxt-build/vitest/isolation failure until the entire chain is green. Then run the targeted phase evidence greps: (a) `pnpm vitest run tests/core/context` explicitly green (the §18 required suites: ContextOptimizer/ContextCompressor/TokenBudget + the manifest lockstep suite); (b) zero imports of the deleted context-helper module in src/ and tests/; (c) the R-3 isolation check passed within the chain (content bundle has no AI/vault tokens). Golden Rule 10: nothing is done until this passes.</action>
  <acceptance_criteria>
    - `pnpm run verify:phase-4` exits 0 (eslint + prettier + tsc + wxt build + full vitest + isolation).
    - Grep gate: zero src/ or tests/ imports of the deleted module's specifier; `tests/core/context` suite green in isolation.
    - No exact test-count assertion anywhere in the gate (P-5).
  </acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-4</automated>
  </verify>
  <done>verify:phase-4 green end-to-end — Phase 4 DONE per §18; all four CTX probe edges sealed by shipped suites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built artifacts → isolation check | The wxt build output crosses the R-3 boundary — the content/background bundles must stay free of AI/vault symbols |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-31 | Information Disclosure | built content bundle | high | mitigate | The isolation check (check-content-bundle.mjs) runs INSIDE verify:phase-4 (R-3) — AI runtime/vault/IndexedDB symbols in the content or background bundles fail the gate (03-09 extended token set) |
| T-04-32 | Tampering | spec/registry drift | low | mitigate | W-1 scoped-regex verify anchors every canonical code/string to its spec slice; the lockstep guard (04-03) pins manifest/section kind parity |
| T-04-33 | Tampering | npm installs | low | mitigate | No packages installed in this phase (R-9 — verified against package.json) |
</threat_model>

<verification>
- `pnpm run verify:phase-4` — the complete §24 chain (the phase gate itself).
- `pnpm vitest run tests/core/context` — the §18-required suites, explicitly green.
- Grep gates: zero deleted-module imports in src/ and tests/; canonical additions present in the spec slices (Task 2).
</verification>

<success_criteria>
- verify:phase-4 green: eslint, prettier --check, tsc --noEmit, wxt build, full vitest, isolation check.
- REQUIREMENTS.md/ROADMAP.md/spec records match what was built (D-04-01 + W-1).
- §18 Phase-4 DONE-when all satisfied; the four unresolved CTX probe edges are sealed by shipped suites (flagged-unverified disposition retained).
- Phase 4 complete — ready for Phase 4a per the §18 master order.
</success_criteria>

<output>
Create `.planning/phases/04-context-adaptive-execution/04-07-SUMMARY.md` when done.
</output>
