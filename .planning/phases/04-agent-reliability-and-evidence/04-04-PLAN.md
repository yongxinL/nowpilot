---
phase: 04-agent-reliability-and-evidence
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - src/core/ai/AgentOrchestrator.ts
  - src/components/chat/useChatStreaming.ts
  - tests/core/ai/AgentOrchestrator.test.ts
autonomous: true
requirements: [AGT-03, AGT-04]
user_setup: []

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An aborted runAgentTurn RESOLVES with an AgentTurnOutcome carrying status 'aborted' — it does NOT reject with a thrown AbortError (Q1 / AGT-04 DONE-when 'abort produces aborted')"
    - "The aborted outcome carries streamedText '', operationId == input.operationId, the trajectory terminal phase 'aborted', and persistTurn is NEVER invoked (D-45 — partial dropped)"
    - "The consumer (useChatStreaming) branches on output.status === 'aborted' and does not treat the aborted turn as a failure or an error toast; the stopped note comes from handleStopGenerating as today"
    - "Non-caller aborts (e.g. renderer 'aborted' termination, mid-stream errors) still surface through the existing error path — only the caller-signal AbortError converts to the returned outcome (defensive catch remains)"
    - "The closed status set carries the aborted terminal: trajectory terminal state and outcome status both 'aborted' (C.1, AGT-04)"
  artifacts:
    - path: "src/core/ai/AgentOrchestrator.ts"
      provides: "Central AbortError conversion at the runAgentTurn boundary — caller-signal abort returns { status: 'aborted', streamedText: '', ... } instead of throwing; trajectory enters 'aborted'; persistTurn not invoked"
      exports: ["runAgentTurn", "AgentTurnInput", "AgentTurnOutcome"]
    - path: "src/components/chat/useChatStreaming.ts"
      provides: "Branch on output.status === 'aborted' after runAgentTurn — clears generating state, skips the buffer flush and the config-required toast, keeps the defensive AbortError catch for non-caller aborts"
      exports: ["useChatStreaming"]
  key_links:
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/components/chat/useChatStreaming.ts"
      via: "runAgentTurn now RESOLVES on abort — the hook branches on output.status === 'aborted' (was: catch DOMException AbortError at line ~258)"
      pattern: "status === 'aborted'|AbortError"
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/trajectory.ts"
      via: "the boundary catch enters the trajectory terminal 'aborted' (validated against the closed table — every non-terminal row allows 'aborted')"
      pattern: "enter\\('aborted'\\)"
  prohibitions:
    - "persistTurn must never fire on the aborted path (D-45) — the aborted outcome skips the persist seam entirely"
    - "No new error codes — 'aborted' is the C.1 status value, not a §21.6 code (D-38)"
    - "Do not swallow ProviderError or other non-abort errors — only DOMException AbortError converts to the outcome"
---

<objective>
Expansion: convert abort handling from thrown-AbortError to a RETURNED 'aborted' AgentTurnOutcome at the runAgentTurn boundary (Q1 — the phase's most invasive behavioral change), update the sole consumer (useChatStreaming) to branch on the returned status, and rework test cases (e) that asserted the throw contract.

Purpose: satisfy the AGT-03/AGT-04 DONE-when ("abort produces `aborted`") — every turn, including an aborted one, resolves with a structured outcome; the partial is still dropped and persistTurn never fires (D-45).
Output: boundary abort conversion in `AgentOrchestrator.ts`, consumer branch in `useChatStreaming.ts`, reworked case (e) in `AgentOrchestrator.test.ts`, and the full phase gate green.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §28.2 AGT-03/AGT-04 + Appendix C.1 (AgentTurnOutcome status set incl. 'aborted') + §20.6 (ActiveStreamState — distinct from trajectory) + D-45 persist seam (03-06 SUMMARY)
@.planning/phases/04-agent-reliability-and-evidence/04-CONTEXT.md  # D-66 (abort → aborted), D-63 (in-memory trajectory), D-45 reference
@.planning/phases/04-agent-reliability-and-evidence/04-RESEARCH.md  # Pitfall 3 (throw vs returned outcome — Q1), Open Question 1 (recommendation: convert to returned outcome), A4
@.planning/phases/04-agent-reliability-and-evidence/04-PATTERNS.md  # useChatStreaming.ts section (Q1 sketch — branch on output.status), AgentOrchestrator.ts section (three throw sites 215/256/300), tests section (case (e) 241-272)
@src/core/ai/AgentOrchestrator.ts  # the three DOMException('aborted','AbortError') throw sites (loop-top ~300, route ~215, renderer ~256) + finish() + trajectory hooks from 04-01..04-03
@src/components/chat/useChatStreaming.ts  # runAgentTurn call at 209, abort catch 258-261, reasonCode branch 223, streamedText flush 244-252
@tests/core/ai/AgentOrchestrator.test.ts  # case (e) 241-272 — asserts rejects.toThrow(DOMException)
@tests/core/ai/chat-integration.test.ts  # case (c) abort drops the partial — must stay green (journalSpy not called)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boundary AbortError conversion — runAgentTurn resolves with status 'aborted' instead of throwing</name>
  <files>src/core/ai/AgentOrchestrator.ts</files>
  <read_first>@src/core/ai/AgentOrchestrator.ts (the three DOMException('aborted','AbortError') throw sites — loop-top ~300, route abort ~215, renderer aborted ~256; the finish() outcome assembly; the trajectory hooks) · 04-RESEARCH.md Pitfall 3 + Open Question 1 (A4: central catch at the runAgentTurn boundary, streamedText '', no persistTurn) · 04-PATTERNS.md AgentOrchestrator.ts section (Q1 conversion sketch) · @src/core/ai/trajectory.ts (closed table — 'aborted' is a legal target from every non-terminal row)</read_first>
  <action>Convert abort to a returned outcome in `src/core/ai/AgentOrchestrator.ts` (Q1/A4): wrap the entire body of `runAgentTurn` (the loop + finish + configurationRequiredOutcome paths) in a try/catch. In the catch: when `err instanceof DOMException && err.name === 'AbortError'` — the caller-signal abort — return the aborted AgentTurnOutcome instead of rethrowing: status 'aborted', reasonCode 'aborted' (the C.1 status value doubles as the descriptive reason; do NOT invent a §21.6 code, D-38), operationId input.operationId, evidence [], plannerCalls/toolCalls the loop's current counters, streamedText '', toolResults the accumulated results, and the trajectory snapshot with the tracker having entered the terminal 'aborted' phase (enter('aborted') is validated against the closed table — legal from every non-terminal row, AGT-01). persistTurn is NEVER invoked on this path (D-45 — the partial is dropped; the existing finish() persist seam is inside the try and only fires on completed turns). Any OTHER error (ProviderError from the renderer CR-06 path, routed provider errors, etc.) rethrows unchanged — only the caller-signal AbortError converts (the three existing throw sites inside the try can stay as-is: the boundary catch is the single conversion point — do NOT add per-site returns, keeping the loop structure intact). Keep the loop-top `if (input.abortSignal.aborted) throw new DOMException('aborted', 'AbortError')` as the first check (Appendix I) — the boundary catch converts it. Add a debugLog entry for the aborted conversion (SCREAMING_SNAKE code + payload {operationId, plannerCalls, toolCalls}) via debugLog. Strict-clean only. Run `pnpm run lint` — note test case (e) will FAIL at this point (it asserts the throw); that rework is Task 2 of this plan (expected mid-plan RED on that one case group).</action>
  <acceptance_criteria>`pnpm run lint` passes; `grep -n "status: 'aborted'" src/core/ai/AgentOrchestrator.ts` shows the boundary catch returning the aborted outcome; `grep -n "AbortError" src/core/ai/AgentOrchestrator.ts` shows the catch condition (err instanceof DOMException && err.name === 'AbortError'); `grep -n "persistTurn" src/core/ai/AgentOrchestrator.ts` shows the persist invocation remains ONLY inside the completed-turn path (no persist call in the catch); zero new `@ts-expect-error NP-STRICT` markers. (Case (e) is RED until Task 2 — expected.)</acceptance_criteria>
  <verify>
    <automated>pnpm run lint</automated>
  </verify>
  <done>Boundary catch converts caller-signal AbortError to a returned 'aborted' outcome; persistTurn never fires on abort; other errors rethrow unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Consumer branch + reworked abort tests + full phase gate</name>
  <files>src/components/chat/useChatStreaming.ts, tests/core/ai/AgentOrchestrator.test.ts</files>
  <read_first>@src/components/chat/useChatStreaming.ts (runAgentTurn call at 209-221, the abort catch 258-261, handleStopGenerating 280-289 — the stopped note is appended there synchronously) · @tests/core/ai/AgentOrchestrator.test.ts (case (e) 241-272 — rework target; baseInput 117-126; the outcome assertions from 04-01) · @src/core/ai/AgentOrchestrator.ts (the boundary conversion from Task 1 — exact aborted outcome shape) · 04-PATTERNS.md useChatStreaming.ts section (Q1 branch sketch) + tests section (case (e) rework)</read_first>
  <action>Update `src/components/chat/useChatStreaming.ts` for the returned aborted outcome (Q1): after `const output = await runAgentTurn({...})` and BEFORE the `configuration_required` reasonCode branch, add `if (output.status === 'aborted') { setIsGenerating(false); return; }` — the partial is dropped, nothing persisted (the orchestrator never invoked persistTurn on the aborted path), and the stopped note was already appended synchronously by handleStopGenerating (line ~286). This branch sits INSIDE the existing try, so the `finally` still clears np_active_stream and isGenerating. Keep the existing DOMException AbortError catch (lines 258-261) as a DEFENSIVE fallback for non-caller aborts (e.g. renderer-internal 'aborted' terminations that never reach the boundary catch) — it stays green and must not be removed. Do not change the reasonCode-branching consumers (configuration_required at 223, empty-answer mark at 252). Rework case (e) in `tests/core/ai/AgentOrchestrator.test.ts` (Q1 — the throw contract is replaced by the returned outcome): test 1 (pre-aborted signal) — `runAgentTurn(baseInput({ abortSignal: controller.signal, persistTurn }))` now RESOLVES: assert `output.status` is 'aborted', `output.streamedText` is '', `output.operationId` is 'op-orchestrator', `output.reasonCode` is 'aborted', `output.trajectory.phase` is 'aborted', `output.evidence` equals [], and `persistTurn` was NOT called; test 2 (abort mid-stream during the renderer with SlowAbortStreamProvider) — same assertions (status 'aborted', streamedText '', persistTurn not called). Verify the full gate: `npx vitest run tests/core/ai/AgentOrchestrator.test.ts tests/core/ai/chat-integration.test.ts` (chat-integration case (c) — abort drops the partial — must stay green: the hook's new branch clears generating state, journalSpy still never fires), then `pnpm run verify:phase-4`.</action>
  <acceptance_criteria>`npx vitest run tests/core/ai/AgentOrchestrator.test.ts tests/core/ai/chat-integration.test.ts` green (case (e) resolves with 'aborted' status + streamedText '' + persistTurn not called; chat-integration (c) still asserts journalSpy not called); `pnpm run verify:phase-4` GREEN (the full phase gate — trajectory + OutcomeVerifier + whole tests/core/ai suite); `grep -n "status === 'aborted'" src/components/chat/useChatStreaming.ts` shows the branch before the configuration_required check; the defensive AbortError catch remains in the hook.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-4</automated>
  </verify>
  <done>Abort resolves with 'aborted' outcome end-to-end (orchestrator → consumer); case (e) and chat-integration (c) green; full phase-4 gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller AbortSignal → runAgentTurn | The user's Stop action crosses here — previously a thrown AbortError, now a returned 'aborted' outcome; the boundary catch is the single conversion point |
| runAgentTurn → useChatStreaming | The aborted outcome exits the loop into the consumer — the hook must branch on status, not treat it as a failure toast |
| runAgentTurn → persistTurn seam | The D-45 boundary — an aborted turn must never reach the journaled append (partial dropped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-10 | Tampering | aborted-turn persistence | high | mitigate | The boundary catch returns the aborted outcome WITHOUT invoking persistTurn (D-45 — partial dropped); the persist call remains inside the completed-turn path only; asserted by reworked case (e) (persistTurn not called) + chat-integration (c) (journalSpy never fires) |
| T-4-11 | Spoofing | aborted outcome mislabeling | medium | mitigate | Only DOMException('aborted','AbortError') converts — ProviderError (CR-06 renderer mid-stream error) and routed errors rethrow unchanged (case (i) stays green); an internal failure cannot masquerade as a user abort |
| T-4-12 | DoS | consumer error-toast storm | low | mitigate | The hook branches on status 'aborted' before any error toast — a stopped turn produces no 'Generation failed' toast and no partial text (handleStopGenerating owns the stopped note) |
| T-4-SC | Tampering | package installs | high | mitigate | Package legitimacy gate N/A — zero new dependencies (no install commands in this plan). |
</threat_model>

<verification>
- `pnpm run lint` green after each task commit.
- `pnpm run verify:phase-4` green at plan end — THE phase gate: trajectory tests + OutcomeVerifier tests + full tests/core/ai suite (AgentOrchestrator + chat-integration).
- Grep guard: zero new `@ts-expect-error NP-STRICT` markers; the defensive AbortError catch remains in useChatStreaming.
- Case (e) must assert resolution (not rejection) with status 'aborted' + persistTurn not called.
</verification>

<success_criteria>
1. Abort produces a returned 'aborted' AgentTurnOutcome (AGT-04 DONE-when) — never a thrown AbortError from the runAgentTurn boundary.
2. persistTurn never fires on abort (D-45); the partial is dropped.
3. useChatStreaming branches on status 'aborted' — no failure toast, no partial text, stopped note preserved.
4. Non-caller errors rethrow unchanged; defensive catch retained.
5. `pnpm run verify:phase-4` GREEN — phase 4 complete per §18.
</success_criteria>

<output>
Create `.planning/phases/04-agent-reliability-and-evidence/04-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/ai/AgentOrchestrator.ts | + boundary AbortError conversion → returned 'aborted' outcome (Q1/A4) | **[this plan]** (module evolved 04-01..04-03) |
| src/components/chat/useChatStreaming.ts | + output.status === 'aborted' branch; defensive AbortError catch retained | **[this plan]** |
| tests/core/ai/AgentOrchestrator.test.ts | + case (e) reworked: resolves with 'aborted' outcome, persistTurn not called | **[this plan]** |