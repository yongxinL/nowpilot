---
phase: 04-agent-reliability-and-evidence
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/core/ai/AgentOrchestrator.ts
  - tests/core/ai/AgentOrchestrator.test.ts
autonomous: true
requirements: [AGT-02]
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A side-effecting tool result (ok === true, verifier registered for the tool) WITHOUT CompletionEvidence forces the outcome status to 'partial' with a guard reasonCode — never 'completed' (AGT-02 / risk R-8)"
    - "The guard runs unconditionally in finish() — it overrides buildOutcome's status, so 'never a clean success' holds even when the verifier itself passes (D-65 ordering)"
    - "The guard is exercised by an injected fake side-effecting result via the input.verifiers seam + a mocked ExecutorService.execute (D-67 — no fake tools registered)"
    - "The reasonCode for a guard downgrade is a descriptive literal, not an invented §21.6 error code (D-38)"
  artifacts:
    - path: "src/core/ai/AgentOrchestrator.ts"
      provides: "finish() completion guard wiring — guardMissingEvidence(toolResults, effectiveVerifiers) forces status 'partial' + guard reasonCode before the outcome is returned"
      exports: ["runAgentTurn", "AgentTurnInput", "AgentTurnOutcome"]
  key_links:
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/OutcomeVerifier.ts"
      via: "finish() calls guardMissingEvidence with the effective verifier set AFTER buildOutcome — the guard overrides buildOutcome's status unconditionally"
      pattern: "guardMissingEvidence"
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/types.ts"
      via: "the guard reads ToolExecutionResult.evidence (the spec 4339 seam from plan 04-01) on each ok result"
      pattern: "\\.evidence"
  prohibitions:
    - "No fake tool registration to exercise the guard — inject results via ExecutorService.execute mocks + input.verifiers (D-67)"
    - "No new error-code constants for the guard reason — descriptive literal only (D-38)"
---

<objective>
Expansion: wire the Renderer completion guard (D-65) into the orchestrator's finish() so a side-effecting tool result with no CompletionEvidence can never produce a clean 'completed' outcome (AGT-02 / risk R-8), and prove it with the false-completion test the phase's DONE-when requires.

Purpose: close the "skips the verifier and marks a write done" hole at the outcome layer — the single choke point for "never silently claims success" (golden rule 8). The guard helper already ships from plan 04-01; this plan wires it unconditionally and tests it with an injected fake side-effecting result.
Output: guard wiring in `AgentOrchestrator.ts` + the false-completion test case in `AgentOrchestrator.test.ts`.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §28.2 AGT-02 + golden rule 8 (spec 214) + risk R-8 (spec 229) + spec 4339 (evidence seam comment "set for side-effecting tools")
@.planning/phases/04-agent-reliability-and-evidence/04-CONTEXT.md  # D-65 (renderer completion guard), D-64 (zero verifiers), D-67 (injected results)
@.planning/phases/04-agent-reliability-and-evidence/04-RESEARCH.md  # Pattern 4 (guard ordering — guard overrides buildOutcome status), Pitfall 4 (false-completion test), A5
@.planning/phases/04-agent-reliability-and-evidence/04-PATTERNS.md  # AgentOrchestrator.ts section (D-65 sketch), OutcomeVerifier.ts section
@src/core/ai/AgentOrchestrator.ts  # created/evolved in 04-01 — finish() integration point (currently around lines 231-280)
@src/core/ai/OutcomeVerifier.ts  # created in 04-01 — guardMissingEvidence export
@src/core/ai/ExecutorService.ts  # object export at :71 — vi.spyOn(ExecutorService, 'execute') injection seam
@tests/core/ai/AgentOrchestrator.test.ts  # extended in 04-01 — add the guard case group
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renderer completion guard wiring — ok side-effecting result without evidence forces status 'partial'</name>
  <files>src/core/ai/AgentOrchestrator.ts</files>
  <read_first>@src/core/ai/AgentOrchestrator.ts (the finish() built in 04-01 — locate the buildOutcome call and the outcome assembly) · @src/core/ai/OutcomeVerifier.ts (guardMissingEvidence signature — created 04-01) · 04-RESEARCH.md Pattern 4 (guard ordering recommendation: guard evaluates before/overrides buildOutcome's status) + Pitfall 4 (the DONE-when requires a false-completion test) · 04-PATTERNS.md AgentOrchestrator.ts section D-65 sketch</read_first>
  <action>Wire the completion guard into `finish()` in `src/core/ai/AgentOrchestrator.ts` (D-65): import `guardMissingEvidence` from `./OutcomeVerifier`. After `buildOutcome(...)` computes the base outcome and BEFORE the outcome is returned (and BEFORE the persistTurn seam fires), evaluate `const missing = guardMissingEvidence(toolResults, effectiveVerifiers)` where effectiveVerifiers = `{ ...VerifierRegistry.getAll(), ...(input.verifiers ?? {}) }` (the same set passed to buildOutcome). When `missing` is true, FORCE the outcome: status 'partial' and reasonCode 'missing_evidence' (a descriptive literal — do NOT export it as an error-code constant, D-38); the evidence array, counters, operationId, and trajectory remain whatever buildOutcome produced. The override is unconditional — it wins over buildOutcome's 'completed'/'failed' status (research A5 ordering: guard overrides after buildOutcome so "never a clean success" holds even when the registered verifier itself returns ok). The renderer still renders the final answer (the streamedText is produced as usual — the downgrade is on the outcome STATUS, not the rendered text, matching the "renderer must not claim execution without matching evidence" requirement at the outcome layer; UI "Done" surfacing is Phase 15). Add a debugLog entry when the guard fires (code style: SCREAMING_SNAKE + payload {operationId, toolName(s) with missing evidence, reasonCode}) — use debugLog, never console.log. Do NOT touch the abort throw sites or the loop in this task (they are 04-03/04-04 work). Keep the code strict-clean (zero @ts-expect-error NP-STRICT markers). Run `pnpm run lint` and `npx vitest run tests/core/ai/AgentOrchestrator.test.ts` (existing cases must stay green — with zero verifiers registered the guard is vacuous in production, D-64).</action>
  <acceptance_criteria>`pnpm run lint` passes; `npx vitest run tests/core/ai/AgentOrchestrator.test.ts` stays green (all existing cases incl. the 04-01 outcome-contract assertions); `grep -n "guardMissingEvidence" src/core/ai/AgentOrchestrator.ts` shows the import + a call in finish(); the guard call is positioned after buildOutcome and before the return/persist seam; no `@ts-expect-error NP-STRICT` marker added.</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/ai/AgentOrchestrator.test.ts</automated>
  </verify>
  <done>Guard wired unconditionally in finish() (import + override after buildOutcome); production behavior unchanged with zero verifiers (D-64 vacuity); existing tests green.</done>
</task>

<task type="auto">
  <name>Task 2: False-completion test — injected ok side-effecting result without evidence → status 'partial', never 'completed'</name>
  <files>tests/core/ai/AgentOrchestrator.test.ts</files>
  <read_first>@tests/core/ai/AgentOrchestrator.test.ts (baseInput 117-126, seedEnv 97-115, planSpy pattern 175-177, the 04-01 outcome assertions) · @src/core/ai/AgentOrchestrator.ts (the guard wiring from Task 1 — verify the exact override semantics) · @src/core/ai/ExecutorService.ts (execute contract + object export for vi.spyOn, D-67 seam) · @src/core/ai/OutcomeVerifier.ts (Verifier interface — the fake verifier must satisfy it) · 04-RESEARCH.md Pitfall 4 (the false-completion proof) + D-67 (injected results, no fake tool registration)</read_first>
  <action>Add a new case group `(j) completion guard (D-65/AGT-02) — ok side-effecting result without evidence is never 'completed'` to `tests/core/ai/AgentOrchestrator.test.ts`: build a fake side-effecting result via `vi.spyOn(ExecutorService, 'execute').mockResolvedValue({ toolName: 'side_effect_tool', ok: true, data: null, error: null, durationMs: 5 })` — NOTE: no `evidence` field on the result (that is the R-8 hole). Register a fake verifier for 'side_effect_tool' via the `input.verifiers` override (a plain object satisfying the Verifier interface: postconditionId 'fake-postcondition', verify resolving `{ ok: true }` — the verifier itself PASSES, proving the guard fires independent of verifier outcome). Script the planner with `vi.spyOn(PlannerService, 'plan')` to return run_tool 'side_effect_tool' then answer 'direct_answer' (the zero-tool schema cannot emit run_tool, so the planSpy is required — D-46 pattern). Run `runAgentTurn(baseInput({ verifiers: { side_effect_tool: fakeVerifier } }))`. Assert: `output.status` is 'partial' (NEVER 'completed' — the AGT-02 proof), `output.reasonCode` is 'missing_evidence', `output.evidence` has length 1 (buildOutcome ran the registered verifier and produced evidence — the guard still overrode the status), `output.operationId` is 'op-orchestrator', `output.toolResults[0].toolName` is 'side_effect_tool', and `output.streamedText` is the rendered answer (the render still ran — only the status is downgraded). Add a second assertion in the same group: when the injected result DOES carry evidence (add `evidence: { toolName: 'side_effect_tool', operationId: 'op-orchestrator', postconditionId: 'fake-postcondition', ok: true, verifiedAt: Date.now() }` to the mockResolvedValue) the outcome is 'completed' — the guard is vacuous when evidence is present. Run the file; then run `pnpm run verify:phase-4` (must stay green — the guard is exercised only by this injection, D-64).</action>
  <acceptance_criteria>`npx vitest run tests/core/ai/AgentOrchestrator.test.ts` green with the new case group (j): ok-without-evidence → 'partial'/'missing_evidence' (never 'completed') and ok-with-evidence → 'completed'; `pnpm run verify:phase-4` green; zero new tool registrations in ToolRegistry (grep ToolRegistry.register in the test file = 0 new calls — D-67 forbids fake tool registration).</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-4</automated>
  </verify>
  <done>False-completion test proves ok-side-effecting-without-evidence is 'partial'; the with-evidence control proves 'completed'; no fake tools registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExecutorService.execute → ToolExecutionResult | The executor's result crosses into the guard — a result claiming ok WITHOUT the evidence a registered verifier implies (R-8: skipped postcondition verification) is the threat this plan closes |
| finish() → AgentTurnOutcome | The guarded status exits the loop — 'completed' must be impossible while a side-effecting result lacks evidence (AGT-02) |
| AgentTurnInput.verifiers (test seam) | Injected verifier maps cross into the guard — D-67 test-only seam, never a production registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-05 | Spoofing / Tampering | finish() completion guard | high | mitigate | Guard override is UNCONDITIONAL after buildOutcome (research A5 ordering): ok + verifier-present + evidence-absent → status 'partial' + reasonCode 'missing_evidence', never 'completed' — the R-8 write-marked-done hole is closed at the outcome layer (AGT-02); proven by the false-completion test (Task 2) |
| T-4-06 | Tampering | injected verifier seam | low | accept | input.verifiers is a test-only injection mirroring providerSecrets; production effective set = VerifierRegistry.getAll() (ZERO registered in Phase 4, D-64) — an untrusted verifier could only reach the loop through code that already controls the turn |
| T-4-SC | Tampering | package installs | high | mitigate | Package legitimacy gate N/A — zero new dependencies (verified installed tree; no install commands in this plan). |
</threat_model>

<verification>
- `pnpm run lint` green after each task commit.
- `pnpm run verify:phase-4` green at plan end.
- Grep guard: zero new `ToolRegistry.register` calls in the test file (D-67).
- The false-completion test is the AGT-02 gate — it must assert 'partial', never 'completed', for the injected evidence-less side effect.
</verification>

<success_criteria>
1. Guard wired unconditionally in finish() — overrides buildOutcome status (D-65).
2. False-completion test passes: ok side-effecting result without evidence → 'partial'/'missing_evidence' (AGT-02 DONE-when).
3. With-evidence control passes: → 'completed' (guard vacuous when evidence present).
4. No fake tools registered (D-67); zero verifiers in production registry (D-64).
</success_criteria>

<output>
Create `.planning/phases/04-agent-reliability-and-evidence/04-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/ai/AgentOrchestrator.ts | + guard wiring in finish() (guardMissingEvidence override → partial/missing_evidence) | **[this plan]** (module created 04-01) |
| tests/core/ai/AgentOrchestrator.test.ts | + case group (j) false-completion guard tests (D-65/AGT-02) | **[this plan]** |