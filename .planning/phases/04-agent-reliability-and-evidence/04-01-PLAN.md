---
phase: 04-agent-reliability-and-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/harness.ts
  - src/core/ai/types.ts
  - src/core/ai/trajectory.ts
  - src/core/ai/OutcomeVerifier.ts
  - src/core/ai/AgentOrchestrator.ts
  - tests/core/ai/trajectory/TrajectoryTracker.test.ts
  - tests/core/ai/OutcomeVerifier.test.ts
  - tests/core/ai/AgentOrchestrator.test.ts
  - package.json
autonomous: true
requirements: [AGT-01, AGT-02, AGT-03]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every runAgentTurn resolves with an AgentTurnOutcome carrying operationId (== input.operationId), a closed status ('completed'|'partial'|'failed'|'aborted'), evidence: CompletionEvidence[], plannerCalls/toolCalls counters, and the additive streamedText/toolResults/reasonCode fields consumers keep reading (D-61)"
    - "Cap exhaustion yields status 'partial' with reasonCode 'cap_exhausted' — never a successful status (AGT-03)"
    - "Trajectory transitions are asserted against a closed transition table; illegal transitions throw at runtime (AGT-01)"
    - "ToolExecutionResult carries the optional evidence seam typed against the canonical CompletionEvidence (AGT-02, spec 4339)"
    - "The verify:phase-4 gate is re-pointed at tests/core/ai/trajectory + tests/core/ai/OutcomeVerifier.test.ts + tests/core/ai and goes GREEN at the end of this plan (D-68)"
  artifacts:
    - path: "src/types/harness.ts"
      provides: "AgentTrajectoryPhase, AgentTrajectoryState, CompletionEvidence, AgentTurnOutcome verbatim from Appendix C.1 (D-60)"
      exports: ["AgentTrajectoryPhase", "AgentTrajectoryState", "CompletionEvidence", "AgentTurnOutcome"]
    - path: "src/core/ai/trajectory.ts"
      provides: "TrajectoryTracker (transition-table validator, D-62/63) + TRAJECTORY_TRANSITIONS closed table"
      exports: ["TrajectoryTracker", "TRAJECTORY_TRANSITIONS"]
    - path: "src/core/ai/OutcomeVerifier.ts"
      provides: "O.2 Verifier interface + buildOutcome + VerifierRegistry (declare-now, empty) + guardMissingEvidence helper (D-64/D-65)"
      exports: ["Verifier", "buildOutcome", "VerifierRegistry", "guardMissingEvidence"]
    - path: "src/core/ai/AgentOrchestrator.ts"
      provides: "runAgentTurn returns AgentTurnOutcome (C.1 + additive streamedText/toolResults/trajectory), operationId re-added from input.operationId"
      exports: ["runAgentTurn", "AgentTurnInput", "AgentTurnOutcome", "AgentTier", "PersistTurnInput"]
  key_links:
    - from: "src/core/ai/types.ts"
      to: "src/types/harness.ts"
      via: "ToolExecutionResult.evidence?: import('@/types/harness').CompletionEvidence — the spec 4339 seam resolved against the canonical home"
      pattern: "import\\('@/types/harness'\\)"
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/trajectory.ts"
      via: "finish() attaches trajectory.snapshot(plannerCalls, toolCalls) onto the returned outcome (D-63)"
      pattern: "trajectory\\.snapshot|TrajectoryTracker"
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/OutcomeVerifier.ts"
      via: "finish() computes status/evidence via buildOutcome with caps {plannerCalls, toolCalls, capHit} (AGT-03)"
      pattern: "buildOutcome"
  prohibitions:
    - "No parallel copy of the C.1 types in src/core/ai — harness.ts is the single canonical home (D-60)"
    - "No invented statuses beyond the C.1 closed 4-value union (D-38 / §21.6)"
    - "No trajectory persistence — in-memory per turn only (D-63; AITransactionLog is Phase 11)"
---

<objective>
TRACER plan: create the canonical reliability types and wire ONE end-to-end AgentTurnOutcome through the loop — the thinnest path touching every layer the phase modifies (types → trajectory tracker → OutcomeVerifier → AgentOrchestrator loop → outcome), with the phase gate re-pointed and green.

Purpose: prove the C.1 types type-check against the Phase-3 loop (`ToolExecutionResult<unknown>` + the `import('@/types/harness')` seam) before any policy lands; establish the Wave-0 test files (§18-required `tests/core/ai/trajectory/**` + `tests/core/ai/OutcomeVerifier.test.ts`); and make `verify:phase-4` stop being RED (D-68 — the hard prerequisite).
Output: `src/types/harness.ts`, `src/core/ai/trajectory.ts`, `src/core/ai/OutcomeVerifier.ts`, `src/core/ai/types.ts` seam, evolved `AgentOrchestrator.ts`, both §18 test files, `package.json` re-point.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §28.2 (AGT-01…04), Appendix C.1 (4849-4876 — verbatim types), Appendix O.2 (6330-6361 — buildOutcome verbatim), §1.4 (tier caps), spec 4339 (evidence seam), spec 5561-5566 (Appendix I operationId)
@.planning/phases/04-agent-reliability-and-evidence/04-CONTEXT.md  # D-60…D-68
@.planning/phases/04-agent-reliability-and-evidence/04-RESEARCH.md  # Patterns 1-4, Code Examples, Pitfalls 1/5/6/8, A1/A5/A6/A7/A8
@.planning/phases/04-agent-reliability-and-evidence/04-PATTERNS.md  # harness.ts / trajectory.ts / OutcomeVerifier.ts / types.ts / AgentOrchestrator.ts sections
@src/core/ai/AgentOrchestrator.ts  # the loop being evolved (D-61) — current AgentTurnOutput at lines 90-101
@src/core/ai/types.ts  # ToolExecutionResult at 122-133 — evidence seam ABSENT (verified)
@src/core/ai/ExecutorService.ts  # zero-tool contract (D-46), object export for vi.spyOn
@src/core/ai/RendererService.ts  # render() contract + RenderTermination union
@tests/core/ai/AgentOrchestrator.test.ts  # cases (a)-(i); (b) asserts reasonCode 'planner_cap_reached' (kept green this plan)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Canonical types + trajectory tracker + OutcomeVerifier framework + AgentTurnOutcome return contract (one end-to-end outcome through the loop)</name>
  <files>src/types/harness.ts, src/core/ai/types.ts, src/core/ai/trajectory.ts, src/core/ai/OutcomeVerifier.ts, src/core/ai/AgentOrchestrator.ts</files>
  <read_first>@.planning/PRODUCT_SPEC_v0_1.md Appendix C.1 (4849-4876 — verbatim types) + Appendix O.2 (6330-6361 — buildOutcome verbatim) + §28.2 AGT-01..04 + spec 4339 (evidence seam) · 04-RESEARCH.md Pattern 1 (canonical home rule, spec 4833/4837), Pattern 2 (transition table), Pattern 3 (O.2 verbatim), Code Examples (types + buildOutcome + tracker) · 04-PATTERNS.md harness.ts/trajectory.ts/OutcomeVerifier.ts/types.ts/AgentOrchestrator.ts sections · @src/core/ai/AgentOrchestrator.ts (current finish() 231-280, configurationRequiredOutcome() 291-297, loop 299-354) · @src/core/ai/types.ts (ToolExecutionResult 122-133)</read_first>
  <action>Create `src/types/harness.ts` holding `AgentTrajectoryPhase` (10-value closed union: assembling-context | planning | waiting-for-permission | executing | verifying | replanning | rendering | completed | failed | aborted), `AgentTrajectoryState` (operationId, phase, plannerCalls, toolCalls, updatedAt), `CompletionEvidence` (toolName, operationId, postconditionId, ok, verifiedAt, detail?), and `AgentTurnOutcome` (operationId, status: 'completed'|'partial'|'failed'|'aborted', reasonCode, evidence, plannerCalls, toolCalls) — VERBATIM from Appendix C.1 (spec 4849-4876), with a doc comment citing the spec location (mirror src/types/storage.ts layout). No Zod (C.1 types are TS interfaces; O.2 does not mandate schemas). Add to `src/core/ai/types.ts` ToolExecutionResult the additive field `evidence?: import('@/types/harness').CompletionEvidence` (spec 4339) — do NOT rework data/error/code (breaks ExecutorService and its tests; Pitfall 5). Create `src/core/ai/trajectory.ts`: export `TRAJECTORY_TRANSITIONS: Record<AgentTrajectoryPhase, readonly AgentTrajectoryPhase[]>` (closed table — assembling-context→[planning]; planning→[executing, waiting-for-permission, rendering, replanning, failed, aborted]; waiting-for-permission→[executing, replanning, failed, aborted]; executing→[verifying, replanning, failed, aborted]; verifying→[rendering, failed, aborted]; replanning→[planning, failed, aborted]; rendering→[completed, failed, aborted]; completed/failed/aborted→[]) and `TrajectoryTracker` class: constructor(operationId) seeds phase 'assembling-context' with plannerCalls/toolCalls 0; `enter(next)` validates against the table and THROWS Error on illegal transitions (AGT-01); `snapshot(plannerCalls, toolCalls)` returns the C.1 AgentTrajectoryState; `get history()` returns the visited phases array (in-memory diagnostic for AGT-01 tests — NOT part of the outcome shape). Create `src/core/ai/OutcomeVerifier.ts` per Appendix O.2 VERBATIM (spec 6330-6361): `Verifier { postconditionId: string; verify(result: ToolExecutionResult<unknown>): Promise<{ ok: boolean; detail?: string }> }` and `buildOutcome(operationId, results, verifiers, caps)` returning AgentTurnOutcome with status rule `caps.capHit ? 'partial' : sideEffectFailed ? 'failed' : 'completed'` and reasonCodes 'cap_exhausted' | 'postcondition_failed' | 'ok' (descriptive literals, NOT §21.6 error codes — do not export them as error-code constants, D-38). Add `VerifierRegistry` mirroring ToolRegistry (Map-backed register/unregister/get/getAll — declare-now, ZERO registrations in Phase 4, D-64). Add exported `guardMissingEvidence(results: ToolExecutionResult<unknown>[], verifiers: Record<string, Verifier>): boolean` returning true when any result has ok === true AND a verifier is registered for its toolName AND result.evidence === undefined (D-65 guard condition, AGT-02/R-8). Evolve `src/core/ai/AgentOrchestrator.ts` (D-61 additive): replace `AgentTurnOutput` with `AgentTurnOutcome` = C.1 AgentTurnOutcome & { streamedText: string; toolResults: ToolExecutionResult<unknown>[]; trajectory: AgentTrajectoryState } (keep the C.1 fields + additive streamedText/toolResults/trajectory so useChatStreaming keeps reading reasonCode/streamedText). Re-add `operationId` on the return from `input.operationId` (Pitfall 8 — the Phase-3 shape dropped it). Add `verifiers?: Record<string, Verifier>` to AgentTurnInput (injection seam, mirrors providerSecrets pattern; effective set = { ...VerifierRegistry.getAll(), ...input.verifiers }). In finish(): instantiate TrajectoryTracker(input.operationId) at loop top; enter('planning') before each PlannerService.plan call; enter('executing') before ExecutorService.execute; in finish() enter('verifying') then call buildOutcome(input.operationId, toolResults, effectiveVerifiers, { plannerCalls, toolCalls, capHit: reasonCode is 'planner_cap_reached' or 'tool_cap_reached' }) then enter('rendering') before RendererService.render and enter('completed') before returning; terminal reasonCode = 'cap_exhausted' when capHit else the loop's existing reasonCode (keep 'ask_clarification' and planner answer reasonCodes intact — D-61 additive, consumers branch on them). configurationRequiredOutcome() now returns the full AgentTurnOutcome shape with status 'failed', operationId, evidence [], plannerCalls/toolCalls counters, trajectory snapshot (A3 mapping — no provider request started). ask_clarification → status 'completed' (A3 — the question IS the output). ABORT CONTRACT UNCHANGED this plan (three DOMException('aborted','AbortError') throw sites stay; Q1 conversion is plan 04-04). Keep debugLog codes; add trajectory-phase debug entries in the same style (SCREAMING_SNAKE + payload object). NP-STRICT ceiling 0: no @ts-expect-error NP-STRICT markers, no bare @ts-ignore (the C.1/O.2 shapes type-check against ToolExecutionResult<unknown> without casts).</action>
  <acceptance_criteria>`pnpm run lint` (tsc --noEmit) passes strict-clean; `npx vitest run tests/core/ai/AgentOrchestrator.test.ts` passes with ZERO test edits (existing cases (a)-(i) stay green under the additive D-61 contract — reasonCode literals and the abort throw preserved); `grep -c "CompletionEvidence" src/core/ai/types.ts` >= 1 (seam added); `src/types/harness.ts` exports all four C.1 types; `verify:phase-4` in package.json still points at tests/core/context (re-point is Task 2).</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/ai/AgentOrchestrator.test.ts</automated>
  </verify>
  <done>tsc strict-clean; all existing AgentOrchestrator tests pass unmodified (additive contract proven end-to-end through the evolved loop); C.1 types + seam + tracker + verifier modules exist and type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 §18 test files + verify:phase-4 re-point + outcome-contract assertions</name>
  <files>tests/core/ai/trajectory/TrajectoryTracker.test.ts, tests/core/ai/OutcomeVerifier.test.ts, tests/core/ai/AgentOrchestrator.test.ts, package.json</files>
  <read_first>@src/core/ai/trajectory.ts (created Task 1 — the module under test) · @src/core/ai/OutcomeVerifier.ts (created Task 1) · 04-PATTERNS.md tests/core/ai/trajectory/** + tests/core/ai/OutcomeVerifier.test.ts sections (case lists) · 04-RESEARCH.md Validation Architecture (AGT-01/02/03 test map) + Pitfall 4 (false-completion test is the AGT-02 proof) · @tests/core/ai/AgentOrchestrator.test.ts (existing cases a-i; baseInput 117-126; planSpy 175-177) · @tests/core/ai/ExecutorService.test.ts (input-builder pattern 18-26)</read_first>
  <action>Create `tests/core/ai/trajectory/TrajectoryTracker.test.ts` — pure unit tests, no chrome mocks, no providers (ExecutorService.test.ts style): legal transition chain assembling-context → planning → executing → verifying → rendering → completed passes; illegal transition (e.g. assembling-context → completed) throws with the illegal-transition message; snapshot(plannerCalls, toolCalls) reflects the counters and bumps updatedAt; operationId matches the constructor arg (D-63); the 'waiting-for-permission' row exists in TRAJECTORY_TRANSITIONS with no Phase-4 trigger (closed machine completeness, AGT-01); history() records the visited phase order. Create `tests/core/ai/OutcomeVerifier.test.ts` (AGT-02/03, Wave-0): input-builder toolResult(overrides) mirroring ExecutorService.test.ts (default {toolName:'fake_tool', ok:true, data:null, error:null, durationMs:10}); buildOutcome with caps.capHit true → status 'partial' + reasonCode 'cap_exhausted' (never 'completed' — AGT-03); registered verifier whose verify returns {ok:false} → status 'failed' + reasonCode 'postcondition_failed'; zero verifiers + ok results → status 'completed' + reasonCode 'ok' + evidence [] (D-64 vacuity); evidence shape: postconditionId = the verifier's id, operationId = the input operationId; guardMissingEvidence returns false for ok-result-with-evidence and true for ok-result-with-registered-verifier-but-no-evidence (the AGT-02 proof — Pitfall 4). Extend `tests/core/ai/AgentOrchestrator.test.ts` with outcome-contract assertions on the EXISTING cases (do NOT re-script case (b) — that is plan 04-03): case (a) happy path adds `expect(output.status).toBe('completed')`, `expect(output.operationId).toBe('op-orchestrator')` (Pitfall 8 correlation), `expect(output.evidence).toEqual([])`, `expect(output.plannerCalls).toBe(1)`, `expect(output.toolCalls).toBe(0)`, and `expect(output.trajectory.phase).toBe('completed')`; case (c) ask_clarification adds `expect(output.status).toBe('completed')` (A3); case (h) configuration_required adds `expect(output.status).toBe('failed')` (A3); case (b) adds ONLY `expect(output.status).toBe('partial')` alongside the existing reasonCode assertion (capHit → partial, AGT-03 — the same-tool loop still exhausts the cap in this plan because the AGT-04 repeated-identity policy does NOT exist yet). Re-point `package.json` verify:phase-4 (D-68) from `tsc --noEmit && vitest run tests/core/context` to `tsc --noEmit && vitest run tests/core/ai/trajectory tests/core/ai/OutcomeVerifier.test.ts tests/core/ai` (mirror verify:phase-3 at line 20 as the template; trajectory + OutcomeVerifier stay mandatory per D-68).</action>
  <acceptance_criteria>`pnpm run verify:phase-4` passes GREEN (tsc strict-clean + trajectory tests + OutcomeVerifier tests + full tests/core/ai suite); TrajectoryTracker.test.ts covers legal/illegal transitions + snapshot counters + per-turn operationId; OutcomeVerifier.test.ts covers capHit→partial, side-effect-fail→failed, zero-verifier vacuity, and the guard false-completion case; AgentOrchestrator.test.ts asserts status/operationId/counters/trajectory on cases (a)/(b)/(c)/(h); package.json verify:phase-4 contains `tests/core/ai/trajectory tests/core/ai/OutcomeVerifier.test.ts tests/core/ai` and no longer references tests/core/context.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-4</automated>
  </verify>
  <done>verify:phase-4 GREEN for the first time (D-68 closed); §18-required test files exist and pass; outcome contract asserted on the existing orchestrator cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentOrchestrator ← runAgentTurn input | operationId, tier caps, abortSignal, providerSecrets, and the NEW verifiers injection seam cross into the loop here — untrusted test fixtures may inject verifier maps |
| AgentOrchestrator → AgentTurnOutcome | The turn's honest outcome (status/evidence/counters) exits the loop — the "never silently claims success" boundary (golden rule 8, spec 214) |
| OutcomeVerifier ← ToolExecutionResult[] | Tool results (incl. the evidence seam) feed buildOutcome — evidence presence/absence decides status |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Spoofing / Tampering | finish() completion path | high | mitigate | False completion (R-8): outcome status is computed by buildOutcome (capHit→partial) and the guard helper ships in this plan; the guard WIRING into finish() is plan 04-02 — status can never be 'completed' while capHit (O.2 verbatim rule, AGT-03) |
| T-4-02 | Tampering | trajectory tracker | medium | mitigate | Illegal transitions throw at runtime against the closed TRAJECTORY_TRANSITIONS table (AGT-01); asserted by TrajectoryTracker.test.ts legal/illegal cases — a failure-masking state can never be recorded |
| T-4-03 | Spoofing | AgentTurnOutcome.status | high | mitigate | Closed 4-value union from C.1 — no invented statuses (D-38/§21.6); tsc exhaustiveness + outcome-contract assertions on existing orchestrator cases (this plan's Task 2) |
| T-4-04 | Information Disclosure | buildOutcome evidence | low | accept | Evidence detail is verifier-controlled (zero verifiers registered in Phase 4, D-64); raw trace persistence is Phase 11 (AITransactionLog) where TraceRedactor applies — no data leaves the machine this plan |
| T-4-SC | Tampering | package installs | high | mitigate | Package legitimacy gate N/A — zero new dependencies this phase (RESEARCH Standard Stack: none; verified installed tree). No install commands exist in this plan. |
</threat_model>

<verification>
- `pnpm run lint` green after every task commit (strict-clean, NP_STRICT_CEILING 0).
- `pnpm run verify:phase-4` GREEN at plan end (D-68 re-point + both §18 test files + full tests/core/ai suite).
- Grep guard: no `@ts-expect-error NP-STRICT` markers in the five new/modified source files.
- Tracer discipline: Task 1 touches one file per layer (types/loop); Task 2 proves the slice with the §18 tests.
</verification>

<success_criteria>
1. Every runAgentTurn resolves with an AgentTurnOutcome (operationId correlated, closed status, evidence, counters) — D-61 additive, consumers untouched.
2. Cap exhaustion → status 'partial' + reasonCode 'cap_exhausted', never 'completed' (AGT-03).
3. Trajectory transitions asserted against the closed table; illegal transitions throw (AGT-01).
4. `ToolExecutionResult.evidence` seam typed against canonical `@/types/harness` (AGT-02, spec 4339).
5. `verify:phase-4` GREEN — the gate stops being RED (D-68).
</success_criteria>

<output>
Create `.planning/phases/04-agent-reliability-and-evidence/04-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-4 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/types/harness.ts | AgentTrajectoryPhase, AgentTrajectoryState, CompletionEvidence, AgentTurnOutcome (C.1 verbatim, D-60) | **[this plan]** |
| src/core/ai/trajectory.ts | TrajectoryTracker, TRAJECTORY_TRANSITIONS (D-62/63) | **[this plan]** |
| src/core/ai/OutcomeVerifier.ts | Verifier, buildOutcome, VerifierRegistry, guardMissingEvidence (O.2 verbatim, D-64/D-65) | **[this plan]** |
| src/core/ai/types.ts | + ToolExecutionResult.evidence seam (AGT-02) | **[this plan]** |
| src/core/ai/AgentOrchestrator.ts | runAgentTurn → AgentTurnOutcome (D-61), trajectory hooks, buildOutcome integration, configuration_required → failed | **[this plan]** + 04-02 (guard) + 04-03 (replan) + 04-04 (abort) |
| tests/core/ai/trajectory/TrajectoryTracker.test.ts | AGT-01 closed-machine tests (new dir, §18) | **[this plan]** |
| tests/core/ai/OutcomeVerifier.test.ts | AGT-02/03 buildOutcome + guard unit tests (§18) | **[this plan]** |
| tests/core/ai/AgentOrchestrator.test.ts | + outcome-contract assertions (status/operationId/counters/trajectory) | **[this plan]** + 04-02 + 04-03 + 04-04 |
| package.json | verify:phase-4 re-pointed (D-68) | **[this plan]** |
| src/components/chat/useChatStreaming.ts | + aborted-status branch (Q1) | 04-04 |