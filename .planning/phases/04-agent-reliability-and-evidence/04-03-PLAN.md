---
phase: 04-agent-reliability-and-evidence
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/core/ai/AgentOrchestrator.ts
  - tests/core/ai/AgentOrchestrator.test.ts
autonomous: true
requirements: [AGT-04, AGT-03]
user_setup: []

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A failed tool execution triggers AT MOST ONE replan per tool within the tier's planner cap (§1.4) — the next planner call after a failure IS the single replan (AGT-04)"
    - "A repeated identical failure (same toolName + same stable error identity) is terminal → outcome status 'failed' (AGT-04) — never a silent retry loop"
    - "A cap breach stays terminal → status 'partial' with reasonCode 'cap_exhausted' (AGT-03 — never 'completed')"
    - "Failure identity keys on toolName + stable signal (code when present, else normalized error), NOT the raw error message alone (Pitfall 7)"
    - "Replan exercises the framework via injected ExecutorService.execute results — zero fake tool registrations (D-67)"
  artifacts:
    - path: "src/core/ai/AgentOrchestrator.ts"
      provides: "AGT-04 replan/terminal policy inside the bounded loop — failureIdentities map + replannedTools set, one replan per failed tool, repeated-identity terminal 'failed', trajectory 'replanning' entry"
      exports: ["runAgentTurn", "AgentTurnInput", "AgentTurnOutcome"]
  key_links:
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/trajectory.ts"
      via: "on non-terminal tool failure the tracker enters 'replanning' (validated against TRAJECTORY_TRANSITIONS) before the next planner call — the loop's legal replanning edge"
      pattern: "enter\\('replanning'\\)"
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/types.ts"
      via: "ToolExecutionResult.code is the stable identity signal (§21.6 code first, e.g. TOOL_REJECTED); error string only as fallback"
      pattern: "\\.code"
  prohibitions:
    - "No fake tool registration — inject failing results via vi.spyOn(ExecutorService, 'execute') (D-67)"
    - "No retry multiplication — AGT-04 replan is ONE layer of the three-layer bound (§1.6.1); total calls stay under §1.4 caps"
    - "No new status values — terminal outcomes use the C.1 closed set ('failed'/'partial'), never invented statuses (D-38)"
---

<objective>
Expansion: implement the AGT-04 deterministic replan/terminal policy inside the bounded loop — at most one replan per failed tool, repeated-identical-failure and cap-breach as terminal outcomes, never a silent success — exercised exclusively through injected executor results (D-67, zero fake tools).

Purpose: replace the Phase-3 retry-until-cap behavior with the deterministic policy the spec mandates (AGT-04, §1.6.1 retry layering bound) and re-script the cap test (b) that the new policy semantically breaks (Pitfall 2 — repeated same-tool failures now terminate 'failed' before the planner cap is reached).
Output: AGT-04 policy in `AgentOrchestrator.ts` + re-scripted case (b) + new replan/terminal test cases in `AgentOrchestrator.test.ts`.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §28.2 AGT-04 (spec 3945 verbatim) + §1.4 (tier caps table) + §1.6.1 (retry layering — three layers, never nest) + §1.5 (ProviderRouter retry layer)
@.planning/phases/04-agent-reliability-and-evidence/04-CONTEXT.md  # D-66 (AGT-04 in the loop), D-67 (injected results)
@.planning/phases/04-agent-reliability-and-evidence/04-RESEARCH.md  # Pattern 5 (replan policy sketch), Pitfall 2 (test (b) breaks), Pitfall 7 (stable identity), A8 (cap_exhausted reasonCode), Common Pitfalls table
@.planning/phases/04-agent-reliability-and-evidence/04-PATTERNS.md  # AgentOrchestrator.ts section (D-66 sketch — failureIdentities/replannedTools), tests section (case (b) 169-193, executor mock seam)
@src/core/ai/AgentOrchestrator.ts  # the loop (299-354) from 04-01 + guard from 04-02 — policy slots after toolResults.push (line ~348)
@src/core/ai/ExecutorService.ts  # object export :71 — vi.spyOn injection seam
@tests/core/ai/AgentOrchestrator.test.ts  # case (b) 169-193 (must re-script), baseInput 117-126, planSpy 175-177, case group (j) from 04-02
</context>

<tasks>

<task type="auto">
  <name>Task 1: AGT-04 deterministic replan/terminal policy in the loop</name>
  <files>src/core/ai/AgentOrchestrator.ts</files>
  <read_first>@src/core/ai/AgentOrchestrator.ts (the loop 299-354 — where toolResults.push(result) lands, the finish() calls, the trajectory hooks from 04-01) · 04-RESEARCH.md Pattern 5 (policy sketch: failure identity, replannedTools set, ≤1 replan per failed tool) + Pitfall 7 (identity = toolName + code, never raw message) + §1.6.1 (three-layer bound) · 04-PATTERNS.md AgentOrchestrator.ts section (D-66 sketch) · @src/core/ai/trajectory.ts (TRAJECTORY_TRANSITIONS — 'executing' and 'replanning' rows; 'replanning' → 'planning' is legal)</read_first>
  <action>Implement the AGT-04 policy in `src/core/ai/AgentOrchestrator.ts` inside the existing while loop, immediately after `toolResults.push(result)` (the point where the executor result is known): declare two loop-scoped collections before the loop — `const failureIdentities = new Map<string, string>()` (toolName → stable identity) and `const replannedTools = new Set<string>()` (toolName → replan budget consumed). Compute the stable failure identity as `result.code ?? (result.error ?? 'ERROR')` — the §21.6 code when present (e.g. TOOL_REJECTED), the error string ONLY as fallback (Pitfall 7; never key on the raw message alone when a code exists). When `result.ok === false`: (1) if `failureIdentities.get(result.toolName) === identity` → REPEATED IDENTICAL FAILURE → terminal: return `finish('repeated_failure')` with outcome status 'failed' (AGT-04 — the loop's existing finish() maps it; status must be 'failed', never 'completed'); (2) else if `replannedTools.has(result.toolName)` → REPLAN BUDGET CONSUMED (≤1 per tool) → terminal: return `finish('replan_exhausted')` with outcome status 'failed'; (3) else — FIRST failure for this tool: record `failureIdentities.set(result.toolName, identity)`, add `replannedTools.add(result.toolName)`, enter the trajectory 'replanning' phase (`trajectory.enter('replanning')` — validated against the closed table, AGT-01), and `continue` the loop — the NEXT planner call IS the single replan. Do NOT consume a new replan budget on the replan's own planner-call count beyond the §1.4 plannerCap (the existing loop-top plannerCap check stays authoritative). Finish mapping: in finish(), when the terminal reasonCode is 'repeated_failure' or 'replan_exhausted', the outcome status is 'failed' (buildOutcome already returns 'failed' when a registered verifier fails — here the STATUS is forced to 'failed' for the policy terminals; capHit stays the ONLY path to 'partial'). Keep the cap path ('planner_cap_reached'/'tool_cap_reached') → status 'partial' + reasonCode 'cap_exhausted' (AGT-03 — unchanged from 04-01). Add debugLog entries for replan/terminal decisions (code style: SCREAMING_SNAKE + payload {operationId, toolName, identity, plannerCalls, toolCalls}) using debugLog, never console.log. Retry layering stays bounded: this AGT-04 replan is one of exactly three layers (ProviderRouter §1.5, AGT-04, one per-stage retry) — never nest (§1.6.1). Strict-clean only. Do NOT touch the abort throw sites (04-04) or the guard (04-02, already wired).</action>
  <acceptance_criteria>`pnpm run lint` passes; `grep -n "replannedTools\|failureIdentities" src/core/ai/AgentOrchestrator.ts` shows the collections declared before the loop and used after toolResults.push; `grep -n "replanning" src/core/ai/AgentOrchestrator.ts` shows the trajectory enter on first failure; existing AgentOrchestrator tests EXCEPT case (b) stay green (case (b) is re-scripted in Task 2 of this plan — its same-tool mock now terminates 'failed' before the cap, the documented Pitfall 2 semantic change); zero new `@ts-expect-error NP-STRICT` markers.</acceptance_criteria>
  <verify>
    <automated>pnpm run lint</automated>
  </verify>
  <done>Policy encoded: one replan per failed tool, repeated-identity and replan-budget terminals → 'failed', cap → 'partial'/'cap_exhausted'; trajectory enters 'replanning' on first failure; no new NP-STRICT markers.</done>
</task>

<task type="auto">
  <name>Task 2: Re-script case (b) with distinct tool names + new replan/terminal test cases</name>
  <files>tests/core/ai/AgentOrchestrator.test.ts</files>
  <read_first>@tests/core/ai/AgentOrchestrator.test.ts (case (b) 169-193 — re-script target; planSpy 175-177; baseInput 117-126; ExecutorService injection pattern from 04-02 case (j)) · @src/core/ai/AgentOrchestrator.ts (the policy from Task 1 — exact terminal semantics) · @src/core/ai/ExecutorService.ts (execute contract for the injection mocks) · 04-RESEARCH.md Pitfall 2 (re-script with DISTINCT tool names per iteration) + Validation Architecture AGT-04 test map</read_first>
  <action>Re-script case (b) (`planner_cap_reached`) in `tests/core/ai/AgentOrchestrator.test.ts` per Pitfall 2: the current mock returns run_tool 'any_tool' repeatedly — under AGT-04 the second execution is a repeated identical failure (TOOL_REJECTED + same toolName) → terminal 'failed' BEFORE the planner cap. Change the planner mock to emit DISTINCT tool names per iteration (e.g. mockResolvedValueOnce run_tool 'search_kb', then run_tool 'write_note', then run_tool 'create_incident' — each fresh tool has its own replan budget, so the loop legitimately exhausts plannerCap). Assert: status 'partial', reasonCode 'cap_exhausted', toolResults length equals the plannerCap value, all tool results carry code 'TOOL_REJECTED', streamedText is the rendered answer (AGT-03 — cap exhaustion is partial, never completed). Add three new case groups: (k) REPEATED IDENTITY → 'failed' — planner always returns run_tool 'same_tool'; `vi.spyOn(ExecutorService, 'execute').mockResolvedValue({ toolName: 'same_tool', ok: false, data: null, error: 'boom', code: 'TOOL_REJECTED', durationMs: 5 })` (same identity both times) → assert status 'failed', reasonCode 'repeated_failure', plannerCalls 2, toolCalls 2, trajectory phase 'failed'; (l) ONE REPLAN THEN COMPLETED — planner mockResolvedValueOnce run_tool 'fail_tool' then answer 'direct_answer'; executor mock rejects 'fail_tool' ONCE then the loop's second planner call answers → assert status 'completed', reasonCode 'direct_answer', plannerCalls 2, toolCalls 1, and trajectory history includes 'replanning' between 'executing' and the terminal (assert via the outcome's trajectory snapshot OR the tracker's history when exposed on the outcome — the 04-01 design puts the final snapshot on the outcome; assert `output.trajectory.phase === 'completed'` plus plannerCalls/toolCalls counters proving the replan happened); (m) REPLAN BUDGET CONSUMED → 'failed' — planner returns run_tool 'same_tool' then run_tool 'same_tool' then answer; executor mock returns DIFFERENT identities for the same tool (first {code:'TOOL_REJECTED'}, second {code:'RATE_LIMITED'} — different stable signal) → the second failure is NOT repeated-identical (different identity) but the replan budget IS consumed → assert status 'failed', reasonCode 'replan_exhausted', toolCalls 2. Do not add any ToolRegistry.register calls (D-67). Run `npx vitest run tests/core/ai/AgentOrchestrator.test.ts`, then `pnpm run verify:phase-4`.</action>
  <acceptance_criteria>`npx vitest run tests/core/ai/AgentOrchestrator.test.ts` green with: re-scripted (b) → 'partial'/'cap_exhausted' with distinct tool names; case (k) repeated-identity → 'failed'/'repeated_failure'; case (l) one-replan-then-completed → 'completed' with counters proving the replan; case (m) replan-budget-consumed → 'failed'/'replan_exhausted'; `pnpm run verify:phase-4` green; zero ToolRegistry.register calls added (grep-assertable).</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-4</automated>
  </verify>
  <done>Case (b) re-scripted with distinct tool names (Pitfall 2 closed); repeated-identity, one-replan-then-completed, and replan-budget-consumed cases green; full phase-4 gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExecutorService.execute → ToolExecutionResult | Failing tool results enter the AGT-04 policy — the identity signal (code vs error) decides replan vs terminal; untrusted result content must not be able to reset the replan budget |
| AgentOrchestrator loop → finish() | Policy terminals ('repeated_failure', 'replan_exhausted', cap) flow into finish() — status must stay within the closed C.1 set |
| trajectory tracker | 'replanning' entry is validated against the closed table — a failure cannot mask itself by skipping the legal transition |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-07 | DoS | unbounded retry amplification (R-2) | high | mitigate | AGT-04 deterministic policy: ≤1 replan per failed tool (replannedTools set), repeated-identity and budget-consumed terminals → 'failed' — the N×N×N fan-out (§1.6.1) is impossible; total planner/tool calls stay under §1.4 caps (loop-top checks unchanged); case (b) re-scripted proves the cap path |
| T-4-08 | Tampering | failure-identity keying | medium | mitigate | Identity = toolName + stable signal (code first, §21.6; error string only as fallback — Pitfall 7) — a provider rewording the error message cannot reset the identity to evade the repeated-failure terminal |
| T-4-09 | Spoofing | silent success on terminal | high | mitigate | Policy terminals map to status 'failed' (never 'completed'); cap → 'partial' (AGT-03); asserted by cases (b)/(k)/(l)/(m) — no code path exits the loop with an unreported failure |
| T-4-SC | Tampering | package installs | high | mitigate | Package legitimacy gate N/A — zero new dependencies (no install commands in this plan). |
</threat_model>

<verification>
- `pnpm run lint` green after each task commit.
- `pnpm run verify:phase-4` green at plan end (AgentOrchestrator suite + trajectory + OutcomeVerifier).
- Grep guard: zero new `ToolRegistry.register` calls (D-67).
- Case (b) must assert 'partial'/'cap_exhausted' (AGT-03) — the re-scripted distinct-tool loop.
</verification>

<success_criteria>
1. One replan per failed tool within the planner cap (AGT-04).
2. Repeated identical failure → terminal 'failed'; replan budget consumed → terminal 'failed' (AGT-04).
3. Cap breach → 'partial'/'cap_exhausted', never 'completed' (AGT-03).
4. Trajectory 'replanning' entered on first failure, validated against the closed table (AGT-01).
5. Framework-exercised only via injected executor results — zero fake tools (D-67).
</success_criteria>

<output>
Create `.planning/phases/04-agent-reliability-and-evidence/04-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/ai/AgentOrchestrator.ts | + AGT-04 replan/terminal policy (failureIdentities, replannedTools, 'replanning' trajectory entry, terminal status mapping) | **[this plan]** (module evolved 04-01/04-02) |
| tests/core/ai/AgentOrchestrator.test.ts | + re-scripted (b) distinct-tool cap test + case groups (k)/(l)/(m) replan/terminal | **[this plan]** |