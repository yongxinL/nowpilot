---
phase: 05-persistent-memory-architecture
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/core/storage/IndexedDBManager.ts
  - src/core/storage/stores/MemoryDB.ts
autonomous: true
requirements:
  - MEM-01
  - MEM-02

must_haves:
  truths:
    - DB_VERSION is 2 (bumped from 1)
    - NowPilotDB interface adds `status`, `tags`, `useCount`, `lastUsedAt` optional fields to memory_userFacts.value
    - NowPilotDB interface adds `state`, `archivedAt` optional fields to memory_summaries.value
    - upgrade callback has `if (oldVersion < 2)` branch (no-op — schemaless stores: new fields added via put() at runtime with defaults)
    - MemoryDB.putUserFact param type includes `status?: 'active' | 'superseded'`, `tags?: string[]`, `useCount?: number`, `lastUsedAt?: number`
    - MemoryDB.putSummary param type includes `state?: 'active' | 'archived'`, `archivedAt?: number`
  artifacts:
    - src/core/storage/IndexedDBManager.ts (modified: DB_VERSION=2, NowPilotDB extended, v2 upgrade branch)
    - src/core/storage/stores/MemoryDB.ts (modified: putUserFact + putSummary parameter types extended)
  key_links:
    - MemoryDB.putUserFact new fields consumed by UserMemoryStore (P04)
    - MemoryDB.putSummary new fields consumed by ConversationMemoryStore (P04)
    - NowPilotDB interface changes consumed by idb type-safe openDB call in getDB()
---

<objective>
Bump IndexedDB schema version from 1 to 2, extend the NowPilotDB TypeScript interface with versioned-fact-model fields (status, tags, useCount, lastUsedAt) and conversation-state fields (state, archivedAt), and extend MemoryDB method parameter types to match.

Purpose: Enable the versioned fact model (active/superseded) and conversation lifecycles (active/archived) that MemoryEngine and stores need.
Output: IndexedDBManager.ts with DB_VERSION=2 and extended NowPilotDB; MemoryDB.ts with extended method signatures.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-persistent-memory-architecture/05-CONTEXT.md
@.planning/phases/05-persistent-memory-architecture/05-RESEARCH.md — §Pattern 3: IndexedDB Schema Migration (lines 229-256)
@.planning/phases/05-persistent-memory-architecture/05-PATTERNS.md — §10 MemoryDB + §11 IndexedDBManager
@src/core/storage/IndexedDBManager.ts
@src/core/storage/stores/MemoryDB.ts
@src/core/memory/memoryTypes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump DB_VERSION to 2 and extend NowPilotDB interface with v2 fields</name>
  <files>src/core/storage/IndexedDBManager.ts</files>
  <read_first>
    - src/core/storage/IndexedDBManager.ts (full file, 203 lines) — current DB_VERSION=1, NowPilotDB interface, upgrade callback
    - .planning/phases/05-persistent-memory-architecture/05-RESEARCH.md (lines 229-256) — Pattern 3: schemaless stores means upgrade callback can be no-op
    - .planning/phases/05-persistent-memory-architecture/05-PATTERNS.md (lines 447-504) — exact field additions for NowPilotDB
    - src/core/memory/memoryTypes.ts — UserMemoryFact interface has status, tags, useCount, lastUsedAt; ConversationSummary has state, archivedAt
  </read_first>
  <action>
    Modify src/core/storage/IndexedDBManager.ts:

    **1. Bump DB_VERSION** (line 156):
    Change `export const DB_VERSION = 1` to `export const DB_VERSION = 2`.

    **2. Extend NowPilotDB interface — memory_userFacts** (lines 58-69):
    Add 4 new optional fields to `memory_userFacts.value`:
    - `status?: 'active' | 'superseded'` — per D-15 versioned fact model
    - `tags?: string[]` — per D-11 MiniSearch indexing
    - `useCount?: number` — per D-12 useCountScore factor
    - `lastUsedAt?: number` — per D-24 low-confidence eviction

    **3. Extend NowPilotDB interface — memory_summaries** (lines 70-79):
    Add 2 new optional fields to `memory_summaries.value`:
    - `state?: 'active' | 'archived'` — per D-22 conversation lifecycle
    - `archivedAt?: number` — per D-22 archiving timestamp

    **4. Add v2 upgrade branch** (after line 187, before the `}` closing the `upgrade` function):
    Add `if (oldVersion < 2) { /* v2: schemaless stores — new fields (status, tags, useCount, lastUsedAt, state, archivedAt) are added via put() at runtime with defaults. No schema alteration needed. */ }`

    All existing stores and upgrade logic from v1 remain untouched. The v2 branch is intentionally a no-op because IndexedDB object stores are schemaless at the value level — new optional fields on TypeScript interfaces don't require store re-creation.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(IndexedDBManager|NowPilotDB)" | grep -i error | grep -c error | grep -q 0; grep -c 'DB_VERSION = 2' src/core/storage/IndexedDBManager.ts | grep -q 1; grep -c "oldVersion < 2" src/core/storage/IndexedDBManager.ts | grep -q 1; grep -c "status.*active.*superseded" src/core/storage/IndexedDBManager.ts | grep -q 1; grep -c "state.*active.*archived" src/core/storage/IndexedDBManager.ts | grep -q 1</automated>
  </verify>
  <done>
    DB_VERSION is 2, NowPilotDB interface has all 6 new optional fields on memory_userFacts and memory_summaries, upgrade callback has oldVersion < 2 branch. TypeScript noEmit passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend MemoryDB putUserFact and putSummary parameter types with v2 fields</name>
  <files>src/core/storage/stores/MemoryDB.ts</files>
  <read_first>
    - src/core/storage/stores/MemoryDB.ts (full file, 112 lines) — current method signatures
    - src/core/storage/IndexedDBManager.ts — updated NowPilotDB interface (after Task 1)
    - src/core/memory/memoryTypes.ts — UserMemoryFact (status, tags, useCount, lastUsedAt), ConversationSummary (state, archivedAt)
    - .planning/phases/05-persistent-memory-architecture/05-PATTERNS.md (lines 409-444) — exact field additions
  </read_first>
  <action>
    Modify src/core/storage/stores/MemoryDB.ts:

    **1. Extend putUserFact parameter type** (lines 40-48):
    Add 4 new optional fields to the inline type annotation on the `fact` parameter:
    - `status?: 'active' | 'superseded'`
    - `tags?: string[]`
    - `useCount?: number`
    - `lastUsedAt?: number`

    **2. Extend getAllUserFacts return type** (lines 57-66):
    Add the same 4 optional fields to the inline return type. This keeps the TypeScript return type consistent with what idb now returns from the upgraded schema.

    **3. Extend putSummary parameter type** (lines 77-83):
    Add 2 new optional fields:
    - `state?: 'active' | 'archived'`
    - `archivedAt?: number`

    **4. Extend getSummary return type** (lines 92-100):
    Add the same 2 optional fields.

    Keep all method bodies unchanged (try/catch, getDB(), debugLog patterns remain identical). The idb library handles the optional fields transparently — records without these fields get undefined at read time.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "MemoryDB" | grep -i error | grep -c error | grep -q 0; grep -c "status.*active.*superseded" src/core/storage/stores/MemoryDB.ts | grep -q 1; grep -c "state.*active.*archived" src/core/storage/stores/MemoryDB.ts | grep -q 1; grep -c "tags.*string" src/core/storage/stores/MemoryDB.ts | grep -q 1; grep -c "useCount.*number" src/core/storage/stores/MemoryDB.ts | grep -q 1; npx vitest run tests/core/storage/domainStores.test.ts 2>&1 | grep -c "failed" | grep -q 0</automated>
  </verify>
  <done>
    MemoryDB method signatures match NowPilotDB v2 interface. Existing domain store tests pass unchanged. TypeScript noEmit passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Schema migration does not introduce new trust boundaries — it extends existing IndexedDB schemas |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | IndexedDB schema migration (v1→v2) | low | accept | idb upgrade callback is idempotent; existing v1 records survive migration intact. New fields are optional — no data loss risk. |
</threat_model>

<verification>
Run `npx tsc --noEmit` — both modified files must compile. Run `npx vitest run tests/core/storage/domainStores.test.ts` — all existing MemoryDB tests must pass unchanged.
</verification>

<success_criteria>
DB_VERSION=2, NowPilotDB extended, MemoryDB extended. All existing tests pass. TypeScript compiles.
</success_criteria>

<output>
Create `.planning/phases/05-persistent-memory-architecture/05-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Consumed By |
|--------|------|------|-------------|
| `DB_VERSION = 2` | const | src/core/storage/IndexedDBManager.ts | getDB() callers |
| `NowPilotDB` (extended) | interface | src/core/storage/IndexedDBManager.ts | idb openDB type parameter |
| `memory_userFacts.value.status` | type field | src/core/storage/IndexedDBManager.ts | P04 UserMemoryStore |
| `memory_userFacts.value.tags` | type field | src/core/storage/IndexedDBManager.ts | P04 UserMemoryStore |
| `memory_userFacts.value.useCount` | type field | src/core/storage/IndexedDBManager.ts | P04 UserMemoryStore |
| `memory_userFacts.value.lastUsedAt` | type field | src/core/storage/IndexedDBManager.ts | P04 UserMemoryStore |
| `memory_summaries.value.state` | type field | src/core/storage/IndexedDBManager.ts | P04 ConversationMemoryStore |
| `memory_summaries.value.archivedAt` | type field | src/core/storage/IndexedDBManager.ts | P04 ConversationMemoryStore |
| `MemoryDB.putUserFact` (extended) | method signature | src/core/storage/stores/MemoryDB.ts | P04 UserMemoryStore |
| `MemoryDB.putSummary` (extended) | method signature | src/core/storage/stores/MemoryDB.ts | P04 ConversationMemoryStore |
