---
phase: 09-llm-wiki-filesystem-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/core/notes/NoteQA.ts
  - src/core/notes/NoteChatConverter.ts
  - tests/core/notes/NoteQA.test.ts
  - tests/core/notes/NoteChatConverter.test.ts
autonomous: true
requirements:
  - LLM-WIKI-05
  - LLM-WIKI-06
  - LLM-WIKI-07
  - NMEM-01
  - NMEM-03

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "NoteQA.ask() retrieves MiniSearch top-5 + MemoryEngine facts, returns balanced-tier synthesis with per-statement citations"
    - "NoteQA tiny mode falls back to plain MiniSearch results when balanced tier unavailable (no LLM synthesis)"
    - "NoteChatConverter.draftFromChat() uses conversation messages + MemoryEngine.assemble() for richer drafts"
    - "AI-enhanced search rerank (LLM-WIKI-05) activates when <3 MiniSearch results or AI search enabled"
    - "NoteQAResult includes clickable citations with noteId, title, snippet"
    - "NoteDraft includes title, content, tags, wikilinks, categoryPath, summary"
  artifacts:
    - "src/core/notes/NoteQA.ts"
    - "src/core/notes/NoteChatConverter.ts"
    - "tests/core/notes/NoteQA.test.ts"
    - "tests/core/notes/NoteChatConverter.test.ts"
  key_links:
    - "NoteQA → MiniSearchIndex.query (top-5 retrieval)"
    - "NoteQA → MemoryEngine.retrieveMemoryHints (NMEM-01 memory-aware RAG)"
    - "NoteQA → StructuredOutput.requestJson (balanced-tier synthesis)"
    - "NoteChatConverter → MemoryEngine.assemble (NMEM-03 memory context)"
    - "NoteChatConverter → LinkParser.parseLinks (wikilink extraction)"
---

<objective>
Build the two AI-consumption services: NoteQA (RAG "Ask notes" with citations) and NoteChatConverter (chat-to-note draft with memory context). Both reuse the schemas from Plan 01 and the Phase-3 AI runtime at balanced/fast tier respectively.

Purpose: Complete the LLM-consuming surface — users can query their notes with cited answers and capture chat conversations as structured note drafts.
Output: NoteQA and NoteChatConverter services with full test coverage.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-llm-wiki-filesystem-sync/09-CONTEXT.md
@.planning/phases/09-llm-wiki-filesystem-sync/09-RESEARCH.md
@.planning/phases/09-llm-wiki-filesystem-sync/09-PATTERNS.md
@.planning/phases/09-llm-wiki-filesystem-sync/09-UI-SPEC.md
@src/core/notes/schemas.ts
@src/core/search/MiniSearchIndex.ts
@src/core/memory/MemoryEngine.ts
@src/core/ai/StructuredOutput.ts
@src/core/ai/TierResolver.ts
@src/core/notes/LinkParser.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NoteQA — RAG "Ask notes" with citations</name>
  <files>
    src/core/notes/NoteQA.ts,
    tests/core/notes/NoteQA.test.ts
  </files>
  <read_first>
    src/core/search/MiniSearchIndex.ts,
    src/core/memory/MemoryEngine.ts,
    src/core/ai/StructuredOutput.ts,
    src/core/ai/TierResolver.ts,
    src/core/notes/schemas.ts
  </read_first>
  <behavior>
    - Test 1: LLM-WIKI-06 — balanced-tier synthesis returns cited answer (mock provider, assert NoteQAResult shape with citations array)
    - Test 2: LLM-WIKI-06 — tiny mode falls back to plain MiniSearch when resolveTier('balanced') returns null (fallback: true, no LLM call)
    - Test 3: NMEM-01 — memory hints from MemoryEngine.retrieveMemoryHints included in RAG context alongside note snippets
    - Test 4: MiniSearch top-5 retrieval feeds synthesis (seed 10 notes, assert only top-5 used)
    - Test 5: LLM-WIKI-05 — AI-enhanced rerank activates when <3 MiniSearch results
  </behavior>
  <action>
**NoteQA.ts (LLM-WIKI-05/06, NMEM-01, D-117):**
- Create `src/core/notes/NoteQA.ts` exporting `NoteQA` object with `ask()` method.
- `ask(query: string, db: IDBPDatabase<NotesDBV1>, abortSignal?: AbortSignal): Promise<NoteQAResult>`:
  1. **Retrieval:** Call `MiniSearchIndex.query(db, query)` for top-5 note hits (D-109 persistent notes index).
  2. **Memory facts (NMEM-01):** Call `MemoryEngine.retrieveMemoryHints(query, { tier: 'balanced' })` for relevant user facts/preferences.
  3. **Context assembly:** Combine note hits + memory hints into context array with source attribution (`{ source: noteId, title, snippet }` for notes; `{ source: 'memory:{id}', title: type, snippet: content }` for memory).
  4. **Tier check:** Resolve `resolveTier('balanced')`. If null → **tiny mode fallback** (§524): return `{ answer: null, citations: [], fallback: true, mode: 'keyword-only' }` (no LLM call).
  5. **Synthesis:** Call `requestJson(NoteQAResultSchema, buildSynthesisPrompt(query, context), { operationId, providerId, model, timeoutMs: 25_000, ... })` at balanced tier.
  6. **AI rerank (LLM-WIKI-05):** If MiniSearch returns <3 results OR `np_notes_llm_features.aiSearch` is true, rerank top-10 by semantic relevance via fast-tier call. Set `reranked: true` on result.
  7. Return `NoteQAResult` with `mode: 'ai-enhanced' | 'keyword-only'`.

- **UI-SPEC data contract:** `NoteQAResult` matches the `NoteQAResult` interface in UI-SPEC §"NoteQA → Ask-Notes Surface" — citations have `noteId`, `title`, `snippet` for clickable Tag rendering.

- **Module state:** Use object-form namespace export (`export const NoteQA = { ask, __test__ }`) per established pattern (MiniSearchIndex, MemoryEngine).

- **Strict-clean:** Zero new `@ts-expect-error NP-STRICT` markers.
  </action>
  <verify>
    <automated>pnpm test -- tests/core/notes/NoteQA.test.ts</automated>
  </verify>
  <done>NoteQA.ask() retrieves MiniSearch top-5 + memory facts, returns balanced-tier synthesis with citations, tiny mode falls back to plain MiniSearch, AI rerank activates on &lt;3 results, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NoteChatConverter — chat/page → structured note draft</name>
  <files>
    src/core/notes/NoteChatConverter.ts,
    tests/core/notes/NoteChatConverter.test.ts
  </files>
  <read_first>
    src/core/memory/MemoryEngine.ts,
    src/core/notes/LinkParser.ts,
    src/core/notes/schemas.ts,
    src/core/ai/StructuredOutput.ts,
    src/core/ai/TierResolver.ts
  </read_first>
  <behavior>
    - Test 1: LLM-WIKI-07 — chat messages → structured note draft with title+content+tags+categoryPath
    - Test 2: NMEM-03 — memory context from MemoryEngine.assemble() enriches the draft
    - Test 3: Draft includes wikilinks extracted from content via LinkParser.parseLinks
    - Test 4: User is gatekeeper — draft is pre-filled, not auto-saved (no NotesDB.put call)
  </behavior>
  <action>
**NoteChatConverter.ts (LLM-WIKI-07, NMEM-03, D-118):**
- Create `src/core/notes/NoteChatConverter.ts` exporting `NoteChatConverter` object with `draftFromChat()` method.
- `draftFromChat(messages: Array<{ role: string; content: string }>, memoryContext?: string, abortSignal?: AbortSignal): Promise<NoteDraft>`:
  1. **Memory context (NMEM-03):** Call `MemoryEngine.assemble()` for compact memory context (created in Plan 01). Use `memoryContext` param if provided (test injection), else call `assemble()`.
  2. **Prompt:** Build prompt with conversation messages + memory context, requesting structured JSON: `{ title, content, tags[], wikilinks[], categoryPath, summary }`.
  3. **Fast-tier call:** Resolve `resolveTier('fast')`, call `requestJson(NoteDraftSchema, prompt, { operationId, timeoutMs: 15_000, ... })`.
  4. **Wikilink extraction:** Run `parseLinks(draft.content)` to extract any `[[Title]]` targets; merge with draft.wikilinks (dedupe).
  5. Return `NoteDraft` — pre-filled for user review, NOT auto-saved (user is gatekeeper per LLM-WIKI-07).

- **UI-SPEC data contract:** `NoteDraft` matches the draft shape consumed by NoteEditor pre-fill in Phase 15.

- **Object-form namespace export** per established pattern.

- **Strict-clean:** Zero new `@ts-expect-error NP-STRICT` markers.
  </action>
  <verify>
    <automated>pnpm test -- tests/core/notes/NoteChatConverter.test.ts</automated>
  </verify>
  <done>NoteChatConverter.draftFromChat() produces structured note draft from conversation + memory context, wikilinks extracted, user-gated (no auto-save), all tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → NoteQA/NoteChatConverter | Untrusted structured data from AI provider crosses into UI rendering |
| Note content → RAG context | Untrusted note content crosses into LLM prompt for synthesis |
| Chat messages → NoteDraft | Untrusted assistant message content crosses into note draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-P9-05 | Tampering | NoteQA synthesis | medium | mitigate | Note content is untrusted data (CTX-02); structured JSON schema (NoteQAResultSchema) via StructuredOutput; system prompt separated from retrieval context |
| T-P9-06 | Information Disclosure | NoteQA/NoteChatConverter prompts | medium | mitigate | TraceRedactor applied before debugLog (§27.6); note content/paths redacted |
| T-P9-07 | Tampering | NoteChatConverter draft | low | mitigate | Draft is user-gated (LLM-WIKI-07): pre-filled for review, never auto-saved; user is gatekeeper |
</threat_model>

<verification>
1. `pnpm test -- tests/core/notes/NoteQA.test.ts tests/core/notes/NoteChatConverter.test.ts` — all green
2. `pnpm lint` — zero new @ts-expect-error NP-STRICT markers
3. NoteQA balanced-tier path invokes StructuredOutput.requestJson
4. NoteQA tiny mode returns fallback:true without LLM call
5. NoteChatConverter does NOT call NotesDB.put (user-gated)
</verification>

<success_criteria>
- NoteQA.ask() returns cited answers from MiniSearch top-5 + memory facts at balanced tier
- NoteQA tiny mode falls back to plain MiniSearch (no LLM) when balanced tier unavailable
- NoteQA AI rerank activates when <3 MiniSearch results
- NoteChatConverter.draftFromChat() produces structured draft with memory context enrichment
- NoteChatConverter extracts wikilinks from draft content
- NoteChatConverter does NOT auto-save (user is gatekeeper)
- All tests green, strict-clean
</success_criteria>

<output>
Create `.planning/phases/09-llm-wiki-filesystem-sync/09-02-SUMMARY.md` when done
</output>
