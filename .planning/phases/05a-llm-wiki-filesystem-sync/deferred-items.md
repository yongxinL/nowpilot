# Deferred Items — Phase 05a

Out-of-scope discoveries logged during execution (never auto-fixed per the
executor scope boundary). Revisit after the phase or during maintenance.

| # | Item | Where | Status | Deferred At |
|---|------|-------|--------|-------------|
| 1 | `tests/core/ai/StreamAdapter.test.ts` (2 tests) + `tests/core/ai/providers/ProviderAdapter.test.ts` (4 tests) fail with the current declared dependency set (`ai` 7.0.48, zod 4.4.3): `capturedOnChunk is not a function` / `client.chat is not a function`. Reproduced identically at the pre-05a baseline commit with a fresh `npm install` (both plain and `--legacy-peer-deps`) — pre-existing failures unrelated to 05a-01's diff. The pre-task `package-lock.json` was already out of sync with `package.json` (root zod 3.25.76 vs declared `^4.4.3`) and `@testing-library/dom` was missing from the lockfile entirely; 05a-01's `npm install` normalized the lockfile (zod 4.4.3 at root, ai 7.0.48, `@testing-library/dom@^10` restored) which surfaced these latent failures. | tests/core/ai/ | open | 2026-08-02 |
| 2 | SC1 render surface — Phase 7 UI rendering of enrichment suggestions: accept/reject render for tags/category/summary (`note:enriched` event → component state). The service layer delivers the event (05a NoteTagger); rendering is Phase 7 scope. | Phase 7 Notes UI (note editor suggestion rows) | open | 2026-08-02 |
| 3 | SC2 clickable citations — Phase 7 UI: [N] citation links navigate to source notes. `Citation[]` delivered (NoteQA, incl. WR-05 markerless fallback); clickable rendering is Phase 7 scope. | Phase 7 "Ask notes" answer Bubble | open | 2026-08-02 |
| 4 | SC3 pre-filled editor — Phase 7 UI: chat/page → `NoteDraft` opens in the editor with the user as gatekeeper (nothing saved until confirm). `NoteDraft` delivered (NoteChatConverter); editor UI is Phase 7 scope. | Phase 7 SaveToNoteDialog | open | 2026-08-02 |
| 5 | Real-browser File System Access verification (D-09 restart-resume, CR-01 fix validation): `showDirectoryPicker()` + extension restart must be exercised in Chrome Full App — vitest/jsdom cannot run real platform handles; the service-layer proxy is the duck-typed native-branch tests (05a-01 task 1). | Phase 7 human test in Chrome Full App (backup folder + restart) | open | 2026-08-02 |
| 6 | UI-SPEC backstop rows (2): RAG in-flight indicator bubble ("Analyzing your notes…") and Ask-bar in-flight indicator — held-out Phase 7 visual tests (`verification: backstop` — Phase 7 verifier abstains → human_needed, never a silent pass). | Phase 7 RAG Bubble / Ask bar (UI-SPEC 05a rows 147, 197) | open | 2026-08-02 |
| 7 | UI-SPEC unresolved row (1): Re-analyze progress widget shape — exact widget (per-note ticker vs progress bar vs message sequence) not pinned by spec. Phase 7 planner assumption: sequential per-note updates via antd message/Progress in Options → Notes. | Phase 7 Options → Notes panel (UI-SPEC 05a row 174) | open | 2026-08-02 |
| 8 | Staleness hint rendering (LLM-WIKI-08 "Content has changed — [Regenerate tags/summary]"): Phase 7 renders from `getStaleNotes()`; the service-layer writer (WR-03, 05a-02 task 2 — `NotesDB.save()` diff-writer) already makes the query viable, so no Phase 7 writer is needed. | Phase 7 NoteList staleness hint | open | 2026-08-02 |
