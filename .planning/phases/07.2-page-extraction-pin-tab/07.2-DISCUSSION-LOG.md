# Phase 7.2: Page Extraction & Pin Tab - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-17
**Phase:** 7.2-Page Extraction & Pin Tab
**Areas discussed:** Content Script Architecture, PageContext Extraction Scope, Pin Tab UI & Behavior, Context Wiring, MCP Tool Contracts, Navigation Detection, Security & Privacy, Pinned Content Budget

---

## Content Script Architecture

| Option | Description | Selected |
|--------|-------------|----------|
| Single entrypoint with internal modules | One content.ts entrypoint composing PageExtractor, PageContextBridge, SPANavigationWatcher, ContentChangeWatcher (dormant) | ✓ |
| Multiple independent entrypoints | Separate WXT content scripts for different responsibilities | |

| Option | Description | Selected |
|--------|-------------|----------|
| chrome.runtime.sendMessage with RuntimeEnvelope | Stateless typed messages via Background SW relay | ✓ |
| chrome.tabs.sendMessage + Ports | Long-lived port connections | |

| Option | Description | Selected |
|--------|-------------|----------|
| Navigation + on-demand extraction | Extract on DOMContentLoaded + SPA nav + explicit request | ✓ |
| On every user interaction | Extract only when user interacts with panel | |
| On navigation only | Extract on load + SPA nav, cache result | |

**User's choice:** Single WXT entrypoint with modular internal architecture. Background SW acts as central relay using chrome.runtime.sendMessage + RuntimeEnvelope<T>. Extraction on navigation + explicit on-demand triggers.

---

## PageContext Extraction Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Readability-first with fallback | @mozilla/readability → article markdown; fall back to visible DOM text | ✓ |
| Full DOM text only | Always extract full DOM as plain text | |
| Metadata + selection only | Only url, title, meta + selected text | |

| Option | Description | Selected |
|--------|-------------|----------|
| Safety ceiling only, optimizer owns compression | Extract full content with high safety limit, mark as truncated if exceeded | ✓ |
| Hard truncation in content script | Enforce character limit at extraction | |

| Option | Description | Selected |
|--------|-------------|----------|
| Separate field, always capture | Always capture selectedText in PageContext | ✓ |
| Explicit trigger only | Only capture on context menu or shortcut | |
| Skip — Phase 8 handles it | Don't capture selection at all | |

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-detect with quality flag | extractionType + extractionQuality fields in PageContext | ✓ |
| Pattern-based domain matching | Regex patterns for known platforms | |

| Option | Description | Selected |
|--------|-------------|----------|
| Spec type + new fields | Full PRODUCT_SPEC type + selectedText + extractionType/Quality | ✓ |
| Simplified type, expand later | Only fields needed now | |

**User's choice:** Readability-first extraction with structured fallback. No hard limit in content script. Selected text always captured as separate field. Full spec type with quality classification fields.

---

## Pin Tab UI & Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Hybrid footer strip + management popover | Always-visible strip above composer, click for popover | ✓ |
| Collapsible panel in header | Hidden by default, toggles open | |
| Dedicated Pages nav tab | Separate tab in nav rail | |

| Option | Description | Selected |
|--------|-------------|----------|
| Compact chips with popover | Favicon + truncated title + pin/unpin actions | ✓ |
| Full-width chips | Full title, more controls | |
| Count badge only | Single button, opens management | |

| Option | Description | Selected |
|--------|-------------|----------|
| Persist closed pins as inactive | Keep PageContext, mark as "Tab Closed", allow unpin | ✓ |
| Auto-remove on close | Silently remove when tab closes | |
| Attempt restore on close | Try to reopen URL | |

| Option | Description | Selected |
|--------|-------------|----------|
| workspaceStore TabContext[] | Structured model with metadata + cached PageContext | ✓ |
| Separate storage key | Independent chrome.storage.local key | |

**User's choice:** Compact footer strip with chip-based UI. Closed pins persist as inactive. workspaceStore.pinnedTabs becomes TabContext[].

---

## Context Wiring

| Option | Description | Selected |
|--------|-------------|----------|
| workspaceStore as single source | Content script → SW → workspaceStore → hooks → optimizer | ✓ |
| On-demand per AI call | Request fresh page context before each AI call | |
| SW memory as intermediary | Store in SW memory, read directly | |

| Option | Description | Selected |
|--------|-------------|----------|
| Always include, pin context too | Active page always, pinned tabs as summaries | ✓ |
| Intent-detection gating | Only when user references page content | |
| Active page only | Never include pinned tabs | |

| Option | Description | Selected |
|--------|-------------|----------|
| Change to PageContext type | Direct object storage, no serialize/deserialize | ✓ |
| Keep as string, serialize at boundary | String type with JSON serialization | |

| Option | Description | Selected |
|--------|-------------|----------|
| Wire both hooks | useChat and useAgent get identical page context | ✓ |
| useChat only | Only chat gets page context | |

**User's choice:** workspaceStore as single source of truth. Always include active page, conditionally include pinned summaries. Change type to PageContext. Wire both hooks.

---

## MCP Tool Contracts

| Option | Description | Selected |
|--------|-------------|----------|
| Fresh active, cached pinned | get-page-content triggers fresh extraction for active, returns cached for pinned | ✓ |
| Return cached from workspaceStore | Always return stored data | |
| Any tab by id, fallback to active | Accept tabId, extract any tab | |

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit action parameter | pin-tab({ tabId, action }) with pin/unpin | ✓ |
| Separate tools | pin-tab + unpin-tab as distinct tools | |
| Toggle on/off | Auto-detect pinned state, toggle | |

| Option | Description | Selected |
|--------|-------------|----------|
| ToolRegistry + SW execution | Register as standard tools, execute via Background SW | ✓ |
| In-process execution | Run in same runtime as caller | |

**User's choice:** Fresh extraction for active page, cached for pinned tabs. Explicit action parameter on pin-tab. Register in ToolRegistry with SW execution.

---

## Navigation Detection

| Option | Description | Selected |
|--------|-------------|----------|
| URL-based via history API + observer | pushState/replaceState/popstate/hashchange + title observer | ✓ |
| DOM mutation heuristics | Watch for significant DOM mutations | |
| URL + DOM combined | Both approaches, URL primary | |

| Option | Description | Selected |
|--------|-------------|----------|
| 300ms debounce | Wait 300ms after SPA nav before extracting | ✓ |
| Immediate, no debounce | Extract on every detected nav | |
| 500ms debounce | Wait 500ms | |

| Option | Description | Selected |
|--------|-------------|----------|
| Load + SPA nav + visibility change | DOMContentLoaded + SPA nav + hidden → visible | ✓ |
| Load + SPA nav only | Only DOMContentLoaded and SPA nav events | |

**User's choice:** URL-based detection with 300ms debounce. Extract on load, SPA nav, and visibility change.

---

## Security & Privacy

| Option | Description | Selected |
|--------|-------------|----------|
| Built-in blocklist | Block chrome://, chrome-extension://, edge://, about:, view-source:, etc. | ✓ |
| User-configurable blocklist | Per-site opt-out via Options | |
| Per-site opt-in | Only extract when explicitly enabled | |

| Option | Description | Selected |
|--------|-------------|----------|
| Active + pinned only | Only current and pinned context, no history | ✓ |
| Memory only, no persistence | Never store page content | |
| Encrypted storage | AES-GCM for page content | |

| Option | Description | Selected |
|--------|-------------|----------|
| Text-only + field stripping | Readability sanitization + strip passwords/tokens/forms | ✓ |
| Text-only + DOMPurify | Double-layer sanitization | |

**User's choice:** Built-in blocklist. Persist active and pinned only. Text-only extraction with field stripping.

---

## Pinned Content Budget

| Option | Description | Selected |
|--------|-------------|----------|
| Active page priority + pinned summaries | Active gets full allocation, pinned tabs as lightweight summaries | ✓ |
| Equal division | 10% budget shared equally | |

| Option | Description | Selected |
|--------|-------------|----------|
| Reject at max, user must unpin | Error when trying to pin 11th tab, link to manage | ✓ |
| FIFO with auto-eviction | Oldest gets removed automatically | |
| User-reorderable with MRU | Reorder via popover, MRU sorting | |

**User's choice:** Active page priority. Max 10 with rejection at limit.

---

## the agent's Discretion

User decided all areas — no decisions left to agent discretion. Implementation details (exact message types, component internals, module structure) are standard engineering concerns covered by existing codebase patterns.

## Deferred Ideas

- MAIN world content script injection (Phase 8 — ServiceNow session tokens)
- ContentChangeWatcher activation (reserved for future dynamic-page support)
- User-configurable blocklist (future enhancement)
- CONT-06 Right-click → "Ask AI" (remains in Phase 8)
- DOMPurify sanitization (not needed; extracted data is structured text, not rendered HTML)
