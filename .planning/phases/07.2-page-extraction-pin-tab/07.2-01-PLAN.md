---
phase: 07.2-page-extraction-pin-tab
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/core/content/PageContext.ts
  - src/core/messaging/pageMessages.ts
  - src/core/stores/workspaceStore.ts
  - tests/core/content/PageContext.test.ts
  - tests/core/workspaceStore.test.ts
autonomous: true
requirements:
  - CONT-03
  - MCP tools #1 (get-page-content)
  - MCP tools #5 (pin-tab)

must_haves:
  truths:
    - "@mozilla/readability@0.6.0 and turndown@7.2.4 are installed at exact pinned versions in package.json"
    - "PageContext interface is exported from src/core/content/PageContext.ts with all PRODUCT_SPEC §25.1 fields plus Phase 7.2 extensions"
    - "TabContext interface is exported from src/core/content/PageContext.ts with tabId, windowId, page, pinnedAt, active, url, title"
    - "workspaceStore.currentPageContext type is PageContext | null (was string | null) and pinnedTabs type is TabContext[] (was string[])"
    - "workspaceStore has addPinnedTab and removePinnedTab setters enforcing max-10 limit and tabId deduplication"
    - "pageMessages.ts exports PAGE_CONTEXT_UPDATED and GET_PAGE_CONTEXT_REQUEST constants with Zod schemas"
    - "Existing workspaceStore tests pass after type migration; new PageContext type tests pass"
    - "npm ls @mozilla/readability and npm ls turndown show clean installs"
  artifacts:
    - src/core/content/PageContext.ts
    - src/core/messaging/pageMessages.ts
    - tests/core/content/PageContext.test.ts
  key_links:
    - "workspaceStore imports PageContext type → does not import from content/ code paths"
    - "pageMessages schemas match PageContext shape → any mismatch breaks SW validation"
---

<objective>
Install missing packages (`@mozilla/readability`, `turndown`), define the `PageContext` and `TabContext` types, migrate `workspaceStore` from `string`/`string[]` to `PageContext`/`TabContext[]`, and create message type constants with Zod schemas.

Purpose: Every downstream plan depends on these types being stable. Wave 0 must complete before any content script module, MCP tool, or UI component can be built.
Output: `src/core/content/PageContext.ts`, `src/core/messaging/pageMessages.ts`, migrated `workspaceStore.ts`, installed packages.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.2-page-extraction-pin-tab/07.2-CONTEXT.md
@.planning/phases/07.2-page-extraction-pin-tab/07.2-RESEARCH.md
@.planning/phases/07.2-page-extraction-pin-tab/07.2-PATTERNS.md
@src/core/stores/workspaceStore.ts
@src/core/navigation/navigationTypes.ts
@src/core/ai/tools/ToolDefinition.ts
@src/core/messaging/runtimeEnvelope.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @mozilla/readability and turndown</name>
  <files>package.json, pnpm-lock.yaml</files>
  <action>
Install the two extraction libraries as exact-version dependencies per RESEARCH.md Standard Stack (D-06, D-28):

```bash
pnpm add @mozilla/readability@0.6.0 turndown@7.2.4
```

Verify installation:
```bash
pnpm ls @mozilla/readability turndown --depth 0
```

Both packages pass the Package Legitimacy Audit (RESEARCH.md §Package Legitimacy Audit: both Approved, 9+ years old, 2.5M+/5.8M+ weekly downloads). No `[ASSUMED]` or `[SUS]` findings — no human checkpoint needed.

Per D-05 (ISOLATED world only): These libraries are used exclusively inside content scripts. WXT's vite bundler will tree-shake them from the main app bundle — no risk of polluting the UI runtime.
  </action>
  <verify>
    <automated>pnpm ls @mozilla/readability@0.6.0 turndown@7.2.4 --depth 0</automated>
  </verify>
  <done>Both packages installed at exact pinned versions, lockfile updated, no deprecation warnings, no postinstall scripts executed.</done>
</task>

<task type="auto">
  <name>Task 2: Define PageContext, TabContext types, and page message schemas</name>
  <files>src/core/content/PageContext.ts, src/core/messaging/pageMessages.ts, tests/core/content/PageContext.test.ts</files>
  <action>
Create two type/contract files. No runtime dependencies — pure TypeScript type exports and Zod schemas.

**`src/core/content/PageContext.ts`** — Interface exports (pattern from `src/core/navigation/navigationTypes.ts`):

Export `PageContext` interface matching:
- PRODUCT_SPEC §25.1 base fields: `url` (string), `origin` (string), `hostname` (string), `title` (string), `html?` (string, NOT persisted per RESEARCH.md Pitfall 2 — doc it), `markdown?` (string), `meta` (Record<string, string>), `extractedAt` (number), `addonId?` (string, Phase 8 placeholder), `addonFields?` (Record<string, unknown>, Phase 8 placeholder)
- Phase 7.2 extensions per D-08/D-09/D-10: `selectedText?` (string), `extractionType` (union literal: `'readability' | 'visible-content' | 'metadata-only'`), `extractionQuality` (union literal: `'article' | 'generic' | 'minimal'`)

Export `TabContext` interface matching:
- `tabId` (number), `windowId` (number), `page` (PageContext), `pinnedAt?` (number), `active?` (boolean — false when pinned tab is closed, D-13), `url?` (string — for matching/reactivating closed pins), `title?` (string — display when tab closed)

No classes, no default exports — interfaces only.

**`src/core/messaging/pageMessages.ts`** — Message constants + Zod schemas (pattern from `src/core/messaging/runtimeEnvelope.ts`):

Export `PAGE_CONTEXT_UPDATED` and `GET_PAGE_CONTEXT_REQUEST` as `as const` string literals.

Export `pageContextPayloadSchema` — Zod v4 object matching PageContext shape for SW-side payload validation (HARD-05 compliance). Include all fields from PageContext interface.

Export `getPageContextRequestSchema` — Zod object: `{ type: z.literal(GET_PAGE_CONTEXT_REQUEST), timestamp: z.number().optional() }`.

**`tests/core/content/PageContext.test.ts`** — Type-level assertions + schema validation tests:

Create the `tests/core/content/` directory first. Test that:
- A valid PageContext object conforms to the interface (structural check)
- `pageContextPayloadSchema.parse()` accepts valid payload, rejects missing required fields
- TabContext shape includes all required fields
- `extractionType` rejects invalid values (Zod enum)

Use `describe`/`it` vitest pattern matching existing test files.
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="PageContext"</automated>
  </verify>
  <done>PageContext and TabContext interfaces compile without errors. pageMessages schemas parse valid payloads and reject malformed ones. Type-level test file passes. No runtime code in PageContext.ts — pure type exports.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate workspaceStore types and add new setters</name>
  <files>src/core/stores/workspaceStore.ts, tests/core/workspaceStore.test.ts</files>
  <action>
Modify `src/core/stores/workspaceStore.ts` per D-14, D-18, D-30. Follow PATTERNS.md §11 exactly.

**Import addition (after line 3):**
```typescript
import type { PageContext, TabContext } from '../content/PageContext';
```

**Type migration (lines 16-17, 29-30):**
- `pinnedTabs: string[]` → `pinnedTabs: TabContext[]`
- `currentPageContext: string | null` → `currentPageContext: PageContext | null`
- `setPinnedTabs: (pinnedTabs: string[]) => void` → `setPinnedTabs: (pinnedTabs: TabContext[]) => void`
- `setCurrentPageContext: (currentPageContext: string | null) => void` → `setCurrentPageContext: (currentPageContext: PageContext | null) => void`

**Add new setter signatures (after line 35):**
```typescript
addPinnedTab: (tab: TabContext) => void;
removePinnedTab: (tabId: number) => void;
```

**Default values (lines 83-84):**
- `pinnedTabs: [] as TabContext[],`
- `currentPageContext: null as PageContext | null,`

**Setter implementations (after line 97):**
```typescript
setPinnedTabs: (pinnedTabs: TabContext[]) => set({ pinnedTabs }),
setCurrentPageContext: (currentPageContext: PageContext | null) => set({ currentPageContext }),
```

**New setter implementations:**
```typescript
addPinnedTab: (tab: TabContext) =>
  set((state) => {
    if (state.pinnedTabs.length >= 10) return state; // D-30: reject at limit
    if (state.pinnedTabs.some(t => t.tabId === tab.tabId)) return state; // deduplicate
    return { pinnedTabs: [...state.pinnedTabs, tab] };
  }),
removePinnedTab: (tabId: number) =>
  set((state) => ({
    pinnedTabs: state.pinnedTabs.filter(t => t.tabId !== tabId),
  })),
```

**Backward compatibility:** `chrome.storage.local` stores JSON — old persisted `pinnedTabs: []` (empty array) and `currentPageContext: null` deserialize correctly as new default values. No migration needed (RESEARCH.md Pattern 4).

**`tests/core/workspaceStore.test.ts`** — Extend existing tests (this file already exists):

Add tests for:
- `addPinnedTab` appends a TabContext to pinnedTabs array
- `addPinnedTab` rejects at 10 (no-op, returns unchanged state)
- `addPinnedTab` deduplicates by tabId
- `removePinnedTab` removes the matching tabId
- `removePinnedTab` does nothing for unknown tabId
- `setCurrentPageContext` accepts a full PageContext object (type verification)

Use existing vitest patterns in the file (`vi.stubGlobal`, `chrome.storage` mocking).
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="workspaceStore"</automated>
  </verify>
  <done>workspaceStore types match PageContext/TabContext interfaces. Default values deserialize from old persisted JSON without errors. addPinnedTab enforces max-10 + dedup. removePinnedTab filters by tabId. All existing workspaceStore tests continue to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → workspaceStore | Untrusted persisted JSON must be validated against new types; old `pinnedTabs: []` must deserialize safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.2-01 | Tampering | workspaceStore persisted state | medium | mitigate | Zustand persist middleware uses WriteJournal with `np_workspace` key. Old `[]`/`null` values deserialize safely — no migration crash. PageContext Zod schemas in pageMessages.ts validate payloads at SW boundary before store writes. |
| T-07.2-02 | Information Disclosure | chrome.storage.local | low | accept | PageContext stored via WriteJournal persists to chrome.storage.local (plain text, no encryption). Stored content is metadata + extracted text — no passwords, no tokens (D-28 stripping happens at extraction time in Plan 02). No historical archive (D-27: active + pinned only). |
| T-07.2-SC | Tampering | npm/pnpm installs | high | mitigate | Both packages passed legitimacy audit (Approved). slopcheck required before install. No human checkpoint — both are well-established. |
</threat_model>

<verification>
```bash
# After all tasks complete:
pnpm test -- --testPathPattern="(PageContext|workspaceStore)"
pnpm ls @mozilla/readability@0.6.0 turndown@7.2.4 --depth 0
```
</verification>

<success_criteria>
- `@mozilla/readability@0.6.0` and `turndown@7.2.4` in `dependencies` at exact versions
- `src/core/content/PageContext.ts` exports `PageContext` and `TabContext` interfaces
- `src/core/messaging/pageMessages.ts` exports message constants and Zod schemas
- `workspaceStore` uses `PageContext | null` and `TabContext[]` types
- `addPinnedTab` rejects at 10, deduplicates by tabId
- `removePinnedTab` filters by tabId
- All existing workspaceStore tests pass (no regressions)
- New type tests pass
</success_criteria>

<output>
Create `.planning/phases/07.2-page-extraction-pin-tab/07.2-01-SUMMARY.md` when done
</output>
