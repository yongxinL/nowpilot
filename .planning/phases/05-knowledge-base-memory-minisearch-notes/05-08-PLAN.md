---
phase: 05-knowledge-base-memory-minisearch-notes
plan: 08
type: execute
wave: 6
depends_on: [05-07, 05-05]
files_modified:
  - src/components/notes/NoteGraphView.tsx
  - src/components/pages/NotesPage.tsx
  - tests/components/notes/NoteGraphView.test.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [KNW-02]
must_haves:
  truths:
    - "NoteGraphView (d3-force ^3, §18 create-list path, Standalone bundle only — R-3) renders the derived note graph: nodes = notes (Circle radius ~10-14px; SELECTED/current note fill = colorPrimary, others = colorFillTertiary, isolated notes = reduced-opacity colorTextQuaternary-family but visible), edges = NoteGraph.edges (Line/Path, stroke colorBorder, arrowless), labels = truncated note titles (12px colorTextSecondary) beside nodes; theme colors come from tokens at runtime via theme-token access, never hardcoded hex (UI-SPEC Color)."
    - "Graph states per UI-SPEC + spec §12: < 3 notes → STR.notes.graphEmpty ('Create at least 3 notes to see the graph') and the simulation is NEVER rendered below 3 nodes; loading → STR.notes.graphLoading + skeleton block; error → STR.notes.graphFailed + Retry; node click → open that note (onOpenNote callback — the page selects it + switches to Notes view, the single navigation contract D-05-17); node hover → Tooltip with the full title."
    - "Reduced motion (UI-SPEC Motion): prefers-reduced-motion honored — the simulation runs to equilibrium and renders the final layout directly (simulation.tick(n) synchronous, no rAF animation loop); otherwise the tick-event rendering pattern (d3js.org/d3-force CITED API: forceSimulation + forceLink distance ~80 + forceManyBody strength ~-200 + forceCenter)."
    - "Tests use manual tick stepping (simulation.tick(n) — synchronous, no rAF, Pitfall 6): jsdom component tests never hang; graph DATA derivation (NoteGraph) tests stay in node env (05-05)."
    - "NotesPage graph view: Segmented 'Graph' toggle → NoteGraphView full-pane (padding lg, colorBgBase); the graph rebuilds on note:saved (subscribe) and on mount; node click switches to Notes view + selects the note."
    - "verify:phase-5 passes GREEN end-to-end: the §24 chain (eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run) — D-05-19, spec line 3685's narrow form (tsc + vitest run tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts) is satisfied as a subset (all paths exist); the R-3 isolation check (Appendix G) confirms no memory/IndexedDB/MiniSearch/d3-force tokens leak into the background/content bundles."
    - "REQUIREMENTS.md KNW-01..05 checkboxes flip to [x] (all five requirement rows realized across plans 05-01..08); the phase gate records the UI-SPEC ⚠ unresolved autocomplete max-height assumption outcome."
  artifacts:
    - "src/components/notes/NoteGraphView.tsx (d3-force simulation + states + reduced-motion)"
    - "src/components/pages/NotesPage.tsx (Graph view wiring)"
    - "tests/components/notes/NoteGraphView.test.tsx"
    - ".planning/REQUIREMENTS.md (KNW-01..05 checked)"
  key_links:
    - "NoteGraph.edges/backlinkIndex (05-05) are the graph's data source — NoteGraphView imports them, never re-derives from bodies (D-05-17)."
    - "NotesPage (05-07) owns the Segmented state + the onOpenNote navigation — NoteGraphView is a controlled pane."
    - "d3-force@^3 (05-01 install) is imported ONLY here (R-3) — the isolation grep is the gate."
  flagged_assumptions:
    - "A4 [research, ASSUMED]: d3-force 3.0.0 ESM/CJS works under WXT's bundler — the wxt build step of verify:phase-5 proves it."
    - "UI-SPEC ⚠ unresolved (from 05-07): the autocomplete dropdown max-height assumption is confirmed at verify time and recorded in this plan's SUMMARY."
    - "The two UI-SPEC backstop items are re-verified here per the UI Considerations lift: (1) notes list remains interactive at 1,000+ notes — 05-07's list is plain-scroll (no virtualization/capping), so this backstop is expected to resolve insufficient_spec → human_needed unless the executor produces explicit evidence; (2) MiniSearch <50ms/1000-notes real-world latency (manual verification per 05-VALIDATION)."
  prohibitions:
    - "No hex colors in NoteGraphView (theme tokens only, UI-SPEC Color); no text animation (spec §12.6 — only layout settling)."
    - "No d3-force import anywhere except NoteGraphView.tsx (R-3 — never side panel/background SW)."
    - "No graph store / no parse-at-render (D-05-17)."
    - "No exact test-count assertions in verify:phase-5 (P-5)."
---

<!-- 05-08 (2026-08-13): Wave-6 graph + phase gate. NoteGraphView (d3-force ^3,
     reduced-motion tick-stepping, theme-token colors, <3 → empty copy) wired
     into NotesPage's Graph view, then the phase gate: verify:phase-5 green
     (§24 chain, D-05-19) + R-3 isolation + REQUIREMENTS KNW-01..05 checkboxes.
     Golden Rule 10 — the phase is done only when the gate passes. -->

<objective>
Finish the UI with the d3-force `NoteGraphView` (§18 create list, KNW-02) wired into NotesPage's Graph view, then run the phase gate: `verify:phase-5` green end-to-end (§24 chain per D-05-19), the R-3 isolation check, and the REQUIREMENTS.md KNW-01..05 checkboxes (Golden Rule 10 — the phase ends only when the gate passes).

Purpose: the note graph + backlinks is ROADMAP SC#2 (browse the graph in the Standalone Notes view); the gate proves every requirement (KNW-01..05) is realized and every §24 chain step is green before the phase is declared done.

Output: NoteGraphView.tsx + NotesPage graph wiring + NoteGraphView.test.tsx; verify:phase-5 green; KNW checkboxes flipped.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-CONTEXT.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-PATTERNS.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-UI-SPEC.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-VALIDATION.md
@src/components/notes/NoteGraphView.tsx
@src/components/pages/NotesPage.tsx
@src/core/notes/NoteGraph.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: NoteGraphView — d3-force simulation + states + reduced motion (Pitfall 6)</name>
  <files>src/components/notes/NoteGraphView.tsx, tests/components/notes/NoteGraphView.test.tsx</files>
  <read_first>
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Common Operation 4 (L442-452 — the CITED d3-force API) + Pitfall 6 (L370-374 — jsdom hang / tick-stepping)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-UI-SPEC.md Graph visual contract (L205-213 — node/edge colors, <3 empty, reduced motion, click-to-open, Tooltip)
    - src/core/notes/NoteGraph.ts (edges/backlinkIndex — the data source, 05-05)
    - node_modules/d3-force (forceSimulation/forceLink/forceManyBody/forceCenter exports verified at install — 05-01)
    - src/components/pages/OptionsPage.tsx (antd Skeleton/Empty/Button + ErrorBoundary page pattern)
  </read_first>
  <action>
    Create src/components/notes/NoteGraphView.tsx (THE ONLY file importing d3-force — R-3, Standalone bundle):
    - Props: `{ notes: readonly Pick<Note, 'id' | 'title' | 'links'>[]; selectedNoteId?: string; onOpenNote: (noteId: string) => void; loading?: boolean; error?: boolean; onRetry?: () => void }` (controlled pane — the page owns state).
    - States: loading → STR.notes.graphLoading + antd Skeleton block; error → STR.notes.graphFailed + Retry Button (onRetry); notes.length < 3 → STR.notes.graphEmpty ('Create at least 3 notes to see the graph') — the simulation is NEVER constructed below 3 nodes (spec §12 row, zero-one-many E5).
    - Data (derived, D-05-17): `const edgeList = edges(notes);` nodes = notes.map((n) => ({ id: n.id, title: n.title, selected: n.id === selectedNoteId, degree: 0 })) with degree counted from edgeList (isolated = degree 0); edges map to { source, target } (note ids, WIKI-ID-01).
    - Simulation: `import { forceSimulation, forceLink, forceManyBody, forceCenter } from 'd3-force';` — `const simulation = forceSimulation(nodes).force('link', forceLink<GraphNode, GraphLink>(edgeList).id((d) => d.id).distance(80)).force('charge', forceManyBody().strength(-200)).force('center', forceCenter(width / 2, height / 2));` (width/height from a fixed viewBox or container measurement — planner discretion; a fixed viewBox 800×600 with preserveAspectRatio keeps jsdom deterministic).
    - Rendering: `<svg>` with a `<g>` per node (Circle radius 10-14 + truncated `<text>` label 12px) and a `<line>`/`<path>` per edge. Colors from theme tokens at RUNTIME (access the antd theme token via the existing ThemeStore/config seam or antd's useToken — never hex literals; UI-SPEC Color): selected node fill colorPrimary, others colorFillTertiary, isolated reduced-opacity (colorTextQuaternary-family), edges stroke colorBorder.
    - Interaction: node click → onOpenNote(node.id); node hover → antd Tooltip with the full title (wrapper div or per-node title attribute — planner discretion); pan/zoom optional (planner discretion, d3-zoom NOT required).
    - Reduced motion: `window.matchMedia('(prefers-reduced-motion: reduce)').matches` → run `simulation.tick(300)` synchronously and render the FINAL positions directly, then `simulation.stop()` (no tick-event animation); otherwise the standard `simulation.on('tick', ...)` pattern. Both paths render the same SVG structure (deterministic tests).
    - Cleanup: useEffect return → simulation.stop() on unmount.

    In tests/components/notes/NoteGraphView.test.tsx (new file; jsdom-align env; manual tick stepping — Pitfall 6: never await real simulation ticks):
    - < 3 notes (0, 1, 2) → STR.notes.graphEmpty copy and NO <svg> simulation rendered.
    - 4 notes with 2 edges → an <svg> renders with 4 node groups and 2 edge lines (query the DOM by data-node-id / data-edge markers); the selected node's circle carries the selected marker.
    - Reduced-motion path: stub matchMedia to 'reduce' → the component renders node positions without rAF (the test asserts the tick(300) final-layout path — a spy on simulation usage or simply that rendering completes synchronously with node <circle> cx/cy attributes present).
    - Node click → onOpenNote called with the clicked note id (fireEvent on the node <g>).
    - Loading → STR.notes.graphLoading + Skeleton; error → STR.notes.graphFailed + Retry fires onRetry.
  </action>
  <acceptance_criteria>
    - NoteGraphView.tsx contains the import literal `from 'd3-force'` (the ONLY file in src/ importing it — grep src/ excluding this file prints 0) and the literals `forceLink<` and `forceManyBody().strength(-200)` and `simulation.tick(300)` and `prefers-reduced-motion`.
    - The file contains NO hex color literal (grep `#[0-9a-fA-F]` prints 0 — theme tokens only, UI-SPEC Color).
    - tests/components/notes/NoteGraphView.test.tsx exits 0 with `pnpm vitest run tests/components/notes/NoteGraphView.test.tsx --bail=1` (no hang — tick-stepping is synchronous).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/components/notes/NoteGraphView.test.tsx --bail=1</automated>
  </verify>
  <done>NoteGraphView renders the derived graph (states + reduced-motion + token colors, click-to-open); tests green without jsdom hang (KNW-02/SC#2).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Graph view into NotesPage</name>
  <files>src/components/pages/NotesPage.tsx</files>
  <read_first>
    - src/components/pages/NotesPage.tsx (the 05-07 workspace — the Segmented [Notes|Graph] already declared in the header; the graph pane slot + view state)
    - src/core/notes/NoteGraph.ts (edges/backlinkIndex for the graph data)
    - src/core/events/EventBus.ts ('note:saved' subscription pattern from 05-07)
  </read_first>
  <action>
    In src/components/pages/NotesPage.tsx (edit — the 05-07 file):
    - The `view: 'notes' | 'graph'` Segmented state (already present from 05-07) gains its Graph branch: when view === 'graph', render `<NoteGraphView notes={allNotes} selectedNoteId={selectedId} onOpenNote={handleGraphOpen} loading={graphLoading} error={graphError} onRetry={reloadGraph} />` full-pane (padding lg, colorBgBase) INSTEAD of the two-pane Notes layout.
    - `handleGraphOpen(noteId)`: setSelectedId(noteId) + setView('notes') (the single navigation contract, D-05-17) — and clear any dirty draft via the existing discard guard first (reuse the 05-07 dirty-check path: if dirty → STR.notes.discard Popconfirm; on discard → open; on keep → stay).
    - Graph data: allNotes (already the page's state); the graph derives edges on demand via NoteGraph.edges (05-05) — no graph store, rebuild happens naturally on note:saved list refresh (the page's existing note:saved handler already refreshes allNotes — the graph pane re-derives from the prop change).
    - graphLoading: true during the initial listNotes mount (reuse the page's loading state); graphError: derived from the list error state + Retry re-runs the read (share the existing loadFailed retry — no new error state).
    - Dirty-guard interaction: toggling to Graph with a dirty draft → STR.notes.discard Popconfirm (per the 05-07 dirty-guard contract, now also covering the view toggle).
  </action>
  <acceptance_criteria>
    - NotesPage.tsx imports NoteGraphView (`import { NoteGraphView } from '@/components/notes/NoteGraphView'`) and renders it in the graph branch (grep `NoteGraphView` in NotesPage.tsx prints ≥ 2 — import + JSX).
    - `handleGraphOpen` sets selectedId + switches the view (grep the callback literal).
    - The graph branch reuses the existing loading/error/retry state (no duplicate error state: grep `graphFailed` in NotesPage.tsx — present ONLY as STR.notes.graphFailed passed to NoteGraphView, not a new local branch).
    - `pnpm vitest run tests/components/pages/NotesPage.test.tsx --bail=1` stays green and `pnpm exec tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/components/pages/NotesPage.test.tsx tests/components/notes --bail=1 && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Graph view wired into NotesPage (node click → open + switch view, dirty-guard on toggle, shared loading/error states); tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — verify:phase-5 green + isolation + KNW checkboxes (Golden Rule 10)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-VALIDATION.md (Sampling Rate + Manual-Only Verifications — the gate context)
    - .planning/REQUIREMENTS.md L89-93 (the KNW-01..05 rows to check)
    - .planning/PRODUCT_SPEC_v0_1.md §24 chain + L3685 (verify:phase-5 narrow form — the subset contract) + Appendix G (isolation)
    - package.json (verify:phase-5 script from 05-01)
  </read_first>
  <action>
    Run the phase gate (the acceptance criterion IS the command — T-4a-27 precedent):
    - `pnpm run verify:phase-5` — the FULL §24 chain (eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run). The spec L3685 narrow form (`tsc --noEmit && vitest run tests/core/memory tests/core/search tests/core/notes/LinkParser.test.ts`) is satisfied as a subset — all three test paths exist with green tests (05-01..07).
    - R-3 isolation check (Appendix G precedent): grep the build output (`dist/` or the wxt build artifacts per the prior-phase isolation script) for the phase tokens — confirm NO memory/IndexedDB/MiniSearch/d3-force leakage into the background SW or content-script bundles (only the Standalone bundle may reference them; the isolation test pattern from verify:phase-3/4a applies — the background/content tokens are the R-3 AI/vault set PLUS the Phase-5 additions where the background is concerned).
    - Fix any gate failure in-place (Rule 3 — the executor owns drift fixes; the gate command IS the acceptance criterion).
    - The UI-SPEC ⚠ unresolved autocomplete max-height assumption: confirm the dropdown scrolls within ~320px in the built Standalone bundle (a quick visual/DOM check via the existing component test or manual Standalone load) and record the outcome in the SUMMARY.
    - The two UI-SPEC backstops: record the evidence — (1) notes list interactive at 1,000+ notes (05-07's list is plain-scroll — no virtualization/capping mechanism; unless the executor can produce explicit evidence to the contrary, mark `insufficient_spec → human_needed` per the UI-Considerations status vocabulary, never a silent pass), (2) MiniSearch <50ms real-world latency (05-VALIDATION Manual-Only instructions — load 1,000 seeded notes in Standalone and confirm); if no explicit evidence is producible in this gate, mark them `insufficient_spec → human_needed` per the UI-Considerations status vocabulary (never a silent pass).
    - In .planning/REQUIREMENTS.md: flip KNW-01..05 `- [ ]` → `- [x]` (rows L89-93) and update the Traceability table rows for KNW-01..05 (Phase 5 | Done) with the standard note style (e.g. `> **KNW re-map (05-08):** …` summarizing the per-plan realization). Do NOT touch any other requirement rows. Use Edit (scoped) — never Write over REQUIREMENTS.md.
  </action>
  <acceptance_criteria>
    - `pnpm run verify:phase-5` exits 0 (the full §24 chain incl. `wxt build` — proving d3-force bundles cleanly, A4) — the gate command IS the acceptance criterion.
    - The isolation grep finds NO Phase-5 R-3 tokens in the background SW / content-script bundles (the check command exits 0 / 0 matches for the background token set).
    - REQUIREMENTS.md contains `- [x] **KNW-01**` through `- [x] **KNW-05**` (all five flipped) and the Traceability rows read `| KNW-01…05 | Phase 5 | Done |`.
    - The UI-SPEC ⚠ unresolved + 2 backstop outcomes are recorded in 05-08-SUMMARY.md (no silent passes).
  </acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-5</automated>
  </verify>
  <done>verify:phase-5 green end-to-end (§24 chain, D-05-19/Golden Rule 10); R-3 isolation clean; KNW-01..05 checked; UI considerations recorded — Phase 5 is DONE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note links[]/titles → d3-force graph nodes | stored note data crosses into the SVG DOM — display-only (no HTML), click → navigation callback |
| d3-force bundle → extension surfaces | the graph library is runtime-only — R-3: Standalone bundle only, never background SW / side panel / content scripts |
| verify:phase-5 gate | the §24 chain is the phase-done authority — a green gate is the security posture evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-28 | Tampering | SVG DOM injection via note titles | medium | mitigate | note titles render as SVG <text> content (never HTML) + antd Tooltip handles the hover text; the page's render path already sanitizes bodies via PortableMarkdown (05-07, R-10); no dangerouslySetInnerHTML anywhere in NoteGraphView |
| T-05-29 | Denial of Service | d3-force simulation hang in tests/runtime | medium | mitigate | reduced-motion tick(300) synchronous render + stop(); standard path uses the tick-event pattern with useEffect cleanup (simulation.stop on unmount); tests step ticks manually (Pitfall 6 — never await real ticks); <3 notes never constructs a simulation |
| T-05-30 | Tampering | R-3 isolation breach (d3-force/MiniSearch/IndexedDB in background) | high | mitigate | d3-force imported ONLY in NoteGraphView.tsx; the phase-gate isolation grep asserts the background/content bundles carry none of the Phase-5 R-3 tokens (memory/IndexedDB/MiniSearch/d3-force) — a breach fails verify:phase-5 and blocks the gate |
| T-05-31 | Tampering | forged graph data via tampered links[] | low | accept | edges() treats links[] as opaque ids; unknown targets render as isolated nodes (zero-one-many contract); dangling reconciliation (WIKI-ID-04, 05-05) keeps links consistent on save — no integrity boundary crossed |
</threat_model>

<verification>
- `pnpm vitest run tests/components/notes/NoteGraphView.test.tsx --bail=1` green (tick-stepping, no hang).
- `pnpm vitest run tests/components/pages/NotesPage.test.tsx tests/components/notes --bail=1` green (graph wiring regression).
- `pnpm run verify:phase-5` green — the §24 chain (eslint + prettier + tsc + wxt build + vitest run), D-05-19/Golden Rule 10.
- R-3 isolation grep clean for the background/content bundles.
- REQUIREMENTS.md KNW-01..05 flipped.
</verification>

<success_criteria>
- NoteGraphView: d3-force graph with states (<3 → graphEmpty, loading, error+Retry), token colors (no hex), reduced-motion tick-stepping, click-to-open — KNW-02/SC#2.
- NotesPage Graph view wired (single navigation contract, shared states, dirty-guard on toggle).
- verify:phase-5 green end-to-end (D-05-19; spec L3685 subset satisfied); R-3 isolation clean; KNW-01..05 checked — Phase 5 sealed per Golden Rule 10.
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-base-memory-minisearch-notes/05-08-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/components/notes/NoteGraphView.tsx` — d3-force (forceSimulation/forceLink/forceManyBody/forceCenter) graph pane: `{ notes, selectedNoteId, onOpenNote, loading?, error?, onRetry? }`, states (graphLoading/graphEmpty/graphFailed), token colors, reduced-motion `simulation.tick(300)` final-layout path, SVG node/edge markers
- `src/components/pages/NotesPage.tsx` — Graph view branch (NoteGraphView full-pane), `handleGraphOpen` (select + switch to Notes view), dirty-guard on view toggle, shared loading/error/retry states
- `tests/components/notes/NoteGraphView.test.tsx` (jsdom tick-stepping, no hang)
- `.planning/REQUIREMENTS.md` — KNW-01..05 `[x]` + Traceability rows (Phase 5 | Done) + KNW re-map note
