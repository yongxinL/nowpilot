---
phase: 05-knowledge-base-memory-minisearch-notes
plan: 10
type: execute
wave: 2
depends_on: [05-09]
files_modified:
  - src/components/pages/NotesPage.tsx
  - src/core/context/ContextOptimizer.ts
  - src/components/pages/useStreamingLLM.ts
  - src/components/notes/WikilinkAutocomplete.tsx
  - src/components/notes/BacklinksPanel.tsx
  - src/components/notes/NoteGraphView.tsx
  - src/core/i18n/strings.ts
  - tests/components/pages/NotesPage.test.tsx
  - tests/core/context/ContextOptimizer.test.ts
  - tests/components/pages/useStreamingLLM.test.tsx
  - tests/components/notes/WikilinkAutocomplete.test.tsx
  - tests/components/notes/BacklinksPanel.test.tsx
  - tests/components/notes/NoteGraphView.test.tsx
autonomous: true
requirements: [KNW-01, KNW-02, KNW-03, KNW-05]
gap_closure: true

must_haves:
  truths:
    - "Dirty drafts are never silently discarded: New note, New note from page, backlink rows, and resolved wikilinks all route through one guarded navigation helper that Popconfirms STR.notes.discard when dirty (CR-02)"
    - "The optimizer degradation ladder shares one reduced-hints source: a turn where top-3 memory + compact system fits the window never throws CONTEXT_TOO_LARGE (WR-02)"
    - "useStreamingLLM derives privacy mode from the real preferences for the upfront renderer invocation — prefs is assigned before the second invocation resolves (WR-07)"
    - "NotesPage logs the canonical codes: STORE_WRITE for putNote/getNote failures, NOTE_LINK_PARSE_FAILED only at the parse boundary (WR-03)"
    - "All five Phase-5 canonical codes have live call sites: SEARCH_INDEX_REBUILD_FAILED wraps index rebuilds, NOTE_GRAPH_FAILED wraps graph derivation (WR-04)"
    - "Shift+Enter while the wikilink dropdown is open inserts a newline instead of [[Title]] (WR-08)"
    - "The BacklinksPanel collapse control tooltip distinguishes expanded vs collapsed copy (IN-01)"
    - "applySelect dispatches state directly from a ref-read list — no state setters inside the setAllNotes updater (IN-02)"
    - "relativeTime honors the user's preferredLanguage, defaulting to 'en' (IN-03)"
    - "NoteGraphView renders a deterministic phyllotaxis layout before the first tick and preserves node positions across list refreshes (IN-04)"
    - "Search zero-hit query renders STR.notes.searchEmpty (UI-SPEC covered lift)"
    - "Loading states render skeleton rows + captions; error states render inline copy + Retry (UI-SPEC covered lift)"
    - "Long bodies scroll within the editor column with the title row pinned (UI-SPEC covered lift)"
    - "1-note lists show the single card (UI-SPEC covered lift)"
    - { statement: "notes list remains interactive at 1,000+ notes (virtualized or capped+paged rendering)", verification: backstop }
  artifacts:
    - "NotesPage.tsx — guardedOpen/guarded navigation helper + shared discard Popconfirm; STORE_WRITE/NOTE_LINK_PARSE_FAILED code split in handleSave; ref-based applySelect; locale-threaded relativeTime; SEARCH_INDEX_REBUILD_FAILED emits at index rebuilds"
    - "ContextOptimizer.ts — shared reduced-hints source between the reduce-topk and minimal-mode ladder steps"
    - "useStreamingLLM.ts — prefs assigned before the renderer upfront invocation"
    - "WikilinkAutocomplete.tsx — Shift+Enter falls through to the TextArea default"
    - "BacklinksPanel.tsx + strings.ts — distinct collapse/expand tooltip copy"
    - "NoteGraphView.tsx — phyllotaxis pre-tick fallback + position preservation + NOTE_GRAPH_FAILED emit"
    - "tests/components/pages/NotesPage.test.tsx — 4 dirty-guard path regressions"
    - "tests/core/context/ContextOptimizer.test.ts — WR-02 must-not-throw regression"
    - "tests/components/pages/useStreamingLLM.test.tsx — WR-07 privacy-mode ordering regression"
    - "tests/components/notes/WikilinkAutocomplete.test.tsx — WR-08 Shift+Enter regression"
    - "tests/components/notes/BacklinksPanel.test.tsx — IN-01 tooltip regression"
    - "tests/components/notes/NoteGraphView.test.tsx — IN-04 phyllotaxis + preservation regressions"
  key_links:
    - "Dirty state (dirty/dirtyRef) ↔ every navigation entry point — one guard helper owns the discard contract so no path can bypass it"
    - "reduce-topk ↔ minimal-mode ladder steps — both must consume the same reduced memory set or the reduction is silently undone"
    - "privacyModeFromPrefs(prefs) ↔ StageResolver closure — the upfront invocations must see the assigned prefs, not undefined"
    - "index rebuild sites (loadNotes/handleDelete) ↔ SEARCH_INDEX_REBUILD_FAILED — a failed rebuild is logged, never silent"
  prohibitions:
    - "The dirty-guard confirmation must never navigate more than once per confirm (no double applySelect)."
    - "appendTurn must never write a seq that already exists for the conversation (composite-key overwrite is data loss)."
    - "The memory-section budget must count the packed text (WMB + fact lines + separators), never per-fact estimates that ignore the working-memory block."
    - "Star-persistence regression must round-trip through real storage (toggle → stored np_workspace payload → re-init hydrate), never assert in-memory membership alone."
---

<objective>
Gap-closure plan 10: the UI + optimizer/wiring closure from VERIFICATION.md (score 7/11) and 05-REVIEW.md — the second of the two closure waves, sequenced after the core persistence/budget plan (05-09).

Purpose: Close the remaining blocker (CR-02 dirty-draft data loss across four navigation paths) and the warning/info cohort (WR-02 minimal-mode re-pack, WR-03 error-code misattribution, WR-04 dead codes, WR-07 prefs ordering, WR-08 Shift+Enter, IN-01..04). The optimizer/wiring fixes (WR-02, WR-07) live here because ContextOptimizer.ts and useStreamingLLM.ts own the memory-section degradation ladder and the hook wiring respectively — the same files the WR-01 fixes in 05-09 feed into.

Output: Every navigation path guards the dirty draft; the optimizer cannot spuriously throw CONTEXT_TOO_LARGE; canonical codes are attributed and emitted; Shift+Enter inserts newlines; the graph renders deterministically and preserves layout.

Constraint: preserve the byte-unchanged invariants pinned by the passing suites — the drop-in identity test (useStreamingLLM.test.tsx L328), the minimal-mode section-bytes tests (ContextOptimizer.test.ts L115-138), the NoteGraphView tick-stepping tests, and all NotesPage tests must stay green. The WR-02 fix must be a no-op when no reduction occurred (memoryHints empty → reduced-hints source unchanged).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-VERIFICATION.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-REVIEW.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-PATTERNS.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-UI-SPEC.md
@src/components/pages/NotesPage.tsx
@src/core/context/ContextOptimizer.ts
@src/components/pages/useStreamingLLM.ts
@src/components/notes/WikilinkAutocomplete.tsx
@src/components/notes/BacklinksPanel.tsx
@src/components/notes/NoteGraphView.tsx
@src/core/i18n/strings.ts
</context>

## Artifacts this phase produces

| Symbol / File | Kind | Produced by | Details |
|---|---|---|---|
| `NotesPage.tsx` guarded navigation helper | modify | Task 1 | `guardedNavigate(fn)` + `pendingNavRef` + `navDiscardPending` + one shared discard Popconfirm over a hidden trigger; wires New note, New note from page, BacklinksPanel rows, Preview wikilinks |
| `NotesPage.tsx` handleSave code split | modify | Task 1 | parse step → `NOTE_LINK_PARSE_FAILED`; putNote/getNote failures → `STORE_WRITE`; reconcileAfterSave catch → `STORE_WRITE` |
| `NotesPage.tsx` applySelect | modify | Task 1 | reads the note from a ref-synced list; dispatches setState directly (no setters inside the `setAllNotes` updater) |
| `NotesPage.tsx` relativeTime | modify | Task 1 | locale param threaded from a mount-time `readPersonaPrefs()` (default 'en') |
| `NotesPage.tsx` index rebuilds | modify | Task 1 | `buildNotesIndex`/`buildTitleIndex` wrapped with `debugLog(SEARCH_INDEX_REBUILD_FAILED)` |
| `tests/components/pages/NotesPage.test.tsx` | extend | Task 1 | 4 dirty-guard path regressions |
| `ContextOptimizer.ts` reduced-hints source | modify | Task 2 | shared `reducedMemoryHints` between reduce-topk and minimal-mode re-pack |
| `useStreamingLLM.ts` prefs ordering | modify | Task 2 | prefs assigned before the renderer upfront invocation |
| `tests/core/context/ContextOptimizer.test.ts` | extend | Task 2 | WR-02 must-not-throw regression |
| `tests/components/pages/useStreamingLLM.test.tsx` | extend | Task 2 | WR-07 renderer privacy-mode regression |
| `WikilinkAutocomplete.tsx` handleKeyDown | modify | Task 3 | accepts `shiftKey`; Shift+Enter falls through (no insert, no preventDefault) |
| `BacklinksPanel.tsx` + `strings.ts` | modify | Task 3 | distinct collapse/expand tooltip copy (two new STR.notes keys) |
| `NoteGraphView.tsx` layout | modify | Task 3 | exported `phyllotaxisLayout` pre-tick fallback + position preservation across restarts + `NOTE_GRAPH_FAILED` emit on derivation failure |
| 3 component test files | extend | Task 3 | WR-08 / IN-01 / IN-04 regressions |

<tasks>

<task type="auto">
  <name>Task 1: CR-02+WR-03+IN-02+IN-03+WR-04 — one guarded navigation path, correct codes, ref-based selection, locale, index-rebuild emits</name>
  <files>src/components/pages/NotesPage.tsx, tests/components/pages/NotesPage.test.tsx</files>
  <read_first>
    - src/components/pages/NotesPage.tsx (applySelect L214-231, handleOpenNote L233-240, handleNewNote L421-429, handleNewNoteFromPage L431-445, handleSave L264-307, reconcileAfterSave L314-349, handleDelete rebuild L404-407, dirtyRef L353-359, wikilinks onOpen L1014, BacklinksPanel onOpenNote L1040, header buttons L720-732)
    - src/components/pages/NotesPage.test.tsx (existing dirty-guard test L262-280 + star test L246-260 + new-from-page test L298+ to mirror)
    - src/core/i18n/strings.ts (notes block L54+ — copy contract)
    - src/core/ai/persona/personaConfig.ts (readPersonaPrefs — the sanctioned read accessor for the locale)
    - src/core/components/PortableMarkdown.tsx (resolved wikilink DOM: `data-np-wikilink-resolved="1"` anchor — the test click target)
  </read_first>
  <action>
    CR-02 (05-REVIEW.md CR-02, VERIFICATION truth #10): four navigation paths call `applySelect`/`setDraft` unconditionally and silently discard a dirty draft — `handleNewNote` (L421), `handleNewNoteFromPage` (L431), BacklinksPanel rows via `handleOpenNote` (L1040), resolved wikilinks in Preview via `handleOpenNote` (L1014). Route all four through ONE guarded navigation helper:

    1. Add a shared guard: `const pendingNavRef = useRef<(() => void) | null>(null)` + `const [navDiscardPending, setNavDiscardPending] = useState(false)`. Add `guardedNavigate = useCallback((fn: () => void) => { if (dirtyRef.current) { pendingNavRef.current = fn; setNavDiscardPending(true); return; } fn(); }, [])` — `dirtyRef` (already synced at L353-359) avoids stale closures.
    2. Reuse the existing `pendingGraphOpenRef`/`graphDiscardPending` mechanism pattern: render ONE controlled `Popconfirm` (title `STR.notes.discard`, `okButtonProps={{ danger: true }}`, `open={navDiscardPending}`) wrapped around a hidden trigger `<span data-np-nav-guard="1" style={{ display: 'none' }} />` mounted inside the page root (inside `ErrorBoundary`, after the view panes). `onConfirm` runs `pendingNavRef.current` then clears; `onOpenChange(false)` and `onCancel` clear without navigating.
    3. Rewire the four entry points to `guardedNavigate`:
       - `handleNewNote` body → `guardedNavigate(() => { <existing setDraft/setSelectedId/setDirty/setMode/setSaveError body> })`
       - `handleNewNoteFromPage` body → `guardedNavigate(() => { <existing body — reads useWorkspaceStore.getState().workspace.currentPageContext and drafts the page-export> })`
       - BacklinksPanel `onOpenNote={handleOpenNote}` (L1040) → `onOpenNote={(id) => guardedNavigate(() => handleOpenNote(id))}`
       - Preview `wikilinks.onOpen: handleOpenNote` (L1014) → `onOpen: (id) => guardedNavigate(() => handleOpenNote(id))`
       Do NOT touch the graph-flow guard (`handleGraphOpen`/graph-pane Popconfirm) or `renderCard`'s per-card Popconfirm — both already guard their paths; changing them risks the pinned tests.
    4. WR-03 (L300-305, L343-347): `handleSave`'s catch currently logs `NOTE_LINK_PARSE_FAILED` for putNote/getNote failures. Split the boundary: wrap ONLY the `parseLinks(draft.content)` call in its own inner try/catch that logs `ERROR_CODES.NOTE_LINK_PARSE_FAILED` + `setSaveError(true)` + return (the parse-boundary code gets its live call site — WR-04 closes for it too); the OUTER catch (putNote/getNote/resolve failures) logs `ERROR_CODES.STORE_WRITE`. `reconcileAfterSave`'s catch (L344) → `ERROR_CODES.STORE_WRITE`.
    5. IN-02 (L214-231): `applySelect` currently runs `setSelectedId/setDraft/setDirty/setMode/setSaveError` inside the `setAllNotes` updater. Add `const allNotesRef = useRef<Note[]>([])` + `useEffect(() => { allNotesRef.current = allNotes; }, [allNotes])`; rewrite `applySelect` to read `allNotesRef.current.find(...)` and dispatch the state updates directly (no updater form). No behavior change — selection must still no-op when the id is not in the list.
    6. IN-03 (L109-117): `relativeTime(ts, now, locale: string = 'en')` — the third param feeds `new Intl.RelativeTimeFormat(locale, { numeric: 'auto' })`. Export `relativeTime` for the unit test. In the page, read the locale once on mount via `readPersonaPrefs()` from `@/core/ai/persona/personaConfig` (the Phase-3 sanctioned read accessor — D-05-18 keeps it working), state `locale` defaulting to `'en'`, and pass it at the call site (L632). The read must be non-blocking and never throw (wrap in try/catch, fallback 'en').
    7. WR-04 (SEARCH_INDEX_REBUILD_FAILED): wrap the `buildNotesIndex`/`buildTitleIndex` calls in `loadNotes` (L166-167) and `handleDelete` (L406-407) in try/catch with `debugLog(ERROR_CODES.SEARCH_INDEX_REBUILD_FAILED, ..., { module: 'NotesPage', extra: { noteCount: notes.length } })` — a failed rebuild falls back to the previous index (or null) and never throws. Keep the incremental add path (`addToNotesIndex`) unchanged.

    Regression tests in `tests/components/pages/NotesPage.test.tsx` (one per bypass path — the existing dirty-guard test at L262 is the pattern: make dirty via `fireEvent.change` on `[data-np-note-body="1"]`, then `findByText(STR.notes.discard)`, then click the OK button):
    - "CR-02 new note: dirty draft + New note → discard Popconfirm; Discard creates the empty draft, Keep editing stays"
    - "CR-02 new note from page: dirty draft + [data-np-new-from-page] click (seed `currentPageContext` in the workspace store first — mirror the L298 test) → discard Popconfirm"
    - "CR-02 backlinks: seed two notes with n2 linking n1, select n1, dirty the draft, click `[data-np-backlink-row="1"]` → discard Popconfirm; OK opens n2"
    - "CR-02 wikilink: seed Alpha + a note whose content contains `[[Alpha]]`, select it, dirty the draft (edit the title), switch to Preview, click `[data-np-wikilink-resolved="1"]` → discard Popconfirm; OK opens Alpha"
    - IN-03 unit test: `relativeTime(ts, now, 'de')` produces a German-format string distinct from `relativeTime(ts, now, 'en')` (e.g. 'vor 1 Tag' vs '1 day ago') — export import from the page module.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/pages/NotesPage.test.tsx --bail=1</automated>
  </verify>
  <done>
    - Code: exactly one `guardedNavigate` helper; all four bypass paths route through it; the shared discard Popconfirm is controlled by `navDiscardPending`
    - Code: `handleSave` logs `NOTE_LINK_PARSE_FAILED` only at the parse boundary and `STORE_WRITE` for put/get failures; `reconcileAfterSave` logs `STORE_WRITE`
    - Code: `applySelect` reads `allNotesRef.current` and dispatches state directly
    - Code: `relativeTime` takes a locale param (default 'en') and the page threads `readPersonaPrefs().preferredLanguage`
    - Code: `buildNotesIndex`/`buildTitleIndex` sites are wrapped with `SEARCH_INDEX_REBUILD_FAILED` debugLogs
    - Behavior: each of the 4 navigation paths Popconfirms on a dirty draft (Discard navigates, Keep stays)
    - The full `tests/components/pages/NotesPage.test.tsx` suite (existing 9 + new) exits 0
  </done>
  <acceptance_criteria>
    - Code: `NotesPage.tsx` contains one `guardedNavigate` callback and the four entry points (handleNewNote, handleNewNoteFromPage, BacklinksPanel onOpenNote, wikilinks onOpen) all call it
    - Behavior: dirty draft + New note → `STR.notes.discard` Popconfirm appears; OK → empty draft; Cancel → draft unchanged
    - Behavior: dirty draft + backlink row click → Popconfirm; OK → the backlinked note opens
    - Behavior: dirty draft + resolved wikilink click in Preview → Popconfirm; OK → the target note opens
    - Code: `handleSave`'s outer catch passes `ERROR_CODES.STORE_WRITE`; the inner parse wrap passes `ERROR_CODES.NOTE_LINK_PARSE_FAILED`; `reconcileAfterSave`'s catch passes `ERROR_CODES.STORE_WRITE`
    - Code: no state setter invocation remains inside the `setAllNotes` updater callback in `applySelect`
    - Code: `SEARCH_INDEX_REBUILD_FAILED` appears in `debugLog` calls at both index-rebuild sites
    - Test: `pnpm vitest run tests/components/pages/NotesPage.test.tsx --bail=1` exits 0 (pre-existing dirty-guard/star/save-pipeline tests unchanged and green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WR-02+WR-07 — shared reduced-hints across the ladder, prefs before the upfront renderer invocation</name>
  <files>src/core/context/ContextOptimizer.ts, src/components/pages/useStreamingLLM.ts, tests/core/context/ContextOptimizer.test.ts, tests/components/pages/useStreamingLLM.test.tsx</files>
  <read_first>
    - src/core/context/ContextOptimizer.ts (ladder reduce-topk L369-386, minimal-mode L387-400, buildPackInput L120-151)
    - src/core/context/ContextCompressor.ts (reduceMemoryTopK L140-172 — slices to top-3 internally)
    - src/components/pages/useStreamingLLM.ts (L172-207 — invocation closure, prefs assignment, upfront calls)
    - tests/core/context/ContextOptimizer.test.ts (minimal-mode tests L115-138, FIVE_HINTS fixture L587-601, CONTEXT_TOO_LARGE test L140-159)
    - tests/components/pages/useStreamingLLM.test.tsx (send-path + upfront-resolution tests L202-261, drop-in identity L328)
  </read_first>
  <action>
    WR-02 (05-REVIEW.md WR-02): the ladder fires reduce-topk (L369) then minimal-mode (L387); minimal-mode re-packs via `buildPackInput(input, true, …)` which rebuilds the memory section from the FULL `input.memoryHints` (top-5), undoing the top-3 reduction — a turn where compact system + top-3 memory would fit still throws CONTEXT_TOO_LARGE.

    In `ContextOptimizer.optimize` (before the ladder loop at L303): introduce the shared reduced-hints source `let reducedMemoryHints: readonly RetrievedMemory[] = input.memoryHints;`. In the reduce-topk branch (L369-386), when `r.dropped.length > 0` (a real reduction applied), set `reducedMemoryHints = input.memoryHints.slice(0, 3)` — the SAME slice reduceMemoryTopK applies internally (top-3 whole-item, D-04-13). In the minimal-mode branch (L387-400), replace the re-pack with `sections = packSections(buildPackInput({ ...input, memoryHints: reducedMemoryHints }, true, trusted?.contextText));` so both ladder steps consume the same reduced set. When no reduction occurred (empty hints or no memory section), `reducedMemoryHints` stays `input.memoryHints` — byte-identical to today (the minimal-mode section-bytes tests must not change). Update the module header comment (L17-25) to state the shared-source rule.

    WR-07 (05-REVIEW.md WR-07): in `useStreamingLLM.send`, `prefs` is assigned at L207 but `invocation('planner')` (L188) and `invocation('renderer')` (L189) run before it — `privacyModeFromPrefs(undefined)` → 'prefer-local' for the upfront calls. Restructure so the renderer upfront call sees the real value:
    1. Resolve the planner invocation first: `const plannerInv = invocation('planner');` (the planner link is inherently first — its window derives the tier that derives prefs; note this in the code comment).
    2. Derive `plannerTier` → `const plannerInjection = await assembleMemory(plannerTier);` → assign `prefs = plannerInjection.preferences;`.
    3. THEN resolve the renderer invocation `const rendererInv = invocation('renderer');` — its `privacyModeFromPrefs(prefs)` now reads the assigned value — and `rendererTier` → `rendererInjection = await assembleMemory(rendererTier);`.
    The section bytes and the per-stage optimizer outputs must be identical (same prefs value, same contexts) — the drop-in identity test (L328) and the tier-divergence test (L287) must stay green. Update the L164-171 comment to document the ordering contract (planner link first by necessity, renderer sees real prefs).

    Regression tests:
    - WR-02 in `tests/core/context/ContextOptimizer.test.ts`: "WR-02: over-budget where top-3 + compact fits never throws CONTEXT_TOO_LARGE" — build a `ContextOptimizerInput` (medium/small window) whose memoryHints are large facts (e.g. 5 facts ≈ 2,500 tokens each — sized so the default-system + top-5 pack exceeds `inputBudget`, the default-system + top-3 pack still exceeds it, and the compact-system + top-3 pack fits), plus a `userInput` sized to straddle the same boundary; call `optimize` and assert it RETURNS (no throw), `provenance.stepsFired` contains both `'reduce-topk'` and `'minimal-mode'`, and the final memory section text equals `buildMemorySectionText({ memoryHints: <the top-3 slice> })`. Reuse the existing fixture helpers (`FIXED_PERSONA_BLOCK`, `MEMORY_ENABLED_PREFS`).
    - WR-07 in `tests/components/pages/useStreamingLLM.test.tsx`: extend the existing "both stages resolved upfront" test (or add a sibling): mock the memory-engine boundary so the planner injection returns `UserPreferences` with `allowCloudFallbackFromLocal: false`; after `send()`, inspect the `createStageInvocation` call args and assert the RENDERER-stage invocation received `privacyMode: 'local-only'` (the value `privacyModeFromPrefs` derives from that preference) — pinning that prefs was assigned before the renderer upfront call. Keep all other assertions of the original test intact.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/context/ContextOptimizer.test.ts tests/components/pages/useStreamingLLM.test.tsx --bail=1</automated>
  </verify>
  <done>
    - Code: `optimize` carries `reducedMemoryHints`; the reduce-topk branch updates it to `slice(0, 3)`; the minimal-mode re-pack consumes it
    - Behavior: over-budget input where top-3 + compact system fits → `optimize` returns (no CONTEXT_TOO_LARGE); stepsFired has reduce-topk + minimal-mode; final memory section is the top-3 text
    - Code: `useStreamingLLM.send` assigns `prefs` between the planner and renderer upfront invocations
    - Behavior: the renderer `createStageInvocation` receives the privacy mode derived from the real planner-injection preferences
    - Both touched suites (existing + new) exit 0; the drop-in identity and minimal-mode section-bytes tests unchanged
  </done>
  <acceptance_criteria>
    - Code: `ContextOptimizer.ts` has a `reducedMemoryHints` variable consumed by the minimal-mode branch; `reducedMemoryHints = input.memoryHints.slice(0, 3)` fires only when the reduce-topk step dropped items
    - Behavior: the WR-02 fixture (top-5 over budget, top-3 + compact under) resolves via `optimize` without throwing; final memory text is built from the top-3 hints
    - Code: `useStreamingLLM.ts` assigns `prefs = plannerInjection.preferences` before `invocation('renderer')` runs
    - Behavior: with `allowCloudFallbackFromLocal: false` in the mocked planner prefs, the renderer `createStageInvocation` call carries `privacyMode: 'local-only'`
    - Test: `pnpm vitest run tests/core/context/ContextOptimizer.test.ts tests/components/pages/useStreamingLLM.test.tsx --bail=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: WR-08+IN-01+IN-04+WR-04 — autocomplete Shift+Enter, backlinks tooltip, deterministic graph layout, NOTE_GRAPH_FAILED</name>
  <files>src/components/notes/WikilinkAutocomplete.tsx, src/components/notes/BacklinksPanel.tsx, src/components/notes/NoteGraphView.tsx, src/core/i18n/strings.ts, tests/components/notes/WikilinkAutocomplete.test.tsx, tests/components/notes/BacklinksPanel.test.tsx, tests/components/notes/NoteGraphView.test.tsx</files>
  <read_first>
    - src/components/notes/WikilinkAutocomplete.tsx (handleKeyDown L114-141, handle type L60-62)
    - src/components/notes/BacklinksPanel.tsx (tooltip ternary L51)
    - src/components/notes/NoteGraphView.tsx (positions state L107-110, simulation effect L112-153, render fallback L203-230)
    - src/core/i18n/strings.ts (notes block L54+ — where the two new keys land)
    - tests/components/notes/WikilinkAutocomplete.test.tsx (keyboard test L52-90)
    - tests/components/notes/BacklinksPanel.test.tsx
    - tests/components/notes/NoteGraphView.test.tsx (reduced-motion tick-stepping L105-115, FIXTURE L82-87)
  </read_first>
  <action>
    NOTE (executor — 7-file span): commit Task 3 in sub-steps, one component per commit — (1) WikilinkAutocomplete.tsx + its test, (2) BacklinksPanel.tsx + strings.ts + its test, (3) NoteGraphView.tsx + its test — running that component's suite after each commit (`pnpm vitest run tests/components/notes/WikilinkAutocomplete.test.tsx --bail=1`, then the BacklinksPanel suite, then the NoteGraphView suite) before starting the next sub-step; the full three-suite command below runs at the end over the assembled result.

    WR-08 (05-REVIEW.md WR-08): the imperative `handleKeyDown` inserts on ANY Enter regardless of modifiers (L128-133), swallowing Shift+Enter (the newline shortcut) while the dropdown is open. The page forwards the real React keydown event which carries `shiftKey`.

    1. `WikilinkAutocomplete.tsx`: widen the `WikilinkAutocompleteHandle` type to `handleKeyDown: (event: { key: string; shiftKey?: boolean; preventDefault?: () => void }) => void`. (05-PATTERNS.md role-match analog: `src/components/cmdk/CmdKPicker.tsx` — the combobox state/keydown pattern this file already follows; keep that shape.) In the handler (L128), guard the insert branch: when `key === 'Enter' && event.shiftKey === true`, RETURN without `preventDefault` and without `onInsert` — the TextArea default inserts the newline (the page's `handleBodyKeyDown` forwards the event and nothing else intercepts it). Tab and plain Enter keep inserting. Update the header comment (L15-17) to document Shift+Enter fall-through.
    2. IN-01 (05-REVIEW.md IN-01): `BacklinksPanel.tsx` L51 — the tooltip ternary is dead (`expanded ? STR.notes.backlinks : STR.notes.backlinks`). (05-PATTERNS.md role-match analog: `src/components/pages/OptionsPage.tsx` — the antd Card/List surface pattern this panel already follows; keep that shape.) Add two keys to `strings.ts` under `notes:` (following the existing English copy style): `backlinksCollapse: 'Collapse backlinks'` and `backlinksExpand: 'Expand backlinks'`. Use `title={expanded ? STR.notes.backlinksCollapse : STR.notes.backlinksExpand}` on the Tooltip AND the same state-specific string for the button `aria-label` (the section's own `aria-label={STR.notes.backlinks}` stays).
    3. IN-04 (05-REVIEW.md IN-04) + WR-04 (NOTE_GRAPH_FAILED): `NoteGraphView.tsx`:
       a. Add an exported pure helper `phyllotaxisLayout(nodes: readonly NoteGraphNode[]): ReadonlyMap<string, { x: number; y: number }>` — a deterministic sunflower/phyllotaxis arrangement (golden-angle ~137.5°, `radius = sqrt(i) * scale`, centered on VIEW_W/2, VIEW_H/2, scaled to fit the viewBox) with NO Date.now/Math.random (determinism rule — the same input yields the same map).
       b. Pre-tick fallback: in the render path, compute `const displayPositions = positions ?? phyllotaxisLayout(nodes);` and use it for the edge/node coordinates (L203-229) — the first frame renders the deterministic layout instead of every node at (0,0).
       c. Position preservation: mirror `positions` into a ref — `const positionsRef = useRef<ReadonlyMap<string, { x: number; y: number }> | null>(null);` kept in sync by `useEffect(() => { positionsRef.current = positions; }, [positions])`. In the simulation effect (L112-153), before building the simulation, seed each node's `x`/`y` from the ref (`const prev = positionsRef.current?.get(node.id); if (prev) { node.x = prev.x; node.y = prev.y; }` else leave the node for d3 (or seed from `phyllotaxisLayout`); d3-force respects pre-set coordinates as initial positions, so a list refresh (note:saved) no longer re-randomizes the layout. Do NOT add `positions` to the effect's dependency array — `setPositions` fires on every tick with a NEW Map reference, so adding it to deps re-runs the effect on every tick (rebuild sim → tick → setPositions → deps change) in an infinite loop that hangs the reduced-motion tick-stepping tests. The effect already re-runs on `nodes`/`edgeList` change (the list-refresh case) and reads the latest layout through `positionsRef`; if exhaustive-deps lint flags the ref read, silence it with a targeted eslint-disable comment on the dependency line rather than widening the deps.
       d. WR-04 (NOTE_GRAPH_FAILED): wrap the `edges(notes)` derivation in the `edgeList` useMemo (L91) in try/catch — on failure `debugLog(ERROR_CODES.NOTE_GRAPH_FAILED, ..., { module: 'NoteGraphView' })` and fall back to `[]` (the pane then renders its normal empty/edge-less state; the page's error state is driven by `listState` and unchanged).
    4. Tests:
       - WikilinkAutocomplete.test.tsx: "Shift+Enter falls through (WR-08)" — call the imperative handle with `{ key: 'Enter', shiftKey: true, preventDefault }` → `onInsert` NOT called and `preventDefault` NOT called; then `{ key: 'Enter', shiftKey: false }` → `onInsert` called. The existing keyboard test (L52) keeps its plain-Enter/Tab expectations.
       - BacklinksPanel.test.tsx: "collapse tooltip distinguishes state (IN-01)" — the expanded button carries `aria-label`/Tooltip `STR.notes.backlinksCollapse`; click to collapse → `STR.notes.backlinksExpand`.
       - NoteGraphView.test.tsx: (i) "phyllotaxisLayout is deterministic and in-view (IN-04)" — the same 4-node fixture yields an identical map across two calls, every node within `[0, VIEW_W] × [0, VIEW_H]`, and not all at (0,0); (ii) "first render without ticks shows the phyllotaxis fallback, never (0,0) (IN-04)" — render 4 notes WITHOUT the reduced-motion stub (no tick event yet), assert every node circle's cx/cy differs from 0 (the pre-tick frame renders the deterministic layout); (iii) "positions survive a list refresh (IN-04)" — render with the reduced-motion stub, capture the node coordinates, re-render with an ADDED note and an updated existing note, and assert the pre-existing nodes keep their captured coordinates (no re-randomization).
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/notes/WikilinkAutocomplete.test.tsx tests/components/notes/BacklinksPanel.test.tsx tests/components/notes/NoteGraphView.test.tsx --bail=1</automated>
  </verify>
  <done>
    - Code: `handleKeyDown` accepts `shiftKey` and returns early for Shift+Enter (no insert, no preventDefault)
    - Code: `STR.notes.backlinksCollapse`/`backlinksExpand` exist in strings.ts; the BacklinksPanel Tooltip + aria-label use the state-specific key
    - Code: `phyllotaxisLayout` exported + used as the pre-tick render fallback; the simulation seeds nodes from previous positions via `positionsRef` (NOT via the effect dependency array)
    - Code: the `edges(notes)` useMemo logs `NOTE_GRAPH_FAILED` on failure
    - Behavior: Shift+Enter while the dropdown is open does not insert; expanded/collapsed tooltips differ; first graph frame is deterministic; node coordinates survive a refresh
    - All three component suites (existing + new) exit 0
  </done>
  <acceptance_criteria>
    - Code: `WikilinkAutocompleteHandle.handleKeyDown` accepts `{ key, shiftKey?, preventDefault? }`; the Enter branch is skipped when `shiftKey === true`
    - Behavior: `handleKeyDown({ key: 'Enter', shiftKey: true, preventDefault })` → `onInsert` not called, `preventDefault` not called; `{ key: 'Enter', shiftKey: false }` → `onInsert` called
    - Code: `strings.ts` notes block contains `backlinksCollapse` and `backlinksExpand`; `BacklinksPanel.tsx` uses them state-specifically
    - Behavior: expanded panel tooltip text ≠ collapsed panel tooltip text
    - Code: `NoteGraphView.tsx` exports `phyllotaxisLayout`; render uses `positions ?? phyllotaxisLayout(nodes)`; the simulation effect seeds from previous positions via `positionsRef`
    - Code: `positions` is NOT added to the simulation effect's dependency array — the deps stay `[nodes, edgeList, notes.length, loading, error]` (a positionsRef mirror keeps the seeding path lint-clean without the re-simulation loop)
    - Behavior: first render (no tick) shows node cx/cy ≠ 0; re-render after a list change preserves pre-existing node coordinates (asserted WITHOUT adding positions to the effect deps)
    - Code: the `edgeList` useMemo catch logs `ERROR_CODES.NOTE_GRAPH_FAILED`
    - Test: `pnpm vitest run tests/components/notes/WikilinkAutocomplete.test.tsx tests/components/notes/BacklinksPanel.test.tsx tests/components/notes/NoteGraphView.test.tsx --bail=1` exits 0 (pre-existing tests unchanged and green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user draft ↔ navigation | Four navigation entry points cross from an unsaved editor draft into another note/view; an unguarded crossing silently destroys user input (data loss) |
| ladder degradation ↔ model window | reduce-topk/minimal-mode re-pack decisions cross the context budget boundary; an inconsistent memory set causes a spurious terminal error |
| hook wiring ↔ provider invocation | privacy mode computed from prefs crosses into the StageResolver closure; a stale read desyncs tier/budget resolution |
| user keyboard input ↔ autocomplete | Shift+Enter crosses the combobox keydown handler; swallowing it destroys the universal newline shortcut |
| derived graph layout ↔ render | node/edge coordinates cross the d3-force simulation boundary into SVG; a (0,0) frame or a re-randomized layout degrades the visual contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10-01 | Tampering | NotesPage guarded navigation | critical | mitigate | CR-02 Task 1: all four bypass paths route through `guardedNavigate` + the shared discard Popconfirm (Discard navigates once, Keep stays); 4 regression tests pin each path — acceptance: `tests/components/pages/NotesPage.test.tsx` exits 0 |
| T-05-10-02 | Denial of Service | ContextOptimizer ladder | high | mitigate | WR-02 Task 2: `reducedMemoryHints` shared between reduce-topk and minimal-mode; the must-not-throw regression proves top-3 + compact fits — acceptance: `ContextOptimizer.test.ts` exits 0 |
| T-05-10-03 | Tampering | useStreamingLLM prefs ordering | medium | mitigate | WR-07 Task 2: prefs assigned before the renderer upfront invocation; the privacy-mode regression pins `privacyMode: 'local-only'` for a no-fallback preference — acceptance: `useStreamingLLM.test.tsx` exits 0 |
| T-05-10-04 | Tampering | NotesPage error attribution | medium | mitigate | WR-03 Task 1: STORE_WRITE for put/get failures, NOTE_LINK_PARSE_FAILED only at the parse boundary — monitoring/attribution correctness (GR-9) |
| T-05-10-05 | Denial of Service | NoteGraphView/NotesPage rebuild paths | low | mitigate | WR-04 Tasks 1+3: index rebuilds and graph derivation wrapped with SEARCH_INDEX_REBUILD_FAILED / NOTE_GRAPH_FAILED debugLogs — a failure is logged, never silent |
| T-05-10-06 | Spoofing | WikilinkAutocomplete keydown | low | mitigate | WR-08 Task 3: Shift+Enter falls through to the TextArea default — the newline shortcut is preserved (a11y/UX correctness) |
| T-05-10-07 | Information Disclosure | NoteGraphView first frame | low | mitigate | IN-04 Task 3: deterministic phyllotaxis fallback replaces the (0,0) origin flash; positions preserved across refreshes (visual contract) |
| T-05-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan — approved-stack packages unchanged |
</threat_model>

<verification>
- `pnpm vitest run tests/components/pages/NotesPage.test.tsx tests/core/context/ContextOptimizer.test.ts tests/components/pages/useStreamingLLM.test.tsx tests/components/notes/WikilinkAutocomplete.test.tsx tests/components/notes/BacklinksPanel.test.tsx tests/components/notes/NoteGraphView.test.tsx --bail=1` — all six touched suites green (pre-existing + new regressions)
- `pnpm exec tsc --noEmit` — type-safe (new handle signature, exported relativeTime/phyllotaxisLayout, new STR keys)
- Phase gate after both closure plans: `pnpm run verify:phase-5` — eslint + prettier + tsc + wxt build + vitest run over the 102-file/922-test baseline + the new regressions
- R-3 isolation: no new background-SW/content-script imports (all touched files are Standalone/Side-Panel surface modules)
</verification>

<success_criteria>
- All 4 dirty-guard bypass paths close (CR-02) with one regression test each
- WR-02 regression proves top-3 + compact system never throws CONTEXT_TOO_LARGE
- WR-07 regression pins the renderer privacy mode derived from real prefs
- WR-03/WR-04: canonical codes attributed and all five Phase-5 codes have live call sites (MEMORY_RETRIEVAL_FAILED via 05-09, MEMORY_EXTRACT_FAILED existing, NOTE_LINK_PARSE_FAILED parse-boundary, NOTE_GRAPH_FAILED + SEARCH_INDEX_REBUILD_FAILED here)
- WR-08 Shift+Enter inserts a newline; IN-01 tooltip distinguishes state; IN-02 applySelect is updater-pure; IN-03 locale threaded; IN-04 graph renders deterministically and preserves layout
- Human backstops from VERIFICATION.md (1,000-note list interactivity, MiniSearch real-world < 50 ms, graph visual pass, delete-menu spec gap decision) remain recorded for the end-of-phase human checkpoint — not automated here
- Flagged assumptions (never silently dropped): (a) the planner upfront invocation necessarily resolves before prefs is known (its window derives the tier that derives prefs) — only the renderer — the second upfront call — sees the assigned value; (b) the wikilink-click regression selector is `[data-np-wikilink-resolved="1"]` per PortableMarkdown's DOM contract; (c) UI-SPEC ⚠ unresolved autocomplete max-height is CONFIRMED as `MAX_DROPDOWN_HEIGHT = 320 + overflowY auto` (05-08 verify-time confirmation, per the KNW re-map note)
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-base-memory-minisearch-notes/05-10-SUMMARY.md` when done
</output>
