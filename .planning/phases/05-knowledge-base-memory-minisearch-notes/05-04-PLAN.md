---
phase: 05-knowledge-base-memory-minisearch-notes
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - src/core/memory/MemoryEngine.ts
  - src/core/memory/MemoryExtractor.ts
  - tests/core/memory/MemoryEngine.test.ts
  - tests/core/memory/MemoryExtractor.test.ts
autonomous: true
requirements: [KNW-04, KNW-05]
must_haves:
  truths:
    - "MemoryEngine.ts is the SINGLE orchestrator entry for surfaces (D-05-02): assemble(), recordTurn(), summariseIfNeeded(), updateWorkingMemory(), subscribe() — surfaces never talk to the individual stores directly (R-4)."
    - "assemble({ query, conversationId, tier, nowMs? }) returns a MemoryInjection DTO { memories: RetrievedMemory[]; workingMemoryBlock: string; preferences: UserPreferences }: (1) working-memory block read from UserMemoryStore FIRST (D-05-09 — injected before facts so it can never crowd them out, ≤300 tokens by construction); (2) user facts scored via MemoryScorer (scores in [0,1]) sorted desc; (3) budgets per D-05-06/GR-6: top-5 maximum, top-3 in tiny tier (ModelContextTier 'tiny'), running total ≤ 1000 tokens via estimateTokens (whole-item drops from the bottom, NEVER a substring slice — D-04-13/D-05-06); (4) preferences from PreferenceMemoryStore.read(); (5) never injects secrets (facts are already redacted at write time, O.10)."
    - "recordTurn({ conversationId, role, content, timestamp? }) appends the turn through ConversationMemoryStore and triggers summariseIfNeeded; summariseIfNeeded(conversationId) runs the 12-message compactor through ConversationMemoryStore (single-writer primary surface documented §13)."
    - "updateWorkingMemory(patch) routes to UserMemoryStore's O.10 updater (TraceRedactor-redacted, ≤300 tokens) — the ONLY sanctioned slice is the template tail, never a fact's content."
    - "MemoryExtractor.ts is the haiku-tier LLM stage (D-05-10): extractMemory({ turns, opts }) routes EVERY AI call through PersonaInjector.inject('memoryExtractor', PROMPTS.memoryExtractor.system, { persona, prefs }) (GR-3 — the Phase-3 seeded stage, D-11) + requestJson(MemoryExtractorResultSchema, sections, ctx) with exactly ONE repair then STRUCTURED_OUTPUT_FAILED (GR-4, Appendix L); the system prompt forbids secrets/raw customer data (R-10); provider/structure failures never throw — debugLog(MEMORY_EXTRACT_FAILED) + null return (non-blocking contract: the 5a call site fires after the IndexedDB write, 'save never waits' §22.1)."
    - "MemoryExtractorResultSchema is co-located in MemoryExtractor.ts (zod 3): { memories: array of { content, type 'fact'|'preference'|'pattern', tags default [], confidence 0..1 default 0.5, source 'explicit'|'inferred'|'system' default 'inferred' }, max 10 per call } — the extractor maps results to UserMemoryFact (created/updated = nowMs) and returns them for the caller (5a) to persist via MemoryEngine.addFacts (single-writer preserved)."
    - "The whole engine is deterministic where testable: injectable nowMs threads through assemble/recordTurn/updateWorkingMemory (Pitfall 6 precedent); IndexedDB/chrome-bound work lives in the store layer, MemoryEngine orchestrates."
  artifacts:
    - "src/core/memory/MemoryEngine.ts (assemble, recordTurn, summariseIfNeeded, updateWorkingMemory, addFacts, subscribe)"
    - "src/core/memory/MemoryExtractor.ts (MemoryExtractorResultSchema, MemoryExtractorResult, extractMemory)"
    - "tests/core/memory/MemoryEngine.test.ts"
    - "tests/core/memory/MemoryExtractor.test.ts"
  key_links:
    - "assemble's MemoryInjection is the hook's (05-06) input: memories → ContextOptimizerInput.memoryHints, workingMemoryBlock → the memory section prefix, preferences → the preferences section (D-05-07/08/09)."
    - "The per-tier budget (top-3 tiny / top-5 otherwise) happens HERE — the optimizer's reduceMemoryTopK (05-06) is only the fallback safety net (Pitfall 5)."
    - "MemoryExtractor's schema + PROMPTS.memoryExtractor.system + PersonaInjector('memoryExtractor') are the Phase-5a NoteTagger/NoteQA memory-upsert seam (§3.4 note — the ONLY notes→memory direction, D-05)."
  flagged_assumptions:
    - "Open Q3 [research, resolved]: assemble per stage with the stage's tier (the hook passes tier; top-5/top-3 decided in MemoryEngine) AND reduceMemoryTopK stays a real fallback (05-06)."
    - "A5 [research, ASSUMED]: conversationId 'default' stays the hook value until Phase 7 — ConversationMemoryStore still implements per-conversation keying (store tests prove it, 05-03); conversation-memory PROMPT injection (summary + recent turns) is the M2/Phase-7 wiring, the store + compactor + LRU ship fully here."
    - "The conversationSummarizer LLM stage (PROMPTS.conversationSummarizer exists) is NOT wired into MemoryEngine in Phase 5 — the compactor's summarise seam is injectable (05-03) and the LLM stage needs a PipelineStage-union decision (out of scope); documented seam."
  prohibitions:
    - "No prompt assembly in surfaces (GR-3) — MemoryEngine is the core builder the hook calls; the optimizer selects/threads sections."
    - "No mid-structure truncation: budget degradation is whole-item drops (top-5 → top-3 → empty), never text.slice on a fact (D-04-13/D-05-06)."
    - "No new error codes: retrieval failures reuse STORE_READ; extraction failures use the 05-01 MEMORY_EXTRACT_FAILED code."
    - "No secrets in memory injection (R-10/D-05-06): working-memory writes redact at write time (O.10); the extractor prompt forbids secrets."
---

<!-- 05-04 (2026-08-13): Wave-3 orchestration. MemoryEngine = the single surface
     entry (D-05-02) — assemble() budgets per tier (top-5/top-3 tiny/≤1000
     tokens, working memory first, D-05-06/09) and recordTurn/summariseIfNeeded
     dispatch to the 05-02/05-03 stores; MemoryExtractor = the haiku-tier LLM
     stage (D-05-10) routing through PersonaInjector('memoryExtractor') +
     requestJson (GR-3/GR-4, one repair, R-10 prompt). Open Q3 resolved:
     assemble per stage with the stage tier; the optimizer fallback (05-06)
     stays real. -->

<objective>
Build the orchestration layer: `MemoryEngine` — the ONLY surface entry for memory read/write (D-05-02) owning store dispatch, §3.4 scoring, budget enforcement (top-5 / top-3 tiny / ≤1000 tokens / working memory first), and injection assembly into the `MemoryInjection` DTO; and `MemoryExtractor` — the haiku-tier LLM stage (D-05-10) routing through PersonaInjector + requestJson with the Zod schema, one-repair GR-4 semantics, and the R-10 no-secrets prompt.

Purpose: the hook (05-06) calls `memoryEngine.assemble()` — the Golden-Rule-3 core builder that turns stores into the `memoryHints`/`preferences`/working-memory data the optimizer sections are made of. Without it, KNW-04/05 (budgeted memory + ≤1000-token injection) have no implementation.

Output: MemoryEngine.ts + MemoryExtractor.ts + both required test files green.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-CONTEXT.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-PATTERNS.md
@src/core/memory/types.ts
@src/core/memory/MemoryScorer.ts
@src/core/memory/UserMemoryStore.ts
@src/core/memory/ConversationMemoryStore.ts
@src/core/memory/PreferenceMemoryStore.ts
@src/core/context/TokenBudget.ts
@src/core/ai/persona/PersonaInjector.ts
@src/core/ai/StructuredOutput.ts
@src/core/prompts/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MemoryEngine — assemble budgets + orchestration surface (D-05-02/06/09)</name>
  <files>src/core/memory/MemoryEngine.ts, tests/core/memory/MemoryEngine.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md §3.4 injection rules (L598-616: top-5 / top-3 tiny / ≤1000 tokens / never secrets) + §3.6 budget (L614-634: working memory ≤300, truncate block before dropping facts) + §13 single-writer
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Pattern 3 (L271-287 — the MemoryInjection DTO + assemble shape) + Common Operation 1 (L391-407) + Pitfall 5 (budget location)
    - src/core/context/ModelContextTier.ts (ModelContextTier 'tiny'|'small'|'medium'|'large' + classifyModelContext)
    - src/core/context/TokenBudget.ts (estimateTokens L36-44 — the ONLY token counter, Pitfall 1)
  </read_first>
  <action>
    Create src/core/memory/MemoryEngine.ts (service module — orchestrates the 05-02/05-03 stores; store/IDB work stays in the stores, MemoryEngine composes):
    - `export const MAX_MEMORY_TOKENS = 1000;` (GR-6 §3.4) and `export const WORKING_MEMORY_MAX_TOKENS = 300;` (§3.6).
    - `export interface AssembleOptions { query: string; conversationId: string; tier: ModelContextTier; nowMs?: number }`.
    - `assemble(deps, opts: AssembleOptions): Promise<MemoryInjection>` where deps = `{ facts: UserMemoryStoreAPI; prefs: PreferenceMemoryStoreAPI; conversation: ConversationMemoryStoreAPI }` (structural injection — deterministic tests, no singleton imports): (1) `workingMemoryBlock = deps.conversation-free` — read via facts.readWorkingMemory(db) → its markdown or '' (never throws; fallback ''); (2) `scored = await deps.facts.retrieve(db, opts.query, opts.nowMs ?? Date.now())` → map top entries to `RetrievedMemory { id, content, type, tags, score }`; (3) BUDGET: `const maxMemories = opts.tier === 'tiny' ? 3 : 5; const memories = scored.slice(0, maxMemories);` then enforce the token cap with estimateTokens — drop from the END while `totalTokens > MAX_MEMORY_TOKENS` (whole-item drops, never slice — D-04-13); (4) `preferences = await deps.prefs.read()`; (5) return `{ memories, workingMemoryBlock, preferences }`. Header comment: D-05-02 single-orchestrator contract + the budget rules + no-secrets note.
    - `recordTurn(deps, { conversationId, role, content, timestamp? }): Promise<void>` — dispatch appendTurn to deps.conversation (never throws; GR-9 debugLog STORE_WRITE on failure).
    - `summariseIfNeeded(deps, conversationId, opts?): Promise<void>` — dispatch to deps.conversation.summariseIfNeeded (12-message compactor, seam for the LLM summarizer — 05-03).
    - `updateWorkingMemory(deps, db, patch): Promise<WorkingMemory>` — read current (or initWorkingMemory(WORKING_MEMORY_RESOURCE_ID)) → deps.facts.updateWorkingMemory(current, patch) → persist via deps.facts.putWorkingMemory → return the new block. Redaction + ≤300-token trim happen inside the O.10 updater (05-02).
    - `addFacts(deps, db, facts: UserMemoryFact[]): Promise<void>` — batch putFact for each (single-writer surface for extractor/5a callers — D-05-02/§3.4 note).
    - `subscribe(listener): () => void` — a lightweight change-notification seam (in-memory Set of listeners; fire on recordTurn/addFacts/updateWorkingMemory with a `{ kind, conversationId? }` payload; returns unsubscribe). The hook/UI may use it to trigger re-assemble (planner discretion — keep minimal).
    - Every exported function never throws (write paths); reads degrade to safe empties.

    In tests/core/memory/MemoryEngine.test.ts (new file; required §18; fake-indexeddb + fake chrome.storage harness; @vitest-environment node):
    - Budgets (D-05-06 pin): seed 8 scored facts + a fixed nowMs → assemble with tier 'medium' returns exactly 5 memories; tier 'tiny' returns exactly 3; scores within [0,1] and sorted desc.
    - Token cap: seed facts whose joined text exceeds 1000 tokens (estimateTokens) → assemble drops whole facts from the end until ≤ 1000 (assert the returned set is a prefix of the scored list and every returned fact is intact — no substring content).
    - Working memory first: a populated working-memory block + facts → workingMemoryBlock is the O.10 template content and memories are the fact list (the JOIN order — block before facts — is the 05-06 optimizer's contract, pinned here by the block being present + non-empty).
    - Preferences: a stored UserPreferences (via the PreferenceMemoryStore fake) → assemble.preferences deep-equals it (compact-JSON source, D-05-08).
    - recordTurn + summariseIfNeeded dispatch: a fake conversation store records the calls (spy); appendTurn routes the exact { conversationId, role, content }.
    - updateWorkingMemory: patch { Name: 'Alice' } → returned markdown contains '**Name**: Alice'; a secret-shaped value is redacted; a >300-token patch trims to ≤ 300 tokens.
    - Never-throws: assemble against a closed db resolves with { memories: [], workingMemoryBlock: '', preferences: defaults } (no rejection).
  </action>
  <acceptance_criteria>
    - MemoryEngine.ts contains `export const MAX_MEMORY_TOKENS = 1000` and `export const WORKING_MEMORY_MAX_TOKENS = 300` and `export async function assemble(` and the budget literal `opts.tier === 'tiny' ? 3 : 5` (or equivalent named constants MAX_MEMORIES_TINY/MAX_MEMORIES).
    - The file contains NO text.slice/substring on fact content (grep `slice(` — allowed only in the O.10 updater, which lives in UserMemoryStore) and NO `chrome.` reference.
    - MemoryEngine.test.ts exits 0 with `pnpm vitest run tests/core/memory/MemoryEngine.test.ts --bail=1` (required §18).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/memory/MemoryEngine.test.ts --bail=1</automated>
  </verify>
  <done>MemoryEngine.assemble budgets (top-5/top-3 tiny/≤1000 tokens, working memory first) + recordTurn/summariseIfNeeded/updateWorkingMemory/addFacts/subscribe ship; required §18 test green (KNW-04/05).</done>
</task>

<task type="auto">
  <name>Task 2: MemoryExtractor — haiku-tier LLM stage via PersonaInjector + requestJson (D-05-10)</name>
  <files>src/core/memory/MemoryExtractor.ts, tests/core/memory/MemoryExtractor.test.ts</files>
  <read_first>
    - src/core/ai/persona/PersonaInjector.ts (PipelineStage union L27 incl. 'memoryExtractor' — the Phase-3 seeded stage, D-11; inject() L53-62)
    - src/core/prompts/index.ts L59-64 (PROMPTS.memoryExtractor.system — 'Do not store secrets or raw customer data' R-10)
    - src/core/ai/StructuredOutput.ts (requestJson L92+ + StructuredOutputContext L58-71 — the Zod + one-repair GR-4 contract; isStructuredOutputFailed)
    - .planning/PRODUCT_SPEC_v0_1.md §3.4 note (LLM-Wiki extracts into this store via the MemoryExtractor schema — the ONLY notes→memory direction, D-05)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Pattern 3 + Pitfall 5 + MemoryExtractor row in the test map (haiku tier, non-blocking)
  </read_first>
  <action>
    Create src/core/memory/MemoryExtractor.ts (LLM-stage service, StructuredOutput conventions):
    - `export const MemoryExtractorResultSchema = z.object({ memories: z.array(z.object({ content: z.string().min(1), type: z.enum(['fact','preference','pattern']), tags: z.array(z.string()).default([]), confidence: z.number().min(0).max(1).default(0.5), source: z.enum(['explicit','inferred','system']).default('inferred') })).max(10) })` and `export type MemoryExtractorResult = z.infer<typeof MemoryExtractorResultSchema>` (zod 3 API).
    - `export interface ExtractMemoryOptions { operationId: string; persona?: PersonaProfile; prefs?: UserPreferences; nowMs?: number }`.
    - `export async function extractMemory(turns: readonly { role: 'user' | 'assistant' | 'tool'; content: string }[], opts: ExtractMemoryOptions, callProviderJsonMode: StructuredOutputContext['callProviderJsonMode']): Promise<UserMemoryFact[] | null>`:
      (1) `const system = PersonaInjector.inject('memoryExtractor', PROMPTS.memoryExtractor.system, { persona: opts.persona, prefs: opts.prefs })` — EVERY AI call routes through PersonaInjector (GR-3); (2) build the sections array: `[{ kind: 'system', text: system, tokens: estimateTokens(system), stable: true, sourceId: 'memory-extractor' }, { kind: 'user_input', text: JSON.stringify(turns.map((t) => ({ role: t.role, content: t.content }))), tokens: estimateTokens(...), stable: false, sourceId: 'memory-extractor-input' }]` — do not inline the persona prompt, PersonaInjector owns it; (3) `const result = await requestJson(MemoryExtractorResultSchema, sections, { operationId: opts.operationId, providerId, model, timeoutMs, callProviderJsonMode, abortSignal })` — providerId/model/timeout are caller-provided via an options field or a minimal default ('memory-extractor'/'haiku' defaults documented — planner discretion; the test injects stubs); (4) map `result.memories` to UserMemoryFact `{ id: crypto.randomUUID(), content, type, tags, confidence, source, createdAt: nowMs, updatedAt: nowMs, lastUsedAt: undefined, useCount: 0 }`; (5) catch: `isStructuredOutputFailed(err)` or any error → `debugLog(ERROR_CODES.MEMORY_EXTRACT_FAILED, 'memory extraction failed', { module: 'MemoryExtractor', extra: { operationId } })` (never log the raw model output — R-10) → return null (never throws — the caller's save path is never blocked, §22.1).
    - Header comment: D-05-10 (haiku tier, primary-surface only, non-blocking), D-11 stage, the R-10 prompt note, one-repair GR-4.

    In tests/core/memory/MemoryExtractor.test.ts (new file; @vitest-environment node; stub the LLM via the injected callProviderJsonMode):
    - Valid extraction: the stub returns a JSON string matching the schema → extractMemory returns UserMemoryFact[] with the mapped fields (created/updated = the injected nowMs, useCount 0).
    - GR-4 one repair: the stub returns INVALID JSON on attempt 1 and VALID on attempt 2 (repair) → the call succeeds (repair path exercised — the repair section appends per the StructuredOutput repair contract).
    - STRUCTURED_OUTPUT_FAILED: the stub returns invalid JSON twice → extractMemory returns null (never throws) and the debugLog fired with MEMORY_EXTRACT_FAILED (spy).
    - Provider failure: the stub throws a generic Error → null + MEMORY_EXTRACT_FAILED, no crash.
    - PersonaInjector route pin: spy on PersonaInjector.inject — called with stage 'memoryExtractor' and baseSystem === PROMPTS.memoryExtractor.system (GR-3/D-11) and the persona/prefs opts threaded.
    - Schema boundary: the stub returns extra unknown fields and 12 memories (over max 10) → the 12-item payload is rejected (repair/STROUT failure path) — the schema caps the call (bounded, R-2).
  </action>
  <acceptance_criteria>
    - MemoryExtractor.ts contains `export const MemoryExtractorResultSchema = z.object({` and `export async function extractMemory(` and the literal `PersonaInjector.inject('memoryExtractor'` and `PROMPTS.memoryExtractor.system` and `ERROR_CODES.MEMORY_EXTRACT_FAILED`.
    - The file contains NO `throw` in extractMemory (null on failure) and no raw-output logging (debugLog extra has no content field).
    - MemoryExtractor.test.ts exits 0 with `pnpm vitest run tests/core/memory/MemoryExtractor.test.ts --bail=1` (KNW-05, GR-4) and MemoryEngine.test.ts still passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/memory/MemoryExtractor.test.ts --bail=1</automated>
  </verify>
  <done>MemoryExtractor routes through PersonaInjector('memoryExtractor') + requestJson (one repair, MEMORY_EXTRACT_FAILED on failure, never throws); tests green (KNW-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stores (IndexedDB + chrome.storage) → MemoryEngine.assemble | persisted memory crosses into injection — budgets + no-secrets are the gate |
| MemoryInjection DTO → optimizer (05-06) | the assembled memory section crosses into the prompt — trustPrefs.memory gate applies at the hook (Open Q6) |
| conversation turns → MemoryExtractor (LLM) | user conversation text crosses the LLM boundary — the system prompt must forbid secret persistence (R-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-16 | Tampering | prompt injection via retrieved memory content | high | mitigate | MemoryEngine assembles the section as DATA (GR-7 — never instruction text); the hook drops memoryHints entirely when trustPrefs.memory === false (Open Q6, 05-06); memory content is user's own store (already redacted at write); the memory section is model-context data, not a policy input |
| T-05-17 | Information Disclosure | secrets in memory injection / extractor path | high | mitigate | O.10 TraceRedactor on every working-memory write (05-02, verbatim); PROMPTS.memoryExtractor.system forbids secrets; extractMemory debugLog extra never carries content (only operationId); D-05-06 'never inject secrets' is enforced by the write-time redaction + the extractor prompt |
| T-05-18 | Denial of Service | unbounded extraction payloads / repair loops | medium | mitigate | MemoryExtractorResultSchema caps memories at 10 per call (zod .max(10)); requestJson = exactly one repair then STRUCTURED_OUTPUT_FAILED (GR-4 — no repair loops, R-2); extractMemory returns null on failure (never blocks the save path) |
| T-05-19 | Tampering | budget bypass via forged store data | low | accept | Budgets (top-5/top-3/≤1000) are enforced in assemble over the retrieved set regardless of content; forged store rows only affect their own fact's score — no privilege boundary |
</threat_model>

<verification>
- `pnpm vitest run tests/core/memory/MemoryEngine.test.ts --bail=1` green (required §18 — KNW-04/05).
- `pnpm vitest run tests/core/memory/MemoryExtractor.test.ts --bail=1` green (KNW-05, GR-4).
- `pnpm vitest run tests/core/memory --bail=1` green (05-01/02/03 files too — the wave-merge sampling per 05-VALIDATION).
- `pnpm exec tsc --noEmit` green.
- Acceptance greps: MAX_MEMORY_TOKENS / MAX_WORKING_MEMORY_TOKENS literals, no slice on facts, no throw in extractMemory.
</verification>

<success_criteria>
- MemoryEngine: single orchestrator (D-05-02) with assemble budgets (top-5/top-3 tiny/≤1000 tokens/working memory first — D-05-06/09), recordTurn/summariseIfNeeded dispatch, O.10 updateWorkingMemory routing, addFacts + subscribe.
- MemoryExtractor: haiku-tier via PersonaInjector('memoryExtractor') + requestJson (GR-3/GR-4, one repair, MEMORY_EXTRACT_FAILED, never throws) — D-05-10.
- Scores in [0,1] (KNW-04); injection ≤1000 tokens / working memory ≤300 (KNW-05).
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-base-memory-minisearch-notes/05-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/core/memory/MemoryEngine.ts` — `MAX_MEMORY_TOKENS` (1000), `WORKING_MEMORY_MAX_TOKENS` (300), `AssembleOptions`, `assemble(deps, opts)` → `MemoryInjection`, `recordTurn(deps, …)`, `summariseIfNeeded(deps, …)`, `updateWorkingMemory(deps, db, patch)`, `addFacts(deps, db, facts)`, `subscribe(listener)`
- `src/core/memory/MemoryExtractor.ts` — `MemoryExtractorResultSchema` (zod 3, `.max(10)`), `MemoryExtractorResult`, `ExtractMemoryOptions`, `extractMemory(turns, opts, callProviderJsonMode)` → `UserMemoryFact[] | null`
- `tests/core/memory/MemoryEngine.test.ts` (required §18; budgets + token cap + working-memory-first + dispatch)
- `tests/core/memory/MemoryExtractor.test.ts` (GR-4 one-repair + STRUCTURED_OUTPUT_FAILED + PersonaInjector route + schema cap)
