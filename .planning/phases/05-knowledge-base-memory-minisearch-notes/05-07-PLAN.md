---
phase: 05-knowledge-base-memory-minisearch-notes
plan: 07
type: execute
wave: 5
depends_on: [05-05]
files_modified:
  - src/components/core/PortableMarkdown.tsx
  - src/components/notes/BacklinksPanel.tsx
  - src/components/notes/WikilinkAutocomplete.tsx
  - src/core/workspace/WorkspaceStore.ts
  - src/components/pages/NotesPage.tsx
  - tests/components/notes/BacklinksPanel.test.tsx
  - tests/components/notes/WikilinkAutocomplete.test.tsx
  - tests/components/pages/NotesPage.test.tsx
autonomous: true
requirements: [KNW-01, KNW-02, KNW-03]
must_haves:
  truths:
    - "PortableMarkdown gains an OPTIONAL wikilinks?: { resolve: (title: string) => { id?: string } | null } prop (Open Q4 resolution, default undefined → byte-identical for existing consumers): resolved titles render as clickable colorPrimary links, unresolved as muted/dashed (colorTextTertiary + dashed underline) with a 'Create note' affordance (WIKI-ID-03, UI-SPEC Color); DOMPurify.sanitize stays UNCONDITIONAL (R-10/T-1-07) and escapeRawHtml stays on — wikilink spans are pre-processed into safe link markup, never raw HTML."
    - "BacklinksPanel renders the current note's in-links (NoteGraph.backlinkIndex on the current note list, derived on demand — D-05-17): rows = title (14px/600) + 1-line snippet, click → open that note (callback); empty → STR.notes.backlinksEmpty ('No backlinks yet.'); section title STR.notes.backlinks ('Backlinks', 16px/600), collapsible; backlink count may badge the header (accent colorPrimary) — UI-SPEC BacklinksPanel contract."
    - "WikilinkAutocomplete is a custom anchored popover over the body TextArea (Open Q5 resolution — antd AutoComplete's text-input coupling fights caret insertion): triggers on '[[', lists matching existing note titles via MiniSearch title matching (< 50 ms p95 ≤ 5,000 notes, spec §22.1); keyboard ↑/↓/Enter/Tab/Esc; Enter/Tab inserts '[[Title]]' at the caret; no matches → dropdown closes SILENTLY (the user finishes an unresolved link — WIKI-ID-03, no blocking state); a11y combobox contract binding: aria-haspopup='listbox', aria-expanded, aria-activedescendant on the active option, role='listbox'/'option', polite insertion announcement (CmdKPicker combobox-state pattern); dropdown max-height ~320 px + internal scroll (the UI-SPEC unresolved item — explicit planner assumption)."
    - "NotesPage replaces the E5 placeholder (D-05-16/17): page header (SearchOutlined Input allowClear + Segmented Notes|Graph + 'New note from page' ghost Button shown ONLY when currentPageContext exists + 'New note' primary Button); Notes view = list column (~300px, note cards sorted updated desc with title 14/600 + star toggle + 2-line-clamp snippet + tag chips + relative time; empty STR.notes.empty, loading STR.notes.loading + Skeleton rows, error STR.notes.loadFailed + Retry, zero-hit search STR.notes.searchEmpty, results count '[n] notes') + editor column (no-selection STR.notes.selectNote; title Input 20/600 borderless + star + Edit|Preview Segmented + dirty 'Unsaved changes' caption + 'Save Note' primary + ⋯ More → Delete; tag chips + Add tag inline; body TextArea (Edit, '[[ ' triggers autocomplete) / PortableMarkdown (Preview, wikilinks resolved/unresolved per the contract); BacklinksPanel below)."
    - "The save pipeline runs D-05-15 VERBATIM: parseLinks(body) → resolveLinks(targets, allNotes) → NotesDB.put → EventBus.emit('note:saved', { noteId }) → MiniSearch incremental add + graph/backlinks re-derivation; write paths never throw (failure → STR.notes.saveFailed inline Retry, draft retained); delete via Popconfirm STR.notes.deleteConfirm (danger colorError OK) → NotesDB.deleteNote → WIKI-ID-04 dangling-edge reconciliation + index rebuild; dirty guard Popconfirm STR.notes.discard on switching notes/graph with a dirty draft; Cmd/Ctrl+S saves; New note creates an unsaved draft (crypto.randomUUID() assigned at FIRST save — no empty rows; source.kind 'manual'); New note from page pre-fills title+body from currentPageContext with source.kind 'page-export' (D-05-13 — SC#5 Page → PageContentService → Note → MiniSearch path)."
    - "Star toggles persist in WorkspaceStore.workspace.selectedNotes: string[] (D-18 declared field activated as the favorites set — no type widening, no new storage key; a minimal toggleSelectedNote action added to WorkspaceStore); StarFilled colorWarning when starred, StarOutlined otherwise; icon-only controls carry aria-label + Tooltip (≥36px touch targets, Standalone)."
    - "Save-time reconciliation (WIKI-ID-03, D-05-14): after a successful save, promoteUnresolvedLinks runs against the other notes whose unresolvedLinks[] match the saved title (bounded MiniSearch title lookup, primary surface only, NEVER blocks the save)."
    - "Every new UI string comes from STR.notes.* (05-01 keys verbatim) — no inline literals (Golden Rule 2); GR-9 codes to debugLog only, never in UI text (R-10)."
  artifacts:
    - "src/components/core/PortableMarkdown.tsx (wikilinks? prop + resolver pipeline)"
    - "src/components/notes/BacklinksPanel.tsx"
    - "src/components/notes/WikilinkAutocomplete.tsx"
    - "src/core/workspace/WorkspaceStore.ts (toggleSelectedNote action)"
    - "src/components/pages/NotesPage.tsx (REPLACE placeholder — real workspace)"
    - "tests/components/notes/BacklinksPanel.test.tsx"
    - "tests/components/notes/WikilinkAutocomplete.test.tsx"
    - "tests/components/pages/NotesPage.test.tsx"
  key_links:
    - "resolveLinks/promoteUnresolvedLinks (05-05) are the save-pipeline correctness contract — NotesPage must call them before every put (never resolve at render, WIKI-ID-02 anti-pattern)."
    - "MiniSearchIndex (05-05) powers BOTH the search field and the autocomplete — the same instance (built on mount, incremental on note:saved) serves both."
    - "'note:saved' (05-01 EventBus) is the re-derivation signal — NotesPage's own handler + the graph/backlinks listeners consume it (D-05-12/15/17)."
    - "currentPageContext (WorkspaceStore, Phase 4a) gates the 'New note from page' affordance — D-05-13/SC#5."
  flagged_assumptions:
    - "Open Q4 [research, resolved]: PortableMarkdown wikilinks via an optional prop with pre-processed link spans (default undefined → zero behavior change); DOMPurify stays unconditional."
    - "Open Q5 [research, resolved]: WikilinkAutocomplete = custom anchored popover (AutoComplete's text-input coupling fights caret insertion); a11y contract binding either way."
    - "UI-SPEC ⚠ unresolved: autocomplete dropdown max-height ~320 px + internal scroll is a planner assumption to confirm at verify time (05-08 gate task records the outcome)."
  prohibitions:
    - "No rich-text toolbar / no Directory column / no Inspector column / no Import-Backup toolbar / no Ask-notes RAG / no SaveToNoteDialog LLM draft (UI-SPEC invisible-by-contract + fencing list — all 5a)."
    - "No dangerouslySetInnerHTML anywhere — notes render ONLY through PortableMarkdown (DOMPurify unconditional, escapeRawHtml) — R-10/T-1-07."
    - "No new storage keys: star reuses selectedNotes; view toggle is component-local (no np_ key)."
    - "No memory/trust UI (KNW-04/05 core-only — 04b content-trust card unchanged)."
    - "No inline copy — every string via STR.notes.* (Golden Rule 2)."
---

<!-- 05-07 (2026-08-13): Wave-5 Notes workspace. The E5 placeholder becomes the
     real Standalone Notes view (UI-SPEC contract): list + editor + search +
     save pipeline + star + delete + dirty guard, with BacklinksPanel +
     WikilinkAutocomplete + the PortableMarkdown wikilink extension (Open Q4/5
     resolutions). MiniSearch powers search AND autocomplete; LinkParser drives
     the save pipeline; WorkspaceStore.selectedNotes activates as the favorites
     set (D-18, no widening). -->

<objective>
Replace the E5 placeholder with the real Notes workspace (UI-SPEC): the PortableMarkdown wikilink extension (Open Q4), `BacklinksPanel` and `WikilinkAutocomplete` (Open Q5) under `src/components/notes/`, and the full `NotesPage` (list + editor + search + save pipeline + star + delete + dirty guard) with the `note:saved` re-derivation wiring (D-05-15/16).

Purpose: this is the KNW-01/02/03 surface — atomic note CRUD with wikilinks, backlinks, full-text search, and the end-to-end Page → PageContentService → Note → MiniSearch path (D-05-13, SC#5). The UI consumes the 05-05 pure core (never re-implements parsing/graph/search) and the 05-01 keys/events (Golden Rule 2).

Output: PortableMarkdown wikilinks + BacklinksPanel + WikilinkAutocomplete + NotesPage + WorkspaceStore.toggleSelectedNote; three component test files green.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-CONTEXT.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-PATTERNS.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-UI-SPEC.md
@src/components/core/PortableMarkdown.tsx
@src/components/pages/NotesPage.tsx
@src/components/pages/OptionsPage.tsx
@src/components/cmdk/CmdKPicker.tsx
@src/core/notes/LinkParser.ts
@src/core/notes/NoteGraph.ts
@src/core/search/MiniSearchIndex.ts
@src/core/storage/NotesDB.ts
@src/core/events/EventBus.ts
@src/core/workspace/WorkspaceStore.ts
@src/core/i18n/strings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PortableMarkdown wikilinks (Open Q4) + BacklinksPanel</name>
  <files>src/components/core/PortableMarkdown.tsx, src/components/notes/BacklinksPanel.tsx, tests/components/notes/BacklinksPanel.test.tsx</files>
  <read_first>
    - src/components/core/PortableMarkdown.tsx (full file — current XMarkdown + DOMPurify.sanitize + escapeRawHtml body; the extension point)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-UI-SPEC.md Color contract (resolved = colorPrimary link; unresolved = colorTextTertiary + dashed underline + 'Create note') + Copywriting (backlinks/backlinksEmpty/createNote)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Pitfall 3 (L352-356 — no wikilink props today; optional-prop recommendation) + Open Q4
    - src/core/notes/NoteGraph.ts (backlinkIndex — the BacklinksPanel data source, 05-05)
    - src/components/pages/OptionsPage.tsx (antd Card/List/Empty pattern for the panel)
  </read_first>
  <action>
    Modify src/components/core/PortableMarkdown.tsx:
    - Extend PortableMarkdownProps with `/** Phase 5 (D-05-14/16): optional wikilink resolution — default undefined → zero behavior change for existing consumers. */ wikilinks?: { resolve: (title: string) => { id?: string } | null };` (Open Q4).
    - In the render path: when `wikilinks` is provided, PRE-PROCESS the content — replace each `[[Title]]` span with a SAFE placeholder token before sanitization and re-substitute after: for each match, call `wikilinks.resolve(title)`; resolved → a `<a data-np-wikilink="1" href="#note-{id}" data-title="{title}">{title}</a>`-shaped element; unresolved → a `<span data-np-wikilink-unresolved="1" data-title="{title}">[[{title}]]</span>`-shaped element (with a `data-create-note` marker). The mechanism MUST keep DOMPurify.sanitize unconditional + escapeRawHtml on (R-10/T-1-07) — the substitution happens on the ALREADY-sanitized XMarkdown output via a safe DOM walk that only maps the marker tokens to React elements (never dangerouslySetInnerHTML, never raw HTML passthrough). If `wikilinks` is undefined → render exactly as today (byte-identical path).
    - Styling: resolved → colorPrimary link (opens the target via the onOpenNote callback the page supplies); unresolved → colorTextTertiary + dashed underline + a 'Create note' affordance (STR.notes.createNote) that fires the page's onCreateNote(title) callback. Export a small `WikilinkClickHandler` type `{ onOpen?: (noteId: string) => void; onCreate?: (title: string) => void }` and add it to the prop shape `wikilinks?: { resolve: ...; onOpen?: ...; onCreate?: ... }`.

    Create src/components/notes/BacklinksPanel.tsx (OptionsPage antd pattern; §18 create-list path):
    - Props: `{ noteId: string; notes: readonly Pick<Note, 'id' | 'title' | 'content'>[]; onOpenNote: (noteId: string) => void }`.
    - Compute in-links via NoteGraph.backlinkIndex(notes).get(noteId) ?? [] (derived on demand — D-05-17); render a collapsible section (antd Collapse or a chevron Button — planner discretion per UI-SPEC 'collapsible'): header = STR.notes.backlinks (+ optional Badge count, accent colorPrimary), rows = note title (14px/600) + 1-line snippet (content first line, 2-line clamp), row click → onOpenNote(note.id); empty → STR.notes.backlinksEmpty (12px muted).
    - Icon-only collapse chevron carries aria-label + Tooltip (≥36px touch target).

    In tests/components/notes/BacklinksPanel.test.tsx (new file; jsdom-align env per vitest.config):
    - Renders the rows for the notes whose links[] contain noteId (fixture: 3 notes, one links to the current → exactly that one row with title + snippet).
    - Empty: current note with no in-links → STR.notes.backlinksEmpty text.
    - Click row → onOpenNote called with the backlink note's id.
    - PortableMarkdown: with wikilinks prop → resolved title renders as a link (data-np-wikilink marker) and unresolved renders muted/dashed + 'Create note' affordance; WITHOUT the prop → the body renders with NO marker elements (byte-identical — the marker data attributes are absent) and DOMPurify still sanitizes (an injected `<script>` in the body is stripped — the existing sanitization test pattern).
  </action>
  <acceptance_criteria>
    - PortableMarkdown.tsx contains the literal `wikilinks?:` in the props interface and the doc `default undefined` note; the file still contains `DOMPurify.sanitize` and `escapeRawHtml` (unconditional sanitization preserved).
    - BacklinksPanel.tsx contains `backlinkIndex(` (import from '@/core/notes/NoteGraph') and `STR.notes.backlinks` references and the callback literal `onOpenNote`.
    - tests/components/notes/BacklinksPanel.test.tsx exits 0 with `pnpm vitest run tests/components/notes/BacklinksPanel.test.tsx --bail=1`.
    - `pnpm vitest run tests/components/core/PortableMarkdown.test.tsx --bail=1` stays green (existing consumers byte-identical) and `pnpm exec tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/components/notes/BacklinksPanel.test.tsx tests/components/core/PortableMarkdown.test.tsx --bail=1</automated>
  </verify>
  <done>PortableMarkdown wikilinks (Open Q4, DOMPurify intact) + BacklinksPanel (derived in-links, empty/count states) ship; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: WikilinkAutocomplete — anchored combobox (Open Q5) with the binding a11y contract</name>
  <files>src/components/notes/WikilinkAutocomplete.tsx, tests/components/notes/WikilinkAutocomplete.test.tsx</files>
  <read_first>
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-UI-SPEC.md WikilinkAutocomplete contract (L245-250 — trigger '[[', keyboard, silent close, a11y combobox binding) + Interaction Contract search section
    - src/components/cmdk/CmdKPicker.tsx L68-89 (controlled/open + highlighted index + keydown handling — the combobox state pattern to copy)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Open Q5 (custom anchored popover) + Pitfall 6 (jsdom)
    - src/core/search/MiniSearchIndex.ts (searchNotes — title matching < 50 ms p95 ≤ 5,000 notes)
  </read_first>
  <action>
    Create src/components/notes/WikilinkAutocomplete.tsx (custom anchored popover — Open Q5):
    - Controlled widget: `{ open: boolean; onOpenChange: (open: boolean) => void; query: string; onQueryChange: (q: string) => void; matches: ReadonlyArray<{ id: string; title: string }>; onInsert: (title: string) => void; highlighted: number; onHighlightChange: (i: number) => void; }` — the PAGE owns the trigger state (TextArea onKeyDown detects '[[', caret position tracked; insertion appends ']]' + places the caret after) — the component is a pure listbox+anchor renderer (testable, no DOM measurement beyond the anchor rect).
    - Trigger contract: parent calls onQueryChange as the text after the last '[[ ' is typed; when query length ≥ 1 → open; matches via searchNotes(titleIndex, query, { limit: 8 }); when matches.length === 0 → the dropdown closes SILENTLY (WIKI-ID-03 — no blocking state, no error; the parent keeps typing).
    - Keyboard (the parent forwards keydown): ↑/↓ move highlighted (wrap), Enter/Tab insert '[[{title}]]' at the caret + close, Esc closes; mouse click inserts.
    - A11y (BINDING): the TextArea anchor announces `aria-haspopup="listbox"` + `aria-expanded` + `aria-controls`; the dropdown is `role="listbox"` with `role="option"` items; the active option carries `aria-activedescendant` on the anchor + `id` on the option; insertion announced politely (a visually-hidden aria-live region updates on insert — planner discretion). Items: title (14px) + optional tag chip; active item highlight = colorPrimaryBg (UI-SPEC Color). Dropdown max-height ~320 px + internal overflow-y auto (UI-SPEC ⚠ unresolved assumption).
    - R-3 note: MiniSearch instance is passed in as a prop (the page's mounted index — 05-05), never constructed inside.

    In tests/components/notes/WikilinkAutocomplete.test.tsx (new file; jsdom-align env):
    - Renders role='listbox' + role='option' items for the passed matches with aria-activedescendant on the highlighted one.
    - ArrowDown/ArrowUp change the highlighted index (wrap-around); Enter/Tab call onInsert with the highlighted title; Esc calls onOpenChange(false).
    - Empty matches → the component renders nothing (silent close contract).
    - A click on an option calls onInsert with its title.
    - The anchor carries aria-haspopup='listbox' + aria-expanded when open.
  </action>
  <acceptance_criteria>
    - WikilinkAutocomplete.tsx contains the literals `role="listbox"` and `role="option"` and `aria-activedescendant` and `aria-haspopup` and `max-height: 320px` (or a MAX_DROPDOWN_HEIGHT = 320 constant used in style).
    - The component file imports NO d3-force and NO chrome.* (R-3 — pure combobox).
    - tests/components/notes/WikilinkAutocomplete.test.tsx exits 0 with `pnpm vitest run tests/components/notes/WikilinkAutocomplete.test.tsx --bail=1`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/components/notes/WikilinkAutocomplete.test.tsx --bail=1</automated>
  </verify>
  <done>WikilinkAutocomplete ships the binding a11y combobox contract (custom popover, silent close, keyboard nav, ≤320px scroll); tests green.</done>
</task>

<task type="auto">
  <name>Task 3: NotesPage — real workspace (list + editor + search + save pipeline + star + delete)</name>
  <files>src/components/pages/NotesPage.tsx, src/core/workspace/WorkspaceStore.ts, tests/components/pages/NotesPage.test.tsx</files>
  <read_first>
    - src/components/pages/NotesPage.tsx (the E5 placeholder to REPLACE — D-15 comment + current imports)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-UI-SPEC.md Visual Hierarchy + Interaction Contract + Copywriting (the full page contract) + Invisible-by-contract (the 5a fencing)
    - src/core/storage/NotesDB.ts (openNotesDB/listNotes/putNote/deleteNote/getNoteByTitle L73-160)
    - src/core/notes/LinkParser.ts (parseLinks/resolveLinks/promoteUnresolvedLinks — the save pipeline) + src/core/notes/NoteGraph.ts (backlinkIndex) + src/core/search/MiniSearchIndex.ts (buildNotesIndex/searchNotes/addToNotesIndex/removeFromNotesIndex)
    - src/core/events/EventBus.ts ('note:saved' — subscribed for index/graph re-derivation)
    - src/core/workspace/WorkspaceStore.ts (selectedNotes field + the actions block — the D-18 field to activate)
    - src/components/pages/OptionsPage.tsx (page + ErrorBoundary + App.useApp + STR imports pattern)
  </read_first>
  <action>
    In src/core/workspace/WorkspaceStore.ts: add `toggleSelectedNote: (noteId: string) => void` to the store shape — `set((s) => ({ workspace: { ...s.workspace, selectedNotes: s.workspace.selectedNotes.includes(noteId) ? s.workspace.selectedNotes.filter((id) => id !== noteId) : [...s.workspace.selectedNotes, noteId] } }))` (zustand set, D-18 declared field activated as the favorites set — no type widening, no new storage key; np_workspace persistence already covers it).

    REPLACE src/components/pages/NotesPage.tsx (OptionsPage pattern — ErrorBoundary + antd + App.useApp + STR imports):
    - State: `allNotes` (listNotes on mount + on note:saved/delete), `selectedId`, `draft` ({ title, content, tags, source } for the editor), `dirty`, `mode: 'edit' | 'preview'`, `query`, `view: 'notes' | 'graph'` (component-local Segmented state per UI-SPEC — no storage key), a mounted MiniSearch notes index (useState<MiniSearch | null>; built via buildNotesIndex(listNotes) on mount, rebuilt after delete, incremental addToNotesIndex on note:saved) + a title-only MiniSearch for the autocomplete.
    - Page header: SearchOutlined Input allowClear (STR.notes.searchPlaceholder) filtering the list live via searchNotes (query empty → full list sorted updated desc; zero hits → STR.notes.searchEmpty; results count STR.notes.resultsCount '[n] notes' below the list, hidden when query empty); Segmented [Notes|Graph] (STR.notes.viewNotes/viewGraph, active = colorPrimary); ghost Button STR.notes.newNoteFromPage (rendered ONLY when useWorkspaceStore.getState().workspace.currentPageContext exists — D-05-13; drafts with title = page title, body = page markdown, source.kind 'page-export'); primary Button STR.notes.newNote (PlusOutlined) → empty unsaved draft (source.kind 'manual', focus title).
    - List column (~300px, own scroll): note cards (title 14/600 ellipsize 1 line + StarOutlined/StarFilled toggle → toggleSelectedNote; snippet 2-line clamp; tag chips truncate +N; relative time caption) sorted updated desc; selected card = colorPrimaryBg pill + colorPrimary title; empty → STR.notes.empty; loading → STR.notes.loading + Skeleton rows; error → STR.notes.loadFailed + Retry (re-runs listNotes).
    - Editor column: no selection → centered muted STR.notes.selectNote; selected/draft → header (title Input 20/600 borderless, star toggle, Segmented Edit|Preview, dirty caption STR.notes.unsaved + Badge dot, primary STR.notes.save button disabled when !dirty, ⋯ More → Delete Popconfirm STR.notes.deleteConfirm with [title] quoted + danger colorError OK) + tag chips row (closable Tags + STR.notes.addTag inline input, Enter commits) + body (Edit: TextArea with the '[[ ' autocomplete wiring — WikilinkAutocomplete from Task 2, insertion at caret; Preview: PortableMarkdown with wikilinks={{ resolve, onOpen: select + switch to Notes view, onCreate: draft a new note titled Title }}) + BacklinksPanel (Task 1).
    - Save pipeline (D-05-15 VERBATIM): on save click or Cmd/Ctrl+S — `const targets = parseLinks(draft.content); const { links, unresolvedLinks } = resolveLinks(targets, allNotes);` → build the Note (id: existing ?? crypto.randomUUID() — assigned at FIRST save; title fallback 'Untitled'; created/updated timestamps; tags; source.kind; version) → `await putNote(db, note)` (never throws) → `EventBus.emit('note:saved', { noteId: note.id })` → addToNotesIndex + setState refresh; failure → STR.notes.saveFailed inline Retry (draft retained); save-time reconciliation (WIKI-ID-03): after put, `promoteUnresolvedLinks(otherNotes, { id: note.id, title: note.title })` and persist the promoted notes (bounded, primary surface only, NEVER blocks save — fire-and-forget with debugLog on failure).
    - Delete: Popconfirm OK → `deleteNote(db, selectedId)` → removeFromNotesIndex → WIKI-ID-04 dangling-edge reconciliation (resolveDanglingOnDelete on the other notes, move dangling ids back into unresolvedLinks[] with the title lookup) → refresh list + rebuild index; failure → STR.notes.deleteFailed notification.error (App.useApp), note stays.
    - Dirty guard: switching selectedId / toggling to Graph view with dirty → Popconfirm STR.notes.discard (Discard danger / Keep editing).
    - Search field + autocomplete share the mounted index; the index is in-memory only (D-05-12 — no durable store).
    - R-3: everything runs in the Standalone surface (NotesPage is Standalone-only; no chrome.* in tests — the DB/list functions are injected or mocked).

    In tests/components/pages/NotesPage.test.tsx (new file; jsdom-align env; mock NotesDB via fake-indexeddb + stub the EventBus or subscribe in-test):
    - Empty state: no notes → STR.notes.empty rendered, header CTAs visible.
    - New note: click STR.notes.newNote → editor shows an empty draft (selectNote copy gone), Save disabled.
    - Save pipeline: type title + content with '[[Alpha]]' where Alpha exists → save → NotesDB.put called with links[] containing Alpha's id and the 'note:saved' event emitted with { noteId }.
    - Unresolved: '[[Ghost]]' → save → unresolvedLinks ['Ghost'] persisted; reconcile: create note titled 'Ghost' → promoteUnresolvedLinks path persists the earlier note's links[] update.
    - Delete: Popconfirm → deleteNote called → list refreshes; delete failure → STR.notes.deleteFailed toast.
    - Star: toggle → WorkspaceStore.selectedNotes contains the id (store assertion); unstar removes it.
    - Dirty guard: dirty draft + switch note → STR.notes.discard Popconfirm appears.
    - Search: type a query → list filtered by searchNotes results; zero hits → STR.notes.searchEmpty.
    - New note from page: with a seeded currentPageContext → the ghost button renders and pre-fills title/body + source.kind 'page-export'; without → hidden.
  </action>
  <acceptance_criteria>
    - NotesPage.tsx contains the literal `parseLinks(` and `resolveLinks(` calls in the save handler, `EventBus.emit('note:saved'` (or the event-bus wrapper call), `promoteUnresolvedLinks(` and `resolveDanglingOnDelete(` usages, and `STR.notes.` references for ≥ 10 distinct keys.
    - The old placeholder copy ('Notes live here once you save your first one.') is GONE from NotesPage.tsx (grep prints 0 — E5 placeholder retired, D-05-16).
    - WorkspaceStore.ts contains `toggleSelectedNote:` and no WorkspaceState type change (grep types/workspace.ts — selectedNotes stays `string[]`).
    - NotesPage.tsx contains NO dangerouslySetInnerHTML and NO raw HTML string building for the body (PortableMarkdown is the only renderer).
    - tests/components/pages/NotesPage.test.tsx exits 0 with `pnpm vitest run tests/components/pages/NotesPage.test.tsx --bail=1` and `pnpm exec tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/components/pages/NotesPage.test.tsx --bail=1 && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>NotesPage is the real workspace (list/editor/search/save pipeline/star/delete/dirty guard/new-note-from-page); WorkspaceStore.selectedNotes activated; tests green (KNW-01/03, SC#5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note bodies (NotesDB) → PortableMarkdown render | untrusted note content crosses into the DOM — DOMPurify + escapeRawHtml are the gate (R-10) |
| note titles/links → autocomplete + backlinks | stored titles cross into UI — display-only data, never executed |
| 'note:saved' broadcast → re-derivation handlers | a forged event must not corrupt the index/state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-24 | Tampering | XSS via note bodies / wikilink titles (PortableMarkdown) | high | mitigate | DOMPurify.sanitize stays UNCONDITIONAL + escapeRawHtml on (R-10/T-1-07); wikilink substitution is a post-sanitization safe DOM walk mapping marker tokens to React elements — never dangerouslySetInnerHTML, never raw HTML passthrough; the sanitization test asserts a script-injected body is stripped even with the wikilinks prop |
| T-05-25 | Tampering | forged note:saved events | medium | mitigate | the page's note:saved handler re-validates the payload noteId (string) before touching the index/state; EventBus handler try/catch + EVT_HANDLER debugLog built in (Pitfall 7) |
| T-05-26 | Information Disclosure | raw error codes / note content in UI | low | mitigate | GR-9 codes go to debugLog only — never in UI text (R-10); debugLog auto-redacts; UI copy is the STR.notes.* canonical strings only |
| T-05-27 | Tampering | save-time reconciliation write race | low | accept | promoteUnresolvedLinks runs fire-and-forget after the primary put (WIKI-ID-03 never blocks save); a race only defers promotion to the next save — no integrity violation (single-writer primary surface, §13) |
</threat_model>

<verification>
- `pnpm vitest run tests/components/notes --bail=1` green (BacklinksPanel + WikilinkAutocomplete).
- `pnpm vitest run tests/components/pages/NotesPage.test.tsx --bail=1` green (save pipeline/star/delete/dirty/search/new-note-from-page).
- `pnpm vitest run tests/components/core/PortableMarkdown.test.tsx --bail=1` green (existing consumers byte-identical).
- `pnpm exec tsc --noEmit` green.
- Acceptance greps: wikilinks prop, no placeholder copy, no dangerouslySetInnerHTML, toggleSelectedNote.
</verification>

<success_criteria>
- PortableMarkdown wikilinks via optional prop (Open Q4) — DOMPurify unconditional.
- BacklinksPanel + WikilinkAutocomplete (Open Q5, binding a11y contract) — the §18 create-list components ship.
- NotesPage = the real workspace (D-05-16/17) with the D-05-15 save pipeline, star via WorkspaceStore.selectedNotes (D-18 activation), delete + WIKI-ID-04 reconciliation, dirty guard, new-note-from-page (D-05-13/SC#5).
- All UI copy via STR.notes.* (Golden Rule 2); no 5a surfaces (fencing respected).
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-base-memory-minisearch-notes/05-07-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/components/core/PortableMarkdown.tsx` — `PortableMarkdownProps.wikilinks?: { resolve(title) → { id? } | null; onOpen?(noteId); onCreate?(title) }` (optional, default undefined), safe post-sanitize substitution
- `src/components/notes/BacklinksPanel.tsx` — `{ noteId, notes, onOpenNote }`, derived in-links via `NoteGraph.backlinkIndex`, collapsible, STR.notes.backlinks/backlinksEmpty
- `src/components/notes/WikilinkAutocomplete.tsx` — controlled combobox `{ open, query, matches, highlighted, onInsert, … }`, `role="listbox"`/`role="option"`, `aria-activedescendant`, MAX_DROPDOWN_HEIGHT 320px scroll
- `src/core/workspace/WorkspaceStore.ts` — `toggleSelectedNote(noteId)` action (D-18 `selectedNotes` activation)
- `src/components/pages/NotesPage.tsx` — REPLACED placeholder: full workspace (header search + Segmented + New-note-from-page + New note; list column; editor column; save pipeline parseLinks→resolveLinks→putNote→note:saved; delete + WIKI-ID-04; dirty guard; star; autocomplete + backlinks wiring)
- `tests/components/notes/BacklinksPanel.test.tsx` · `tests/components/notes/WikilinkAutocomplete.test.tsx` · `tests/components/pages/NotesPage.test.tsx`
