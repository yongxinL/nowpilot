---
phase: 05-knowledge-base-memory-minisearch-notes
plan: 06
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - src/core/ai/types.ts
  - src/core/context/ContextPack.ts
  - src/core/context/ContextOptimizer.ts
  - src/core/context/ContextCompressor.ts
  - src/components/pages/useStreamingLLM.ts
  - tests/core/context/ContextOptimizer.test.ts
autonomous: true
requirements: [KNW-05]
must_haves:
  truths:
    - "ContextOptimizerInput gains an additive optional `workingMemoryBlock?: string` field (D-04-07 additive-input-extension precedent — existing construction sites compile unchanged; only the hook sets it)."
    - "ContextPack.ts exports buildMemorySectionText({ memoryHints, workingMemoryBlock }) and buildPreferencesSectionText(prefs): the memory text joins the working-memory block FIRST then the retrieved facts as '- [score] content' lines (D-05-09 — working memory can never be crowded out); preferences text = JSON.stringify(prefs) (compact JSON incl. personaId/personaOverrides, D-05-08, deterministic key order); ContextPack already emits both as stable:true sections (sourceId 'preferences'/'memory') — the threading makes the previously-dead slots REAL."
    - "ContextOptimizer.buildPackInput threads preferencesText + memoryText from the input (currently dropped); empty memoryHints + no workingMemoryBlock → NO memory section at all (the memory-disabled gate — trustPrefs.memory === false at the hook yields this input)."
    - "ContextCompressor.reduceMemoryTopK is REAL (no longer a no-op): it accepts the memory source (hints + workingMemoryBlock) and when the ladder fires 'reduce-topk' it RE-BUILDS the memory section from memoryHints.slice(0, 3) (whole-item drops, D-04-13 no-slice gate — the fallback safety net behind MemoryEngine's per-tier budget, Pitfall 5); the optimizer ladder passes input data to it."
    - "useStreamingLLM.ts calls MemoryEngine.assemble per stage (planner tier + renderer tier from classifyModelContext(stageInvocation.modelContextWindow) — the StageInvocation's resolved window, T-04-22) and passes { memoryHints: injection.memories, workingMemoryBlock: injection.workingMemoryBlock, preferences: injection.preferences } into optimizerBase (GR-3: the hook calls the core builder, never assembles prompt text — the old empty-array literal is gone)."
    - "The hook drops memoryHints when trustPrefs.memory === false (Open Q6 resolution — mirrors the 04b page gate; the optimizer stays pure): memory-disabled → no memory section in the OptimizedContext."
    - "conversationId stays 'default' (Assumption A5); the memory section rides stable:true (A6 — cache-stability preserved; per-turn memory churn invalidating the anthropic cache prefix is the documented F-5 tradeoff)."
  artifacts:
    - "src/core/ai/types.ts (ContextOptimizerInput.workingMemoryBlock?)"
    - "src/core/context/ContextPack.ts (buildMemorySectionText, buildPreferencesSectionText)"
    - "src/core/context/ContextOptimizer.ts (buildPackInput threading + ladder data pass)"
    - "src/core/context/ContextCompressor.ts (reduceMemoryTopK real)"
    - "src/components/pages/useStreamingLLM.ts (assemble wiring + trustPrefs.memory gate)"
    - "tests/core/context/ContextOptimizer.test.ts (extended)"
  key_links:
    - "MemoryInjection (05-04) → hook → optimizer: the exact DTO field names (memories/workingMemoryBlock/preferences) must match the optimizerBase assignments — a rename in one plan breaks tsc here."
    - "The memory section is what the trustPrefs.memory gate controls (T-05-16 mitigation) — the memory-disabled test is the security pin."
    - "ContextPack's stable:true flags are the anthropic cache-prefix contract (F-5) — the threading must not change section kinds or stability."
  flagged_assumptions:
    - "Open Q3 [research, resolved]: assemble per stage with the stage's tier (Pitfall 5) + reduceMemoryTopK real fallback (this plan)."
    - "Open Q6 [research, resolved]: the hook drops memoryHints when trustPrefs.memory === false (keeps the optimizer pure, MemoryEngine store-focused)."
    - "A6 [research, ASSUMED]: memory section stable:true — per-turn churn invalidates the cache prefix only when memory changes; acceptable (F-5 intent preserved)."
  prohibitions:
    - "No prompt text authored in the hook (GR-3) — the section text is built by ContextPack from structured data; PROMPTS constants live in src/core/prompts."
    - "No mid-structure truncation anywhere (D-04-13) — reduce-topk is whole-item drops; no slice/substring on a fact's content."
    - "No change to ContextPack section kinds/stability flags (F-5 cache contract)."
    - "No new storage keys or store access in the hook (single-writer preserved, D-05-02)."
---

<!-- 05-06 (2026-08-13): Wave-4 wiring. The dead ContextPack slots
     (preferencesText/memoryText) become REAL — ContextOptimizer.buildPackInput
     threads them, ContextCompressor.reduceMemoryTopK stops being a no-op, and
     useStreamingLLM calls MemoryEngine.assemble per stage (tier from the
     resolved StageInvocation window) with the trustPrefs.memory gate (Open Q6).
     Open Q3 resolved: per-stage assemble + real fallback. -->

<objective>
Wire real memory into the context pipeline: thread `memoryHints`/`workingMemoryBlock`/`preferences` through `ContextOptimizer.buildPackInput` into the (previously dead) `preferences`/`memory` PromptSection slots (D-05-07/08/09), realize `reduceMemoryTopK` as a whole-item fallback (Pitfall 5), and rewire `useStreamingLLM.ts` to call `MemoryEngine.assemble` per stage with the trustPrefs.memory gate (Open Q6).

Purpose: this is the KNW-05 injection contract — memory ≤1000 tokens / top-5 (top-3 tiny) actually lands in the prompt sections, the working-memory block rides first, preferences inject as compact JSON, and the memory section is gated by the 04b trust envelope (T-05-16 mitigation).

Output: ai/types.ts + ContextPack.ts + ContextOptimizer.ts + ContextCompressor.ts + useStreamingLLM.ts wired; ContextOptimizer.test.ts extended green.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-CONTEXT.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-PATTERNS.md
@src/core/ai/types.ts
@src/core/context/ContextPack.ts
@src/core/context/ContextOptimizer.ts
@src/core/context/ContextCompressor.ts
@src/components/pages/useStreamingLLM.ts
@src/core/memory/MemoryEngine.ts
@src/core/preferences/trustConfig.ts
@tests/core/context/ContextOptimizer.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread memory/preferences through ContextOptimizer (dead slots become real)</name>
  <files>src/core/ai/types.ts, src/core/context/ContextPack.ts, src/core/context/ContextOptimizer.ts, tests/core/context/ContextOptimizer.test.ts</files>
  <read_first>
    - src/core/context/ContextPack.ts L70-95 (the preferences/memory section emission — slots exist; stable:true + sourceId literals) + the ContextPackInput interface (preferencesText?/memoryText? fields)
    - src/core/context/ContextOptimizer.ts L120-136 (buildPackInput — currently DROPS preferences/memory) + L234+ (optimize signature)
    - src/core/ai/types.ts L134-161 (PromptSection kinds + ContextOptimizerInput — memoryHints/preferences fields; the D-04-07 additive-input-extension precedent comment)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Common Operation 3 (L434-440 — the exact threading reference) + Pitfall 5
    - src/core/memory/types.ts (RetrievedMemory/UserPreferences shapes for the helper signatures)
  </read_first>
  <action>
    In src/core/ai/types.ts: add ONE optional field to ContextOptimizerInput — `/** Phase 5 (D-05-09): the always-on working-memory Markdown block, injected BEFORE retrieved facts in the memory section. */ workingMemoryBlock?: string;` — additive only, right after `memoryHints` (L160). No other type change.

    In src/core/context/ContextPack.ts: add two exported pure helpers beside the section assembly:
    - `export function buildMemorySectionText(input: { memoryHints: readonly import('@/core/memory/types').RetrievedMemory[]; workingMemoryBlock?: string }): string | undefined` — `const factLines = input.memoryHints.map((f) => \`- [${f.score.toFixed(2)}] ${f.content}\`)`; `const parts = [input.workingMemoryBlock, ...factLines].filter((p) => p && p.length > 0)`; return parts.length > 0 ? parts.join('\n\n') : undefined (working memory FIRST — D-05-09; never slices a fact).
    - `export function buildPreferencesSectionText(prefs: import('@/core/memory/types').UserPreferences): string` — `JSON.stringify(prefs)` (compact JSON, deterministic key order — D-05-08).
    - Header note: Phase 5 (D-05-07/08/09) — these helpers are shared by buildPackInput and the reduce-topk fallback so the section text can never diverge.

    In src/core/context/ContextOptimizer.ts buildPackInput: after the existing fields, add the Phase-5 threading (D-05-07/08/09):
    - `const preferencesText = input.preferences ? buildPreferencesSectionText(input.preferences) : undefined;`
    - `const memoryText = buildMemorySectionText({ memoryHints: input.memoryHints, workingMemoryBlock: input.workingMemoryBlock });`
    - return them in the pack input object (spread only when non-empty — `...(preferencesText ? { preferencesText } : {})` and `...(memoryText ? { memoryText } : {})` so the no-memory path stays byte-identical to pre-5). Import both helpers from './ContextPack'.
    - Do NOT change the minimalMode path's persona/tool logic; do NOT change ContextPack section kinds or stability flags (F-5).

    In tests/core/context/ContextOptimizer.test.ts (EXTEND — file exists; the optimizerBase fixture at ~L65 has memoryHints: []):
    - New case: optimizerBase + memoryHints [2 facts with fixed scores] + workingMemoryBlock 'WMB' + preferences { responseStyle: 'concise', … } → the OptimizedContext sections include a 'memory' section whose text STARTS with the working-memory block, then the '- [0.87] …' fact lines (order pin), and a 'preferences' section whose text === JSON.stringify(preferences); both stable:true with sourceId 'preferences'/'memory' (unchanged from Phase-4 emission).
    - New case: memoryHints [] + workingMemoryBlock undefined → NO memory section (byte-identical to the existing empty fixture — regression pin).
    - New case: preferences with personaId/personaOverrides set → JSON.stringify includes them (D-05-08 compact-JSON incl. persona overrides).
    - Existing tests all keep passing (the additive input never changes the no-memory path).
  </action>
  <acceptance_criteria>
    - ai/types.ts ContextOptimizerInput contains the literal `workingMemoryBlock?: string;`.
    - ContextPack.ts contains `export function buildMemorySectionText(` and `export function buildPreferencesSectionText(` and the join literal `parts.join('\n\n')`.
    - ContextOptimizer.ts buildPackInput references both helpers (grep `buildMemorySectionText(` and `buildPreferencesSectionText(` in ContextOptimizer.ts prints ≥ 1 each) and ContextPack's section-kind literals ('preferences'/'memory' + stable:true) are UNCHANGED (diff the section-emission block).
    - Extended ContextOptimizer.test.ts exits 0 with `pnpm vitest run tests/core/context/ContextOptimizer.test.ts --bail=1` and the existing suite (`pnpm vitest run tests/core/context --bail=1`) stays green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context/ContextOptimizer.test.ts --bail=1</automated>
  </verify>
  <done>memoryHints/workingMemoryBlock/preferences thread into real stable memory+preferences sections (dead slots closed, D-05-07/08/09); no-memory path byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Realize reduceMemoryTopK — whole-item fallback (Pitfall 5, D-04-13)</name>
  <files>src/core/context/ContextCompressor.ts, src/core/context/ContextOptimizer.ts, tests/core/context/ContextOptimizer.test.ts</files>
  <read_first>
    - src/core/context/ContextCompressor.ts L126-133 (the current NO-OP reduceMemoryTopK + its marker comment) + L145-165 (LADDER_STEPS — 'reduce-topk' is step 6)
    - src/core/context/ContextOptimizer.ts L354-361 (the ladder's reduce-topk case — passes only sections today)
    - src/core/context/ContextPack.ts (buildMemorySectionText — the shared formatter from Task 1)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Pitfall 5 (L364-368: re-build the memory section from input.memoryHints.slice(0,3) — pure, no text slicing)
  </read_first>
  <action>
    In src/core/context/ContextCompressor.ts: change reduceMemoryTopK's signature to `export function reduceMemoryTopK(sections: readonly PromptSection[], memorySource?: { memoryHints: readonly import('@/core/memory/types').RetrievedMemory[]; workingMemoryBlock?: string }): CompressionResult`:
    - When `memorySource` is undefined → keep the current passthrough semantics (return `{ sections: [...sections], compressionApplied: 'topk', dropped: [] }` — standalone callers unchanged).
    - When memorySource IS provided: find the 'memory'-kind section; rebuild its text via buildMemorySectionText({ memoryHints: memorySource.memoryHints.slice(0, 3), workingMemoryBlock: memorySource.workingMemoryBlock }) (top-3 whole-item fallback — D-04-13 no-slice; import from './ContextPack'); if the rebuilt text exists and differs from the current section text → replace the section in place (same kind/sourceId/stable:true, tokens recomputed via estimateTokens) and return dropped: ['memory'] + compressionApplied 'topk'; if no memory section or unchanged → passthrough with dropped: [].
    - Remove/replace the NO-OP marker comment (the function is real now) — keep a doc comment naming Pitfall 5 (the fallback safety net behind MemoryEngine's per-tier budget, 05-04) and the D-04-13 whole-item rule.
    - In src/core/context/ContextOptimizer.ts ladder 'reduce-topk' case: pass the memory source — `reduceMemoryTopK(sections, { memoryHints: input.memoryHints, workingMemoryBlock: input.workingMemoryBlock })`.

    In tests/core/context/ContextOptimizer.test.ts (extend):
    - New case: force the ladder to 'reduce-topk' (drive the compressor directly — a unit case on reduceMemoryTopK with 5 memoryHints + a 6-section pack): the result sections contain a memory section built from EXACTLY the top-3 hints (slice(0,3)) and dropped includes 'memory'; fact content is never a substring (whole items).
    - New case: reduceMemoryTopK(sections) WITHOUT memorySource → passthrough (backward compat, dropped []).
    - Existing ladder tests (drop-debug/drop-secondary/summarise-history/etc.) keep passing.
  </action>
  <acceptance_criteria>
    - ContextCompressor.ts contains the literal `memorySource.memoryHints.slice(0, 3)` (the top-3 fallback) and `buildMemorySectionText(` (shared formatter import) and the function is no longer documented as a no-op (the marker comment is gone — grep the old marker phrase prints 0).
    - ContextOptimizer.ts ladder passes `{ memoryHints: input.memoryHints, workingMemoryBlock: input.workingMemoryBlock }` to reduceMemoryTopK (grep prints ≥ 1).
    - `pnpm vitest run tests/core/context --bail=1` green (whole suite — the reduction must not disturb other ladder steps).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/context --bail=1</automated>
  </verify>
  <done>reduceMemoryTopK is real (top-3 whole-item fallback via the shared formatter); the optimizer ladder passes the memory source; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Hook wiring — MemoryEngine.assemble per stage + trustPrefs.memory gate (GR-3)</name>
  <files>src/components/pages/useStreamingLLM.ts</files>
  <read_first>
    - src/components/pages/useStreamingLLM.ts L150-210 (optimizerBase construction + per-stage optimize calls + StageResolver closures L167-174)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Common Operation 1 (assemble call shape) + Pitfall 5 (per-stage tiers) + Open Q6 (hook drops memoryHints when trustPrefs.memory === false)
    - src/core/context/ModelContextTier.ts (classifyModelContext — derive tier from stageInvocation.modelContextWindow)
    - src/core/memory/MemoryEngine.ts (assemble signature + MemoryInjection — the 05-04 contract)
    - src/core/preferences/trustConfig.ts (TrustPrefs.memory field)
  </read_first>
  <action>
    Modify src/components/pages/useStreamingLLM.ts (GR-3 — data only, no prompt assembly):
    - Build a single `memoryEngine`-bound core caller: `const assembleMemory = async (tier: ModelContextTier) => memoryEngineAssemble({ query: trimmed, conversationId: 'default', tier });` — construct the MemoryEngine with the real store deps (UserMemoryStore/PreferenceMemoryStore/ConversationMemoryStore bound to openMemoryDB/runMigrations + Setting) OR import a singleton `getMemoryEngine()` factory exported from MemoryEngine.ts (05-04) — planner discretion; the hook never constructs stores inline.
    - After the existing `trustPrefs`/`currentPage` reads and BEFORE the two optimize() calls: `const plannerTier = classifyModelContext(plannerInv.modelContextWindow);` and `const rendererTier = classifyModelContext(rendererInv.modelContextWindow);` then `const [plannerInjection, rendererInjection] = await Promise.all([assembleMemory(plannerTier), assembleMemory(rendererTier)]);` (per-stage budgets — Pitfall 5/Open Q3).
    - Compute the persona block ONCE from the planner injection's preferences: `const personaBlock = buildPersonaBlock(resolvePersona(DEFAULT_PERSONA, plannerInjection.preferences));` — the persona pipeline now reads the same UserPreferences the preferences section injects (D-05-18 read path stays compatible).
    - In optimizerBase: `memoryHints: trustPrefs.memory ? plannerInjection.memories : [],` (Open Q6 — the hook drops memoryHints when memory is disabled; mirrors the 04b page gate), `workingMemoryBlock: trustPrefs.memory ? plannerInjection.workingMemoryBlock : undefined,`, `preferences: plannerInjection.preferences,`. The renderer optimize() call receives the same optimizerBase spread (both stages share the shape; the tier budgets differ only in the memories arrays — pass rendererInjection.memories for the renderer call's memoryHints, keep plannerInjection.preferences for both — preferences are stage-independent).
    - `conversationId: 'default'` stays (A5). Remove the old preferences line that came from readPersonaPrefs() — the store read replaces it; keep the `privacyModeFromPrefs(prefs)` invocation working (derive `prefs = plannerInjection.preferences` for the StageResolver closures).
    - Golden Rule 3 comment: the hook calls the core builder (MemoryEngine.assemble) and passes DATA; the optimizer selects/threads sections.

    No new test file (the optimizer-level memory-disabled case in Task 1 covers the section behavior); verify this task by tsc + targeted greps + the existing hook-dependent suites (`pnpm vitest run tests/components/pages --bail=1` — useStreamingLLM tests must not regress; if a hook test constructs optimizerBase with the old readPersonaPrefs import, keep the import only where still needed).
  </action>
  <acceptance_criteria>
    - useStreamingLLM.ts contains the literal `memoryEngineAssemble({` (or `assembleMemory(`) and `classifyModelContext(plannerInv.modelContextWindow)` and `classifyModelContext(rendererInv.modelContextWindow)` and the gate literal `trustPrefs.memory ?`.
    - The file no longer contains the uncommented literal `memoryHints: [],` (grep -v '^\\s*//' | grep -c 'memoryHints: \\[\\],' prints 0 — the old empty-array assignment is gone).
    - The file contains NO prompt-text constants (no `You are` / no memory-section string building — GR-3; grep 'join(\`\\n\\n\`)' in the hook prints 0 unless it is existing non-prompt code).
    - `pnpm exec tsc --noEmit` passes and `pnpm vitest run tests/components/pages --bail=1` stays green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run tests/components/pages --bail=1</automated>
  </verify>
  <done>Hook calls MemoryEngine.assemble per stage (tier from the resolved window), gates memory on trustPrefs.memory (Open Q6), and passes data only (GR-3); tsc + pages suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MemoryInjection DTO → optimizerBase (hook) | assembled memory crosses into the prompt pipeline — trustPrefs.memory gate + GR-3 data-only are the controls |
| memory/preferences sections → model (ContextPack) | the section text crosses the provider boundary — it is model-context data, never policy (GR-7) |
| ladder compression (reduce-topk) | budget-driven section surgery — must never slice facts mid-structure (D-04-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Tampering | prompt injection via retrieved memory into the memory section | high | mitigate | trustPrefs.memory gate at the hook (Open Q6) — memory disabled → memoryHints dropped → no memory section (test-pinned); memory content is user's own redacted store data assembled as DATA (GR-7); the 04b trust envelope's instructionAuthority:false semantics apply to any retrieved content feeding later phases |
| T-05-21 | Tampering | mid-structure truncation by the compression ladder | medium | mitigate | reduceMemoryTopK rebuilds via buildMemorySectionText from whole hints (slice(0,3) is item-level, never substring) — D-04-13 no-slice gate; the shared formatter means the fallback text can never diverge from the pack-time format |
| T-05-22 | Information Disclosure | preferences compact JSON leaking into logs | low | mitigate | preferences ride the normal section path (no logging); debugLog paths in the hook already route through TraceRedactor (R-10); JSON.stringify output never enters debugLog extra |
| T-05-23 | Tampering | cache-prefix drift from memory churn (A6) | low | accept | memory section stays stable:true (F-5 contract) — per-turn churn invalidates the anthropic cache prefix only when memory changes; the documented tradeoff requires an ADR to change |
</threat_model>

<verification>
- `pnpm vitest run tests/core/context --bail=1` green (threading + reduce-topk + existing ladder suite).
- `pnpm exec tsc --noEmit` green (additive input + hook wiring compile).
- `pnpm vitest run tests/components/pages --bail=1` green (hook regression).
- Acceptance greps: workingMemoryBlock field, helper literals, trustPrefs.memory gate, no old empty memoryHints literal.
</verification>

<success_criteria>
- ContextOptimizerInput.workingMemoryBlock? additive (D-04-07 precedent).
- buildPackInput threads real memory + preferences sections (dead slots closed — D-05-07/08/09); no-memory path byte-identical.
- reduceMemoryTopK real top-3 whole-item fallback (Pitfall 5/D-04-13).
- Hook: per-stage assemble (tier from resolved windows), trustPrefs.memory gate (Open Q6), GR-3 data-only, conversationId 'default' (A5).
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-base-memory-minisearch-notes/05-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/core/ai/types.ts` — `ContextOptimizerInput.workingMemoryBlock?: string` (additive optional)
- `src/core/context/ContextPack.ts` — `buildMemorySectionText({ memoryHints, workingMemoryBlock })` (working memory first, '- [score] content' lines), `buildPreferencesSectionText(prefs)` (compact JSON)
- `src/core/context/ContextOptimizer.ts` — `buildPackInput` threads `preferencesText`/`memoryText`; ladder 'reduce-topk' passes the memory source
- `src/core/context/ContextCompressor.ts` — `reduceMemoryTopK(sections, memorySource?)` real top-3 whole-item fallback (shared formatter)
- `src/components/pages/useStreamingLLM.ts` — `assembleMemory(tier)` per-stage MemoryEngine.assemble, `trustPrefs.memory` gate, `workingMemoryBlock` threading, persona block from the store preferences
- `tests/core/context/ContextOptimizer.test.ts` — extended: section threading + order pin, memory-disabled gate, reduce-topk top-3, compact-JSON preferences
