---
phase: 05-knowledge-base-memory-minisearch-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/memory/types.ts
  - src/core/error/errorCodes.ts
  - src/core/storage/Setting.ts
  - src/core/events/EventBus.ts
  - src/types/harness.ts
  - src/core/i18n/strings.ts
  - package.json
  - .planning/PRODUCT_SPEC_v0_1.md
  - tests/core/memory/MemoryTypes.test.ts
autonomous: true
requirements: [KNW-01, KNW-03, KNW-04, KNW-05]
must_haves:
  truths:
    - "src/core/memory/types.ts declares UserMemoryFact (§3.4 verbatim: id/content/type 'fact'|'preference'|'pattern'/tags/confidence/source 'explicit'|'inferred'|'system'/createdAt/updatedAt/lastUsedAt?/useCount), ConversationMemory (§3.3 verbatim incl. lastMessages role 'user'|'assistant'|'tool' + tokens), ConversationMeta (§21.3), and MemoryInjection { memories: RetrievedMemory[]; workingMemoryBlock: string; preferences: UserPreferences } — the R-1 single home; stores import, never re-declare (D-05-01)."
    - "UserPreferencesSchema (zod 3) is exported from src/core/memory/types.ts co-located beside UserPreferences (GR-4 harness.ts L211-251 precedent) — the np_persona write gate PreferenceMemoryStore uses."
    - "The five Phase-5 canonical codes (MEMORY_RETRIEVAL_FAILED, MEMORY_EXTRACT_FAILED, NOTE_LINK_PARSE_FAILED, NOTE_GRAPH_FAILED, SEARCH_INDEX_REBUILD_FAILED) exist in src/core/error/errorCodes.ts (Open Q7 vocabulary) AND are mirrored in the spec Appendix C.2 closed set (W-1 gate precedent); stores reuse STORE_READ/STORE_WRITE, never new idb codes."
    - "np_conversation_meta: { area: 'local' } is registered in Setting.ts STORAGE_KEY_REGISTRY next to np_persona (Pitfall 4 closed — settingRead no longer silently falls back)."
    - "'note:saved' is a member of EVENT_TYPES in src/core/events/EventBus.ts (§20.11 Flow 3 vocabulary) and NOTE_SAVE stays untouched (Pitfall 7 — EventBus.test.ts keeps passing)."
    - "WorkingMemory + WORKING_MEMORY_TEMPLATE (the §3.6 five-line # User Profile block, verbatim) land in src/types/harness.ts at the Appendix C.1 home (R-1, spec L4976-4988) — the O.10 updater imports them from here."
    - "strings.ts notes block gains the 23 new STR.notes.* keys from the 05-UI-SPEC Copywriting Contract table verbatim (Golden Rule 2); existing keys newNote/loading/empty/loadFailed are reused, not re-added."
    - "package.json gains verify:phase-5 = the §24 chain byte-identical to verify:phase-4b (D-05-19, spec line 3685's narrow form is a subset — the test paths must exist by 05-08)."
    - "d3-force@^3 is installed (approved stack, Package Legitimacy Audit OK — minisearch + d3-force both OK verdicts, no [SUS]/[SLOP], no blocking-human gate)."
  artifacts:
    - "src/core/memory/types.ts (UserMemoryFact, ConversationMemory, ConversationMeta, MemoryInjection, UserPreferencesSchema)"
    - "src/core/error/errorCodes.ts (5-member Phase-5 block)"
    - "src/core/storage/Setting.ts (np_conversation_meta registry row)"
    - "src/core/events/EventBus.ts ('note:saved' member)"
    - "src/types/harness.ts (WorkingMemory + WORKING_MEMORY_TEMPLATE)"
    - "src/core/i18n/strings.ts (STR.notes.* 23 new keys)"
    - "package.json (verify:phase-5 script, d3-force dependency)"
    - ".planning/PRODUCT_SPEC_v0_1.md (Appendix C.2 Phase-5 mirror)"
    - "tests/core/memory/MemoryTypes.test.ts"
  key_links:
    - "memory/types.ts is the C.1 home every store plan (05-02/03/04) imports — a second declaration anywhere fails R-1."
    - "np_conversation_meta registration (Setting.ts) is the ConversationMemoryStore LRU persistence contract (05-03) — unregistered, archive/evict silently no-ops (Pitfall 4)."
    - "'note:saved' is what NotesPage (05-07) and NoteGraphView (05-08) subscribe to for index/graph re-derivation (D-05-12/15/17)."
    - "verify:phase-5 is the phase gate every later wave's final acceptance runs against (05-08 gate task)."
  flagged_assumptions:
    - "A1 [research, ASSUMED]: MiniSearch v7 raw scores need [0,1] normalization at the index layer (D-05-11 tool seam) — decided in 05-05, unaffected here."
    - "A4 [research, ASSUMED]: d3-force@^3 resolves to 3.0.0 and its ESM/CJS dual build works under WXT's bundler — the install task verifies the build stays green."
    - "Open Q7 [research, resolved]: Phase-5 code vocabulary is exactly the five codes named above; stores reuse STORE_READ/STORE_WRITE."
  prohibitions:
    - "No new packages beyond d3-force@^3 (R-9 — minisearch/antd/icons/x-markdown already installed)."
    - "No invented type homes: all Phase-5 memory types land in src/core/memory/types.ts; WorkingMemory lands in harness.ts (R-1)."
    - "No EVENT_TYPES member removed or renamed — NOTE_SAVE stays for backward compat (Pitfall 7)."
    - "No STR shape changes beyond the notes object — no new top-level keys (strings.ts as const)."
---

<!-- 05-01 (2026-08-13): Wave-1 foundation. The R-1 type homes (UserMemoryFact/
     ConversationMemory/ConversationMeta/MemoryInjection in memory/types.ts, C.1
     verbatim) + the five canonical Phase-5 codes + the np_conversation_meta
     registry row (Pitfall 4) + 'note:saved' (Pitfall 7) + WorkingMemory in
     harness.ts + the STR.notes.* Copywriting Contract + the verify:phase-5 gate
     script + the d3-force install. Assumption-delta: no-change — this plan adds
     NEW identities only (no existing type renamed/reshaped); the only stored
     key added is np_conversation_meta (new, area local). -->

<objective>
Lay the Wave-1 foundation every Phase-5 module imports: the canonical memory types in `@/core/memory/types` (R-1 home, D-05-01), the five Phase-5 error codes in `errorCodes.ts` (Open Q7) mirrored in the spec Appendix C.2 (W-1), the `np_conversation_meta` storage-registry row (Pitfall 4), the `note:saved` event (Pitfall 7), `WorkingMemory` in `harness.ts` (C.1), the 23 STR.notes.* canonical strings (Golden Rule 2), the `verify:phase-5` gate script (D-05-19), and the `d3-force@^3` install.

Purpose: everything downstream (stores, MemoryEngine, search, notes core, notes UI) imports these types/codes/keys/events — nothing in the phase builds without them; the two verified landmines (unregistered np_conversation_meta → silent no-op, missing 'note:saved' → emit fails) are closed here.

Output: memory/types.ts + errorCodes.ts + Setting.ts + EventBus.ts + harness.ts + strings.ts extended, verify:phase-5 script + d3-force in package.json, spec C.2 mirrored, MemoryTypes.test.ts green.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-CONTEXT.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-PATTERNS.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-UI-SPEC.md
@src/core/memory/types.ts
@src/core/error/errorCodes.ts
@src/core/storage/Setting.ts
@src/core/events/EventBus.ts
@src/types/harness.ts
@src/core/i18n/strings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land the Phase-5 memory types + UserPreferencesSchema in memory/types.ts (R-1)</name>
  <files>src/core/memory/types.ts, tests/core/memory/MemoryTypes.test.ts</files>
  <read_first>
    - src/core/memory/types.ts (the existing C.1 home — RetrievedMemory/UserPreferences already there; header names the R-1 contract)
    - .planning/PRODUCT_SPEC_v0_1.md §3.3 ConversationMemory (L551-560) + §3.4 UserMemoryFact (L578-586) + §21.3 ConversationMeta (L3401-3419) — the VERBATIM sources
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Pattern 3 (MemoryInjection DTO, L273-287) + Open Q8 (ConversationMeta persistence)
    - src/types/harness.ts L211-251 (co-located Zod boundary schema precedent — schema beside the type it validates)
  </read_first>
  <action>
    Append a Phase-5 block to src/core/memory/types.ts AFTER the existing UserPreferences interface — do not disturb the existing RetrievedMemory/UserPreferences shapes (R-1 in-place rule). Add:
    - `export interface UserMemoryFact` verbatim from §3.4: id: string; content: string; type: 'fact' | 'preference' | 'pattern'; tags: string[]; confidence: number; source: 'explicit' | 'inferred' | 'system'; createdAt: number; updatedAt: number; lastUsedAt?: number; useCount: number.
    - `export interface ConversationMemory` verbatim from §3.3: conversationId: string; summary: string; summaryTokens: number; lastMessages: Array<{ role: 'user' | 'assistant' | 'tool'; content: string; tokens: number; timestamp: number }>; updatedAt: number.
    - `export interface ConversationMeta` (§21.3 + §15.1 LRU semantics): conversationId: string; status: 'active' | 'archived'; messageCount: number; lastAccessed: number; updatedAt: number; summary?: string.
    - `export interface MemoryInjection` (RESEARCH Pattern 3 DTO): memories: RetrievedMemory[]; workingMemoryBlock: string; preferences: UserPreferences.
    - Co-located `export const UserPreferencesSchema = z.object({...})` (zod 3 API only) mirroring UserPreferences EXACTLY: responseStyle z.enum(['concise','balanced','detailed']); preferredLanguage z.string(); preferStructuredOutput z.boolean(); allowCloudFallbackFromLocal z.boolean(); defaultProviderId z.string().optional(); toolAutonomy z.enum(['ask_every_time','allow_safe_tools','manual_only']); defaultSurface z.enum(['sidepanel','standalone']); personaId z.string().optional(); personaOverrides z.object({ name: z.string().optional(); tone: z.enum(['professional-warm','concise','friendly']).optional(); brevity: z.enum(['brief','balanced','detailed']).optional() }).optional(). Export `export type UserPreferencesInput = z.infer<typeof UserPreferencesSchema>` only if needed by consumers — the canonical type stays the interface.
    - Section comment naming the spec sources (L551-560/578-586/3401-3419), the phase (KNW-04/D-05-01), and the R-1 note that stores import these, never re-declare.

    In tests/core/memory/MemoryTypes.test.ts (new file, tests/core/memory/ dir does not exist — create it; @vitest-environment node is fine — pure types):
    - UserPreferencesSchema.safeParse accepts a full valid UserPreferences fixture (positive gate).
    - safeParse REJECTS an out-of-union responseStyle ('chatty') and a numeric preferStructuredOutput (negative gates — assert parsed.error.issues.some((i) => i.path.join('.') === ...), ContextProvenanceManifest.test.ts L55-66 precedent).
    - Shape-parity guard: the required keys of UserMemoryFact/ConversationMemory/ConversationMeta/MemoryInjection compile against the interfaces (a fixture object literal assigned to each interface type — tsc enforces).
    - UserPreferencesSchema parses a fixture with personaOverrides set (D-05-08 compact-JSON shape incl. persona overrides) and a fixture without (both pass — optionality).
  </action>
  <acceptance_criteria>
    - src/core/memory/types.ts contains the literals `export interface UserMemoryFact {` and `export interface ConversationMemory {` and `export interface ConversationMeta {` and `export interface MemoryInjection {` and `export const UserPreferencesSchema = z.object({`.
    - The existing `export interface RetrievedMemory {` and `export interface UserPreferences {` declarations are byte-unchanged (grep both and diff against the pre-task content).
    - The file imports zod (`import { z } from 'zod';`) and does NOT import from any other Phase-5 module (types.ts stays dependency-light — only the existing `import type { ProviderId } from '../ai/types'`).
    - MemoryTypes.test.ts exits 0 with `pnpm vitest run tests/core/memory/MemoryTypes.test.ts --bail=1` (vitest 4: `--bail=1`, never `-x`).
    - `pnpm exec tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/memory/MemoryTypes.test.ts --bail=1 && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>UserMemoryFact/ConversationMemory/ConversationMeta/MemoryInjection + UserPreferencesSchema landed in memory/types.ts (C.1, R-1); MemoryTypes.test.ts green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-5 canonical codes + spec C.2 mirror + np_conversation_meta + note:saved event</name>
  <files>src/core/error/errorCodes.ts, .planning/PRODUCT_SPEC_v0_1.md, src/core/storage/Setting.ts, src/core/events/EventBus.ts</files>
  <read_first>
    - src/core/error/errorCodes.ts (the canonical `as const` object — the CONTEXT_TOO_LARGE block L91-96 is the exact template; the object closes with UNKNOWN at L106)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-PATTERNS.md (errorCodes section L394-408 — the exact Phase-5 block literal; EventBus section L410-425; Setting section L375-390)
    - src/core/events/EventBus.ts (EVENT_TYPES L10-23 — NOTE_SAVE at L20 must stay)
    - src/core/storage/Setting.ts (STORAGE_KEY_REGISTRY L60-81 — np_persona at L67 is the precedent row)
  </read_first>
  <action>
    In src/core/error/errorCodes.ts: inside the canonical `as const` object, add a Phase-5 block BEFORE the UNKNOWN member (L106): `MEMORY_RETRIEVAL_FAILED: 'MEMORY_RETRIEVAL_FAILED',` `MEMORY_EXTRACT_FAILED: 'MEMORY_EXTRACT_FAILED',` `NOTE_LINK_PARSE_FAILED: 'NOTE_LINK_PARSE_FAILED',` `NOTE_GRAPH_FAILED: 'NOTE_GRAPH_FAILED',` `SEARCH_INDEX_REBUILD_FAILED: 'SEARCH_INDEX_REBUILD_FAILED',` with a doc comment naming: the phase (Phase 5, KNW-01..05, Open Q7), the W-1 spec-mirror note, and the rule that stores reuse STORE_READ/STORE_WRITE for idb failures. Do NOT touch any existing member.

    In .planning/PRODUCT_SPEC_v0_1.md: mirror the five codes into the Appendix C.2 closed set (find the C.2 error-code listing — prior Phase-4/4b mirrors appended blocks there; add a `--- Phase 5 (canonical additions) ---` block listing the five codes with the same literal strings). This is the W-1 gate precedent (Phase-1/04 did the same mirror); the executor locates the C.2 code block by searching for the Phase-4b/4 mirror comment.

    In src/core/storage/Setting.ts: add `np_conversation_meta: { area: 'local' },` to STORAGE_KEY_REGISTRY directly after the np_persona row (L67) — a NEW registration (Pitfall 4; Runtime State Inventory verified the key is absent today).

    In src/core/events/EventBus.ts: append `'note:saved',` to EVENT_TYPES (after NOTE_SAVE at L20, before THEME_CHANGED). Keep NOTE_SAVE (Phase-1 vocabulary, backward compat — EventBus.test.ts keeps passing). EventBusManager constructs `new EventBus(EVENT_TYPES)` from the same array — no other wiring needed.
  </action>
  <acceptance_criteria>
    - errorCodes.ts contains each of the five literals `MEMORY_RETRIEVAL_FAILED:` `MEMORY_EXTRACT_FAILED:` `NOTE_LINK_PARSE_FAILED:` `NOTE_GRAPH_FAILED:` `SEARCH_INDEX_REBUILD_FAILED:` exactly once as definition lines (grep -c per code prints 1; header comments avoid repeating the literals — comment-text discipline).
    - .planning/PRODUCT_SPEC_v0_1.md Appendix C.2 contains all five code strings (grep the spec for `SEARCH_INDEX_REBUILD_FAILED` prints ≥ 1).
    - Setting.ts STORAGE_KEY_REGISTRY contains the line `np_conversation_meta: { area: 'local' },` and np_persona is unchanged.
    - EventBus.ts EVENT_TYPES contains `'note:saved'` AND `'NOTE_SAVE'` (both literals present).
    - `pnpm exec tsc --noEmit` passes and `pnpm vitest run tests/core/events/EventBus.test.ts --bail=1` passes (existing suite unaffected).
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run tests/core/events/EventBus.test.ts --bail=1</automated>
  </verify>
  <done>Five Phase-5 canonical codes in errorCodes.ts + spec C.2 mirror (W-1); np_conversation_meta registered (Pitfall 4); 'note:saved' in EVENT_TYPES with NOTE_SAVE intact (Pitfall 7).</done>
</task>

<task type="auto">
  <name>Task 3: WorkingMemory in harness.ts + STR.notes.* keys + verify:phase-5 script + d3-force install</name>
  <files>src/types/harness.ts, src/core/i18n/strings.ts, package.json</files>
  <read_first>
    - src/types/harness.ts (the C.1 home — the Phase-4b ContextItem block L18-224 is the in-place template; header L13-16 lists WorkingMemory as a future group)
    - .planning/PRODUCT_SPEC_v0_1.md §3.6 (L614-634 — WorkingMemory interface + WORKING_MEMORY_TEMPLATE verbatim) + Appendix C L4976-4988 (the C.1 home declaration)
    - src/core/i18n/strings.ts notes block L54-78 (existing keys newNote/loading/empty/loadFailed — reused verbatim)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-UI-SPEC.md Copywriting Contract table L126-153 (the canonical verbatim source for all 23 new STR.notes.* keys — Golden Rule 2)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Open Q8 + Package Legitimacy Audit (minisearch + d3-force both OK — no blocking-human gate)
  </read_first>
  <action>
    In src/types/harness.ts: append `export interface WorkingMemory { resourceId: string; markdown: string; tokens: number; updatedAt: number }` and `export const WORKING_MEMORY_TEMPLATE = \`# User Profile\n- **Name**:\n- **Role / Team**:\n- **Environment**:\n- **Preferences**:\n- **Long-term Goals**:\`;` — verbatim from §3.6 (the five-line template, exact field labels). Section comment naming the spec sources (L614-634 + Appendix C L4976-4988) and the R-1 note that the O.10 updater (05-02) imports both from here.

    In src/core/i18n/strings.ts: append to the existing `notes:` block (L54-78) the 23 new keys with the UI-SPEC Copywriting Contract values VERBATIM: newNoteFromPage 'New note from page', searchPlaceholder 'Search notes, tags, or content…', searchEmpty 'No notes match your search.', resultsCount '[n] notes', loadingNote 'Loading note...', saveFailed 'Failed to save note. [Retry]', selectNote 'Select a note or create a new one.', save 'Save Note', unsaved 'Unsaved changes', discard 'Discard unsaved changes?', deleteConfirm 'Delete "[title]"? This cannot be undone.', deleteFailed "Couldn't delete the note. [Retry]", backlinks 'Backlinks', backlinksEmpty 'No backlinks yet.', graphLoading 'Building graph...', graphEmpty 'Create at least 3 notes to see the graph', graphFailed 'Failed to render graph. [Retry]', viewNotes 'Notes', viewGraph 'Graph', createNote 'Create note', addTag 'Add tag', star 'Star', unstar 'Unstar'. Keep the existing keys (newNote/loading/empty/loadFailed + the LLM-Wiki 5a keys already present) untouched. Do NOT add keys outside the notes object.

    In package.json: add `"verify:phase-5": "eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run"` — byte-identical chain shape to verify:phase-4b. Do NOT modify existing verify keys; no test-count assertions (P-5).

    Install: `pnpm add d3-force@^3` (approved stack — AGENTS.md §7; Package Legitimacy Audit verdict OK, no postinstall scripts, no [SUS] — no checkpoint needed). Verify the package.json dependency entry `"d3-force": "^3.0.0"` and that the existing minisearch/antd versions are untouched.
  </action>
  <acceptance_criteria>
    - harness.ts contains `export interface WorkingMemory {` and `export const WORKING_MEMORY_TEMPLATE =` with the template including the literals `# User Profile` and `Long-term Goals`.
    - strings.ts notes block contains all 23 new keys; `node -e` assertion: for each of the 23 keys, `typeof STR.notes[k] === 'string'` and the value deep-equals the UI-SPEC table string (spot-check deleteConfirm, saveFailed, graphEmpty).
    - strings.ts contains NO new top-level STR group (grep the file for `notes:` — still one occurrence).
    - package.json scripts contains `"verify:phase-5": "eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run"` (node -e prints the full chain); verify:phase-1..4b keys byte-unchanged.
    - package.json dependencies contains `"d3-force"` and `node_modules/d3-force/package.json` exists; `pnpm exec tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); const s=p.scripts['verify:phase-5']; if(!s||!s.includes('wxt build')||!s.includes('prettier --check')) process.exit(1); if(!p.dependencies['d3-force']) process.exit(2)" && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>WorkingMemory + template in harness.ts (C.1); 23 STR.notes.* keys verbatim; verify:phase-5 script added (§24 chain, A8/D-05-19); d3-force@^3 installed (approved, audit OK).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local (np_persona / np_conversation_meta) → core read paths | persisted values cross into core — malformed/tampered storage must never crash or silently no-op (Pitfall 4) |
| EventBus 'note:saved' → subscribers | a forged/foreign event must not corrupt note index/graph re-derivation |
| package.json installs | third-party code enters the bundle (R-9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | np_conversation_meta / np_persona storage keys | medium | mitigate | np_conversation_meta now registered (Pitfall 4 closed) so settingRead/Writes are permission-checked via Setting.ts's promise-chain mutex; every store read (05-02/03/04) is try/catch with GR-9 debugLog + fallback defaults (NotesDB convention) — tampered values degrade, never crash |
| T-05-02 | Tampering | EVENT_TYPES 'note:saved' broadcast | medium | mitigate | 'note:saved' is an EventBus-registered literal (constructor validates the list); handlers run in the built-in try/catch + EVT_HANDLER debugLog; subscribers (05-07/08) re-validate the payload noteId before touching NotesDB/index |
| T-05-SC | Tampering | npm install (d3-force) | low | mitigate | Approved stack (AGENTS.md §7); Package Legitimacy Audit verdicts OK for minisearch + d3-force (no [SUS]/[SLOP], no postinstall scripts, legacy check OK) — no blocking-human checkpoint required; verify script gates the install |
| T-05-03 | Information Disclosure | errorCodes/debugLog paths | low | mitigate | debugLog auto-routes through TraceRedactor (R-10); this plan logs nothing user-data-bearing — only module + issue counts at later plans' call sites |
</threat_model>

<verification>
- `pnpm vitest run tests/core/memory/MemoryTypes.test.ts --bail=1` green (Wave-0 file created in this plan).
- `pnpm exec tsc --noEmit` green — type + zod additions compile.
- `pnpm vitest run tests/core/events/EventBus.test.ts --bail=1` green — NOTE_SAVE untouched.
- node -e package.json assertion confirms verify:phase-5 chain + d3-force dependency.
- Acceptance greps: 5 codes in errorCodes.ts + spec C.2, np_conversation_meta row, both EVENT_TYPES literals, 23 STR.notes.* keys.
</verification>

<success_criteria>
- UserMemoryFact/ConversationMemory/ConversationMeta/MemoryInjection verbatim in memory/types.ts with co-located UserPreferencesSchema (GR-4).
- Five Phase-5 canonical codes in errorCodes.ts + spec Appendix C.2 mirror (W-1).
- np_conversation_meta registered area:'local' (Pitfall 4); 'note:saved' in EVENT_TYPES with NOTE_SAVE intact (Pitfall 7).
- WorkingMemory + WORKING_MEMORY_TEMPLATE in harness.ts (C.1).
- 23 STR.notes.* keys verbatim from the UI-SPEC Copywriting Contract.
- verify:phase-5 script present (§24 chain, D-05-19); d3-force@^3 installed (A4).
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-base-memory-minisearch-notes/05-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/core/memory/types.ts` — `UserMemoryFact` (interface, §3.4 verbatim), `ConversationMemory` (§3.3), `ConversationMeta` (§21.3), `MemoryInjection` (DTO), `UserPreferencesSchema` (zod 3, co-located gate)
- `src/core/error/errorCodes.ts` — `ERROR_CODES.MEMORY_RETRIEVAL_FAILED` · `MEMORY_EXTRACT_FAILED` · `NOTE_LINK_PARSE_FAILED` · `NOTE_GRAPH_FAILED` · `SEARCH_INDEX_REBUILD_FAILED` (canonical Phase-5 block)
- `.planning/PRODUCT_SPEC_v0_1.md` — Appendix C.2 Phase-5 mirror (W-1)
- `src/core/storage/Setting.ts` — `STORAGE_KEY_REGISTRY.np_conversation_meta` = { area: 'local' }
- `src/core/events/EventBus.ts` — `EVENT_TYPES` member `'note:saved'` (NOTE_SAVE retained)
- `src/types/harness.ts` — `WorkingMemory` (interface) + `WORKING_MEMORY_TEMPLATE` (five-line §3.6 block)
- `src/core/i18n/strings.ts` — `STR.notes.*` 23 new canonical keys (newNoteFromPage, searchPlaceholder, searchEmpty, resultsCount, loadingNote, saveFailed, selectNote, save, unsaved, discard, deleteConfirm, deleteFailed, backlinks, backlinksEmpty, graphLoading, graphEmpty, graphFailed, viewNotes, viewGraph, createNote, addTag, star, unstar)
- `package.json` — `scripts.verify:phase-5` (§24 chain), `dependencies.d3-force` ^3.0.0
- `tests/core/memory/MemoryTypes.test.ts` (new dir `tests/core/memory/`)
