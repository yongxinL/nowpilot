---
phase: 05-knowledge-base-memory-minisearch-notes
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/workspace/WorkspaceStore.ts
  - src/core/memory/MemoryEngine.ts
  - src/core/memory/ConversationMemoryStore.ts
  - tests/core/workspace/WorkspaceStore.test.ts
  - tests/core/memory/MemoryEngine.test.ts
  - tests/core/memory/ConversationMemoryStore.test.ts
autonomous: true
requirements: [KNW-04, KNW-05]
gap_closure: true

must_haves:
  truths:
    - "Star toggles persist: toggleSelectedNote → np_workspace payload carries selectedNotes → re-init from storage hydrates the star set (CR-01)"
    - "The combined memory section (working-memory block + fact lines + separators) never exceeds 1000 tokens when packed via buildMemorySectionText (WR-01)"
    - "appendTurn never overwrites an existing conversation message: on index-read failure the new turn is written at seq = stored messageCount + 1, and the seq-1 message survives (WR-05)"
    - "getMemoryEngine().assemble reuses one IndexedDB connection across calls — openMemoryDB is invoked exactly once per factory lifetime (WR-06)"
    - "Memory retrieval failures at the MemoryEngine orchestration boundary log the canonical MEMORY_RETRIEVAL_FAILED code (WR-04)"
    - "Notes list empty state renders STR.notes.empty with header CTAs visible (UI-SPEC covered lift)"
    - "Graph view renders STR.notes.graphEmpty below 3 notes and never constructs the simulation (UI-SPEC covered lift)"
  artifacts:
    - "src/core/workspace/WorkspaceStore.ts — 'selectedNotes' in ACTIVE_FIELDS; sanitizeStored accepts an array-of-strings selectedNotes"
    - "src/core/memory/MemoryEngine.ts — assemble budgets facts against the packed-section estimate (WMB + fact lines ≤ MAX_MEMORY_TOKENS); singleton db handle; MEMORY_RETRIEVAL_FAILED emitted in the retrieval catch"
    - "src/core/memory/ConversationMemoryStore.ts — appendTurn seq fallback = (existing?.messageCount ?? 0) + 1 on index-read failure"
    - "tests/core/workspace/WorkspaceStore.test.ts — star persistence round-trip (toggle → storage → re-init → present) + malformed selectedNotes rejected"
    - "tests/core/memory/MemoryEngine.test.ts — combined-section ≤1000 regression + single-openMemoryDB regression"
    - "tests/core/memory/ConversationMemoryStore.test.ts — seq fallback no-overwrite regression"
  key_links:
    - "WorkspaceStore serialization set (ACTIVE_FIELDS/pickActive/sanitizeStored) ↔ np_workspace durability — a field excluded here silently resets on reload"
    - "MemoryEngine.assemble budget ↔ ContextPack.buildMemorySectionText — the packed text is the only honest token measurement of the memory section"
    - "appendTurn seq derivation ↔ MemoryMessage composite key [conversationId, seq] — a reused seq is a silent overwrite (data loss)"
  prohibitions:
    - "appendTurn must never write a seq that already exists for the conversation (composite-key overwrite is data loss)."
    - "The memory-section budget must count the packed text (WMB + fact lines + separators), never per-fact estimates that ignore the working-memory block."
    - "Star-persistence regression must round-trip through real storage (toggle → stored np_workspace payload → re-init hydrate), never assert in-memory membership alone."
    - "The dirty-guard confirmation must never navigate more than once per confirm (no double applySelect)."
---

<objective>
Gap-closure plan 09: the CORE data-correctness blockers from VERIFICATION.md (score 7/11, status gaps_found) and 05-REVIEW.md.

Purpose: Close the two data-loss blockers and the memory-budget violation that live in the core layer — star persistence (CR-01), the combined memory-section token cap (WR-01), the conversation seq-overwrite data-loss mode (WR-05), the IndexedDB connection leak (WR-06), and the dead MEMORY_RETRIEVAL_FAILED canonical code (WR-04). These are failed must-have truths (VERIFICATION truths #7, #9) and KNW-05 is BLOCKED at the budget level until WR-01 lands.

Output: WorkspaceStore persists selectedNotes; MemoryEngine enforces the ≤1000-token cap against the REAL packed section and reuses one DB connection; ConversationMemoryStore cannot overwrite seq-1 on a transient read failure; all three canonical codes used in this plan fire.

Constraint: preserve every byte-unchanged invariant pinned by the passing suites (VERIFICATION.md Required Artifacts table) — the existing WorkspaceStore.test.ts/MemoryEngine.test.ts/ConversationMemoryStore.test.ts cases must stay green. Follow 05-PATTERNS.md Shared Patterns (write paths never throw, GR-9 codes, estimateTokens as the only counter, fake-indexeddb fresh factory per test).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-VERIFICATION.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-REVIEW.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-PATTERNS.md
@src/core/workspace/WorkspaceStore.ts
@src/core/memory/MemoryEngine.ts
@src/core/memory/ConversationMemoryStore.ts
@tests/core/workspace/WorkspaceStore.test.ts
@tests/core/memory/MemoryEngine.test.ts
@tests/core/memory/ConversationMemoryStore.test.ts
</context>

## Artifacts this phase produces

| Symbol / File | Kind | Produced by | Details |
|---|---|---|---|
| `WorkspaceStore.ts` ACTIVE_FIELDS | modify | Task 1 | adds `'selectedNotes'` to the D-18 serialization set |
| `WorkspaceStore.ts` sanitizeStored | modify | Task 1 | accepts an array-of-strings `selectedNotes` in the validated output |
| `tests/core/workspace/WorkspaceStore.test.ts` | extend | Task 1 | CR-01 persistence round-trip + T-1-13 malformed-value rejection |
| `MemoryEngine.ts` assemble budget | modify | Task 2 | facts budgeted against the packed-section estimate via `buildMemorySectionText`; guarded drop loop + last-resort block truncation per §3.6 |
| `MemoryEngine.ts` singleton db handle | modify | Task 2 | module/factory-held `openMemoryDB()` promise reused across assemble calls |
| `MemoryEngine.ts` retrieval catch | modify | Task 2 | logs `ERROR_CODES.MEMORY_RETRIEVAL_FAILED` at the orchestration boundary |
| `tests/core/memory/MemoryEngine.test.ts` | extend | Task 2 | combined-section ≤1000 regression + single-open regression |
| `ConversationMemoryStore.ts` appendTurn | modify | Task 3 | seq fallback = `(existing?.messageCount ?? 0) + 1` on index-read failure |
| `tests/core/memory/ConversationMemoryStore.test.ts` | extend | Task 3 | seq-fallback no-overwrite regression |

<tasks>

<task type="auto">
  <name>Task 1: CR-01 — persist selectedNotes through the np_workspace serialization set</name>
  <files>src/core/workspace/WorkspaceStore.ts, tests/core/workspace/WorkspaceStore.test.ts</files>
  <read_first>
    - src/core/workspace/WorkspaceStore.ts (current ACTIVE_FIELDS L51-58, pickActive L74-82, sanitizeStored L93-112, toggleSelectedNote L356-364)
    - tests/core/workspace/WorkspaceStore.test.ts (existing hydration/LWW/malformed patterns to extend)
  </read_first>
  <action>
    Per D-18/CR-01 (05-REVIEW.md CR-01, VERIFICATION truth #9): `toggleSelectedNote` already routes through the journaled `update()` path, but the star set never reaches storage because `selectedNotes` is absent from the D-18 serialization set.

    1. Add `'selectedNotes'` to the `ACTIVE_FIELDS` array (L51-58) — the D-18 active set. Insert it after `'openedStandaloneTabId'` and before `'version'`. Do NOT add any other field (D-18 T-1-05 — inert fields stay untouched).
    2. Extend `sanitizeStored` (L93-112): when `Array.isArray(v.selectedNotes) && v.selectedNotes.every((id) => typeof id === 'string')` is true, include `selectedNotes` in the validated `out` object; otherwise leave it absent (T-1-13: malformed storage is never merged — the same drop-and-degrade contract as the existing `openedStandaloneTabId` guard). `pickActive` needs no change — it already iterates `ACTIVE_FIELDS` (L74-82), so adding the field to the set is sufficient for both the `write-np-workspace` step and the journal-entry payload.
    3. Update the module-header comment: the durability sentence (L4-6 "serializes ONLY the D-18 active fields (workspaceId / conversationId / activeSurface / openedStandaloneTabId)") must include `selectedNotes` so the header matches the code (acceptance-grep hygiene: the header is not allowed to contradict the field list).
    4. Add the CR-01 regression test to `tests/core/workspace/WorkspaceStore.test.ts`:
       - "star toggles persist through np_workspace (CR-01)": `init()` → `toggleSelectedNote('note-a')` → await `vi.waitFor` until `fakeBrowser.storage.local.get(NP_WORKSPACE_KEY)` returns a payload whose `np_workspace.selectedNotes` deep-equals `['note-a']` → `resetStore()` → `init()` again → `workspace.selectedNotes` contains `'note-a'`. This is the reload simulation — the star must survive a fresh init from storage.
       - "malformed selectedNotes in storage is never merged (T-1-13)": seed `np_workspace` with a non-array `selectedNotes: 'nope'` (and one with `selectedNotes: [42]`) → `init()` → `workspace.selectedNotes` stays `[]`, store hydration still succeeds.
    Do NOT alter `toggleSelectedNote`, `pickActive`, or any other WorkspaceStore surface. Do NOT change the `update()`/journal path — it already works once the field is in the set.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/workspace/WorkspaceStore.test.ts --bail=1</automated>
  </verify>
  <done>
    - Source assertion: `WorkspaceStore.ts` ACTIVE_FIELDS contains the literal `'selectedNotes'`; sanitizeStored's validated output carries `selectedNotes` only when it is an array of strings
    - Behavior: toggle star → await storage write → fresh `init()` → star present in `workspace.selectedNotes`
    - Behavior: malformed `selectedNotes` payloads hydrate to `[]` without throwing
    - The full `tests/core/workspace/WorkspaceStore.test.ts` suite (existing 9 tests + new ones) exits 0
  </done>
  <acceptance_criteria>
    - Code: `WorkspaceStore.ts` ACTIVE_FIELDS = ['workspaceId','conversationId','activeSurface','openedStandaloneTabId','selectedNotes','version','updatedAt']
    - Code: `sanitizeStored` includes `selectedNotes` in `out` only when `Array.isArray(v.selectedNotes) && v.selectedNotes.every((id) => typeof id === 'string')`
    - Behavior: toggle star → re-init store from storage → star present (the CR-01 regression test passes)
    - Behavior: `selectedNotes: 'nope'` / `selectedNotes: [42]` in stored np_workspace → hydration yields `[]`, no throw
    - Test: `pnpm vitest run tests/core/workspace/WorkspaceStore.test.ts --bail=1` exits 0 (all pre-existing tests unchanged and green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WR-01+WR-06+WR-04 — combined ≤1000 memory budget, one DB connection, MEMORY_RETRIEVAL_FAILED emitted</name>
  <files>src/core/memory/MemoryEngine.ts, tests/core/memory/MemoryEngine.test.ts</files>
  <read_first>
    - src/core/memory/MemoryEngine.ts (assemble L150-224, getMemoryEngine L359-384)
    - src/core/context/ContextPack.ts (buildMemorySectionText L56-63 — the packed formatter the budget must measure)
    - tests/core/memory/MemoryEngine.test.ts (existing budget/token-cap/working-memory tests L108-261 to preserve)
  </read_first>
  <action>
    Per WR-01/WR-06 (05-REVIEW.md) + spec §3.4/§3.6 (PRODUCT_SPEC L679: the working-memory block "counts against the memory budget (≤ 1000 tokens total)" and the block is kept while retrieved facts degrade). Three changes in `src/core/memory/MemoryEngine.ts`:

     1. WR-01 — combined-section budget: in `assemble` (L192-210), replace the per-fact `estimateTokens(m.content)` running cap with a cap measured against the REAL packed section. Import `buildMemorySectionText` from `@/core/context/ContextPack` (one-directional import — ContextPack does not import MemoryEngine). For a candidate list, `packedTokens(hints) = estimateTokens(buildMemorySectionText({ memoryHints: hints, workingMemoryBlock }) ?? '')`. The add-loop keeps facts (top-k per tier first, then) while `memories.length === 0 || packedTokens([...memories, m]) <= MAX_MEMORY_TOKENS`; the drop-from-the-end loop pops while `packedTokens(memories) > MAX_MEMORY_TOKENS && memories.length > 0` — RETAIN the current length guard (L206: `&& memories.length > 0`) in the pseudocode so a corrupt oversized block cannot drive the loop past empty and underflow the slice. Then add the LAST-RESORT block truncation per spec §3.6 (L679: "if over budget, truncate the block **before** dropping retrieved facts"): after the drop loop, if `memories.length === 0 && packedTokens([]) > MAX_MEMORY_TOKENS` (facts are gone and the WMB alone still exceeds the cap — a corrupt >1000-token block outside the O.10 ≤300 sanctioned write path), truncate the working-memory block to fit, e.g. step the block down by quarter-char slices (or token-estimate bisection) while `packedTokens([]) > MAX_MEMORY_TOKENS && block.length > 0`, using `estimateTokens` as the only counter (05-PATTERNS Shared Pattern). This makes the ≤1000-token truth hold UNCONDITIONALLY — the block-first order from §3.6 is honored only as the last resort (the O.10-valid ≤300-token block never reaches it, so the normal degradation is still facts-first whole-item drops per D-04-13). Do NOT slice any fact content (D-04-13 whole-item drops preserved).
    2. WR-06 — single DB connection: `getMemoryEngine()`'s surface closure (L376-381) currently calls `openMemoryDB()` per assemble. Hold the open promise in a factory/module-level holder: `let memoryDbPromise: Promise<IDBPDatabase<MemoryDBSchema>> | null = null` (module scope, beside `memoryEngineSurface`), a `getMemoryDb()` helper that opens once and reuses, with `.catch(() => { memoryDbPromise = null; })` chained so a rejection self-heals on the next call; `assemble` in the surface awaits `getMemoryDb()` instead of a fresh `openMemoryDB()`.
    3. WR-04 — emit MEMORY_RETRIEVAL_FAILED: the retrieval catch in `assemble` (L184-190) currently logs `STORE_READ`. Change it to `ERROR_CODES.MEMORY_RETRIEVAL_FAILED` — this is the MemoryEngine orchestration boundary (the idb-level failure is already logged inside `UserMemoryStore.retrieve` with STORE_READ, Q7 preserved; the orchestrator-level semantic gets its own canonical code so the 05-01 Phase-5 block has a live call site). Update the module-header comment (L9-18) to state the retrieval-boundary code. The closed-db test (L391-414) must keep resolving with safe empties — do not change its assertion.

    Regression tests added to `tests/core/memory/MemoryEngine.test.ts`:
    - WR-01 "combined section ≤ MAX_MEMORY_TOKENS with a populated working-memory block": deps with `readWorkingMemory` returning a ~250-300-token block (build one from WORKING_MEMORY_TEMPLATE plus filler) + 5 large facts (~250 tokens each); after `assemble`, assert `estimateTokens(buildMemorySectionText({ memoryHints: result.memories, workingMemoryBlock: result.workingMemoryBlock })) <= MAX_MEMORY_TOKENS` and that whole facts (intact content) were dropped from the end.
    - WR-01 "the block is NOT truncated and facts degrade": with the same fixture assert `result.workingMemoryBlock` is byte-identical to the input block (the O.10-valid block is kept whole; facts were dropped to fit — the normal degradation order is facts-first, block truncation is the last resort only).
    - WR-01 "corrupt oversized block is truncated as the last resort": deps with `readWorkingMemory` returning a >1000-token block (e.g. ~1400 tokens — outside the O.10 ≤300 sanctioned write path) and no facts fitting the cap; after `assemble`, assert `estimateTokens(buildMemorySectionText({ memoryHints: result.memories, workingMemoryBlock: result.workingMemoryBlock })) <= MAX_MEMORY_TOKENS` even when `result.memories.length === 0`, AND that the drop loop never threw/underflowed (the `memories.length > 0` guard held).
    - WR-06 "getMemoryEngine().assemble opens MemoryDB exactly once": `vi.resetModules()`, fresh-import `@/core/storage/MemoryDB` and `@/core/memory/MemoryEngine`, `vi.spyOn(MemoryDB, 'openMemoryDB')` BEFORE the first `getMemoryEngine()` call (the factory is lazy), then call `getMemoryEngine().assemble(...)` twice with distinct queries — assert `openMemoryDB` was called exactly once and both calls resolve with a `MemoryInjection`. The test file already runs in `@vitest-environment node` with a fresh `IDBFactory` per test — the real open works against fake-indexeddb.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/memory/MemoryEngine.test.ts --bail=1</automated>
  </verify>
  <done>
    - Config: `assemble` budgets facts against `estimateTokens(buildMemorySectionText({ memoryHints, workingMemoryBlock }))` — the packed text, never per-fact content alone
    - Config: the drop-from-the-end loop retains the `memories.length > 0` guard; the last-resort block truncation fires only when facts reach 0 and the packed block alone still exceeds the cap
    - Config: `getMemoryEngine()` holds one `openMemoryDB()` promise reused across assemble calls (rejection self-heals)
    - Code: the assemble retrieval catch logs `ERROR_CODES.MEMORY_RETRIEVAL_FAILED`
    - Behavior: packed memory section (WMB + facts) ≤ 1000 tokens unconditionally — O.10-valid block stays byte-identical (facts degrade whole), corrupt oversized block gets truncated as the last resort
    - Behavior: two `getMemoryEngine().assemble()` calls → exactly one `openMemoryDB()` invocation
    - The full `tests/core/memory/MemoryEngine.test.ts` suite (existing 8 test blocks + new) exits 0
  </done>
  <acceptance_criteria>
    - Config: `MemoryEngine.ts` imports `buildMemorySectionText` from `@/core/context/ContextPack` and the add/drop budget loops use the packed-section estimate against `MAX_MEMORY_TOKENS`
    - Config: `getMemoryEngine()`'s assemble resolves the DB via a module-held `memoryDbPromise` (open once, `.catch` resets to null)
    - Code: the assemble retrieval catch passes `ERROR_CODES.MEMORY_RETRIEVAL_FAILED` to `debugLog`
    - Behavior: `estimateTokens(buildMemorySectionText({ memoryHints: result.memories, workingMemoryBlock: result.workingMemoryBlock })) <= 1000` for a WMB + 5-large-facts fixture
    - Behavior: with a populated WMB, `result.workingMemoryBlock` is byte-identical to the input (facts dropped first, never the block — block truncation is the last resort)
    - Behavior: a corrupt >1000-token block with no fitting facts still yields a packed section ≤ 1000 (last-resort truncation), and the drop loop never underflows past empty
    - Test: `vi.resetModules()` + spy regression shows `openMemoryDB` called exactly once across two assemble calls
    - Test: `pnpm vitest run tests/core/memory/MemoryEngine.test.ts --bail=1` exits 0 (pre-existing budget tests unchanged and green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: WR-05 — appendTurn seq fallback cannot overwrite the seq-1 message</name>
  <files>src/core/memory/ConversationMemoryStore.ts, tests/core/memory/ConversationMemoryStore.test.ts</files>
  <read_first>
    - src/core/memory/ConversationMemoryStore.ts (appendTurn L145-183)
    - src/core/storage/MemoryDB.ts (getMessagesForConversation L182-196 — swallows failures into [])
    - tests/core/memory/ConversationMemoryStore.test.ts (appendTurn round-trip block L245-296 to extend)
  </read_first>
  <action>
    Per WR-05 (05-REVIEW.md): on index-read failure `getMessagesForConversation` returns `[]`, so `lastSeq` becomes 0 and the new turn is written at `seq: 1` — the composite key `[conversationId, seq]` (MemoryDB L51-79) then OVERWRITES the existing first message. Data-loss mode on a transient IDB error.

    In `appendTurn` (L154-183): when `messages.length === 0` (read returned nothing — either a genuinely empty conversation OR a swallowed read failure), the seq base must come from the stored meta, never 0 when the conversation already has messages. Change the seq derivation (L157) to:
    - `const lastSeq = messages.length > 0 ? messages[messages.length - 1].seq : 0;`
    - `const seqBase = messages.length > 0 ? lastSeq : (existing?.messageCount ?? 0);`
    - write the new message with `seq: seqBase + 1`.
    The `messageCount` computation (L165-166) already uses `existing?.messageCount` — leave it. Update the header comment (L140-144) to state the seq fallback explicitly: "on index-read failure the seq base falls back to the stored meta.messageCount so a new turn can never overwrite the conversation's existing messages".
    Do NOT change any other store behavior (tier turns, compactor, LRU, never-throw contract).

    Regression test in `tests/core/memory/ConversationMemoryStore.test.ts`:
    - "WR-05: a failed index read must not overwrite the seq-1 message": seed one turn normally (`appendTurn` with content 'hello' → seq 1, meta.messageCount 1) → `vi.spyOn(MemoryDBModule, 'getMessagesForConversation').mockRejectedValueOnce(new Error('idb boom'))` (the store's catch converts the rejection to `[]`, exercising the fallback) → `appendTurn` with content 'second turn' → read the messages back (restore the spy first) → assert BOTH rows exist: seq 1 content 'hello' (unchanged — never overwritten) and seq 2 content 'second turn'; assert meta.messageCount === 2. The import in the test must mirror the source module so the spy replaces the live binding.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/memory/ConversationMemoryStore.test.ts --bail=1</automated>
  </verify>
  <done>
    - Code: `appendTurn` computes the new seq from `(existing?.messageCount ?? 0) + 1` whenever the index read yields zero messages
    - Behavior: seed seq-1 message → index read fails → append → seq-1 message byte-intact + new message at seq 2
    - The full `tests/core/memory/ConversationMemoryStore.test.ts` suite (existing + new) exits 0
  </done>
  <acceptance_criteria>
    - Code: `ConversationMemoryStore.ts` appendTurn writes `seq: (existing?.messageCount ?? 0) + 1` when `messages.length === 0` (no seq reuse)
    - Behavior: after seeding one turn and forcing one `getMessagesForConversation` rejection, the stored rows are `[seq 1 'hello', seq 2 'second turn']` — the first message is never overwritten
    - Test: `pnpm vitest run tests/core/memory/ConversationMemoryStore.test.ts --bail=1` exits 0 (pre-existing appendTurn/LRU/compactor tests unchanged and green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user action → np_workspace storage | Star toggles cross from the React surface into chrome.storage.local via the journaled update path; a serialization-set omission silently drops the payload (data loss) |
| MemoryEngine → MemoryDB | assemble reads working memory + facts across the IndexedDB boundary; a transient read failure must degrade to safe empties, never corrupt or over-budget injection |
| ConversationMemoryStore → MemoryDB messages | appendTurn writes the composite-keyed message row; a reused seq overwrites existing user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09-01 | Tampering | WorkspaceStore ACTIVE_FIELDS/sanitizeStored | high | mitigate | CR-01 Task 1: selectedNotes enters the D-18 serialization set and passes the T-1-13 shape gate (array-of-strings only); the star persistence regression test round-trips through real storage (toggle → stored payload → re-init), proving the star survives reload — acceptance: test exits 0 |
| T-05-09-02 | Information Disclosure | MemoryEngine assemble budget | high | mitigate | WR-01 Task 2: the packed memory section (WMB + facts) is capped at ≤1000 tokens via the real packed-text estimate; the regression test asserts `estimateTokens(buildMemorySectionText(...)) <= 1000` — acceptance: test exits 0; the block is kept whole and facts degrade first (D-04-13), with a last-resort block truncation per §3.6 guaranteeing the ≤1000 truth even for a corrupt oversized block |
| T-05-09-03 | Tampering | ConversationMemoryStore appendTurn seq | critical | mitigate | WR-05 Task 3: seq base falls back to `(existing?.messageCount ?? 0) + 1` on index-read failure; the regression test proves the seq-1 message survives a forced read failure — acceptance: both rows present (seq 1 'hello', seq 2 'second turn') |
| T-05-09-04 | Denial of Service | getMemoryEngine().assemble IDB connections | medium | mitigate | WR-06 Task 2: module-held single `openMemoryDB()` promise reused across assemble calls (rejection self-heals); regression test pins exactly one `openMemoryDB()` invocation across two assembles — acceptance: spy count == 1 |
| T-05-09-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan — the closure touches existing modules only (COVERAGE.md decision); approved-stack packages unchanged |
</threat_model>

<verification>
- `pnpm vitest run tests/core/workspace/WorkspaceStore.test.ts tests/core/memory/MemoryEngine.test.ts tests/core/memory/ConversationMemoryStore.test.ts --bail=1` — all three touched suites green (pre-existing + new regressions)
- `pnpm exec tsc --noEmit` — type-safe (new imports: `buildMemorySectionText`, the module-held db promise)
- Full-suite regression at the phase close: `pnpm run verify:phase-5` (eslint + prettier + tsc + wxt build + vitest run) — run by 05-10's final gate or the phase gate task; do not regress the 102-file/922-test green baseline
- R-3 isolation: no new background-SW/content-script imports introduced (all touched files are Standalone/Side-Panel modules — unchanged bundle surface)
</verification>

<success_criteria>
- WorkspaceStore.test.ts exits 0 with the CR-01 round-trip + malformed-payload tests
- MemoryEngine.test.ts exits 0 with the combined-≤1000 and single-open regression tests
- ConversationMemoryStore.test.ts exits 0 with the seq-fallback no-overwrite regression test
- All three failed VERIFICATION truths addressed by this plan: truth #7 (KNW-05 budget), truth #9 (CR-01 stars); WR-01/05/06 warnings closed
- Flagged assumptions (never silently dropped): (a) an O.10-valid WMB is ≤300 tokens by construction, so the last-resort block truncation never fires on the sanctioned write path — the normal degradation is facts-first whole-item drops; the truncation exists only so the ≤1000-token truth holds unconditionally against a corrupt >1000-token block (facts at 0, block alone over the cap); (b) `vi.waitFor` is required for the CR-01 storage assertion because journaled writes are async (`void journaledUpdateWorkspace`)
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-base-memory-minisearch-notes/05-09-SUMMARY.md` when done
</output>
