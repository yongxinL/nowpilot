---
phase: 05-knowledge-base-memory-minisearch-notes
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/core/memory/ConversationMemoryStore.ts
  - src/core/memory/PreferenceMemoryStore.ts
  - src/core/ai/persona/personaConfig.ts
  - tests/core/memory/ConversationMemoryStore.test.ts
  - tests/core/memory/PreferenceMemoryStore.test.ts
autonomous: true
requirements: [KNW-04, KNW-05]
must_haves:
  truths:
    - "ConversationMemoryStore.ts implements per-conversation rolling summary + recent turns (§3.3, D-05-03): getRecentTurns(conversationId, tier) returns the last 2 turns (tiny) / 4 (small) / 6 (medium|large) as ConversationMemory.lastMessages plus the stored summary; message bodies live ONLY in MemoryDB (MemoryMessage conversationId+seq composite key + by-conversation index — never chrome.storage)."
    - "summariseIfNeeded(conversationId) triggers when messageCount % 12 === 0 (compactor: keep head = system + first 2, middle replaced by a summary, tail = last 4, §15.3) with an injectable summarise seam (opts.summarise?: (middle) => Promise<string>; default = deterministic structural placeholder) so the core stays LLM-free and tests are deterministic — the LLM summarizer stage (conversationSummarizer PROMPTS entry) is a documented 5a seam."
    - "LRU per §15.3: np_conversation_meta (chrome.storage.local via settingWrite, registered 05-01) tracks { status: 'active'|'archived', messageCount, lastAccessed, updatedAt }; >10 active → oldest archived; >100 archived → oldest evicted via WriteJournal persistJournalEntry op 'evict-conversation'; archive after 30 min idle. Meta round-trips through the Setting layer (Pitfall 4 pin)."
    - "PreferenceMemoryStore.ts is the np_persona WRITER (D-05-18, R-7/R2): write(prefs: UserPreferences) goes through settingWrite(NP_PERSONA_KEY) gated by UserPreferencesSchema (GR-4); read() returns the stored UserPreferences — accepting BOTH the Phase-5 UserPreferences shape AND a legacy Phase-3 PersonaProfile shape (converted via resolvePersona(DEFAULT_PERSONA, prefs) → { personaId, personaOverrides }), empty/invalid → BASE defaults, never throws, PERSONA_LOAD_FAILED debugLog on schema failure (personaConfig precedent)."
    - "personaConfig.ts read path stays working with NO Phase-3 pipeline regression (Pitfall 1): loadPersona tries UserPreferencesSchema first then PersonaProfileSchema; readPersona() for a UserPreferences-shaped value derives via resolvePersona(DEFAULT_PERSONA, prefs); readPersonaPrefs() still returns personaOverrides from a stored legacy profile (Phase-3 PersonaInjector behavior byte-identical)."
    - "Conversation meta is metadata in chrome.storage.local; message BODIES + summaries stay in MemoryDB (Open Q8 resolution, §23 ADR metadata-local/bodies-IDB split)."
  artifacts:
    - "src/core/memory/ConversationMemoryStore.ts (appendTurn, getRecentTurns, summariseIfNeeded, archive/evict LRU)"
    - "src/core/memory/PreferenceMemoryStore.ts (write, read)"
    - "src/core/ai/persona/personaConfig.ts (dual-shape loadPersona + derive readPersona)"
    - "tests/core/memory/ConversationMemoryStore.test.ts"
    - "tests/core/memory/PreferenceMemoryStore.test.ts"
  key_links:
    - "np_conversation_meta registration (05-01 Setting.ts) is the LRU persistence contract — the archive/evict round-trip test must go through settingWrite/settingRead, not direct chrome.storage."
    - "PreferenceMemoryStore.read() + personaConfig dual-shape are what MemoryEngine.assemble (05-04) and the hook (05-06) consume for the preferences section — the compact-JSON source (D-05-08)."
    - "The dual-shape shim is the Phase-3 regression guard: PersonaInjector → resolvePersona → readPersona/readPersonaPrefs must keep returning identical output for a stored legacy profile (Pitfall 1 test)."
  flagged_assumptions:
    - "Open Q1 [research, resolved]: np_persona stores the full UserPreferences shape (UserPreferencesSchema-gated); legacy PersonaProfile values are converted on read (RECOMMENDED — single key, spec §3.5 'persists in this store (np_persona)')."
    - "Open Q8 [research, resolved]: ConversationMeta persists in chrome.storage.local via Setting.ts (spec §15.1 + §23 ADR); bodies/summaries in MemoryDB."
    - "M2 rolling-summary (spec §3.3 note) is a refinement of the compactor — the seam accepts a summariser that maintains the rolling observation log; Phase 5 ships the structural default + the seam."
  prohibitions:
    - "No new error codes (Open Q7 rule) — ConversationMemoryStore/PreferenceMemoryStore reuse STORE_READ/STORE_WRITE; persona failures reuse PERSONA_LOAD_FAILED."
    - "No message bodies in chrome.storage.local (§15.1 Pitfall 4)."
    - "No second UserPreferences/ConversationMeta declaration (R-1 — imports from @/core/memory/types)."
    - "No persona-in-fact-store blur: PreferenceMemoryStore owns np_persona; UserMemoryStore (05-02) never stores persona (R-7/R2)."
---

<!-- 05-03 (2026-08-13): Wave-2 memory store plan B. ConversationMemoryStore
     (§3.3 turns + §15.3 LRU/compactor over MemoryDB + np_conversation_meta) +
     PreferenceMemoryStore (np_persona WRITER, D-05-18) + the personaConfig
     dual-shape read shim (Open Q1 resolution — Pitfall 1 guard). Open Q8
     resolved: meta in chrome.storage.local, bodies in MemoryDB. -->

<objective>
Build the conversation + preference memory stores: `ConversationMemoryStore` (per-conversation rolling summary + tiered recent turns over the MemoryDB substrate, §15.3 LRU 10-active/100-archived with the evict-conversation WriteJournal op, 12-message compactor), and `PreferenceMemoryStore` — the `np_persona` WRITER (D-05-18) with the dual-shape read shim in `personaConfig.ts` (Open Q1 resolution) so the Phase-3 persona pipeline cannot regress (Pitfall 1).

Purpose: D-05-02's single-orchestrator contract needs BOTH stores in place before MemoryEngine (05-04) can dispatch; the np_persona writer migration is the R-7/R2 requirement (persona is user config, never a fact) with a hard no-regression guard on the Phase-3 read path.

Output: ConversationMemoryStore.ts + PreferenceMemoryStore.ts + personaConfig.ts dual-shape read; both test files green.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-CONTEXT.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md
@.planning/phases/05-knowledge-base-memory-minisearch-notes/05-PATTERNS.md
@src/core/memory/types.ts
@src/core/storage/MemoryDB.ts
@src/core/storage/Setting.ts
@src/core/storage/WriteJournal.ts
@src/core/ai/persona/personaConfig.ts
@src/core/ai/persona/PersonaProfile.ts
@src/core/ai/persona/PersonaInjector.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConversationMemoryStore — turns + summary + §15.3 LRU + compactor</name>
  <files>src/core/memory/ConversationMemoryStore.ts, tests/core/memory/ConversationMemoryStore.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md §3.3 (L551-572 — ConversationMemory shape + last-N tier rules + 12-message summarise rule) + §15.3 LRU (L1986-1991: 10 active / 100 archived / evict-conversation op / archive after 30 min idle / compactor head+summary+tail)
    - src/core/storage/MemoryDB.ts (messages + conversationSummaries stores — appendTurn/getRecentTurns substrate)
    - src/core/storage/Setting.ts (settingRead/settingWrite L139-204 — np_conversation_meta round-trip; registered 05-01)
    - src/core/storage/WriteJournal.ts (persistJournalEntry L137 — the evict-conversation op home; check the WriteJournalEntry shape)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Pitfall 4 (unregistered key no-op) + Open Q8 (meta target)
  </read_first>
  <action>
    Create src/core/memory/ConversationMemoryStore.ts (NotesDB convention: write paths never throw, try/catch + debugLog STORE_READ/STORE_WRITE with module 'ConversationMemoryStore'):
    - `export const ACTIVE_CONVERSATION_LIMIT = 10;` `export const ARCHIVED_CONVERSATION_LIMIT = 100;` `export const ARCHIVE_IDLE_MS = 30 * 60_000;` `export const COMPACTOR_INTERVAL = 12;`
    - `appendTurn(db, { conversationId, role, content, timestamp }): Promise<void>` — put a MemoryMessage (seq = last seq + 1, derived from a count read or the meta messageCount) into MemoryDB messages + update np_conversation_meta via settingWrite: { conversationId, status: 'active', messageCount: n+1, lastAccessed: timestamp, updatedAt: timestamp, summary } (read-modify-write; missing meta → create). Then call the summarise trigger check.
    - `getRecentTurns(db, conversationId, tier: ModelContextTier): Promise<ConversationMemory>` — read messages via the by-conversation index, keep the LAST 2 (tiny) / 4 (small) / 6 (medium|large) turns, map each to { role, content, tokens: estimateTokens(content), timestamp } (import estimateTokens from '@/core/context/TokenBudget'); attach summary from conversationSummaries (or np_conversation_meta.summary) + summaryTokens = estimateTokens(summary) + updatedAt = max(meta.updatedAt, last message timestamp). Role mapping: 'system' rows are kept only as part of the head in the compactor, never in lastMessages (the §3.3 role union is 'user'|'assistant'|'tool' — filter system rows out of recent turns).
    - `summariseIfNeeded(db, conversationId, opts?: { summarise?: (middle: readonly MemoryMessage[]) => Promise<string> }): Promise<void>` — read meta; when messageCount % 12 === 0: load all messages, split head = [system + first 2] (system row(s) then 2 turns), tail = last 4, middle = the rest; `const summary = opts?.summarise ? await opts.summarise(middle) : \`[${middle.length} messages compacted]\`` (deterministic structural default — the LLM summarizer stage is the 5a seam); persist ConversationMemory summary to conversationSummaries + meta.summary + meta.messageCount unchanged (raw bodies retained in MemoryDB per §3.3 'store message bodies in IndexedDB only').
    - LRU: `archiveIdleConversations(db, nowMs): Promise<void>` — list meta entries, any 'active' with nowMs - lastAccessed > ARCHIVE_IDLE_MS → status 'archived'; `enforceLimits(db): Promise<void>` — >10 active → archive oldest by lastAccessed; >100 archived → evict oldest (status removed + `persistJournalEntry({ op: 'evict-conversation', conversationId, at: nowMs } as WriteJournalEntry)` — fill the entry shape per WriteJournal.ts; never throws).
    - Header comment naming §3.3/§15.3 source lines + Open Q8 (meta local / bodies IDB) + single-writer §13.

    In tests/core/memory/ConversationMemoryStore.test.ts (new file; fake-indexeddb fresh factory per test + fake chrome.storage for Setting — follow the existing Setting/NotesDB test harness; @vitest-environment node):
    - Per-tier turn counts: seed 8 messages → getRecentTurns with 'tiny' returns exactly the last 2, 'small' 4, 'medium' 6, 'large' 6; system rows absent from lastMessages.
    - Compactor: seed 13 messages (messageCount 13 → %12 = 1, not triggered) and 24 (triggered at 24) — assert at 24 the summary is persisted ('[N messages compacted]' structural default with N = middle length) and the head/tail rule (system + first 2 + last 4 retained as raw rows in MemoryDB).
    - Custom summarise seam: opts.summarise returns a fixed string → conversationSummaries.summary === that string (seam works).
    - LRU archive: meta with lastAccessed older than 30 min idle → archived after archiveIdleConversations; >10 active → oldest archived; >100 archived → evict fires a WriteJournal entry with op 'evict-conversation' (assert via loadPendingEntries or a spy on persistJournalEntry).
    - Setting round-trip (Pitfall 4 pin): meta written via settingWrite('np_conversation_meta') is readable back through settingRead with status/messageCount intact (fails if the key is unregistered — the 05-01 registry row is the contract).
    - Write-never-throws: appendTurn on a closed db resolves (no rejection).
  </action>
  <acceptance_criteria>
    - ConversationMemoryStore.ts contains `export const ACTIVE_CONVERSATION_LIMIT = 10` and `COMPACTOR_INTERVAL = 12` and the function literals `export async function getRecentTurns(` and `export async function summariseIfNeeded(` and `export async function enforceLimits(`.
    - The file contains NO `throw` in exported functions and NO direct chrome.storage access (meta via settingRead/settingWrite imports only).
    - The evict path contains the literal `'evict-conversation'`.
    - ConversationMemoryStore.test.ts exits 0 with `pnpm vitest run tests/core/memory/ConversationMemoryStore.test.ts --bail=1` (vitest 4: `--bail=1`).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/memory/ConversationMemoryStore.test.ts --bail=1</automated>
  </verify>
  <done>ConversationMemoryStore ships tiered turns + 12-message compactor (seam) + §15.3 LRU with the evict-conversation WriteJournal op over the Setting-registered np_conversation_meta; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: PreferenceMemoryStore — np_persona writer + dual-shape read (Open Q1)</name>
  <files>src/core/memory/PreferenceMemoryStore.ts, src/core/ai/persona/personaConfig.ts, tests/core/memory/PreferenceMemoryStore.test.ts</files>
  <read_first>
    - src/core/ai/persona/personaConfig.ts (full file — the read path to extend: loadPersona L35-63 + readPersonaPrefs L75-94; NP_PERSONA_KEY L26)
    - src/core/ai/persona/PersonaProfile.ts (PersonaProfileSchema + DEFAULT_PERSONA + identity/languageStyle field names for the legacy conversion)
    - src/core/ai/persona/PersonaInjector.ts (resolvePersona L29-41 — the derivation for UserPreferences-shaped values)
    - src/core/memory/types.ts (UserPreferences + UserPreferencesSchema — 05-01)
    - .planning/phases/05-knowledge-base-memory-minisearch-notes/05-RESEARCH.md Pitfall 1 (L340-344) + Open Q1 (L511-514) + Common Operation 1 (assemble preferences step)
    - src/core/storage/Setting.ts (settingWrite L139-204 — promise-chain mutex, never throws)
  </read_first>
  <action>
    Create src/core/memory/PreferenceMemoryStore.ts (personaConfig structural copy + writer):
    - `export const PREFERENCE_MEMORY_RESOURCE_ID = 'preferences';`
    - `export const DEFAULT_USER_PREFERENCES: UserPreferences` — the BASE_PERSONA_PREFS shape from personaConfig (responseStyle 'balanced', preferredLanguage 'en', preferStructuredOutput true, allowCloudFallbackFromLocal false, toolAutonomy 'allow_safe_tools', defaultSurface 'sidepanel') — exported here (personaConfig re-imports it if needed, or keeps its own — the values must be byte-identical; test pins the equality).
    - `write(prefs: UserPreferences): Promise<void>` — `UserPreferencesSchema.safeParse(prefs)`; on failure debugLog(PERSONA_LOAD_FAILED, 'np_persona write rejected by UserPreferencesSchema', { module: 'PreferenceMemoryStore' }) and return (never throws); on success `settingWrite(NP_PERSONA_KEY, parsed.data)` (R-7 comment: persona is USER CONFIG, never a fact; single key §3.5).
    - `read(): Promise<UserPreferences>` — dual-shape read (Pitfall 1 shim): settingRead(NP_PERSONA_KEY) → undefined → DEFAULT_USER_PREFERENCES; try UserPreferencesSchema.safeParse first → pass through; else try PersonaProfileSchema.safeParse (legacy Phase-3 value) → convert: `resolvePersona(DEFAULT_PERSONA, { ...DEFAULT_USER_PREFERENCES, personaId: legacy.id, personaOverrides: { name: legacy.identity.name, tone: legacy.languageStyle.tone, brevity: legacy.languageStyle.brevity } })` then map the merged profile back to the UserPreferences overrides shape (personaId = merged.id, personaOverrides from merged identity/languageStyle); neither parses → debugLog(PERSONA_LOAD_FAILED, …) → DEFAULT_USER_PREFERENCES. NEVER throws.
    - Header comment: D-05-18 (writer moves here), Open Q1 resolution, R-7/R2 (never a fact), Pitfall 1 guard.

    Modify src/core/ai/persona/personaConfig.ts (dual-shape read, D-05-18):
    - loadPersona: after the `stored === undefined` early return, FIRST try `UserPreferencesSchema.safeParse(stored)` (new shape — a stored UserPreferences passes through: `return { persona: resolvePersona(DEFAULT_PERSONA, parsed.data), loaded: true }`); only when that fails, fall through to the existing PersonaProfileSchema path (legacy, unchanged behavior). Import UserPreferencesSchema + UserPreferences from '@/core/memory/types' (type-only where possible — no cycle: memory/types imports only ai/types ProviderId).
    - readPersonaPrefs: for the UserPreferences-shaped stored value, the overrides derive from the resolved persona (unchanged downstream shape). Keep the exported API byte-identical (readPersona/readPersonaPrefs/loadPersona signatures unchanged).
    - Header comment: add the dual-shape note (Phase 5, D-05-18 — the store is the writer; the accessor stays the read seam with legacy compat).

    In tests/core/memory/PreferenceMemoryStore.test.ts (new file; fake chrome.storage.local harness per Setting tests; @vitest-environment node):
    - write→read round-trip: write a full UserPreferences → read returns it byte-equal (schema gate passed).
    - Legacy compat (Pitfall 1 pin): seed np_persona with a legacy PersonaProfile-shaped value (PersonaProfileSchema-valid fixture) → read() returns UserPreferences with personaId + personaOverrides derived from the legacy id/name/tone/brevity; AND personaConfig.readPersona() returns the same persona as before the migration (no PERSONA_LOAD_FAILED, no DEFAULT_PERSONA reset).
    - Invalid stored value (e.g. a number) → read() returns DEFAULT_USER_PREFERENCES, never throws.
    - Schema-gate write: write({ ...valid, responseStyle: 'chatty' }) resolves WITHOUT persisting (read() still returns the previous value) and logs PERSONA_LOAD_FAILED.
    - Phase-3 pipeline no-regression: readPersonaPrefs() for a legacy-stored profile returns personaOverrides { name, tone, brevity } identical to the pre-migration mapping (byte-identical assertion against the known fixture).
  </action>
  <acceptance_criteria>
    - PreferenceMemoryStore.ts contains `export async function write(` and `export async function read(` and the literal `UserPreferencesSchema.safeParse` and NO `throw` statement.
    - personaConfig.ts contains `UserPreferencesSchema.safeParse(stored)` (new first path) and still contains `PersonaProfileSchema.safeParse(stored)` (legacy fallback) — both literals present.
    - `pnpm exec tsc --noEmit` passes (no import cycle introduced).
    - PreferenceMemoryStore.test.ts exits 0 with `pnpm vitest run tests/core/memory/PreferenceMemoryStore.test.ts --bail=1` AND the existing persona pipeline tests still pass (`pnpm vitest run tests/core/ai/persona --bail=1` — Phase-3 files untouched semantics).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/memory/PreferenceMemoryStore.test.ts tests/core/ai/persona --bail=1 && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>PreferenceMemoryStore writes + dual-shape reads np_persona (Open Q1 resolved); personaConfig keeps the Phase-3 read path working (Pitfall 1 closed); tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local np_conversation_meta → ConversationMemoryStore | persisted LRU metadata crosses into core — unregistered key silently no-ops (Pitfall 4), tampered meta corrupts archive/evict |
| chrome.storage.local np_persona → PreferenceMemoryStore / personaConfig | persisted persona crosses into the persona pipeline — shape drift silently resets the persona (Pitfall 1) |
| MemoryDB messages → getRecentTurns | persisted message bodies cross into retrieval — malformed rows must not crash the injection path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering | np_persona shape drift (PreferenceMemoryStore write vs Phase-3 read) | high | mitigate | Dual-shape read shim: UserPreferencesSchema first, PersonaProfileSchema legacy fallback, resolvePersona derivation — a UserPreferences value can never PERSONA_LOAD_FAILED-reset the persona (Pitfall 1); write gate rejects invalid shapes before persist; regression test pins the Phase-3 pipeline output byte-identical |
| T-05-09 | Tampering | np_conversation_meta LRU metadata | medium | mitigate | Key registered in Setting.ts (05-01, Pitfall 4 closed); reads/writes go through settingRead/settingWrite (permission table + mutex); missing meta → fresh active record; malformed meta → STORE_READ debugLog + treated as absent (archive/evict degrade to no-op, never throw) |
| T-05-10 | Information Disclosure | message bodies / conversation meta logging | medium | mitigate | GR-9 debugLog extra carries only ids/counts (never content); bodies stay in MemoryDB IndexedDB (never chrome.storage — §15.1); TraceRedactor is automatic in debugLog (R-10) |
| T-05-11 | Tampering | MessageDB rows consumed by getRecentTurns | low | accept | content is the user's own conversation; rows are read-only via the by-conversation index; MemoryEngine (05-04) applies budget caps downstream; no trust boundary crossed (conversation memory is not injected as retrieved facts in Phase 5) |
</threat_model>

<verification>
- `pnpm vitest run tests/core/memory/ConversationMemoryStore.test.ts --bail=1` green.
- `pnpm vitest run tests/core/memory/PreferenceMemoryStore.test.ts --bail=1` green + `pnpm vitest run tests/core/ai/persona --bail=1` green (Phase-3 no-regression).
- `pnpm exec tsc --noEmit` green — dual-shape imports compile without cycles.
- Acceptance greps: ACTIVE_CONVERSATION_LIMIT/COMPACTOR_INTERVAL literals, 'evict-conversation' op, both schema.safeParse literals in personaConfig.
</verification>

<success_criteria>
- ConversationMemoryStore: tiered recent turns (2/4/6), 12-message compactor with injectable summarise seam, §15.3 LRU (10/100, 30-min idle archive, evict-conversation WriteJournal op) — D-05-03.
- PreferenceMemoryStore: np_persona writer + dual-shape read (Open Q1), write never persists invalid shapes (GR-4), never throws.
- personaConfig Phase-3 read path unchanged for legacy values (Pitfall 1 closed, D-05-18).
- Meta local / bodies IDB (Open Q8 resolved).
</success_criteria>

<output>
Create `.planning/phases/05-knowledge-base-memory-minisearch-notes/05-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/core/memory/ConversationMemoryStore.ts` — `ACTIVE_CONVERSATION_LIMIT` (10), `ARCHIVED_CONVERSATION_LIMIT` (100), `ARCHIVE_IDLE_MS` (30 min), `COMPACTOR_INTERVAL` (12), `appendTurn`, `getRecentTurns(db, conversationId, tier)`, `summariseIfNeeded(db, conversationId, opts?)`, `archiveIdleConversations(db, nowMs)`, `enforceLimits(db)`
- `src/core/memory/PreferenceMemoryStore.ts` — `PREFERENCE_MEMORY_RESOURCE_ID`, `DEFAULT_USER_PREFERENCES`, `write(prefs)`, `read()`
- `src/core/ai/persona/personaConfig.ts` — `loadPersona` dual-shape path (UserPreferencesSchema → PersonaProfileSchema fallback), `readPersona()` resolvePersona derivation
- `tests/core/memory/ConversationMemoryStore.test.ts`
- `tests/core/memory/PreferenceMemoryStore.test.ts`
