---
phase: 03-cost-effective-ai-runtime
plan: 09
type: execute
wave: 5
depends_on:
  - "03-02"
  - "03-04"
  - "03-06"
  - "03-07"
files_modified:
  - src/core/stores/providerStore.ts
  - src/core/stores/workspaceStore.ts
  - src/entrypoints/background.ts
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - PROV-04
  - PROV-05
  - PROV-06
  - PROV-07
  - AIRN-01
  - AIRN-02
  - AIRN-03
  - AIRN-04
  - AIRN-05
  - AIRN-06
  - AIRN-07
  - AIRN-08
  - AIRN-09

must_haves:
  truths:
    - "providerStore extended with modelEntries, providerPriority, and tierAssignments fields"
    - "providerStore persists new fields through existing EncryptedStorage adapter (key: np_providers)"
    - "workspaceStore.activeProvider field readable by ProviderRouter without modifications"
    - "background.ts contains zero imports from src/core/ai/ (MV3 restriction verified)"
    - "All existing tests continue to pass after store modifications"
  artifacts:
    - path: "src/core/stores/providerStore.ts"
      provides: "Extended with model registry fields consumed by ProviderRegistry"
      modified: true
      must_not_break:
        - "selectedProvider"
        - "apiKeys"
        - "setSelectedProvider"
        - "setApiKey"
        - "EncryptedStorage persistence"
  key_links:
    - from: "src/core/stores/providerStore.ts"
      to: "src/core/ai/providers/ProviderRegistry.ts"
      via: "ProviderRegistry reads provider config (model list, priorities, tier assignments) from providerStore"
      pattern: "providerStore"
    - from: "src/core/stores/workspaceStore.ts"
      to: "src/core/ai/router/ProviderRouter.ts"
      via: "ProviderRouter reads activeProvider from workspaceStore to determine preferred provider chain per D-08"
      pattern: "activeProvider"
</must_haves>

<objective>
Integrate the AI runtime with existing stores: extend providerStore with model registry fields, verify workspaceStore integration points, and enforce the MV3 background SW restriction (no AI runtime imports).

Purpose: The AI runtime needs to read provider configurations and active provider selections from existing Zustand stores. This plan wires the last integration points without breaking existing functionality.
Output: Modified providerStore.ts, verified workspaceStore.ts (unchanged), verified background.ts (no AI imports)
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cost-effective-ai-runtime/03-PATTERNS.md (items 26, 27, 28)
@.planning/phases/03-cost-effective-ai-runtime/03-CONTEXT.md (Code Context: Integration Points lines 94-99)
@.planning/phases/03-cost-effective-ai-runtime/03-RESEARCH.md (Pitfall 4: MV3 Background SW restrictions)
@src/core/stores/providerStore.ts (current implementation)
@src/core/stores/workspaceStore.ts (current implementation)
@src/entrypoints/background.ts (current implementation)
@src/core/ai/providers/providerTypes.ts (ModelEntry type)
@src/core/ai/providers/ProviderRegistry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend providerStore with Model Registry Fields</name>
  <files>src/core/stores/providerStore.ts</files>
  <read_first>
    @src/core/stores/providerStore.ts (full current file — 48 lines)
    @.planning/phases/03-cost-effective-ai-runtime/03-PATTERNS.md (item 26, lines 789-843)
    @src/core/ai/providers/providerTypes.ts (ModelEntry, ProviderConfig types)
  </read_first>
  <action>
    Extend `src/core/stores/providerStore.ts` to add model registry fields. These fields are consumed by ProviderRegistry for model list, provider priority, and tier assignments.

    **Add to `ProviderState` interface:**
    ```typescript
    export interface ProviderState {
      // Existing fields (KEEP UNCHANGED):
      selectedProvider: string | null;
      apiKeys: Record<string, string>;
      setSelectedProvider: (provider: string | null) => void;
      setApiKey: (provider: string, key: string) => void;

      // NEW — Model registry fields (per D-02, D-04, D-08):
      modelEntries: ModelEntry[];
      providerPriority: string[];
      tierAssignments: Record<string, string>; // tier → providerId
      setModelEntries: (entries: ModelEntry[]) => void;
      setProviderPriority: (priority: string[]) => void;
      setTierAssignments: (assignments: Record<string, string>) => void;
    }
    ```

    **Add to `persist` initial state:**
    ```typescript
    modelEntries: [],
    providerPriority: [],
    tierAssignments: {},
    setModelEntries: (modelEntries) => set({ modelEntries }),
    setProviderPriority: (providerPriority) => set({ providerPriority }),
    setTierAssignments: (tierAssignments) => set({ tierAssignments }),
    ```

    **Import additions:**
    ```typescript
    import type { ModelEntry } from '../ai/providers/providerTypes';
    ```

    **Persistence:** All new fields are serialized through the existing `encryptedJSONStorage`. The `np_providers` key is unchanged — new fields are part of the same persisted state object. API keys remain encrypted at rest; model metadata (non-sensitive) is stored alongside.

    **Important constraints:**
    - Do NOT change existing field names or types (`selectedProvider`, `apiKeys`, `setSelectedProvider`, `setApiKey`)
    - Do NOT change the `encryptedJSONStorage` adapter or `name: 'np_providers'`
    - Do NOT add AI SDK imports to the store file (store stays framework-agnostic)
    - New setter functions follow existing pattern: simple `set()` call

    **Test verification:** Run existing provider store tests and ensure they still pass. The new fields have defaults (`[]`, `[]`, `{}`) so existing tests should not break.
  </action>
  <verify>
    <automated>npx vitest run tests/core/workspaceStore.test.ts tests/core/storage/</automated>
  </verify>
  <done>providerStore extended with modelEntries, providerPriority, tierAssignments fields and setters; existing fields unchanged; existing tests pass; EncryptedStorage persistence intact</done>
</task>

<task type="auto">
  <name>Task 2: Verify workspaceStore Integration Points</name>
  <files>src/core/stores/workspaceStore.ts (VERIFY ONLY — no modifications needed)</files>
  <read_first>
    @src/core/stores/workspaceStore.ts
    @.planning/phases/03-cost-effective-ai-runtime/03-CONTEXT.md (Code Context lines 94-99)
  </read_first>
  <action>
    **This is a verification-only task — no code changes to workspaceStore.ts.**

    Verify these integration points are correct for Phase 3 consumption:

    1. **`activeProvider` field** — `workspaceStore.getState().activeProvider` returns the user's selected provider ID (string | null). The ProviderRouter reads this to determine the preferred provider chain per D-08
    2. **`activeSkillRun` field** — exists as nullable string. Available for Phase 7 agent consumption but NOT consumed by Phase 3
    3. **`activeAddonContext` field** — exists as nullable record. Available for Phase 8 content scripts but NOT consumed by Phase 3
    4. **Persistence:** workspaceStore persists to `chrome.storage.local` with key `np_workspace` via WriteJournal — no changes needed
    5. **Cross-surface sync:** BroadcastBus WORKSPACE_UPDATED events propagate workspace changes — Phase 3 services read workspace state but do NOT write to it

    **Verification actions:**
    - Confirm `activeProvider` is typed as `string | null` and accessible via `useWorkspaceStore.getState().activeProvider`
    - Confirm no `src/core/ai/` imports exist in workspaceStore.ts
    - Run existing workspaceStore tests to confirm no regressions
  </action>
  <verify>
    <automated>npx vitest run tests/core/workspaceStore.test.ts</automated>
  </verify>
  <done>workspaceStore verified — activeProvider field correctly typed and accessible; no AI runtime imports; all existing tests pass; no modifications needed</done>
</task>

<task type="auto">
  <name>Task 3: Verify Background Service Worker Has Zero AI Runtime Imports</name>
  <files>src/entrypoints/background.ts (VERIFY ONLY — no modifications unless violations found)</files>
  <read_first>
    @src/entrypoints/background.ts
    @.planning/phases/03-cost-effective-ai-runtime/03-RESEARCH.md (Pitfall 4 lines 420-428)
  </read_first>
  <action>
    **MV3 restriction enforcement (Pitfall 4):** AI runtime code MUST NOT be imported by the background service worker. MV3 SWs are ephemeral (terminate after ~30s inactivity) and cannot support long-lived AI provider connections.

    **Verification steps:**
    1. Read `src/entrypoints/background.ts` — confirm ALL imports
    2. Grep for `src/core/ai/` imports: `grep -r "from.*ai/" src/entrypoints/background.ts` should return ZERO matches
    3. Grep for AI SDK imports: `grep -r "@ai-sdk\|from 'ai'" src/entrypoints/background.ts` should return ZERO matches
    4. If any AI runtime imports are found → REMOVE them (this is a violation)

    **Expected result:** `background.ts` should import only:
    - `wxt/utils/define-background`
    - `../core/utils/debugLog`
    - Stores that don't import AI runtime (workspaceStore, providerStore are fine — they're Zustand stores)

    **If violations found:** Remove the AI runtime imports. If the background SW genuinely needs AI runtime functionality, that's an architectural error — the code must be restructured to run in the sidepanel/standalone context instead.
  </action>
  <verify>
    <automated>bash -c '! grep -q "from.*src/core/ai/\|from.*@ai-sdk\|from.*\"ai\"" src/entrypoints/background.ts && echo "PASS: No AI runtime imports in background.ts" || echo "FAIL: AI runtime imports found in background.ts"'</automated>
  </verify>
  <done>background.ts verified — zero imports from src/core/ai/ or @ai-sdk/* or 'ai'; MV3 restriction enforced; existing background tests pass</done>
</task>

</tasks>

<threat_model>
## Plan-Specific Threats

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-B | Information Disclosure | providerStore model metadata in chrome.storage.local | low | accept | Model entries (non-sensitive metadata) stored alongside encrypted API keys; EncryptedStorage encrypts the entire payload including model metadata; no raw API keys in storage |
</threat_model>

<verification>
- `npx vitest run` all existing tests pass (no regressions)
- providerStore new fields accessible via `useProviderStore.getState().modelEntries`
- background.ts has zero AI runtime imports
- workspaceStore integration verified
</verification>

<success_criteria>
- [ ] providerStore extended with modelEntries, providerPriority, tierAssignments + setters
- [ ] Existing providerState fields unchanged (selectedProvider, apiKeys, etc.)
- [ ] Existing tests pass without modification
- [ ] workspaceStore verified — activeProvider field accessible, no AI imports
- [ ] background.ts has zero imports from src/core/ai/ or AI SDK packages
- [ ] All 196+ existing tests continue to pass (full suite green)
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime/03-09-SUMMARY.md` when done
</output>
