---
phase: 03-cost-effective-ai-runtime
plan: 07
type: execute
wave: 4
depends_on:
  - "03-06"
  - "03-04"
  - "03-05"
files_modified:
  - src/core/ai/pipeline/AgentOrchestrator.ts
autonomous: true
requirements:
  - AIRN-04

must_haves:
  truths:
    - "AgentOrchestrator enforces tier caps: tiny=1, small=2, medium=3, large=5 planner calls max"
    - "AgentOrchestrator loops Planner→Executor until Planner returns 'answer' or tier cap reached"
    - "AgentOrchestrator emits typed events: plan-created, tool-called, tool-result, text-delta, text-complete, error"
    - "AgentOrchestrator creates AbortManager per operation with per-stage child signals"
    - "Tool results from Executor are fed back to Planner in subsequent loop iterations"
    - "After Planner loop ends, Renderer streams final response"
  artifacts:
    - path: "src/core/ai/pipeline/AgentOrchestrator.ts"
      provides: "Pipeline coordinator: Planner→Executor loop with tier caps, unified event stream, abort integration"
      min_lines: 80
      exports:
        - "AgentOrchestrator"
        - "agentOrchestrator"
  key_links:
    - from: "src/core/ai/pipeline/AgentOrchestrator.ts"
      to: "src/core/ai/pipeline/PlannerService.ts"
      via: "Orchestrator calls planner.plan() in loop; receives PlannerDecision → routes to Executor or Renderer"
      pattern: "planner\\.plan"
    - from: "src/core/ai/pipeline/AgentOrchestrator.ts"
      to: "src/core/ai/pipeline/ExecutorService.ts"
      via: "Orchestrator calls executor.execute() when Planner returns run_tool action"
      pattern: "executor\\.execute"
    - from: "src/core/ai/pipeline/AgentOrchestrator.ts"
      to: "src/core/ai/pipeline/RendererService.ts"
      via: "Orchestrator yields renderer events through its own event stream after planner loop ends"
      pattern: "renderer\\.render"
    - from: "src/core/ai/pipeline/AgentOrchestrator.ts"
      to: "src/core/ai/streaming/AbortManager.ts"
      via: "Orchestrator creates AbortManager, passes child signals to each stage per D-17"
      pattern: "abortManager\\.createStageTimeout"
</must_haves>

<objective>
Create the AgentOrchestrator — the pipeline coordinator that loops Planner→Executor with tier caps, then delegates to Renderer, emitting a unified typed event stream per D-06. This is the top-level entry point for all AI operations.

Purpose: The AgentOrchestrator is the single public API for running AI turns. All downstream consumers (Phase 7 Chat UI, Phase 7 Agent page) call `agentOrchestrator.run()` and consume the event stream.
Output: AgentOrchestrator class+singleton with async generator `run()` method
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cost-effective-ai-runtime/03-RESEARCH.md (AgentOrchestrator code example lines 830-896)
@.planning/phases/03-cost-effective-ai-runtime/03-PATTERNS.md (item 12)
@.planning/phases/03-cost-effective-ai-runtime/03-CONTEXT.md (D-05, D-06, D-07, D-17, D-18)
@src/core/ai/pipeline/PlannerService.ts
@src/core/ai/pipeline/ExecutorService.ts
@src/core/ai/pipeline/RendererService.ts
@src/core/ai/pipeline/pipelineTypes.ts
@src/core/ai/router/ProviderRouter.ts
@src/core/ai/streaming/AbortManager.ts
@src/core/ai/streaming/TimeoutConfig.ts
@src/core/ai/config/aiConfig.ts
@src/core/storage/stores/AITransactionLogDB.ts (analog for class+singleton, async patterns)
@src/core/utils/debugLog.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AgentOrchestrator with Planner→Executor→Renderer Loop</name>
  <files>src/core/ai/pipeline/AgentOrchestrator.ts</files>
  <read_first>
    @.planning/phases/03-cost-effective-ai-runtime/03-RESEARCH.md (Orchestrator code example lines 843-896)
    @src/core/ai/pipeline/pipelineTypes.ts (OrchestratorEvent, PlannerDecision, ToolExecutionResult)
    @src/core/ai/config/aiConfig.ts (AI_CONFIG.tierCap)
  </read_first>
  <behavior>
    - Test 1: Planner returns `{ action: 'answer' }` → Orchestrator emits `plan-created`, skips Executor loop, delegates to Renderer, emits `text-delta` + `text-complete`
    - Test 2: Planner returns `{ action: 'run_tool', toolName: 'echo', toolInput: { text: 'hello' } }` → Orchestrator emits `plan-created` → `tool-called` → `tool-result` → feeds result back to Planner for next iteration → eventually answer → Renderer
    - Test 3: Orchestrator enforces tier cap — tiny tier allows max 1 Planner call (loop exits even if Planner still requests tools)
    - Test 4: Tool results from Executor are passed to Planner in subsequent `plan()` calls
    - Test 5: User cancellation (abortManager.cancel()) during Planner → error event emitted, loop stops
    - Test 6: Orchestrator applies per-stage timeouts via `abortManager.createStageTimeout(3000)` for Planner, `createStageTimeout(10000)` for Executor
    - Test 7: Single-model mode per D-07: Planner and Renderer share the same model when only one is configured
  </behavior>
  <action>
    Create `AgentOrchestrator` class+singleton following RESEARCH.md code example lines 843-896.

    **Constructor DI:**
    ```typescript
    constructor(
      private planner: PlannerService,
      private executor: ExecutorService,
      private renderer: RendererService,
      private router: ProviderRouter,
    ) {}
    ```

    **`async *run(userMessage: string, systemPrompt: string, tier: CostTier, preferredProviders: string[]): AsyncGenerator<OrchestratorEvent>`:**

    1. Create `new AbortManager()` for this operation
    2. Get tier cap: `AI_CONFIG.tierCap[tier]` (1/2/3/5)
    3. Track `let plannerCalls = 0`, `const toolResults: ToolExecutionResult[] = []`
    4. **Planner→Executor loop:**
       - While `plannerCalls < tierCap`:
         - Create planner signal: `abortManager.createStageTimeout(DEFAULT_TIMEOUT_CONFIG.planner)` (3s)
         - Call `this.planner.plan(tier, preferredProviders, systemPrompt, this.buildPlannerPrompt(userMessage, toolResults), plannerSignal)`
         - `plannerCalls++`
         - Yield `{ type: 'plan-created', decision: result }`
         - If `result.action === 'answer' || result.action === 'ask_clarification'` → break (proceed to Renderer)
         - If `result.action === 'run_tool' && result.toolName`:
           - Create tool signal: `abortManager.createStageTimeout(DEFAULT_TIMEOUT_CONFIG.executorTool)` (10s)
           - Yield `{ type: 'tool-called', toolName: result.toolName, input: result.toolInput }`
           - Call `this.executor.execute(result.toolName, result.toolInput ?? {}, toolSignal)`
           - Yield `{ type: 'tool-result', toolName: result.toolName, result }`
           - Push result to `toolResults`
    5. **Renderer phase:**
       - Create renderer signal: `abortManager.createStageTimeout(DEFAULT_TIMEOUT_CONFIG.renderer)` (5s)
       - Build messages array: system prompt + user message + tool results summary
       - `for await (const event of this.renderer.render('flash', preferredProviders, systemPrompt, messages, rendererSignal))`:
         - Yield each event (text-delta, text-complete, error)
    6. **Error handling:** Wrap entire loop in try/catch. On error:
       - If `DOMException` with name `'AbortError'` → yield `{ type: 'error', message: 'Operation cancelled' }`
       - Other errors → yield `{ type: 'error', message: err.message }`
       - All errors logged via `debugLog('error', ...)`

    **`private buildPlannerPrompt(userMessage: string, toolResults: ToolExecutionResult[]): string`:**
    - If no tool results → return userMessage
    - If tool results exist → return userMessage + "\n\nPrevious tool results:\n" + JSON.stringify(toolResults)

    **`cancel()` method:** Delegates to `abortManager.cancel('User cancelled')` for external cancellation (UI).

    **Export:** `export const agentOrchestrator = new AgentOrchestrator(plannerService, executorService, rendererService, providerRouter)` singleton.

    **Test file:** `tests/core/ai/pipeline/AgentOrchestrator.test.ts` — mock PlannerService (return controlled decisions), ExecutorService (return controlled results), RendererService (return controlled stream). Test: answer-only flow, tool-then-answer flow, tier cap enforcement, abort propagation, tool result feeding.
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/pipeline/AgentOrchestrator.test.ts</automated>
  </verify>
  <done>AgentOrchestrator created; Planner→Executor loop works with tier caps; tool results fed back to Planner; Renderer called after loop; abort propagation works; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AgentOrchestrator Integration Tests</name>
  <files>tests/core/ai/pipeline/AgentOrchestrator.test.ts (extend with integration cases)</files>
  <read_first>
    @src/core/ai/pipeline/AgentOrchestrator.ts
    @tests/core/ai/pipeline/PlannerService.test.ts
    @tests/core/ai/pipeline/ExecutorService.test.ts
    @tests/core/ai/pipeline/RendererService.test.ts
  </read_first>
  <behavior>
    - Test 1: Multi-turn tool chain — Planner calls toolA → Executor returns result → Planner calls toolB → Executor returns result → Planner answers → Renderer streams
    - Test 2: Orchestrator respects tier cap exactly — small tier (cap=2), Planner returns run_tool twice, third call is NOT made (forced answer or error)
    - Test 3: Abort during tool execution → error event emitted, loop stops, no Renderer call
    - Test 4: Abort during Renderer → text-complete with partial text (if tokens received, per D-18) then error
    - Test 5: Empty preferredProviders → ProviderRouter returns null → Orchestrator emits error event
    - Test 6: Planner returns ask_clarification → loop breaks, no Renderer call, event emitted
    - Test 7: All OrchestratorEvent types emitted in correct order for full pipeline
  </behavior>
  <action>
    Extend the AgentOrchestrator test file with comprehensive integration cases using mocked pipeline services.

    **Mock strategy:**
    - `PlannerService`: Mock `plan()` to return pre-scripted decision sequences using `vi.fn().mockResolvedValueOnce()` for each call
    - `ExecutorService`: Mock `execute()` to return pre-scripted `ToolExecutionResult` values
    - `RendererService`: Mock `render()` to return an async generator yielding pre-scripted events
    - `ProviderRouter`: Mock `selectModel()` to return a valid `{ instance, modelId, providerId }`

    **Test patterns:**
    - Collect all events from `agentOrchestrator.run()` into an array using `for await`
    - Assert event order: `plan-created` → `tool-called` → `tool-result` → ... → `text-delta` → `text-complete`
    - Assert event count matches expectations
    - Test edge cases: empty loops, max iterations, early abort

    **Verify abort timing:** Use `vi.useFakeTimers()` to control stage timeouts. Test that planner stage timeout (3s) fires independently while executor stage still runs.
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/pipeline/AgentOrchestrator.test.ts</automated>
  </verify>
  <done>Integration tests cover multi-tool chains, tier cap enforcement, abort during all stages, empty providers, ask_clarification action; all events verified in correct order</done>
</task>

</tasks>

<threat_model>
## Plan-Specific Threats

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-D | Denial of Service | AgentOrchestrator unbounded loops | medium | mitigate | Tier caps (1-5) enforced per AIRN-04; per-stage timeouts (3s/10s/5s); root AbortController for user cancellation; total operation bound: max 5×3s + 5×10s + 5s = 70s worst case |
| T-03-01-B | Spoofing | Tool result injection into Planner prompt | medium | mitigate | Tool results are JSON-stringified and appended to a clearly demarcated section of the Planner prompt; Planner system prompt instructs the model to treat tool results as authoritative data, not user input |
</threat_model>

<verification>
- `npx vitest run tests/core/ai/pipeline/` all tests green
- AgentOrchestrator full pipeline: answer-only, tool-chain, multi-tool
- Tier cap enforcement: tiny=1, small=2, medium=3, large=5
- Abort during Planner, Executor, Renderer all handled correctly
</verification>

<success_criteria>
- [ ] AgentOrchestrator loops Planner→Executor with tier cap enforcement
- [ ] Tool results fed back to Planner in subsequent iterations
- [ ] Renderer called after Planner loop ends
- [ ] All OrchestratorEvent types emitted in correct order
- [ ] AbortManager created per operation with per-stage child signals
- [ ] User cancellation propagates through all stages
- [ ] Single-model mode: Planner and Renderer share model per D-07
- [ ] Integration tests cover full pipeline scenarios
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime/03-07-SUMMARY.md` when done
</output>
