---
phase: 03-cost-effective-ai-runtime
plan: 03
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - src/core/ai/tools/ToolRegistry.ts
  - src/core/ai/tools/PermissionService.ts
  - src/core/ai/tools/fixtures/echoTool.ts
  - src/core/ai/tools/fixtures/counterTool.ts
  - src/core/ai/tools/fixtures/getTimeTool.ts
autonomous: true
requirements:
  - AIRN-02

must_haves:
  truths:
    - "ToolRegistry registers tools by name and rejects duplicate registrations"
    - "ToolRegistry.get() returns undefined for unknown tool names (closed-enum validation)"
    - "PermissionService.canExecute() returns false for all tools in default implementation (default-deny)"
    - "echoTool echoes its input string unchanged through Zod validation"
    - "counterTool maintains stateful count across multiple invocations"
    - "getTimeTool returns current ISO timestamp"
  artifacts:
    - path: "src/core/ai/tools/ToolRegistry.ts"
      provides: "Map-based tool registry with register/get/has/list, implements closed-enum validation"
      min_lines: 25
      exports:
        - "ToolRegistry"
        - "toolRegistry"
    - path: "src/core/ai/tools/PermissionService.ts"
      provides: "PermissionService interface + DefaultPermissionService default-deny implementation"
      min_lines: 20
      exports:
        - "PermissionService"
        - "DefaultPermissionService"
        - "permissionService"
    - path: "src/core/ai/tools/fixtures/echoTool.ts"
      provides: "Echo fixture tool — safe, no permission required"
      min_lines: 15
      exports:
        - "echoTool"
    - path: "src/core/ai/tools/fixtures/counterTool.ts"
      provides: "Stateful counter fixture tool — safe, no permission required"
      min_lines: 20
      exports:
        - "counterTool"
    - path: "src/core/ai/tools/fixtures/getTimeTool.ts"
      provides: "Time fixture tool — safe, no permission required"
      min_lines: 15
      exports:
        - "getTimeTool"
  key_links:
    - from: "src/core/ai/tools/ToolRegistry.ts"
      to: "src/core/ai/pipeline/ExecutorService.ts"
      via: "ExecutorService calls toolRegistry.get(toolName) for closed-enum validation per D-12"
      pattern: "toolRegistry\\.get"
    - from: "src/core/ai/tools/PermissionService.ts"
      to: "src/core/ai/pipeline/ExecutorService.ts"
      via: "ExecutorService calls permissionService.canExecute() before each tool execution per D-13"
      pattern: "permissionService\\.canExecute"
</must_haves>

<objective>
Create the tool validation framework: ToolRegistry (closed-enum validation), PermissionService (default-deny interface), and 3 test fixture tools (echo, counter, getTime) per D-12 and D-13. No real built-in tools — those arrive in Phase 7.

Purpose: Phase 3 builds the full tool validation framework but uses test-only fixtures to verify the pipeline. The ExecutorService (Plan 03-06) consumes ToolRegistry and PermissionService.
Output: ToolRegistry singleton, PermissionService interface + default implementation, 3 fixture tool definitions
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cost-effective-ai-runtime/03-RESEARCH.md
@.planning/phases/03-cost-effective-ai-runtime/03-PATTERNS.md (items 15, 16, 17, 18)
@.planning/phases/03-cost-effective-ai-runtime/03-CONTEXT.md (D-12, D-13)
@src/core/ai/tools/ToolDefinition.ts
@src/core/commands/keymapRegistry.ts
@src/core/storage/stores/AITransactionLogDB.ts
@src/core/utils/debugLog.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ToolRegistry with Closed-Enum Validation</name>
  <files>src/core/ai/tools/ToolRegistry.ts</files>
  <read_first>
    @src/core/commands/keymapRegistry.ts (exact analog — Map-based registry pattern)
    @src/core/ai/tools/ToolDefinition.ts (ToolDefinition interface)
  </read_first>
  <behavior>
    - Test 1: `toolRegistry.register(echoTool)` adds tool to registry
    - Test 2: `toolRegistry.register(echoTool)` twice throws `Error: Tool "echo" is already registered`
    - Test 3: `toolRegistry.get('echo')` returns the registered ToolDefinition
    - Test 4: `toolRegistry.get('nonexistent')` returns undefined (closed-enum: unknown tools are rejected)
    - Test 5: `toolRegistry.has('echo')` returns true, `toolRegistry.has('fake')` returns false
    - Test 6: `toolRegistry.list()` returns array of all registered ToolDefinition objects
    - Test 7: `toolRegistry.unregister('echo')` removes tool; subsequent `get('echo')` returns undefined
  </behavior>
  <action>
    Create `ToolRegistry` class as an EXACT structural copy of `KeymapRegistry`:

    - `#tools = new Map<string, ToolDefinition>()` using JS private field
    - `register(tool: ToolDefinition): void` — throws if tool.name already registered
    - `unregister(name: string): void` — deletes from map
    - `get(name: string): ToolDefinition | undefined` — lookup (returns undefined for unknown names — this IS the closed-enum validation)
    - `has(name: string): boolean`
    - `list(): ToolDefinition[]` — returns Array.from(this.#tools.values())
    - No `debugLog` calls (caller logs — ExecutorService handles logging)

    **Export pattern:** `export const toolRegistry = new ToolRegistry()` singleton + `export class ToolRegistry` for testing.

    **Test file:** `tests/core/ai/tools/ToolRegistry.test.ts` — import `ToolRegistry` class (not singleton), create fresh instance in `beforeEach`, test all CRUD operations, duplicate registration error.
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/tools/ToolRegistry.test.ts</automated>
  </verify>
  <done>ToolRegistry class exports both class and singleton; register/get/has/list/unregister work; duplicate registration throws; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PermissionService Interface with Default-Deny Implementation</name>
  <files>src/core/ai/tools/PermissionService.ts</files>
  <read_first>
    @.planning/phases/03-cost-effective-ai-runtime/03-CONTEXT.md (D-13 — PermissionService interface with default-deny)
    @src/core/storage/stores/AITransactionLogDB.ts (class+singleton pattern)
  </read_first>
  <behavior>
    - Test 1: `permissionService.canExecute('echo', {})` returns false (default-deny for unknown tools)
    - Test 2: `permissionService.canExecute('some-dangerous-tool', {})` returns false (default-deny)
    - Test 3: A test implementation that extends DefaultPermissionService and overrides canExecute can return true for specific fixtures
  </behavior>
  <action>
    Create `PermissionService` interface + `DefaultPermissionService` class:

    ```typescript
    export interface PermissionService {
      canExecute(toolName: string, toolInput: Record<string, unknown>): Promise<boolean>;
    }

    export class DefaultPermissionService implements PermissionService {
      async canExecute(toolName: string, _toolInput: Record<string, unknown>): Promise<boolean> {
        // Default-deny per D-13: all tools require explicit permission.
        // Phase 3: always returns false. Phase 7: replaced by UI dialog implementation.
        debugLog('info', `[PermissionService] Permission denied for tool: ${toolName}`);
        return false;
      }
    }
    ```

    **Design for Phase 7:** The interface is the contract. Phase 7 creates a `UIPermissionService implements PermissionService` that shows Allow once / Allow always / Deny dialog. ExecutorService calls `permissionService.canExecute()` without knowing which implementation is behind it — no changes needed to ExecutorService.

    **Export:** `export const permissionService = new DefaultPermissionService()` singleton + `export class DefaultPermissionService` + `export type { PermissionService }`.

    **Test file:** `tests/core/ai/tools/PermissionService.test.ts` — test default-deny behavior, test that custom implementations can override.
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/tools/PermissionService.test.ts</automated>
  </verify>
  <done>PermissionService interface and DefaultPermissionService created; default-deny verified; tests pass; interface is extensible for Phase 7 UI-based permission</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 3 Test Fixture Tools (echo, counter, getTime)</name>
  <files>
    src/core/ai/tools/fixtures/echoTool.ts,
    src/core/ai/tools/fixtures/counterTool.ts,
    src/core/ai/tools/fixtures/getTimeTool.ts
  </files>
  <read_first>
    @.planning/phases/03-cost-effective-ai-runtime/03-PATTERNS.md (item 18, fixture tool pattern)
    @src/core/ai/tools/ToolDefinition.ts (ToolDefinition interface)
  </read_first>
  <behavior>
    - Test 1: `echoTool.execute({ text: 'hello' }, { abortSignal })` returns `{ echoed: 'hello' }`
    - Test 2: `echoTool.inputSchema.safeParse({ text: 'hi' })` passes; `echoTool.inputSchema.safeParse({ wrong: 'key' })` fails
    - Test 3: `counterTool.execute({ action: 'increment' }, ...)` returns `{ count: 1 }`; second call returns `{ count: 2 }`; `{ action: 'reset' }` returns `{ count: 0 }`
    - Test 4: `getTimeTool.execute({}, ...)` returns `{ time: string }` in ISO 8601 format
    - Test 5: All 3 fixtures have `category: 'safe'` and `requiresPermission: false`
    - Test 6: echoTool respects abortSignal — when signal is already aborted, execute rejects with AbortError
  </behavior>
  <action>
    Create 3 fixture tool files. Each is a `const` named export implementing `ToolDefinition`. Zod v4 schemas for input/output validation. All are `category: 'safe'`, `requiresPermission: false`. These exercises validation (Zod), timeout (AbortSignal), and permission paths for pipeline verification.

    **echoTool.ts:**
    ```typescript
    import { z } from 'zod';
    import type { ToolDefinition } from '../ToolDefinition';
    export const echoTool: ToolDefinition = {
      name: 'echo',
      description: 'Returns the input text unchanged. Fixture tool for pipeline testing.',
      inputSchema: z.object({ text: z.string() }),
      outputSchema: z.object({ echoed: z.string() }),
      category: 'safe',
      requiresPermission: false,
      execute: async (input, context) => {
        if (context.abortSignal.aborted) throw new DOMException('Aborted', 'AbortError');
        const { text } = input as { text: string };
        return { echoed: text };
      },
    };
    ```

    **counterTool.ts:**
    - Stateful: module-level `let count = 0` (persists across calls within same session — resets on extension restart)
    - `inputSchema: z.object({ action: z.enum(['increment', 'decrement', 'reset']) })`
    - `outputSchema: z.object({ count: z.number() })`
    - `execute` checks `context.abortSignal.aborted`, then updates count

    **getTimeTool.ts:**
    - `inputSchema: z.object({})` — no input
    - `outputSchema: z.object({ time: z.string() })`
    - `execute` checks `context.abortSignal.aborted`, returns `{ time: new Date().toISOString() }`

    **Test file:** `tests/core/ai/tools/fixtures/echoTool.test.ts` (also test counter and getTime) — test execute behavior, Zod schema validation, abort signal handling, output schema validation.
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/tools/fixtures/</automated>
  </verify>
  <done>3 fixture tools created; echo echoes, counter counts and resets, getTime returns ISO timestamp; all Zod schemas validate correctly; abortSignal respected; tests pass</done>
</task>

</tasks>

<threat_model>
## Plan-Specific Threats

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-A | Elevation of Privilege | ToolRegistry closed-enum validation | high | mitigate | get() returns undefined for unknown names → ExecutorService rejects; no wildcard or pattern matching — strict string equality against registered tool names |
| T-03-03-B | Elevation of Privilege | DefaultPermissionService | high | mitigate | Default-deny returns false for ALL tools; Phase 7 replaces with UI-based permission; interface contract ensures ExecutorService requires no changes |
</threat_model>

<verification>
- `npx vitest run tests/core/ai/tools/` all tests green
- `npx tsc --noEmit` passes for all tool files
- echoTool, counterTool, getTimeTool all conform to ToolDefinition interface
</verification>

<success_criteria>
- [ ] ToolRegistry singleton with register/get/has/unregister/list
- [ ] Closed-enum validation: unknown tool names return undefined
- [ ] PermissionService interface + DefaultPermissionService default-deny
- [ ] 3 fixture tools with Zod v4 input/output schemas
- [ ] Counter tool stateful (session-scoped)
- [ ] Echo tool respects AbortSignal
- [ ] All tests pass
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime/03-03-SUMMARY.md` when done
</output>
