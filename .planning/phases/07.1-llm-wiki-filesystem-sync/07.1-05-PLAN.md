---
phase: 07.1-llm-wiki-filesystem-sync
plan: 05
type: execute
wave: 3
depends_on:
  - 07.1-01
  - 07.1-02
  - 07.1-04
files_modified:
  - src/components/notes/NoteEditor.tsx
  - src/components/notes/NoteList.tsx
autonomous: true
requirements:
  - CAT-03
  - CAT-05
  - LLM-WIKI-01
  - LLM-WIKI-03
  - LLM-WIKI-04
  - LLM-WIKI-05
  - LLM-WIKI-08
  - LLM-WIKI-09

must_haves:
  truths:
    - "NoteEditor has category input field between toolbar and split pane with validation per CAT-05 (red border on invalid)"
    - "NoteEditor displays LLM tag suggestions as TagSuggestions component after save, with accept/reject per tag (LLM-WIKI-01, D-13)"
    - "NoteEditor shows summary line with 'Regenerate' button when summary exists (LLM-WIKI-03, LLM-WIKI-04)"
    - "NoteEditor shows staleness Alert when content changed after last LLM analysis (LLM-WIKI-08)"
    - "NoteList supports category tree view toggle (flat list ↔ AntD Tree) via CategoryTree component (CAT-02)"
    - "NoteList displays summary line below each note title as secondary text (LLM-WIKI-03)"
    - "NoteList shows 'Orphan' badge for notes with 0 wikilinks + 0 backlinks, with 'Find context' action (LLM-WIKI-09)"
    - "NoteList has AI search toggle (Switch) that enables LLM reranking when MiniSearch results are low (LLM-WIKI-05)"
  artifacts:
    - "src/components/notes/NoteEditor.tsx (modified)"
    - "src/components/notes/NoteList.tsx (modified)"
  key_links:
    - "NoteEditor imports TagSuggestions component for rendering LLM tag suggestions"
    - "NoteEditor category input validates via normalizeCategoryPath() from LinkParser"
    - "NoteList imports CategoryTree component for tree view mode"
    - "NoteList backlinks prop from parent NotesPage enables orphan detection computation"
---

<objective>
Extend NoteEditor with category input validation, LLM tag suggestion display (via TagSuggestions), summary display with regenerate action, and staleness hints. Extend NoteList with category tree view (via CategoryTree), summary line display, orphan badge with "Find context" action, and AI search toggle for LLM reranking.

Purpose: These are the two most heavily-used notes components. Adding LLM-powered features here creates the user-facing experience for auto-tagging, categorization, and maintenance awareness.

Output: 2 modified components with new props, state, and conditional rendering. Zero breaking changes to existing behavior — all new features are additive and conditional.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-llm-wiki-filesystem-sync/07.1-RESEARCH.md # §4 (NoteEditor), §5 (NoteList)
@.planning/phases/07.1-llm-wiki-filesystem-sync/07.1-PATTERNS.md # §14 (NoteEditor modify), §15 (NoteList modify)
@.planning/LLM-WIKI-SPEC.md # §6.3 — NoteEditor breakdown (lines 205-230), NoteList breakdown (lines 232-254)
@src/components/notes/NoteEditor.tsx
@src/components/notes/NoteList.tsx
@src/components/notes/TagSuggestions.tsx
@src/components/notes/CategoryTree.tsx
@src/core/notes/LinkParser.ts # Note interface, normalizeCategoryPath
@src/core/notes/NoteTagger.ts # NoteTagger.analyze() for regenerate
@src/core/notes/noteTypes.ts # LlmFeatureToggles
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend NoteEditor with category input, tag suggestions, summary, and staleness hint</name>
  <files>src/components/notes/NoteEditor.tsx</files>
  <read_first>@src/components/notes/NoteEditor.tsx, @.planning/LLM-WIKI-SPEC.md # §6.3 (lines 205-230)</read_first>
  <action>
    **Step A — Add new props to NoteEditorProps (extend lines 63-68):**
    ```typescript
    // NEW optional props (add to existing interface):
    onRegenerateTags?: () => void;
    llmFeatures?: LlmFeatureToggles;
    suggestedTags?: string[];
    suggestedCategory?: string;
    suggestedSummary?: string;
    suggestedTagsLoading?: boolean;
    onAcceptTag?: (tag: string) => void;
    onRejectTag?: (tag: string) => void;
    acceptedTags?: Set<string>;
    categoryPath?: string;
    onCategoryChange?: (path: string) => void;
    categoryError?: string | null;
    isContentStale?: boolean;
    ```

    **Step B — Add new state (after existing line 72):**
    - `categoryInput: string` — controlled category path input, initialized from `note?.categoryPath || suggestedCategory || ''`
    - `categoryValidationError: string | null` — validation error for red border

    **Step C — Insert category input between toolbar and split pane (after line 185):**
    Follow the toolbar Input pattern (lines 160-166) — variant: borderless, full width.
    - `Input` component with placeholder `"Category path (e.g. InfoTech/Database/MySQL)"`
    - On blur: validate with `normalizeCategoryPath(value)` from LinkParser — if returns `{ error }`, set `categoryValidationError` and show red border. If valid, normalize the value and call `onCategoryChange(normalized)`
    - On change: clear validation error. Sync back to parent.
    - Style: `marginBottom: 8`, label "Category:" via `Typography.Text` (type: secondary, size: small)

    **Step D — Insert tag suggestions after category input (LLM-WIKI-01, D-13):**
    - Import `TagSuggestions` from `./TagSuggestions`
    - Render conditionally: `{suggestedTags && suggestedTags.length > 0 && (<TagSuggestions suggestedTags={suggestedTags} acceptedTags={acceptedTags || new Set()} onAccept={onAcceptTag!} onReject={onRejectTag!} loading={suggestedTagsLoading} />)}`
    - This section appears after the save pipeline invokes NoteTagger and returns suggestions. Parent (NotesPage) passes them down.

    **Step E — Insert summary display after tag suggestions (LLM-WIKI-03, LLM-WIKI-04):**
    - Conditional: `{note?.summary && (<div style={{ marginBottom: 8 }}><Typography.Text type="secondary">{note.summary}</Typography.Text> <Button type="link" size="small" onClick={onRegenerateTags}>Regenerate</Button></div>)}`
    - The "Regenerate" button calls `onRegenerateTags` which triggers NoteTagger.analyze() for the current note.

    **Step F — Insert staleness hint below preview (LLM-WIKI-08):**
    - Conditional: `{isContentStale && (<Alert type="warning" message="Content has changed" description={<Button type="link" size="small" onClick={onRegenerateTags}>Regenerate tags/summary</Button>} style={{ marginTop: 8 }} banner />)}`
    - `isContentStale` is computed by parent (NotesPage): `note.updated > (note.tagsGeneratedAt || 0) || note.updated > (note.summaryGeneratedAt || 0)`

    **Step G — Keep existing behavior intact:**
    - All existing props and state remain. New props are optional with defaults. No breaking changes to existing callers.
    - The `handleSave` callback (lines 117-121) is unchanged — parent handles the save pipeline extension (NotesPage in Plan 06).

    **Step H — Verify:**
    - Existing NoteEditor tests continue to pass (no breaking changes)
    - New conditional sections render only when corresponding props are provided
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/notes/NoteEditor.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>NoteEditor has category input with blur validation (CAT-05 normalization, red border on invalid); TagSuggestions component integration for LLM tag suggestions (D-13); summary display with Regenerate button (LLM-WIKI-03/04); staleness Alert when content changed (LLM-WIKI-08); all existing behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Extend NoteList with category tree, summary line, orphan badge, and AI search toggle</name>
  <files>src/components/notes/NoteList.tsx</files>
  <read_first>@src/components/notes/NoteList.tsx, @.planning/LLM-WIKI-SPEC.md # §6.3 (lines 232-254)</read_first>
  <action>
    **Step A — Add new props to NoteListProps (extend lines 8-15):**
    ```typescript
    // NEW optional props:
    backlinks?: Map<string, BacklinkEntry[]>;
    viewMode?: 'flat' | 'tree';
    onToggleViewMode?: () => void;
    aiSearchEnabled?: boolean;
    onToggleAiSearch?: () => void;
    allNotes?: Note[];  // needed for CategoryTree
    ```

    **Step B — Add view mode toggle UI (in header area, above search input):**
    - Add a small button group: `[List] [Tree]` or a toggle `Button` that switches `viewMode`.
    - When `viewMode === 'tree'`: replace the flat list (lines 100-150) with `<CategoryTree notes={allNotes || notes} selectedNoteId={selectedNoteId} onSelect={onSelect} />`
    - When `viewMode === 'flat'`: keep existing flat list behavior. Add enhancements below to flat list items.
    - Import `CategoryTree` from `./CategoryTree`

    **Step C — Add summary line to list items (LLM-WIKI-03):**
    - In the flat list item rendering (lines 100-149), after the title text (line ~122), add:
    - `{item.summary && (<Typography.Text type="secondary" ellipsis style={{ fontSize: 12 }}>{item.summary}</Typography.Text>)}`
    - Style: `display: 'block'`, `marginTop: 2`, `fontSize: 12`. Shortens long summaries with ellipsis.

    **Step D — Add orphan badge (LLM-WIKI-09):**
    - Compute orphan status: note has 0 outgoing wikilinks (no `[[...]]` in content) AND 0 incoming backlinks (`backlinks?.get(note.id)` is empty/undefined)
    - For orphan notes, render `<Tag color="default" style={{ marginLeft: 8, fontSize: 10 }}>Orphan</Tag>` after the title
    - On click, show a dropdown or popover with "Find context" action that triggers RAG Q&A via callback to parent. Add new prop: `onFindContext?: (noteId: string) => void`

    **Step E — Add AI search toggle (LLM-WIKI-05):**
    - Beside the search `Input` (line 72), add a small `<Switch checked={aiSearchEnabled} onChange={onToggleAiSearch} size="small" />` with label "AI" via `Typography.Text` (type: secondary, size: small)
    - When `aiSearchEnabled` is true, the search behavior changes: empty-search results and low-result searches (< 3 results) route through LLM reranking. This routing logic is implemented in the parent (NotesPage), not in NoteList itself. NoteList only provides the toggle UI and exposes `aiSearchEnabled` state upward.

    **Step F — Keep existing behavior intact:**
    - All existing props remain required. New props are optional with defaults: `viewMode` defaults to `'flat'`, `aiSearchEnabled` defaults to `false`, `backlinks` defaults to empty map.
    - The existing search, sort, and delete behaviors are unchanged.

    **Step G — Verify:**
    - Existing NoteList tests continue to pass
    - Tree view renders CategoryTree when viewMode='tree'
    - Orphan badge appears only for orphan notes
    - AI toggle renders and calls onToggleAiSearch
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/notes/NoteList.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>NoteList supports viewMode toggle (flat ↔ tree via CategoryTree); summary line renders below titles; orphan badge on notes with 0 wikilinks + 0 backlinks with 'Find context' action; AI search Switch beside search input; all existing behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → NoteEditor category input | Category path is user-editable field — validated on blur per CAT-05 normalization rules |
| User → TagSuggestions | User accepts/rejects LLM suggestions — gatekeeper per D-13 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7.1-18 | Tampering | NoteEditor category input — path traversal | medium | mitigate | `normalizeCategoryPath()` called on blur — strips `.` and `..`, removes leading/trailing slashes, collapses duplicates, validates segments. Red border on invalid input prevents save with invalid path. Path is validated at the service layer (NoteFileSync) before any filesystem operation. |
| T-7.1-19 | Spoofing | NoteList orphan badge | low | accept | Orphan detection is algorithmic — computed from backlinks map and wikilink presence in content. No LLM involved. False positives are possible (notes that genuinely have no links) — this is a feature, not a bug. Badge is informational only. |
</threat_model>

<verification>
1. `pnpm vitest run tests/components/notes/NoteEditor.test.tsx` — existing tests pass + new features render correctly
2. `pnpm vitest run tests/components/notes/NoteList.test.tsx` — existing tests pass + new features render correctly
3. `pnpm exec tsc --noEmit` — zero type errors
4. Full test suite: `pnpm vitest run` — no regressions
</verification>

<success_criteria>
- [ ] NoteEditor category input validates on blur, shows red border for invalid paths (CAT-05), calls normalizeCategoryPath
- [ ] NoteEditor renders TagSuggestions when suggestedTags prop is provided (LLM-WIKI-01)
- [ ] NoteEditor shows summary + Regenerate button when summary exists (LLM-WIKI-03/04)
- [ ] NoteEditor shows staleness Alert when isContentStale is true (LLM-WIKI-08)
- [ ] NoteList toggles between flat list and CategoryTree view modes (CAT-02)
- [ ] NoteList renders summary line below each title (LLM-WIKI-03)
- [ ] NoteList renders "Orphan" badge for notes with 0 wikilinks + 0 backlinks (LLM-WIKI-09)
- [ ] NoteList renders AI search Switch beside search input (LLM-WIKI-05)
- [ ] No breaking changes — all new props are optional, existing behavior preserved
</success_criteria>

<output>
Create `.planning/phases/07.1-llm-wiki-filesystem-sync/07.1-05-SUMMARY.md` when done
</output>
