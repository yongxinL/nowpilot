---
phase: 07.1-llm-wiki-filesystem-sync
plan: 06
subsystem: integration
tags: [notes-page, chat-to-note, save-pipeline, rag-qa]
key-files:
  modified:
    - src/core/pages/NotesPage.tsx
    - src/components/notes/SaveToNoteDialog.tsx
    - src/components/chat/ChatMessage.tsx
metrics:
  duration: ~8 min
  tasks: 3 of 3
---

# Plan 07.1-06: NotesPage Integration Summary

**Objective:** Wire NotesPage with Q&A, sync, tagging; extend SaveToNoteDialog with LLM drafts; add Save to note to ChatMessage.

## Commit

`f0c4d84` — All 3 files modified

## Changes Made

### NotesPage.tsx
- AskNotesInput integrated above toolbar for RAG Q&A (LLM-WIKI-06)
- Save pipeline extended: filesystem sync (fire-and-forget per D-09) + NoteTagger.analyze() if LLM features enabled
- Sync status Tag (green/red/gray) in toolbar (SYNC-02, SYNC-08)
- Category view toggle (flat ↔ tree) propagated to NoteList
- LLM features loaded from chrome.storage.local on mount
- Tag suggestions, summary, category, staleness passed to NoteEditor

### SaveToNoteDialog.tsx
- New props: conversationMessages, memoryContext, existingNoteTitles
- On mount: calls NoteChatConverter.convert() with conversation + memory context per MEM-03
- Pre-fills title, content, tags from LLM draft (D-13)
- Shows Spin loading during LLM drafting
- NotePreview for rendered markdown preview
- Suggested tags displayed as non-interactive Tag components

### ChatMessage.tsx
- Added onSaveToNote callback prop
- "Save to note" Button in Bubble footer for non-streaming assistant messages only

## Deviations from Plan

- Memory facts routing (MEM-02) deferred to comment placeholder — actual memory fact upsert requires full MemoryEngine.extract() pipeline

## Self-Check: PASSED

- [x] AskNotesInput integrated calling NoteQA.ask()
- [x] Save pipeline fires sync + tagger after DB write
- [x] Sync status Tag in toolbar
- [x] Category view toggle propagated
- [x] LLM features loaded from storage
- [x] SaveToNoteDialog pre-fills from NoteChatConverter
- [x] SaveToNoteDialog shows loading + NotePreview
- [x] ChatMessage shows Save to note for assistant
- [x] No type errors
