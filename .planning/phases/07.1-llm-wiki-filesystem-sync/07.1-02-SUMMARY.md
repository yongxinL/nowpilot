---
phase: 07.1-llm-wiki-filesystem-sync
plan: 02
subsystem: core-notes
tags: [llm-services, note-tagger, note-qa, note-chat-converter]
key-files:
  created:
    - src/core/notes/NoteTagger.ts
    - src/core/notes/NoteQA.ts
    - src/core/notes/NoteChatConverter.ts
    - tests/core/notes/NoteTagger.test.ts
    - tests/core/notes/NoteQA.test.ts
    - tests/core/notes/NoteChatConverter.test.ts
metrics:
  duration: ~5 min
  tasks: 3 of 3 (TDD)
  tests-passed: 21 (7 + 8 + 6)
---

# Plan 07.1-02: LLM Core Services Summary

**Objective:** Create NoteTagger, NoteQA, and NoteChatConverter — the intelligence layer for the notes system.

## Task Commits

1. **Task 1: NoteTagger** — Haiku-tier tag/category/summary/memory extraction (7 tests)
2. **Task 2: NoteQA** — Flash-tier RAG Q&A with MiniSearch + MemoryEngine (8 tests)
3. **Task 3: NoteChatConverter** — Haiku-tier chat-to-note draft with memory context (6 tests)

All committed in `c46ae41`.

## Files Created

- `src/core/notes/NoteTagger.ts` — analyze() returns TaggerResult; retry-once, never-throw
- `src/core/notes/NoteQA.ts` — ask() returns QAResult with citations; MiniSearch retrieval + MemoryEngine.assemble() per MEM-01
- `src/core/notes/NoteChatConverter.ts` — convert() returns ConverterResult; memory context per MEM-03
- `tests/core/notes/NoteTagger.test.ts` — 7 tests
- `tests/core/notes/NoteQA.test.ts` — 8 tests
- `tests/core/notes/NoteChatConverter.test.ts` — 6 tests

## Decisions Made

- All three services use `providerRegistry.getModelsForTier()` with `(any)` cast for model instance (matching MemoryExtractor pattern)
- NoteQA uses synthetic `notes-rag-${Date.now()}` conversationId for MemoryEngine.assemble() per MEM-01
- All services return typed fallbacks (empty result shapes) on failure per D-04 / T-7.1-08

## Deviations from Plan

- Removed `maxTokens` from generateText calls to match current AI SDK v5 type constraints (pre-existing pattern in MemoryExtractor/ContextCompressor)

## Self-Check: PASSED

- [x] NoteTagger uses Haiku-tier LLM per D-01/D-02
- [x] NoteQA uses Flash-tier LLM per D-02, integrates MiniSearch + MemoryEngine
- [x] NoteChatConverter uses Haiku-tier LLM, integrates memory context per MEM-03
- [x] All 3 services: retry-once, never-throw, repairAndValidate, singleton exports
- [x] 21/21 tests pass across all 3 test files
