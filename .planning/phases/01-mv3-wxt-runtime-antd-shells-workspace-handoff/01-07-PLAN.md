---
phase: 01-mv3-wxt-runtime-antd-shells-workspace-handoff
plan: 07
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - entrypoints/sidepanel/main.tsx
  - src/core/workspace/WorkspaceRouter.ts
  - src/core/commands/registerWorkspaceCommands.ts
  - src/components/common/MirrorBanner.tsx
  - src/components/chat/SidepanelChat.tsx
  - src/components/chat/ChatComposer.tsx
  - src/components/chat/ChatMessageList.tsx
  - src/components/chat/ChatHeader.tsx
  - src/components/common/ThemeToggle.tsx
  - src/core/theme/ThemeStore.ts
  - src/core/theme/ThemeSync.ts
  - src/components/standalone/WorkspaceSidebar.tsx
  - tests/core/commands/registerWorkspaceCommands.test.ts
  - tests/components/MirrorBanner.test.tsx
  - tests/core/theme/ThemeSync.test.tsx
autonomous: false
requirements: [REQ-F05, REQ-F12]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "entrypoints/sidepanel/main.tsx no longer calls window.close() after opening the Standalone view; it calls WorkspaceRouter.openStandalone(workspaceId, conversationId, page) instead of its own ad-hoc chrome.tabs.query/find/create dedup logic."
    - "Opening the Standalone view shows an 'Opening standalone view…' loading indicator, and a failed chrome.tabs.create/update surfaces \"Couldn't open Standalone view\" as an actionable (not silent) error instead of leaving the user with no feedback."
    - "Side Panel registers the Flow-10 base command set (open-standalone-view, open-options, toggle-theme, reload-extension) through the same registerWorkspaceCommands module Plan 01-06 introduced for the Standalone surface."
    - "When a WORKSPACE_HANDOFF broadcast arrives at the Side Panel, the composer disables, the message list becomes read-only, and a MirrorBanner reading 'Switched to Standalone.' with a 'Refocus here' action link appears between the header and the message area."
    - "Clicking 'Refocus here' clears the mirror state and re-enables the composer without a page reload."
    - "The post-onboarding empty Side Panel conversation shows the exact caption 'Start a conversation by asking NowPilot a question.' with no suggestion chips and no mascot art (RICH-I-01 Welcome cards are explicitly deferred to Phase 15 per 01-UI-SPEC.md)."
    - "ThemeToggle renders as an AntD Segmented three-option control (Auto/Light/Dark) instead of the single cycling icon button, wired to useThemeStore.mode, and is mounted in both the Side Panel header (ChatHeader) and the Standalone top chrome (WorkspaceSidebar)."
    - "Toggling theme on one surface propagates to the other via chrome.storage.onChanged (not only the existing np_theme BroadcastChannel), and a failed chrome.storage.sync write surfaces 'Couldn't apply theme to other surface' with an actionable 'Try syncing again' link instead of failing silently."
    - "Edge (adjacency, REQ-F12): CommandRegistry.register() throws on a duplicate command id — the Side Panel's 4 Flow-10 commands can never collide with each other or with a stray double-registration on remount."
    - "Edge (empty, REQ-F12): the Side Panel's Flow-10 command set is never empty — exactly 4 commands (open-standalone-view, open-options, toggle-theme, reload-extension) are registered on every mount before Cmd+K can be opened."
    - "Edge (ordering, REQ-F12): CommandRegistry.search results preserve registration-order for equally-ranked matches, stable across re-renders."
    - {statement: "The mirror banner clears itself automatically (no user click needed) if the Standalone tab closes mid-session, via a reverse WORKSPACE_HANDOFF-adjacent signal.", verification: backstop}
  artifacts:
    - src/components/common/MirrorBanner.tsx
    - src/core/commands/registerWorkspaceCommands.ts (registerSidepanelCommands, extended from Plan 01-06)
  key_links:
    - "entrypoints/sidepanel/main.tsx -> WorkspaceRouter.openStandalone -> chrome.tabs.create(standalone.html) -> StandaloneShell.hydrateFromURL -> notifyWorkspaceHandoff -> onWorkspaceSync (Side Panel) -> MirrorBanner"
    - "ThemeStore.setMode/setPack -> chrome.storage.sync.np_theme -> chrome.storage.onChanged listener (both surfaces) -> ConfigProvider re-render (APPR-04, no remount)"
  prohibitions:
    - requirement_id: REQ-F05
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT present the Side Panel mirror as if it were still the authoritative, writable surface after handoff — the disabled composer's placeholder must communicate that Standalone view is active, not silently accept and drop input."
    - requirement_id: REQ-F12
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT execute reload-extension (drops in-progress chat/agent state) via auto-trigger or type-ahead partial match in the command palette — only an explicit full selection of that command may run it."
---

<objective>
Wire the visual and interactive half of Flow 11 on the Side Panel (D-04/D-05/D-06), register the Side Panel's Flow-10 command set (D-08), and complete the cross-surface theme propagation work Plan 01-01 only half-finished at the persistence layer (D-10's UI half): a visible `Segmented` theme control on both surfaces, driven by `chrome.storage.onChanged`, with an actionable failure toast instead of a silent no-op.

Purpose: this plan is the capstone for three of ROADMAP's four Phase-1 success criteria — "Standalone view opens... with no message loss," "Cmd+K palette opens with the Flow 10 command set on both surfaces," and "Theme toggle changes propagate to both surfaces immediately" — which is why it closes with a blocking human-verify checkpoint rather than relying solely on automated tests for the parts that are inherently visual (mirror banner appearance, Segmented control rendering, cross-surface propagation timing).

Output: a working, non-closing Side Panel post-handoff mirror state; the Side Panel's registered Flow-10 command set; a visible, propagating, failure-toast-equipped theme Segmented control on both surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-CONTEXT.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-UI-SPEC.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-PATTERNS.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Side Panel entrypoint — openStandalone wiring + Flow-10 command registration</name>
  <files>entrypoints/sidepanel/main.tsx, src/core/workspace/WorkspaceRouter.ts, src/core/commands/registerWorkspaceCommands.ts, tests/core/commands/registerWorkspaceCommands.test.ts</files>
  <read_first>
    Read `entrypoints/sidepanel/main.tsx` in full again (137 lines — `handleOpenStandalone` at
    lines 11-40 does its OWN ad-hoc `chrome.tabs.query({})` + `.find()` dedup instead of calling
    `WorkspaceRouter`, then calls `window.close()` at lines 35-39; the command registration
    `useEffect` at lines 70-107 registers `toggle-theme` and `open-full-app`, not the Flow-10 base
    set). Read `src/core/workspace/WorkspaceRouter.ts` fresh (state after Plan 01-06's
    `openStandalone`/`hydrateFromURL` fix) — this task adds a `notifyWorkspaceHandoff` call inside
    `hydrateFromURL` so the Standalone view's boot sequence actually fires the broadcast Flow 11
    requires (confirmed this session: `WorkspaceSync.ts`'s `notifyWorkspaceHandoff` exists but
    nothing calls it yet). Read `src/core/commands/registerWorkspaceCommands.ts` from Plan
    01-06 for the existing `registerStandaloneCommands` shape to mirror.
  </read_first>
  <behavior>
    - Test 1: `registerSidepanelCommands({ onOpenStandalone, onOpenOptions, onToggleTheme,
      onReloadExtension })` registers exactly 4 commands with ids `open-standalone-view`,
      `open-options`, `toggle-theme`, `reload-extension` (no `focus-side-panel`, since the user is
      already in the Side Panel).
    - Test 2: `hydrateFromURL(new URLSearchParams('workspaceId=ws3&conversationId=c9'))` results
      in a `WORKSPACE_HANDOFF` message being published on the `np_workspace` BroadcastChannel
      (assert via a `subscribe` spy registered before calling `hydrateFromURL`).
    - Test 3: `openStandalone(wsId, convId, page, onSettled)` calls `onSettled(undefined)` on a
      successful `tabs.create`/`tabs.update`, and calls `onSettled(new Error(...))` when a mocked
      `chrome.runtime.lastError`-equivalent rejection occurs on the tabs call.
  </behavior>
  <action>
    In `src/core/commands/registerWorkspaceCommands.ts`, add a sibling export
    `registerSidepanelCommands(handlers: { onOpenStandalone: () => void; onOpenOptions: () =>
    void; onToggleTheme: () => void; onReloadExtension: () => void }): () => void` registering the
    Side Panel's 4-command Flow-10 base set (`open-standalone-view`, `open-options`,
    `toggle-theme`, `reload-extension`) with copy from 01-UI-SPEC.md's Copywriting Contract table,
    returning an unregister function. In `entrypoints/sidepanel/main.tsx`, delete the ad-hoc
    `chrome.tabs.query({})`/`.find()` dedup logic and the `window.close()` call inside
    `handleOpenStandalone` (lines 11-40); replace the function body with a direct call to
    `WorkspaceRouter.openStandalone(useWorkspaceStore.getState().workspaceId,
    useWorkspaceStore.getState().conversationId ?? undefined)` (import `openStandalone` from
    `../../src/core/workspace/WorkspaceRouter` and `useWorkspaceStore` from
    `../../src/core/workspace/WorkspaceStore`) — this removes the duplicate dedup implementation
    PATTERNS.md flags, delegating entirely to `WorkspaceRouter`'s own (now-fixed) dedup. Replace
    the existing inline command-registration `useEffect` with a call to
    `registerSidepanelCommands({...})`, renaming the `'open-full-app'` id references to
    `'open-standalone-view'` and adding the new `open-options` command (calling the existing
    `handleOpenOptions` function already defined in this file). In `WorkspaceRouter.ts`'s
    `hydrateFromURL`, after setting `workspaceId`/`conversationId` via `set()` (Plan 01-06's fix),
    add a call to `notifyWorkspaceHandoff(wsId, convId ?? '')` from `./WorkspaceSync` (only when
    `wsId` is present — this is the actual handoff signal, not a page-load-with-no-params no-op).
    Add an optional fourth parameter `onSettled?: (err?: Error) => void` to
    `WorkspaceRouter.openStandalone`, invoked once the `tabs.update`/`tabs.create` callback runs —
    pass `undefined` on success and a `new Error(chrome.runtime.lastError?.message ?? 'unknown
    error')` when `chrome.runtime.lastError` is set inside that callback (chrome's callback-style
    APIs surface failures via `chrome.runtime.lastError`, not a thrown exception). In
    `entrypoints/sidepanel/main.tsx`'s `handleOpenStandalone`, wrap the call: show
    `antMessage.loading({ content: 'Opening standalone view…', key: 'open-standalone', duration: 0
    })` immediately before calling `openStandalone(...)`, and in the `onSettled` callback either
    call `antMessage.destroy('open-standalone')` (success, no further toast needed since the
    mirror banner itself is the success signal) or replace it with `antMessage.error({ content:
    "Couldn't open Standalone view", key: 'open-standalone' })` plus a follow-up action affordance
    — render the "Try Opening Again" ghost action as a second `antMessage` action button if the
    AntD `message` API version in use supports an inline action, otherwise as a plain
    `antMessage.error` whose click-through re-invokes the same `handleOpenStandalone` function (do
    not block on adding a custom action button if AntD's `message.error` config in this codebase's
    pinned version does not support one — a plain dismissible error message satisfying the
    "actionable, not silent" requirement is the acceptance bar, not a specific AntD API shape).
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/commands/registerWorkspaceCommands.test.ts</automated>
  </verify>
  <done>Side Panel no longer closes itself after opening Standalone; it delegates to WorkspaceRouter.openStandalone with a loading toast that resolves to either silent success (mirror banner appears) or an actionable "Couldn't open Standalone view" error; it registers the Flow-10 base set; hydrateFromURL now actually broadcasts WORKSPACE_HANDOFF.</done>
  <acceptance_criteria>`grep -c "window.close()" entrypoints/sidepanel/main.tsx` returns 0; `grep -c "chrome.tabs.query({})" entrypoints/sidepanel/main.tsx` returns 0; `grep -n "Opening standalone view" entrypoints/sidepanel/main.tsx` matches; `grep -n "Couldn't open Standalone view" entrypoints/sidepanel/main.tsx` matches; the three new behavior tests pass.</acceptance_criteria>
  <reversibility rating="costly">Touches every entry point and the BroadcastBus typed-message contract for the handoff's Side-Panel-facing half; this is the other side of the D-04 decision Plan 01-06 started.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MirrorBanner component + SidepanelChat integration + composer disable + empty-state simplification</name>
  <files>src/components/common/MirrorBanner.tsx, src/components/chat/SidepanelChat.tsx, src/components/chat/ChatComposer.tsx, src/components/chat/ChatMessageList.tsx, tests/components/MirrorBanner.test.tsx</files>
  <read_first>
    Read `src/components/chat/SidepanelChat.tsx` in full again (444 lines — the header render at
    lines 279-286, the main chat flow container starting line 289, no existing subscription to
    `onWorkspaceSync` today). Read `src/components/chat/ChatComposer.tsx` lines 1-60 (props
    interface) and lines 180-235 (the `<textarea>`'s `value`/`onChange`/`placeholder`/`className`
    props and the send `<button>`'s `disabled` condition at line 227) — this task adds a
    `disabled?: boolean` prop threaded through both the textarea and the send button. Read
    `src/components/chat/ChatMessageList.tsx` lines 52-95 (the current empty-state render: a "Hi,
    How can I assist you today?" heading plus a row of quick-action chip buttons — 01-UI-SPEC.md
    explicitly requires this simplified to a single centered caption with NO chips, since RICH-I-01
    Welcome cards are Phase-15 scope and this scaffold's chip row is a premature preview of that
    feature). Read `src/core/workspace/WorkspaceSync.ts` for the `onWorkspaceSync` subscribe
    helper this task consumes.
  </read_first>
  <behavior>
    - Test 1: `<MirrorBanner onRefocus={fn} />` renders the literal text "Switched to Standalone."
      and a clickable "Refocus here" link.
    - Test 2: clicking "Refocus here" calls the `onRefocus` callback exactly once.
  </behavior>
  <action>
    Create `src/components/common/MirrorBanner.tsx` exporting `MirrorBanner: React.FC<{ onRefocus:
    () => void }>` rendering a 32px-tall, full-width bar with `colorPrimaryBg` background (via
    `theme.useToken()`), a hairline `colorBorder` top+bottom border, the left-aligned caption
    "Switched to Standalone." (12px, `colorTextBase`) and a right-aligned "Refocus here" link
    (12px, `colorPrimary`, underlined, `onClick={onRefocus}`) — per 01-UI-SPEC.md's Visual Anchors
    table for this element (no `colorError`/`colorWarning`; this is an informational banner, not
    an error surface). In `SidepanelChat.tsx`, add local state `const [mirrored, setMirrored] =
    useState(false)`, subscribe to `onWorkspaceSync` in a `useEffect` and set `mirrored` to `true`
    when a `WORKSPACE_HANDOFF` message arrives (unsubscribe in cleanup); render `<MirrorBanner
    onRefocus={() => setMirrored(false)} />` immediately after the `ChatHeader` block (between the
    header and the "Main Chat Flow Container" div) only when `mirrored` is `true`; pass
    `disabled={mirrored}` to the `ChatComposer` render call. In `ChatComposer.tsx`, add `disabled?:
    boolean` to `ChatComposerProps`, apply it to the `<textarea>` (`disabled={disabled}`,
    `placeholder={disabled ? 'Standalone view is active' : 'Ask anything, @ models, / prompts'}`)
    and to the send `<button>` (`disabled={disabled || (!inputPrompt.trim() &&
    activeAttachments.length === 0)}`). In `ChatMessageList.tsx`, replace the current empty-state
    JSX (the "Hi, How can I assist you today?" heading plus the quick-action chip row) with a
    single centered `<Text type="secondary" style={{ fontSize: 12 }}>Start a conversation by
    asking NowPilot a question.</Text>` above the (still-enabled, unless `disabled` is also
    threaded here — it is not needed here since the composer itself is disabled separately) — keep
    the outer container's centering/spacing structure, only replace the heading+chip-row content.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/MirrorBanner.test.tsx</automated>
  </verify>
  <done>MirrorBanner renders and wires onRefocus; SidepanelChat shows it on WORKSPACE_HANDOFF and disables the composer; the empty-state caption matches 01-UI-SPEC.md verbatim with no chips.</done>
  <acceptance_criteria>Both MirrorBanner tests pass; `grep -n "Start a conversation by asking NowPilot a question" src/components/chat/ChatMessageList.tsx` matches; `grep -c "For YouTube\|Summarize\|Explain" src/components/chat/ChatMessageList.tsx` returns 0 (the premature RICH-I-01 chip preview is gone).</acceptance_criteria>
  <reversibility rating="reversible">Pure UI-side behavior — a local boolean flag and a new small component; no external contract change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ThemeToggle Segmented conversion + chrome.storage.onChanged propagation + failure toast + mount on both surfaces</name>
  <files>src/components/common/ThemeToggle.tsx, src/core/theme/ThemeStore.ts, src/core/theme/ThemeSync.ts, src/components/chat/ChatHeader.tsx, src/components/standalone/WorkspaceSidebar.tsx, tests/core/theme/ThemeSync.test.tsx</files>
  <read_first>
    Read `src/components/common/ThemeToggle.tsx` in full again (49 lines — a single icon `<button>`
    that cycles `light→dark→auto` on click; note this component is currently mounted ONLY in
    `src/main.tsx`, the plain Vite dev-shell harness, NOT in either MV3 entrypoint — confirmed this
    session via grep — so this task's mount step is net-new, not a rewire of an existing mount
    point). Read `src/core/theme/ThemeStore.ts` fresh (state after Plan 01-01's sync-storage-target
    change) for the `mode`/`pack`/`setMode`/`setPack` shape this task wires a `chrome.storage.onChanged`
    listener around. Read `src/components/chat/ChatHeader.tsx` in full (its existing `Tooltip`
    icon-button pattern at lines 36-70 is the mount-point template — add the Segmented control as a
    sibling, not a replacement, of the existing Options/Full-page icon buttons). Read
    `src/components/standalone/WorkspaceSidebar.tsx`'s top-chrome section (already converted off
    Tailwind by Plan 01-03) to find its header/top-bar row for the second mount point.
  </read_first>
  <behavior>
    - Test 1: a `chrome.storage.onChanged` event for key `np_theme` with a new `mode` value not
      matching the current in-memory `useThemeStore.getState().mode` updates the store (mocked
      `chrome.storage.onChanged.addListener`).
    - Test 2: a simulated `chrome.storage.sync.set` rejection during `setMode` surfaces an
      `antMessage.error` call containing "Couldn't apply theme to other surface" and does not
      throw uncaught.
    - Test 3: the local surface's `mode` still updates even when the cross-surface sync write
      fails (local-first, propagation-retry-is-separate, per 01-UI-SPEC.md's Copywriting Contract).
  </behavior>
  <action>
    Rewrite `ThemeToggle.tsx` to render an AntD `Segmented` with 3 options (`Auto`/`Light`/`Dark`
    mapped to `ThemeMode` `'auto'`/`'light'`/`'dark'`), `value={mode}`, `onChange={(val) =>
    setMode(val as ThemeMode)}`, selected option using `colorPrimaryBg` fill (AntD's default
    Segmented selected-state token, no custom override needed). Add a `chrome.storage.onChanged`
    listener (guarded behind `typeof chrome !== 'undefined' && chrome.storage?.onChanged`,
    mirroring the existing `hasChromeStorage` guard pattern from `chromeStorageAdapter.ts`) inside
    a `useEffect` in `useThemeSync()` (`ThemeSync.ts`) that, on a change to the `np_theme` key,
    parses the new value and calls `setMode`/`setPack` only if they differ from current state
    (same no-op guard pattern `setMode`/`setColorTheme` already use internally). Wrap the
    `chrome.storage.sync.set` write path (inside `ThemeStore.ts`'s persist `storage` adapter,
    which Plan 01-01 already targets at sync) with a `.catch()` that calls into a small
    exported `onThemeSyncError` callback hook consumed by `ThemeToggle.tsx` to trigger
    `antMessage.error("Couldn't apply theme to other surface")` with an inline "Try syncing
    again" action that re-invokes the failed write — the local `mode`/`pack` state change must
    NOT be rolled back on this failure (local-first, per 01-UI-SPEC.md). Mount `<ThemeToggle />`
    in `ChatHeader.tsx` as a new sibling icon-region element (Side Panel) and in
    `WorkspaceSidebar.tsx`'s top-chrome row (Standalone) — both mount points render the exact same
    component, satisfying "no per-surface copy" (APPR-04).
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/theme/ThemeSync.test.tsx</automated>
  </verify>
  <done>ThemeToggle is a Segmented 3-option control mounted on both surfaces; chrome.storage.onChanged propagates cross-surface; a sync-write failure surfaces an actionable toast without rolling back the local change.</done>
  <acceptance_criteria>All 3 behavior tests pass; `grep -n "Segmented" src/components/common/ThemeToggle.tsx` matches; `grep -n "ThemeToggle" src/components/chat/ChatHeader.tsx src/components/standalone/WorkspaceSidebar.tsx` matches in both files.</acceptance_criteria>
  <reversibility rating="reversible">UI-side control swap plus an additive storage listener; the underlying ThemeStore contract (mode/pack/setMode/setPack) from Plan 01-01 is unchanged, only consumed more broadly.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Workspace-handoff + Flow-10 + cross-surface theme propagation capstone</name>
  <what-built>The Side Panel post-handoff mirror banner, the cross-surface Segmented theme toggle with chrome.storage.onChanged propagation, and the Flow-10 command sets on both surfaces — together these are the visual/interactive proof of three of ROADMAP's four Phase-1 success criteria.</what-built>
  <how-to-verify>
    Run `pnpm build:ext` and load `.output/chrome-mv3` as an unpacked extension. Open the Side
    Panel, open Cmd+K, confirm the command list shows "Open Standalone view", "Open Options",
    "Toggle Theme", "Reload Extension" (4 commands, no "Focus Side Panel" — that is
    Standalone-only). Click "Open Standalone view" (or run the command): confirm a Standalone tab
    opens (or focuses if already open, click it twice to confirm no duplicate tab is created), and
    confirm the Side Panel now shows the "Switched to Standalone." mirror banner with a disabled
    composer reading "Standalone view is active" as its placeholder. Click "Refocus here" and
    confirm the banner disappears and the composer re-enables. In the Standalone tab, open Cmd+K
    and confirm its command list shows "Focus Side Panel", "Open Options", "Toggle Theme", "Reload
    Extension" (4 commands, no "Open Standalone view"). Toggle the theme Segmented control (visible
    in both the Side Panel header and the Standalone top chrome) from Light to Dark on one surface
    and confirm the OTHER surface's Segmented control and overall color scheme update within roughly
    1 second with no page reload.
  </how-to-verify>
  <resume-signal>Type "approved" once all five checks above pass, or describe exactly which check failed and what you observed instead.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

This plan's contribution to the phase-wide artifact inventory:

- `src/core/commands/registerWorkspaceCommands.ts`'s `registerSidepanelCommands()` (sibling to Plan 01-06's `registerStandaloneCommands()`).
- `src/components/common/MirrorBanner.tsx` — the post-handoff Side Panel status banner.
- `WorkspaceRouter.openStandalone`'s `onSettled` callback parameter (loading/error toast wiring).
- `ThemeToggle.tsx` rewritten as an AntD `Segmented` control; `chrome.storage.onChanged` listener in `ThemeSync.ts`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Side Panel <-> Standalone cross-surface broadcast | WORKSPACE_HANDOFF and theme-change broadcasts travel over BroadcastChannel/chrome.storage.onChanged, both scoped to this extension's own origin — not attacker-reachable, but the mirror-state UI must not falsely claim authority it does not have |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Tampering | MirrorBanner / disabled composer state desync | medium | mitigate | mirrored state is driven directly by the WORKSPACE_HANDOFF subscription and cleared only by explicit user click, with no other write path that could leave the composer disabled with no banner explaining why |
| T-01-20 | Information Disclosure | theme-sync failure toast | low | accept | The toast surfaces only a generic "Couldn't apply theme to other surface" string, never the underlying chrome.storage error object (which could theoretically include quota/internal details) |
| T-01-21 | Denial of Service | reload-extension reachable via Cmd+K on both surfaces | medium | mitigate | Requires an explicit full command selection (Enter/click on the highlighted row), never an auto-trigger on partial type-ahead match — matches this plan's REQ-F12 prohibition |

</threat_model>

<verification>
`pnpm vitest run tests/components tests/core/theme tests/core/commands` passes; `tsc --noEmit` passes; the blocking human-verify checkpoint is approved.
</verification>

<success_criteria>
Side Panel demotes to a read-only mirror (not a close) after handoff and re-enables on Refocus; both surfaces register their correct Flow-10 command sets; theme changes propagate cross-surface via chrome.storage.onChanged with an actionable failure path — all confirmed by a human visual pass, not automated tests alone.
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-07-SUMMARY.md` when done.
</output>
