---
phase: 01-mv3-wxt-runtime-antd-shells-workspace-handoff
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - tests/isolation/cross-entrypoint-imports.test.ts
  - src/core/runtime/RuntimeEnvelope.ts
  - tests/core/runtime/RuntimeEnvelope.test.ts
  - src/core/workspace/WorkspaceStore.ts
  - tests/core/workspace/WorkspaceStore.test.ts
autonomous: true
requirements: [REQ-R02, REQ-R04, REQ-R05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The isolation test greps the real component directories (src/components/chat/, src/components/standalone/, src/components/options/), not the non-existent components/app/ and components/sidepanel/ paths — a real cross-import between chat/ and standalone/ would now fail the test."
    - "The isolation test asserts zero fetch( calls anywhere under entrypoints/content/**."
    - "RuntimeEnvelope.ts declares PAGE_LIVE_CONTEXT, PAGE_EXTRACTION_REQUESTED, and PAGE_HTML_PAYLOAD as type-only MessageType values with baseUrl and truncated reserved on the PAGE_HTML_PAYLOAD payload shape; no runtime handler or extraction strategy exists for them yet (Phase 6 implements that)."
    - "WorkspaceStore.isPrimaryWriter() exists, is exported, and returns true unconditionally for any caller; a code comment documents that Phase 2 must implement real election semantics before any exclusive-write gating depends on this predicate."
    - "ActiveSurface is renamed from 'sidepanel' | 'full-app' to 'sidepanel' | 'standalone' (D-07 naming canonicalization), with every setActiveSurface call site updated to match."
    - "Edge (boundary, REQ-R02): a shared import from src/types or src/core/* is never flagged as a cross-surface violation by the repointed isolation grep — only true component-directory-to-component-directory imports (chat/<->standalone/<->options/) count."
    - "Edge (empty, REQ-R02): if a component directory legitimately contains zero files, the isolation grep reports zero cross-imports for it without throwing — a vacuous-but-correct pass, distinct from the pre-fix vacuous-because-wrong-path failure mode this task fixes."
    - "Edge (empty, REQ-R04): createEnvelope('PAGE_HTML_PAYLOAD', { html: '', baseUrl: '', truncated: true }, 'content') is a structurally valid envelope even with an empty html string, since Phase 1 has no runtime handler that would need non-empty content to function."
    - "Edge (adjacency, REQ-R05): calling isPrimaryWriter() from two different surfaces (Side Panel and Standalone) in the same tick both return true — there is no cross-surface coordination in Phase 1; this is the documented, accepted behavior until Phase 2 implements real election."
  artifacts:
    - src/core/runtime/RuntimeEnvelope.ts (PAGE_LIVE_CONTEXT, PAGE_EXTRACTION_REQUESTED, PAGE_HTML_PAYLOAD)
    - src/core/workspace/WorkspaceStore.ts (isPrimaryWriter())
  key_links:
    - "WorkspaceStore.isPrimaryWriter() -> Phase 2 MemoryEngine write-path gating (interface frozen now, election semantics land later)"
    - "RuntimeEnvelope PAGE_HTML_PAYLOAD -> Phase 6 PageContentService (type reserved now, strategy implemented later)"
  prohibitions:
    - requirement_id: REQ-R02
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT flag a legitimate shared import (src/types, src/core/*) as a cross-surface violation — the isolation grep must scope to component-directory-to-component-directory imports only, or it punishes correct shared-infra code instead of catching real leaks."
    - requirement_id: REQ-R04
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT ship any runtime handler or extraction-strategy logic for PAGE_EXTRACTION_REQUESTED/PAGE_HTML_PAYLOAD in Phase 1 — these are type-only declarations; adding real logic here would front-run Phase 6's Defuddle-vs-Readability architecture decision before RESEARCH.md's Phase-6 spike happens."
    - requirement_id: REQ-R05
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT omit a code comment documenting that isPrimaryWriter() unconditionally returns true in Phase 1 — an undocumented always-true stub silently becomes a permanent (and incorrect) assumption once Phase 2's MemoryEngine starts depending on it for write-exclusivity."
---

<objective>
Land the three additive, low-risk declarations that close out the phase's "prove it's real, not vacuous" and "reserve the type contract" work: repoint the vacuous isolation test at the codebase's actual directory structure (D-17, REQ-R02), declare the frozen extraction envelope types Phase 6 will implement against (D-15, REQ-R04), and add the `isPrimaryWriter()` predicate plus the `ActiveSurface` naming rename WorkspaceRouter's fix (next plan) depends on (D-16/D-07, REQ-R05).

Purpose: `tests/isolation/cross-entrypoint-imports.test.ts` today greps `src/components/app/` and `src/components/sidepanel/` — neither directory exists, so all three assertions pass vacuously regardless of what the codebase actually does (Pitfall 6). This plan makes the isolation guard real before any more UI wiring lands on top of it. The two type/predicate additions are pure interface declarations with no behavioral logic — safe, additive, and exactly the pattern this project already uses elsewhere (declare now, populate later, per the Note.type precedent RESEARCH.md cites).

Output: a real isolation test; `PAGE_LIVE_CONTEXT`/`PAGE_EXTRACTION_REQUESTED`/`PAGE_HTML_PAYLOAD` type declarations; `WorkspaceStore.isPrimaryWriter()`; `ActiveSurface` renamed to use `'standalone'` instead of `'full-app'`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-CONTEXT.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-RESEARCH.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-PATTERNS.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repoint the vacuous isolation test at real directories + add the content-script fetch guard</name>
  <files>tests/isolation/cross-entrypoint-imports.test.ts</files>
  <read_first>
    Read `tests/isolation/cross-entrypoint-imports.test.ts` in full (41 lines — three `it()`
    blocks grep `src/components/sidepanel/` and `src/components/app/`, neither of which exists;
    `ls src/components/` confirms the real directories are `chat/`, `common/`, `history/`,
    `notes/`, `options/`, `pages/`, `standalone/`). Read RESEARCH.md's Pitfall 6 and D-17 for the
    exact scope: real directories to check are `src/components/chat/` (Side Panel),
    `src/components/standalone/` (Standalone view), `src/components/options/` (Options) — assert
    no cross-imports between any two of these three in either direction, and add a fourth check
    that `entrypoints/content/**` (renamed in Plan 01-02) contains zero `fetch(` calls.
  </read_first>
  <behavior>
    - Test 1: no file under `src/components/chat/` imports from `src/components/standalone/` or
      `src/components/options/`.
    - Test 2: no file under `src/components/standalone/` imports from `src/components/chat/` or
      `src/components/options/`.
    - Test 3: no file under `src/components/options/` imports from `src/components/chat/` or
      `src/components/standalone/`.
    - Test 4: `grep -r "fetch(" entrypoints/content/**` returns zero matches.
    - Test 5 (regression guard): a deliberately-injected temporary cross-import (added and then
      removed within the test body via a throwaway fixture string, not a real file) proves the
      grep pattern actually matches when a violation exists — this is the "prove the gate isn't
      vacuous" assertion RESEARCH.md Pitfall 6 calls for; implement it as a unit test on the grep
      REGEX itself (`expect(pattern.test("from '../chat/Foo'")).toBe(true)` style) rather than
      writing a throwaway file to disk.
  </behavior>
  <action>
    Rewrite `tests/isolation/cross-entrypoint-imports.test.ts` to grep the three real component
    directories (`src/components/chat/`, `src/components/standalone/`, `src/components/options/`)
    for cross-imports in both directions using the same `execSync('grep -r ...')` pattern the
    existing file already uses (do not introduce a new test-execution mechanism), scope the
    import-path grep pattern to exclude imports of shared, non-UI modules (`src/types`,
    `src/core/*`, `src/services/*`) so a legitimate shared-infra import is never flagged as a
    violation — for example, match on `from '.*components/(chat|standalone|options)/` rather than
    a bare directory-name substring that would also match unrelated path segments. Add the fourth
    check for `fetch(` under `entrypoints/content/**`. Add the fifth "prove it's not vacuous"
    regex self-test described above.
  </action>
  <verify>
    <automated>pnpm vitest run tests/isolation/cross-entrypoint-imports.test.ts</automated>
  </verify>
  <done>The isolation test greps real directories, catches an injected violation in its own regex self-test, and asserts zero fetch( calls in the content-script directory.</done>
  <acceptance_criteria>All 5 test cases pass; `grep -c "components/app\|components/sidepanel" tests/isolation/cross-entrypoint-imports.test.ts` returns 0 (the old vacuous paths are gone).</acceptance_criteria>
  <reversibility rating="reversible">Test-file-only change; no production code touched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Frozen extraction envelope types (type-only, D-15/REQ-R04)</name>
  <files>src/core/runtime/RuntimeEnvelope.ts, tests/core/runtime/RuntimeEnvelope.test.ts</files>
  <read_first>
    Read `src/core/runtime/RuntimeEnvelope.ts` in full again (47 lines; `MessageTypeValues` now
    has 10 entries after Plan 01-02 added `CONTENT_SCRIPT_READY`/`SPA_NAVIGATION`). Read
    01-CONTEXT.md's D-15 for the exact three type names and the two reserved fields on
    `PAGE_HTML_PAYLOAD`. This is a type-only addition — no handler registration, no strategy
    logic; Phase 6 implements the actual Defuddle/Readability extraction against these types.
  </read_first>
  <action>
    Add `'PAGE_LIVE_CONTEXT'`, `'PAGE_EXTRACTION_REQUESTED'`, and `'PAGE_HTML_PAYLOAD'` to
    `MessageTypeValues` in `src/core/runtime/RuntimeEnvelope.ts`. Add a payload-shape type
    (exported, not wired into `RuntimeEnvelope<T>`'s generic in any special way — it is just an
    available named type for Phase 6 to import) named `PageHtmlPayload` with fields `html: string`,
    `baseUrl: string`, `truncated: boolean`, and a reserved (optional, undefined-in-Phase-1)
    `strategyId?: string` field for Phase 17's ServiceNow strategy registration. Add a one-line
    code comment directly above the three new `MessageTypeValues` entries stating that these are
    type-only reservations with no handler registered until Phase 6 (and Phase 17 for the
    ServiceNow `strategyId`), so a future reader does not assume a working extraction path exists
    today.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/runtime/RuntimeEnvelope.test.ts</automated>
  </verify>
  <done>PAGE_LIVE_CONTEXT, PAGE_EXTRACTION_REQUESTED, PAGE_HTML_PAYLOAD compile as valid MessageType values with no runtime handler registered; PageHtmlPayload exports baseUrl/truncated/strategyId.</done>
  <acceptance_criteria>`grep -c "PAGE_LIVE_CONTEXT\|PAGE_EXTRACTION_REQUESTED\|PAGE_HTML_PAYLOAD" src/core/runtime/RuntimeEnvelope.ts` returns at least 3 (once per MessageTypeValues entry, plus the PageHtmlPayload interface); a test constructs `createEnvelope('PAGE_HTML_PAYLOAD', { html: '', baseUrl: '', truncated: false }, 'content')` and asserts `isEnvelope()` returns true.</acceptance_criteria>
  <reversibility rating="costly">Cross-phase type contract — Phase 6 and Phase 17 import these symbols by name; renaming them later touches two future phases' code instead of zero.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: isPrimaryWriter() predicate + ActiveSurface rename to 'standalone'</name>
  <files>src/core/workspace/WorkspaceStore.ts, tests/core/workspace/WorkspaceStore.test.ts</files>
  <read_first>
    Read `src/core/workspace/WorkspaceStore.ts` in full again (121 lines; `export type
    ActiveSurface = 'sidepanel' | 'full-app'` at line 5; `setActiveSurface` action at lines 87-90;
    `activeSurface: 'sidepanel' as ActiveSurface` in `initialState` at line 45). This is the same
    file Plan 01-04 Task 3 already modified for the storage/version migration — this task lands
    after that commit, so re-read the file fresh rather than assuming the line numbers above are
    still exact once Plan 01-04's edits are applied.
  </read_first>
  <behavior>
    - Test 1: `isPrimaryWriter()` returns `true` when called with no arguments.
    - Test 2: `isPrimaryWriter()` returns `true` when called a second time in the same test
      (proving it is not stateful/toggling — Phase 1's interface is unconditional).
    - Test 3: `ActiveSurface` accepts `'standalone'` as a valid value and rejects `'full-app'` at
      the type level (a `// @ts-expect-error` compile-time assertion in the test file, or a
      runtime string-literal check against the exact two allowed values).
  </behavior>
  <action>
    Change `export type ActiveSurface = 'sidepanel' | 'full-app'` to `export type ActiveSurface =
    'sidepanel' | 'standalone'` and update the one `activeSurface: 'sidepanel' as ActiveSurface`
    initial-state reference (no other call site sets `'full-app'` literally today, per this
    session's grep — confirm this is still true after Plan 01-04's edits before relying on it).
    Add `export function isPrimaryWriter(): boolean { return true; }` as a module-level export
    alongside the store definition (matching the file's existing module-export style for
    `ActiveSurface`/`TabContext`, not shoehorned into the zustand action map, since it has no
    `set()` side effect). Add a code comment directly above it: `// Phase 1: always true (interface
    frozen, no election yet). Phase 2 implements real election semantics (background-SW
    authoritative vs. tabs.query highest-id vs. BroadcastBus subscriber) before MemoryEngine write
    paths gate on this — do not assume this always returns true past Phase 2.`
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/workspace/WorkspaceStore.test.ts</automated>
  </verify>
  <done>isPrimaryWriter() is exported and returns true unconditionally with a documented Phase-2 swap-point comment; ActiveSurface uses 'standalone' instead of 'full-app'.</done>
  <acceptance_criteria>`grep -n "export function isPrimaryWriter" src/core/workspace/WorkspaceStore.ts` matches; `grep -c "'full-app'" src/core/workspace/WorkspaceStore.ts` returns 0; the Phase-2 swap-point comment is present verbatim above the function.</acceptance_criteria>
  <reversibility rating="reversible">Additive interface (isPrimaryWriter) plus a narrow string-literal rename confined to this one file per this session's grep; Phase 2 layers real enforcement on top without changing the signature.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Isolation test suite -> future cross-surface import discipline | A real (non-vacuous) isolation gate is the only automated defense against Side-Panel/Standalone/Options code leaking into each other's bundles |
| PAGE_HTML_PAYLOAD type reservation -> Phase 6 trust boundary | This phase declares the shape untrusted extracted HTML will eventually carry; Phase 7's trust-aware context work depends on this shape existing early |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13 | Tampering | vacuous isolation test (pre-fix) | medium | mitigate | Task 1's regex self-test proves the gate would actually catch a real violation, not just pass by construction |
| T-01-14 | Information Disclosure | PAGE_HTML_PAYLOAD future extracted content | low | accept | Type-only in Phase 1 — no runtime path exists to leak anything yet; Phase 6/7 own the actual trust/redaction controls over extracted HTML |
| T-01-15 | Elevation of Privilege | isPrimaryWriter() always-true stub misused as a real gate before Phase 2 | medium | mitigate | Documented code comment at the call site is the mitigation available in Phase 1; no consumer of this predicate exists yet in this phase to misuse it |

</threat_model>

<verification>
`pnpm vitest run tests/isolation tests/core/runtime tests/core/workspace` passes; `tsc --noEmit` passes.
</verification>

<success_criteria>
The isolation test is real (catches an injected violation in its own self-test); the three frozen envelope types compile with no runtime logic attached; isPrimaryWriter() exists with a documented Phase-2 swap point; ActiveSurface uses the canonical 'standalone' name.
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-05-SUMMARY.md` when done.
</output>
