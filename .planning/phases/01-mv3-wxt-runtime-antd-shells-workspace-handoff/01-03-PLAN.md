---
phase: 01-mv3-wxt-runtime-antd-shells-workspace-handoff
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - wxt.config.ts
  - package.json
  - src/index.css
  - tsconfig.json
  - src/components/standalone/StandaloneWorkspace.tsx
  - src/components/standalone/WorkspaceSidebar.tsx
autonomous: false
requirements: [REQ-R19, REQ-R21]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "wxt.config.ts manifest.permissions is exactly ['sidePanel','storage','tabs'] — cookies, alarms, scripting, contextMenus, notifications, declarativeNetRequest are all removed."
    - "grep 'tailwind|shadcn|@radix-ui' package.json returns zero matches; grep 'framer-motion' package.json returns zero matches; the motion (^12) package remains untouched."
    - "src/index.css no longer imports tailwindcss; no tailwind.config.* file exists in the repo."
    - "tsconfig.json declares strict: true; every resulting compiler error is either fixed inline (trivial ≤1-line casts) or suppressed with a numbered // @ts-expect-error NP-STRICT-<n>: <reason> marker; a new verify:phase-1 grep assertion fails the build if the marker count exceeds the declared ceiling."
    - "immer is ^11 and zod is ^4 in package.json dependencies (bumped from ^10.1.1 / ^3.24.0)."
    - {statement: "StandaloneWorkspace.tsx and WorkspaceSidebar.tsx render with no visible layout collapse after the Tailwind plugin is removed — every Tailwind utility className touched by Phase 1 has an AntD-token or inline-style equivalent.", verification: backstop}
  artifacts:
    - wxt.config.ts (permissions array reduced to 3 entries)
    - tsconfig.json (strict: true)
    - package.json (immer ^11, zod ^4, no tailwind deps)
  key_links:
    - "wxt.config.ts manifest.permissions -> chrome runtime API surface actually exercised by src/ + entrypoints/ (verified 0 references to the 6 removed permissions this session)"
    - "tsconfig.json strict:true -> every .ts/.tsx file in src/ + entrypoints/ (NP-STRICT ceiling grep gate in package.json verify:phase-1)"
  prohibitions:
    - requirement_id: REQ-R21
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT re-add cookies, scripting, contextMenus, notifications, or alarms as manifest-declared (required) permissions in a later phase — Phase 17+ re-additions must use chrome.permissions.request()/optional_permissions at point of use, never a blanket manifest re-add, or this phase's least-privilege work regresses silently."
    - requirement_id: REQ-R19
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT leave a Phase-1-touched component visually broken (unstyled, collapsed layout) after the Tailwind plugin is removed without a human visual check — Tailwind utility classNames degrade silently (dead no-op strings), not as a build failure, so the grep gate passing is not sufficient proof of visual correctness."
---

<objective>
Land the three build-config-level decisions that are independent of the messaging tracer but must both precede the store-strip work in Plan 01-04 and precede any further UI edits: least-privilege manifest permissions (D-19a, REQ-R21), removal of the spec-forbidden Tailwind scaffold leftover (D-18, REQ-R19), and the stack-version bump plus `strict: true` enablement with its bounded escape hatch (D-20/D-21).

Purpose: `wxt.config.ts` currently declares 9 permissions when only 3 are exercised (`sidePanel`, `storage`, `tabs` — verified this session via grep), `tailwindcss`/`@tailwindcss/vite` are still in `package.json` devDependencies (the Phase-1 grep gate FAILS today), and `tsconfig.json` still has `strict: false`. All three are declarative/config changes with no functional dependency on the messaging tracer from Plan 01-02, but the `strict: true` sweep must run AFTER the tracer's new files (`BackgroundRouter.ts`, the renamed content script) exist, so the NP-STRICT ceiling accounts for all Phase-1-created code rather than being computed against a stale tree.

Output: `wxt.config.ts` with a 3-entry permissions array; `package.json`/`src/index.css` with zero Tailwind traces; `tsconfig.json` with `strict: true`; a bounded, grep-enforced `NP-STRICT-<n>` marker ceiling; `immer` ^11 and `zod` ^4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-CONTEXT.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-RESEARCH.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-UI-SPEC.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Least-privilege manifest permissions (D-19a)</name>
  <files>wxt.config.ts</files>
  <read_first>
    Read `wxt.config.ts` in full (68 lines; `manifest.permissions` at lines 31-40 currently lists
    9 entries: `sidePanel, storage, cookies, alarms, tabs, scripting, contextMenus, notifications,
    declarativeNetRequest`). This session's grep confirmed zero references to `chrome.cookies`,
    `chrome.alarms`, `chrome.scripting`, `chrome.contextMenus`, `chrome.notifications`, or
    `chrome.declarativeNetRequest` anywhere in `src/` or `entrypoints/` — do not re-verify this,
    it is already confirmed; just make the removal.
  </read_first>
  <action>
    Reduce `manifest.permissions` in `wxt.config.ts` to exactly `['sidePanel', 'storage', 'tabs']`.
    Leave `host_permissions`, `action`, `icons`, `side_panel`, `options_ui`, `options_page`, and
    `content_security_policy` untouched — D-19a's deviation from Appendix G applies only to the
    `permissions` array. Do not add `unlimitedStorage` (that is Phase 2, per ADR-STACK-02) and do
    not add `optional_permissions` yet (Phase 17 introduces the `chrome.permissions.request()`
    flow for `cookies`/`scripting`/`contextMenus` when the ServiceNow/Selection-Ask-AI features
    that need them actually ship).
  </action>
  <verify>
    <automated>grep -A6 "permissions:" wxt.config.ts | grep -c "'sidePanel'\|'storage'\|'tabs'"</automated>
  </verify>
  <done>wxt.config.ts manifest.permissions contains exactly sidePanel, storage, tabs.</done>
  <acceptance_criteria>The automated grep returns 3; `grep -c "cookies\|alarms\|scripting\|contextMenus\|notifications\|declarativeNetRequest" wxt.config.ts` returns 0.</acceptance_criteria>
  <reversibility rating="reversible">Declarative permission array; each dropped entry is re-added at its owning phase per D-19a's own table.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Tailwind-removal visual non-regression (Standalone view, light+dark)</name>
  <what-built>Removed the Tailwind CSS plugin, its `@import`, and the devDependency from the two Phase-1-touched components that use Tailwind utility classNames most heavily (`StandaloneWorkspace.tsx`, `WorkspaceSidebar.tsx`), converting their className strings to AntD tokens / inline styles so the removal does not silently collapse the layout.</what-built>
  <how-to-verify>
    Run `pnpm build:ext` then load `.output/chrome-mv3` as an unpacked extension in Chrome. Open
    the Standalone view (right-click the extension icon's context menu is not needed — open
    `standalone.html` directly via `chrome-extension://<id>/standalone.html`). Confirm: (1) the
    left Sider renders at its expected width bands (not collapsed to 0 or stretched full-width),
    (2) the workspace card's rounded-corner panel and border are visible, not flush/unstyled,
    (3) toggling the Sider collapse control still animates/resizes visibly, (4) both light and
    dark mode (toggle via the Cmd+K "Toggle Theme" command) render with readable text contrast.
  </how-to-verify>
  <resume-signal>Type "approved" once the Standalone view renders correctly in both light and dark mode, or describe the specific visual regression you see.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove Tailwind scaffold leftover (D-18) and convert the two touched components</name>
  <files>package.json, wxt.config.ts, src/index.css, src/components/standalone/StandaloneWorkspace.tsx, src/components/standalone/WorkspaceSidebar.tsx</files>
  <read_first>
    Read `wxt.config.ts` line 1-2 and 14-15 (`import tailwindcss from '@tailwindcss/vite'` and
    `plugins: [tailwindcss() as any]`). Read `src/index.css` line 1 (`@import "tailwindcss"` — read
    the full file to see what else it declares before editing). Read `package.json` devDependencies
    (`@tailwindcss/vite: ^4.3.3`, `tailwindcss: ^4.3.3`). Read `src/components/standalone/StandaloneWorkspace.tsx`
    in full (61 lines — every visual style is a Tailwind `className` string, e.g. line 22's
    `"h-full w-full flex overflow-hidden bg-[#eceef0] dark:bg-zinc-950 font-sans"`). Read
    `src/components/standalone/WorkspaceSidebar.tsx` in full (301 lines, entirely Tailwind-className-driven
    per 01-PATTERNS.md — the single heaviest conversion burden in Phase-1 scope; current widths are
    `w-16`/`w-[230px]`, not the DESIGN_SYSTEM §8.2 72/240px target — this task normalizes them to
    72px/240px inline `style` values while converting, closing that drift in the same pass).
  </read_first>
  <action>
    Remove the `@tailwindcss/vite` import and `plugins: [tailwindcss() as any]` entry from
    `wxt.config.ts` (leave the rest of the `vite: () => ({...})` block, including
    `chunkSizeWarningLimit`/`rollupOptions`, untouched). Delete line 1 (`@import "tailwindcss"`)
    from `src/index.css`; check for and delete any `tailwind.config.*` file at the repo root
    (confirm with `ls tailwind.config.*` first — none may exist, in which case this sub-step is a
    no-op). Remove `tailwindcss` and `@tailwindcss/vite` from `package.json` devDependencies.
    Convert every Tailwind utility `className` in `StandaloneWorkspace.tsx` and
    `WorkspaceSidebar.tsx` to inline `style` objects or AntD `theme.useToken()` values (matching
    the pattern already used correctly elsewhere in this codebase, e.g.
    `SidepanelChat.tsx`'s `const { token } = theme.useToken()`), normalizing the Sider's
    collapsed/expanded widths to exactly `72`/`240` px per DESIGN_SYSTEM §8.2 in the same edit. Do
    not touch any other component's Tailwind classNames in this task — components outside this
    plan's `files_modified` list keep their Tailwind classes inert (unstyled but not
    build-breaking) until their own owning phase converts them; that is an accepted, documented
    gap, not a regression this plan must fix.
  </action>
  <verify>
    <automated>grep -c "tailwind\|shadcn\|@radix-ui" package.json; grep -c "framer-motion" package.json; grep -c "@import \"tailwindcss\"" src/index.css</automated>
  </verify>
  <done>package.json has zero tailwind/shadcn/@radix-ui/framer-motion references; src/index.css no longer imports tailwindcss; the two touched components render via inline styles/AntD tokens instead of Tailwind utility classes.</done>
  <acceptance_criteria>All three grep counts above return 0; `pnpm build` (tsc --noEmit && vite build) succeeds with no new errors attributable to this task.</acceptance_criteria>
  <reversibility rating="costly">Every CSS-bearing file this task touches changes; visual regression risk is real (Tailwind classes degrade silently to no-ops, not build errors) — mitigated by the preceding checkpoint:human-verify task, run before this task's commit is treated as final.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Stack bump (immer/zod) + strict:true sweep with bounded NP-STRICT ceiling</name>
  <files>package.json, tsconfig.json, package.json (verify:phase-1 script)</files>
  <read_first>
    Read `package.json` dependencies (`immer: ^10.1.1`, `zod: ^3.24.0`) and `tsconfig.json` in
    full (currently `strict: false` at line 8; `include`/`exclude` already cover `src/`,
    `entrypoints/`, `tests/`, and the WXT-generated `.wxt/wxt.d.ts`). Read the current
    `verify:phase-1` script in `package.json` (`"tsc --noEmit && vitest run tests/core/runtime
    tests/core/events tests/core/workspace tests/core/theme"` — this glob is narrower than the
    full `tests/` tree; RESEARCH.md flags this as a gap that must be widened once the Wave-0 test
    directories from later plans (`tests/background`, `tests/components`, `tests/core/storage`,
    `tests/isolation`) exist — do not widen it in THIS task since those directories do not all
    exist yet; a later plan's task will do the final widen once every directory is populated).
  </read_first>
  <action>
    Bump `immer` to `^11` and `zod` to `^4` in `package.json` dependencies; run `pnpm install` and
    resolve any immediate breaking-API fallout from the major bumps (immer 11's stricter
    prototype-pollution guards, zod 4's schema-API renames) by grepping every `zod`/`immer` import
    site under `src/` and adjusting call sites to the v11/v4 API — this is expected to be a small
    surface since the codebase's zod/immer usage is limited to store-shape validation and the
    zustand `immer()` middleware wrapper. Set `tsconfig.json`'s `strict: true`. Run `tsc --noEmit`
    and triage the resulting error list into two buckets: (a) trivial ≤1-line fixes (an implicit
    `any` parameter that just needs a type annotation, a nullable access that needs an optional
    chain) — fix these inline; (b) genuinely structural gaps (the `wxt.config.ts` tailwind-plugin
    `as any` cast — already removed by Task 2 of this plan so this specific instance is now moot
    — WXT-generated `.wxt` shims, third-party typing gaps) — suppress each with a sequential
    `// @ts-expect-error NP-STRICT-<n>: <reason>` comment directly above the offending line, where
    `<n>` increments per file and `<reason>` is a short noun phrase naming the blocking gap. Add a
    new grep-based assertion (as its own `vitest` test file, e.g.
    `tests/core/strict/np-strict-ceiling.test.ts`) that counts `NP-STRICT-` markers across `src/`
    and `entrypoints/` and fails if the count exceeds the ceiling recorded as a constant in that
    same test file — set the ceiling to the exact count remaining after this task's cheap-fix
    sweep (do not pad it). Record the ceiling value in `.planning/STATE.md`'s watch-items list as
    the Phase 2-3 NP-STRICT reduction target (this STATE.md edit is documentation only, not a
    frontmatter-tracked file for this plan).
  </action>
  <verify>
    <automated>tsc --noEmit && pnpm vitest run tests/core/strict</automated>
  </verify>
  <done>tsconfig.json has strict:true; tsc --noEmit passes cleanly (errors are either fixed or suppressed with numbered NP-STRICT markers); the ceiling test passes against the exact post-sweep marker count; immer is ^11 and zod is ^4.</done>
  <acceptance_criteria>`grep '"strict": true' tsconfig.json` matches; `tsc --noEmit` exits 0; `grep -rc "NP-STRICT-" src/ entrypoints/` is less than or equal to the ceiling constant in `tests/core/strict/np-strict-ceiling.test.ts`; `grep '"immer": "\^11' package.json` and `grep '"zod": "\^4' package.json` both match.</acceptance_criteria>
  <reversibility rating="costly">Touches every TypeScript file that had a structural typing gap; the strict-mode enable is effectively one-way going forward (Phase 2-3 reduces the ceiling to 0, never re-widens it) — reversible via git revert only while no later Phase-1 plan has landed on top.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

This plan's contribution to the phase-wide artifact inventory:

- `wxt.config.ts`'s `manifest.permissions` reduced to `['sidePanel', 'storage', 'tabs']`.
- Zero-Tailwind `package.json`/`src/index.css`; `StandaloneWorkspace.tsx`/`WorkspaceSidebar.tsx` converted to AntD tokens/inline styles.
- `tsconfig.json`'s `strict: true`; the `// @ts-expect-error NP-STRICT-<n>: <reason>` suppression convention; `tests/core/strict/np-strict-ceiling.test.ts` (the grep-enforced ceiling test).
- `immer` `^11`, `zod` `^4` in `package.json`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest permission surface -> Chrome Web Store review | A least-privilege manifest reduces both attack surface and reviewer scrutiny for a privacy-first extension |
| Build config (wxt.config.ts, package.json) -> shipped bundle | Removing a forbidden UI framework (Tailwind) enforces the spec's explicit dependency allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Elevation of Privilege | wxt.config.ts manifest.permissions | high | mitigate | Reduced from 9 to 3 declared permissions, matching verified actual usage (0 references to the 6 removed permissions across src/+entrypoints/ this session) |
| T-01-09 | Tampering | strict:true NP-STRICT suppression markers | medium | mitigate | Ceiling is grep-enforced in its own test file, not a documentation-only promise; any future PR that adds a suppression without justification fails the ceiling test |
| T-01-SC | Tampering | immer ^11 / zod ^4 install | medium | mitigate | Both packages already audited in 01-RESEARCH.md's Package Legitimacy Audit table (canonical publishers, no SLOP/genuine-SUS verdicts); this task installs the exact versions already approved there, no new checkpoint required |

</threat_model>

<verification>
`tsc --noEmit` passes; `pnpm vitest run tests/core/strict` passes; the three grep gates (tailwind/shadcn/@radix-ui, framer-motion, permissions array) all return their expected zero/three counts; the human-verify checkpoint for the Tailwind-removal visual check is approved before this plan's final commit.
</verification>

## Task Breakdown

## Task 01-03-01 — Least-privilege manifest
**Requirements:** REQ-R21 · driver **D-19a** · **Threat ref:** T-01-07 · **`autonomous`: true**

### Objective
Reduce `wxt.config.ts` `manifest.permissions` to exactly `['sidePanel','storage','tabs']`, preserving all other manifest fields.

### Authoritative references
- D-19a (CONTEXT, single source of truth for the permission set); REQ-R21; spec §0.2 + Appendix G (manifest shape).
- `01-PATTERNS.md`: current `permissions:` array in `wxt.config.ts` includes `cookies, alarms, scripting, contextMenus, notifications, declarativeNetRequest`.

### Files
- **Modify:** `wxt.config.ts`

### Ordered implementation steps
1. **Re-grep (VAI-08):** locate the `permissions:` array in `wxt.config.ts`.
2. Set it to exactly `permissions: ['sidePanel', 'storage', 'tabs']`.
3. **Preserve verbatim** every other manifest field: `content_security_policy`, `host_permissions`, `optional_host_permissions`, `side_panel`, `action`, `web_accessible_resources`, and any `manualChunks` build config (Appendix G).

### Edge / error behavior
- Removing `scripting`/`contextMenus`/etc. must not break Phase-1 code (none of it uses those APIs; Phase 17 will use `chrome.permissions.request()` at runtime instead of manifest-required).

### Async ownership
- Config-only; no runtime behavior.

### Exact tests + observable assertions
- `grep -A6 "permissions:" wxt.config.ts` shows exactly the 3 entries and none of the 6 removed.
- The 6 removed permissions do not appear anywhere in `wxt.config.ts` `manifest.permissions`.

### Verification command + expected result
`grep -A8 "permissions:" wxt.config.ts` → exactly `['sidePanel','storage','tabs']` (VALIDATION `01-03-01`). Build still succeeds: `pnpm build` (or `wxt build`) exits 0.

### Prohibitions / non-goals
- MUST NOT re-add any of the 6 removed permissions as manifest-required in ANY later phase.
- MUST NOT touch `host_permissions`/`optional_host_permissions` (Phase 17 owns host-permission strategy).

---

## Task 01-03-02 — Remove Tailwind + convert touched components  [autonomous: false — blocking human visual check]
**Requirements:** REQ-R19 · driver **D-18** · **Threat ref:** T-01-11 · **`autonomous`: false**

### Objective
Remove the Tailwind toolchain and convert every Tailwind className touched by Phase 1 (in `StandaloneWorkspace.tsx` + `WorkspaceSidebar.tsx`) to AntD tokens / on-scale inline styles, with a blocking human visual pass in light + dark.

### Authoritative references
- D-18 (CONTEXT); REQ-R19; spec §0.2 (Tailwind/shadcn/@radix-ui banned) + STACK §35 (`motion ^12` retained); `01-UI-SPEC.md` (spacing/typography scale + visual anchors); `DESIGN_SYSTEM.md` §6/§13 (token usage).
- `01-PATTERNS.md`: `@tailwindcss/vite` in `wxt.config.ts`; `@import "tailwindcss"` in `src/index.css`; Tailwind utility classNames concentrated in `WorkspaceSidebar.tsx` (heaviest) and `StandaloneWorkspace.tsx`.

### Files
- **Modify:** `wxt.config.ts` (remove `@tailwindcss/vite` plugin)
- **Modify:** `package.json` (remove `tailwindcss`, `@tailwindcss/vite`)
- **Modify:** `src/index.css` (remove `@import "tailwindcss"`; delete any `tailwind.config.*`)
- **Modify:** `src/components/standalone/StandaloneWorkspace.tsx`
- **Modify:** `src/components/standalone/WorkspaceSidebar.tsx`

### Ordered implementation steps
1. **Re-grep (VAI-08):** find the `@tailwindcss/vite` import/plugin entry, the `@import "tailwindcss"` line, any `tailwind.config.{js,ts,cjs}`, and enumerate every Tailwind className in the two components.
2. Remove the plugin from `wxt.config.ts`; remove the `@import "tailwindcss"` from `src/index.css`; delete `tailwind.config.*` if present.
3. `pnpm remove tailwindcss @tailwindcss/vite`. Keep `motion` (`^12`) — do NOT remove it.
4. **Convert classNames → AntD tokens / inline styles** for every Phase-1-touched element. Rules:
   - Spacing (padding/margin/gap) only from `{4,8,16,24,32}` px; `1–2px` only for borders/strokes.
   - Font sizes only from `{12,13,14,16}`; weights `{400,600}`.
   - Colors from `theme.useToken()` (e.g., `token.colorBgContainer`, `token.colorBorder`, `token.colorTextBase`) — no hardcoded hex.
   - Prefer AntD layout components (`Flex`, `Space`) over ad-hoc flex utilities where a 1:1 mapping exists.
5. Ensure no element silently loses styling (a removed className that collapsed layout must be replaced, not dropped).

### Edge / error / empty / overflow behavior
- Converted `WorkspaceSidebar` must preserve its collapsed/expanded widths and overflow scrolling (verify visually).
- Long workspace names must still truncate/ellipsis as before (do not regress overflow handling).
- Dark mode: all converted surfaces must use tokens so dark theme renders correctly (no hardcoded light colors).

### Async ownership
- Static styling change; no async.

### Exact tests + observable assertions
- `grep -E 'tailwind|shadcn|@radix-ui' package.json` → **0**.
- `grep 'framer-motion' package.json` → **0** (and `motion` remains present).
- `grep -rE '(padding|margin|gap):\s*(2px|12px|20px)' src/components/standalone/` → **0** (UI-SPEC spacing gate).
- `grep -rE 'fontSize:\s*(11|20|24|28|30)' src/components/standalone/` → **0** (UI-SPEC typography gate).
- **BLOCKING human visual pass:** load-unpacked, open the Standalone view in **light and dark**; confirm the sidebar + workspace render with correct spacing, no unstyled/collapsed regions, correct token colors. (grep passing is necessary but NOT sufficient.)

### Verification command + expected result
`grep -E 'tailwind|shadcn|@radix-ui' package.json` → 0; UI-SPEC greps → 0; human visual pass recorded (VALIDATION `01-03-checkpoint`). Build succeeds: `pnpm build` exits 0.

### Prohibitions / non-goals
- MUST NOT remove `motion` (`^12`).
- MUST NOT leave dead/unstyled classNames that collapse layout.
- MUST NOT introduce off-scale spacing or font sizes.
- MUST NOT convert components outside the two Phase-1-touched files (others are converted in their owning waves).

---

## Task 01-03-03 — `strict:true` + immer/zod bump + NP-STRICT sweep + ceiling gate
**Requirements:** REQ-R19 (type-safety) · drivers **D-20, D-21** · **Threat ref:** T-01-13 · **`autonomous`: true**

### Objective
Enable `strict:true`, bump immer `^11` / zod `^4`, apply all ≤1-line strict fixes, suppress only genuine structural residue with numbered `NP-STRICT-` markers, record the marker count as `NP_STRICT_CEILING`, and gate it with a test.

### Authoritative references
- D-20 (strict), D-21 (immer/zod) — CONTEXT; spec §7.3 (immer), §7.4 (zod), §7.8 (strict).
- `01-PATTERNS.md`: `tsconfig.json` currently lacks `strict`; several call sites need optional chaining once `activeSession` becomes nullable (that nullability is introduced in 01-04, so account for the current tree only here).

### Files
- **Modify:** `tsconfig.json` (`"strict": true`)
- **Modify:** `package.json` (immer `^11`, zod `^4`, `NP_STRICT_CEILING`)
- **Create:** `tests/core/strict/np-strict-ceiling.test.ts`
- **Modify:** `src/**`, `entrypoints/**` — ≤1-line strict fixes only

### Exact signatures / contract shapes
```jsonc
// package.json — single source of truth for the ceiling (M6)
"NP_STRICT_CEILING": 0    // set to the actual residual count from Step 5
```
```ts
// tests/core/strict/np-strict-ceiling.test.ts (ILLUSTRATIVE, NON-AUTHORITATIVE)
import pkg from '../../../package.json';
// count = number of 'NP-STRICT-' occurrences across src/ + entrypoints/
expect(count).toBeLessThanOrEqual(pkg.NP_STRICT_CEILING);
```

### Ordered implementation steps (the ordered discovery — finding M6)
1. Set `"strict": true` in `tsconfig.json`.
2. `pnpm add immer@^11 zod@^4`; run `pnpm install`.
3. Run `tsc --noEmit`; capture the full diagnostic list.
4. Fix every diagnostic resolvable in **≤1 line** (optional chaining, non-null where provably safe, explicit param types, narrowing). Do NOT refactor multi-line logic.
5. For each remaining **structural** diagnostic that cannot be fixed in ≤1 line, suppress with a numbered marker: `// @ts-expect-error NP-STRICT-<n>: <one-line reason>`. Number sequentially from 1.
6. Count residual markers: `N = $(grep -rc 'NP-STRICT-' src/ entrypoints/ | awk -F: '{s+=$2} END{print s}')`.
7. Write `"NP_STRICT_CEILING": N` in `package.json` (**single location** — not STATE.md).
8. Create `tests/core/strict/np-strict-ceiling.test.ts` that reads `NP_STRICT_CEILING` from `package.json` and fails if the live marker count exceeds it.

### Edge / error behavior
- **Ceiling = 0 case:** if the ≤1-line sweep clears everything, set `NP_STRICT_CEILING: 0` and the test asserts zero markers.
- **No `@ts-ignore`:** only numbered `@ts-expect-error NP-STRICT-<n>` is permitted; bare `as any` outside a numbered marker is prohibited.
- **H4 hand-off:** record in the plan that waves 4–8 must not raise `N`; 01-08 re-asserts it.

### Async ownership
- Build/config change; no runtime async.

### Exact tests + observable assertions (`tests/core/strict/np-strict-ceiling.test.ts`)
- `tsc --noEmit` exits 0 (all diagnostics either fixed or NP-STRICT-suppressed).
- Live `grep -rc 'NP-STRICT-' src/ entrypoints/` count ≤ `NP_STRICT_CEILING`.
- Injecting an unnumbered `as any` that raises a new strict error would fail `tsc` (self-consistency — optional negative check).

### Verification command + expected result
`tsc --noEmit && pnpm vitest run tests/core/strict` → green; count ≤ ceiling (VALIDATION `01-03-03`).

### Prohibitions / non-goals
- MUST NOT use `@ts-ignore`.
- MUST NOT use bare `as any` outside a numbered `NP-STRICT-` marker.
- MUST NOT store the ceiling in two places (single source: `package.json`).
- MUST NOT pre-emptively change `activeSession` nullability here (that is 01-04's D-11 change).

<success_criteria>
Manifest is least-privilege (3 permissions); Tailwind is fully removed with no visual regression in the two touched components; strict:true is active with a bounded, test-enforced NP-STRICT ceiling; immer/zod are bumped to the spec-mandated major versions.
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace-handoff/01-03-SUMMARY.md` when done.
</output>
