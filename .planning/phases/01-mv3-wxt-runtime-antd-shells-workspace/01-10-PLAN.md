---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 10
type: execute
wave: 1
depends_on: []              # Executes against the already-landed 01-01…01-09 baseline (verify:phase-1 green); do NOT touch the 9 existing plans
gap_closure: true
files_modified:
  - src/entrypoints/sidepanel/main.tsx
  - src/entrypoints/standalone/main.tsx
  - tests/entrypoints/sidepanel.test.tsx
  - tests/entrypoints/standalone.test.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RUNTIME-01, RUNTIME-02, RUNTIME-03, WSPC-01, WSPC-02]
must_haves:
  truths:
    - "Both entrypoint mounts (src/entrypoints/sidepanel/main.tsx, src/entrypoints/standalone/main.tsx) fire the workspace lifecycle after ThemeStore hydrate, per REVIEW WR-03: useWorkspaceStore.getState().init() → getState().start('<surface>') → the module-scope workspaceSync ref .start(), one chain per surface — per-file call-site counts (code lines only): each of useWorkspaceStore.getState().init, getState().start(, new WorkspaceSync( appears exactly once in sidepanel/main.tsx and exactly once in standalone/main.tsx (header-comment rewrites cannot inflate per-file counts)."
    - "useAddonSettingsStore.getState().init() is called at both entrypoint mounts (grep == 2), per REVIEW WR-02; the D-06 'Configure later' escape persists across surface loads: np_addon_settings { onboarding: { done: true } } seeded in storage → a fresh module evaluation hydrates settings.onboarding.done === true → SidePanelRouter renders the shell instead of the OnboardingModal (behavioral test green)."
    - "np_workspace flows at mount: after the module-scope wiring resolves, useWorkspaceStore.isReady is true, workspace.activeSurface matches the mounted surface ('sidepanel' | 'standalone'), and workspace.version >= 1 (start() bumped it and wrote the D-18 active fields through the storage adapter) — asserted by the mount smoke tests."
    - "The full Phase 1 gate stays green with the wiring in place: pnpm verify:phase-1 exits 0; existing tests/entrypoints, tests/core/workspace, tests/core/registry suites pass unchanged; REQUIREMENTS.md Phase-1 traceability rows (RUNTIME-01…05, WSPC-01…05) flipped to Done."
  artifacts:
    - src/entrypoints/sidepanel/main.tsx (mount wiring: addon-settings hydrate + workspace init/start/sync)
    - src/entrypoints/standalone/main.tsx (same, surface 'standalone')
    - tests/entrypoints/sidepanel.test.tsx (wiring assertions + onboarding round-trip)
    - tests/entrypoints/standalone.test.tsx (wiring assertions + hydration assertion)
    - .planning/REQUIREMENTS.md (traceability rows reconciled)
  key_links:
    - "Entrypoint module scope → useWorkspaceStore.init() → np_workspace hydration + onChanged LWW listener; start() writes the D-18 active fields back to storage."
    - "Entrypoint module scope → useAddonSettingsStore.init() → np_addon_settings hydration; SidePanelRouter reads settings.onboarding.done live (T-1-18) to decide Onboarding vs shell."
    - "WorkspaceSync.start() → broadcastBus.startHeartbeat + WORKSPACE_UPDATED subscription + bridge subscribe — the cross-surface sync loop the 01-06 plan promised but never activated."
flagged_assumptions:
  - statement: "RUNTIME-01 — edge probe 'unclassified — review manually' (spec-less fallback): unresolved. FLAGGED ASSUMPTION, not a must-have truth. The 01-10 gate task re-runs verify:phase-1 (build + 4 entrypoints), which keeps RUNTIME-01's shipped evidence intact."
    verification: flagged-unverified
  - statement: "RUNTIME-02 — edge probe 'unclassified — review manually': unresolved. FLAGGED ASSUMPTION. Onboarding gate behavior is covered by existing entrypoint tests; the D-06 persistence clause is closed by this plan's behavioral round-trip test."
    verification: flagged-unverified
  - statement: "RUNTIME-03 — edge probe 'unclassified — review manually': unresolved. FLAGGED ASSUMPTION. Tab dedupe is behavior-tested (01-06); the handoff state machine becomes live with this plan's WorkspaceSync wiring; real-browser PING/PONG election remains a human-verification item (host lacks Chrome libs)."
    verification: flagged-unverified
  - statement: "RUNTIME-04 — edge probe 'unclassified — review manually': unresolved. FLAGGED ASSUMPTION. Theme behavior unchanged by 01-10 (mount code only); ThemeStore suite + existing entrypoint tests remain the evidence."
    verification: flagged-unverified
  - statement: "RUNTIME-05 — edge probe 'unclassified — review manually': unresolved. FLAGGED ASSUMPTION. Page skeletons unchanged by 01-10; existing component/entrypoint suites remain the evidence."
    verification: flagged-unverified
  - statement: "WSPC-01 — edge probe 'unclassified — review manually': unresolved. FLAGGED ASSUMPTION. This plan wires WorkspaceStore init/start at the mounts (the failed truth); the storage adapter round-trip stays covered by WorkspaceStore.test.ts."
    verification: flagged-unverified
  - statement: "WSPC-02 — edge probe 'unclassified — review manually': unresolved. FLAGGED ASSUMPTION. This plan activates WorkspaceSync at the mounts (the failed truth); heartbeat/LWW/handoff unit coverage remains in WorkspaceSync.test.ts."
    verification: flagged-unverified
  - statement: "WSPC-03 — edge probe 'unclassified — review manually': unresolved. FLAGGED ASSUMPTION. Messaging layer unchanged by 01-10; WR-01 logging rewiring is addressed by 01-11."
    verification: flagged-unverified
  - statement: "WSPC-04 idempotency probe — 'What happens if this runs twice on the same input?': unresolved. FLAGGED ASSUMPTION. In-scope guards: store init() is idempotent (isReady + remove-then-add onChanged listener), WorkspaceSync.start() re-entry is guarded by the stopped flag, BroadcastBus.startHeartbeat is stop-first."
    verification: flagged-unverified
  - statement: "WSPC-04 concurrency probe — 'If interrupted or run in parallel, what is guaranteed?': unresolved. FLAGGED ASSUMPTION. The init().then(start) sequencing is the prescribed ordering; LWW (version) governs concurrent writes. First-ever simultaneous boot of both surfaces (before either writes np_workspace) may briefly diverge workspaceIds — residual edge, carried in the 01-10 threat model."
    verification: flagged-unverified
  - statement: "WSPC-05 — edge probe 'unclassified — review manually': unresolved. FLAGGED ASSUMPTION. debugLog canonical-code usage is extended by 01-11's WR-01 rewiring; component evidence unchanged by 01-10."
    verification: flagged-unverified
prohibitions: []
---

<objective>
Close the two FAILED verification truths of Phase 1 with pure entrypoint-mount wiring (REVIEW.md WR-02 + WR-03 fixes): (1) activate the workspace lifecycle — useWorkspaceStore.init() → start('<surface>') → WorkspaceSync.start() — at both entrypoint mounts so np_workspace hydrates, activeSurface reflects the mounted surface, and the cross-surface sync loop (heartbeat / WORKSPACE_UPDATED LWW / handoff state machine / mirroring) actually runs in the shipped extension; (2) call useAddonSettingsStore.init() at both mounts so the D-06 'Configure later' escape persists across surface loads. Then prove both closures with mount-level behavioral tests and re-verify the whole phase gate.

Purpose: The phase gate (verify:phase-1) is green and 8/10 requirements hold, but the phase goal's core "share" clauses — surfaces share workspace state (SC2) and onboarding persists (goal 'share onboarding') — are dead code in the shipped extension: WorkspaceStore.init/start and WorkspaceSync.start have ZERO production callers (grep-verified). This plan restores the 01-06 plan's own intent ('01-08 shells and 01-09 mounts consume it').
Output: 2 entrypoint mounts wired, 2 test files extended, REQUIREMENTS.md traceability reconciled, verify:phase-1 re-green.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VERIFICATION.md (gaps[0] workspace wiring, gaps[1] onboarding persistence)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-REVIEW.md (WR-02, WR-03 — the exact fixes this plan implements)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md (conventions only — status fields are validate-phase-owned, W-13; do NOT edit)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-06, D-07, D-09, D-18)
@.planning/PRODUCT_SPEC_v0_1.md (Flow 9 line ~1702, Flow 11 line ~1710, Appendix M lines ~5729)

# Baseline: plans 01-01…01-09 are EXECUTED and verify:phase-1 is green. This gap-closure plan is ADDITIVE — it must not change any existing plan's acceptance criteria or must_haves. Requirement coverage: RUNTIME-04/RUNTIME-05/WSPC-04 remain owned by the executed plans 01-05/01-07/01-08/01-09; this plan claims the IDs its tasks genuinely touch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire workspace lifecycle + onboarding hydration at both entrypoint mounts (WR-02 + WR-03)</name>
  <files>
    src/entrypoints/sidepanel/main.tsx, src/entrypoints/standalone/main.tsx
  </files>
  <read_first>
    src/entrypoints/sidepanel/main.tsx (the file being modified — note the existing module-scope block at the bottom)
    src/entrypoints/standalone/main.tsx (the file being modified)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VERIFICATION.md (gaps section: the exact missing wiring is quoted in gaps[0].missing and gaps[1].missing)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-REVIEW.md (WR-02 lines 113-128, WR-03 lines 130-149 — the fixes)
    src/core/workspace/WorkspaceStore.ts (API: init/start signatures; NP_WORKSPACE_KEY; isReady flag)
    src/core/workspace/WorkspaceSync.ts (constructor signature: new WorkspaceSync(surface: ActiveSurface))
    src/core/registry/AddonSettingsStore.ts (API: init() signature)
  </read_first>
  <action>
    In BOTH src/entrypoints/sidepanel/main.tsx and src/entrypoints/standalone/main.tsx:

    1. Add three imports next to the existing useThemeStore import:
       import { useWorkspaceStore } from '@/core/workspace/WorkspaceStore';
       import { WorkspaceSync } from '@/core/workspace/WorkspaceSync';
       import { useAddonSettingsStore } from '@/core/registry/AddonSettingsStore';
       (Import paths are aliased — the existing entrypoint imports already use the '@/...' alias, so no tsconfig change.)

     2. In the module-scope block guarded by `if (typeof document !== 'undefined')`, IMMEDIATELY AFTER the existing `void useThemeStore.getState().init();` line (do not reorder or replace it), add:
        - `void useAddonSettingsStore.getState().init();`  (WR-02 — hydrate np_addon_settings so onboarding.done persists)
        - a module-scope workspace-sync ref (WR-03, per the review fix's 'keep a module-level ref for stop()'): `const workspaceSync = new WorkspaceSync('sidepanel');` in sidepanel/main.tsx (surface literal 'standalone' in standalone/main.tsx) — the WorkspaceSync constructor is side-effect-free (it only assigns fields); only start() activates subscriptions/timers, so constructing before hydration completes is safe.
        - the workspace lifecycle chain (WR-03): `void useWorkspaceStore.getState().init().then(() => { useWorkspaceStore.getState().start('sidepanel'); workspaceSync.start(); });` in sidepanel/main.tsx, and the identical chain with surface literal 'standalone' and its own module-scope ref in standalone/main.tsx.

    3. Do NOT: change the theme isReady null-gate in SidePanelRoot/StandaloneRoot, add a second XProvider/ConfigProvider (Appendix F one-per-surface is a phase fixture), modify the mod+k useEffect capture, modify the createRoot call, or import any content-script module (Pitfall 4 — entrypoints never import content modules).

     4. Update each entrypoint's top header comment: note that the mount now also hydrates the addon-settings store and starts the workspace store/sync per the 01-10 gap closure (WR-02/WR-03) — keep the existing notes about Appendix F/BLOCKER 2 intact. Do NOT write the literal call expressions (useWorkspaceStore.getState().init, getState().start(, new WorkspaceSync(, useAddonSettingsStore.getState().init) into the header comments — describe them by concept — so the per-file call-site grep counts stay unambiguous.

    The chain must match the VERIFICATION.md gaps[0].missing prescription in shape (init().then(start; sync.start())) with the WorkspaceSync instance held in the module-scope ref declared in step 2 — the .then ordering is REQUIRED so start() never runs before hydration completes. The prescribed wiring is pure entrypoint mount code: no new contracts, no MessageType additions, no chrome.* calls outside the existing stores/bus (extension-context rules preserved: AI + IndexedDB stay out of the background SW; content scripts stay extraction-only).
  </action>
  <verify>
    <automated>set -o pipefail; pnpm tsc --noEmit 2>&1 | tail -5 && grep "useWorkspaceStore.getState().init" src/entrypoints/sidepanel/main.tsx | grep -vc "://" && grep "useWorkspaceStore.getState().init" src/entrypoints/standalone/main.tsx | grep -vc "://" && grep "getState().start(" src/entrypoints/sidepanel/main.tsx | grep -vc "://" && grep "getState().start(" src/entrypoints/standalone/main.tsx | grep -vc "://" && grep "new WorkspaceSync(" src/entrypoints/sidepanel/main.tsx | grep -vc "://" && grep "new WorkspaceSync(" src/entrypoints/standalone/main.tsx | grep -vc "://" && grep "useAddonSettingsStore.getState().init" src/entrypoints/sidepanel/main.tsx | grep -vc "://" && grep "useAddonSettingsStore.getState().init" src/entrypoints/standalone/main.tsx | grep -vc "://"</automated>
  </verify>
  <done>
    Both mounts fire the workspace lifecycle (init → start('<surface>') → the module-scope WorkspaceSync ref .start()) and the addon-settings hydration after ThemeStore hydrate; per-file call-site counts: each of useWorkspaceStore.getState().init, getState().start(, new WorkspaceSync(, useAddonSettingsStore.getState().init appears exactly once in sidepanel/main.tsx and exactly once in standalone/main.tsx; tsc green.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep "useWorkspaceStore.getState().init" src/entrypoints/sidepanel/main.tsx | grep -vc "://"` returns 1 and `grep "useWorkspaceStore.getState().init" src/entrypoints/standalone/main.tsx | grep -vc "://"` returns 1 (one call per entrypoint — per-file scope, header comments cannot inflate the counts)
    - `grep "getState().start(" src/entrypoints/sidepanel/main.tsx | grep -vc "://"` returns 1 and `grep "getState().start(" src/entrypoints/standalone/main.tsx | grep -vc "://"` returns 1 ('sidepanel' + 'standalone')
    - `grep "new WorkspaceSync(" src/entrypoints/sidepanel/main.tsx | grep -vc "://"` returns 1 and `grep "new WorkspaceSync(" src/entrypoints/standalone/main.tsx | grep -vc "://"` returns 1 (the module-scope ref, one per entrypoint)
    - `grep "useAddonSettingsStore.getState().init" src/entrypoints/sidepanel/main.tsx | grep -vc "://"` returns 1 and `grep "useAddonSettingsStore.getState().init" src/entrypoints/standalone/main.tsx | grep -vc "://"` returns 1 (one per entrypoint)
    - `grep "\.start('sidepanel')" src/entrypoints/sidepanel/main.tsx | grep -vc "://"` returns 1 and `grep "\.start('standalone')" src/entrypoints/standalone/main.tsx | grep -vc "://"` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount-wiring behavioral tests — prove the two closed truths</name>
  <files>
    tests/entrypoints/sidepanel.test.tsx, tests/entrypoints/standalone.test.tsx
  </files>
  <read_first>
    tests/entrypoints/sidepanel.test.tsx (the file being modified — note the beforeEach resets and the static import of '@/entrypoints/sidepanel/main')
    tests/entrypoints/standalone.test.tsx (the file being modified)
    tests/setup.ts (fakeBrowser reset per test; document is defined in jsdom so the module-scope wiring fires at import time)
    src/core/registry/AddonSettingsStore.ts (NP_ADDON_SETTINGS_KEY = 'np_addon_settings', settings.onboarding.done shape)
    src/core/workspace/WorkspaceStore.ts (NP_WORKSPACE_KEY = 'np_workspace', isReady/activeSurface/version fields)
  </read_first>
  <action>
    Extend BOTH entrypoint test files (keep every existing assertion unchanged — they must continue to pass):

    sidepanel.test.tsx — add two tests:
    (a) 'workspace lifecycle fires at module scope': render createSidePanelApp, then await waitFor(() => expect(useWorkspaceStore.getState().isReady).toBe(true)) (import waitFor and useWorkspaceStore at the top of the file); assert workspace.activeSurface === 'sidepanel' and workspace.version >= 1 (start() bumped the version from 0).
    (b) 'onboarding round-trips through np_addon_settings on a fresh module load' (the D-06 persistence closure): inside the test body, seed `await chrome.storage.local.set({ np_addon_settings: { onboarding: { done: true } } })` (the chrome global is the wxt fakeBrowser stub — same binding WorkspaceStore.test.ts uses); then `vi.resetModules()`; then dynamic-import the fresh module instances: `const { createSidePanelApp: FreshApp } = await import('@/entrypoints/sidepanel/main');` and `const { useAddonSettingsStore: FreshAddon } = await import('@/core/registry/AddonSettingsStore');`; assert FreshAddon.getState().settings.onboarding?.done === true (the fresh module's module-scope init() hydrated the seeded storage) AND render FreshApp and assert the OnboardingModal heading is NOT shown and the shell header IS shown (STR.onboarding.heading absent; 'NowPilot' present).

    standalone.test.tsx — add two tests:
    (a) 'workspace lifecycle fires at module scope': await waitFor(useWorkspaceStore isReady); assert workspace.activeSurface === 'standalone' and workspace.version >= 1.
    (b) 'addon settings hydrate from np_addon_settings on a fresh module load': seed np_addon_settings { onboarding: { done: true } }, vi.resetModules(), dynamic-import '@/entrypoints/standalone/main' + '@/core/registry/AddonSettingsStore'; assert settings.onboarding?.done === true.

    Notes: vi.resetModules() is required because the static top-of-file import already executed the module-scope wiring against EMPTY storage at file load; the dynamic import re-evaluates the module so the init() chain reads the seeded storage. The dynamic tests use the FRESH store instances (FreshAddon / the reset-module store) — do not assert on the statically-imported store inside those tests. All new tests use the existing fakeBrowser + RTL patterns (waitFor/findBy*). Do not modify tests/setup.ts or any test outside tests/entrypoints/.
  </action>
  <verify>
    <automated>set -o pipefail; pnpm vitest run tests/entrypoints 2>&1 | tail -8</automated>
  </verify>
  <done>
    tests/entrypoints green with the new assertions: workspace isReady/activeSurface/version proven at both mounts; onboarding.done proven to round-trip through np_addon_settings on a fresh module load (sidepanel gate behavior + both surfaces' hydration).
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/entrypoints` exits 0 (all existing + new tests)
    - `grep "workspace.activeSurface" tests/entrypoints/sidepanel.test.tsx | grep -vc "://"` >= 1 and `grep "workspace.activeSurface" tests/entrypoints/standalone.test.tsx | grep -vc "://"` >= 1
    - `grep -c "np_addon_settings" tests/entrypoints/sidepanel.test.tsx` >= 1 (round-trip test present)
    - `grep -c "settings.onboarding" tests/entrypoints/sidepanel.test.tsx` >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase gate re-green + REQUIREMENTS.md traceability reconciliation</name>
  <files>
    .planning/REQUIREMENTS.md (touch ONLY the two Phase-1 range rows; src/* touched only if the gate catches a regression)
  </files>
  <read_first>
    .planning/REQUIREMENTS.md (lines 180-181 are the stale range rows; lines 12-24 are the detail section with [x] marks — leave those as-is)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md (reference only — do NOT edit status/nyquist_compliant/wave_0_complete fields, W-13)
  </read_first>
  <action>
     1. Run the full Phase 1 gate: `pnpm verify:phase-1` (eslint → prettier --check → tsc --noEmit → wxt build → vitest run → node tests/isolation/check-content-bundle.mjs) must exit 0 with the Task 1 wiring and Task 2 tests in place. If it fails, fix the regression at its source with small targeted edits (prefer the entrypoint/test files owned by this plan); if a structural defect surfaces, STOP and report — do not rewrite anything outside this plan's files_modified. This full-gate run (~2 min) is the MANDATED phase gate and the accepted exception to the 30 s feedback-latency sampling threshold (nyquist_8b) — the fast targeted suites (tsc, tests/entrypoints) ran inside Tasks 1-2 for iteration; this task runs the full gate ONCE as the final check.
    2. Reconcile the REQUIREMENTS.md traceability rows (lines 180-181) with the Edit tool (scoped replacement, never a whole-file write): row `| RUNTIME-01…05 | Phase 1 | In Progress (RUNTIME-01, RUNTIME-02, RUNTIME-03, RUNTIME-04 done) |` → status `Done` (the row omits RUNTIME-05 — the flipped row must reflect all five IDs, either by listing them all or by the compact `Done` status); row `| WSPC-01…05 | Phase 1 | In Progress (WSPC-01, WSPC-02, WSPC-03, WSPC-04, WSPC-05 done) |` → status `Done`. Do NOT touch the detail-section [x] marks (lines 12-24), any other phase row, or the coverage note. The VERIFICATION.md requirement-coverage table (which shows all 10 Phase-1 IDs satisfied after this plan) is the source of truth for the flip.
    3. Record the gate run + reconciliation result in this plan's SUMMARY only (01-10-SUMMARY.md) — per W-13, do NOT edit 01-VALIDATION.md status fields.
  </action>
  <verify>
    <automated>set -o pipefail; pnpm verify:phase-1 2>&1 | tail -12 && grep -n "RUNTIME-01…05" .planning/REQUIREMENTS.md && grep -n "WSPC-01…05" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    `pnpm verify:phase-1` exits 0; both Phase-1 range rows in REQUIREMENTS.md show status Done; no other REQUIREMENTS.md row or the detail section changed; VALIDATION.md untouched.
  </done>
  <acceptance_criteria>
    - `pnpm verify:phase-1` exits 0
    - `grep -n "RUNTIME-01…05" .planning/REQUIREMENTS.md` shows `| RUNTIME-01…05 | Phase 1 | Done |` (or a row whose status is Done and covers all five IDs)
    - `grep -n "WSPC-01…05" .planning/REQUIREMENTS.md` shows `| WSPC-01…05 | Phase 1 | Done |`
    - `grep -c "\[x\]" .planning/REQUIREMENTS.md` is unchanged from the pre-task value (detail section untouched)
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

**Modified files:**
- `src/entrypoints/sidepanel/main.tsx` — new imports (useWorkspaceStore, WorkspaceSync, useAddonSettingsStore); module-scope wiring after the theme hydrate: `void useAddonSettingsStore.getState().init();` + module-scope ref `const workspaceSync = new WorkspaceSync('sidepanel');` + `void useWorkspaceStore.getState().init().then(() => { useWorkspaceStore.getState().start('sidepanel'); workspaceSync.start(); });`
- `src/entrypoints/standalone/main.tsx` — same wiring with surface literal `'standalone'` and its own module-scope `workspaceSync` ref
- `tests/entrypoints/sidepanel.test.tsx` — 2 new tests (workspace lifecycle fires at module scope; onboarding round-trips through np_addon_settings on fresh module load via vi.resetModules + dynamic import)
- `tests/entrypoints/standalone.test.tsx` — 2 new tests (workspace lifecycle; addon-settings hydration on fresh module load)
- `.planning/REQUIREMENTS.md` — traceability rows 180-181 flipped to Done (scoped Edit only)

**Symbols / call sites created or activated:**
- Call sites (production): `useAddonSettingsStore.getState().init()` ×2, `useWorkspaceStore.getState().init()` ×2, `useWorkspaceStore.getState().start('sidepanel'|'standalone')` ×2, `new WorkspaceSync('sidepanel'|'standalone')` ×2 held in module-scope `workspaceSync` refs (`.start()` called inside the init().then chain) — the WR-02/WR-03 wiring that activates the already-built WorkspaceStore/WorkspaceSync/AddonSettingsStore in the shipped extension.

**No new test files** — the existing entrypoint smoke tests are extended.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → store hydration | np_workspace / np_addon_settings are written by extension contexts; any extension context can write them — hydration must validate before merge |
| entrypoint module scope → async init chain | The mount fires fire-and-forget init chains; a race (start before hydrate, double-start on re-import) must not corrupt workspace state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-10-01 | Tampering | np_workspace / np_addon_settings hydration at the mounts | medium | mitigate | Both stores already apply the T-1-13 sanitizer (sanitizeStored) before merge and the onChanged LWW version gate; the wiring only CALLS the existing init() — no new merge path is introduced (WorkspaceStore.ts:60-79, AddonSettingsStore.ts:36-46) |
| T-1-10-02 | DoS / race | init().then(start) sequencing + double-start | low | mitigate | The .then ordering (WR-03 prescription) guarantees start() never runs before hydration; init() is idempotent (isReady + remove-then-add onChanged listener); WorkspaceSync.start() re-entry guarded by its stopped flag; BroadcastBus.startHeartbeat is stop-first |
| T-1-10-03 | Spoofing | Surface claim in start('<surface>') | low | accept | The surface is a compile-time literal per mount ('sidepanel' in sidepanel/main.tsx, 'standalone' in standalone/main.tsx) and the store boundary is the ActiveSurface union type; no user-controllable input reaches the argument |
| T-1-10-04 | Elevation of privilege | Entire wiring surface | low | accept | Pure entrypoint mount code — no new permissions (manifest untouched), no new MessageType values (Pitfall 5), no new chrome.* calls outside the existing stores/bus; AI + IndexedDB remain out of the background SW (R-3), content scripts remain extraction-only (R-5) |
</threat_model>

<verification>
- `pnpm verify:phase-1` green (full §24 chain) — re-run with the wiring in place
- grep hygiene (per-file scope, code lines only): useWorkspaceStore.getState().init == 1 per entrypoint file, getState().start( == 1 per entrypoint file, new WorkspaceSync( == 1 per entrypoint file, useAddonSettingsStore.getState().init == 1 per entrypoint file (sidepanel/main.tsx + standalone/main.tsx)
- `pnpm vitest run tests/entrypoints` green (existing + new assertions)
- REQUIREMENTS.md rows 180-181 flipped to Done; 01-VALIDATION.md untouched (W-13)
- Outstanding HUMAN-verification items carried from 01-VERIFICATION.md (unchanged by this plan, executable only on a host with Chrome system libs — the CfT binary fails with missing libnspr4.so): real-browser mount of sidepanel.html/standalone.html, action-button → side panel gesture, standalone tab dedupe in real windows, cross-window PING/PONG election + LWW adoption (now that the wiring exists), theme propagation across two live surfaces, real keyboard focus flow.
</verification>

<success_criteria>
- Failed truth 1 closed: WorkspaceStore.init/start + WorkspaceSync.start have exactly 2 production callers (one per entrypoint mount); np_workspace hydrates and the cross-surface sync loop runs in the shipped extension
- Failed truth 2 closed: useAddonSettingsStore.init() called at both mounts; onboarding.done round-trips through np_addon_settings (behavioral test)
- Phase gate re-green with no regression to the 9 executed plans' acceptance criteria or must_haves
- REQUIREMENTS.md traceability reconciled
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-10-SUMMARY.md` when done
</output>
