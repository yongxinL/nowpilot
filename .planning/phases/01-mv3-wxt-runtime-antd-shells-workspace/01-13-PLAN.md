---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 13
type: execute
wave: 6
depends_on: [01-02, 01-03, 01-04, 01-05, 01-06, 01-07, 01-08, 01-09, 01-10, 01-11, 01-12]
files_modified:
  - tests/isolation/banned-imports.test.ts
  - package.json
  - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md
autonomous: false
requirements: [CORE-01]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The repository has a repo-level banned-import gate covering all three pattern groups named in the phase's success criterion 5 — the unsafe-HTML injection pair, the banned styling and primitive libraries, and the banned motion package — each as its own failing case."
    - "The gate has teeth: for each pattern group a scratch file containing the pattern makes the gate fail, and deleting the scratch file makes it green again."
    - "The gate also inspects the dependency manifest for the banned packages, so declaring one is a failure even when no source file imports it."
    - "The gate permits the approved motion library: only the banned motion package name fails, and the approved alternative used by the design system does not."
    - "`verify:phase-1` runs the §24 minimum path set and every suite this phase added. A phase-owned suite that the gate does not run does not exist as far as acceptance is concerned."
    - "`verify:phase-1` includes the new service-level suite directory, so the fixture-adapter and port suites are gated rather than merely present."
    - "`verify:phase-1` includes the manifest inspection suite and the marking audit suites, so a build-artifact drift or a missing page marker fails the phase gate rather than passing unnoticed."
    - "The full suite is green and its size exceeds the pre-phase baseline, because a suite count that did not grow is not evidence of new coverage."
    - "The manual criteria are recorded with an observed result, not a screenshot alone: a screenshot without an observed-result record is not evidence."
    - "All five of the phase's success criteria are demonstrably true, and the validation strategy document's sign-off block is completed."
    - "{ statement: 'The phase gate is reproducible from a clean checkout: after removing build output and reinstalling from the lockfile, `verify:phase-1` still passes end to end.', verification: backstop }"
    - "{ statement: 'No suite in the gate passes vacuously: each scan-based case reports a non-zero scanned-item count, so a gate that stops covering anything fails rather than silently passing.', verification: backstop }"
  artifacts:
    - "tests/isolation/banned-imports.test.ts — the three-group banned-import and dependency gate"
    - "package.json — the realigned `verify:phase-1` composition"
    - "01-VALIDATION.md — completed sign-off block with the manual observed results"
  key_links:
    - "`verify:phase-1` path list → every suite this phase added: a suite outside the list is invisible to acceptance."
    - "The banned-import gate → the build: the gate is the only automated defence against the three banned pattern groups re-entering the source or the dependency manifest."
    - "The manual evidence records → the five success criteria: the browser-chrome criteria cannot be proven in jsdom and must be observed in real Chrome."
  prohibitions:
    - "The phase gate must not be narrowed to make it pass: removing a suite path, lowering a threshold or raising the strictness ceiling to reach green is prohibited."
    - "A banned pattern must not be permitted by narrowing the gate's scan scope to a directory that happens to be clean."
    - "The manual criteria must not be signed off from a unit test alone; where the behaviour depends on the browser extension runtime, only a real Chrome observation counts."
    - "No success criterion may be marked met while a Wave 0 suite from the validation strategy is still absent."
---

<objective>
Gate the phase: add the missing banned-import gate, compose `verify:phase-1` so it actually runs everything this phase built, and collect the real-browser evidence the five success criteria require.

Purpose: success criterion 5 is `pnpm run verify:phase-1` passing with the banned-import greps at zero — but the phase gate currently runs only the suites that existed before this phase, and only one of the three banned pattern groups has a script today. A gate that omits the new coverage is worse than no gate, because it reports green. Separately, four of the five success criteria depend on browser-chrome behaviour — the side panel opening, the chord working on two operating systems, the cross-surface handoff and theme propagation, and the gesture-gated panel open — which jsdom cannot prove and which the project's verification discipline requires be observed rather than asserted.
Output: a three-group banned-import and dependency gate with proven teeth, a `verify:phase-1` composition that runs the §24 minimum plus every new suite, and a recorded real-Chrome evidence set for the manual criteria.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md
@.planning/ROADMAP.md
@package.json
@scripts/verify-no-tailwind.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three-group banned-import and dependency gate with proven teeth</name>
  <files>tests/isolation/banned-imports.test.ts</files>
  <read_first>
    - scripts/verify-no-tailwind.sh (the existing single-group gate this one complements, and the reason its scan scope must stay in step with this one)
    - package.json (the dependency manifest the gate also inspects)
    - .planning/ROADMAP.md (Phase 1 success criterion 5 — the three named pattern groups)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md § A9 (the approved motion library stays permitted; only the banned one fails) and § S9
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md (the Wave 0 gap naming this gate)
  </read_first>
  <action>
Create `tests/isolation/banned-imports.test.ts` with three independent groups, each its own case so a failure names the group:

1. **Unsafe HTML injection.** Scan every source file under `src/` for the two unsafe-HTML constructs the spec bans and assert zero occurrences. Strip comments before matching, using the same comment-stripping helper the isolation suite uses, so a comment that merely names the construct does not trip the gate and a real usage cannot hide behind one.
2. **Banned styling and primitive libraries.** Scan `src/` for imports of the banned styling framework, the banned component-copy layer and the banned primitive library namespace, and separately read `package.json` and assert none of those package names appears in dependencies or devDependencies. A declared-but-unimported banned package must still fail.
3. **Banned motion package.** Scan `src/` and `package.json` for the banned motion package name and assert zero. Assert explicitly that the approved motion library the design system permits is **not** matched, so the gate cannot be widened by accident into rejecting an approved dependency.

Each scan case must report and assert a non-zero count of scanned files, so a scan whose target path stops resolving fails instead of passing vacuously.

Prove the gate has teeth before committing: for each group, create a scratch source file containing that group's pattern, run the suite, confirm the corresponding case fails and names the group, then delete the scratch file and confirm the suite is green. Also inject a banned dependency name into `package.json` temporarily, confirm group 2 fails, then restore. Record the observed results in the commit body.

Keep the file read-only against the repository — it scans and asserts, and writes nothing.
  </action>
  <verify>
    <automated>npx vitest run tests/isolation/banned-imports.test.ts</automated>
    <fails_when>exit code is non-zero, or any of the three group cases is reported failed or skipped, or a scan case reports zero scanned files</fails_when>
    <automated>npx vitest run tests/isolation tests/core/strict &amp;&amp; npx tsc --noEmit</automated>
    <fails_when>any suite fails, or typecheck exits non-zero, or any output line contains `error TS`</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/isolation/banned-imports.test.ts` exists with three independent group cases plus the approved-library non-match assertion.
    - Every scan case asserts a non-zero scanned-file count.
    - The unsafe-HTML case strips comments before matching.
    - The dependency check reads `package.json` and fails on a declared banned package even with no source import.
    - The commit body records, per group, the observed failing run against a deliberate violation and the observed green run after restoring.
    - The suite writes nothing to the repository.
  </acceptance_criteria>
  <done>The three banned pattern groups are gated in source and in the dependency manifest, with each group proven to fail against a deliberate violation.</done>
</task>

<task type="auto">
  <name>Task 2: Compose `verify:phase-1` so the gate runs everything this phase built</name>
  <files>package.json</files>
  <read_first>
    - package.json (the file being modified — the current phase gate composition)
    - .planning/product/PRODUCT_SPEC.md §24 (the Phase-1 minimum path set the gate must keep)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md (the Wave 0 suite list and the "every new suite must be added to the gate" rule)
    - scripts/verify-no-tailwind.sh (the one bash gate the composition still calls)
  </read_first>
  <action>
Rewrite the phase gate in `package.json` so it runs, in this order: the typecheck, then the §24 Phase-1 minimum path set (the five core directories named in the spec), then every suite directory this phase added or extended — the two surface-shell suites and the palette, onboarding, marker and page suites under the component tree; the workspace, theme, runtime, events, commands, input, onboarding, storage and strictness suites under the core tree; the background suites; the isolation suites including the manifest inspection and the banned-import gate; the new service-level suite directory; and the existing banned-styling script.

Keep the gate as an explicit path list rather than a bare whole-suite invocation, so a future suite must be added deliberately and a path that stops resolving is visible. Verify that every path in the list resolves by running the gate twice: once as composed, and once with a deliberately misspelled path to confirm the failure is loud rather than silent. Restore the correct path and confirm green.

Confirm the suite count and the test count both exceed the pre-phase baseline of eighteen files and one hundred and sixty-six tests; a gate that grew its path list but not its coverage is a composition error, not progress.

Do not remove or narrow any existing path, and do not raise the strictness ceiling in the same file to reach green.
  </action>
  <verify>
    <automated>pnpm run verify:phase-1</automated>
    <fails_when>exit code is non-zero, or any vitest path in the command reports a failure, or the run reports fewer test files or tests than the pre-phase baseline of eighteen files and one hundred and sixty-six tests</fails_when>
    <automated>node -e "const s=require('./package.json').scripts['verify:phase-1'];const req=['tests/core/runtime','tests/core/events','tests/core/workspace','tests/core/theme','tests/background','tests/components','tests/isolation','tests/services','tests/core/input','scripts/verify-no-tailwind.sh'];const miss=req.filter(p=>!s.includes(p));console.log(miss.length?('MISSING:'+miss.join(',')):'COMPLETE')"</automated>
    <fails_when>the output is anything other than `COMPLETE`</fails_when>
    <automated>node -e "console.log(require('./package.json').NP_STRICT_CEILING)"</automated>
    <fails_when>the printed value is not `0`</fails_when>
  </verify>
  <acceptance_criteria>
    - `verify:phase-1` runs the typecheck, the §24 minimum path set, every suite this phase added or extended, the service-level suite directory, and the banned-styling script.
    - Every path in the command resolves; a deliberately misspelled path was observed to fail loudly before being restored.
    - The gate is an explicit path list, not a bare whole-suite invocation.
    - `NP_STRICT_CEILING` is still `0`.
    - The run reports more test files and more tests than the pre-phase baseline.
    - No pre-existing suite path was removed or narrowed.
  </acceptance_criteria>
  <done>`verify:phase-1` composites every suite this phase built, keeps the §24 minimum, and is proven to fail loudly on a bad path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-Chrome evidence and phase acceptance</name>
  <files>.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md § Manual-Only Verifications (the five criteria and their instructions)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md § Phase 1 Surface Contracts (the geometry to compare against while observing)
    - .planning/ROADMAP.md (Phase 1's five success criteria)
    - .opencode/skills/nowpilot-phase-verification/SKILL.md (the observed-result rule)
  </read_first>
  <what-built>
The phase's runtime, both surface shells, the shared onboarding shell, the command palette and its keymap binding, the theme contract, the workspace handoff, the marker convention and the credential strip. The unit and component suites cannot prove any of the following, because they depend on the real Chrome extension runtime: the side panel opening, the operating-system chord reaching the page, two live surfaces propagating a storage change, the gesture-gated side-panel open, and a stored plaintext credential being deleted and reported neutrally.
  </what-built>
  <how-to-verify>
Load the freshly built extension unpacked in a real Chrome profile and record an **observed result** for each item. A screenshot without an observed-result record is not evidence.

1. **Side panel and first-run onboarding.** In a fresh profile, click the extension action. Record: the side panel opens; onboarding presents in the panel; all four steps render; the key field masks its value; validation runs against the fixture and reaches both a success and a failure state. Then confirm no tab and no panel opened automatically during install.
2. **The chord on both operating systems.** On macOS press the meta-key chord; on Windows or Linux press the control-key chord. Record the observed result on each surface for each platform: the palette opens, the pinned command set renders, the dev-only command is absent from this production build, and pressing the chord again closes the palette.
3. **Handoff, focus and theme propagation.** From the side panel run `Open Standalone view`. Record: Standalone opens; running it again focuses the existing tab and creates no second one; the composer draft typed in the panel arrives in Standalone; theme toggled from the palette updates both surfaces with no reload; the side panel stays writable and shows no read-only banner.
4. **Gesture-gated panel open.** From Standalone run `Focus Side Panel`. Record whether the panel opens on the current tab, and if it does not, record the exact error surfaced.
5. **Legacy credential cleanup.** Seed a legacy plaintext credential into the extension's local storage, reload the extension, and record: the neutral notice appears once and is dismissible; the stored field is gone; no part of the value is displayed anywhere; a second reload shows no notice and no re-appearance of the field.
6. **Deferred and fixture marking.** Open each Standalone page and record whether it shows a deferred or preview panel naming its owning phase, with no skeleton and no enabled control that would need a later-phase service.

Record the observed result for each item in the validation document's manual-evidence table, replacing the placeholder instructions with the actual observation. If an item fails, do not edit the criterion: fix the defect in the owning plan and re-plan if the fix is out of scope.
  </how-to-verify>
  <resume-signal>Type "approved" with the six observed results recorded, or describe what failed and on which item.</resume-signal>
  <acceptance_criteria>
    - Each of the six items has an observed result recorded in the validation document — not a screenshot reference, and not a restatement of the criterion.
    - The `## Validation Sign-Off` block's five checkboxes are completed truthfully, including "Wave 0 covers all MISSING references" and "no three consecutive tasks without automated verify".
    - `nyquist_compliant: true` in the validation frontmatter is set only after every Wave 0 suite from the gap list exists and runs inside the phase gate.
    - Any item that failed is either fixed by its owning plan and re-observed, or recorded as an open gap with an owning plan id.
    - The phase's five success criteria are each marked met with the evidence that supports them.
  </acceptance_criteria>
  <done>Six observed real-Chrome results are recorded, the validation sign-off is completed truthfully, and every success criterion is either met with evidence or recorded as an open gap with an owner.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase gate → acceptance claim | The gate is the only automated statement that the phase is done; a gate that omits coverage reports false green. |
| banned pattern gate → source and manifest | The gate is the last automated defence against a banned construct re-entering the codebase or the dependency list. |
| browser runtime → recorded evidence | Only a real Chrome observation can prove the browser-chrome criteria; a screenshot without an observed result proves nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-65 | Tampering | Phase gate composition | high | mitigate | The gate keeps an explicit path list, a required-path probe asserts all ten required paths are present, and a deliberately misspelled path was observed to fail loudly before being restored. |
| T-1-66 | Repudiation | Vacuous gate | high | mitigate | Every scan-based case asserts a non-zero scanned-item count and the gate's suite and test counts are compared against the pre-phase baseline, so a gate that stops covering anything fails. |
| T-1-67 | Tampering | Banned constructs re-entering | high | mitigate | The three-group gate checks source and the dependency manifest, strips comments before matching, and each group was proven to fail against a deliberate violation. |
| T-1-68 | Spoofing | Manual criteria signed off from unit tests | high | mitigate | The checkpoint forbids signing off browser-chrome behaviour from a jsdom test and requires an observed result per item. |
| T-1-69 | Elevation of privilege | Gate narrowed to reach green | high | mitigate | Removing a suite path, lowering a threshold or raising the strictness ceiling is prohibited; the required-path probe and the ceiling assertion make each a red check. |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install occurs in this plan; the gate inspects the dependency manifest and fails on a banned package name, including one added without a source import. |
</threat_model>

<verification>
- `pnpm run verify:phase-1` is green and reports more files and tests than the pre-phase baseline.
- The required-path probe prints `COMPLETE`.
- `NP_STRICT_CEILING` is `0`.
- `tests/isolation/banned-imports.test.ts` is green with three group cases and non-zero scan counts.
- Six observed real-Chrome results are recorded in the validation document.
</verification>

<success_criteria>
- The three banned pattern groups are gated in source and in the dependency manifest, with proven teeth.
- `verify:phase-1` runs the §24 minimum plus every suite this phase added.
- The five phase success criteria are each backed by evidence, with browser-chrome criteria observed in real Chrome.
- The validation strategy's sign-off block is completed truthfully.
</success_criteria>

## Artifacts this phase produces

| Kind | Symbol / path | Created by |
|------|---------------|------------|
| Test | `tests/isolation/banned-imports.test.ts` — three-group source and dependency gate | Task 1 |
| Config | `package.json` `verify:phase-1` realigned composition | Task 2 |
| Evidence | Six observed real-Chrome results plus the completed sign-off block in `01-VALIDATION.md` | Task 3 |

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-13-SUMMARY.md` when done
</output>
