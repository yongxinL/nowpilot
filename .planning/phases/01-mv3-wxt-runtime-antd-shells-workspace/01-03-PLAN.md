---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/core/messaging/MessageBus.ts
  - src/core/messaging/MessageBusBridge.ts
  - src/core/events/EventBus.ts
  - src/core/events/EventBusManager.ts
  - tests/core/events/EventBus.test.ts
  - tests/core/messaging/MessageBus.test.ts
  - tests/core/messaging/MessageBusBridge.test.ts
autonomous: true
requirements: [RUNTIME-02, WSPC-03]
must_haves:
  truths:
    - "App Messages are the ONLY cross-context interface (UI-SPEC: all contexts send Messages through the content-script Host interface; no direct imports) — established as a language-level guard: cross-file imports of surface modules from core are build-time errors (Rule R-4 established, enforcement at module level via the bridge boundary)."
    - "MessageBus wraps wxt fakeBrowser runtime events for tests AND background port broadcasts for runtime (port.enableEmitter) — same code path, no test-only branches."
    - "EventBus has subscribe/emit/unsubscribe, typed events, subscribeToScope, and error handling with optional stack param."
    - "MessageBusBridge (bridge wrapper for MessageBus) exposes exactly: connect, disconnect, publish, subscribe, addMessageListener, removeMessageListener, getNetworkStatus (contract from Appendix C/§19.2)."
    - "No callbacks inside .postMessage() calls (Pitfall 1 — this plan has none, guarded at 01-06); BackgroundAction is a typed const object (APP_COMMAND_OPEN_SIDE_PANEL, APP_COMMAND_OPEN_STANDALONE) shared via src/core/messaging/BackgroundActions.ts as literal strings."
  artifacts:
    - src/core/messaging/MessageBus.ts
    - src/core/messaging/MessageBusBridge.ts
    - src/core/events/EventBus.ts
    - src/core/events/EventBusManager.ts
    - tests/core/events/EventBus.test.ts
    - tests/core/messaging/MessageBus.test.ts
    - tests/core/messaging/MessageBusBridge.test.ts
  key_links:
    - "MessageBusBridge consumes RuntimeEnvelope + MessageType from 01-02 — the whitelist guards every message (Pitfall 5)."
    - "EventBusManager dispatches events per surface scope (sidepanel/standalone/background) using the same EventBus instance; 01-05/01-06 subscribe here."
  flagged_assumptions:
    - statement: "RUNTIME-02 messaging — MessageBus uses wxt fakeBrowser runtime events in vitest; real background port broadcasts verified via fakeBrowser wiring, not a real browser."
      verification: flagged-unverified
    - statement: "WSPC-03 envelope transport — request/response verified here at the unit level; cross-surface transport integration deferred to browser e2e (RESEARCH A8)."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Build the messaging and event infrastructure: MessageBus (wxt runtime events + background port broadcasts), MessageBusBridge (the Appendix C/§19.2 bridge contract), EventBus with typed events and scoped subscription, and EventBusManager.

Purpose: All cross-context communication in the phase flows through this layer. Without it, content scripts, side panel, standalone, and background cannot exchange Messages — and every later plan subscribes here.
Output: 4 source files + 3 Zod fixture test files.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-17, D-16, D-13)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 5, validation map line ~537)
@.planning/PRODUCT_SPEC_v0_1.md (Appendix C line ~4137; §19.2 line ~2808; Appendix E line ~5019)

# Depends on 01-01 (wxt ^0.19 pin, vitest config) and 01-02 (RuntimeEnvelope, MessageType). No prior SUMMARYs yet.
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | No sidePanel code — delegated to 01-06 |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | No surface imports here — MessageBus is dependency-free core |
| P5 | Message contracts bypassing RuntimeEnvelope | **GUARDED HERE** — MessageBusBridge only accepts RuntimeEnvelope-shaped messages; MessageTypeValues whitelist rejects unknown types |
| P6 | matchMedia-in-jsdom | Handled in 01-01 |
| P7 | zustand persist for cross-surface | No persist here — delegated to 01-05/01-06 |

<tasks>

<task type="auto">
  <name>Task 1: EventBus + EventBusManager (typed event system)</name>
  <files>
    src/core/events/EventBus.ts, src/core/events/EventBusManager.ts, tests/core/events/EventBus.test.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix E (lines 5033-5039) — event definitions
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (events/bus, lines 158-175)
  </read_first>
  <action>
    Create src/core/events/EventBus.ts exporting a typed EventBus class: constructor(events: readonly string[]) with subscribe(event, handler), emit(event, data), unsubscribe(event, handler); subscribeToScope(scope: 'sidepanel'|'standalone'|'background', handler) returning an unsubscribe fn; emit returns boolean; error handling wraps handler calls in try/catch and calls debugLog with code EVT_HANDLER (see 01-04) — never let one handler break the loop. Event types are declared as a const events array (SHOW_HANDOFF_PENDING, SHOW_HANDOFF_COMPLETE, WORKSPACE_SYNC_START, WORKSPACE_SYNC_COMPLETE, WORKSPACE_MIRRORING_START, WORKSPACE_MIRRORING_STOP, WORKSPACE_ELECTION_FAILED, SIDEPANEL_OPENED, STANDALONE_OPENED, NOTE_SAVE, THEME_CHANGED, NETWORK_STATUS_CHANGED) matching Appendix E event names. Create src/core/events/EventBusManager.ts as a singleton wrapper: getEventBus() returning the shared instance (lazy-init). Write tests/core/events/EventBus.test.ts: subscribe/emit delivers data; unsubscribe stops delivery; subscribeToScope delivers only scoped events; one handler throwing does not break the next (error caught).
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/events/EventBus.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    EventBus supports subscribe/emit/unsubscribe/subscribeToScope; handler errors caught and routed to debugLog EVT_HANDLER (import deferred — debugLog lands in 01-04, so guard with typeof check); EventBusManager singleton works; EventBus tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/events/EventBus.test.ts` exits 0
    - `grep -c "subscribeToScope" src/core/events/EventBus.ts` == 1
    - `grep -c "SHOW_HANDOFF_PENDING\|WORKSPACE_SYNC_START\|THEME_CHANGED" src/core/events/EventBus.ts` == 3
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: MessageBus (wxt runtime + port bridge)</name>
  <files>
    src/core/messaging/MessageBus.ts, tests/core/messaging/MessageBus.test.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix E (lines 5019-5032) — message registry
    .planning/PRODUCT_SPEC_v0_1.md §19.2 (lines 2808-2824) — App Messages
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 5 lines 383-387)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (messaging, lines 177-206)
  </read_first>
  <action>
    Create src/core/messaging/MessageBus.ts exporting a MessageBus class that wraps BOTH transport paths: (1) wxt runtime events via browser.runtime.onMessage.addListener / browser.runtime.sendMessage (tests use wxt/testing fakeBrowser), and (2) background port broadcasts via chrome.runtime.connect({name:'np-port'}) + port.postMessage + port.onMessage.addListener, including port.enableEmitter for wxt-safe streams. Expose methods: connect(portName), publish(message: RuntimeEnvelope), subscribe(callback), broadcastToPorts(message), disconnect(). Use the MessageTypeValues whitelist from 01-02 to reject messages with unknown types before dispatch (throw Error with code MSG_UNKNOWN_TYPE — §C.2 code list, see 01-04). Do NOT import any surface module (React/antd/zustand) — MessageBus is the dependency-free core (Pitfall 4). Write tests/core/messaging/MessageBus.test.ts using wxt fakeBrowser: sendMessage delivers a RuntimeEnvelope-shaped message to a listener; publish() drops messages with non-whitelist types; port broadcast delivers to the connected port listener.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/messaging/MessageBus.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    MessageBus wraps runtime events AND port broadcasts; unknown message types rejected via whitelist; fakeBrowser tests green for sendMessage, publish, broadcastToPorts.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/messaging/MessageBus.test.ts` exits 0
    - `grep -c "MessageTypeValues" src/core/messaging/MessageBus.ts` == 1
    - `grep -c "runtime.onMessage\|runtime.sendMessage\|connect" src/core/messaging/MessageBus.ts` >= 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: MessageBusBridge (cross-context bridge wrapper)</name>
  <files>
    src/core/messaging/MessageBusBridge.ts, tests/core/messaging/MessageBusBridge.test.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md §19.2 (lines 2808-2824) — App Messages contract
    .planning/PRODUCT_SPEC_v0_1.md Appendix C (lines 4141-4154) — envelope contract
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md D-16, D-17
  </read_first>
  <action>
    Create src/core/messaging/MessageBusBridge.ts exporting class MessageBusBridge wrapping MessageBus with the contract: connect(), disconnect(), publish(message), subscribe(callback), addMessageListener(listener), removeMessageListener(listener), getNetworkStatus(): Promise<boolean>. It accepts ONLY RuntimeEnvelope-shaped messages (type-checked at compile + whitelist at runtime from 01-02). This is the single choke point every surface (sidepanel, standalone, content host) imports — surfaces NEVER import MessageBus directly (Rule R-4). Write tests/core/messaging/MessageBusBridge.test.ts: publish→subscribe roundtrip delivers; addMessageListener/removeMessageListener work; getNetworkStatus resolves to navigator.onLine.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/messaging/MessageBusBridge.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    MessageBusBridge exposes the full §19.2 contract (7 methods); surfaces import only MessageBusBridge; roundtrip + listener + network tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/messaging/MessageBusBridge.test.ts` exits 0
    - `grep -c "addMessageListener\|removeMessageListener\|getNetworkStatus" src/core/messaging/MessageBusBridge.ts` == 3
    - `grep -rn "from '@/core/messaging/MessageBus'" src/core/messaging/MessageBusBridge.ts | wc -l` == 1 (only the bridge wraps MessageBus)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content page → bridge | Untrusted page context tries to send Messages through the bridge; the bridge is the only authorized entry |
| surface → surface | Messages cross extension contexts; spoofed/malformed messages must never dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-04 | Spoofing | MessageBusBridge dispatch | high | mitigate | MessageTypeValues whitelist rejects unknown types at runtime (throw MSG_UNKNOWN_TYPE); only RuntimeEnvelope-shaped messages parse (01-02 Zod fixtures at every public boundary) |
| T-1-05 | Tampering | EventBus handler loop | medium | mitigate | handler try/catch isolates failures (debugLog EVT_HANDLER); one broken handler never breaks the loop |
</threat_model>

<verification>
- `pnpm vitest run tests/core/events tests/core/messaging` green
- `pnpm tsc --noEmit` green
</verification>

<success_criteria>
- App Messages flow through MessageBusBridge only (Rule R-4 module-level)
- EventBus typed + scoped + resilient
- RuntimeEnvelope whitelist enforced in every dispatch
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-03-SUMMARY.md` when done
</output>
