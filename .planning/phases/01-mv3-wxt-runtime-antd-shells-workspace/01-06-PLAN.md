---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 06
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - src/core/workspace/WorkspaceStore.ts
  - src/core/workspace/WorkspaceRouter.ts
  - src/core/workspace/WorkspaceSync.ts
  - tests/core/workspace/WorkspaceStore.test.ts
  - tests/core/workspace/WorkspaceRouter.test.ts
  - tests/core/workspace/WorkspaceSync.test.ts
autonomous: true
requirements: [RUNTIME-03, WSPC-01, WSPC-02]
must_haves:
  truths:
    - "WorkspaceStore is a zustand store over WorkspaceState (§21.5 + D-18) whose canonical persistence is chrome.storage.local key np_workspace via a storage adapter — NOT zustand persist (Pitfall 7); chrome.storage.onChanged propagates cross-surface writes."
    - "WorkspaceRouter owns surface routing: it opens the side panel / standalone window and manages openedStandaloneTabId — chrome.sidePanel.open is called only inside a user-gesture callback chain using callback-style chrome.tabs.query (Pitfall 1: never await the result, never drop the gesture window)."
    - "WorkspaceSync emits PING/PONG heartbeats and background state via MessageBus (D-17 additions, 01-02) and manages the WORKSPACE_HANDOFF state machine (pending → complete / electionFailed) with the WORKSPACE_MIRROR flow."
    - "Store init/start/stop/snapshot/handoff/mirror/router all log via debugLog with canonical codes (WORKSPACE_*) and never throw (Golden Rule 9)."
    - "WorkspaceState fields outside the D-18 active set stay inert: reads return them as declared, writes never touch them (no silent type widening)."
  artifacts:
    - src/core/workspace/WorkspaceStore.ts
    - src/core/workspace/WorkspaceRouter.ts
    - src/core/workspace/WorkspaceSync.ts
    - tests/core/workspace/WorkspaceStore.test.ts
    - tests/core/workspace/WorkspaceRouter.test.ts
    - tests/core/workspace/WorkspaceSync.test.ts
  key_links:
    - "WorkspaceStore imports WorkspaceState/TabContext from 01-02 (src/types/workspace.ts, src/core/content/PageContext.ts) — no type re-declaration (R-1)."
    - "WorkspaceRouter imports MessageBusBridge from 01-03 for OPEN_SIDE_PANEL/OPEN_STANDALONE; WorkspaceSync publishes PING/PONG through the same bridge (whitelist enforced)."
  flagged_assumptions:
    - statement: "RUNTIME-03 workspace — router uses chrome.tabs.query callback-style; real user-gesture sidePanel.open behavior cannot be fully verified in jsdom (Pitfall 1) and is flagged for browser e2e."
      verification: flagged-unverified
    - statement: "WSPC-02 sync — PING/PONG heartbeat verified at unit level with fakeBrowser; cross-window election behavior deferred to browser e2e (RESEARCH A8)."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Build the workspace state layer: WorkspaceStore (zustand over §21.5 WorkspaceState, chrome.storage.local persistence + onChanged — Pitfall 7 guard), WorkspaceRouter (surface routing incl. the Pitfall-1-safe side panel opener), and WorkspaceSync (PING/PONG heartbeats, handoff state machine, mirroring).

Purpose: The workspace is the single source of truth for both surfaces; 01-08 shells and 01-09 mounts consume it, and handoff (Flow 11) depends on WorkspaceSync.
Output: 3 source files + 3 test files.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-17, D-18, D-13)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 1 lines 366-371, Pitfall 7)
@.planning/PRODUCT_SPEC_v0_1.md (Appendix M line ~5729; §21.5 line ~3339; Flow 11 line ~1710)

# Depends on 01-02 (WorkspaceState, MessageType), 01-03 (MessageBusBridge), 01-04 (debugLog codes).
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | **GUARDED HERE** — WorkspaceRouter calls chrome.sidePanel.open inside a callback-style chrome.tabs.query chain (never awaited, never dropped); crbug 1478648 |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | No surface imports in workspace core |
| P5 | Message contracts bypassing RuntimeEnvelope | Messages go through MessageBusBridge from 01-03 (whitelist enforced) |
| P6 | matchMedia-in-jsdom | Handled in 01-01 |
| P7 | zustand persist for cross-surface | **GUARDED HERE** — WorkspaceStore uses chrome.storage.local np_workspace + onChanged, NOT zustand persist |

<tasks>

<task type="auto">
  <name>Task 1: WorkspaceStore (zustand over §21.5 + storage adapter)</name>
  <files>
    src/core/workspace/WorkspaceStore.ts, tests/core/workspace/WorkspaceStore.test.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix M (lines 5729-5780) — WorkspaceStore contract
    .planning/PRODUCT_SPEC_v0_1.md §21.5 (lines 3339-3367) — WorkspaceState
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 7 lines 388-392)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (workspace store, lines 300-345)
  </read_first>
  <action>
    Create src/core/workspace/WorkspaceStore.ts: zustand store (create from 'zustand', v5) over WorkspaceState imported from '@/types/workspace' (01-02), with a storage adapter that serializes ONLY the D-18 active fields (workspaceId, conversationId, activeSurface, openedStandaloneTabId) plus version/updatedAt to chrome.storage.local key np_workspace. State { workspace: WorkspaceState; isReady: boolean }; actions { init(): Promise<void> (reads np_workspace, merges with defaults from §21.5, subscribes chrome.storage.onChanged once for np_workspace — foreign write → merge + set, debugLog WORKSPACE_INIT/SYNC silent:true, never throw); start(): Promise<void> (sets activeSurface 'sidepanel'|'standalone', bumps version, writes storage — WORKSPACE_START); stop(); snapshot(): WorkspaceState (WORKSPACE_SNAPSHOT); update(fn: (ws) => WorkspaceState) (immer-style patch — use immer produce from 'immer' approved stack — then persist); setActiveSurface(surface); setOpenedStandaloneTabId(id?) }. NO zustand persist middleware (Pitfall 7). Export singleton useWorkspaceStore(). Import debugLog + WORKSPACE_* codes from 01-04. Write tests/core/workspace/WorkspaceStore.test.ts with fakeBrowser storage: init with empty storage returns defaults (workspaceId non-empty, activeSurface 'sidepanel'); start sets activeSurface and writes np_workspace; update preserves inert fields untouched (D-18); chrome.storage.onChanged foreign write merges into state; init failure falls back to defaults without throwing.
<!-- planner-discipline-allow: persist -->
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/workspace/WorkspaceStore.test.ts -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    WorkspaceStore persists D-18 active fields to np_workspace via adapter (no persist middleware); onChanged merges; inert fields untouched; tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/workspace/WorkspaceStore.test.ts -x` exits 0
    - `grep -c "np_workspace\|onChanged" src/core/workspace/WorkspaceStore.ts` >= 2
    - `grep -c "persist" src/core/workspace/WorkspaceStore.ts` == 0 (Pitfall 7 guard)
    - `grep -c "from '@/types/workspace'" src/core/workspace/WorkspaceStore.ts` == 1 (R-1: no re-declared types)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WorkspaceRouter (Pitfall 1-safe surface routing)</name>
  <files>
    src/core/workspace/WorkspaceRouter.ts, tests/core/workspace/WorkspaceRouter.test.ts
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 1 lines 366-371 — crbug 1478648)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (workspace router, lines 300-345)
  </read_first>
  <action>
    Create src/core/workspace/WorkspaceRouter.ts with openSidePanel(triggerTabId?: number): Promise<void> and openStandalone(): Promise<void>. openSidePanel per Pitfall 1: when called from a user gesture, chain callback-style — chrome.tabs.query({ active: true, currentWindow: true }, (tabs) => { chrome.sidePanel.open({ tabId: tabs[0]?.id ?? triggerTabId, windowId: tabs[0]?.windowId }); }) — NEVER await chrome.tabs.query's return and never split the gesture across async boundaries (crbug 1478648: open() must run within the same task as the gesture; calling sidePanel.open in a detached promise loses the gesture). Wrap failures in debugLog(TABS_QUERY/CONNECT_FAILED, ...) — never throw. openStandalone: chrome.windows.create({ url: chrome.runtime.getURL('standalone.html'), type: 'popup', width, height } per UI-SPEC standalone window spec), then setOpenedStandaloneTabId via useWorkspaceStore (WORKSPACE_ROUTER on error). Do not import chrome APIs at module top-level in a way that breaks fakeBrowser tests — import { browser } from 'wxt/browser' (wxt proxy; fakeBrowser-compatible). Write tests/core/workspace/WorkspaceRouter.test.ts with fakeBrowser: openSidePanel calls chrome.tabs.query then chrome.sidePanel.open with the resolved tabId (assert callback chain order and that sidePanel.open receives {tabId, windowId}); openStandalone calls chrome.windows.create and updates the store's openedStandaloneTabId; query failure logs debugLog and does not throw.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/workspace/WorkspaceRouter.test.ts -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    openSidePanel uses callback-style tabs.query → sidePanel.open chain (Pitfall 1 guard proven by test asserting the callback chain); openStandalone creates the popup window and records tab id; tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/workspace/WorkspaceRouter.test.ts -x` exits 0
    - `grep -c "sidePanel.open" src/core/workspace/WorkspaceRouter.ts` == 1
    - `grep -rn "await browser.tabs.query\|await chrome.tabs.query" src/core/workspace/ | wc -l` == 0 (Pitfall 1 guard)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: WorkspaceSync (PING/PONG, handoff, mirroring)</name>
  <files>
    src/core/workspace/WorkspaceSync.ts, tests/core/workspace/WorkspaceSync.test.ts
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md D-17 (PING/PONG/GET_CONTENT_CAPABILITIES additions)
    .planning/PRODUCT_SPEC_v0_1.md Flow 11 (line ~1710) — handoff
  </read_first>
  <action>
    Create src/core/workspace/WorkspaceSync.ts: class WorkspaceSync using MessageBusBridge (01-03) to publish and subscribe RuntimeEnvelope messages. Methods: startHeartbeat(): void (publish PING every 30s via setInterval, expect PONG; on missing PONG → debugLog WORKSPACE_HANDOFF/ELECTION-related codes, never throw); stopHeartbeat(); requestHandoff(target: 'sidepanel'|'standalone'): Promise<void> (publish WORKSPACE_HANDOFF with the current snapshot from useWorkspaceStore; transitions handoff state pending → on PONG from target complete → on failure electionFailed, updating the store and emitting SHOW_HANDOFF_* events via EventBus from 01-03); startMirroring(): void (publish WORKSPACE_MIRROR snapshot on store updates while handoff pending; emits WORKSPACE_MIRRORING_START/STOP events). All handlers subscribe via bridge.addMessageListener and validate messages against the MessageType whitelist (unknown types ignored — Pitfall 5). Use browser alarms or setInterval? Use setInterval (no background SW needed this phase). Write tests/core/workspace/WorkspaceSync.test.ts with fakeBrowser: startHeartbeat publishes PING; a subscribed PONG reply clears the pending flag; requestHandoff publishes WORKSPACE_HANDOFF and on PONG reply transitions the store to handoffComplete and emits SHOW_HANDOFF_COMPLETE; a non-whitelist message type is ignored (no handler invocation).
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/workspace/WorkspaceSync.test.ts -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    WorkspaceSync heartbeats (PING/PONG), handoff state machine (pending→complete/electionFailed), and mirroring events all tested green with fakeBrowser; unknown types ignored.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/workspace/WorkspaceSync.test.ts -x` exits 0
    - `grep -c "PING\|PONG\|WORKSPACE_HANDOFF" src/core/workspace/WorkspaceSync.ts` >= 3
    - `grep -c "MessageTypeValues" src/core/workspace/WorkspaceSync.ts` >= 1 (Pitfall 5 whitelist)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| surface → workspace storage | Any surface writes np_workspace; foreign writes must be validated before merge |
| message wire → WorkspaceSync | Incoming PONG/handoff replies are untrusted until whitelist-checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-12 | Spoofing | WorkspaceSync handlers | high | mitigate | Every inbound message passes the MessageTypeValues whitelist (Pitfall 5) before dispatch; non-whitelist types ignored |
| T-1-13 | Tampering | WorkspaceStore merge | medium | mitigate | Foreign np_workspace writes are schema-validated against WorkspaceState defaults before merge; unknown keys dropped (never spread raw storage) |
| T-1-14 | Denial of Service | Heartbeat loop | low | accept | 30s interval with stopHeartbeat teardown; a missing PONG only transitions handoff state, no crash path |
</threat_model>

<verification>
- `pnpm vitest run tests/core/workspace -x` green
- `pnpm tsc --noEmit` green
- Pitfall 1 guard grep: no awaited tabs.query in router
- Pitfall 7 guard grep: no zustand persist in stores
</verification>

<success_criteria>
- WorkspaceStore is the single np_workspace source (D-18 active fields), tested against fakeBrowser storage + onChanged
- WorkspaceRouter opens surfaces without losing the user gesture (Pitfall 1)
- WorkspaceSync heartbeats + handoff state machine green, whitelist-enforced
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-06-SUMMARY.md` when done
</output>
