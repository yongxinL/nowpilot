---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/core/runtime/RuntimeEnvelope.ts
  - src/core/runtime/MessageType.ts
  - src/core/runtime/OperationId.ts
  - src/core/content/PageContext.ts
  - src/types/workspace.ts
  - src/types/harness.ts
  - src/core/i18n/strings.ts
  - src/core/prompts/index.ts
  - tests/core/runtime/RuntimeEnvelope.test.ts
  - tests/core/runtime/OperationId.test.ts
autonomous: true
requirements: [RUNTIME-01, WSPC-03]
must_haves:
  truths:
    - "RuntimeEnvelope fixtures parse (Phase 1 DONE-when) — RuntimeEnvelope and ResponseEnvelope shapes match Appendix C exactly, enforced by Zod fixture tests per §0.3."
    - "MessageType.ts = Appendix E values PLUS the four D-17 additions (PING, PONG, GET_CONTENT_CAPABILITIES, CONTENT_CAPABILITIES) — additions to the canonical enum, NOT a phase-local contract (spec reconciliation #2)."
    - "OperationId uses crypto.randomUUID() — no hand-rolled Date.now+rand (Don't Hand-Roll)."
    - "WorkspaceState declares the FULL §8.4 field set with only workspaceId/conversationId/activeSurface/openedStandaloneTabId active (D-18) — no type churn in later phases."
    - "Canonical type homes preserved (R-1): PageContext/TabContext live in src/core/content/PageContext.ts per Appendix C; harness types in src/types/harness.ts per §C.1; WorkspaceState in src/types/workspace.ts per §21.5."
    - "STR (Appendix B) and PROMPTS (Appendix A) constants are seeded verbatim from the spec — canonical strings, no paraphrase."
    - "ProviderId is declared locally in src/types/workspace.ts with the exact union ('openai' | 'anthropic' | 'gemini' | 'ollama') as a Phase-1 local declaration; canonical home is src/core/ai/types.ts which lands in Phase 3 — swap the import then (flagged assumption)."
  artifacts:
    - src/core/runtime/RuntimeEnvelope.ts (RuntimeEnvelope, ResponseEnvelope)
    - src/core/runtime/MessageType.ts (MessageType, MessageTypeValue, MessageTypeValues)
    - src/core/runtime/OperationId.ts (createOperationId)
    - src/core/content/PageContext.ts (PageContext, TabContext)
    - src/types/workspace.ts (ActiveSurface, WorkspaceState, ProviderId)
    - src/types/harness.ts (CompletionEvidence minimal subset)
    - src/core/i18n/strings.ts (STR)
    - src/core/prompts/index.ts (PROMPTS)
  key_links:
    - "RuntimeEnvelope imports MessageTypeValue from MessageType.ts — the only allowed message-type vocabulary (Pitfall 5: no throwaway contracts)."
    - "WorkspaceState imports TabContext/PageContext from src/core/content/PageContext.ts and ProviderId from the local declaration — consumers (01-06 WorkspaceStore) depend on these exact paths."
    - "tests/core/runtime/RuntimeEnvelope.test.ts is the §0.3 Zod fixture for every public messaging boundary."
  flagged_assumptions:
    - statement: "RUNTIME-01 envelope contract — RuntimeEnvelope serialize/deserialize verified by Zod fixture tests; cross-context wire behavior verified in 01-03 via fakeBrowser runtime events, not a real browser."
      verification: flagged-unverified
    - statement: "WSPC-03 envelope transport — the request/response contract is unit-tested here and in 01-03; real cross-surface transport integration is deferred to browser e2e (no Chrome binary, RESEARCH A8)."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Create the canonical type registry foundation: RuntimeEnvelope + ResponseEnvelope (Appendix C), MessageType including the D-17 content-bridge additions (Appendix E + reconciliation #2), OperationId, PageContext/TabContext (Appendix C), WorkspaceState with the full D-18 field set (§21.5), the harness types home (§C.1), and the Appendix A/B canonical string constants.

Purpose: Every message, every workspace read, and every error path in the phase depends on these exact shapes. Golden Rule 2: never invent identifiers.
Output: 8 source files + 2 Zod fixture test files.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-17, D-18)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (reconciliation #1/#2, Pitfall 5)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (section C types, section G)
@.planning/PRODUCT_SPEC_v0_1.md (Appendix C line ~4137, Appendix E line ~5019, §21.5 line ~3339, Appendix A line ~3962, Appendix B line ~4029)

# No prior SUMMARYs — this plan creates the first src/ types. Depends on 01-01 (pinned zod ^3).
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | Handled in 01-06 — no sidePanel code here |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | Handled in 01-01/01-07 — this plan's files are all dependency-free |
| P5 | Message contracts bypassing RuntimeEnvelope | **GUARDED HERE** — RuntimeEnvelope/ResponseEnvelope are the ONLY message shapes; MessageType is the only type vocabulary (D-17); MessageTypeValues export enables the whitelist in 01-03 |
| P6 | matchMedia-in-jsdom | Handled in 01-01 |
| P7 | zustand persist for cross-surface | Handled in 01-05/01-06 |

<tasks>

<task type="auto">
  <name>Task 1: RuntimeEnvelope + MessageType + OperationId (canonical contract)</name>
  <files>
    src/core/runtime/RuntimeEnvelope.ts, src/core/runtime/MessageType.ts, src/core/runtime/OperationId.ts, tests/core/runtime/RuntimeEnvelope.test.ts, tests/core/runtime/OperationId.test.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix C (lines 4141-4154) — RuntimeEnvelope/ResponseEnvelope verbatim
    .planning/PRODUCT_SPEC_v0_1.md Appendix E (lines 5021-5042) — MessageType registry verbatim
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (reconciliation #2 line 266; Pitfall 5 lines 383-387)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md section C (lines 222-259)
  </read_first>
  <action>
    Create src/core/runtime/MessageType.ts with the Appendix E MessageType const object VERBATIM (all 15 canonical values: PROXY_FETCH, EXTRACT_PAGE_CONTENT, OPEN_SIDE_PANEL, OPEN_STANDALONE, SESSION_TOKEN_UPDATE, BACKGROUND_STATE, KEEPALIVE_PING, PORT_STREAM_START/CHUNK/END/ABORT, ADDON_EVENT, WORKSPACE_HANDOFF, WORKSPACE_UPDATED, WORKSPACE_HEARTBEAT) PLUS the four D-17 additions as documented extensions of the canonical enum: PING, PONG, GET_CONTENT_CAPABILITIES, CONTENT_CAPABILITIES (spec reconciliation #2 — these are additions to the registry, NOT a phase-local contract). Export type MessageTypeValue and the MessageTypeValues array exactly as Appendix E lines 5040-5041. Create src/core/runtime/RuntimeEnvelope.ts with the Appendix C interface verbatim (id: string; type: MessageTypeValue; createdAt: number; source: 'sidepanel'|'background'|'content'|'addon'|'standalone'; target?: same union; payload: T) and the ResponseEnvelope discriminated union verbatim (ok:true with data; ok:false with error {code, message, retryable}) per lines 4143-4153. Create src/core/runtime/OperationId.ts exporting createOperationId(): string that returns crypto.randomUUID() (secure context — never Date.now+rand). Write tests/core/runtime/RuntimeEnvelope.test.ts as a §0.3 Zod fixture test: build a Zod schema mirroring RuntimeEnvelope and ResponseEnvelope, assert a valid request fixture parses, a PONG-style response fixture parses, and an invalid payload (wrong type field / missing id) fails parse. Write tests/core/runtime/OperationId.test.ts asserting createOperationId() returns a UUID v4-shaped string and two calls differ. All files in this task MUST remain dependency-free (no React, no zustand, no antd) — they are the content-script-safe core (Pitfall 4).
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/runtime/RuntimeEnvelope.test.ts tests/core/runtime/OperationId.test.ts -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    MessageType.ts contains all 15 Appendix E values + exactly the 4 D-17 values; RuntimeEnvelope/ResponseEnvelope match Appendix C verbatim; createOperationId returns crypto.randomUUID(); both fixture tests pass; the three source files import nothing outside the typescript standard lib.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/runtime/RuntimeEnvelope.test.ts tests/core/runtime/OperationId.test.ts -x` exits 0
    - `grep -c "PING\|PONG\|GET_CONTENT_CAPABILITIES\|CONTENT_CAPABILITIES" src/core/runtime/MessageType.ts` == 4
    - `grep -c "randomUUID" src/core/runtime/OperationId.ts` == 1
    - `grep -rn "from 'react'\|from 'zustand'\|from 'antd'" src/core/runtime/ | wc -l` == 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PageContext + WorkspaceState + harness types (canonical type homes)</name>
  <files>
    src/core/content/PageContext.ts, src/types/workspace.ts, src/types/harness.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix C (lines 4219-4237) — PageContext/TabContext verbatim
    .planning/PRODUCT_SPEC_v0_1.md §21.5 (lines 3341-3367) — WorkspaceState verbatim
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md D-18 (full field set, only 4 active)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (types/workspace.ts analog, lines 356-374)
  </read_first>
  <action>
    Create src/core/content/PageContext.ts exporting PageContext (url, origin, hostname, title, html?, markdown?, meta Record<string,string>, extractedAt, addonId?, addonFields?) and TabContext (tabId, windowId, page: PageContext, pinnedAt?) per Appendix C lines 4219-4237 verbatim (canonical home per R-1 — do not relocate these types). Create src/types/workspace.ts exporting: type ProviderId = 'openai' | 'anthropic' | 'gemini' | 'ollama' (Phase-1 local declaration; the canonical home src/core/ai/types.ts lands in Phase 3 — add a one-line comment noting the future import swap), type ActiveSurface = 'sidepanel' | 'standalone', and interface WorkspaceState with the FULL §21.5 field set verbatim: workspaceId, conversationId, activeProvider?: ProviderId, selectedModel?, pinnedTabs: TabContext[], currentPageContext?: PageContext, selectedNotes: string[], activeAddonContext?, activeSkillRun?, activeSurface: ActiveSurface, openedStandaloneTabId?, version, updatedAt (D-18: all fields declared; only workspaceId/conversationId/activeSurface/openedStandaloneTabId are active in Phase 1 — the rest are inert by type presence only). Import PageContext/TabContext from '@/core/content/PageContext'. Create src/types/harness.ts with the Phase-1 minimal subset of the §C.1 harness home: export interface CompletionEvidence (the shape referenced by Appendix C ToolExecutionResult — evidence is set for side-effecting tools; declare a minimal { probe: string; path: string; capturedAt: number }-style contract and note it will extend with the §28.2 harness types in Phase 3a). No implementation logic — types only.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    PageContext/TabContext exist at src/core/content/PageContext.ts matching Appendix C; WorkspaceState at src/types/workspace.ts declares all 14 §21.5 fields including the inert D-18 set; ProviderId is the exact 4-value union; harness.ts exports CompletionEvidence; `pnpm tsc --noEmit` passes.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "openedStandaloneTabId\|activeSkillRun\|activeAddonContext\|currentPageContext\|pinnedTabs\|selectedNotes\|selectedModel\|activeProvider" src/types/workspace.ts` == 8 (all D-18 inert fields present in the type)
    - `grep -c "pinnedAt" src/core/content/PageContext.ts` == 1 (TabContext carries pinnedAt)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Canonical strings + prompt constants (Appendices A/B verbatim)</name>
  <files>
    src/core/i18n/strings.ts, src/core/prompts/index.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix B (lines 4031-4135) — STR verbatim
    .planning/PRODUCT_SPEC_v0_1.md Appendix A (lines 3964-4025) — PROMPTS verbatim
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md section G (line 467)
  </read_first>
  <action>
    Create src/core/i18n/strings.ts exporting const STR seeded VERBATIM from Appendix B (lines 4033-4134): all canonical strings including chat.loading/empty/errorRetry/offline/contextReduced/minimalMode/noProvider/maxPinnedTabs/cannotPin, diagnostics.*, tools.*, memory.*, onboarding.*, notes.*, agent.*, standalone.openTitle/opening/openFailed/minWidth, workspace.handoffPending/handoffComplete/mirroringNotice/electionFailed, options.*, rich.*. Do not paraphrase any string (Golden Rule 2; UI copy is locked by the Copywriting Contract). Create src/core/prompts/index.ts exporting const PROMPTS VERBATIM from Appendix A (lines 3966-4024): planner/renderer/memoryExtractor/conversationSummarizer/repairJson/titleGen/noteTagger/noteQA/noteChatConvert/clarify/followUpSuggest, each with its exact system string, cacheable flag and tier. Do not hard-code persona text into these constants (Appendix A note — the persona block is prepended by PersonaInjector in Phase 3, keep them byte-stable). No i18n framework — plain exported constants (CONTEXT the agent's Discretion).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -3 && node -e "const {STR}=require('./.output/dev/chrome-mv3/assets/strings.js')" 2>/dev/null; node --experimental-vm-modules -e "1" >/dev/null 2>&1; grep -c "handoffComplete\|mirroringNotice\|minimalMode\|askPlaceholder" src/core/i18n/strings.ts</automated>
  </verify>
  <done>
    STR contains every Appendix B string (spot-checked group by group: chat, diagnostics, tools, memory, onboarding, notes, agent, standalone, workspace, options, rich); PROMPTS contains all 11 Appendix A prompt keys with exact system strings; `pnpm tsc --noEmit` passes.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "handoffComplete\|mirroringNotice\|minimalMode\|askPlaceholder\|personaTagline" src/core/i18n/strings.ts` == 5
    - `grep -c "followUpSuggest\|noteChatConvert\|repairJson\|conversationSummarizer" src/core/prompts/index.ts` == 4
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code → message wire | RuntimeEnvelope shape is the only cross-context contract; untrusted page content may try to spoof it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-04 | Spoofing | RuntimeEnvelope/MessageType | high | mitigate | MessageType is the ONLY allowed vocabulary (MessageTypeValues export feeds the whitelist in 01-03/01-07); ResponseEnvelope is the only reply shape; Zod fixture tests per §0.3 enforce shape at every public boundary so a spoofed or malformed envelope fails parse before dispatch |
| T-1-05 | Tampering | WorkspaceState type | medium | mitigate | Full §21.5 field set declared verbatim (D-18); consumers may not widen the type in later phases without type-check failures — inert fields are the boundary against type drift |
</threat_model>

<verification>
- `pnpm vitest run tests/core/runtime/RuntimeEnvelope.test.ts tests/core/runtime/OperationId.test.ts -x` green
- `pnpm tsc --noEmit` green
- MessageType grep proves exactly 4 D-17 additions
</verification>

<success_criteria>
- RuntimeEnvelope fixtures parse (DONE-when)
- MessageType = Appendix E + D-17 (no phase-local contract)
- WorkspaceState full field set (D-18)
- STR/PROMPTS verbatim from Appendices A/B
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-02-SUMMARY.md` when done
</output>
