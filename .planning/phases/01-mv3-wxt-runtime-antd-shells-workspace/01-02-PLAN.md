---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/core/stores/themeStore.ts
  - src/core/stores/workspaceStore.ts
  - src/entrypoints/background.ts
  - src/core/utils/debugLog.ts
  - src/core/components/ErrorBoundary.tsx
  - src/core/messaging/runtimeEnvelope.ts
  - tests/core/themeStore.test.ts
  - tests/core/workspaceStore.test.ts
  - tests/core/background.test.ts
  - tests/core/ErrorBoundary.test.tsx
  - tests/core/runtimeEnvelope.test.ts
autonomous: true
requirements:
  - THEME-01
  - WRKSP-01
  - HARD-05
  - HARD-06
  - HARD-08
  - HARD-09
  - HARD-10

must_haves:
  truths:
    - "ThemeStore persists mode (light/dark/auto) to chrome.storage.sync and rehydrates on page load"
    - "WorkspaceStore persists workspaceId, conversationId, activeProvider, activeSurface to chrome.storage.session"
    - "Background SW registers all chrome API listeners synchronously at module load — main() callback is never async"
    - "ErrorBoundary catches React render errors and renders AntD Result component with 'Try Again' and 'Reload Page' buttons"
    - "RuntimeEnvelope validates cross-context messages against a typed Zod schema before processing"
    - "All catch blocks in files created by this plan call debugLog with structured metadata (level, message, data)"
    - "Zero instances of innerHTML or dangerouslySetInnerHTML in all files created by this plan"
    - "All imperative AntD APIs (message, notification, modal) use App.useApp() hook, never static imports"
  artifacts:
    - path: "src/core/stores/themeStore.ts"
      provides: "Zustand store with chrome.storage.sync persistence adapter for theme mode"
      exports: ["useThemeStore"]
    - path: "src/core/stores/workspaceStore.ts"
      provides: "Zustand store with chrome.storage.session persistence adapter for workspace metadata"
      exports: ["useWorkspaceStore"]
    - path: "src/entrypoints/background.ts"
      provides: "MV3 service worker — onInstalled, sidePanel setup, command routing, message bridge"
    - path: "src/core/utils/debugLog.ts"
      provides: "Structured debug logging utility gated behind __DEV__"
      exports: ["debugLog"]
    - path: "src/core/components/ErrorBoundary.tsx"
      provides: "React class component error boundary with AntD Result fallback"
      exports: ["ErrorBoundary"]
    - path: "src/core/messaging/runtimeEnvelope.ts"
      provides: "Typed cross-context message envelope with Zod validation and sender verification"
      exports: ["RuntimeEnvelope", "validateEnvelope"]
  key_links:
    - from: "src/core/components/ErrorBoundary.tsx"
      to: "src/core/utils/debugLog.ts"
      via: "componentDidCatch calls debugLog('error', ...) with error message and componentStack"
    - from: "src/core/stores/themeStore.ts"
      to: "chrome.storage.sync"
      via: "Zustand persist middleware with custom chromeSyncStorage adapter"
    - from: "src/core/stores/workspaceStore.ts"
      to: "chrome.storage.session"
      via: "Zustand persist middleware with custom chromeSessionStorage adapter"
    - from: "src/entrypoints/background.ts"
      to: "chrome.runtime.onInstalled"
      via: "Synchronous addListener call in defineBackground main() callback"
    - from: "src/core/messaging/runtimeEnvelope.ts"
      to: "chrome.runtime.onMessage"
      via: "validateEnvelope is called before processing any incoming message"
  prohibitions: []
---

<objective>
Establish the three foundational stores (ThemeStore, WorkspaceStore) with chrome.storage persistence, the background service worker with HARD-08 synchronous listener compliance, and the safety utilities (debugLog, ErrorBoundary, RuntimeEnvelope) that every subsequent plan depends on.

Purpose: These are the most-referenced modules in the codebase — ThemeStore feeds XProvider themes, WorkspaceStore feeds all cross-surface state, ErrorBoundary wraps every page, debugLog is called from every catch block, and RuntimeEnvelope gates all cross-context messaging.
Output: 6 source files + 5 test files, all type-checking cleanly with tests passing.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (TDD): ThemeStore (THEME-01) and WorkspaceStore (WRKSP-01) with chrome.storage persistence</name>
  <files>tests/core/themeStore.test.ts, tests/core/workspaceStore.test.ts, src/core/stores/themeStore.ts, src/core/stores/workspaceStore.ts</files>
  <read_first>01-RESEARCH.md lines 208-248 (Pattern 1 — Zustand persist with chrome.storage adapter, exact code template), 01-RESEARCH.md lines 437-442 (Pitfall 3 — storage quotas: ThemeStore < 100B in sync, WorkspaceStore metadata-only in session), 01-PATTERNS.md lines 149-236 (themeStore and workspaceStore pattern excerpts with adapter code), tests/setup.ts (chrome API mocks available)</read_first>
  <behavior>
    - ThemeStore test 1: Default mode is 'auto' when chrome.storage.sync returns null (fresh install)
    - ThemeStore test 2: setMode('dark') updates state AND calls chrome.storage.sync.set with serialized state
    - ThemeStore test 3: On rehydration, chrome.storage.sync.get returns 'dark' → store initializes with mode='dark'
    - ThemeStore test 4: setMode rejects values outside the 'light' | 'dark' | 'auto' union (TypeScript compile-time check)
    - WorkspaceStore test 1: Default state has workspaceId=null, conversationId=null, activeProvider=null, activeSurface='sidepanel'
    - WorkspaceStore test 2: setActiveProvider('openai') persists to chrome.storage.session.set
    - WorkspaceStore test 3: setActiveSurface('fullapp') updates activeSurface and persists
    - WorkspaceStore test 4: Only lightweight metadata stored — verify store shape has no message body or conversation tree fields
  </behavior>
  <action>
    Implement two Zustand stores following the RESEARCH.md Pattern 1 template exactly:

    **ThemeStore** (`src/core/stores/themeStore.ts`):
    - Define `ThemeMode = 'light' | 'dark' | 'auto'`
    - Define `ThemeState` interface with `mode: ThemeMode` and `setMode: (mode: ThemeMode) => void`
    - Create `useThemeStore` with `create<ThemeState>()(persist(...))` 
    - Persist middleware config: `name: 'nowpilot-theme'`, storage adapter wrapping `chrome.storage.sync` (getItem returns `result[name] ?? null`, setItem calls `chrome.storage.sync.set`, removeItem calls `chrome.storage.sync.remove`)
    - Default `mode: 'auto'`
    - Export ONLY `useThemeStore` (default export not needed)

    **WorkspaceStore** (`src/core/stores/workspaceStore.ts`):
    - Define `Surface = 'sidepanel' | 'fullapp' | 'popup'`
    - Define `WorkspaceState` interface with `workspaceId: string | null`, `conversationId: string | null`, `activeProvider: string | null`, `activeSurface: Surface`
    - Add setter actions: `setWorkspaceId`, `setConversationId`, `setActiveProvider`, `setActiveSurface`
    - Persist middleware config: `name: 'nowpilot-workspace'`, storage adapter wrapping `chrome.storage.session` (same pattern as sync adapter but calling `chrome.storage.session.*`)
    - Default `activeSurface: 'sidepanel'`, all others `null`
    - CRITICAL per RESEARCH.md Pitfall 3: The state shape MUST contain ONLY lightweight metadata — workspaceId (string|null), conversationId (string|null), activeProvider (string|null), activeSurface (Surface). No message bodies, no conversation trees, no large objects. chrome.storage.session has a 10MB limit but individual items should be kept minimal.

    Write tests first (RED phase), then implement stores (GREEN phase).
    After both stores pass their tests, run `pnpm tsc --noEmit` to verify type correctness.
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/themeStore.test.ts` exits 0 with 4+ tests passing
    - `pnpm vitest run tests/core/workspaceStore.test.ts` exits 0 with 4+ tests passing
    - `src/core/stores/themeStore.ts` exports `useThemeStore` function typed as `UseBoundStore<StoreApi<ThemeState>>`
    - `src/core/stores/workspaceStore.ts` exports `useWorkspaceStore` with state shape containing only the 4 metadata fields (no message/body/tree fields)
    - `pnpm tsc --noEmit` passes (both stores type-check)
    - chrome.storage.sync mock in tests verifies that `set` was called with `nowpilot-theme` key for ThemeStore
    - chrome.storage.session mock in tests verifies that `set` was called with `nowpilot-workspace` key for WorkspaceStore
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/themeStore.test.ts tests/core/workspaceStore.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>ThemeStore persists to chrome.storage.sync with light/dark/auto mode. WorkspaceStore persists to chrome.storage.session with metadata-only state shape. Both stores have passing unit tests and type-check cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (TDD): Background SW with synchronous listener registration (HARD-08) and debugLog utility (HARD-09)</name>
  <files>tests/core/background.test.ts, src/entrypoints/background.ts, src/core/utils/debugLog.ts</files>
  <read_first>01-RESEARCH.md lines 337-430 (Pattern 5 + Pitfall 1 — synchronous listener registration, anti-pattern: async main), 01-PATTERNS.md lines 283-331 (background.ts pattern with correct/incorrect examples, CRITICAL rule about non-async main()), tests/setup.ts (chrome mocks — onInstalled, commands, runtime.onMessage already stubbed)</read_first>
  <behavior>
    - Background test 1: The `main()` callback in `defineBackground` is NOT an async function (verify via Function.prototype.toString or by checking that the exported function doesn't return a Promise)
    - Background test 2: `chrome.runtime.onInstalled.addListener` is called within the synchronous execution of main() — not deferred by setTimeout or .then()
    - Background test 3: On install (reason === 'install'), `chrome.sidePanel.setPanelBehavior` is called with `{ openPanelOnActionClick: true }`
    - Background test 4: `chrome.commands.onCommand.addListener` is registered synchronously
    - Background test 5: `chrome.runtime.onMessage.addListener` is registered and returns `true` (keeps channel open for async response)
    - debugLog test 1: debugLog('error', 'test', { detail: 'x' }) calls console.error with timestamp-prefixed message when __DEV__ is true
    - debugLog test 2: debugLog does NOT call console methods when __DEV__ is false (production suppression)
    - debugLog test 3: Accepts all four log levels ('debug', 'info', 'warn', 'error') and routes to the correct console method
  </behavior>
  <action>
    Implement the background service worker and debug logging utility:

    **debugLog** (`src/core/utils/debugLog.ts`):
    - Type: `type LogLevel = 'debug' | 'info' | 'warn' | 'error'`
    - Function signature: `export function debugLog(level: LogLevel, message: string, data?: unknown): void`
    - Implementation: Check `__DEV__` (WXT global) — if true, call `console[level](\`[NowPilot ${new Date().toISOString()}] ${message}\`, data ?? '')`; if false, no-op
    - This is the single logging utility used by ALL catch blocks across the codebase (HARD-09)

    **Background SW** (`src/entrypoints/background.ts`):
    - Use `defineBackground` from `wxt/sandbox` (WXT auto-imports this — do NOT write an import statement for it)
    - CRITICAL (HARD-08): The `main()` callback passed to `defineBackground` MUST NOT be `async`. Research confirms: async main creates event gaps because Chrome may fire events before async initialization completes, and those events are silently dropped (RESEARCH.md Pitfall 1).
    - Register THREE listeners synchronously inside main():
      1. `chrome.runtime.onInstalled.addListener((details) => { ... })` — on `details.reason === 'install'`, call `chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick: true }).catch(console.error)`
      2. `chrome.commands.onCommand.addListener((command) => { ... })` — placeholder that calls `debugLog('info', 'command received', { command })`; actual routing comes in Plan 01-03/01-05
      3. `chrome.runtime.onMessage.addListener((message, sender, sendResponse) => { ...; return true; })` — placeholder that calls `debugLog('debug', 'message received', { message })`; message routing fills in across later phases. The `return true` keeps the sendResponse channel open for async handling (mandatory for MV3 SW)

    - NEVER put any listener registration inside setTimeout, setInterval, or after an await.
    - If any async initialization is needed (none in this phase), use the pattern: declare a variable, kick off the async work via `.then()`, and check the variable in the listener (RESEARCH.md lines 422-429).

    Write tests first (RED phase). For the background test, mock `defineBackground` to capture the main callback and test:
    - The captured callback is not async
    - It calls chrome API addListener methods synchronously (not inside setTimeout)
    Then implement (GREEN phase).
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/background.test.ts` exits 0 with 5+ tests passing
    - `src/entrypoints/background.ts` main() callback is NOT declared with `async` keyword
    - `src/entrypoints/background.ts` calls `chrome.runtime.onInstalled.addListener`, `chrome.commands.onCommand.addListener`, `chrome.runtime.onMessage.addListener` at module load (not deferred)
    - `src/entrypoints/background.ts` `onMessage` listener returns `true` (MV3 async channel requirement)
    - `src/core/utils/debugLog.ts` exports `debugLog` function with `LogLevel` parameter
    - `debugLog` is gated on `__DEV__` — no console output in production
    - `debugLog('error', 'msg', data)` calls `console.error` in dev mode with ISO timestamp prefix
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/background.test.ts && grep -n 'async' src/entrypoints/background.ts; test $? -eq 1</automated>
  </verify>
  <done>Background SW registers all listeners synchronously at module load (HARD-08 compliant). debugLog utility routes to correct console level with timestamp and __DEV__ gate (HARD-09 ready). Both have passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (TDD): ErrorBoundary (HARD-06), RuntimeEnvelope (HARD-05), and HARD-10 static enforcement</name>
  <files>tests/core/ErrorBoundary.test.tsx, tests/core/runtimeEnvelope.test.ts, src/core/components/ErrorBoundary.tsx, src/core/messaging/runtimeEnvelope.ts</files>
  <read_first>01-RESEARCH.md lines 489-536 (ErrorBoundary class component with AntD Result — exact code template with handleReset, componentDidCatch calling debugLog), 01-RESEARCH.md lines 790-795 (RuntimeEnvelope — message spoofing mitigation with Zod schema validation), 01-RESEARCH.md lines 798-809 (Security Domain — threat patterns and standard mitigations), 01-PATTERNS.md lines 386-429 (ErrorBoundary.tsx full pattern with key rule: must be class component), tests/setup.ts (chrome mocks), src/core/utils/debugLog.ts (import for ErrorBoundary)</read_first>
  <behavior>
    - ErrorBoundary test 1: When child throws during render, getDerivedStateFromError sets hasError=true and captures the Error
    - ErrorBoundary test 2: Error state renders AntD Result with status="error", title="Something went wrong", and two buttons ("Try Again", "Reload Page")
    - ErrorBoundary test 3: Clicking "Try Again" calls handleReset which clears hasError back to false and re-renders children
    - ErrorBoundary test 4: componentDidCatch calls debugLog with 'error' level, error.message, and componentStack
    - RuntimeEnvelope test 1: validateEnvelope accepts a message with valid shape (type, source, payload) and returns the parsed envelope
    - RuntimeEnvelope test 2: validateEnvelope rejects a message missing required fields (type, source, or payload) with a typed error
    - RuntimeEnvelope test 3: validateEnvelope rejects a message where source is not 'background' | 'sidepanel' | 'fullapp' | 'popup' (spoofed sender)
    - RuntimeEnvelope test 4: validateEnvelope rejects a message where payload fails Zod schema validation (wrong types)
    - HARD-10 check: Static grep confirms NO occurrences of `innerHTML` or `dangerouslySetInnerHTML` in any file created by this plan
  </behavior>
  <action>
    Implement the ErrorBoundary and RuntimeEnvelope:

    **ErrorBoundary** (`src/core/components/ErrorBoundary.tsx`):
    - Must be a React **class component** — React error boundaries cannot be function components
    - Extends `Component<Props, State>` from React
    - Props: `{ children: ReactNode }`
    - State: `{ hasError: boolean; error: Error | null }`
    - Implements `static getDerivedStateFromError(error: Error): State` → returns `{ hasError: true, error }`
    - Implements `componentDidCatch(error: Error, info: ErrorInfo): void` → calls `debugLog('error', 'ErrorBoundary caught error', { error: error.message, componentStack: info.componentStack })`
      — imports debugLog from `@/core/utils/debugLog`
    - Implements `handleReset = (): void` → `this.setState({ hasError: false, error: null })`
    - `render()` method: if `this.state.hasError`, return `<Result status="error" title="Something went wrong" subTitle={this.state.error?.message} extra={[<Button key="retry" type="primary" onClick={this.handleReset}>Try Again</Button>, <Button key="reload" onClick={() => window.location.reload()}>Reload Page</Button>]} />`
    - Otherwise return `this.props.children`
    - Imports: `{ Component }` from 'react', `{ ErrorInfo, ReactNode }` from 'react' (types), `{ Button, Result }` from 'antd', `debugLog` from `@/core/utils/debugLog`

    **RuntimeEnvelope** (`src/core/messaging/runtimeEnvelope.ts`):
    - Define `MessageSource = 'background' | 'sidepanel' | 'fullapp' | 'popup'`
    - Define `Envelope<T = unknown>` interface: `{ type: string; source: MessageSource; payload: T; timestamp?: number }`
    - Use Zod to create a schema `envelopeSchema` that validates: `type: z.string()`, `source: z.enum(['background', 'sidepanel', 'fullapp', 'popup'])`, `payload: z.unknown()`, `timestamp: z.number().optional()`
    - Export `validateEnvelope<T>(message: unknown): Envelope<T>` function that:
      1. Parses `message` through `envelopeSchema` using `envelopeSchema.safeParse(message)`
      2. If `!result.success`, calls `debugLog('warn', 'Invalid message envelope', { errors: result.error.flatten() })` and throws `new Error('Invalid message envelope')`
      3. Returns the typed `result.data` cast as `Envelope<T>`
    - Purpose (HARD-05): Every cross-context message (via chrome.runtime.sendMessage or chrome.storage.onChanged) must pass through `validateEnvelope` before being processed — this prevents spoofed messages from content scripts or other extensions (RESEARCH.md line 801-802)

    **HARD-10 static verification** (no file — verification step only):
    - After creating all files, run: `grep -rn 'innerHTML\|dangerouslySetInnerHTML' src/core/stores/ src/core/utils/ src/core/components/ src/core/messaging/ src/entrypoints/background.ts`
    - Must return zero matches. This verifies no XSS-vulnerable patterns in Phase 1 core files.

    Write tests first (RED phase), then implement (GREEN phase).
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/ErrorBoundary.test.tsx` exits 0 with 4+ tests passing
    - `pnpm vitest run tests/core/runtimeEnvelope.test.ts` exits 0 with 4+ tests passing
    - `src/core/components/ErrorBoundary.tsx` is a class component (extends Component) — NOT a function component
    - `ErrorBoundary.tsx` imports `Result` from 'antd' and `debugLog` from `@/core/utils/debugLog`
    - `ErrorBoundary.tsx` `componentDidCatch` calls `debugLog('error', ...)` with error message and componentStack
    - `src/core/messaging/runtimeEnvelope.ts` exports `validateEnvelope` and `Envelope` type
    - `runtimeEnvelope.ts` uses Zod's `z.enum` for the `source` field with the four valid surface names
    - `grep -rn 'innerHTML\|dangerouslySetInnerHTML' src/core/stores/ src/core/utils/ src/core/components/ src/core/messaging/ src/entrypoints/background.ts` returns zero matches (HARD-10 enforcement)
    - `pnpm tsc --noEmit` passes (all new files type-check)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/ErrorBoundary.test.tsx tests/core/runtimeEnvelope.test.ts && pnpm tsc --noEmit && grep -rn 'innerHTML\|dangerouslySetInnerHTML' src/core/stores/ src/core/utils/ src/core/components/ src/core/messaging/ src/entrypoints/background.ts; test $? -eq 1</automated>
  </verify>
  <done>ErrorBoundary catches render errors and shows AntD Result fallback with retry/reload (HARD-06). RuntimeEnvelope validates cross-context messages with Zod schemas (HARD-05). All source files HARD-10 clean (no html injection patterns). Both have passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.session ↔ renderer surfaces | Workspace state crosses between Side Panel and Full App through shared storage |
| chrome.runtime.onMessage ↔ background SW | Incoming messages from any extension context (including content scripts in later phases) |
| React render tree ↔ ErrorBoundary | Unhandled errors in component subtree cross into error boundary |
| User input (future) ↔ AntD components | Currently no user input in this plan, but static AntD API calls bypass ConfigProvider context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-SW | Denial of Service | background.ts | high | mitigate | HARD-08: All chrome.*.addListener calls happen synchronously at module load. Main callback is non-async. Verified by test that inspects function signature and verifies no setTimeout wrapping. |
| T-02-XSS | Tampering | All components | critical | mitigate | HARD-10: Zero tolerance for innerHTML/dangerouslySetInnerHTML. Static grep gate runs after every task. Use AntD Typography or React-safe text rendering exclusively. |
| T-02-ERR | Denial of Service | All pages | high | mitigate | HARD-06: ErrorBoundary class component wraps every page route. On error, renders AntD Result (not white screen). componentDidCatch logs to debugLog for diagnostics. |
| T-02-CATCH | Repudiation | All modules | medium | mitigate | HARD-09: All catch blocks call debugLog with structured metadata (level, message, data). debugLog gates on __DEV__ for production safety. No empty catches. |
| T-02-MSG | Spoofing | runtimeEnvelope.ts | high | mitigate | HARD-05: validateEnvelope uses Zod to enforce type, source (enum of 4 valid surfaces), and payload shape. Invalid/spoofed messages are logged and rejected before processing. |

**Threat disposition summary:** 5 threats — 5 mitigated, 0 accepted, 0 transferred.
</threat_model>

<verification>
<automated>
# Run all plan tests + type check + HARD-10 static gate
pnpm vitest run tests/core/themeStore.test.ts tests/core/workspaceStore.test.ts tests/core/background.test.ts tests/core/ErrorBoundary.test.tsx tests/core/runtimeEnvelope.test.ts
pnpm tsc --noEmit
grep -rn 'innerHTML\|dangerouslySetInnerHTML' src/core/stores/ src/core/utils/ src/core/components/ src/core/messaging/ src/entrypoints/background.ts; test $? -eq 1
# Verify synchronous SW: confirm main() in defineBackground is NOT async
grep -n 'async' src/entrypoints/background.ts; test $? -eq 1
</automated>
</verification>

<success_criteria>
- [ ] All 5 test files pass (themeStore 4+, workspaceStore 4+, background 5+, ErrorBoundary 4+, runtimeEnvelope 4+)
- [ ] `pnpm tsc --noEmit` exits 0 for all source files
- [ ] Background SW main() is non-async (HARD-08)
- [ ] Zero instances of innerHTML/dangerouslySetInnerHTML in plan files (HARD-10)
- [ ] ErrorBoundary is a React class component with AntD Result fallback (HARD-06)
- [ ] RuntimeEnvelope uses Zod enum validation for message source (HARD-05)
- [ ] debugLog is gated on `__DEV__` and called from ErrorBoundary's componentDidCatch (HARD-09)
- [ ] ThemeStore state shape is `{ mode }` only (< 100 bytes in sync storage)
- [ ] WorkspaceStore state shape is 4 metadata fields only (no message bodies)
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-02-SUMMARY.md` when done
</output>

## Artifacts this plan produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `ThemeMode` | type | `themeStore.ts` | Union type: `'light' \| 'dark' \| 'auto'` |
| `ThemeState` | interface | `themeStore.ts` | Store shape — `{ mode: ThemeMode; setMode }` |
| `useThemeStore` | hook | `themeStore.ts` | Zustand hook with chrome.storage.sync persist |
| `Surface` | type | `workspaceStore.ts` | Union type: `'sidepanel' \| 'fullapp' \| 'popup'` |
| `WorkspaceState` | interface | `workspaceStore.ts` | Store shape — 4 metadata fields + setters |
| `useWorkspaceStore` | hook | `workspaceStore.ts` | Zustand hook with chrome.storage.session persist |
| `LogLevel` | type | `debugLog.ts` | `'debug' \| 'info' \| 'warn' \| 'error'` |
| `debugLog` | function | `debugLog.ts` | Structured logging gated on `__DEV__` |
| `default export` | function | `background.ts` | `defineBackground` entry — SW with synchronous listeners |
| `ErrorBoundary` | class | `ErrorBoundary.tsx` | React error boundary with AntD Result |
| `Props` | interface | `ErrorBoundary.tsx` | `{ children: ReactNode }` |
| `State` | interface | `ErrorBoundary.tsx` | `{ hasError: boolean; error: Error \| null }` |
| `MessageSource` | type | `runtimeEnvelope.ts` | `'background' \| 'sidepanel' \| 'fullapp' \| 'popup'` |
| `Envelope<T>` | interface | `runtimeEnvelope.ts` | Typed message envelope `{ type, source, payload, timestamp? }` |
| `validateEnvelope<T>` | function | `runtimeEnvelope.ts` | Zod validation + rejection of spoofed cross-context messages |
| `envelopeSchema` | const | `runtimeEnvelope.ts` | Zod schema for `Envelope` shape |
