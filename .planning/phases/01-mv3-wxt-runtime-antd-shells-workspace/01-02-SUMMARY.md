---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 02
subsystem: core-infrastructure
tags: [stores, background, error-boundary, messaging, logging]
key-files:
  - src/core/stores/themeStore.ts
  - src/core/stores/workspaceStore.ts
  - src/core/utils/debugLog.ts
  - src/core/components/ErrorBoundary.tsx
  - src/core/messaging/runtimeEnvelope.ts
  - src/entrypoints/background.ts
metrics:
  test_files: 6
  total_tests: 25
  passing_tests: 25
  hard_08_compliant: true
  hard_10_clean: true
---

# Summary: Plan 01-02 — Core Stores, Background SW, Safety Utilities

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1    | `7c7d999` | ThemeStore, WorkspaceStore with chrome.storage persistence |
| 2    | `7c7d999` | Background SW (synchronous listeners), debugLog utility |
| 3    | `7c7d999` | ErrorBoundary (class component, AntD Result), RuntimeEnvelope (Zod validation) |

## Deviations

- **WXT auto-imports**: `defineBackground` uses explicit import from `wxt/utils/define-background` instead of WXT auto-import because the auto-import type declarations were not generated by `wxt prepare`.
- **debugLog __DEV__ gate**: Uses `typeof __DEV__ === 'undefined'` guard so the function works in tests (where `__DEV__` is undefined) and is suppressed in production builds where WXT replaces `__DEV__` with `false`.

## Self-Check: PASSED

- [x] All 25 tests pass (themeStore 4, workspaceStore 4, background 5, debugLog 3, ErrorBoundary 4, runtimeEnvelope 5)
- [x] `pnpm tsc --noEmit` exits 0
- [x] background.ts main() is non-async (HARD-08)
- [x] Zero instances of innerHTML/dangerouslySetInnerHTML (HARD-10)
- [x] ErrorBoundary is a class component with AntD Result fallback (HARD-06)
- [x] RuntimeEnvelope uses Zod enum validation for message source (HARD-05)
- [x] debugLog gated on `__DEV__`, called from ErrorBoundary.componentDidCatch (HARD-09)
- [x] ThemeStore state shape is `{ mode }` only (< 100 bytes sync)
- [x] WorkspaceStore state shape is 4 metadata fields only (no message bodies)
