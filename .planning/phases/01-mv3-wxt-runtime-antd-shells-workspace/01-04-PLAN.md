---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/core/i18n/strings.ts
  - tests/core/i18n/strings.test.ts
autonomous: true
requirements: [CORE-01]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every user-visible string reachable in Phase 1 resolves through `t('key')` from `src/core/i18n/strings.ts`; no Phase-1 component carries an inline literal and no AntD locale default is reachable."
    - "`t()` resolves every Phase-1 key to a value that is not the key itself — a missing key returns its own name and the test makes that a failure rather than a silent rendering of `a11y.options` on screen."
    - "The canonical shell/state/error strings carry the values pinned in `01-UI-SPEC.md` § Copywriting Contract verbatim, including `Connected` without an exclamation mark, `Testing connection...` with three ASCII periods, and `Check connection` rather than `Connect Provider`."
    - "Every icon-only control's accessible name and tooltip string is pinned in the map and matches the UI-SPEC actions table, so no icon-only control depends on an AntD locale default."
    - "AntD default-chrome overrides are reachable as strings: `common.continue` resolves to `Continue`, `common.notNow` resolves to `Not now`, and `onboarding.providerPlaceholder` resolves to `Select an AI provider` rather than a locale value."
    - "Interpolation is available and total: `format(key, params)` replaces every `{token}` occurrence in the resolved value and returns the input unchanged when a token has no supplied param, so a missing param degrades to a visible placeholder rather than `undefined` reaching the DOM."
    - "Run twice, the string map is identical: `strings.ts` is a pure data module with no side effects, no Chrome access and no environment read, so importing it twice yields the same table."
    - "Two suites importing the module concurrently share one immutable table; nothing in the module mutates `strings` after definition."
    - "The `deferred.*` namespace carries `Not available yet`, `Preview data`, `This arrives in a later release.` and `This preview does not contact a provider.` verbatim, so `DeferredNotice` in plan `01-10` needs no new copy."
    - "The theme sync failure strings carry `Theme sync failed — your display mode is still applied.` and `Retry sync`, which name the problem and the next step respectively."
    - "Shell render failure strings carry `Something went wrong`, `Reload NowPilot to continue.` and `Reload`, so no AntD locale title or subtitle can appear in the `Result`."
  artifacts:
    - "src/core/i18n/strings.ts — the canonical Phase-1 string map plus `format(key, params)`"
    - "tests/core/i18n/strings.test.ts — the copy-exactness and coverage gate"
  key_links:
    - "`t('key')` in every Phase-1 component → one key in this module: a component that inlines a literal bypasses the map and cannot be translated or pinned."
    - "UI-SPEC § Copywriting Contract → this module: the map is the single place the pinned copy lives, so the contract has one implementation."
    - "`deferred.*` keys → the `data-np-backing` regions in plan `01-10`: the marker's visible text and its full-sentence `aria-label` both come from here."
  prohibitions:
    - "The string map must not contain a copy of any credential, sentinel value, example key shape or provider secret — all four onboarding failure labels describe the failure, never the value that caused it."
    - "No Phase-1 string may promise a capability the phase does not deliver: the composer body copy names exactly one next step and never implies a working send path, and no string may claim provider connectivity is implemented."
    - "The map must not keep a second spelling of a canonical string; where the prototype diverged, the prototype value is replaced, not kept alongside."
---

<objective>
Land the complete canonical Phase-1 string map and its `format()` helper, so every later plan consumes copy instead of inventing it.

Purpose: the UI-SPEC pins every user-visible Phase-1 string under `§ Copywriting Contract`, and PRODUCT_SPEC §0.2 forbids paraphrasing canonical strings. Today those strings are scattered, several prototype values diverge from Appendix B, and the `t()` helper has no interpolation — so `Step {n} of 4` and `Connection failed: …` cannot be rendered from the map at all. Landing the map once, before the parallel expansion plans, keeps `src/core/i18n/strings.ts` under a single writer and makes copy-exactness a test rather than a review habit.
Output: `src/core/i18n/strings.ts` carrying every Phase-1 key with its pinned value plus `format(key, params)`, and `tests/core/i18n/strings.test.ts` asserting value-exactness and full key coverage.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md
@.planning/product/PRODUCT_SPEC.md
@src/core/i18n/strings.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the canonical Phase-1 string map and the `format()` helper</name>
  <files>src/core/i18n/strings.ts</files>
  <behavior>
    - Case: `t('chat.empty')` returns `Start a conversation`.
    - Case: `t('onboarding.connected')` returns `Connected` — no trailing punctuation.
    - Case: `t('onboarding.testing')` returns `Testing connection...` with three ASCII periods, not a single ellipsis glyph.
    - Case: `t('onboarding.validate')` returns `Check connection`.
    - Case: `t('onboarding.skip')` returns `Skip`.
    - Case: `t('a11y.options')` returns `Options`; `t('a11y.closeDialog')` returns `Close`.
    - Case: `t('common.continue')` returns `Continue`; `t('common.notNow')` returns `Not now`.
    - Case: `t('deferred.reasonFixture')` returns `This preview does not contact a provider.`
    - Case: `t('a.key.that.does.not.exist')` returns `a.key.that.does.not.exist` (fallback preserved).
    - Case: `format('onboarding.stepIndicator', { n: 2 })` returns `Step 2 of 4`.
    - Case: `format('onboarding.failed', { error: 'the provider rejected this key' })` returns the resolved value with the error slot substituted.
    - Case: `format('onboarding.stepIndicator', {})` leaves the unresolved token visible rather than producing `undefined`.
  </behavior>
  <read_first>
    - src/core/i18n/strings.ts (the file being modified — the existing `t()` implementation and the current key set)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md § Copywriting Contract (all four canonical string tables and the prototype→canonical corrections table)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md § Fixture-Backed & Deferred Marking Convention (the four `deferred.*` values)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md § Theme Contract, § Handoff Interaction Contract, § Command Palette Contract
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md § Pattern 5 (the canonical provider validation codes)
  </read_first>
  <action>
Extend `src/core/i18n/strings.ts` into the canonical Phase-1 map. Keep the module's existing shape: a single `Record<string, string>` named `strings`, exported `t(key)` returning `strings[key] ?? key`, and no side effects, no Chrome access and no environment read.

**Required keys and exact values** (replace any existing key whose value diverges):

Shells, states and errors — `chat.empty` = `Start a conversation`; `chat.noProvider` = `Configure an AI provider in Settings first.`; `chat.composerPlaceholder` = `Ask anything, choose a workflow, or use / prompts`; `workspace.handoffPending` = `Opening workspace in standalone view...`; `workspace.handoffComplete` = `Workspace opened in standalone view.`; `standalone.openFailed` = `Failed to open Standalone view`; `standalone.minWidth` = `This view is optimized for wider screens; open the side panel for narrow layouts.`; `standalone.globalSearchPlaceholder` = `Search notes, tags, or content…`; `theme.syncFailed` = `Theme sync failed — your display mode is still applied.`; `theme.syncRetry` = `Retry sync`; `shell.errorTitle` = `Something went wrong`; `shell.errorBody` = `Reload NowPilot to continue.`; `shell.errorReload` = `Reload`; `provider.credentialsCleared` = `Provider credentials must be configured again after secure credential storage is available.`; `provider.credentialsClearedDismiss` = `Dismiss`.

Onboarding — `onboarding.step1Title` = `Meet NowPilot`; `onboarding.step2Title` = `Pick a provider`; `onboarding.step3Title` = `Enter your API key`; `onboarding.step4Title` = `Validate connection`; `onboarding.stepIndicator` = `Step {n} of 4`; `onboarding.providerPlaceholder` = `Select an AI provider`; `onboarding.testing` = `Testing connection...`; `onboarding.validate` = `Check connection`; `onboarding.connected` = `Connected`; `onboarding.failed` = `Connection failed: {error}`; `onboarding.retry` = `Retry`; `onboarding.editKey` = `Edit key`; `onboarding.finish` = `Finish setup`; `onboarding.continue` = `Continue`; `onboarding.back` = `Back`; `onboarding.skip` = `Skip`; `onboarding.switchToFullSetup` = `Switch to Full setup`; `onboarding.showKey` = `Show API key`; `onboarding.hideKey` = `Hide API key`; `onboarding.keyPlaceholder` = `Paste your API key`; `onboarding.step1Body` = `NowPilot is your workspace copilot. Setup takes four short steps.`; `onboarding.step2Body` = `Choose the AI provider NowPilot will use. You can change this later.`; `onboarding.step3Body` = `Paste your provider key. It stays in this screen and is not saved in this release.`; `onboarding.step4Body` = `Check the connection before finishing setup.`

Typed validation-failure labels (the `{error}` slot values) — `provider.error.PROVIDER_AUTH` = `the provider rejected this key`; `provider.error.PROVIDER_5XX` = `the provider is unavailable`; `provider.error.NETWORK` = `the network is unavailable`; `provider.error.PROVIDER_CHECK_FAILED` = `the connection could not be verified`.

Accessible names — `a11y.options` = `Options`; `a11y.switchToFullChat` = `Switch to Full chat`; `a11y.attach` = `Attach`; `a11y.chatHistory` = `Chat history`; `a11y.newChat` = `New chat`; `a11y.send` = `Send message`; `a11y.help` = `Help`; `a11y.feedback` = `Feedback`; `a11y.globalSearch` = `Global search`; `a11y.moreActions` = `More actions`; `a11y.collapseSidebar` = `Collapse sidebar`; `a11y.expandSidebar` = `Expand sidebar`; `a11y.closeDialog` = `Close`.

Palette chrome — `commands.placeholder` = `Search commands…`; `commands.noResults` = `No matching commands — try a different search term`; `commands.category.navigation` = `Navigation`; `commands.category.theme` = `Appearance`; `commands.category.system` = `System`. Command **labels and descriptions** are registry data owned by plan `01-08`, not keys in this module.

Deferred marker — `deferred.tag` = `Not available yet`; `deferred.fixtureTag` = `Preview data`; `deferred.reasonDeferred` = `This arrives in a later release.`; `deferred.reasonFixture` = `This preview does not contact a provider.`; `deferred.phaseBody` = `This page arrives in Phase {phase}.`

Common — `common.continue` = `Continue`; `common.notNow` = `Not now`.

Destructive confirmation prompt — `command.reloadExtension.confirm` = `Reload extension: this restarts the extension and discards unsaved state. Continue?`

**Divergences to correct, not keep:** any existing value equal to `Connected!`, `Skip for now`, `Try again`, `Connect Provider`, `Open in Full Tab`, `Choose a provider`, `Start chatting`, `Something went wrong. Please reload the extension.` or a category literal `Theme`/`Extension` is replaced by the canonical key/value above; do not keep the old spelling alongside the new one.

**Interpolation.** Add and export `format(key: string, params: Record<string, string | number>): string`. It resolves `t(key)` then replaces every `{token}` occurrence with `String(params[token])`; when a token has no supplied param it leaves the `{token}` text intact so a missing param is visible rather than rendering `undefined`. Keep `format` pure and total — it never throws on a missing key or an empty params object.

**Legacy keys.** The prototype keys still consumed by `src/components/OnboardingModal.tsx`, `src/components/chat/**`, `src/components/options/**` and `src/components/standalone/**` must remain resolvable until their owning plan removes the consumer. Group them under a single leading comment block that states: `// LEGACY — prototype-only keys, removed by the plan that deletes their last consumer (01-09 onboarding, 01-10 pages, 01-11 dev shell). Do not add keys here.` Do not delete a key that still has a consumer — a deleted key renders its own name on screen.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/components tests/core</automated>
    <fails_when>typecheck exits non-zero, or any existing component or core suite reports a failure (a removed legacy key renders its own name and breaks a literal assertion)</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('src/core/i18n/strings.ts','utf8');const req=['chat.empty','chat.noProvider','workspace.handoffPending','workspace.handoffComplete','standalone.openFailed','standalone.minWidth','theme.syncFailed','theme.syncRetry','shell.errorTitle','shell.errorBody','shell.errorReload','provider.credentialsCleared','commands.placeholder','commands.noResults','onboarding.step1Title','onboarding.step4Title','onboarding.stepIndicator','onboarding.providerPlaceholder','onboarding.testing','onboarding.validate','onboarding.connected','onboarding.failed','onboarding.finish','onboarding.switchToFullSetup','onboarding.keyPlaceholder','a11y.closeDialog','deferred.tag','deferred.fixtureTag','deferred.reasonDeferred','deferred.reasonFixture','common.continue','common.notNow','command.reloadExtension.confirm'];const miss=req.filter(k=>!s.includes(\"'\"+k+\"'\"));console.log(miss.length?('MISSING:'+miss.join(',')):'ALL_PRESENT')"</automated>
    <fails_when>the output is anything other than `ALL_PRESENT`</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/core/i18n/strings.ts` exports `t` and `format`; `t` keeps the `strings[key] ?? key` fallback.
    - `format` replaces `{token}` occurrences and leaves an unsupplied token intact.
    - The module performs no Chrome access, no `fetch`, no storage read and no environment read.
    - `t('onboarding.connected')` is exactly `Connected` and no key's value ends with an exclamation mark.
    - `t('onboarding.testing')` contains three ASCII periods and no ellipsis glyph.
    - Every key in the UI-SPEC actions-and-accessible-names table exists with its pinned value.
    - No key's value contains a credential-shaped example, an API key pattern or a sentinel value.
    - Legacy prototype keys remain resolvable and sit under a single `// LEGACY` comment block.
    - No prototype divergence value listed in the UI-SPEC corrections table survives anywhere in the file as part of a key value.
  </acceptance_criteria>
  <done>The canonical Phase-1 string map and `format()` helper exist, every pinned key resolves to its pinned value, and no consumer regressed.</done>
</task>

<task type="auto">
  <name>Task 2: Copy-exactness and coverage gate</name>
  <files>tests/core/i18n/strings.test.ts</files>
  <read_first>
    - src/core/i18n/strings.ts (the module under test)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md § Copywriting Contract (the source of every expected value)
    - tests/core/theme/ThemeStore.test.ts (the assertion style convention for a pure-module suite)
  </read_first>
  <action>
Create `tests/core/i18n/strings.test.ts`. Assert, as separate cases:

1. **Value exactness.** A table-driven case that maps each Phase-1 key to its pinned value and asserts `t(key)` strictly equals it. Include `onboarding.connected`, `onboarding.testing`, `onboarding.validate`, `onboarding.skip`, `onboarding.finish`, `commands.category.theme`, `shell.errorTitle`, `shell.errorBody`, `shell.errorReload`, `deferred.reasonFixture`, `common.continue`, `common.notNow`.
2. **No self-resolving keys.** For the full required-key list, assert `t(key) !== key` — a missing key renders its own name and must be a red test.
3. **Interpolation.** Assert `format('onboarding.stepIndicator', { n: 1 })` equals `Step 1 of 4`, `format('onboarding.stepIndicator', { n: 4 })` equals `Step 4 of 4`, and `format('onboarding.stepIndicator', {})` still contains `{n}`.
4. **No interpolation left unrendered in a final string.** Assert no Phase-1 key other than the two interpolation carriers contains a `{` character.
5. **Locale-default override coverage.** Assert `t('common.continue')`, `t('common.notNow')`, `t('onboarding.providerPlaceholder')` and `t('a11y.closeDialog')` all differ from the AntD locale defaults they override; assert each is non-empty and not equal to its key.
6. **Credential-shape absence.** Assert that no value in the map matches a provider-key shape: no value contains a run of characters prefixed by a key-style prefix, no value contains `sk-`, and no value contains a 32-or-more character unbroken alphanumeric run. This is the string map's own sentinel-absence check.
7. **Purity.** Import the module twice and assert the resolved value table is identical and that `Object.isFrozen`-style mutation safety holds: mutating the returned value of `t()` does not change the next call's result (values are strings, so assert two successive calls are strictly equal after a failed mutation attempt).
  </action>
  <verify>
    <automated>npx vitest run tests/core/i18n/strings.test.ts</automated>
    <fails_when>exit code is non-zero, or any of the seven cases is reported as failed or skipped, or the suite reports fewer than seven test cases</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>exit code is non-zero, or any output line contains `error TS`</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/core/i18n/strings.test.ts` exists and passes with at least seven cases.
    - The value-exactness case is table-driven and covers every shell/state/error key plus every onboarding action label.
    - The no-self-resolving case enumerates the full required-key list, not a sample.
    - The suite contains a case asserting that no map value carries a provider-key shape.
    - No case asserts against a regex that would match a partial word (assertions are strict equality).
  </acceptance_criteria>
  <done>Copy-exactness and key coverage are locked by a seven-case suite, including the string map's own credential-shape absence check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| string map → rendered DOM | Every Phase-1 user-visible string crosses this boundary; a missing key renders its own name, and a wrong value misstates the product's behaviour. |
| error label → user | The onboarding failure strings are the only place a validation failure is described; they must describe the failure and never the credential. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-16 | Information disclosure | `provider.error.*` labels and `onboarding.failed` | high | mitigate | The four failure labels describe the failure (key rejected, provider unavailable, network unavailable, unverifiable) and the `{error}` slot takes a label, never a credential; `tests/core/i18n/strings.test.ts` asserts no map value carries a provider-key shape. |
| T-1-17 | Spoofing / Transparency | `chat.composerPlaceholder` and the empty-state body | medium | mitigate | The copy names exactly one next step and does not imply a working send path; plan `01-02`'s shell suite asserts the send control is `disabled` with `data-np-backing="deferred"`, so the words and the control agree. |
| T-1-18 | Repudiation | Missing key silently renders its own name | medium | mitigate | The no-self-resolving case enumerates the full required-key list and fails on any key that resolves to itself. |
| T-1-19 | Tampering | Locale defaults reachable | medium | mitigate | `common.continue`, `common.notNow`, `onboarding.providerPlaceholder` and `a11y.closeDialog` are asserted to differ from the AntD defaults they override. |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install occurs in this plan. The single phase install is performed in `01-02` under the Package Legitimacy Audit verdict `OK`; any version bump requires a blocking human checkpoint. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/core/i18n/strings.test.ts` is green with at least seven cases.
- `npx vitest run tests/components tests/core` is green (no legacy key was deleted while a consumer still used it).
- The required-key presence probe prints `ALL_PRESENT`.
</verification>

<success_criteria>
- Every user-visible Phase-1 string resolves through one key in `src/core/i18n/strings.ts`.
- Copy-exactness against the UI-SPEC is an automated test, not a review step.
- Interpolation exists and is total, so `Step {n} of 4` and `Connection failed: …` render from the map.
- No credential-shaped value appears anywhere in the map.
</success_criteria>

## Planner Assumptions (flagged)

The UI-SPEC pins step **titles** and the step indicator but not step **bodies**, and it pins `onboarding.failed` with a bracketed `[error]` token while its own bracket convention defines bracketed tokens as action labels. These gaps are resolved by the planner and must be visible to the operator:

| # | Assumption | Where | Risk if wrong |
|---|------------|-------|---------------|
| P1 | The four onboarding step bodies (`onboarding.step1Body` … `step4Body`) are planner-authored copy consistent with the pinned titles. | `src/core/i18n/strings.ts` | Operator rewrites four strings; no structural impact. |
| P2 | `onboarding.failed` is authored as `Connection failed: {error}` (curly interpolation slot) rather than the UI-SPEC's `Connection failed: [error]`, because the same UI-SPEC defines bracketed tokens as action labels. Rendered output is unchanged. | `src/core/i18n/strings.ts`, `01-MIGRATION-INVENTORY.md` | A copy reviewer may prefer the literal bracketed form; changing the token is a one-line edit plus its test row. |
| P3 | The four typed validation failure labels (`provider.error.*`) are planner-authored because the UI-SPEC pins the typed codes but not their human text. | `src/core/i18n/strings.ts` | Operator rewrites four strings. |
| P4 | `deferred.phaseBody` (`This page arrives in Phase {phase}.`) is planner-authored; the UI-SPEC's marker table lists only four `deferred.*` keys and D-16 additionally requires the deferred panel to name its owning roadmap phase. | `src/core/i18n/strings.ts`, plan `01-10` | Operator rewrites one string. |

## Artifacts this phase produces

| Kind | Symbol / path | Created by |
|------|---------------|------------|
| Module | `src/core/i18n/strings.ts` — canonical Phase-1 string map | Task 1 |
| Function | `t(key)` (retained) | Task 1 |
| Function | `format(key, params)` | Task 1 |
| Test | `tests/core/i18n/strings.test.ts` | Task 2 |

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-04-SUMMARY.md` when done
</output>
