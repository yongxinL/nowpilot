---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/core/error/errorCodes.ts
  - src/core/error/debugLog.ts
  - src/core/security/TraceRedactor.ts
  - src/core/i18n/index.ts
  - src/core/prompts/keymap.ts
  - src/core/components/ErrorBoundary.tsx
  - src/core/components/PortableMarkdown.tsx
  - src/core/components/MinimalMode.tsx
  - src/core/components/FocusTrap.tsx
  - tests/core/error/debugLog.test.ts
  - tests/components/ErrorBoundary.test.tsx
  - tests/components/PortableMarkdown.test.tsx
  - tests/components/MinimalMode.test.tsx
  - tests/components/FocusTrap.test.tsx
autonomous: true
requirements: [RUNTIME-01, RUNTIME-02, WSPC-05]
must_haves:
  truths:
    - "Every catch calls debugLog(code, …) with a canonical §C.2 error code (Golden Rule 9) — debugLog is the single entry point for error observability; no direct console.error except inside debugLog itself."
    - "debugLog receives (code, message, options?: { error?: Error; context?: string; extra?: Record<string, unknown>; addonId?: string; module?: string; silent?: boolean }) and routes all string content through TraceRedactor.redact before persist/UI/export (R-10)."
    - "ErrorBoundary is a React class component that catches render errors, logs via debugLog, shows a user-facing fallback (STR.chat.errorRetry), and never renders raw error text."
    - "PortableMarkdown renders @ant-design/x-markdown PortableMarkdown with skipHtml: true; DOMPurify sanitizes any HTML before rendering (R-7/security: never render AI/tool output raw); addon-provided content is always trust: 'retrieved'. It is the ONLY markdown renderer in the phase — the banned third-party markdown renderer packages are never imported."
    - "MinimalMode renders a read-only spinner state (STR.chat.minimalMode) with no input box, no send button; used when budget is exhausted (Graceful Degradation §2.4)."
    - "FocusTrap traps focus within a modal/dialog container using keydown Tab handling and returns focus to the trigger on unmount."
  artifacts:
    - src/core/error/errorCodes.ts (ErrorCode canonical §C.2 list, phase subset)
    - src/core/error/debugLog.ts
    - src/core/security/TraceRedactor.ts (redact placeholder — R-10 audit point)
    - src/core/i18n/index.ts (i18n wrapper over STR)
    - src/core/prompts/keymap.ts (keyboard shortcut constants)
    - src/core/components/ErrorBoundary.tsx, PortableMarkdown.tsx, MinimalMode.tsx, FocusTrap.tsx
    - tests/core/error/debugLog.test.ts, tests/components/*.test.tsx
  key_links:
    - "debugLog routes through TraceRedactor (R-10) — redaction is mandatory before persist/UI/export; ErrorBoundary/MessageBus/EventBus all route errors through debugLog."
    - "PortableMarkdown is the ONLY renderer for markdown content; consumed by chat/notes in 01-08/01-09."
  flagged_assumptions:
    - statement: "RUNTIME-02 debugLog — TraceRedactor.redact is a thin pass-through placeholder until its full implementation in the security phase; debugLog is contract-stable."
      verification: flagged-unverified
    - statement: "WSPC-05 focus management — FocusTrap verified via testing-library in jsdom; real keyboard-interaction coverage deferred to browser e2e."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Build the shared diagnostics, i18n, keyboard shortcut, and core React component layer: canonical §C.2 error codes + debugLog (Golden Rule 9), i18n wrapper over STR, keymap constants, and the four cross-cutting React components (ErrorBoundary, PortableMarkdown, MinimalMode, FocusTrap).

Purpose: Every later plan (workspace, registry, shells, pages) uses these. Golden Rule 9 and R-10 are only satisfiable with a single debugLog entry point.
Output: 5 source files (2 error + 1 TraceRedactor placeholder, 1 i18n, 1 keymap) + 4 React components + 5 test files.
Scope note (deliberate, W-14): this plan batches 13 files because the four core components are tiny (<60 lines each) and share the ErrorBoundary/debugLog contract — splitting them would fragment one concern across two waves at higher coordination cost.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-17)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (validation map, security domain)
@.planning/PRODUCT_SPEC_v0_1.md (Appendix C.2 line ~4918, Appendix A, Appendix B)

# Depends on 01-01 (vitest/tsx config, antd pins) and 01-02 (STR, PROMPTS). Components imported by 01-08/01-09.
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | No sidePanel code — delegated to 01-06 |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | Components are UI-layer; NOT imported by content scripts (isolation enforced in 01-07/01-09) |
| P5 | Message contracts bypassing RuntimeEnvelope | No messaging here — handoff to 01-03 |
| P6 | matchMedia-in-jsdom | Handled in 01-01 (setup.ts polyfill) |
| P7 | zustand persist for cross-surface | No persist here — delegated to 01-05/01-06 |

<tasks>

<task type="auto">
  <name>Task 1: Error codes + debugLog (Golden Rule 9 entry point)</name>
  <files>
    src/core/error/errorCodes.ts, src/core/error/debugLog.ts, src/core/security/TraceRedactor.ts, tests/core/error/debugLog.test.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix C.2 (lines 4918-4961) — canonical error codes
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (security domain — TraceRedactor line ~230)
  </read_first>
  <action>
    Create src/core/error/errorCodes.ts exporting the canonical ErrorCode union and ERROR_CODES const, sourced from Appendix C.2 and restricted to the phase subset: MSG_UNKNOWN_TYPE, MSG_DESERIALIZE, MSG_SERIALIZE, PORT_DISCONNECTED, CONNECT_FAILED, TABS_QUERY, CONTENT_EXTRACT, CONTENT_CAPABILITIES, STORE_READ, STORE_WRITE, STORE_SYNC, CHROME_ON_CHANGED, WORKSPACE_INIT, WORKSPACE_START, WORKSPACE_STOP, WORKSPACE_SNAPSHOT, WORKSPACE_HANDOFF, WORKSPACE_MIRROR, WORKSPACE_ROUTER, REGISTRY_INIT, THEME_INIT, THEME_WRITE, THEME_ON_CHANGED, THEME_MATCH_MEDIA, CMDK_QUERY, CMDK_COMMAND, ONBOARDING_WRITE, ONBOARDING_DONE, EVT_HANDLER, BRIDGE_PUBLISH, BRIDGE_SUBSCRIBE, BRIDGE_LISTENER, NETWORK_STATUS, COMPONENT_RENDER, COMPONENT_UNMOUNT, PROMISE_REJECT, SIDEPANEL_BEHAVIOR, UNKNOWN. (SIDEPANEL_BEHAVIOR is added for the background LifecycleManager setPanelBehavior catch — 01-09 Task 3. Full canonical list lives in spec Appendix C.2; later phases extend this file — never duplicate or re-export elsewhere.) Each code is a string literal constant. Path reconciliation (W-7, Golden Rule 2): §18 create list names `src/core/log/debugLog.ts`; this plan deliberately homes debugLog at `src/core/error/debugLog.ts` co-located with errorCodes.ts because the two are a single contract (codes + the only logger) — every plan in this phase imports from `@/core/error/`, and a later phase may relocate to `log/` if the spec's split becomes load-bearing (documented deviation, no invented semantics). Create src/core/error/debugLog.ts exporting a debugLog(code: ErrorCode, message: string, options?: { error?: Error; context?: string; extra?: Record<string, unknown>; addonId?: string; module?: string; silent?: boolean }): void that (a) never throws, (b) routes every string argument through TraceRedactor.redact — import { redact } from '@/core/security/TraceRedactor', and create that module as a minimal placeholder exporting redact(s: string): string { return s } with a TODO comment noting the real redactor lands in the security phase (RESEARCH marks redaction as a later-phase concern; R-10 audit point), (c) prefixes output with [code], (d) calls console.error only with redacted payloads, (e) returns early when silent is true. Write tests/core/error/debugLog.test.ts: code+message emitted; error instance passed through; known secret-like payload strings are passed through redact (mock redact via vi.mock to prove the call); silent suppresses output; never throws on malformed input.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/error/debugLog.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    errorCodes.ts exports the phase subset of canonical codes; debugLog never throws, routes through redact, respects silent; tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/error/debugLog.test.ts` exits 0
    - `grep -c "WORKSPACE_INIT\|THEME_INIT\|REGISTRY_INIT\|CMDK_QUERY" src/core/error/errorCodes.ts` == 4
    - `grep -c "redact" src/core/error/debugLog.ts` >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: i18n wrapper + keymap constants</name>
  <files>
    src/core/i18n/index.ts, src/core/prompts/keymap.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix A (lines 3966-4024) — prompt keys
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (stack — no i18n framework)
  </read_first>
  <action>
    Create src/core/i18n/index.ts exporting a thin i18n wrapper over the STR const (from 01-02): getString(key: keyof typeof STR): string returning the STR value; formatString(template: string, params: Record<string, string|number>): string replacing {placeholder} tokens; t(key) alias for brevity; getLocale()/setLocale() stubs returning 'en' (no i18n framework per CONTEXT — plain constants; agent's Discretion, documented in a comment). Create src/core/prompts/keymap.ts exporting const KEYMAP with shortcut constants (CMD_K = 'mod+k', CMD_N = 'mod+n', CMD_B = 'mod+b', ESC = 'Escape', CMD_ENTER = 'mod+Enter') and isCmdK(e: KeyboardEvent): boolean matching mod+k on mac (metaKey) or ctrl+k elsewhere. Constants only — no UI logic.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    i18n wrapper exports getString/formatString/t + locale stubs; keymap exports KEYMAP + isCmdK; tsc passes.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "CMD_K\|isCmdK" src/core/prompts/keymap.ts` == 2
    - `grep -c "getString\|formatString" src/core/i18n/index.ts` == 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Core React components (ErrorBoundary, PortableMarkdown, MinimalMode, FocusTrap)</name>
  <files>
    src/core/components/ErrorBoundary.tsx, src/core/components/PortableMarkdown.tsx, src/core/components/MinimalMode.tsx, src/core/components/FocusTrap.tsx, tests/components/ErrorBoundary.test.tsx, tests/components/PortableMarkdown.test.tsx, tests/components/MinimalMode.test.tsx, tests/components/FocusTrap.test.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md §2.4 (budget degradation), Appendix B chat strings
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (security domain — DOMPurify line ~230; stack — @ant-design/x-markdown)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (components/ErrorBoundary, PortableMarkdown, MinimalMode, FocusTrap classification lines 116-145)
  </read_first>
  <action>
    Create src/core/components/ErrorBoundary.tsx: class component with componentDidCatch(error, errorInfo) → debugLog(COMPONENT_RENDER, error.message, {error, context: componentStack}); state {hasError}; renders children normally; on error renders a Card with STR.chat.errorRetry text and a "Try again" Button that resets state (optional onReset callback prop). NEVER renders raw error text to the user (R-10/security — redact before display; show generic STR text only). Create src/core/components/PortableMarkdown.tsx: wraps @ant-design/x-markdown's PortableMarkdown with skipHtml: true; sanitizes any HTML input via DOMPurify.sanitize before rendering (import dompurify — approved stack); accepts { content: string; trust?: 'retrieved'|'untrusted' } and sets a data-trust attribute for styling hooks; untrusted content is still sanitized (defense in depth — sanitization is unconditional); renders null for empty content. This is the ONLY markdown renderer in the phase — do not import any third-party markdown renderer package (banned). Create src/core/components/MinimalMode.tsx: renders a centered read-only state (STR.chat.minimalMode text; NO input box, NO send button, per §2.4 budget degradation). Create src/core/components/FocusTrap.tsx: wraps children in a container div with tabIndex={-1}, ref; onKeyDown captures Tab/Shift+Tab cycling focus among visible focusable elements (querySelectorAll('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])') filtered by offsetParent visibility); Escape calls onEscape (default noop); on mount focuses the first focusable unless autoFocus=false; on unmount restores focus to the element captured as active at mount. Write tests: ErrorBoundary.test.tsx (child throws → fallback text shows; reset button recovers; raw error message string absent from DOM); PortableMarkdown.test.tsx (markdown → html rendering; skipHtml strips inline HTML; script tags removed from untrusted content); MinimalMode.test.tsx (queryByRole('textbox') null; no send button; minimalMode string present); FocusTrap.test.tsx (Tab cycles inside; Escape triggers onEscape; focus returns to trigger on unmount). Components must not import any page/shell module.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/ErrorBoundary.test.tsx tests/components/PortableMarkdown.test.tsx tests/components/MinimalMode.test.tsx tests/components/FocusTrap.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>
    All 4 components render per contract; ErrorBoundary never shows raw errors; PortableMarkdown sanitizes + skipHtml; MinimalMode has no input/send; FocusTrap traps + returns focus; all 4 test files green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/components/ErrorBoundary.test.tsx tests/components/PortableMarkdown.test.tsx tests/components/MinimalMode.test.tsx tests/components/FocusTrap.test.tsx` exits 0
    - `grep -c "componentDidCatch" src/core/components/ErrorBoundary.tsx` == 1
    - `grep -c "DOMPurify\|skipHtml" src/core/components/PortableMarkdown.tsx` >= 1
    - `grep -rn "react-markdown" src/ | wc -l` == 0 (banned markdown renderer never imported)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI/tool output → UI | Untrusted markdown/HTML reaches PortableMarkdown; must be sanitized before render |
| render error → user | Component crashes must surface as generic fallback, never raw error text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-06 | Information Disclosure | debugLog/TraceRedactor | high | mitigate | All logged strings pass redact() before console/persist (R-10); debugLog never logs raw payloads; silent flag for sensitive flows |
| T-1-07 | Tampering (XSS) | PortableMarkdown | high | mitigate | skipHtml: true + DOMPurify.sanitize on any HTML input; trust flag is styling-only, sanitize is unconditional (defense in depth) |
| T-1-08 | Tampering | ErrorBoundary | medium | mitigate | Never renders raw error text; fallback uses STR.chat.errorRetry; error details only via debugLog (redacted) |
| T-1-09 | Spoofing | debugLog code param | low | accept | Callers are first-party extension modules; codes are compile-time typed union, low exploitability |
</threat_model>

<verification>
- `pnpm vitest run tests/core/error tests/components` green
- `pnpm tsc --noEmit` green
- Banned-markdown-renderer grep clean
</verification>

<success_criteria>
- Golden Rule 9 satisfiable: every catch → debugLog with canonical code
- R-10 redaction wired at the single log entry point
- All 4 core components exist, tested, dependency-safe
- STR/PROMPTS/keymap consumable by 01-08/01-09
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-04-SUMMARY.md` when done
</output>
