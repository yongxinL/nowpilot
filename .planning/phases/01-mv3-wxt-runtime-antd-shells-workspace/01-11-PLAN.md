---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 11
type: execute
wave: 2
depends_on: [01-10]          # WorkspaceSync becomes LIVE via 01-10's mount wiring — the WR-04 inbound-adoption hardening must ship with/after that activation
gap_closure: true
files_modified:
  - src/core/events/EventBus.ts
  - src/core/messaging/MessageBus.ts
  - src/core/runtime/BroadcastBus.ts
  - src/core/ai/ProviderRegistry.ts
  - src/core/workspace/WorkspaceStore.ts
  - src/core/workspace/WorkspaceSync.ts
  - src/core/background/BackgroundRouter.ts
  - tests/core/workspace/WorkspaceSync.test.ts
autonomous: true
requirements: [WSPC-03, WSPC-05]
must_haves:
  truths:
    - "The messaging layer's error logging is live (Golden Rule 9 / WR-01): EventBus, MessageBus, and BroadcastBus import the real debugLog and ERROR_CODES and make direct canonical calls (EVT_HANDLER / MSG_SERIALIZE) — comment-filtered grep for the ambient hook declaration across src/ == 0 and for the runtime type-check guard wrappers == 0; ProviderRegistry.notify catch logs ERROR_CODES.EVT_HANDLER (WR-08 — no empty catch)."
    - "WorkspaceSync.handleRemoteUpdate validates inbound state before adoption (WR-04): sanitizeStored is exported from WorkspaceStore and applied to the remote state; a malformed state payload or a foreign workspaceId is ignored; a matching workspaceId with a higher version is adopted — WorkspaceSync.test.ts covers all three cases (the existing adoption test is updated to a same-workspaceId fixture)."
    - "BackgroundRouter guards inbound envelope shape before any property access (WR-09): isRuntimeEnvelopeShape is exported from MessageBus and used in the onMessage listener — a malformed message from a valid sender gets a workerState.fail(MSG_DESERIALIZE, 'malformed envelope') reply instead of a synchronous throw; no manual type assertion on the inbound message remains in the listener."
    - "The full Phase 1 gate stays green with the hardening in place: pnpm verify:phase-1 exits 0; tests/core/events, tests/core/messaging, tests/core/runtime, tests/core/workspace suites all pass."
    - "D-09 — 'Configure provider' CTA deep-links to Options in the Standalone view via WorkspaceRouter; same behaviour as WR-05, lands with the standalone-nav work. Deferred: onboarding completes once a provider is configured (Phase 3)."
  artifacts:
    - src/core/events/EventBus.ts (real debugLog import; direct ERROR_CODES.EVT_HANDLER calls; guard wrappers removed)
    - src/core/messaging/MessageBus.ts (real debugLog import; direct ERROR_CODES.MSG_SERIALIZE call; isRuntimeEnvelopeShape exported)
    - src/core/runtime/BroadcastBus.ts (real debugLog import; direct ERROR_CODES.MSG_SERIALIZE call; guard wrappers removed)
    - src/core/ai/ProviderRegistry.ts (notify catch now logs ERROR_CODES.EVT_HANDLER)
    - src/core/workspace/WorkspaceStore.ts (sanitizeStored exported for shared T-1-13 inbound use)
    - src/core/workspace/WorkspaceSync.ts (handleRemoteUpdate routes through sanitizeStored + workspaceId scope gate)
    - src/core/background/BackgroundRouter.ts (isRuntimeEnvelopeShape guard + fail-envelope reply)
    - tests/core/workspace/WorkspaceSync.test.ts (updated adoption fixture + malformed/foreign-id negative tests)
  key_links:
    - "EventBus/MessageBus/BroadcastBus catch bodies → debugLog → TraceRedactor.redact (R-10 observability contract restored where it was dead)."
    - "WorkspaceSync inbound WORKSPACE_UPDATED path → WorkspaceStore.sanitizeStored (T-1-13 shape guard) → workspaceId scope gate (M.3) → version-LWW adoption."
    - "BackgroundRouter listener → MessageBus.isRuntimeEnvelopeShape (single shared guard, no duplicate implementation) → workerState.fail reply (Pitfall 5)."
flagged_assumptions:
  - statement: "WSPC-04 idempotency probe — 'What happens if this runs twice on the same input?': unresolved (spec-less probe fallback). FLAGGED ASSUMPTION, not a must-have truth. In-scope hardening: WorkspaceSync.start() re-entry is guarded by its stopped flag; the inbound adoption is version-LWW gated (idempotent echo suppression); BroadcastBus.startHeartbeat is stop-first."
    verification: flagged-unverified
  - statement: "WSPC-04 concurrency probe — 'If interrupted or run in parallel, what is guaranteed?': unresolved. FLAGGED ASSUMPTION. The 01-11 workspaceId scope gate changes cross-workspace adoption semantics: a genuine first-ever simultaneous boot of both surfaces (before either writes np_workspace) can briefly diverge workspaceIds and neither adopts the other — residual M.3 single-writer-election edge, recorded in this plan's threat model as accepted."
    verification: flagged-unverified
  - statement: "Full 11-row edge-probe list (RUNTIME-01…05, WSPC-01…03, WSPC-05) lives in 01-10-PLAN.md flagged_assumptions — this plan carries only the WSPC-04 rows it is most relevant to."
    verification: flagged-unverified
prohibitions: []
---

<objective>
Harden the messaging and workspace-sync layer that 01-10 just made LIVE, and restore Golden Rule 9 error observability where it is silently dead. Concretely: (1) rewire EventBus/MessageBus/BroadcastBus from the never-bound ambient hook to the real debugLog import with canonical §C.2 codes (REVIEW WR-01) and fill ProviderRegistry's empty catch (WR-08); (2) make WorkspaceSync.handleRemoteUpdate validate inbound state before adoption — reuse WorkspaceStore's T-1-13 sanitizer, add the M.3 workspaceId scope gate (REVIEW WR-04) — now that 01-10 activates sync in production, this path is reachable and must not adopt malformed or foreign-workspace snapshots; (3) guard BackgroundRouter's inbound cast with the shared shape predicate and reply a fail envelope instead of throwing synchronously (REVIEW WR-09).

Purpose: The 01-10 wiring turns the sync loop on; this plan makes that loop safe and observable. WR-01 is cheap (3 import swaps + guard removals) and removes dead error logging in exactly the modules the sync depends on — Golden Rule 9 ('every catch calls debugLog … no empty catches') is a hard project rule. WR-04 and WR-09 are the two message-deserialization/untrusted-inbound threats that become live with the wiring (security enforcement: ASVS L1, block-on high — all dispositions are mitigate, none block).
Output: 4 error-logging rewires, 1 shared sanitizer export + inbound adoption guard, 1 shared shape-guard export + listener hardening, updated WorkspaceSync tests, phase gate re-green.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-REVIEW.md (WR-01 lines 90-111, WR-04 lines 151-170, WR-08 lines 228-251, WR-09 lines 253-274 — the exact fixes)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VERIFICATION.md (anti-patterns table lines 180-190 — the WR-* findings this plan addresses)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md (conventions only — status fields validate-phase-owned, W-13; do NOT edit)

# Baseline: 01-10 wires the mounts (wave 1). This plan runs after it (wave 2) and hardens the now-live sync path. Requirement coverage: RUNTIME-01…05, WSPC-01/02/04 remain owned by the executed plans + 01-10; this plan claims WSPC-03 (messaging layer) and WSPC-05 (debugLog canonical §C.2 usage). No new packages are installed (package.json untouched — supply-chain threat T-1-SC from 01-01 remains the phase-level guard).

# REVIEW findings OUTSIDE this gap-closure scope — explicit deferral so the executor sees the decision: WR-05 (Options deep-link lands on Chat) and WR-06 (OptionsPage theme-save toast dead code) are NOT addressed by 01-10/01-11 — they are owned by their respective later work (the standalone-nav persistence work for WR-05; the ThemeStore write-path change that surfaces setMode failure for WR-06). WR-07 (debugLog emits options.extra without a redaction pass) is deferred to the security phase that implements real TraceRedactor — today redact is a pass-through stub so nothing leaks, and once real redaction lands it must apply inside debugLog per R-10. IN-04 (MSG_SERIALIZE vs CONNECT_FAILED) is already explicitly deferred in Task 1's action below (error-code registry churn without a spec C.2 update).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire dead error logging — real debugLog in EventBus/MessageBus/BroadcastBus (WR-01) + ProviderRegistry catch (WR-08)</name>
  <files>
    src/core/events/EventBus.ts, src/core/messaging/MessageBus.ts, src/core/runtime/BroadcastBus.ts, src/core/ai/ProviderRegistry.ts
  </files>
  <read_first>
    src/core/events/EventBus.ts (the file being modified — the ambient hook block and the two guarded catch bodies)
    src/core/messaging/MessageBus.ts (the file being modified — the ambient hook block and the guarded publish catch)
    src/core/runtime/BroadcastBus.ts (the file being modified — the ambient hook block and the guarded emit catch)
    src/core/ai/ProviderRegistry.ts (the file being modified — the notify catch, lines 35-38)
    src/core/error/debugLog.ts (the real implementation being imported — signature debugLog(code: ErrorCode, message: string, options?: DebugLogOptions))
    src/core/error/errorCodes.ts (canonical codes: EVT_HANDLER, MSG_SERIALIZE both present)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-REVIEW.md (WR-01 lines 90-111 — the exact rewire; WR-08 lines 228-251)
  </read_first>
  <action>
    In each of src/core/events/EventBus.ts, src/core/messaging/MessageBus.ts, src/core/runtime/BroadcastBus.ts:
    1. DELETE the never-bound ambient hook declaration block (the local DebugLogFn type alias plus the type-only global it declares) — it was written as a placeholder for 01-04 and 01-04 landed; the runtime never binds it, so every guarded catch body was dead.
    2. ADD the real imports next to the existing imports: `import { debugLog } from '@/core/error/debugLog';` and `import { ERROR_CODES } from '@/core/error/errorCodes';`. (The '@/core/error' modules carry no surface dependencies — Pitfall 4 'dependency-free core' is about zustand/React/antd, not shared error infra; WorkspaceSync already imports them.)
    3. REPLACE every runtime type-check guard wrapper (the condition that checked whether the hook was a function — always false in production) around the catch-body calls with direct unconditional calls:
       - EventBus.emit: both catch bodies become `debugLog(ERROR_CODES.EVT_HANDLER, \`EventBus handler error for event "${event}"\`, { error: err, context: 'EventBus.emit' });` and the scoped variant with context 'EventBus.emit.scope' — use the same messages and payload shapes the guards already passed.
       - MessageBus.publish: the catch body becomes `debugLog(ERROR_CODES.MSG_SERIALIZE, 'MessageBus.publish: runtime send failed', { error: err, context: 'MessageBus.publish' });` (keep the MSG_SERIALIZE code — IN-04's CONNECT_FAILED suggestion is explicitly deferred: not part of this gap, error-code registry churn without a spec C.2 update is out of scope).
       - BroadcastBus.emit: the catch body becomes `debugLog(ERROR_CODES.MSG_SERIALIZE, 'BroadcastBus.emit: runtime send failed', { error: err, context: 'BroadcastBus.emit' });`
    4. UPDATE each module's stale header comment that promised the deferred 01-04 wiring (the 'Deferred debugLog … 01-04 wires the real import' notes) — replace with a one-line note that the gap-closure plan 01-11 wired the real import; keep the rest of the header contract text intact.

    In src/core/ai/ProviderRegistry.ts (WR-08): replace the empty catch in notify() with `catch (err) { debugLog(ERROR_CODES.EVT_HANDLER, 'ProviderRegistry listener error', { error: err instanceof Error ? err : undefined, module: 'ProviderRegistry' }); }` — the file already imports debugLog and ERROR_CODES (verify, and add the imports if a tree-shake removed them). Keep the comment's intent ('a broken listener must never break the registry') in the new catch body's doc note.

    No behavior change beyond logging: handlers, whitelists, and dispatch logic stay byte-identical. No new test files — the existing EventBus/MessageBus/BroadcastBus suites assert behavior, not the logger binding, and must pass unchanged.
  </action>
  <verify>
    <automated>set -o pipefail; pnpm tsc --noEmit 2>&1 | tail -5 && grep -rn "import { debugLog }" src/core/events/EventBus.ts src/core/messaging/MessageBus.ts src/core/runtime/BroadcastBus.ts | grep -vc "://" && pnpm vitest run tests/core/events tests/core/messaging tests/core/runtime 2>&1 | tail -5</automated>
  </verify>
  <done>
    All three messaging modules import the real debugLog + ERROR_CODES with direct canonical calls; ProviderRegistry's catch logs EVT_HANDLER; the ambient hook declaration is gone from src/; the guarded wrapper pattern is gone; tsc + messaging/events/runtime suites green.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -rn "import { debugLog }" src/core/events/EventBus.ts src/core/messaging/MessageBus.ts src/core/runtime/BroadcastBus.ts | grep -vc "://"` returns 3
    - `grep -rn "import { ERROR_CODES }" src/core/events/EventBus.ts src/core/messaging/MessageBus.ts src/core/runtime/BroadcastBus.ts | grep -vc "://"` returns 3
    - `grep -c "ERROR_CODES.EVT_HANDLER" src/core/ai/ProviderRegistry.ts` >= 1
    - `pnpm vitest run tests/core/events tests/core/messaging tests/core/runtime` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Harden WorkspaceSync inbound adoption — shared sanitizer + workspaceId scope gate (WR-04)</name>
  <files>
    src/core/workspace/WorkspaceStore.ts, src/core/workspace/WorkspaceSync.ts, tests/core/workspace/WorkspaceSync.test.ts
  </files>
  <read_first>
    src/core/workspace/WorkspaceStore.ts (the file being modified — the private sanitizer at lines 60-79, NP_WORKSPACE_KEY, the onChanged LWW handler that already uses it)
    src/core/workspace/WorkspaceSync.ts (the file being modified — handleRemoteUpdate at lines 198-217)
    tests/core/workspace/WorkspaceSync.test.ts (the file being modified — the adoption test at lines 80-102 currently uses a DIFFERENT workspaceId and will FAIL under the new gate; it must be updated)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-REVIEW.md (WR-04 lines 151-170 — the fix sketch)
  </read_first>
  <action>
    In src/core/workspace/WorkspaceStore.ts: change the module-private sanitizer function declaration to `export function sanitizeStored(...)` — same body, same return contract (Partial<WorkspaceState> of the D-18 active fields, or null when malformed). Update its doc comment: it is now the SHARED T-1-13 inbound gate consumed by both the store's own onChanged handler and WorkspaceSync's remote-update path. Do not change its validation logic (workspaceId/conversationId strings, activeSurface enum, finite version >= 0, finite updatedAt, optional integer openedStandaloneTabId).

    In src/core/workspace/WorkspaceSync.ts: extend the existing store import to `import { useWorkspaceStore, sanitizeStored } from '@/core/workspace/WorkspaceStore';`. Rewrite handleRemoteUpdate so that instead of adopting the remote state verbatim it:
      1. keeps the existing shape pre-check on payload.state (object, non-null) — early return when it fails,
      2. runs the inbound state through sanitizeStored — a null result (malformed / missing D-18 fields) is IGNORED (log WORKSPACE_SYNC, return),
      3. adds the M.3 workspace-scope gate: when the sanitized workspaceId does not equal the local workspaceId, IGNORE (log WORKSPACE_SYNC, return) — the side panel is per-window and BroadcastBus delivers to ALL extension contexts, so a snapshot from another window's workspace must never be adopted,
      4. keeps the version-LWW gate (sanitized version must be a number > local version),
      5. adopts via `useWorkspaceStore.setState({ workspace: { ...local, ...sanitized } })` — merging keeps the inert D-18 fields from the local state (T-1-05), unlike the old verbatim swap,
      and keeps the existing debugLog(ERROR_CODES.WORKSPACE_SYNC, ...) calls for the adopted/ignored paths.

    In tests/core/workspace/WorkspaceSync.test.ts:
      - UPDATE the existing adoption test ('a WORKSPACE_UPDATED with a higher remote version merges into the store'): the remote fixture must now carry the LOCAL workspaceId (the test's freshWorkspace uses 'ws-local') with a higher version (e.g. version 9) — the other fields (conversationId 'conv-remote', activeSurface 'standalone') still assert the merge. This test currently passes a DIFFERENT workspaceId and would now be correctly rejected by the new scope gate — update it, do not delete it.
      - ADD a negative test: a WORKSPACE_UPDATED with a foreign workspaceId (e.g. 'ws-foreign') and a higher version is IGNORED — the store's workspaceId/version stay at the local values.
      - ADD a negative test: a malformed state payload (e.g. { state: { version: 99 } } — missing workspaceId/conversationId) is IGNORED — store unchanged, no throw.
      Keep the existing LWW-lower/equal test, handoff tests, whitelist test, and mirroring tests unchanged.
  </action>
  <verify>
    <automated>set -o pipefail; pnpm vitest run tests/core/workspace/WorkspaceSync.test.ts tests/core/workspace/WorkspaceStore.test.ts 2>&1 | tail -6 && pnpm tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>
    sanitizeStored exported and consumed by WorkspaceSync; handleRemoteUpdate shape-validates, scope-gates on workspaceId, and LWW-gates before adopting via a field-preserving merge; WorkspaceSync.test.ts covers adopted (same-id, higher version), foreign-id ignored, malformed ignored; all workspace suites green.
  </done>
  <acceptance_criteria>
    - `grep "export function sanitizeStored" src/core/workspace/WorkspaceStore.ts | grep -vc "://"` returns 1
    - `grep -c "sanitizeStored" src/core/workspace/WorkspaceSync.ts` >= 2 (import + call)
    - `grep "workspaceId !== local.workspaceId" src/core/workspace/WorkspaceSync.ts | grep -vc "://"` returns 1 (scope gate present)
    - `grep -c "ws-foreign" tests/core/workspace/WorkspaceSync.test.ts` >= 1 (foreign-id negative test present)
    - `pnpm vitest run tests/core/workspace/WorkspaceSync.test.ts tests/core/workspace/WorkspaceStore.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: BackgroundRouter inbound shape guard — shared predicate + fail-envelope reply (WR-09)</name>
  <files>
    src/core/messaging/MessageBus.ts, src/core/background/BackgroundRouter.ts
  </files>
  <read_first>
    src/core/messaging/MessageBus.ts (the file being modified — the module-private shape predicate at the bottom, lines 135-144)
    src/core/background/BackgroundRouter.ts (the file being modified — the onMessage listener at lines 47-76, specifically the type assertion on the inbound message and the whitelist check that reads .type from it)
    src/core/runtime/workerState.ts (fail signature: fail(code: ErrorCode, message: string, id?: string) — id defaults to a fresh operationId)
    src/core/error/errorCodes.ts (MSG_DESERIALIZE present)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-REVIEW.md (WR-09 lines 253-274 — the fix sketch: reuse the shared predicate, reply a fail envelope rather than throwing)
  </read_first>
  <action>
    In src/core/messaging/MessageBus.ts: change the module-private shape predicate declaration to `export function isRuntimeEnvelopeShape(...)` — same body (id/type/createdAt/source all typed), same doc intent. This is the SINGLE shared guard (MessageBus.dispatchInbound already uses it; do not duplicate it in BackgroundRouter).

    In src/core/background/BackgroundRouter.ts:
    1. Add the import: `import { isRuntimeEnvelopeShape } from '@/core/messaging/MessageBus';` (alongside the existing MessageBus import at the top).
    2. In the onMessage listener, immediately AFTER the existing sender.id guard (`if (sender.id !== chrome.runtime.id) return false;`) and BEFORE any property access, add the shape guard: when the inbound message fails the predicate, reply `sendResponse(workerState.fail(ERROR_CODES.MSG_DESERIALIZE, 'malformed envelope'))` and `return true;` — the sender's promise resolves with a fail envelope instead of hanging on a synchronous listener throw.
    3. REMOVE the manual type assertion on the inbound message (the `as` cast that typed the raw message) — after the predicate guard the message is narrowed by the type predicate, so the declaration becomes a plain const binding. Everything downstream (the whitelist check reading .type, dispatch, the ok/fail replies, the catch → EVT_HANDLER) stays unchanged.
    4. Update the listener's header comment: note the structural guard now precedes the whitelist check (validates SHAPE first, then TYPE), matching MessageBus.dispatchInbound's ordering.
     No new test file: fakeBrowser does not model sender.id for sendMessage dispatch (verified — no BackgroundRouter test exists today), so verification is by source assertion + tsc + the full suite. Do not add chrome-mock hacks to force a listener test.
     The final `pnpm verify:phase-1` run in the verify block (~2 min: eslint → prettier → tsc → wxt build → vitest → isolation) is the MANDATED phase gate and the accepted exception to the 30 s feedback-latency sampling threshold (nyquist_8b) — the fast targeted suites (tsc, tests/core/events, tests/core/messaging, tests/core/runtime) ran inside Tasks 1-2 for iteration; the full gate runs ONCE here as the final check.
  </action>
  <verify>
    <automated>set -o pipefail; pnpm tsc --noEmit 2>&1 | tail -3 && grep -c "isRuntimeEnvelopeShape" src/core/background/BackgroundRouter.ts && grep -rn "declare const debugLog\|typeof debugLog" src/core/events/ src/core/messaging/ src/core/runtime/ | grep -vc "://" && pnpm verify:phase-1 2>&1 | tail -8</automated>
  </verify>
  <done>
    BackgroundRouter validates envelope SHAPE before touching .type; a malformed message from a valid sender gets a MSG_DESERIALIZE fail-envelope reply; the manual assertion is gone; the shared predicate is exported once; tsc + full verify:phase-1 green.
  </done>
  <acceptance_criteria>
    - `grep "export function isRuntimeEnvelopeShape" src/core/messaging/MessageBus.ts | grep -vc "://"` returns 1
    - `grep -c "isRuntimeEnvelopeShape" src/core/background/BackgroundRouter.ts` >= 2 (import + guard call)
    - `grep -c "ERROR_CODES.MSG_DESERIALIZE" src/core/background/BackgroundRouter.ts` >= 1 (fail-envelope reply present)
    - `grep -rn "declare const debugLog\|typeof debugLog" src/core/events/ src/core/messaging/ src/core/runtime/ | grep -vc "://"` returns 0 (WR-01 regression guard — the ambient hook and its guards are fully gone from code lines)
    - `pnpm verify:phase-1` exits 0
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

**Modified files:**
- `src/core/events/EventBus.ts` — ambient hook declaration deleted; real `debugLog`/`ERROR_CODES` imports; both catch bodies now call `debugLog(ERROR_CODES.EVT_HANDLER, ...)` directly; stale 'deferred 01-04' header comment updated
- `src/core/messaging/MessageBus.ts` — same rewire (catch body → `debugLog(ERROR_CODES.MSG_SERIALIZE, ...)`); `isRuntimeEnvelopeShape` exported for BackgroundRouter
- `src/core/runtime/BroadcastBus.ts` — same rewire (catch body → `debugLog(ERROR_CODES.MSG_SERIALIZE, ...)`)
- `src/core/ai/ProviderRegistry.ts` — `notify()` catch now logs `ERROR_CODES.EVT_HANDLER` (WR-08)
- `src/core/workspace/WorkspaceStore.ts` — `sanitizeStored` exported (shared T-1-13 inbound gate)
- `src/core/workspace/WorkspaceSync.ts` — `handleRemoteUpdate` routes inbound state through `sanitizeStored`, gates on `workspaceId !== local.workspaceId` (M.3 scope) + version-LWW, adopts via field-preserving merge
- `src/core/background/BackgroundRouter.ts` — shape guard via `isRuntimeEnvelopeShape` before whitelist check; `workerState.fail(MSG_DESERIALIZE, 'malformed envelope')` reply; manual type assertion removed
- `tests/core/workspace/WorkspaceSync.test.ts` — adoption fixture updated to same-workspaceId; +2 negative tests (foreign workspaceId ignored, malformed state ignored)

**Symbols created / exported:**
- `export function sanitizeStored(value: unknown): Partial<WorkspaceState> | null` (WorkspaceStore — was module-private)
- `export function isRuntimeEnvelopeShape(message: unknown): message is RuntimeEnvelope<unknown>` (MessageBus — was module-private)

**No new test files, no new packages** (package.json untouched), no manifest changes, no new MessageType values (Pitfall 5 preserved).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension-context sender → BackgroundRouter listener | Any extension context can sendMessage; the listener must survive malformed/null payloads without throwing (WR-09) |
| remote WORKSPACE_UPDATED payload → WorkspaceSync.handleRemoteUpdate | BroadcastBus delivers to ALL extension contexts; a malformed or foreign-workspace snapshot must never be adopted (WR-04) |
| catch bodies → observability sink | Golden Rule 9 / R-10: every catch must actually reach debugLog (WR-01/WR-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-11-01 | Tampering | WorkspaceSync.handleRemoteUpdate inbound adoption (WR-04) | medium | mitigate | Inbound state runs through the shared sanitizeStored (T-1-13: D-18 shape guard, null on malformed), then the workspaceId scope gate (M.3 — foreign-workspace snapshots ignored), then version-LWW; adoption is a field-preserving merge (inert fields untouched). Negative tests added to WorkspaceSync.test.ts |
| T-1-11-02 | DoS | BackgroundRouter listener synchronous throw on malformed message (WR-09) | medium | mitigate | isRuntimeEnvelopeShape guard precedes any property access; malformed messages from valid senders get workerState.fail(MSG_DESERIALIZE, 'malformed envelope') — the sender promise resolves, never hangs; the §16.2 sender.id check is retained |
| T-1-11-03 | Information disclosure | Dead error logging in EventBus/MessageBus/BroadcastBus (WR-01) | medium | mitigate | Real debugLog import + canonical §C.2 codes (EVT_HANDLER/MSG_SERIALIZE) restore the R-10 observability contract; messages are identical to what the dead guards already passed — no new data is logged |
| T-1-11-04 | Repudiation / observability | ProviderRegistry.notify empty catch (WR-08) | low | mitigate | Catch logs ERROR_CODES.EVT_HANDLER; a broken listener is isolated and visible |
| T-1-11-05 | Tampering | First-boot cross-workspace divergence (consequence of the WR-04 scope gate) | low | accept | If both surfaces boot simultaneously BEFORE either writes np_workspace, each generates a fresh workspaceId and the scope gate prevents adoption of the other's snapshot — M.3 single-writer-election edge; surfaces converge from the next LWW write once a common id exists (side panel writes first in practice); flagged in flagged_assumptions |
| T-1-11-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | This plan installs NO packages (package.json untouched, verified); the phase-level T-1-SC guard from 01-01 (approved-stack + slopcheck) remains in force for future phases |
</threat_model>

<verification>
- `pnpm verify:phase-1` green (full §24 chain) — re-run with the hardening in place
- WR-01 regression guard: no ambient hook declaration and no runtime type-check guard wrappers remain in src/core/events, src/core/messaging, src/core/runtime (grep == 0)
- WR-04: sanitizeStored exported once + consumed by WorkspaceSync; workspaceId scope gate present; tests cover adopted / foreign-id ignored / malformed ignored
- WR-09: isRuntimeEnvelopeShape exported once; BackgroundRouter uses it before .type; MSG_DESERIALIZE fail-envelope reply present
- suites green: tests/core/events, tests/core/messaging, tests/core/runtime, tests/core/workspace, tests/entrypoints
- 01-VALIDATION.md untouched (W-13); new task verification recorded in 01-11-SUMMARY.md only
</verification>

<success_criteria>
- Golden Rule 9 restored in the messaging layer: every catch in EventBus/MessageBus/BroadcastBus/ProviderRegistry reaches debugLog with a canonical §C.2 code
- The now-live WorkspaceSync inbound path rejects malformed and foreign-workspace snapshots (T-1-13 + M.3 scope)
- BackgroundRouter survives malformed inbound messages without throwing and answers with a fail envelope
- Phase gate re-green with no regression to the 9 executed plans or to 01-10
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-11-SUMMARY.md` when done
</output>
