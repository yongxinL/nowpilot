---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - wxt.config.ts
  - vitest.config.ts
  - tsconfig.json
  - .prettierrc
  - tests/setup.ts
  - tests/isolation/check-content-bundle.mjs
  - tests/isolation/no-content-script-ui.test.ts
  - tests/e2e/load-smoke.mjs
  - src/entrypoints/sidepanel/index.html
  - src/entrypoints/sidepanel/main.tsx
  - src/entrypoints/standalone/index.html
  - src/entrypoints/standalone/main.tsx
autonomous: true
requirements: [RUNTIME-01]
must_haves:
  truths:
    - "D-01 honored: npm package-lock.json removed; pnpm is the only package manager (pnpm-lock.yaml is the lockfile)."
    - "Stack pinned to approved lines (RESEARCH Standard Stack): wxt ^0.19.29, @wxt-dev/module-react ^1.2.2, antd ^6.5.3, @ant-design/x ^2.9.0, @ant-design/icons ^6.3.2, @ant-design/x-markdown ^2.9.0, motion ^12.43.0, zustand ^5.0.14, immer ^10.2.0, zod ^3.25.76, typescript ^5.9.3, vitest ^4.1.10 — NOT wxt 0.21 / immer 11 / zod 4 / typescript 7 (Pitfall 2 + stack-drift flags)."
    - "wxt.config.ts matches Appendix G verbatim (manualChunks antd / antd-x / antd-x-markdown / ant-icons / defuddle / yaml / react; target chrome120; side_panel default_path sidepanel.html)."
    - "Entrypoint stems are `sidepanel/` and `standalone/` (NOT `app/`) — spec reconciliation #1 (Pitfall 3)."
    - "verify:phase-1 script exists and runs eslint + prettier + tsc --noEmit + wxt build + `vitest run` (the FULL suite — tests/core, tests/components, tests/entrypoints, tests/isolation) + node tests/isolation/check-content-bundle.mjs (D-02/D-04, RESEARCH line 570; B2 — the gate runs every Phase-1 test, not a four-dir subset)."
    - "Test toolchain wired per Pattern 4: vitest.config.ts uses WxtVitest() from wxt/testing/vitest-plugin; tests/setup.ts polyfills window.matchMedia and resets fakeBrowser per test; NO @wxt-dev/testing package (does not exist — RESEARCH Pitfall 3 guard)."
    - "API coverage decision (recorded): Phase 1 integrates NO external third-party API/SDK — wxt/testing/vitest-plugin + fakeBrowser are internal tooling mocking the chrome.* platform (extension platform, not an external service). No COVERAGE.md created."
    - "Package legitimacy: RESEARCH audit table is well-formed, all packages Approved (msw SLOP verdict resolved as false positive) — no blocking human checkpoint required."
  artifacts:
    - package.json (pinned deps + verify scripts)
    - wxt.config.ts
    - vitest.config.ts
    - tests/setup.ts
    - tests/isolation/check-content-bundle.mjs
    - tests/isolation/no-content-script-ui.test.ts
    - tests/e2e/load-smoke.mjs
    - pnpm-lock.yaml
  key_links:
    - "src/entrypoints/sidepanel/main.tsx and standalone/main.tsx stubs exist so `wxt build` produces sidepanel.html + standalone.html from wave 1 (plan 09 replaces stubs with real shells)."
    - "package.json verify:phase-1 depends on the wxt build + isolation script chain — the phase gate cannot pass before plan 07 creates the content entrypoint."
    - "verify:e2e-phase-1 (I1, Option A) is a SEPARATE browser-load gate (Chrome for Testing + puppeteer-core) from the unit gate — it proves sidepanel.html + standalone.html actually mount in a real MV3 browser; it is not part of verify:phase-1."
  flagged_assumptions:
    - statement: "RUNTIME-01 (4 entrypoints build) — verified via `wxt build` + entrypoint-stem assertions; real-browser load-unpacked verification is deferred because no Chrome binary is installed (RESEARCH A8)."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Bootstrap the greenfield WXT MV3 extension: migrate to pnpm (D-01), scaffold via `pnpm dlx wxt@0.19.29 init` (D-05), pin the approved stack (Pitfall 2), lay down the Appendix G build config verbatim with `sidepanel/` + `standalone/` entrypoint stubs (Pitfall 3), wire the vitest toolchain (D-03, Pattern 4), create the isolation check script, and define the `verify:phase-1` gate (D-02/D-04).

Purpose: Every later plan depends on a correct, version-pinned build + test foundation. Deviating from Appendix G or the pinned versions would cascade into every subsequent wave.
Output: package.json with pinned deps and verify scripts, wxt.config.ts (Appendix G verbatim), vitest.config.ts, tests/setup.ts, tests/isolation/check-content-bundle.mjs + tests/isolation/no-content-script-ui.test.ts (vitest wrapper, I4), tests/e2e/load-smoke.mjs (verify:e2e-phase-1, I1 Option A), sidepanel/standalone entrypoint stubs, pnpm-lock.yaml.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md
@.planning/PRODUCT_SPEC_v0_1.md (Appendix G line ~5302, §24 line ~3561)

# Note: greenfield repo — no src/ or package.json yet. No prior SUMMARYs exist.
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | chrome.sidePanel.open user-gesture / await-drops-gesture (crbug 1478648) | Handled in 01-06 (WorkspaceRouter.focusSidePanel callback-style) — no sidePanel code here |
| P2 | wxt `latest` is 0.21.x but approved stack pins ^0.19 | **GUARDED HERE** — scaffold with explicit `wxt@0.19.29`, pin `"wxt": "^0.19.29"`, never `pnpm update wxt` past 0.19 |
| P3 | `@wxt-dev/testing` does not exist | **GUARDED HERE** — use `wxt/testing/vitest-plugin` + `wxt/testing/fake-browser` only |
| P4 | Content script imports shared modules blow up the bundle | **GUARDED HERE** — Appendix G manualChunks verbatim; isolation grep script created here, enforced in 01-07 |
| P5 | Message contracts bypassing RuntimeEnvelope | Handled in 01-02/01-03 (canonical envelope + whitelist) |
| P6 | Theme breakage from double-provider / matchMedia-in-jsdom | **GUARDED HERE** — matchMedia polyfill in tests/setup.ts (Pitfall 6/A5) |
| P7 | zustand persist middleware for cross-surface state | Handled in 01-05/01-06 (storage adapter + onChanged) |

<tasks>

<task type="auto">
  <name>Task 1: pnpm migration + WXT scaffold + pinned dependency install</name>
  <files>
    package.json, pnpm-lock.yaml, package-lock.json (deleted)
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Standard Stack table lines 96-149, Installation lines 139-145, Package Legitimacy Audit lines 151-180, Pitfall 2 lines 365-369)
    .planning/PRODUCT_SPEC_v0_1.md §7 (approved stack)
  </read_first>
  <action>
    Per D-01: delete the root npm package-lock.json. Per D-05: run `pnpm dlx wxt@0.19.29 init . --template react --pm pnpm` (consult `wxt init --help` for the non-interactive flags if prompts appear; the react template is required). Do NOT scaffold with wxt@latest (Pitfall 2 — latest is 0.21.x and changes the config surface). After the scaffold, pin/add dependencies explicitly so package.json matches the RESEARCH Standard Stack exact lines: wxt ^0.19.29, @wxt-dev/module-react ^1.2.2 (STACK.md's ^0.3 pin is stale — the package only ever published 1.x), react/react-dom ^19.2.8, antd ^6.5.3, @ant-design/x ^2.9.0, @ant-design/icons ^6.3.2, @ant-design/x-markdown ^2.9.0, motion ^12.43.0, zustand ^5.0.14, immer ^10.2.0 (NEVER 11.x), zod ^3.25.76 (NEVER 4.x — Appendix C schemas are zod-3 style), typescript ^5.9.3 (NEVER the 7.x npm tag), vitest ^4.1.10. Dev-deps per D-03 + RESEARCH: @testing-library/react ^16.3.2, @testing-library/dom ^10.4.1, @testing-library/jest-dom ^7.0.0, jsdom ^30.0.1, msw ^2.15.0, @types/chrome ^0.2.5 (eslint/prettier ship with the scaffold). I1 (Option A): also add the smoke-e2e dev deps @puppeteer/browsers + puppeteer-core (exact latest-compatible versions from the npm registry at install time) — used only by `verify:e2e-phase-1` (Task 4), NOT part of the unit gate. Run `pnpm install`. Confirm the scaffold's tsconfig provides the `@/*` → `src/*` path alias (used by every Appendix M/F import) and `strict: true`; add the alias if missing. Do NOT install any banned package (tailwindcss, @tailwindcss/vite, shadcn/ui, @radix-ui/react-*, class-variance-authority, clsx, tailwind-merge, react-markdown, remark-gfm, rehype-highlight, highlight.js, katex, @ant-design/x-sdk, @ant-design/x-card, framer-motion). Never run `pnpm update wxt` past 0.19. I3 (pnpm strict peers): after `pnpm install`, run `pnpm why wxt @wxt-dev/module-react` and confirm single resolved versions with no peer errors; if pnpm's strict peer resolution blocks the install (e.g. @wxt-dev/module-react peer `wxt >=0.19.16`, antd6/react19 combos), do NOT use `--legacy-peer-deps` (npm-only, no effect under pnpm) — prefer a pinned `pnpm.overrides` entry for the offending package, or as a last resort add `strict-peer-dependencies=false` to `.npmrc`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};const checks={wxt:'^0.19.29','@wxt-dev/module-react':'^1.2.2',immer:'^10.2.0',zod:'^3.25.76',typescript:'^5.9.3'};for(const [k,v] of Object.entries(checks)){if(d[k]!==v){console.error(k+' expected '+v+' got '+d[k]);process.exit(1)}};console.log('pins OK')" && test ! -f package-lock.json && test -f pnpm-lock.yaml && pnpm install --frozen-lockfile 2>&1 | tail -3</automated>
  </verify>
  <done>
    package-lock.json absent; pnpm-lock.yaml present; package.json pins match the RESEARCH Standard Stack exact version lines; `@/*` path alias resolves; zero banned packages in package.json; pnpm install exits 0.
  </done>
  <acceptance_criteria>
    - `node -e` pin assertion above exits 0 (exact ^0.19.29 / ^1.2.2 / ^10.2.0 / ^3.25.76 / ^5.9.3)
    - `test -f pnpm-lock.yaml && test ! -f package-lock.json` both true
    - `pnpm install --frozen-lockfile` exits 0
    - tsconfig.json contains a `paths` mapping for `@/*` → `./src/*` and `strict: true`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Appendix G wxt.config.ts + entrypoint stubs (sidepanel/ + standalone/)</name>
  <files>
    wxt.config.ts, src/entrypoints/sidepanel/index.html, src/entrypoints/sidepanel/main.tsx, src/entrypoints/standalone/index.html, src/entrypoints/standalone/main.tsx, .prettierrc (create if scaffold lacks one)
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix G (lines 5302-5358) — copy verbatim, no modernization
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 3 lines 371-374 — standalone stem; Pitfall 2 — 0.19 config surface)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md section A (wxt.config.ts analog, lines 79-115)
  </read_first>
  <action>
    Replace the scaffold's wxt.config.ts with the Appendix G content VERBATIM (lines 5304-5351): defineConfig with srcDir 'src', modules ['@wxt-dev/module-react'], manifest name 'NowPilot', permissions exactly ['sidePanel','storage','cookies','alarms','tabs','scripting','contextMenus','notifications'], optional_permissions ['webNavigation'], host_permissions the two service-now globs, optional_host_permissions ['*://*/*'], side_panel.default_path 'sidepanel.html', action.default_title 'Open NowPilot', the CSP extension_pages string with connect-src *, web_accessible_resources assets/*, and the vite build block with target 'chrome120', sourcemap 'inline', and the manualChunks function returning 'antd' / 'antd-x-markdown' / 'antd-x' / 'ant-icons' / 'defuddle' / 'yaml' / 'react' exactly as Appendix G. Do not add @tailwindcss/vite. Then create the two §18 entrypoint stems (Pitfall 3 — NEVER `src/entrypoints/app/`): src/entrypoints/sidepanel/index.html (standard WXT HTML with a root div and a module script pointing at ./main.tsx) and src/entrypoints/sidepanel/main.tsx as a MINIMAL stub (createRoot render of a plain placeholder div — NO shell imports yet; plan 09 replaces it with the XProvider composition). Same for src/entrypoints/standalone/{index.html, main.tsx} with a distinct placeholder text. Keep the scaffold's src/entrypoints/background.ts if the react template created one (it is a valid WXT background stub so `wxt build` emits background.js from wave 1; plan 09 replaces its body with the full BackgroundRouter + LifecycleManager per §5.1 — BLOCKER-3 revision). Delete the scaffold's popup entrypoint directory if the react template created one (the §18 Phase-1 entrypoint set is background/sidepanel/standalone/content; the action button opens the side panel via setPanelBehavior, not a popup). Create a minimal .prettierrc (semi: true, singleQuote: true, printWidth: 100) only if the scaffold did not ship one.
  </action>
  <verify>
    <automated>pnpm wxt build 2>&1 | tail -5 && ls .output/chrome-mv3/sidepanel.html .output/chrome-mv3/standalone.html</automated>
  </verify>
  <done>
    wxt.config.ts matches Appendix G verbatim (diff against the spec block: no additions, no renames); `pnpm wxt build` exits 0; .output/chrome-mv3 contains both sidepanel.html and standalone.html; no popup entrypoint remains; no app/ directory exists.
  </done>
  <acceptance_criteria>
    - `diff <(sed -n '5304,5351p' .planning/PRODUCT_SPEC_v0_1.md | grep -v '^ *//') wxt.config.ts` has no structural differences in the config body (permit whitespace/comment-only drift)
    - `pnpm wxt build` exits 0 and prints "Built in" without fatal manifest errors
    - `test -f .output/chrome-mv3/sidepanel.html && test -f .output/chrome-mv3/standalone.html`
    - `test ! -d src/entrypoints/popup && test ! -d src/entrypoints/app`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: vitest toolchain + isolation script + verify:phase-1 scripts</name>
  <files>
    vitest.config.ts, tests/setup.ts, tests/isolation/check-content-bundle.mjs, tests/isolation/no-content-script-ui.test.ts, package.json (scripts block)
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pattern 4 lines 319-333, Pitfall 6 lines 389-393, Validation Architecture lines 537-580, Open Question 1)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md section A (vitest.config.ts + tests/setup.ts analogs, lines 117-140)
    .planning/PRODUCT_SPEC_v0_1.md §24 (lines 3561-3594)
  </read_first>
  <action>
    Create vitest.config.ts per Pattern 4: defineConfig from 'vitest/config', plugins [WxtVitest()] imported from 'wxt/testing/vitest-plugin' (Pitfall 3 — NOT '@wxt-dev/testing', which does not exist on npm), test.environment 'jsdom', test.setupFiles ['./tests/setup.ts']. Create tests/setup.ts with two responsibilities: (a) polyfill window.matchMedia (jsdom lacks it — Pitfall 6/A5) with a mock implementing matches/onchange and a stable query result, and (b) import { fakeBrowser } from 'wxt/testing/fake-browser' with a vitest beforeEach that calls fakeBrowser.reset() so chrome.* mocks are clean per test. Create tests/isolation/check-content-bundle.mjs: a node script that walks .output/**/content-scripts/** for built content bundles (plus any .output chunk named like the content entry), reads each file, and exits non-zero if any forbidden token is found (the Appendix G isolation rule set — antd, React, react-dom, defuddle, yaml); if no content bundle exists it exits 0 (nothing to check yet; plan 07 makes it meaningful). I4: also create tests/isolation/no-content-script-ui.test.ts — the §24-named thin vitest wrapper so both filenames exist (a spec DONE-when grep may look for the spec name): it imports { execFileSync } from 'node:child_process' and { it, expect } from 'vitest' and asserts `execFileSync('node', ['tests/isolation/check-content-bundle.mjs'], { stdio: 'pipe' })` does not throw. Add package.json scripts per §24 + D-02/D-04 + RESEARCH line 570: "test": "vitest run", "typecheck": "tsc --noEmit", "lint": "eslint .", "format": "prettier --check .", "build": "wxt build", "dev": "wxt", "test:isolation": "vitest run tests/isolation", and "verify:phase-1": the chained gate — eslint . then prettier --check . then tsc --noEmit then wxt build then `vitest run` (the FULL vitest suite — no dir list, so tests/core/messaging, tests/core/error, tests/core/registry, tests/core/content, tests/components and tests/entrypoints are all executed, not just the four §24 dirs) then node tests/isolation/check-content-bundle.mjs. Also add "verify:all": "tsc --noEmit && vitest run && pnpm run lint" per §24. Do not write any grep pattern into the verify scripts (the DONE-when hygiene greps run as plan-level verification commands in 01-09, not inside package.json).
<!-- planner-discipline-allow: @wxt-dev/testing -->
<!-- planner-discipline-allow: WxtVitest -->
  </action>
  <verify>
    <automated>pnpm vitest run --passWithNoTests 2>&1 | tail -3 && node tests/isolation/check-content-bundle.mjs && node -e "const p=require('./package.json');['verify:phase-1','verify:all','test','typecheck','build'].forEach(s=>{if(!p.scripts[s]){console.error('missing script '+s);process.exit(1)}});console.log('scripts OK')"</automated>
  </verify>
  <done>
    vitest.config.ts uses WxtVitest() + jsdom + tests/setup.ts; tests/setup.ts contains the matchMedia polyfill and the fakeBrowser beforeEach reset; check-content-bundle.mjs runs and exits 0 with no content bundle present; no-content-script-ui.test.ts wraps it as a §24-named vitest test (I4); package.json defines verify:phase-1, verify:all, test, typecheck, lint, format, build, dev, test:isolation; `pnpm vitest run --passWithNoTests` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "WxtVitest" vitest.config.ts` ≥ 1
    - `grep -c "@wxt-dev/testing" package.json` == 0
    - `grep -c "matchMedia" tests/setup.ts` ≥ 1 and `grep -c "fakeBrowser" tests/setup.ts` ≥ 1
    - `node tests/isolation/check-content-bundle.mjs` exits 0
    - `grep -c "execFileSync" tests/isolation/no-content-script-ui.test.ts` == 1 (I4 wrapper present)
    - `node -e` script assertion exits 0 (all 6 scripts present)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Smoke e2e — Chrome for Testing load gate (I1, Option A)</name>
  <files>
    tests/e2e/load-smoke.mjs, package.json (verify:e2e-phase-1 script)
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Assumptions A8 — no system Chrome; Environment Availability line 530)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md (I1 note; Manual-Only Verifications)
  </read_first>
  <action>
    Create tests/e2e/load-smoke.mjs: a standalone node script (I1 Option A) that proves the extension actually LOADS — not just unit-green. It (a) fetches a PINNED Chrome-for-Testing build via @puppeteer/browsers `install` into a repo-local `.cache/chrome-for-testing` dir (or uses a pinned download URL mirror), (b) launches it headless via puppeteer-core, (c) loads the built unpacked extension from `.output/chrome-mv3` with the `--disable-extensions-except` + `--load-extension` flags, and (d) asserts TWO things: sidepanel.html mounts its root without console errors AND standalone.html mounts without console errors (RUNTIME-01 real-browser MV3 load; RUNTIME-02/03 surface mounts). Console `error`-level messages (excluding benign favicon/net errors) cause exit 1 with the messages listed; exit 0 only when both pages mount clean. Fail with a clear "run `pnpm wxt build` first" hint if `.output/chrome-mv3` is absent. Keep it node-built-ins + @puppeteer/browsers + puppeteer-core only. Add package.json script "verify:e2e-phase-1": "node tests/e2e/load-smoke.mjs" — a SEPARATE gate from verify:phase-1 (it needs network/disk to fetch the browser and must not block the unit gate in CI-less envs).
  </action>
  <verify>
    <automated>test -f tests/e2e/load-smoke.mjs && node -e "const p=require('./package.json');if(!p.scripts['verify:e2e-phase-1']){console.error('missing verify:e2e-phase-1');process.exit(1)};console.log('e2e gate wired')"</automated>
  </verify>
  <done>
    tests/e2e/load-smoke.mjs exists and is wired as verify:e2e-phase-1 (separate from verify:phase-1); it loads .output/chrome-mv3 in Chrome for Testing and asserts both surfaces mount console-error-free. (Full run requires `pnpm wxt build` + the pinned browser download — first execution happens when the executor runs it; until then the script exists and the gate is wired.)
  </done>
  <acceptance_criteria>
    - `test -f tests/e2e/load-smoke.mjs` true
    - `grep -c "sidepanel.html\|standalone.html" tests/e2e/load-smoke.mjs` == 2 (both surfaces asserted)
    - `node -e` script assertion exits 0 (verify:e2e-phase-1 present)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Supply chain: pinned deps installed at build time |
| build config → manifest | wxt.config.ts controls permissions, CSP, bundle splitting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01-SC | Tampering | npm/pip/cargo installs (pnpm install) | low | mitigate | RESEARCH Package Legitimacy Audit is well-formed and all packages are Approved (msw SLOP false-positive resolved with postinstall investigation); versions pinned to exact approved lines so a future registry drift cannot silently introduce a different package; no [ASSUMED]/[SUS] packages requiring a blocking human checkpoint |
| T-1-02 | Tampering | wxt.config.ts manifest | medium | mitigate | Appendix G config kept verbatim (diff-checked): minimal permission set, extension_pages CSP restricted to script-src 'self', manualChunks keeps UI libs out of the content bundle (enforced by check-content-bundle.mjs in verify:phase-1) |
| T-1-03 | Tampering | tests/setup.ts chrome mocks | low | accept | fakeBrowser is a test-only in-memory mock; reset per test prevents cross-test state leakage; no production code imports it |
</threat_model>

<verification>
- `pnpm wxt build` succeeds from wave 1 onward (entrypoint stubs exist)
- `pnpm vitest run --passWithNoTests` exits 0 (no tests yet)
- `node tests/isolation/check-content-bundle.mjs` exits 0 (no content bundle yet)
- Pin assertions: immer ^10 / zod ^3 / typescript ^5 / wxt ^0.19 all enforced
</verification>

<success_criteria>
- pnpm is the sole package manager (D-01); npm package-lock.json removed
- WXT 0.19.x scaffold overlaid with the §18 file set (D-05) — config is Appendix G verbatim
- vitest + WxtVitest + fakeBrowser toolchain green (D-03)
- verify:phase-1 + verify:all scripts defined (D-02/D-04)
- sidepanel.html + standalone.html build from wave 1 (RUNTIME-01 foundation)
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-01-SUMMARY.md` when done
</output>
