---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md
autonomous: true
requirements: [CORE-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`01-MIGRATION-INVENTORY.md` exists and is committed as the authoritative conversion map (D-04) — it does not live only inside a PLAN.md."
    - "Every relevant prototype file appears exactly once, with the full D-04 column set: current path; target canonical path; classification (KEEP/ADAPT/REPLACE/REMOVE); current responsibility; intended Phase 1 responsibility; reason; governing requirement IDs; governing architecture/design contract; dependencies and consumers; required tests; migration/compatibility steps; removal timing for temporary adapters; assigned Phase 1 plan; implementation status; verification status."
    - "Every ADAPT / REPLACE / REMOVE row names a Phase 1 plan id (`01-02` … `01-11`); every KEEP row's target path equals its current path."
    - "Every non-Phase-1 page carries exactly one D-16 disposition — `fixture-preview`, `deferred-shell`, or `remove` — and no page is ambiguous or unassigned."
    - "Temporary adapters each carry an owner and a removal condition; no compatibility module exists without one."
    - "`src/main.tsx`, `index.html`, `src/theme/tailwindEquivalents.ts`, `src/components/pages/OptionsPage.tsx`, `src/components/options/PromptIcon.tsx`, `src/theme/tailwindEquivalents.ts` are enumerated as rows (removal or disposition) — no prototype infrastructure is left unclassified."
    - "The eight research hand-off questions are each resolved in the inventory with a recorded decision and rationale: content-script entrypoint path; `standalone/` vs `app/`; `.dark`-class scope; `schemaVersion` vs `version` vs `updatedAt`; exact-version pinning; CSP content; permission set; `Cmd`-primary semantics on macOS."
    - "{ statement: 'The inventory's plan-assignment column is internally consistent with the `depends_on` graph in 01-02…01-11 (no row is assigned to a plan that could not legally touch it)', verification: backstop }"
  artifacts:
    - ".planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md"
  key_links:
    - "Inventory row → Phase-1 plan id: every ADAPT/REPLACE/REMOVE row must name the plan that performs it, and PLANs reference rows rather than duplicating the table."
    - "Inventory D-16 disposition → `data-np-backing` literal: a `fixture-preview` row maps to `data-np-backing=\"fixture\"`, a `deferred-shell` row to `data-np-backing=\"deferred\"`, a `remove` row to neither."
    - "Inventory classification → target canonical path: every KEEP/ADAPT/REPLACE row must name a path under `src/**` (WXT `srcDir: 'src'`), never `entrypoints/**` and never `.wxt/**` or `.output/**`."
  prohibitions:
    - "The inventory must not cite `.wxt/**`, `.output/**` or `dist` as a pattern origin, analog or target path — they are generated mirrors, not tracked source."
    - "The inventory must not leave a prototype module with two live implementations (D-02 forbids parallel prototype and production implementations); every duplicate path resolves to exactly one disposition."
    - "The inventory must not record a target path for a module whose classification was changed during implementation without recording previous classification, replacement classification, reason, affected requirement/contract, affected plan and verification impact."
---

<objective>
Produce and freeze `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md` — the D-04 file-level conversion map — before any executable implementation plan runs.

Purpose: Phase 1 is an in-place prototype→canonical migration (D-01), not a greenfield build. Without a frozen inventory, "convert in place" has no acceptance surface: no one can prove that every prototype file was classified, that no module kept two live implementations, or that every removal was assigned to a plan. This plan is the gate the rest of the phase is measured against.
Output: One committed markdown artifact containing the full D-04 column set, the D-16 per-page disposition table, the eight resolved research hand-off decisions, and the row→plan assignment map that plans 01-02…01-11 reference.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md
@.planning/product/PRODUCT_SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and classify every relevant prototype file with the full D-04 column set</name>
  <files>.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md</files>
  <read_first>
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-01 classification rule, D-02 canonical structure, D-04 column set and minimum coverage, D-16 per-page disposition)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (§ File Classification A–H — the provisional target paths and closest analogs)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (§ Summary, § Recommended Project Structure, § Runtime State Inventory, § Deprecated/outdated list)
    - .planning/product/PRODUCT_SPEC.md §5.1 (entrypoints), §5.4 (page navigation), §8.5 (file structure), §18 Phase 1 (Create list)
    - .planning/codebase/STRUCTURE.md (current layout and test suites)
    - .planning/codebase/CONCERNS.md (the duplication and drift the inventory must resolve)
  </read_first>
  <action>
Create `01-MIGRATION-INVENTORY.md` with a header block stating: artifact purpose, D-04 authority, the freeze date, and the explicit statement that PLANs reference rows rather than duplicating the table.

Enumerate the source set with `git ls-files` (never `find` over the working tree, so generated and ignored trees cannot leak in). The audit universe is exactly: `src/**`, `entrypoints/**`, `tests/**`, `scripts/**`, `wxt.config.ts`, `vite.config.ts`, `vitest.config.ts`, `tsconfig.json`, `package.json`, `.gitignore`, `index.html`, `metadata.json`, `pnpm-workspace.yaml`, `public/**`. Generated trees `.wxt/**`, `.output/**` and the `dist` symlink are explicitly out of the row set and must be named as excluded with the reason "generated mirror".

Emit one markdown table row per file with these columns, in this order and with these exact header names: `Current path` · `Target canonical path` · `Classification` · `Current responsibility` · `Intended Phase 1 responsibility` · `Reason` · `Governing requirement IDs` · `Governing architecture/design contract` · `Dependencies and consumers` · `Required tests` · `Migration/compatibility steps` · `Removal timing` · `Assigned Phase 1 plan` · `Implementation status` · `Verification status`.

Apply the D-01 classification rule when assigning `KEEP` / `ADAPT` / `REPLACE` / `REMOVE`: KEEP only when the module already satisfies canonical ownership, responsibility, API, validation, lifecycle, naming and security contracts; ADAPT when concept and most behaviour are correct but path, types, API, validation or responsibility boundary differs; REPLACE when the implementation conflicts with an authoritative contract (workspace-writer election, background responsibilities, runtime-envelope validation, storage ownership, Chrome MV3 lifecycle, provider/MCP execution boundaries, content-script isolation); REMOVE only when duplicated, obsolete, unused, or an incompatible ordinary web-app / generated-server assumption. Canonical specification and accepted ADRs override prototype names and behaviour.

Minimum coverage the row set must contain, each as its own row unless the files are inseparable parts of one module (then list every included path explicitly in the row): root dev-shell files (`index.html`, `src/main.tsx`, `vite.config.ts`); `wxt.config.ts` and `vitest.config.ts`; `package.json` scripts + dependencies; `tsconfig.json`; `.gitignore`; all four current entrypoints (`entrypoints/background.ts`, `entrypoints/sidepanel/*`, `entrypoints/standalone/*`, `entrypoints/content/core.content.ts`) and `entrypoints/options/**`; both shells (`src/components/standalone/StandaloneShell.tsx`, the not-yet-existing Side Panel shell is recorded as a NEW row whose current path is `—`); Notes and Options pages including every duplicate path under `src/components/pages/**`; `src/core/theme/**` (ThemeStore, ThemeSync, ThemeConfig, chromeStorageAdapter) and `src/theme/**`; `src/core/workspace/**` (WorkspaceStore, WorkspaceRouter, WorkspaceSync); `src/core/messaging/**` (MessageBus, BackgroundRouter); `src/core/runtime/**` (RuntimeEnvelope, BroadcastBus, OperationId, PortReader, workerState); `src/core/events/EventBus.ts`; `src/core/commands/**`; `src/core/input/KeymapRegistry.ts`; `src/core/registry/**`; `src/core/log/debugLog.ts`; `src/core/i18n/strings.ts`; `src/core/prompts/index.ts`; `src/core/components/**`; storage adapters; provider prototypes (`src/services/aiProvider.ts`, `src/components/OnboardingModal.tsx`, `src/store/useExtensionStore.ts`, `src/types/index.ts`); fixtures and mock services; every existing test under `tests/**`; `scripts/verify-no-tailwind.sh`; generated-server/deployment files; obsolete aliases/compatibility modules (`src/theme/tailwindEquivalents.ts`).

For each row set `Implementation status` and `Verification status` to `pending`. Set `Assigned Phase 1 plan` in Task 2 — leave the cells empty in this task only so the two steps stay separately verifiable; do not write `TBD`.

Do not invent classifications to reduce the diff: `src/components/pages/OptionsPage.tsx` and `src/components/options/OptionsPage.tsx` cannot both be KEEP (D-02 forbids parallel implementations), and `src/core/workspace/WorkspaceStore.ts` cannot be KEEP while it persists `np_workspace_store` (D-14).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md &amp;&amp; grep -c '^|' .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md</automated>
    <fails_when>the `test -f` exits non-zero (file absent) or the printed row count is below 60</fails_when>
    <automated>for c in 'Target canonical path' 'Classification' 'Current responsibility' 'Intended Phase 1 responsibility' 'Reason' 'Governing requirement IDs' 'Governing architecture/design contract' 'Dependencies and consumers' 'Required tests' 'Migration/compatibility steps' 'Removal timing' 'Assigned Phase 1 plan' 'Implementation status' 'Verification status'; do grep -q "$c" .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md || printf 'MISSING:%s\n' "$c"; done; printf 'DONE\n'</automated>
    <fails_when>any line beginning `MISSING:` is printed, or the final `DONE` line is absent</fails_when>
    <automated>git ls-files --error-unmatch .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md || echo UNTRACKED</automated>
    <fails_when>the output contains `UNTRACKED` (the artifact must be git-tracked per D-04 — plan 01-11 commits it)</fails_when>
  </verify>
  <acceptance_criteria>
    - `01-MIGRATION-INVENTORY.md` exists at the exact D-04 path and contains a markdown table whose header row contains all fifteen column names verbatim.
    - The table contains at least one row for every file returned by `git ls-files src entrypoints tests scripts` plus the root config files listed in the action.
    - No row's `Current path` or `Target canonical path` names a path under `.wxt/`, `.output/` or `dist`.
    - Every row's `Classification` cell is exactly one of `KEEP`, `ADAPT`, `REPLACE`, `REMOVE` — no other token, no empty cell.
    - Every row's `Assigned Phase 1 plan` cell is empty in this task (filled in Task 2) — it never reads `TBD`, `TBC`, `?` or `none`.
    - The following paths each appear as a `Current path`: `entrypoints/background.ts`, `entrypoints/content/core.content.ts`, `entrypoints/sidepanel/main.tsx`, `entrypoints/standalone/main.tsx`, `entrypoints/options/main.tsx`, `src/main.tsx`, `index.html`, `vite.config.ts`, `src/core/workspace/WorkspaceStore.ts`, `src/core/workspace/WorkspaceSync.ts`, `src/core/workspace/WorkspaceRouter.ts`, `src/core/theme/ThemeStore.ts`, `src/core/theme/ThemeSync.ts`, `src/core/input/KeymapRegistry.ts`, `src/core/runtime/RuntimeEnvelope.ts`, `src/core/messaging/MessageBus.ts`, `src/core/messaging/BackgroundRouter.ts`, `src/core/events/EventBus.ts`, `src/core/commands/CommandRegistry.ts`, `src/core/commands/registerWorkspaceCommands.ts`, `src/core/log/debugLog.ts`, `src/core/registry/Registry.ts`, `src/core/theme/chromeStorageAdapter.ts`, `src/services/aiProvider.ts`, `src/components/OnboardingModal.tsx`, `src/store/useExtensionStore.ts`, `src/types/index.ts`, `src/components/options/PromptIcon.tsx`, `src/theme/tailwindEquivalents.ts`, `scripts/verify-no-tailwind.sh`.
    - The artifact names `.wxt/`, `.output/` and `dist` in an explicit exclusion statement with the reason "generated mirror".
  </acceptance_criteria>
  <done>The inventory exists with the full fifteen-column table, covers every tracked prototype file, and contains no unclassified or non-canonical path.</done>
</task>

<task type="auto">
  <name>Task 2: Assign every row to a plan, assign D-16 dispositions, and resolve the eight hand-off decisions</name>
  <files>.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md</files>
  <read_first>
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md (the table written in Task 1 — the file being modified)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-03 step order, D-16 disposition rule, D-05/D-07/D-11/D-12/D-13 ownership boundaries)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (§ Planner Handoff Checklist — the eight items; § Assumptions Log A1–A11; § Open Questions 1–7)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md § Fixture-Backed & Deferred Marking Convention → "Non-Phase-1 page disposition (D-16)"
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md (Wave 0 gap list — each new suite belongs to a plan)
  </read_first>
  <action>
Fill the `Assigned Phase 1 plan` cell of every `ADAPT` / `REPLACE` / `REMOVE` row with exactly one id from `01-02` … `01-11`. The plan ids and their scope are fixed: `01-02` WXT `srcDir` move + per-surface provider roots + shell render + gate re-pointing + `@wxt-dev/module-react`; `01-03` generated-manifest inspection test + content-script discovery fix; `01-04` canonical Phase-1 string map in `src/core/i18n/strings.ts`; `01-05` theme single source of truth + `getAntdConfig`; `01-06` runtime envelope literals + sender guard + messaging; `01-07` `WorkspaceState` + handoff protocol + `WorkspaceStore`/`WorkspaceRouter`; `01-08` palette + `KeymapRegistry` + command set; `01-09` onboarding shell + typed ports + legacy credential strip; `01-10` page dispositions + `DeferredNotice`; `01-11` dev-shell removal + `verify:phase-1` realignment + phase acceptance. KEEP rows get `—` in that column and must have `Target canonical path` identical to `Current path`.

Add a second table titled `## Non-Phase-1 page disposition (D-16)` with columns `Page` · `Current path` · `Disposition` · `Owning roadmap phase` · `Rationale`. Assign every non-Phase-1 page exactly one of `fixture-preview` | `deferred-shell` | `remove`. Apply the rule literally: `fixture-preview` only where an approved reusable presentation already exists (deterministic local fixtures, page root carries `data-np-backing="fixture"`, `DeferredNotice` shown, later-phase actions disabled); `deferred-shell` where only the route or workspace shell is required (canonical shell + navigation, intentional deferred-state panel naming the owning roadmap phase, page root carries `data-np-backing="deferred"`, no fake controls, no skeleton); `remove` where obsolete, duplicated, conflicting with the canonical information architecture, or generated server/web-app infrastructure. Every `deferred-shell` row must name a real roadmap phase (3, 8, 11, 15, 17 …). No row may be left ambiguous and no page may be assigned a skeleton.

Add a third section titled `## Resolved hand-off decisions` recording each of the eight items with a `Decision` and a one-line `Rationale`, and — where the decision is operator-facing — an `Owner: planner` or `Owner: operator (pending)` marker so the verifier can see which ones are proposals versus locked:
1. content-script entrypoint resolution (recommend `src/entrypoints/content/index.ts`, which matches WXT's `content/index.[jt]s?(x)` glob while preserving §5.1's directory intent);
2. `standalone/` not `app/` (PRODUCT_SPEC §5.1/§18 override §8.5);
3. `.dark`-class scope (AntD must never depend on the class; record whether `src/index.css` keeps it);
4. `WorkspaceState` versioning (`schemaVersion` for shape, `version` for the Phase-2 monotonic write counter, `updatedAt` for staleness);
5. exact-version pinning for `antd` / `@ant-design/x` / `@ant-design/x-markdown` / `@ant-design/icons`;
6. CSP content for Phase 1 (no Phase 1 code performs a network request);
7. permission set kept at `['sidePanel','storage','tabs']` rather than Appendix G's eight, per the least-privilege rule;
8. `Cmd` = platform-primary modifier, so `Cmd+K` on macOS and `Ctrl+K` on Windows/Linux are the same binding.

Add a fourth section titled `## Temporary adapters` listing every ADAPT row that leaves a compatibility export, with columns `Adapter` · `Owner (plan id)` · `Removal condition`. If there are none, write the section with the single sentence `No temporary adapters — every ADAPT row lands its canonical API directly.` Do not leave the section out.
  </action>
  <verify>
    <automated>grep -Eo '\| 01-(0[2-9]|1[01]) ' .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md | sort -u | wc -l</automated>
    <fails_when>the printed count is below 10 (fewer than ten of the eleven plan ids `01-02`…`01-11` are referenced)</fails_when>
    <automated>grep -cE '\| *(TBD|TBC|none|\?) *\|' .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md</automated>
    <fails_when>the printed count is greater than 0 (an unresolved placeholder or `none` token is present in a table cell)</fails_when>
    <automated>for c in 'Non-Phase-1 page disposition' 'fixture-preview' 'deferred-shell' 'Resolved hand-off decisions' 'Temporary adapters' 'data-np-backing'; do grep -q "$c" .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md || printf 'MISSING:%s\n' "$c"; done; printf 'DONE\n'</automated>
    <fails_when>any line beginning `MISSING:` is printed, or the final `DONE` line is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - Every row whose `Classification` is `ADAPT`, `REPLACE` or `REMOVE` has a non-empty `Assigned Phase 1 plan` cell matching `01-0[2-9]` or `01-1[01]`, and all eleven ids are used at least once.
    - Every row whose `Classification` is `KEEP` has `Target canonical path` byte-identical to `Current path`.
    - Every row whose `Classification` is `REMOVE` has a `Removal timing` cell naming the plan in its `Assigned Phase 1 plan` cell.
    - The D-16 table lists every page under `src/components/pages/**` plus `src/components/options/OptionsPage.tsx`, `src/components/notes/NotesWorkspace.tsx`, `src/components/standalone/StandaloneWritePage.tsx`, `src/components/standalone/TeamsPanel.tsx`, `src/components/standalone/ToolsGridPanel.tsx` and `src/components/history/ChatHistoryModal.tsx`, each with exactly one disposition token.
    - The D-16 table contains no cell reading `skeleton`, `loading`, `placeholder` or `spinner` as a disposition.
    - Every `deferred-shell` row's `Owning roadmap phase` cell contains a number from the §18 phase list (3, 8, 11, 15 or 17).
    - The `## Resolved hand-off decisions` section contains all eight numbered items, each with a non-empty `Decision` and `Rationale`.
    - The `## Temporary adapters` section exists.
  </acceptance_criteria>
  <done>Every ADAPT/REPLACE/REMOVE row is assigned to a plan, every non-Phase-1 page has exactly one D-16 disposition, all eight hand-off decisions are recorded, and no placeholder token remains anywhere in the artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze-validate the inventory against the plan set and commit it as the D-04 gate</name>
  <files>.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md</files>
  <read_first>
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md (the file being frozen)
    - .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-04 verifier checklist: every relevant file classified; every ADAPT/REPLACE/REMOVE assigned; target paths exist for completed KEEP/ADAPT/REPLACE; REMOVE entries no longer participate in production builds; temporary adapters have an owner and removal condition; no unclassified prototype infrastructure remains)
    - .planning/codebase/STRUCTURE.md (cross-check the current tree against the row set)
  </read_first>
  <action>
Run the D-04 verifier checklist against the artifact and record the outcome in a top-of-file `## Freeze status` block containing: `Frozen: yes`, `Freeze date: <ISO date>`, `Rows: <n>`, `KEEP: <n>`, `ADAPT: <n>`, `REPLACE: <n>`, `REMOVE: <n>`, and `Pages disposed: <n>`.

Perform and record four checks:
1. Coverage: re-run `git ls-files src entrypoints tests scripts` and confirm every path is present as a `Current path` or is explicitly excluded with a reason in an `## Explicitly excluded` list (the only permitted exclusions are generated trees `.wxt/**`, `.output/**`, `dist`, and the `node_modules` tree).
2. Assignment: confirm no `ADAPT`/`REPLACE`/`REMOVE` row has an empty `Assigned Phase 1 plan` cell.
3. Duplication: confirm no two `KEEP`/`ADAPT` rows share the same `Intended Phase 1 responsibility` in a way that leaves two live implementations of one capability; where a duplicate exists, one of the two rows must be `REMOVE`.
4. Target-path discipline: confirm every non-`REMOVE` row's `Target canonical path` is either an existing tracked path under `src/**` (verify with `git ls-files -- <path>`) or a new file path under `src/**`; no target path is under `entrypoints/**`.

Append a `## Change control` section stating D-04's rule verbatim in substance: classifications are frozen; the executor must not invent or silently change them; any change during implementation must be recorded with previous classification, replacement classification, reason, affected requirement/contract, affected plan and verification impact; material architecture changes require operator review.

Commit the artifact on its own so the gate is independently revertible:
`git add .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md` then commit with message `docs(01-01): freeze Phase 1 migration inventory (D-04)`.

State at the end of the summary that plans `01-02` … `01-11` are now unblocked and that each of them references inventory rows by `Current path` rather than duplicating the table.
  </action>
  <verify>
    <automated>git log --oneline -1 -- .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md</automated>
    <fails_when>the command prints nothing (the artifact has no commit of its own)</fails_when>
    <automated>for c in 'Freeze status' 'Frozen: yes' 'Explicitly excluded' 'Change control'; do grep -q "$c" .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md || printf 'MISSING:%s\n' "$c"; done; printf 'DONE\n'</automated>
    <fails_when>any line beginning `MISSING:` is printed, or the final `DONE` line is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - The artifact carries a `## Freeze status` block whose `Frozen:` value is `yes` and whose five counts are integers that sum consistently (`KEEP + ADAPT + REPLACE + REMOVE` equals `Rows`).
    - The `## Explicitly excluded` section names `.wxt/**`, `.output/**` and `dist`.
    - The `## Change control` section states the D-04 rule that classifications are frozen and that any post-freeze change records previous classification, new classification, reason, affected requirement/contract, affected plan and verification impact.
    - `git log --oneline -1 -- <artifact>` returns exactly one commit whose message contains `01-01` and `migration inventory`.
    - No `Target canonical path` cell matches the pattern `^entrypoints/`.
  </acceptance_criteria>
  <done>The inventory is frozen, self-consistent, committed under its own message, and every downstream plan can cite its rows by path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prototype source → canonical inventory | The inventory is the only artifact that decides which prototype modules survive into production; an omission here silently ships prototype assumptions into every later phase. |
| inventory → Phase 1 plans | Plans cite inventory rows by path; a stale or ambiguous row propagates a wrong target path into executable work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Repudiation | `01-MIGRATION-INVENTORY.md` row set | medium | mitigate | Enumerate the audit universe with `git ls-files` (never a working-tree scan) and record an explicit `## Explicitly excluded` list, so an omitted file is a visible gap rather than a silent one. |
| T-1-02 | Tampering | Inventory classification cells | medium | mitigate | Task 3 freezes the artifact under its own commit and records the D-04 change-control rule; post-freeze edits must carry previous classification, new classification, reason and verification impact. |
| T-1-03 | Information disclosure | Provider-prototype rows | medium | mitigate | Every row reached by `src/services/aiProvider.ts`, `src/components/OnboardingModal.tsx`, `src/store/useExtensionStore.ts` and `src/types/index.ts` is classified `REPLACE` and assigned to `01-09`, which strips plaintext credential fields (D-07). |
| T-1-04 | Elevation of privilege | Manifest/permission rows | medium | mitigate | The `wxt.config.ts` row records that the Phase-1 permission set stays `['sidePanel','storage','tabs']` (least privilege) and that no permission is added silently; the assertion lives in `01-03`'s manifest test. |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs in this plan. `@wxt-dev/module-react@^1.2.2` is the single new install in the phase, carries a Package Legitimacy Audit verdict `OK` with no `postinstall` script, and is installed in `01-02`; `T-1-SC` stays registered in that plan with a blocking human checkpoint required before any version bump. |
</threat_model>

<verification>
- `test -f` on the artifact path returns 0.
- All three Task-verify presence loops print `DONE` with no `MISSING:` line.
- `git log --oneline -1 -- <artifact>` returns one commit.
- `grep -cE '\| *(TBD|TBC|none|\?) *\|' <artifact>` returns 0.
- The freeze-status counts are internally consistent.
</verification>

<success_criteria>
- `01-MIGRATION-INVENTORY.md` exists at the D-04 path, is git-tracked, and is committed independently.
- The D-04 fifteen-column table classifies every tracked prototype file exactly once.
- Every non-KEEP row names a Phase 1 plan; every KEEP row's target equals its current path.
- Every non-Phase-1 page has exactly one D-16 disposition with a named owning roadmap phase where `deferred-shell`.
- All eight research hand-off decisions are recorded with rationale.
- The `## Temporary adapters`, `## Explicitly excluded`, `## Freeze status` and `## Change control` sections exist.
</success_criteria>

## Artifacts this phase produces

| Kind | Symbol / path | Created by |
|------|---------------|------------|
| Artifact | `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-MIGRATION-INVENTORY.md` | Task 1 |
| Table | `D-04 conversion table` (columns: current path, target canonical path, classification, responsibilities, reason, governing requirement IDs, governing contract, dependencies and consumers, required tests, migration steps, removal timing, assigned plan, implementation status, verification status) | Task 1 |
| Table | `Non-Phase-1 page disposition (D-16)` | Task 2 |
| Section | `Resolved hand-off decisions` (8 items) | Task 2 |
| Section | `Temporary adapters` | Task 2 |
| Section | `Freeze status` | Task 3 |
| Section | `Explicitly excluded` | Task 3 |
| Section | `Change control` | Task 3 |
| Git commit | `docs(01-01): freeze Phase 1 migration inventory (D-04)` | Task 3 |

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-01-SUMMARY.md` when done
</output>
