---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 07
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-06]
files_modified:
  - src/core/registry/AddonRegistry.ts
  - src/core/registry/AddonSettingsStore.ts
  - src/core/registry/PageRegistry.ts
  - src/core/content/ContentScriptHost.ts
  - src/core/content/PageContextBridge.ts
  - src/content/index.ts
  - tests/core/registry/AddonRegistry.test.ts
  - tests/core/content/ContentScriptHost.test.ts
  - tests/isolation/check-content-bundle.mjs
autonomous: true
requirements: [RUNTIME-05, WSPC-04]
must_haves:
  truths:
    - "AddonRegistry is a WSPC-04 registry instance: register/unregister/get/list with idempotent re-registration and synchronous Map operations (concurrency-safe by construction)."
    - "PageRegistry tracks PageContext per tab (tabId → PageContext) fed by content scripts via PageContextBridge; registry ops are idempotent and concurrent-safe."
    - "Content scripts are extraction-only: no UI mount, no DOM mutation beyond extraction, ISOLATED world by default (D-16, R-5)."
    - "PageContextBridge (content → core) publishes RuntimeEnvelope messages (EXTRACT_PAGE_CONTENT / PING / GET_CONTENT_CAPABILITIES per D-17) through MessageBusBridge — never direct chrome API calls from content code."
    - "The content bundle stays isolated: after wxt build, no antd/React/react-dom/defuddle/yaml tokens appear in content-script output (Pitfall 4 enforcement — check-content-bundle.mjs becomes meaningful here)."
    - "ContentScriptHost is the only content-script entry; the wxt content script entry (src/content/index.ts) wires it with ISOLATED world + match patterns per RESEARCH/UI-SPEC (all URLs)."
  artifacts:
    - src/core/registry/AddonRegistry.ts
    - src/core/registry/AddonSettingsStore.ts
    - src/core/registry/PageRegistry.ts
    - src/core/content/ContentScriptHost.ts
    - src/core/content/PageContextBridge.ts
    - src/content/index.ts
    - tests/core/registry/AddonRegistry.test.ts
    - tests/core/content/ContentScriptHost.test.ts
    - tests/isolation/check-content-bundle.mjs (upgraded from 01-01)
  key_links:
    - "PageContextBridge imports PageContext/TabContext from 01-02 (src/core/content/PageContext.ts) — canonical homes (R-1)."
    - "ContentScriptHost uses PageRegistry to update page context per tab; 01-08/01-09 surfaces subscribe to workspace for currentPageContext."
  flagged_assumptions:
    - statement: "RUNTIME-05 content bridge — GET_CONTENT_CAPABILITIES handshake verified at unit level; real cross-context content extraction verified in browser e2e (no Chrome binary, RESEARCH A8)."
      verification: flagged-unverified
    - statement: "WSPC-04 registry — AddonRegistry registration is synchronous/idempotent by design; async addon load (Phase 8) will not change the registry contract."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Build the registry layer (AddonRegistry, AddonSettingsStore, PageRegistry — the WSPC-04 idempotent/concurrent-safe pattern) and the content-script skeleton: ContentScriptHost + PageContextBridge (D-16), the wxt content entry, and the Pitfall-4 isolation enforcement.

Purpose: Registries underpin the addon ecosystem (Phase 8) and page-context tracking; the content skeleton is the extraction-only bridge between pages and the workspace (D-16/R-5). Isolation is proven here for the first time.
Output: 6 source files + 2 test files + 1 isolation script upgrade.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-16, D-17)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 4 lines 373-381; content script ISOLATED world)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (core/registry, content, lines 147-175)
@.planning/PRODUCT_SPEC_v0_1.md (§8.1 line ~1190 — extension contexts)

# Depends on 01-02 (PageContext, MessageType D-17), 01-03 (MessageBusBridge), 01-06 (PageRegistry consumers + workspace). 
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | Handled in 01-06 |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | **GUARDED HERE** — check-content-bundle.mjs upgraded to walk built content bundles and fail on antd/React/react-dom/defuddle/yaml tokens; content skeleton imports zero UI libs |
| P5 | Message contracts bypassing RuntimeEnvelope | Content → core messages only via MessageBusBridge (whitelist) |
| P6 | matchMedia-in-jsdom | Handled in 01-01 |
| P7 | zustand persist for cross-surface | Handled in 01-05/01-06 |

<tasks>

<task type="auto">
  <name>Task 1: AddonRegistry + AddonSettingsStore + PageRegistry (WSPC-04 pattern)</name>
  <files>
    src/core/registry/AddonRegistry.ts, src/core/registry/AddonSettingsStore.ts, src/core/registry/PageRegistry.ts, tests/core/registry/AddonRegistry.test.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md §8.6 (addon registry contracts; lines ~1270-1310)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (core/registry lines 147-158; types/registry lines 375-385)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (WSPC-04: idempotent + concurrent-safe registration)
  </read_first>
  <action>
    Create src/core/registry/AddonRegistry.ts as the WSPC-04 registry instance: class AddonRegistry { private addons = new Map<string, AddonEntry>(); register(entry: AddonEntry): void — idempotent (same id re-register replaces atomically, debugLog ADDON_REGISTRY silent:true); unregister(id): void; get(id): AddonEntry | undefined; list(): AddonEntry[]; has(id): boolean }. Define AddonEntry in this file with the §8.6 base shape: { id: string; name: string; version: string; enabled: boolean; settings?: Record<string, unknown>; permissions?: string[] } (full addon manifest lands Phase 8 — note the stub in a comment). Register ops are synchronous Map ops — concurrent-safe by construction; never throws (debugLog ADDON_REGISTRY on invalid entries, skip). Create src/core/registry/AddonSettingsStore.ts: zustand store (create from 'zustand', v5) { settings: Record<string, Record<string, unknown>> } with setSetting(addonId, key, value) (immer produce; ADDON_SETTINGS on error) and getSetting(addonId, key, fallback) — persistable via the 01-06-style chrome.storage.local adapter pattern keyed np_addon_settings (no zustand persist — Pitfall 7), with init() subscribing chrome.storage.onChanged. Create src/core/registry/PageRegistry.ts: class PageRegistry { private pages = new Map<number, PageContext>(); upsert(tabId: number, page: PageContext): void — idempotent upsert, replaces atomically; get(tabId): PageContext | undefined; remove(tabId): void; list(): Array<{tabId: number; page: PageContext}>; clear(): void }. Imports PageContext from '@/core/content/PageContext' (01-02 — canonical home, R-1). Write tests/core/registry/AddonRegistry.test.ts: register/get roundtrip; re-register same id idempotent (replaces, no throw); unregister removes; list order; invalid entry (missing id) logs and is skipped without throwing. Also test AddonSettingsStore: set/get roundtrip; getSetting fallback; onChanged foreign write merges. Also test PageRegistry: upsert/get; upsert same tab replaces; remove; list.
<!-- planner-discipline-allow: persist -->
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/registry/AddonRegistry.test.ts -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    AddonRegistry (idempotent register), AddonSettingsStore (storage adapter, no persist), PageRegistry (tab → PageContext upsert) all built and tested green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/registry/AddonRegistry.test.ts -x` exits 0
    - `grep -c "ADDON_REGISTRY\|ADDON_SETTINGS" src/core/registry/AddonRegistry.ts src/core/registry/AddonSettingsStore.ts` >= 1
    - `grep -c "persist" src/core/registry/AddonSettingsStore.ts` == 0 (Pitfall 7 guard)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ContentScriptHost + PageContextBridge (D-16 content skeleton)</name>
  <files>
    src/core/content/ContentScriptHost.ts, src/core/content/PageContextBridge.ts, tests/core/content/ContentScriptHost.test.ts
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md D-16 (ContentScriptHost/PageContextBridge naming; extraction-only ISOLATED world)
    .planning/PRODUCT_SPEC_v0_1.md §8.1 (lines 1190-1230) — extension contexts
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (content lines 175-185)
  </read_first>
  <action>
    Create src/core/content/PageContextBridge.ts: class PageContextBridge wrapping MessageBusBridge (01-03) for content-side messaging: publishContext(page: PageContext): void (sends EXTRACT_PAGE_CONTENT envelope with the page payload); sendPing(): void (PING — D-17); getCapabilities(): Promise<ContentCapabilities> (sends GET_CONTENT_CAPABILITIES — D-17 — and resolves on the CONTENT_CAPABILITIES reply; timeout 3000ms → debugLog CONTENT_CAPABILITIES, resolve default { extraction: true, domAccess: 'isolated' }); onMessage(cb): unsubscribe. It is the ONLY message path content code uses — no direct chrome API calls outside this bridge. Import PageContext from './PageContext' (01-02). Create src/core/content/ContentScriptHost.ts: class ContentScriptHost with start(): void (installs the PageContextBridge listener, wires EXTRACT_PAGE_CONTENT handling to PageRegistry.upsert, publishes GET_CONTENT_CAPABILITIES handshake, and keeps a live PageContext of the current page via document.title/URL — NO DOM mutation, NO UI mount, extraction-only per R-5) and stop(): void. Do NOT import React/antd/zustand in either file (Pitfall 4 — these two files are the content-bundle root). Write tests/core/content/ContentScriptHost.test.ts with fakeBrowser: start() registers the bridge listener; an incoming EXTRACT_PAGE_CONTENT envelope upserts PageRegistry; PING replies PONG; GET_CONTENT_CAPABILITIES roundtrip resolves capabilities; stop() removes the listener.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/content/ContentScriptHost.test.ts -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    PageContextBridge is the single content message path (D-17 messages + timeout-safe capabilities); ContentScriptHost wires bridge → PageRegistry, extraction-only (no UI/DOM mutation); tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/content/ContentScriptHost.test.ts -x` exits 0
    - `grep -c "GET_CONTENT_CAPABILITIES\|CONTENT_CAPABILITIES" src/core/content/PageContextBridge.ts` >= 1
    - `grep -rn "from 'react'\|from 'antd'\|from 'zustand'" src/core/content/ | wc -l` == 0 (Pitfall 4 guard at source level)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: wxt content entry + isolation enforcement upgrade</name>
  <files>
    src/content/index.ts, tests/isolation/check-content-bundle.mjs
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 4 lines 373-381; content script ISOLATED world)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (content entry, lines 175-185)
  </read_first>
  <action>
    Create src/content/index.ts as the wxt content-script entry using defineContentScript from 'wxt/utils/define-content-script' (wxt ^0.19 API): { matches: ['<all_urls>'], runAt: 'document_idle', world: 'ISOLATED' (D-16 — default world, explicit for clarity), main: () => { const host = new ContentScriptHost(); host.start(); } }. Content scripts NEVER mount UI or mutate the page DOM (R-5 — extraction-only; clipboard-only write-back, none this phase). Upgrade tests/isolation/check-content-bundle.mjs (created in 01-01) from the 01-01 stub: walk .output/**/content-scripts/** and any .output chunk whose name matches the content entry; for EACH content bundle found, scan the file text and exit 1 listing any occurrence of the forbidden tokens (antd, React, react-dom, defuddle, yaml); exit 0 only when every found content bundle is clean OR no content bundle exists (still exit 0 with a note — meaningful enforcement starts once 01-01's build produces bundles). Keep the script dependency-free (node built-ins only).
  </action>
  <verify>
    <automated>pnpm wxt build 2>&1 | tail -3 && node tests/isolation/check-content-bundle.mjs</automated>
  </verify>
  <done>
    src/content/index.ts registers the ISOLATED-world extraction-only content script; check-content-bundle.mjs enforces the isolation rule set on built content bundles and exits 0 on clean output.
  </done>
  <acceptance_criteria>
    - `node tests/isolation/check-content-bundle.mjs` exits 0 after `pnpm wxt build`
    - `grep -c "defineContentScript\|world: 'ISOLATED'" src/content/index.ts` >= 1
    - `grep -c "antd\|React\|react-dom\|defuddle\|yaml" tests/isolation/check-content-bundle.mjs` >= 4 (forbidden tokens enforced)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web page → content script | Arbitrary page DOM/scripts; content script runs ISOLATED world, extraction-only |
| content script → bridge | Page-derived data flows into the extension; must be envelope-typed and capability-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-15 | Tampering (XSS via DOM) | ContentScriptHost | high | mitigate | ISOLATED world (D-16) + extraction-only (no DOM mutation, no UI mount, R-5); extracted text travels in RuntimeEnvelopes only |
| T-1-16 | Spoofing | PageContextBridge inbound | high | mitigate | All inbound messages whitelisted via MessageBusBridge (Pitfall 5); GET_CONTENT_CAPABILITIES reply validated against the ContentCapabilities shape |
| T-1-17 | Tampering (supply chain via bundle) | content bundle | high | mitigate | Pitfall 4 enforcement: check-content-bundle.mjs fails the build if UI libs leak into content bundles (blocking in verify:phase-1) |
</threat_model>

<verification>
- `pnpm vitest run tests/core/registry tests/core/content -x` green
- `pnpm wxt build && node tests/isolation/check-content-bundle.mjs` green
- `pnpm tsc --noEmit` green
</verification>

<success_criteria>
- WSPC-04 registry pattern proven: AddonRegistry/PageRegistry idempotent + concurrent-safe, tested
- Content skeleton extraction-only (D-16), ISOLATED world, single bridge message path
- Isolation enforcement meaningful: content bundles provably UI-lib-free
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-07-SUMMARY.md` when done
</output>
