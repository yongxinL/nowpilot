---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 09
type: execute
wave: 6
depends_on: [01-05, 01-06, 01-07, 01-08]
files_modified:
  - src/entrypoints/background.ts
  - src/core/background/BackgroundRouter.ts
  - src/core/background/LifecycleManager.ts
  - src/core/background/KeepAliveManager.ts
  - src/core/background/ContextMenuHost.ts
  - src/core/runtime/workerState.ts
  - src/entrypoints/sidepanel/main.tsx (W4 — REPLACES the 01-01 stub at this canonical path, does NOT regenerate)
  - src/entrypoints/standalone/main.tsx (W4 — REPLACES the 01-01 stub at this canonical path, does NOT regenerate)
  - tests/core/runtime/workerState.test.ts
  - tests/entrypoints/sidepanel.test.tsx
  - tests/entrypoints/standalone.test.tsx
autonomous: true
requirements: [RUNTIME-02, RUNTIME-05, WSPC-01, WSPC-02]
must_haves:
  truths:
    - "The side panel entrypoint (src/entrypoints/sidepanel/main.tsx — BLOCKER 2: WXT srcDir 'src' puts entrypoints under src/entrypoints/; W4: REPLACES the 01-01 stub body, does not regenerate) mounts SidePanelRouter inside ONE XProvider + ONE ConfigProvider per Appendix F (one provider per surface; theme from getAntdConfig 01-05) — no second provider anywhere."
    - "The standalone entrypoint (src/entrypoints/standalone/main.tsx — W4: REPLACES the 01-01 stub body) mounts StandaloneRouter the same way (Appendix F), rendered in the standalone extension tab (opened via update-or-create tab dedupe, 01-06 W-12 — no popup window dimensions apply)."
    - "Both entrypoints install the global mod+k key capture → CmdKPicker (01-08) and wrap in ErrorBoundary (01-04); neither entrypoint imports content-script modules (Pitfall 4)."
    - "The background SW (src/entrypoints/background.ts, §5.1 canonical shape) registers ALL listeners SYNCHRONOUSLY at module load (DONE-when, §18 line 2465): BackgroundRouter.register() (typed chrome.runtime.onMessage dispatcher with sender.id + MessageType whitelist validation, §16.2), LifecycleManager.register() (chrome.sidePanel.setPanelBehavior({openPanelOnActionClick:true}) on onInstalled + onStartup — §5.3 line 915, RESEARCH Pitfall 1 mitigation; catch → debugLog SIDEPANEL_BEHAVIOR), KeepAliveManager.register() (chrome.alarms keepalive), ContextMenuHost.recreateAll() (chrome.contextMenus, idempotent re-register per startup — RESEARCH A6). BLOCKER 3: replaces the 01-01 scaffold stub body."
    - "workerState.ts (src/core/runtime/workerState.ts, §18 line 2434 + §20.5) exports workerState.ok/workerState.fail ResponseEnvelope helpers; every background/content handler replies via them (Pitfall 5)."
    - "Mount smoke tests render each entrypoint root with fakeBrowser + store fixtures and assert the shell/onboarding tree mounts without throwing."
    - "The full Phase 1 gate is green: `pnpm verify:phase-1` passes end-to-end (eslint → prettier → tsc → wxt build → `vitest run` full suite → content-bundle isolation check)."
    - "The DONE-when hygiene greps (RuntimeEnvelope fixtures parse, MessageType = Appendix E + D-17, no banned imports, whitelist enforcement, Pitfall 1/4/7 guards) run as plan-level verification here and pass."
  artifacts:
    - src/entrypoints/background.ts (BLOCKER 3 — §5.1 composition)
    - src/core/background/BackgroundRouter.ts, LifecycleManager.ts, KeepAliveManager.ts, ContextMenuHost.ts
    - src/core/runtime/workerState.ts
    - src/entrypoints/sidepanel/main.tsx, src/entrypoints/standalone/main.tsx (W4 — both replace their 01-01 stubs)
    - tests/core/runtime/workerState.test.ts
    - tests/entrypoints/sidepanel.test.tsx, tests/entrypoints/standalone.test.tsx
  key_links:
    - "Entrypoints are the ONLY places that call createRoot + mount — everything upstream is tree-imported, nothing else touches ReactDOM."
    - "The ConfigProvider theme comes from getAntdConfig (01-05) fed by ThemeStore — a single theme source across both surfaces."
    - "BackgroundRouter dispatches inbound messages to MessageBus (01-03) and replies via workerState.ok/fail (Pitfall 5); LifecycleManager's setPanelBehavior is the action-button → side panel path (Pitfall 1)."
  flagged_assumptions:
    - statement: "RUNTIME-05 entrypoints — mount verified via jsdom smoke tests; real Chrome load of sidepanel.html/standalone.html (including sidePanel.open gesture flow) deferred to browser e2e (RESEARCH A8)."
      verification: flagged-unverified
    - statement: "WSPC-01 full-suite green — verify:phase-1 runs in the CI-less local environment; green here means the documented command chain exits 0."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Mount the two surfaces (Appendix F: one XProvider + one ConfigProvider per surface), build the background SW (§5.1 canonical composition + workerState, BLOCKER 3), install global key capture, and drive the entire phase to green: full vitest suite, wxt build, content isolation check, and the DONE-when hygiene greps.

Purpose: This is the last plan of Phase 1 — when it passes, the extension builds, all surfaces mount, the background registers listeners synchronously, and the phase's §DONE-when conditions hold. verify:phase-1 (01-01) becomes the phase-completion gate.
Output: 2 entrypoint mounts + 1 background entrypoint + 4 background managers + workerState + 3 test files.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-13, D-16)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Appendix F one-provider-per-surface, validation map)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (entrypoint requirements)
@.planning/PRODUCT_SPEC_v0_1.md (Appendix F line ~5231, §5.1 line 858, §5.3 line 915, Appendix E line 5050, §24 verify scripts)

# Depends on 01-05 (antdConfig), 01-06 (WorkspaceStore/Router), 01-07 (isolation script), 01-08 (routers/shells).
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | Entrypoints never call sidePanel.open — only WorkspaceRouter (01-06) does, gesture-safe |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | Entrypoints never import content modules; check-content-bundle.mjs runs in verify:phase-1 |
| P5 | Message contracts bypassing RuntimeEnvelope | All messaging via 01-03 bridge |
| P6 | matchMedia-in-jsdom | Handled in 01-01 |
| P7 | zustand persist for cross-surface | Stores own persistence; entrypoints are consumers |

<tasks>

<task type="auto">
  <name>Task 1: Side panel entrypoint mount (Appendix F)</name>
  <files>
    src/entrypoints/sidepanel/main.tsx, tests/entrypoints/sidepanel.test.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix F (lines 5231-5280) — one provider per surface
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Appendix F one-provider rule)
  </read_first>
  <action>
    Create src/entrypoints/sidepanel/main.tsx (BLOCKER 2 — WXT srcDir 'src' puts entrypoints under src/entrypoints/, NOT src/sidepanel/; W4 — REPLACES the 01-01 stub body, does NOT regenerate from scratch): the side panel entry root — createRoot from 'react-dom/client' on the sidepanel.html root element; render exactly ONE XProvider (from @ant-design/x) wrapping ONE antd ConfigProvider (theme = getAntdConfig({ mode, pack, compact: true }) from 01-05 fed by useThemeStore()) wrapping ErrorBoundary → SidePanelRouter (01-08); add a global keydown listener (isCmdK from 01-04) toggling CmdKPicker visibility (state lifted here, passed to SidePanelShell). ThemeStore.init() must be awaited/fired before first render (fire-and-forget with a readiness gate on isReady from the store, rendering null until ready to avoid a flash of default theme). No second provider anywhere (Appendix F). No import of any content-script module (Pitfall 4). Create tests/entrypoints/sidepanel.test.tsx: jsdom mount of the entrypoint root (import the main module, but guard DOM createRoot via a render helper that stubs document.getElementById for the entry root — or export a createSidePanelApp() from main.tsx for testability); assert the tree mounts without throwing; assert exactly one ConfigProvider renders (query by role/tree) and the onboarding gate decides the initial screen (onboarding pending → Onboarding text; done → shell header). Use fakeBrowser + store.setState fixtures per 01-08 test patterns.
  </action>
  <verify>
    <automated>pnpm vitest run tests/entrypoints/sidepanel.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>
    Side panel mounts one XProvider + one ConfigProvider with themed ConfigProvider; key capture wired; smoke test green (mounts, single provider, onboarding gate correct).
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/entrypoints/sidepanel.test.tsx` exits 0
    - `grep -c "createRoot\|XProvider" src/entrypoints/sidepanel/main.tsx` >= 2
    - `grep -c "ConfigProvider" src/entrypoints/sidepanel/main.tsx` == 1 (Appendix F: one per surface)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Standalone entrypoint mount (Appendix F)</name>
  <files>
    src/entrypoints/standalone/main.tsx, tests/entrypoints/standalone.test.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix F (lines 5231-5280)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Appendix F one-provider rule)
  </read_first>
  <action>
    Create src/entrypoints/standalone/main.tsx (BLOCKER 2 — replaces the 01-01 stub body at this canonical path, does NOT regenerate, W4) mirroring Task 1 for the standalone surface: createRoot on the standalone.html root; one XProvider + one ConfigProvider (same getAntdConfig({ mode, pack, compact: false }) from 01-05 — standalone uses the default density per RUNTIME-04); ErrorBoundary → StandaloneRouter (01-08); same global isCmdK → CmdKPicker wiring. The standalone opens as an extension TAB via the 01-06 update-or-create dedupe (W-12) — no popup window sizing here. Export createStandaloneApp() for testability like Task 1. No content imports (Pitfall 4). Create tests/entrypoints/standalone.test.tsx: mount smoke test — tree mounts without throwing; exactly one ConfigProvider; StandaloneShell header (standalone.openTitle) renders. Reuse the Task 1 test harness patterns (fakeBrowser, store fixtures, document.getElementById stub).
  </action>
  <verify>
    <automated>pnpm vitest run tests/entrypoints/standalone.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>
    Standalone mounts one XProvider + one ConfigProvider; smoke test green (mounts, single provider, shell header).
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/entrypoints/standalone.test.tsx` exits 0
    - `grep -c "ConfigProvider" src/entrypoints/standalone/main.tsx` == 1 (Appendix F: one per surface)
    - `grep -c "createStandaloneApp" src/entrypoints/standalone/main.tsx` == 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: workerState (ResponseEnvelope helpers, RUNTIME-02 core)</name>
  <files>
    src/core/runtime/workerState.ts, tests/core/runtime/workerState.test.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md §20.5 (lines 3129-3137) — BackgroundWorkerState
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (workerState, lines 274-285)
  </read_first>
  <action>
    Create src/core/runtime/workerState.ts (canonical §18 path line 2434 — W-13: this file had no owning plan; it is created here so the RUNTIME-02 validation map row and the 01-02 MessageType/ResponseEnvelope contracts have a home): export const workerState = { ok<T>(data: T): ResponseEnvelope<T> { return { ok: true, data } }, fail(code: ErrorCode, message: string): ResponseEnvelope<never> { return { ok: false, error: { code, message } } } } where ResponseEnvelope is imported from '@/types/messaging' (01-02, Appendix E) and ErrorCode from '@/core/error/errorCodes' (01-04). Import the BackgroundWorkerState type from §20.5 (01-02 types) for the SW state shape. Every background/content handler replies via these helpers (Pitfall 5). Never throws. Write tests/core/runtime/workerState.test.ts: workerState.ok wraps data with ok:true; workerState.fail produces the { code, message } error shape with ok:false; roundtrip: a fail envelope serialized via RuntimeEnvelope (01-02) deserializes to the same shape.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/runtime/workerState.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    workerState.ok/fail helpers exist at the §18 canonical path with ResponseEnvelope typing; tests green; RUNTIME-02 map row now has an owning task (W-13).
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/runtime/workerState.test.ts` exits 0
    - `grep -c "workerState.ok\|workerState.fail\|ok: true\|ok: false" src/core/runtime/workerState.ts` >= 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Background SW — defineBackground + the four managers (§5.1, BLOCKER 3)</name>
  <files>
    src/entrypoints/background.ts, src/core/background/BackgroundRouter.ts, src/core/background/LifecycleManager.ts, src/core/background/KeepAliveManager.ts, src/core/background/ContextMenuHost.ts
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md §5.1 (lines 858-925) — canonical background shape + §5.3 setPanelBehavior
    .planning/PRODUCT_SPEC_v0_1.md Appendix E (lines 5050-5063) — BackgroundRouter skeleton
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (background, lines 146-175)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 1 lines 361-371, A6 line 494)
  </read_first>
  <action>
    BLOCKER 3 — replace the 01-01 scaffold background stub body. Create src/core/background/BackgroundRouter.ts per Appendix E (lines 5050-5063): export const BackgroundRouter = { register(): void { chrome.runtime.onMessage.addListener((message, sender, sendResponse) => { ... }) } } — a typed chrome.runtime.onMessage dispatcher (§8.1 line 1195) that (a) registers SYNCHRONOUSLY at module load (DONE-when §18 line 2465 — no await before listener registration), (b) validates sender.id === chrome.runtime.id AND MessageTypeValues.includes(envelope.type) (Pitfall 5 + §16.2 line 2014 — unknown/foreign messages get workerState.fail('MSG_UNKNOWN_TYPE', ...)), (c) dispatches valid envelopes to MessageBus (01-03) and replies via workerState.ok/workerState.fail (Task 3); every path returns a value or calls sendResponse — never throws (Golden Rule 9; debugLog EVT_HANDLER on handler errors). Create src/core/background/LifecycleManager.ts: register(): void — subscribes chrome.runtime.onInstalled + chrome.runtime.onStartup, both calling chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick: true }) (§5.3 line 915; RESEARCH Pitfall 1 mitigation lines 361-362 — the action button must open the side panel); failures → debugLog(SIDEPANEL_BEHAVIOR, ...), never throw. Idempotent (safe to re-run on every SW startup, RESEARCH A6). Create src/core/background/KeepAliveManager.ts: register(): void — chrome.alarms.create('nowpilot-keepalive', { periodInMinutes: 0.5 }) + chrome.alarms.onAlarm listener that no-ops (keepalive touch only; alarms permission is in the 01-01 manifest; R-3: no AI/IndexedDB work in the SW — the panel-ping heartbeat is BroadcastBus in 01-06, not here). Create src/core/background/ContextMenuHost.ts: recreateAll(): void — chrome.contextMenus.removeAll() then chrome.contextMenus.create({ id: 'nowpilot-summarize', title: 'NowPilot — Summarize page', contexts: ['page'] }) with an onClicked handler that logs debugLog (real summarization lands in its phase; no-op action this phase, D-15: menu exists but performs no stub behavior beyond the log); idempotent re-register per startup (A6); failures → debugLog, never throw. Create src/entrypoints/background.ts as the §5.1 canonical composition (line 862): export default defineBackground({ type: 'module', persistent: false, main() { BackgroundRouter.register(); LifecycleManager.register(); KeepAliveManager.register(); ContextMenuHost.recreateAll(); } }) — import defineBackground from 'wxt/utils/define-background' (wxt ^0.19). All four registrations run in the SW's FIRST task, synchronously (DONE-when). No React/antd imports anywhere in these five files (Pitfall 4 — the SW must stay lean). Verify via tsc + the workerState/MessageBus tests; setPanelBehavior/contextMenus behavior itself is browser-e2e-verified (RESEARCH A6/A8).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -5 && pnpm vitest run tests/core/runtime/workerState.test.ts tests/core/messaging 2>&1 | tail -3</automated>
  </verify>
  <done>
    background.ts matches the §5.1 canonical shape with all four managers registered synchronously (BLOCKER 3); BackgroundRouter validates sender + whitelist and replies via workerState; tsc green; SW files import no UI libs.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "defineBackground\|BackgroundRouter.register()\|LifecycleManager.register()\|KeepAliveManager.register()\|ContextMenuHost.recreateAll()" src/entrypoints/background.ts` >= 5
    - `grep -c "setPanelBehavior" src/core/background/LifecycleManager.ts` == 1 (§5.3)
    - `grep -c "sender.id\|MessageTypeValues" src/core/background/BackgroundRouter.ts` >= 2 (Pitfall 5 + §16.2)
    - `grep -rn "from 'react'\|from 'antd'" src/core/background/ src/entrypoints/background.ts | wc -l` == 0 (Pitfall 4)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Phase gate green — verify:phase-1 + DONE-when hygiene greps</name>
  <files>
    src/entrypoints/sidepanel/main.tsx, src/entrypoints/standalone/main.tsx, src/entrypoints/background.ts (touched only if the gate fixes require it)
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md (per-task verification map, for reference only)
  </read_first>
  <action>
    Run the complete Phase 1 gate and fix anything it catches: `pnpm verify:phase-1` (the 01-01 chain: eslint → prettier --check → tsc --noEmit → wxt build → `vitest run` (full suite — no dir list) → node tests/isolation/check-content-bundle.mjs) must exit 0. Then run the DONE-when hygiene greps as plan-level verification (NOT added to package.json — per 01-01): (a) RuntimeEnvelope fixtures parse — vitest tests/core/runtime green; (b) MessageType = Appendix E + exactly the 4 D-17 additions (grep counts); (c) no banned package in package.json dependencies (grep the banned list); (d) no zustand persist in ThemeStore/WorkspaceStore/AddonSettingsStore (Pitfall 7); (e) no awaited tabs.query in WorkspaceRouter (Pitfall 1); (f) content isolation — check-content-bundle.mjs exits 0; (g) no UI-lib imports in src/core/content and src/entrypoints/content (Pitfall 4). Fix any failure at its source (prefer the owning plan's file; small targeted fixes only — if a structural defect surfaces, stop and report rather than rewrite). W-13: do NOT edit 01-VALIDATION.md status/nyquist_compliant/wave_0_complete fields — validate-phase owns those; Task 5 only records the green run + hygiene grep results in its SUMMARY (01-09-SUMMARY.md). Do not modify the 01-01 verify:phase-1 script itself.
  </action>
  <verify>
    <automated>pnpm verify:phase-1 2>&1 | tail -15</automated>
  </verify>
  <done>
    `pnpm verify:phase-1` exits 0; all DONE-when hygiene greps pass; results recorded in the SUMMARY only (01-VALIDATION.md status fields left to validate-phase, W-13); nothing is silently skipped.
  </done>
  <acceptance_criteria>
    - `pnpm verify:phase-1` exits 0
    - `grep -c "PING\|PONG\|GET_CONTENT_CAPABILITIES\|CONTENT_CAPABILITIES" src/core/runtime/MessageType.ts` == 4
    - `grep -rn "persist(" src/core/theme/ThemeStore.ts src/core/workspace/WorkspaceStore.ts src/core/registry/AddonSettingsStore.ts | wc -l` == 0
    - `grep -rn "await browser.tabs.query\|await chrome.tabs.query" src/core/workspace/ | wc -l` == 0
    - `node tests/isolation/check-content-bundle.mjs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension page → React root | Entrypoint HTML is extension-controlled; mounts must not execute page-derived strings |
| store readiness → first paint | Uninitialized theme/workspace must not flash incorrect UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-21 | Tampering (XSS via mount) | Entrypoint main.tsx | high | mitigate | Mounts render only through sanitized components (PortableMarkdown 01-04) and STR constants; no innerHTML with dynamic content; CSP from wxt defaults retained |
| T-1-22 | Spoofing | Theme readiness flash | low | accept | isReady gate renders null until ThemeStore init resolves; a blank frame is acceptable, no wrong-theme flash |
| T-1-23 | Tampering (supply chain) | Built bundles | high | mitigate | verify:phase-1 runs check-content-bundle.mjs (Pitfall 4) — a UI-lib leak into content bundles fails the phase gate (T-1-17 enforcement) |
</threat_model>

<verification>
- `pnpm verify:phase-1` green (the full §24 chain)
- All DONE-when hygiene greps pass (listed in Task 5)
- 01-VALIDATION.md status fields left untouched (validate-phase owns them — W-13); results recorded in the SUMMARY
</verification>

<success_criteria>
- Both surfaces mount (one XProvider + one ConfigProvider each, Appendix F)
- Background SW matches §5.1 canonical shape, listeners registered synchronously, setPanelBehavior wired (BLOCKER 3)
- workerState.ok/fail helpers exist at the §18 path (RUNTIME-02, W-13)
- Full Phase 1 suite green: eslint, prettier, tsc, wxt build, vitest, isolation check
- DONE-when conditions hold (envelope fixtures, D-17 additions, no banned imports, Pitfall 1/4/7 guards)
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-09-SUMMARY.md` when done
</output>
