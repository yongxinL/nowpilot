---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 09
type: execute
wave: 5
depends_on: [01-05, 01-06, 01-07, 01-08]
files_modified:
  - src/sidepanel/main.tsx
  - src/standalone/main.tsx
  - tests/entrypoints/sidepanel.test.tsx
  - tests/entrypoints/standalone.test.tsx
autonomous: true
requirements: [RUNTIME-05, WSPC-01, WSPC-02]
must_haves:
  truths:
    - "The side panel entrypoint (src/sidepanel/main.tsx) mounts SidePanelRouter inside ONE XProvider + ONE ConfigProvider per Appendix F (one provider per surface; theme from antdConfig 01-05) — no second provider anywhere."
    - "The standalone entrypoint (src/standalone/main.tsx) mounts StandaloneRouter the same way (Appendix F), reading the standalone window size per UI-SPEC."
    - "Both entrypoints install the global mod+k key capture → CmdKPicker (01-08) and wrap in ErrorBoundary (01-04); neither entrypoint imports content-script modules (Pitfall 4)."
    - "Mount smoke tests render each entrypoint root with fakeBrowser + store fixtures and assert the shell/onboarding tree mounts without throwing."
    - "The full Phase 1 gate is green: `pnpm verify:phase-1` passes end-to-end (eslint → prettier → tsc → wxt build → full vitest suite → content-bundle isolation check)."
    - "The DONE-when hygiene greps (RuntimeEnvelope fixtures parse, MessageType = Appendix E + D-17, no banned imports, whitelist enforcement, Pitfall 1/4/7 guards) run as plan-level verification here and pass."
  artifacts:
    - src/sidepanel/main.tsx
    - src/standalone/main.tsx
    - tests/entrypoints/sidepanel.test.tsx
    - tests/entrypoints/standalone.test.tsx
  key_links:
    - "Entrypoints are the ONLY places that call createRoot + mount — everything upstream is tree-imported, nothing else touches ReactDOM."
    - "The ConfigProvider theme comes from antdConfig (01-05) fed by ThemeStore — a single theme source across both surfaces."
  flagged_assumptions:
    - statement: "RUNTIME-05 entrypoints — mount verified via jsdom smoke tests; real Chrome load of sidepanel.html/standalone.html (including sidePanel.open gesture flow) deferred to browser e2e (RESEARCH A8)."
      verification: flagged-unverified
    - statement: "WSPC-01 full-suite green — verify:phase-1 runs in the CI-less local environment; green here means the documented command chain exits 0."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Mount the two surfaces (Appendix F: one XProvider + one ConfigProvider per surface), install global key capture, and drive the entire phase to green: full vitest suite, wxt build, content isolation check, and the DONE-when hygiene greps.

Purpose: This is the last plan of Phase 1 — when it passes, the extension builds, all surfaces mount, and the phase's §DONE-when conditions hold. verify:phase-1 (01-01) becomes the phase-completion gate.
Output: 2 entrypoint files + 2 mount smoke tests.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-13, D-16)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Appendix F one-provider-per-surface, validation map)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (standalone window size, entrypoint requirements)
@.planning/PRODUCT_SPEC_v0_1.md (Appendix F line ~5231, §24 verify scripts)

# Depends on 01-05 (antdConfig), 01-06 (WorkspaceStore/Router), 01-07 (isolation script), 01-08 (routers/shells).
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | Entrypoints never call sidePanel.open — only WorkspaceRouter (01-06) does, gesture-safe |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | Entrypoints never import content modules; check-content-bundle.mjs runs in verify:phase-1 |
| P5 | Message contracts bypassing RuntimeEnvelope | All messaging via 01-03 bridge |
| P6 | matchMedia-in-jsdom | Handled in 01-01 |
| P7 | zustand persist for cross-surface | Stores own persistence; entrypoints are consumers |

<tasks>

<task type="auto">
  <name>Task 1: Side panel entrypoint mount (Appendix F)</name>
  <files>
    src/sidepanel/main.tsx, tests/entrypoints/sidepanel.test.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix F (lines 5231-5280) — one provider per surface
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Appendix F one-provider rule)
  </read_first>
  <action>
    Create src/sidepanel/main.tsx: the side panel entry root — createRoot from 'react-dom/client' on the sidepanel.html root element; render exactly ONE XProvider (from @ant-design/x) wrapping ONE antd ConfigProvider (theme = buildAntdTheme from 01-05 fed by useThemeStore()) wrapping ErrorBoundary → SidePanelRouter (01-08); add a global keydown listener (isCmdK from 01-04) toggling CmdKPicker visibility (state lifted here, passed to SidePanelShell). ThemeStore.init() must be awaited/fired before first render (fire-and-forget with a readiness gate on isReady from the store, rendering null until ready to avoid a flash of default theme). No second provider anywhere (Appendix F). No import of any content-script module (Pitfall 4). Create tests/entrypoints/sidepanel.test.tsx: jsdom mount of the entrypoint root (import the main module, but guard DOM createRoot via a render helper that stubs document.getElementById for the entry root — or export a createSidePanelApp() from main.tsx for testability); assert the tree mounts without throwing; assert exactly one ConfigProvider renders (query by role/tree) and the onboarding gate decides the initial screen (onboarding pending → Onboarding text; done → shell header). Use fakeBrowser + store.setState fixtures per 01-08 test patterns.
  </action>
  <verify>
    <automated>pnpm vitest run tests/entrypoints/sidepanel.test.tsx -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    Side panel mounts one XProvider + one ConfigProvider with themed ConfigProvider; key capture wired; smoke test green (mounts, single provider, onboarding gate correct).
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/entrypoints/sidepanel.test.tsx -x` exits 0
    - `grep -c "createRoot\|XProvider" src/sidepanel/main.tsx` >= 2
    - `grep -c "ConfigProvider" src/sidepanel/main.tsx` == 1 (Appendix F: one per surface)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Standalone entrypoint mount (Appendix F)</name>
  <files>
    src/standalone/main.tsx, tests/entrypoints/standalone.test.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Appendix F (lines 5231-5280)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (standalone window size)
  </read_first>
  <action>
    Create src/standalone/main.tsx mirroring Task 1 for the standalone window: createRoot on the standalone.html root; one XProvider + one ConfigProvider (same buildAntdTheme from 01-05); ErrorBoundary → StandaloneRouter (01-08); same global isCmdK → CmdKPicker wiring; window size handled by the router/opener (01-06), not here. Export createStandaloneApp() for testability like Task 1. No content imports (Pitfall 4). Create tests/entrypoints/standalone.test.tsx: mount smoke test — tree mounts without throwing; exactly one ConfigProvider; StandaloneShell header (standalone.openTitle) renders. Reuse the Task 1 test harness patterns (fakeBrowser, store fixtures, document.getElementById stub).
  </action>
  <verify>
    <automated>pnpm vitest run tests/entrypoints/standalone.test.tsx -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    Standalone mounts one XProvider + one ConfigProvider; smoke test green (mounts, single provider, shell header).
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/entrypoints/standalone.test.tsx -x` exits 0
    - `grep -c "ConfigProvider" src/standalone/main.tsx` == 1 (Appendix F: one per surface)
    - `grep -c "createStandaloneApp" src/standalone/main.tsx` == 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase gate green — verify:phase-1 + DONE-when hygiene greps</name>
  <files>
    src/sidepanel/main.tsx, src/standalone/main.tsx (touched only if the gate fixes require it)
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-VALIDATION.md (per-task verification map, final status)
  </read_first>
  <action>
    Run the complete Phase 1 gate and fix anything it catches: `pnpm verify:phase-1` (the 01-01 chain: eslint → prettier --check → tsc --noEmit → wxt build → vitest run over all tests/core and tests/components and tests/isolation → node tests/isolation/check-content-bundle.mjs) must exit 0. Then run the DONE-when hygiene greps as plan-level verification (NOT added to package.json — per 01-01): (a) RuntimeEnvelope fixtures parse — vitest tests/core/runtime green; (b) MessageType = Appendix E + exactly the 4 D-17 additions (grep counts); (c) no banned package in package.json dependencies (grep the banned list); (d) no zustand persist in ThemeStore/WorkspaceStore/AddonSettingsStore (Pitfall 7); (e) no awaited tabs.query in WorkspaceRouter (Pitfall 1); (f) content isolation — check-content-bundle.mjs exits 0; (g) no UI-lib imports in src/core/content and src/content (Pitfall 4). Fix any failure at its source (prefer the owning plan's file; small targeted fixes only — if a structural defect surfaces, stop and report rather than rewrite). Update 01-VALIDATION.md status to reflect the green run (nyquist_compliant: true, wave_0_complete: true) if all gates pass.
  </action>
  <verify>
    <automated>pnpm verify:phase-1 2>&1 | tail -15</automated>
  </verify>
  <done>
    `pnpm verify:phase-1` exits 0; all DONE-when hygiene greps pass; 01-VALIDATION.md updated to compliant/complete; nothing is silently skipped.
  </done>
  <acceptance_criteria>
    - `pnpm verify:phase-1` exits 0
    - `grep -c "PING\|PONG\|GET_CONTENT_CAPABILITIES\|CONTENT_CAPABILITIES" src/core/runtime/MessageType.ts` == 4
    - `grep -rn "persist(" src/core/theme/ThemeStore.ts src/core/workspace/WorkspaceStore.ts src/core/registry/AddonSettingsStore.ts | wc -l` == 0
    - `grep -rn "await browser.tabs.query\|await chrome.tabs.query" src/core/workspace/ | wc -l` == 0
    - `node tests/isolation/check-content-bundle.mjs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension page → React root | Entrypoint HTML is extension-controlled; mounts must not execute page-derived strings |
| store readiness → first paint | Uninitialized theme/workspace must not flash incorrect UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-21 | Tampering (XSS via mount) | Entrypoint main.tsx | high | mitigate | Mounts render only through sanitized components (PortableMarkdown 01-04) and STR constants; no innerHTML with dynamic content; CSP from wxt defaults retained |
| T-1-22 | Spoofing | Theme readiness flash | low | accept | isReady gate renders null until ThemeStore init resolves; a blank frame is acceptable, no wrong-theme flash |
| T-1-23 | Tampering (supply chain) | Built bundles | high | mitigate | verify:phase-1 runs check-content-bundle.mjs (Pitfall 4) — a UI-lib leak into content bundles fails the phase gate (T-1-17 enforcement) |
</threat_model>

<verification>
- `pnpm verify:phase-1` green (the full §24 chain)
- All DONE-when hygiene greps pass (listed in Task 3)
- 01-VALIDATION.md flipped to compliant/complete
</verification>

<success_criteria>
- Both surfaces mount (one XProvider + one ConfigProvider each, Appendix F)
- Full Phase 1 suite green: eslint, prettier, tsc, wxt build, vitest, isolation check
- DONE-when conditions hold (envelope fixtures, D-17 additions, no banned imports, Pitfall 1/4/7 guards)
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-09-SUMMARY.md` when done
</output>
