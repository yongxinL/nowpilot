---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 08
type: execute
wave: 5
depends_on: [01-03, 01-04, 01-05, 01-06]
files_modified:
  - src/components/routers/SidePanelRouter.tsx
  - src/components/routers/StandaloneRouter.tsx
  - src/components/sidepanel/SidePanelShell.tsx
  - src/components/standalone/StandaloneShell.tsx
  - src/components/onboarding/Onboarding.tsx
  - src/components/cmdk/CmdKPicker.tsx
  - src/components/pages/sidepanel/ChatPageSkeleton.tsx
  - src/components/pages/standalone/WorkspacePageSkeleton.tsx
  - src/components/pages/ChatPage.tsx
  - src/components/pages/AgentPage.tsx
  - src/components/pages/NotesPage.tsx
  - src/components/pages/OptionsPage.tsx
  - src/core/providers/ProviderRegistry.ts
  - tests/components/sidepanel/SidePanelShell.test.tsx
  - tests/components/standalone/StandaloneShell.test.tsx
  - tests/components/onboarding/Onboarding.test.tsx
  - tests/components/cmdk/CmdKPicker.test.tsx
autonomous: true
requirements: [RUNTIME-03, RUNTIME-05, WSPC-05]
must_haves:
  truths:
    - "SidePanelShell is chat-only per UI-SPEC §17.1 (chat layout, no host-page UI, no write-back — R-5); StandaloneShell mirrors it with the standalone layout."
    - "SidePanelRouter gates on activeProvider presence per D-07 (ProviderRegistry check — NOT an onboarding-done flag, W-10): no provider → Onboarding (persona card, then a disabled surface after 'Configure later', D-06/D-07); provider present → chat shell. StandaloneRouter routes the §18 pages (Chat/Agent/Notes/Options) from StandalonePageRegistry (01-07)."
    - "Onboarding (Flow 9) renders step 1 (welcome + persona card + provider choice UI skeleton) with a 'Configure later' escape that marks onboarding done (D-06) and exits to the disabled surface (D-07: no provider configured ⇒ disabled surface, STR.chat.noProvider) — STR.onboarding.* strings from 01-02."
    - "CmdKPicker (Flow 10) opens on mod+k (KEYMAP.CMD_K via isCmdK, 01-04), shows a searchable list of EXACTLY three commands — Open Standalone / Focus Side Panel / Open Options (W-8, D-15: no stub commands) — filters by query, runs the selected command via WorkspaceRouter (01-06); commands come from a typed const list, never free-form execution."
    - "Page skeletons per §18 create list: ChatPage, AgentPage, NotesPage, OptionsPage (src/components/pages/, canonical names, W-6) + the ChatPageSkeleton/WorkspacePageSkeleton sub-components; each renders its UI-SPEC E5 empty-state copy — Notes 'Notes live here once you save your first one.' + 'New note' CTA, Options 'No provider connected. Set up a provider to start.', Agent 'Agent runs land here.'; no chat logic (chat lands in its phase)."
    - "Shells and pages consume theme via ThemeStore (01-05) and workspace via WorkspaceStore (01-06) — no direct chrome API calls in components (all through stores)."
    - "Components never import content-script modules (Pitfall 4); every error path logs via debugLog with canonical codes (COMPONENT_RENDER etc.)."
  artifacts:
    - src/components/routers/SidePanelRouter.tsx, StandaloneRouter.tsx
    - src/components/sidepanel/SidePanelShell.tsx
    - src/components/standalone/StandaloneShell.tsx
    - src/components/onboarding/Onboarding.tsx
    - src/components/cmdk/CmdKPicker.tsx
    - src/components/pages/sidepanel/ChatPageSkeleton.tsx
    - src/components/pages/standalone/WorkspacePageSkeleton.tsx
    - src/components/pages/ChatPage.tsx, AgentPage.tsx, NotesPage.tsx, OptionsPage.tsx
    - src/core/providers/ProviderRegistry.ts (D-07 gate stub)
    - tests/components/{sidepanel,standalone,onboarding,cmdk}/*.test.tsx
  key_links:
    - "Routers select the surface tree from store state — 01-09 mounts SidePanelRouter/StandaloneRouter as the entry roots."
    - "SidePanelRouter gates on ProviderRegistry.hasActiveProvider() (D-07); CmdKPicker calls WorkspaceRouter.openSidePanel/openStandalone (01-06) — the only surface-open path (Pitfall 1 respected)."
  flagged_assumptions:
    - statement: "RUNTIME-03 routing — router decisions unit-tested with store fixtures; full surface interaction verified in browser e2e."
      verification: flagged-unverified
    - statement: "WSPC-05 focus/Cmd+K — keyboard capture (mod+k) and FocusTrap behavior unit-tested in jsdom; real Chrome keyboard focus flow deferred to e2e."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Build the surface UI: SidePanelRouter/StandaloneRouter, SidePanelShell (chat-only §17.1) + StandaloneShell, Onboarding (Flow 9, D-06/D-07), CmdKPicker (Flow 10, W-8 command set), the §18 page skeletons (ChatPage/AgentPage/NotesPage/OptionsPage per W-6), and the D-07 provider gate.

Purpose: The user can finally see and drive the extension: onboarding gate (D-07 activeProvider check), Cmd+K command palette, and the two shells. 01-09 mounts these at the entrypoints.
Output: 13 source files + 4 test files.
Scope note (deliberate, W-14): this plan batches 13 small files (shells/routers/pages are <80 lines each) because they are one concern — the surface tree — and share the store contracts; splitting would force cross-wave fixture duplication at higher coordination cost.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-06 onboarding, D-13 theme)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (§17.1 side panel chat-only layout; onboarding + Cmd+K flows)
@.planning/PRODUCT_SPEC_v0_1.md (Flow 9 line ~1702, Flow 10 line ~1706, §17.1 line ~2074, §18 Phase 1 create list)

# Depends on 01-03 (EventBus), 01-04 (components/strings), 01-05 (ThemeStore), 01-06 (WorkspaceStore/Router).
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | CmdKPicker routes through WorkspaceRouter (01-06) — never opens sidePanel directly |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | Shells/components never import content modules; content entry isolated in 01-07 |
| P5 | Message contracts bypassing RuntimeEnvelope | Components talk to stores; messaging only via 01-03 bridge |
| P6 | matchMedia-in-jsdom | Handled in 01-01 |
| P7 | zustand persist for cross-surface | Stores own persistence (01-05/01-06); components are read-only consumers |

<tasks>

<task type="auto">
  <name>Task 1: Provider gate + Routers + Shells (surface trees)</name>
  <files>
    src/core/providers/ProviderRegistry.ts, src/components/routers/SidePanelRouter.tsx, src/components/routers/StandaloneRouter.tsx, src/components/sidepanel/SidePanelShell.tsx, src/components/standalone/StandaloneShell.tsx, tests/components/sidepanel/SidePanelShell.test.tsx, tests/components/standalone/StandaloneShell.test.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md §17.1 (lines 2074-2110) — side panel chat-only layout
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md D-07 (activeProvider gate) and D-06
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (shell layout sections)
  </read_first>
  <action>
    Create src/core/providers/ProviderRegistry.ts as the D-07 gate primitive: a minimal in-memory registry (Register-like pattern from 01-07) with registerActiveProvider(providerId: string): void, hasActiveProvider(): boolean, getActiveProvider(): string | undefined. Phase 1 ships NO real providers (providers phase) — the registry starts empty so the gate renders onboarding → disabled surface; UAT/browser tests may register a fake provider id to unlock the chat shell (registerActiveProvider is functional, not hardcoded — @implementation-tier stub per Golden Rule 10; debugLog REGISTRY_INIT, never throws). Create src/components/sidepanel/SidePanelShell.tsx: antd Layout-based shell rendering the chat-only structure per §17.1 — header (title + theme indicator), main content area rendering the ChatPage (Task 3), input placeholder bar (disabled Input with STR.chat.askPlaceholder from 01-02 — NO chat logic, chat lands in its phase); when no provider is active (D-07) the shell renders the disabled surface (STR.chat.noProvider banner, inputs disabled); reads theme from useThemeStore() (01-05) for display only (ConfigProvider wiring happens at mount in 01-09); subscribes to workspace activeSurface via useWorkspaceStore (01-06). Wrap the tree in ErrorBoundary (01-04). Create src/components/standalone/StandaloneShell.tsx: same shell pattern for the standalone extension tab (standalone opens via update-or-create tab in 01-06 W-12 — no popup window dimensions apply) — header with standalone.openTitle (STR), content area rendering the active §18 page (Chat/Agent/Notes/Options from StandalonePageRegistry, 01-07); also wrapped in ErrorBoundary. Create src/components/routers/SidePanelRouter.tsx: reads activeProvider via ProviderRegistry.hasActiveProvider() (D-07 — NOT onboarding-done, W-10) + onboarding-done from useWorkspaceStore; no provider + onboarding pending → render Onboarding (Task 2); no provider + onboarding done ('Configure later') → render the disabled SidePanelShell (D-07 disabled surface); provider present → render SidePanelShell enabled. Create src/components/routers/StandaloneRouter.tsx: renders StandaloneShell with page routing driven by StandalonePageRegistry ids (Chat default). Both routers are pure store/registry-driven selectors — no chrome API calls. Write tests: SidePanelShell.test.tsx (renders header + askPlaceholder input disabled; no send button — queryByRole('button', {name: /send/i}) null; disabled-surface state when no provider); StandaloneShell.test.tsx (renders openTitle header); routers: SidePanelRouter with no provider + onboarding pending shows Onboarding; no provider + onboarding done shows the disabled shell; with a registered provider shows the enabled shell (register via ProviderRegistry.registerActiveProvider in test setup).
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/sidepanel tests/components/standalone 2>&1 | tail -5</automated>
  </verify>
  <done>
    ProviderRegistry gate exists (D-07); both shells render per §17.1 with ErrorBoundary wrappers; routers switch on provider presence (not onboarding-done, W-10); tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/components/sidepanel tests/components/standalone` exits 0
    - `grep -c "hasActiveProvider" src/components/routers/SidePanelRouter.tsx` == 1 (D-07 gate, W-10)
    - `grep -c "ErrorBoundary" src/components/sidepanel/SidePanelShell.tsx src/components/standalone/StandaloneShell.tsx` >= 2
    - `grep -rn "chrome\." src/components/ | wc -l` == 0 (components never call chrome APIs directly)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Onboarding (Flow 9) + CmdKPicker (Flow 10)</name>
  <files>
    src/components/onboarding/Onboarding.tsx, src/components/cmdk/CmdKPicker.tsx, tests/components/onboarding/Onboarding.test.tsx, tests/components/cmdk/CmdKPicker.test.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Flow 9 (lines 1702-1706), Flow 10 (lines 1706-1710)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md D-06 (onboarding step 1 + Configure later)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (cmdk, onboarding, lines 145-155)
  </read_first>
  <action>
    Create src/components/onboarding/Onboarding.tsx per Flow 9 + D-06/D-07: renders step 1 — welcome (STR.onboarding.* from 01-02) + persona card (E7) + a provider-choice UI skeleton (four provider buttons disabled with a 'coming in settings phase' note; per D-06 the full provider configure flow lands in the settings phase) + a "Configure later" Button that marks onboarding done (writes via useWorkspaceStore/AddonSettingsStore — ONBOARDING_WRITE/ONBOARDING_DONE codes on error, never throw) — after 'Configure later' the router exits to the disabled surface (no provider ⇒ disabled shell, D-07), NOT to the chat shell; wrapped in ErrorBoundary. Create src/components/cmdk/CmdKPicker.tsx per Flow 10: an antd Modal (or Dropdown) opened by isCmdK (01-04) captured at the shell level; renders an AutoComplete-style search Input over a static typed COMMANDS const array with EXACTLY three commands per W-8 (D-15: no stub commands, no new-conversation): [{ id: 'open-standalone', label: 'Open Standalone', run: () => WorkspaceRouter.openStandalone() }, { id: 'focus-side-panel', label: 'Focus Side Panel', run: () => WorkspaceRouter.openSidePanel() }, { id: 'open-options', label: 'Open Options', run: () => { WorkspaceRouter.openStandalone(); navigateToPage('options'); } }] — navigateToPage sets the standalone active page (StandalonePageRegistry id) via the standalone router action from Task 1; Enter runs the highlighted command; Escape closes; wrapped in FocusTrap (01-04) + ErrorBoundary. Commands are a typed const array — no free-form execution. Write tests: Onboarding.test.tsx (renders welcome + persona card + provider skeleton; 'Configure later' flips onboarding-done in the store — assert the router shows the disabled shell in the SidePanelRouter test); CmdKPicker.test.tsx (isCmdK opens; typing filters the list; the command list contains EXACTLY open-standalone/focus-side-panel/open-options; Enter on open-standalone calls WorkspaceRouter.openStandalone via vi.mock; Escape closes and focus returns).
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/onboarding tests/components/cmdk 2>&1 | tail -5</automated>
  </verify>
  <done>
    Onboarding implements Flow 9 step 1 (persona card, E7) with the D-06 'Configure later' escape → disabled surface (D-07); CmdKPicker implements Flow 10 with the exact W-8 command set + FocusTrap + router-backed commands; tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/components/onboarding tests/components/cmdk` exits 0
    - `grep -c "Configure later" src/components/onboarding/Onboarding.tsx` == 1
    - `grep -c "Open Standalone\|Focus Side Panel\|Open Options" src/components/cmdk/CmdKPicker.tsx` >= 3 (W-8 exact command set)
    - `grep -c "isCmdK\|WorkspaceRouter" src/components/cmdk/CmdKPicker.tsx` >= 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: §18 page skeletons — ChatPage/AgentPage/NotesPage/OptionsPage + empty states</name>
  <files>
    src/components/pages/sidepanel/ChatPageSkeleton.tsx, src/components/pages/standalone/WorkspacePageSkeleton.tsx, src/components/pages/ChatPage.tsx, src/components/pages/AgentPage.tsx, src/components/pages/NotesPage.tsx, src/components/pages/OptionsPage.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md §18 Phase 1 create list (lines 2420-2447 — canonical src/components/pages/{ChatPage,AgentPage,NotesPage,OptionsPage}.tsx)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (E5 empty states lines 148-150, 225-228)
  </read_first>
  <action>
    Create the four §18-named page files (W-6 — canonical flat names under src/components/pages/, NOT additional subfolders): src/components/pages/ChatPage.tsx renders the chat empty state — STR.chat.empty/loading text (01-02) in an antd Empty/Card with the MinimalMode-eligible placeholder per §2.4, using ChatPageSkeleton for the skeleton block; no chat messages state, no send flow (chat lands in its phase); wrapped in ErrorBoundary. src/components/pages/AgentPage.tsx renders the E5 Agent empty state — STR.agent.* (or the verbatim E5 copy 'Agent runs land here.') with an antd Skeleton block (UI-SPEC E5 loading row: avatar + 2-3 paragraph rows on the 8/12/16 px rhythm). src/components/pages/NotesPage.tsx renders the E5 Notes empty state — verbatim copy 'Notes live here once you save your first one.' (STR.notes.* source) with a 'New note' CTA Button (enabled but no-op with a phase note — D-15: the CTA exists per UI-SPEC E5 line 113/148 but performs no stub action; clicking does nothing beyond a debugLog COMPONENT_RENDER silent:true). src/components/pages/OptionsPage.tsx renders the Account + Appearance card layout (UI-SPEC line 150) — Account card shows the E5 provider empty state 'No provider connected. Set up a provider to start.' (STR.noProvider family; forward-compat for the providers phase); Appearance card renders the displayMode selector (light/dark/auto, D-14) wired to useThemeStore().setMode (01-05) with the E5 theme-persistence error toast ('Couldn't save your display mode...') on failure. Keep the existing src/components/pages/sidepanel/ChatPageSkeleton.tsx + src/components/pages/standalone/WorkspacePageSkeleton.tsx as the shared skeleton sub-components used by the four pages (ChatPageSkeleton = chat empty/skeleton body; WorkspacePageSkeleton = standalone workspace empty state + currentPageContext summary card reading useWorkspaceStore().workspace.currentPageContext — display only). All six components import only store/strings/components from prior plans — never content modules, never chrome APIs (Pitfall 4). No new test files — the shell tests in Task 1 render ChatPage/OptionsPage; verify via tsc + the Task 1 suite.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -5 && pnpm vitest run tests/components/sidepanel tests/components/standalone 2>&1 | tail -3</automated>
  </verify>
  <done>
    ChatPage/AgentPage/NotesPage/OptionsPage exist at §18 canonical paths (W-6) with their E5 empty-state copy lifted verbatim; tsc + shell tests green.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "chat.empty\|chat.loading" src/components/pages/ChatPage.tsx` >= 1
    - `grep -c "Agent runs land here" src/components/pages/AgentPage.tsx` >= 1 (E5)
    - `grep -c "New note" src/components/pages/NotesPage.tsx` >= 1 (E5)
    - `grep -c "No provider connected" src/components/pages/OptionsPage.tsx` >= 1 (E5)
    - `grep -rn "chrome\." src/components/pages/ | wc -l` == 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → Cmd+K | Command selection is constrained to a typed list; no free-form execution |
| store state → UI | Onboarding gate decides which tree renders; stale/false state must not unlock restricted UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-18 | Elevation of Privilege | SidePanelRouter gate | medium | mitigate | Gate reads ProviderRegistry.hasActiveProvider() (D-07, W-10) + onboarding-done from the workspace store (single sources); router re-evaluates on store/registry change — no cached UI flag |
| T-1-19 | Tampering (XSS via markdown) | ChatPageSkeleton/WorkspacePageSkeleton | medium | mitigate | Any future rendered content flows through PortableMarkdown (01-04); skeletons render STR constants only this phase |
| T-1-20 | Spoofing | CmdKPicker commands | low | accept | Static typed command array; execution delegates to WorkspaceRouter (01-06) which validates its own API surface |
</threat_model>

<verification>
- `pnpm vitest run tests/components` green
- `pnpm tsc --noEmit` green
- No chrome.* calls in components (grep-guarded)
</verification>

<success_criteria>
- Both shells render (chat-only §17.1), routers gate on ProviderRegistry activeProvider (D-07, W-10)
- Flow 9 + Flow 10 implemented with D-06 semantics + the exact W-8 command set
- §18 page skeletons in place (ChatPage/AgentPage/NotesPage/OptionsPage, W-6) with E5 empty-state copy
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-08-SUMMARY.md` when done
</output>
