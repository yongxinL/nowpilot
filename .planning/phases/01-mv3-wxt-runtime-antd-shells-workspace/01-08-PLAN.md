---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 08
type: execute
wave: 5
depends_on: [01-03, 01-04, 01-05, 01-06]
files_modified:
  - src/components/routers/SidePanelRouter.tsx
  - src/components/routers/StandaloneRouter.tsx
  - src/components/sidepanel/SidePanelShell.tsx
  - src/components/standalone/StandaloneShell.tsx
  - src/components/onboarding/Onboarding.tsx
  - src/components/cmdk/CmdKPicker.tsx
  - src/components/pages/sidepanel/ChatPageSkeleton.tsx
  - src/components/pages/standalone/WorkspacePageSkeleton.tsx
  - tests/components/sidepanel/SidePanelShell.test.tsx
  - tests/components/standalone/StandaloneShell.test.tsx
  - tests/components/onboarding/Onboarding.test.tsx
  - tests/components/cmdk/CmdKPicker.test.tsx
autonomous: true
requirements: [RUNTIME-03, RUNTIME-05, WSPC-05]
must_haves:
  truths:
    - "SidePanelShell is chat-only per UI-SPEC §17.1 (chat layout, no host-page UI, no write-back — R-5); StandaloneShell mirrors it with the standalone layout."
    - "SidePanelRouter decides between Onboarding (not configured) and the chat shell (configured) using workspace/onboarding state (D-06: onboarding step 1 + 'Configure later'); StandaloneRouter routes workspace pages."
    - "Onboarding (Flow 9) renders step 1 (welcome + provider choice UI skeleton) with a 'Configure later' escape that marks onboarding done (D-06) — STR.onboarding.* strings from 01-02."
    - "CmdKPicker (Flow 10) opens on mod+k (KEYMAP.CMD_K via isCmdK, 01-04), shows a searchable list of commands (open side panel / open standalone / new conversation), filters by query, runs the selected command via WorkspaceRouter (01-06) — commands come from a command list, never free-form execution."
    - "Page skeletons (ChatPageSkeleton, WorkspacePageSkeleton) render the §18 Phase 1 skeleton layout with headers/empty states — STR.chat.empty/loading states wired, no chat logic (chat lands in its phase)."
    - "Shells and pages consume theme via ThemeStore (01-05) and workspace via WorkspaceStore (01-06) — no direct chrome API calls in components (all through stores)."
    - "Components never import content-script modules (Pitfall 4); every error path logs via debugLog with canonical codes (COMPONENT_RENDER etc.)."
  artifacts:
    - src/components/routers/SidePanelRouter.tsx, StandaloneRouter.tsx
    - src/components/sidepanel/SidePanelShell.tsx
    - src/components/standalone/StandaloneShell.tsx
    - src/components/onboarding/Onboarding.tsx
    - src/components/cmdk/CmdKPicker.tsx
    - src/components/pages/sidepanel/ChatPageSkeleton.tsx
    - src/components/pages/standalone/WorkspacePageSkeleton.tsx
    - tests/components/{sidepanel,standalone,onboarding,cmdk}/*.test.tsx
  key_links:
    - "Routers select the surface tree from store state — 01-09 mounts SidePanelRouter/StandaloneRouter as the entry roots."
    - "CmdKPicker calls WorkspaceRouter.openSidePanel/openStandalone (01-06) — the only surface-open path (Pitfall 1 respected)."
  flagged_assumptions:
    - statement: "RUNTIME-03 routing — router decisions unit-tested with store fixtures; full surface interaction verified in browser e2e."
      verification: flagged-unverified
    - statement: "WSPC-05 focus/Cmd+K — keyboard capture (mod+k) and FocusTrap behavior unit-tested in jsdom; real Chrome keyboard focus flow deferred to e2e."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Build the surface UI: SidePanelRouter/StandaloneRouter, SidePanelShell (chat-only §17.1) + StandaloneShell, Onboarding (Flow 9, D-06), CmdKPicker (Flow 10), and the §18 page skeletons.

Purpose: The user can finally see and drive the extension: onboarding gate, Cmd+K command palette, and the two shells. 01-09 mounts these at the entrypoints.
Output: 8 source files + 4 test files.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-06 onboarding, D-13 theme)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (§17.1 side panel chat-only layout; onboarding + Cmd+K flows)
@.planning/PRODUCT_SPEC_v0_1.md (Flow 9 line ~1702, Flow 10 line ~1706, §17.1 line ~2074, §18 Phase 1 create list)

# Depends on 01-03 (EventBus), 01-04 (components/strings), 01-05 (ThemeStore), 01-06 (WorkspaceStore/Router).
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | CmdKPicker routes through WorkspaceRouter (01-06) — never opens sidePanel directly |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | Shells/components never import content modules; content entry isolated in 01-07 |
| P5 | Message contracts bypassing RuntimeEnvelope | Components talk to stores; messaging only via 01-03 bridge |
| P6 | matchMedia-in-jsdom | Handled in 01-01 |
| P7 | zustand persist for cross-surface | Stores own persistence (01-05/01-06); components are read-only consumers |

<tasks>

<task type="auto">
  <name>Task 1: Routers + Shells (surface trees)</name>
  <files>
    src/components/routers/SidePanelRouter.tsx, src/components/routers/StandaloneRouter.tsx, src/components/sidepanel/SidePanelShell.tsx, src/components/standalone/StandaloneShell.tsx, tests/components/sidepanel/SidePanelShell.test.tsx, tests/components/standalone/StandaloneShell.test.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md §17.1 (lines 2074-2110) — side panel chat-only layout
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (shell layout sections)
  </read_first>
  <action>
    Create src/components/sidepanel/SidePanelShell.tsx: antd Layout-based shell rendering the chat-only structure per §17.1 — header (title + theme indicator), main content area rendering the ChatPageSkeleton (Task 3), input placeholder bar (disabled Input with STR.chat.askPlaceholder from 01-02 — NO chat logic, chat lands in its phase); reads theme from useThemeStore() (01-05) for display only (ConfigProvider wiring happens at mount in 01-09); subscribes to workspace activeSurface via useWorkspaceStore (01-06). Wrap the tree in ErrorBoundary (01-04). Create src/components/standalone/StandaloneShell.tsx: same shell pattern for the standalone window — header with standalone.openTitle (STR), content area rendering WorkspacePageSkeleton (Task 3), width/height per UI-SPEC standalone spec; also wrapped in ErrorBoundary. Create src/components/routers/SidePanelRouter.tsx: reads onboarding-done + activeSurface from useWorkspaceStore; when onboarding not done → render Onboarding (Task 2); else render SidePanelShell. Create src/components/routers/StandaloneRouter.tsx: renders StandaloneShell (workspace pages routed inside via WorkspacePageSkeleton). Both routers are pure store-driven selectors — no chrome API calls. Write tests: SidePanelShell.test.tsx (renders header + askPlaceholder input disabled; no send button — queryByRole('button', {name: /send/i}) null); StandaloneShell.test.tsx (renders openTitle header); routers: SidePanelRouter with onboarding pending shows Onboarding, with onboarding done shows SidePanelShell (mock stores via store.setState in test setup).
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/sidepanel tests/components/standalone -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    Both shells render per §17.1 with ErrorBoundary wrappers; routers switch on store state (onboarding vs shell); tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/components/sidepanel tests/components/standalone -x` exits 0
    - `grep -c "ErrorBoundary" src/components/sidepanel/SidePanelShell.tsx src/components/standalone/StandaloneShell.tsx` >= 2
    - `grep -rn "chrome\." src/components/ | wc -l` == 0 (components never call chrome APIs directly)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Onboarding (Flow 9) + CmdKPicker (Flow 10)</name>
  <files>
    src/components/onboarding/Onboarding.tsx, src/components/cmdk/CmdKPicker.tsx, tests/components/onboarding/Onboarding.test.tsx, tests/components/cmdk/CmdKPicker.test.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md Flow 9 (lines 1702-1706), Flow 10 (lines 1706-1710)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md D-06 (onboarding step 1 + Configure later)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (cmdk, onboarding, lines 145-155)
  </read_first>
  <action>
    Create src/components/onboarding/Onboarding.tsx per Flow 9 + D-06: renders step 1 — welcome (STR.onboarding.* from 01-02) + a provider-choice UI skeleton (four provider buttons disabled with a 'coming in settings phase' note; per D-06 the full provider configure flow lands in the settings phase) + a "Configure later" Button that marks onboarding done (writes via useWorkspaceStore/AddonSettingsStore — ONBOARDING_WRITE/ONBOARDING_DONE codes on error, never throw); wrapped in ErrorBoundary. Create src/components/cmdk/CmdKPicker.tsx per Flow 10: an antd Modal (or Dropdown) opened by isCmdK (01-04) captured at the shell level; renders an AutoComplete-style search Input over a static COMMANDS list (open-side-panel → WorkspaceRouter.openSidePanel, open-standalone → WorkspaceRouter.openStandalone, new-conversation → emits NEW_CONVERSATION via EventBus, no-op stub); Enter runs the highlighted command; Escape closes; wrapped in FocusTrap (01-04) + ErrorBoundary. Commands are a typed const array — no free-form execution. Write tests: Onboarding.test.tsx (renders welcome + provider skeleton; 'Configure later' flips onboarding-done in the store — assert router switches in the SidePanelRouter test); CmdKPicker.test.tsx (isCmdK opens; typing filters the list; Enter on open-standalone calls WorkspaceRouter.openStandalone via vi.mock; Escape closes and focus returns).
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/onboarding tests/components/cmdk -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    Onboarding implements Flow 9 step 1 with the D-06 'Configure later' escape; CmdKPicker implements Flow 10 with FocusTrap + router-backed commands; tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/components/onboarding tests/components/cmdk -x` exits 0
    - `grep -c "Configure later" src/components/onboarding/Onboarding.tsx` == 1
    - `grep -c "isCmdK\|WorkspaceRouter" src/components/cmdk/CmdKPicker.tsx` >= 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Page skeletons (§18 Phase 1)</name>
  <files>
    src/components/pages/sidepanel/ChatPageSkeleton.tsx, src/components/pages/standalone/WorkspacePageSkeleton.tsx
  </files>
  <read_first>
    .planning/PRODUCT_SPEC_v0_1.md §18 Phase 1 create list (lines 2420-2447)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (empty states)
  </read_first>
  <action>
    Create src/components/pages/sidepanel/ChatPageSkeleton.tsx per the §18 Phase 1 create list ('chat page skeleton'): renders the chat empty state — STR.chat.empty/loading text (01-02) in an antd Empty/Card with the MinimalMode-eligible placeholder per §2.4; no chat messages state, no send flow (chat lands in its phase); wrapped in ErrorBoundary. Create src/components/pages/standalone/WorkspacePageSkeleton.tsx per §18 ('workspace page skeleton'): renders the standalone workspace empty state (notes list placeholder + currentPageContext summary card reading useWorkspaceStore().workspace.currentPageContext — display only; the workspace pages themselves land in the workspace phase). Both components import only store/strings/components from prior plans — never content modules, never chrome APIs (Pitfall 4). No tests required here beyond the shell tests in Task 1 that render them — verify via tsc + the Task 1 test suite.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -5 && pnpm vitest run tests/components/sidepanel -x 2>&1 | tail -3</automated>
  </verify>
  <done>
    ChatPageSkeleton renders the chat empty state; WorkspacePageSkeleton renders workspace empty state with currentPageContext display; tsc + shell tests green.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "chat.empty\|chat.loading" src/components/pages/sidepanel/ChatPageSkeleton.tsx` >= 1
    - `grep -rn "chrome\." src/components/pages/ | wc -l` == 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → Cmd+K | Command selection is constrained to a typed list; no free-form execution |
| store state → UI | Onboarding gate decides which tree renders; stale/false state must not unlock restricted UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-18 | Elevation of Privilege | SidePanelRouter gate | medium | mitigate | Onboarding-done is read from the workspace store (single source, 01-06); router re-evaluates on store change — no cached UI flag |
| T-1-19 | Tampering (XSS via markdown) | ChatPageSkeleton/WorkspacePageSkeleton | medium | mitigate | Any future rendered content flows through PortableMarkdown (01-04); skeletons render STR constants only this phase |
| T-1-20 | Spoofing | CmdKPicker commands | low | accept | Static typed command array; execution delegates to WorkspaceRouter (01-06) which validates its own API surface |
</threat_model>

<verification>
- `pnpm vitest run tests/components -x` green
- `pnpm tsc --noEmit` green
- No chrome.* calls in components (grep-guarded)
</verification>

<success_criteria>
- Both shells render (chat-only §17.1), routers gate on onboarding state
- Flow 9 + Flow 10 implemented with D-06 semantics
- §18 page skeletons in place for the chat/workspace phases
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-08-SUMMARY.md` when done
</output>
