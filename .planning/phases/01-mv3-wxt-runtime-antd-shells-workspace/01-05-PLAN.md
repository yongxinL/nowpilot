---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-04]
files_modified:
  - src/core/theme/ThemeStore.ts
  - src/core/theme/antdConfig.ts
  - src/core/registry/ThemePackRegistry.ts
  - src/core/theme/themePacks.ts
  - tests/core/theme/ThemeStore.test.ts
  - tests/core/theme/ThemePackRegistry.test.ts
autonomous: true
requirements: [RUNTIME-04, WSPC-04]
must_haves:
  truths:
    - "ThemeStore is a zustand store whose canonical theme source is chrome.storage.local keys np_theme (displayMode) + np_theme_pack (themePack) (D-13) — NOT zustand persist (Pitfall 7: persist is not cross-surface-safe)."
    - "ThemeStore initializes from chrome.storage.local on init (THEME_INIT), writes through setMode to np_theme and setPack to np_theme_pack (THEME_WRITE), subscribes to chrome.storage.onChanged for both keys (THEME_ON_CHANGED), and follows the OS scheme when mode is 'auto' via window.matchMedia (THEME_MATCH_MEDIA)."
    - "ThemePackRegistry (src/core/registry/ThemePackRegistry.ts) is a generic Map-based registry (REGISTRY_INIT) with a ready flag per D-12: the default pack registers ready; liquid-glass/claude-warm register as not-ready — packs register via register(), resolve via get(), list via list(), readiness via isReady()."
    - "Every error path calls debugLog with the canonical code (THEME_* / REGISTRY_INIT) per Golden Rule 9; no empty catches."
    - "antdConfig derives the antd ConfigProvider theme (algorithm, token overrides) from themePack + displayMode via getAntdConfig({ mode, pack, compact }) — one config per surface, no hardcoded theme values in components."
    - "themePacks.ts ships the default pack (ready) plus liquid-glass and claude-warm (registered not-ready, D-12) matching the UI-SPEC design tokens (colorPrimary #3B82F6, borderRadius, fontFamily); DisplayMode = 'light' | 'dark' | 'auto' (D-11) is a SEPARATE axis from ThemePack = 'default' | 'liquid-glass' | 'claude-warm' (W-9 — no 'system' pack id); 'auto' maps to the OS scheme via matchMedia."
  artifacts:
    - src/core/theme/ThemeStore.ts
    - src/core/theme/antdConfig.ts
    - src/core/theme/themePacks.ts
    - src/core/registry/ThemePackRegistry.ts
    - tests/core/theme/ThemeStore.test.ts
    - tests/core/theme/ThemePackRegistry.test.ts
  key_links:
    - "ThemeStore is the ONLY writer/reader of np_theme in chrome.storage.local — surfaces (01-08/01-09) subscribe here; the registry provides the pack lookup."
    - "antdConfig is consumed by XProvider mounting in 01-09 (one ConfigProvider per surface, Appendix F)."
  flagged_assumptions:
    - statement: "RUNTIME-04 theme — chrome.storage.local verified in tests via wxt fakeBrowser storage; real cross-surface propagation is covered by the chrome.storage.onChanged wiring tested here, not a real browser."
      verification: flagged-unverified
    - statement: "WSPC-04 registry — ThemePackRegistry is the first registry instance; AddonRegistry/PageRegistry (01-07) reuse the same shape (idempotent re-registration, concurrent-safe Map ops)."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Build the theme subsystem: ThemeStore (zustand + chrome.storage.local keys np_theme/np_theme_pack, D-13), the theme pack registry (first WSPC-04 registry instance, D-12 ready flag), the default pack (ready) + liquid-glass/claude-warm (registered not-ready), and the antd ConfigProvider theme derivation.

Purpose: Both surfaces (side panel, standalone) must render the same theme from a single canonical source; later shells (01-08) and mounts (01-09) consume it. Pitfall 7 is avoided by design.
Output: 4 source files + 2 test files.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-13 theme via storage.local)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 7, zustand ^5)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (core/theme, core/registry, lines 147-158)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (antd design tokens section)

# Depends on 01-02 (STR, types) and 01-04 (debugLog + error codes). No prior SUMMARYs.
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | No sidePanel code — delegated to 01-06 |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | No surface imports; theme core stays dependency-free except zustand |
| P5 | Message contracts bypassing RuntimeEnvelope | No messaging here |
| P6 | matchMedia-in-jsdom | Handled in 01-01 (setup.ts polyfill) |
| P7 | zustand persist for cross-surface | **GUARDED HERE** — ThemeStore uses chrome.storage.local + storage.onChanged, NOT zustand persist middleware |

<tasks>

<task type="auto">
  <name>Task 1: themePacks + ThemePackRegistry (first registry instance)</name>
  <files>
    src/core/theme/themePacks.ts, src/core/registry/ThemePackRegistry.ts, tests/core/theme/ThemePackRegistry.test.ts
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (antd design tokens)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (core/registry, lines 147-158)
  </read_first>
  <action>
    Create src/core/theme/themePacks.ts exporting type ThemePack = 'default' | 'liquid-glass' | 'claude-warm' and type DisplayMode = 'light' | 'dark' | 'auto' (D-11 — display mode is a separate axis from theme pack, W-9: ThemeId must NOT be 'light'|'dark'|'system'), interface ThemePackDef { id: ThemePack; label: string; ready: boolean; algorithm: 'default' | 'darkAlgorithm'; tokens: { colorPrimary: string; borderRadius: number; fontFamily: string } }, and const THEME_PACKS: Record<ThemePack, ThemePackDef> with default (ready: true, algorithm 'default', colorPrimary per UI-SPEC primary token #3B82F6), liquid-glass (ready: false — D-12 registered but not implemented; token overlay colorBgContainer 'rgba(255,255,255,0.68)' per PATTERNS PACK_TOKEN_OVERLAY), and claude-warm (ready: false — D-12; token overlay colorBgBase '#FAF7F2'). DisplayMode 'auto' has no pack entry — it resolves via matchMedia at ThemeStore level (D-11). Do NOT import antd's theme algorithm strings here — use string literals 'default' | 'darkAlgorithm' so antdConfig maps them (keeps themePacks antd-import-free for testability). Create src/core/registry/ThemePackRegistry.ts as the first WSPC-04 registry instance: class ThemePackRegistry { private packs = new Map<ThemePack, ThemePackDef>(); register(pack): void — idempotent (re-registering same id replaces, logs debugLog REGISTRY_INIT with silent:true on replace); registerAll(packs): void; get(id): ThemePackDef | undefined; list(): ThemePackDef[]; has(id): boolean; isReady(id): boolean — returns the pack's ready flag (D-12: default true, liquid-glass/claude-warm false) } — all ops are synchronous Map ops, concurrency-safe by construction (no async races; callers may call from any context). Export a singleton getThemePackRegistry(); the singleton is pre-registered with THEME_PACKS so readiness is known from startup (D-12). Errors: register with an unknown/invalid id shape → debugLog(REGISTRY_INIT, ...) and skip; never throws (Golden Rule 9). Write tests/core/theme/ThemePackRegistry.test.ts: register+get roundtrip; re-register same id is idempotent (get returns the new pack); has() true/false; isReady() reflects D-12 readiness (default true, liquid-glass/claude-warm false); list() returns registered order; invalid id does not throw.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/theme/ThemePackRegistry.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    THEME_PACKS has default (ready) + liquid-glass/claude-warm (not-ready per D-12); DisplayMode/ThemePack axes split (W-9); ThemePackRegistry supports register (idempotent)/get/list/has/isReady with singleton; tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/theme/ThemePackRegistry.test.ts` exits 0
    - `grep -c "darkAlgorithm\|colorPrimary" src/core/theme/themePacks.ts` >= 2
    - `grep -c "registerAll\|getThemePackRegistry" src/core/registry/ThemePackRegistry.ts` == 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ThemeStore (zustand + chrome.storage.local, D-13)</name>
  <files>
    src/core/theme/ThemeStore.ts, tests/core/theme/ThemeStore.test.ts
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md D-13 (theme via chrome.storage.local.np_theme)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 7 lines 388-392; zustand ^5)
  </read_first>
  <action>
    Create src/core/theme/ThemeStore.ts as a zustand store (create from 'zustand', v5 API) with state { pack: ThemePack; mode: DisplayMode; resolved: 'light'|'dark'; isReady: boolean } and actions { init(): Promise<void>; setMode(mode: DisplayMode): Promise<void>; setPack(pack: ThemePack): Promise<void>; getResolved(): 'light'|'dark' }. Contract per D-13/D-14: (a) init() reads chrome.storage.local keys np_theme (displayMode, fallback 'auto') and np_theme_pack (themePack, fallback 'default'), sets state, subscribes chrome.storage.onChanged once for both keys (foreign-write → update state, debugLog THEME_ON_CHANGED with silent:true) and window.matchMedia('(prefers-color-scheme: dark)') change listener when mode is 'auto' (debugLog THEME_MATCH_MEDIA); errors → debugLog(THEME_INIT, ...) and fall back to 'auto'/'default', never throw. (b) setMode writes chrome.storage.local np_theme (debugLog THEME_WRITE on error) then updates state; setPack writes np_theme_pack; no zustand persist middleware (Pitfall 7). (c) getResolved() returns 'dark' when mode==='dark' or (mode==='auto' and matchMedia dark matches). Export singleton useThemeStore(). D-14: the Phase 1 UI exposes displayMode selection only — setPack exists for the theme-pack architecture (D-10) but no UI calls it until a second pack becomes ready. Import debugLog + THEME_* codes from 01-04; import ThemePack/DisplayMode from themePacks. Write tests/core/theme/ThemeStore.test.ts using wxt fakeBrowser storage + the 01-01 matchMedia polyfill: init with no stored values defaults 'auto'/'default'; init with np_theme='dark' resolves dark; setMode('light') writes chrome.storage.local.np_theme and updates state; setPack('liquid-glass') writes np_theme_pack; chrome.storage.onChanged foreign write updates state; auto + polyfilled dark matchMedia → getResolved() 'dark'.
<!-- planner-discipline-allow: persist -->
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/theme/ThemeStore.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    ThemeStore reads/writes np_theme + np_theme_pack via chrome.storage.local (never zustand persist); subscribes onChanged + matchMedia; init/setMode/setPack/getResolved tested green with fakeBrowser; setPack exists but no UI calls it (D-14).
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/theme/ThemeStore.test.ts` exits 0
    - `grep -c "np_theme_pack\|onChanged" src/core/theme/ThemeStore.ts` >= 2
    - `grep -c "persist" src/core/theme/ThemeStore.ts` == 0 (Pitfall 7 guard)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: antdConfig (theme → ConfigProvider derivation)</name>
  <files>
    src/core/theme/antdConfig.ts
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (theme antdConfig, lines 147-158)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (design tokens)
  </read_first>
  <action>
    Create src/core/theme/antdConfig.ts per Appendix F.2/PATTERNS (canonical): export getAntdConfig(opts: { mode: DisplayMode; pack: ThemePack; compact: boolean }): ConfigProviderProps — derives isDark internally (opts.mode === 'dark' || (opts.mode === 'auto' && typeof window !== 'undefined' && window.matchMedia('(prefers-color-scheme: dark)').matches) — Pitfall 6 guard), builds the algorithm array [isDark ? theme.darkAlgorithm : theme.defaultAlgorithm, ...(opts.compact ? [theme.compactAlgorithm] : [])] (import { theme } from 'antd'; compact only on the side panel per RUNTIME-04), spreads PACK_TOKEN_OVERLAY[opts.pack] token overrides (default: {}, liquid-glass: { colorBgContainer: 'rgba(255,255,255,0.68)' }, claude-warm: { colorBgBase: '#FAF7F2' } — PATTERNS lines 5164-5168) on the UI-SPEC seed tokens (colorPrimary #3B82F6, borderRadius 8, fontFamily system stack), and returns { locale: enUS, theme: { algorithm, token } }. If the pack id is missing from the registry, fall back to the default pack tokens (debugLog REGISTRY_INIT silent:true). No component imports — pure function consumed by the XProvider mounts in 01-09 (one ConfigProvider per surface, Appendix F; getAntdConfig result spreads into XProvider). Keep this file free of React — only antd theme import allowed.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    getAntdConfig maps mode+pack → antd ConfigProviderProps (algorithm array incl. compactAlgorithm; PACK_TOKEN_OVERLAY spread); tsc passes; no React import.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "getAntdConfig\|defaultAlgorithm\|darkAlgorithm\|compactAlgorithm" src/core/theme/antdConfig.ts` >= 3
    - `grep -rn "from 'react'" src/core/theme/ | wc -l` == 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → ThemeStore | Storage content is extension-controlled; foreign writers (another surface) are first-party but must propagate atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-10 | Tampering | ThemeStore/chrome.storage | medium | mitigate | np_theme/np_theme_pack are read-validated against DisplayMode/ThemePack on every load; unknown values fall back to 'auto'/'default' (never trust raw storage); onChanged propagates the same validated value |
| T-1-11 | Denial of Service | ThemeStore init loop | low | accept | onChanged listener registered once (guard flag); self-writes are indistinguishable from foreign writes but are idempotent state updates — low impact |
</threat_model>

<verification>
- `pnpm vitest run tests/core/theme` green
- `pnpm tsc --noEmit` green
- Pitfall 7 guard grep: no zustand persist in ThemeStore
</verification>

<success_criteria>
- ThemeStore is the single np_theme source (D-13), tested against fakeBrowser storage + onChanged
- First WSPC-04 registry instance (ThemePackRegistry) reusable by 01-07
- antdConfig ready for the 01-09 mounts
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-05-SUMMARY.md` when done
</output>
