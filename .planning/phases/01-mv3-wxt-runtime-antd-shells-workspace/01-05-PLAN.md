---
phase: 01-mv3-wxt-runtime-antd-shells-workspace
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-04]
files_modified:
  - src/core/theme/ThemeStore.ts
  - src/core/theme/antdConfig.ts
  - src/core/registry/ThemePackRegistry.ts
  - src/core/theme/themePacks.ts
  - tests/core/theme/ThemeStore.test.ts
  - tests/core/theme/ThemePackRegistry.test.ts
autonomous: true
requirements: [RUNTIME-04, WSPC-04]
must_haves:
  truths:
    - "ThemeStore is a zustand store whose canonical theme source is chrome.storage.local key np_theme (D-13) — NOT zustand persist (Pitfall 7: persist is not cross-surface-safe)."
    - "ThemeStore initializes from chrome.storage.local on init (THEME_INIT), writes through setTheme to chrome.storage.local (THEME_WRITE), subscribes to chrome.storage.onChanged for np_theme (THEME_ON_CHANGED), and follows the OS scheme when mode is 'system' via window.matchMedia (THEME_MATCH_MEDIA)."
    - "ThemePackRegistry (src/core/registry/ThemePackRegistry.ts) is a generic Map-based registry (REGISTRY_INIT) — theme packs are registered via register(), resolved via get(), listed via list()."
    - "Every error path calls debugLog with the canonical code (THEME_* / REGISTRY_INIT) per Golden Rule 9; no empty catches."
    - "antdConfig derives the antd ConfigProvider theme (algorithm, token overrides) from the current theme pack — one config per surface, no hardcoded theme values in components."
    - "themePacks.ts ships at least the light + dark built-in packs matching the UI-SPEC §antd design tokens (colorPrimary, borderRadius, fontFamily), and 'system' is a valid theme id (maps to OS scheme)."
  artifacts:
    - src/core/theme/ThemeStore.ts
    - src/core/theme/antdConfig.ts
    - src/core/theme/themePacks.ts
    - src/core/registry/ThemePackRegistry.ts
    - tests/core/theme/ThemeStore.test.ts
    - tests/core/theme/ThemePackRegistry.test.ts
  key_links:
    - "ThemeStore is the ONLY writer/reader of np_theme in chrome.storage.local — surfaces (01-08/01-09) subscribe here; the registry provides the pack lookup."
    - "antdConfig is consumed by XProvider mounting in 01-09 (one ConfigProvider per surface, Appendix F)."
  flagged_assumptions:
    - statement: "RUNTIME-04 theme — chrome.storage.local verified in tests via wxt fakeBrowser storage; real cross-surface propagation is covered by the chrome.storage.onChanged wiring tested here, not a real browser."
      verification: flagged-unverified
    - statement: "WSPC-04 registry — ThemePackRegistry is the first registry instance; AddonRegistry/PageRegistry (01-07) reuse the same shape (idempotent re-registration, concurrent-safe Map ops)."
      verification: flagged-unverified
prohibitions: []
---

<objective>
Build the theme subsystem: ThemeStore (zustand + chrome.storage.local, D-13), the theme pack registry (first WSPC-04 registry instance), built-in light/dark packs, and the antd ConfigProvider theme derivation.

Purpose: Both surfaces (side panel, standalone) must render the same theme from a single canonical source; later shells (01-08) and mounts (01-09) consume it. Pitfall 7 is avoided by design.
Output: 4 source files + 2 test files.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md (D-13 theme via storage.local)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 7, zustand ^5)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (core/theme, core/registry, lines 147-158)
@.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (antd design tokens section)

# Depends on 01-02 (STR, types) and 01-04 (debugLog + error codes). No prior SUMMARYs.
</context>

## Research Pitfall Guards (all 7; owning plans in parentheses)

| # | Pitfall | Guard in THIS plan |
|---|---------|--------------------|
| P1 | sidePanel.open user-gesture | No sidePanel code — delegated to 01-06 |
| P2 | wxt 0.21 drift | Handled in 01-01 |
| P3 | @wxt-dev/testing does not exist | Handled in 01-01 |
| P4 | content bundle smuggling | No surface imports; theme core stays dependency-free except zustand |
| P5 | Message contracts bypassing RuntimeEnvelope | No messaging here |
| P6 | matchMedia-in-jsdom | Handled in 01-01 (setup.ts polyfill) |
| P7 | zustand persist for cross-surface | **GUARDED HERE** — ThemeStore uses chrome.storage.local + storage.onChanged, NOT zustand persist middleware |

<tasks>

<task type="auto">
  <name>Task 1: themePacks + ThemePackRegistry (first registry instance)</name>
  <files>
    src/core/theme/themePacks.ts, src/core/registry/ThemePackRegistry.ts, tests/core/theme/ThemePackRegistry.test.ts
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (antd design tokens)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (core/registry, lines 147-158)
  </read_first>
  <action>
    Create src/core/theme/themePacks.ts exporting type ThemeId = 'light' | 'dark' | 'system', interface ThemePack { id: ThemeId; label: string; algorithm: 'default' | 'darkAlgorithm'; tokens: { colorPrimary: string; borderRadius: number; fontFamily: string } }, and const THEME_PACKS: Record<ThemeId, ThemePack> with light (algorithm 'default', colorPrimary per UI-SPEC primary token), dark (algorithm 'darkAlgorithm', same colorPrimary), and system (resolves via matchMedia — no fixed algorithm; mark tokens from the light pack with a comment that OS scheme decides at resolution time). Do NOT import antd's theme algorithm strings here — use string literals 'default' | 'darkAlgorithm' so antdConfig maps them (keeps themePacks antd-import-free for testability). Create src/core/registry/ThemePackRegistry.ts as the first WSPC-04 registry instance: class ThemePackRegistry { private packs = new Map<ThemeId, ThemePack>(); register(pack): void — idempotent (re-registering same id replaces, logs debugLog REGISTRY_INIT with silent:true on replace); registerAll(packs): void; get(id): ThemePack | undefined; list(): ThemePack[]; has(id): boolean } — all ops are synchronous Map ops, concurrency-safe by construction (no async races; callers may call from any context). Export a singleton getThemePackRegistry(). Errors: register with an unknown/invalid id shape → debugLog(REGISTRY_INIT, ...) and skip; never throws (Golden Rule 9). Write tests/core/theme/ThemePackRegistry.test.ts: register+get roundtrip; re-register same id is idempotent (get returns the new pack); has() true/false; list() returns registered order; invalid id does not throw.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/theme/ThemePackRegistry.test.ts -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    THEME_PACKS has light/dark/system; ThemePackRegistry supports register (idempotent)/get/list/has with singleton; tests green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/theme/ThemePackRegistry.test.ts -x` exits 0
    - `grep -c "darkAlgorithm\|colorPrimary" src/core/theme/themePacks.ts` >= 2
    - `grep -c "registerAll\|getThemePackRegistry" src/core/registry/ThemePackRegistry.ts` == 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ThemeStore (zustand + chrome.storage.local, D-13)</name>
  <files>
    src/core/theme/ThemeStore.ts, tests/core/theme/ThemeStore.test.ts
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-CONTEXT.md D-13 (theme via chrome.storage.local.np_theme)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-RESEARCH.md (Pitfall 7 lines 388-392; zustand ^5)
  </read_first>
  <action>
    Create src/core/theme/ThemeStore.ts as a zustand store (create from 'zustand', v5 API) with state { theme: ThemeId; resolved: 'light'|'dark'; isReady: boolean } and actions { init(): Promise<void>; setTheme(theme: ThemeId): Promise<void>; getResolved(): 'light'|'dark' }. Contract per D-13: (a) init() reads chrome.storage.local key np_theme (fallback 'system' when absent), sets state, subscribes chrome.storage.onChanged once for np_theme (foreign-write → update state, debugLog THEME_ON_CHANGED with silent:true) and window.matchMedia('(prefers-color-scheme: dark)') change listener when theme is 'system' (debugLog THEME_MATCH_MEDIA); errors → debugLog(THEME_INIT, ...) and fall back to 'system', never throw. (b) setTheme writes chrome.storage.local np_theme (debugLog THEME_WRITE on error) then updates state; no zustand persist middleware (Pitfall 7). (c) getResolved() returns 'dark' when theme==='dark' or (theme==='system' and matchMedia dark matches). Export singleton useThemeStore(). Import debugLog + THEME_* codes from 01-04; import ThemeId from themePacks. Write tests/core/theme/ThemeStore.test.ts using wxt fakeBrowser storage + the 01-01 matchMedia polyfill: init with no stored value defaults 'system'; init with np_theme='dark' resolves dark; setTheme('light') writes chrome.storage.local.np_theme and updates state; chrome.storage.onChanged foreign write updates state; system + polyfilled dark matchMedia → getResolved() 'dark'.
<!-- planner-discipline-allow: persist -->
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/theme/ThemeStore.test.ts -x 2>&1 | tail -5</automated>
  </verify>
  <done>
    ThemeStore reads/writes np_theme via chrome.storage.local (never zustand persist); subscribes onChanged + matchMedia; init/setTheme/getResolved tested green with fakeBrowser.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/theme/ThemeStore.test.ts -x` exits 0
    - `grep -c "np_theme\|onChanged" src/core/theme/ThemeStore.ts` >= 2
    - `grep -c "persist" src/core/theme/ThemeStore.ts` == 0 (Pitfall 7 guard)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: antdConfig (theme → ConfigProvider derivation)</name>
  <files>
    src/core/theme/antdConfig.ts
  </files>
  <read_first>
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-PATTERNS.md (theme antdConfig, lines 147-158)
    .planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-UI-SPEC.md (design tokens)
  </read_first>
  <action>
    Create src/core/theme/antdConfig.ts exporting buildAntdTheme(themeId: ThemeId, resolved: 'light'|'dark'): ThemeConfig where ThemeConfig is antd's ConfigProvider theme object: maps algorithm from the resolved pack ('default' → antd theme.defaultAlgorithm, 'darkAlgorithm' → theme.darkAlgorithm — import { theme } from 'antd'), spreads the pack tokens as token overrides. Also export getAntdConfig(store: { theme: ThemeId; resolved: 'light'|'dark' }) convenience that derives and caches per (themeId, resolved) tuple. No component imports — pure function consumed by the XProvider mounts in 01-09 (one ConfigProvider per surface, Appendix F). If the pack id is missing from the registry, fall back to the light pack tokens (debugLog REGISTRY_INIT silent:true). Keep this file free of React — only antd theme import allowed.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    buildAntdTheme maps packs → antd ThemeConfig (algorithm + tokens); getAntdConfig caches per tuple; tsc passes; no React import.
  </done>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0
    - `grep -c "buildAntdTheme\|defaultAlgorithm\|darkAlgorithm" src/core/theme/antdConfig.ts` >= 2
    - `grep -rn "from 'react'" src/core/theme/ | wc -l` == 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → ThemeStore | Storage content is extension-controlled; foreign writers (another surface) are first-party but must propagate atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-10 | Tampering | ThemeStore/chrome.storage | medium | mitigate | np_theme is read-validated against ThemeId on every load; unknown values fall back to 'system' (never trust raw storage); onChanged propagates the same validated value |
| T-1-11 | Denial of Service | ThemeStore init loop | low | accept | onChanged listener registered once (guard flag); self-writes are indistinguishable from foreign writes but are idempotent state updates — low impact |
</threat_model>

<verification>
- `pnpm vitest run tests/core/theme -x` green
- `pnpm tsc --noEmit` green
- Pitfall 7 guard grep: no zustand persist in ThemeStore
</verification>

<success_criteria>
- ThemeStore is the single np_theme source (D-13), tested against fakeBrowser storage + onChanged
- First WSPC-04 registry instance (ThemePackRegistry) reusable by 01-07
- antdConfig ready for the 01-09 mounts
</success_criteria>

<output>
Create `.planning/phases/01-mv3-wxt-runtime-antd-shells-workspace/01-05-SUMMARY.md` when done
</output>
