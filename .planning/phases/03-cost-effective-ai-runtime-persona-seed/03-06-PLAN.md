---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 06
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05]
files_modified:
  - src/core/ai/AgentOrchestrator.ts
  - tests/core/ai/AgentOrchestrator.test.ts
autonomous: true
requirements: [RICH-R-09]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "runAgentTurn implements the Appendix I bounded loop: Planner → (run_tool → Executor) → Renderer, enforcing §1.4 tier caps (plannerCap/toolCap) — it is the ONLY module that enforces caps and the only caller of PlannerService (Appendix I rule)"
    - "Planner uses the fast tier per-stage (D-55 useTierForStage-style mapping: planner fast, renderer fast for the final stream, executor tool calls default to the turn's tier)"
    - "When TierResolver returns null for a stage tier (D-54a), the orchestrator surfaces a configuration-required state and starts NO provider request"
    - "The turn-end persist seam is declared: runAgentTurn accepts an optional persistTurn callback invoked exactly once per completed turn (D-45 — never per chunk); abort mid-stream → partial dropped, persistTurn NOT invoked"
    - "Every stage system prompt comes from PromptCacheManager.buildSystemPrompt (D-59) — chat and agent share the same persona (RICH-R-09, DONE-when 4)"
  artifacts:
    - path: "src/core/ai/AgentOrchestrator.ts"
      provides: "Appendix I runAgentTurn(input) → AgentTurnOutput { streamedText, toolResults, reasonCode }; tier-cap enforcement; per-stage tier resolution; route via ProviderRouter; persistTurn seam"
      exports: ["runAgentTurn", "AgentTurnInput", "AgentTurnOutput", "AgentTier", "CONFIG_REQUIRED"]
  key_links:
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/PlannerService.ts"
      via: "the orchestrator is the ONLY caller of PlannerService.plan (Appendix I — no component or hook may call it directly)"
      pattern: "PlannerService\\.plan"
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/PromptCacheManager.ts"
      via: "every stage's systemPrompt comes from buildSystemPrompt(stage, {prefs}) — the persona choke-point (D-59)"
      pattern: "buildSystemPrompt"
    - from: "src/core/ai/AgentOrchestrator.ts"
      to: "src/core/ai/ProviderRouter.ts"
      via: "route() resolves the provider for each stage call (tier → providerId/model)"
      pattern: "ProviderRouter|route\\("
  prohibitions:
    - "No component or hook may call PlannerService directly — AgentOrchestrator is the single entry (Appendix I; grep-verified)"
    - "No provider request when a stage tier is unresolved (D-54a) — CONFIG_REQUIRED is surfaced instead of a guessed call"
    - "No per-chunk persistence — the persist seam runs at turn end only (D-45)"
flagged_assumptions:
  - "RICH-R-09 (unclassified probe — resolved here): chat and agent share the persona because chat (useChatStreaming, 03-07) re-points at runAgentTurn, which builds every stage prompt via the D-59 choke-point; the AgentOrchestrator test asserts persona presence in all three stage prompts of one turn"
  - "D-45 seam: the persistTurn callback is wired to the journaled ChatHistoryDB append in plan 03-07; this plan declares and tests the seam contract (called once at turn end, never per chunk, not on abort)"
---

<objective>
Assemble the pipeline into the Appendix I `AgentOrchestrator` — the bounded Planner → Executor → Renderer loop with §1.4 tier caps, per-stage tier resolution (D-55), persona-consistent stage prompts via the D-59 choke-point (RICH-R-09), the D-54a configuration-required state, and the D-45 turn-end persist seam.

Purpose: This is the module every AI call in the phase flows through — chat (03-07 re-points useChatStreaming here) and agent alike. Proving the loop with tier caps, abort, ask_clarification, TOOL_REJECTED, and persona consistency in one test file closes the phase's DONE-when loop.
Output: `AgentOrchestrator.ts` + `AgentOrchestrator.test.ts`.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §1.2 (ask_clarification note), §1.4 (tier caps table), Appendix I (5567-5615 — verbatim loop)
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md  # Pattern 4 architecture diagram, D-44/D-45/D-54a/D-55/D-59, Appendix I abridged code
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md  # AgentOrchestrator section + WriteJournal runJournaled analog
@src/core/ai/PlannerService.ts  # created 03-01 — plan() contract
@src/core/ai/ExecutorService.ts  # created 03-04 — execute() contract
@src/core/ai/RendererService.ts  # created 03-04 — render() contract
@src/core/ai/PromptCacheManager.ts  # created 03-04 — buildSystemPrompt contract
@src/core/ai/TierResolver.ts  # created 03-05 — resolveTier null contract
@src/core/ai/ProviderRouter.ts  # created 03-05 — route() contract
</context>

<tasks>

<task type="auto">
  <name>Task 1: AgentOrchestrator — Appendix I loop + per-stage tiers + config-required + persist seam</name>
  <files>src/core/ai/AgentOrchestrator.ts</files>
  <action>Create `src/core/ai/AgentOrchestrator.ts` per Appendix I verbatim (spec 5567-5615). Export `AgentTier { plannerCap, toolCap, modelTier }` (from the §1.4 tier-caps table), `AgentTurnInput { userInput, sessionId, operationId, tier, prefs?, abortSignal, persistTurn? }`, `AgentTurnOutput { streamedText, toolResults, reasonCode }`, and the string constant `CONFIG_REQUIRED` for the D-54a state. `runAgentTurn(input)` implements the bounded loop exactly: check abort → check plannerCap → `PlannerService.plan(...)` (the ONLY call site in the codebase) → if decision.action is 'answer' or 'ask_clarification', finish with the reasonCode; else (run_tool) check toolCap → `ExecutorService.execute(...)` (zero tools registered → TOOL_REJECTED surfaces, D-46) → push ToolExecutionResult → loop. Per-stage tier (D-55): planner uses fast (spec §1.2), renderer uses fast for the final answer stream, executor tool calls default to the turn's modelTier. For each stage: resolve the tier via TierResolver (TierResolver.resolveTier); if null → return `CONFIG_REQUIRED` state (D-54a — no provider request, no guessing); else route via ProviderRouter.route and pass the resolved provider into the stage. Every stage systemPrompt = `PromptCacheManager.buildSystemPrompt(stage, { prefs })` (D-59 — no other assembly path). finish(reasonCode): `RendererService.render(...)` → assemble AgentTurnOutput. PERSIST SEAM (D-45): if input.persistTurn is provided, invoke it exactly once after a completed turn (user message + assistant streamedText); never inside the delta path; if aborted mid-stream, do NOT invoke it (partial dropped). Abort: `if (input.abortSignal.aborted) throw new DOMException('aborted', 'AbortError')` per Appendix I. Instrument via debugLog. Run `pnpm run verify:phase-3`.</action>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>AgentOrchestrator compiles strict-clean; PlannerService.plan has exactly one call site (grep-verifiable); CONFIG_REQUIRED path starts no provider request; persistTurn invoked once per completed turn.</done>
</task>

<task type="auto">
  <name>Task 2: AgentOrchestrator.test.ts — tier caps, abort, ask_clarification, TOOL_REJECTED, persona consistency, persist seam</name>
  <files>tests/core/ai/AgentOrchestrator.test.ts</files>
  <action>Write `tests/core/ai/AgentOrchestrator.test.ts` using fixture providers from tests/core/ai/fixtures (D-48) and REAL stage services from src/core/ai (Planner/Executor/Renderer are real — only the provider is a fixture; the orchestrator must not need mocks for its own stages). Case groups: (a) happy path — answer decision → AgentTurnOutput with streamedText from the renderer; (b) planner_cap_reached — a fixture planner loop returning run_tool decisions exhausts plannerCap → reasonCode 'planner_cap_reached' (§1.4); (c) ask_clarification — decision.action 'ask_clarification' finishes with that reasonCode and its question/options surface (RICH-C substrate); (d) TOOL_REJECTED — fixture planner returns run_tool with any toolName → ExecutorService rejects → the rejection surfaces in toolResults, loop continues per Appendix I (zero tools, D-46); (e) abort — abortSignal aborted mid-turn → DOMException AbortError propagates; persistTurn NOT invoked; (f) PERSIST SEAM (D-45) — a spy persistTurn is invoked exactly once at turn end with the user message + streamedText, never per delta; (g) PERSONA CONSISTENCY (RICH-R-09 / DONE-when 4) — assert the systemPrompt captured for planner, executor, and renderer stage calls all contain the persona block prefix (byte-stable per stage; the persona name 'NowPilot' appears in all three, prepended FIRST); (h) CONFIG_REQUIRED — TierResolver fixture returning null for fast → runAgentTurn returns the CONFIG_REQUIRED state and no provider fixture was called (D-54a). Run `pnpm run verify:phase-3`.</action>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>AgentOrchestrator.test.ts green (8 case groups); persona block asserted in all three stage prompts of one turn; persist spy called once at turn end and never on abort.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentOrchestrator ← user input | Chat/agent user text enters the loop here — carried into PlannerService as the USER INPUT section (no host-page data in Phase 3) |
| AgentOrchestrator → persist seam | Completed turns cross into ChatHistoryDB via the journaled append — the write-rate boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-18 | DoS | unbounded loop | medium | mitigate | Appendix I loop is hard-capped by §1.4 plannerCap/toolCap (planner_cap_reached / tool_cap_reached terminal states) — the ONLY cap-enforcement point in the phase; test (b) asserts the cap |
| T-3-19 | Tampering | config-required bypass | high | mitigate | D-54a enforced in-code: resolveTier null → CONFIG_REQUIRED, no provider request (prohibition + test (h)); no inference path exists |
| T-3-20 | Information Disclosure | persist seam write-rate | medium | mitigate | persistTurn invoked once per completed turn at turn end (D-45); never inside the delta path; abort drops partial — kills the P2 write-rate risk; journaled append (03-07) is replay-safe |
| T-3-21 | Tampering | abort handling | low | mitigate | AbortError propagation is the first check in the loop (Appendix I); renderer/persist both observe the signal — a cancelled turn cannot persist a partial |
</threat_model>

<verification>
- `pnpm run verify:phase-3` green after every task commit.
- Grep guard: exactly one `PlannerService.plan` call site in src/ (the Appendix I rule).
- The AgentOrchestrator test is the RICH-R-09 gate (persona in all three stage prompts) and the D-45 gate (persist seam).
</verification>

<success_criteria>
1. Bounded Planner → Executor → Renderer loop runs end-to-end with real stages + fixture providers (Appendix I).
2. Tier caps enforced; ask_clarification finishes cleanly; TOOL_REJECTED surfaces through the loop (DONE-when 1).
3. CONFIG_REQUIRED state replaces any guessed provider call (D-54a).
4. Persona block present in planner/executor/renderer system prompts of one turn (RICH-R-09).
5. persistTurn seam: once per completed turn, never per chunk, not on abort (D-45).
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-3 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/ai/AgentOrchestrator.ts | runAgentTurn, AgentTurnInput, AgentTurnOutput, AgentTier, CONFIG_REQUIRED | **[this plan]** |
| src/core/ai/types.ts / ILLMProvider.ts / StreamAdapter.ts / StructuredOutput.ts / PlannerService.ts / prompts/index.ts | spine | 03-01 |
| src/core/ai/persona/*, UserPreferences.ts | persona foundation | 03-02 |
| src/core/ai/providers/*.ts | 5 providers | 03-03 |
| src/core/ai/PromptCacheManager.ts / PromptCacheAdapter.ts / ChunkBuffer.ts / toolSchemas.ts / ExecutorService.ts / RendererService.ts | prompt assembly + stages | 03-04 |
| src/core/runtime/workerState.ts | + ActiveStreamState | 03-04 |
| src/core/ai/ProviderRegistry.ts / TierResolver.ts / ProviderRouter.ts | routing stack (consumed here) | 03-05 |
| src/types/storage.ts / useChatStreaming.ts / OptionsPage.tsx | persistence + chat + options (consumes runAgentTurn) | 03-07 |
| tests/core/ai/** | test files + fixtures/ | all plans |