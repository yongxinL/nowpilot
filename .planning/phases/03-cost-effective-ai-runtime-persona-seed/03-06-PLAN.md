---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 06
type: execute
wave: 4
depends_on: ["03-03", "03-04", "03-05"]
files_modified:
  - src/core/ai/RendererService.ts
  - src/core/ai/AgentOrchestrator.ts
  - tests/core/ai/RendererService.test.ts
  - tests/core/ai/AgentOrchestrator.test.ts
autonomous: true
requirements: [AI-02, AI-03, AI-04]

must_haves:
  truths:
    - "RendererService.render streams via StreamAdapter (Seam 3): text deltas flow to the caller's onDelta callback live, then the accumulated final text is returned; streamText is consumed ONLY inside RendererService/StreamAdapter (AI-SPEC rule)."
    - "Streaming honesty (Pitfall 5): a render ending with finishReason !== 'stop' or an error chunk produces a failed terminal — never a silently-truncated 'complete' text; abortSignal threaded so cancel stops generation (no orphaned billing)."
    - "AgentOrchestrator.runAgentTurn is Appendix I VERBATIM (D-20): returns AgentTurnOutput { operationId, streamedText, toolResults, reasonCode } — NO trajectory states, NO CompletionEvidence, NO OutcomeVerifier (Phase 3a)."
    - "§1.4 tier caps are enforced ONLY by AgentOrchestrator: plannerCap/toolCap/mcpChaining arrive via AgentTurnInput.tier — which is VERBATIM the Appendix I caps shape { plannerCap: number; toolCap: number; mcpChaining: boolean } (spec lines 5527-5531), NOT ModelContextTier; the hook (03-08) populates that verbatim field via capsForTier(context.tier) (tiny 1/1, small 2/1, medium 3/2, large 5/3); cap exhaustion terminates with planner_cap_reached / tool_cap_reached."
    - "Every path terminates in a bounded terminal reasonCode: planner failure → deterministic 'planner_failed' fallback (no re-invocation), provider-unconfigured invocation resolution → 'provider_unconfigured' (no model call), abort → AbortError, caps → *_cap_reached, success → the planner's reasonCode or 'ask_clarification'."
    - "A healthy turn is exactly 2 model calls (one planner + one renderer) regardless of provider (AI-SPEC cost dimension)."
    - "Documented Phase-3 deviations from Appendix I (recorded in the §18 addendum by 03-09): runAgentTurn gains optional onStreamDelta?: (delta: string) => void and optional invocation?: StageResolver; the OUTPUT struct stays verbatim (D-20 intact). AgentTurnInput.tier is NOT a deviation — the spec's verbatim tier field is already the caps shape (plannerCap/toolCap/mcpChaining, lines 5527-5531); capsForTier is a hook-side helper that maps ModelContextTier → that verbatim shape, never a field-type change."
  artifacts:
    - src/core/ai/RendererService.ts
    - src/core/ai/AgentOrchestrator.ts
    - tests/core/ai/{RendererService,AgentOrchestrator}.test.ts
  key_links:
    - "runAgentTurn.finish() → RendererService.render({ ..., model, onDelta }) — the stream reaches the hook's ChunkBuffer via onStreamDelta (03-08)."
    - "The orchestrator resolves per-stage invocations via input.invocation (StageResolver from ProviderRouter 03-05) — PlannerService ctx + RendererService model."
    - "PlannerService/ExecutorService from 03-04 are the loop's stage services; onStreamDelta fixture asserts deltas arrive BEFORE completion."
  flagged_assumptions:
    - "AI-03 [empty]: Phase-3 turns are single-shot ([USER INPUT] only) — no conversation history sections; an empty user input is never sent (Sender disabled); empty stream (zero deltas) still terminates with done/error honestly."
    - "AI-03 [encoding]: chunk boundaries are SDK delta boundaries; text accumulates in arrival order; the renderer never re-orders or normalizes."
    - "AI-02 [concurrency]: runAgentTurn is sequential and single-surface; the loop is the only caller of PlannerService (no component/hook calls PlannerService directly — Appendix I Rule)."
  prohibitions:
    - "No trajectory states / CompletionEvidence / OutcomeVerifier in the orchestrator (D-20 — Phase 3a owns them)."
    - "No maxSteps / SDK loops (bounded while loop only)."
    - "No 'await result' destructuring (ai@4 not thenable)."
    - "No consuming textStream without awaiting a terminal promise (Pitfall 5)."
    - "No component/hook calling PlannerService directly (Appendix I Rule)."
    - "No rendering un-await-verified text as complete (streaming honesty)."
---

<objective>
The Renderer→stream half and the AgentOrchestrator loop: `RendererService.render` (Seam 3 streaming, `onDelta`), and `runAgentTurn` (Appendix I verbatim + the two documented Phase-3 deviations: `onStreamDelta` + `invocation`). AI-02's loop and AI-03's end-to-end streaming converge here; §1.4 caps are enforced only here (AI-04).

Purpose: A healthy turn must cost exactly 2 model calls, stream honestly (done XOR error), and terminate in every path — the AI-SPEC eval dimensions "Cost discipline", "Streaming honesty", and "Task completion / loop termination".
Output: RendererService + AgentOrchestrator + their §18 test files (spies assert exactly-2-calls; deltas-before-completion; terminal states).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/PRODUCT_SPEC_v0_1.md
@src/core/ai/types.ts
@src/core/ai/ProviderRouter.ts
@src/core/ai/StreamAdapter.ts
@src/core/ai/StructuredOutput.ts
@src/core/prompts/index.ts
@tests/fixtures/optimizedContext.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RendererService.render (Seam 3 streaming) + test</name>
  <files>src/core/ai/RendererService.ts, tests/core/ai/RendererService.test.ts</files>
  <read_first>
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Pattern 5 (streamTextToLLMChunks) + Open Question 3 (RendererService render output shape — resolution: option (a) accept onDelta, returned text is the accumulated final string) + Pitfall 2/Pitfall 5
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-AI-SPEC.md §4 Seam 3 (lines 438-471)
    - src/core/ai/StreamAdapter.ts (streamTextToLLMChunks)
    - src/core/ai/ProviderRouter.ts (StageInvocation — model + providerId)
    - src/core/prompts/index.ts (PROMPTS.renderer.system)
  </read_first>
  <behavior>
    - Test 1: render() with a model whose streamed text is 'Hello world' (injected-fetch mock) → onDelta receives incremental deltas, the returned text === 'Hello world', and the terminal chunk was 'done'.
    - Test 2: a mid-stream error (mock returns an error/aborted stream) → the render throws/rejects with STREAM_FAILED and the accumulated partial text is exactly the pre-failure deltas (never truncated-as-complete).
    - Test 3: abortSignal aborts mid-render → the stream cancels and the call rejects (AbortError) — no orphaned generation.
    - Test 4: the renderer prompt is composed from PROMPTS.renderer.system + the context sections (task/user_input), and maxTokens is 512 with maxRetries: 0.
  </behavior>
  <action>
    Create src/core/ai/RendererService.ts:
    - `export interface RenderInput { operationId: string; context: OptimizedContext; userInput: string; toolResults: ToolExecutionResult<unknown>[]; abortSignal: AbortSignal; invocation: StageInvocation; onDelta: (delta: string) => void }`.
    - `export const RendererService = { async render(input: RenderInput): Promise<{ text: string }> }` — composes the renderer system as the context's stable [SYSTEM] section (the byte-stable persona block built by contextHelper in 03-07) PREPENDED to PROMPTS.renderer.system — persona-first join `\`${personaBlock}\n\n${PROMPTS.renderer.system}\`` matching PersonaInjector.inject's order (N.2: persona first/cacheable, then canonical stage system) — and the task from the context sections + userInput; calls streamTextToLLMChunks({ model: input.invocation.model, system, task, maxTokens: 512, abortSignal }); forwards every { type: 'text' } chunk to onDelta; on { type: 'done' } returns the accumulated text; on { type: 'error' } (or finishReason !== 'stop') throws a typed error with code STREAM_FAILED (Golden Rule 9: debugLog with the canonical code + redacted extra); rethrows AbortError untouched (caller maps it). The renderer NEVER re-resolves the provider mid-stream — the invocation is fixed at finish() start (hasStreamedFirstToken freeze, §1.5).
    Header documents: streamText is consumed ONLY here (via StreamAdapter) — the AI-SPEC Seam-3 boundary; the persona block lives inside the context's stable [SYSTEM] section (03-07) so PROMPTS.renderer.system stays byte-stable.
    Create tests/core/ai/RendererService.test.ts per the behavior block (injected-fetch through getAISDKModel for a real stream, OR a StreamAdapter-stub — prefer the real path; fake timers not needed since ChunkBuffer is not in this seam). Assert finishReason 'stop' vs error paths per RESEARCH.
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/RendererService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass — deltas then done; mid-stream error keeps exactly the pre-failure text; abort rejects; maxTokens 512 + maxRetries: 0 asserted.
    - grep -c "streamText" src/core/ai/RendererService.ts >= 1 and grep -c "maxRetries: 0" src/core/ai/RendererService.ts >= 1.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Renderer streams honestly with onDelta; errors surface as failures, never truncated-as-complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentOrchestrator.runAgentTurn (Appendix I + deviations) + test</name>
  <files>src/core/ai/AgentOrchestrator.ts, tests/core/ai/AgentOrchestrator.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md Appendix I (line ~5513 — AgentTurnInput lines 5522-5532 where tier is the VERBATIM caps shape { plannerCap, toolCap, mcpChaining }, AgentTurnOutput, runAgentTurn VERBATIM + Rules) + §1.4 (tier caps table)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md D-20 (pre-evidence verbatim) + D-02 (context helper, Phase-4 deletion target)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Open Question 1 (onStreamDelta seam — resolution: optional param, output struct verbatim) + AI-SPEC §5 "Task completion / loop termination" dimension
    - src/core/ai/PlannerService.ts + ExecutorService.ts + RendererService.ts (the three stage services)
    - src/core/ai/ProviderRouter.ts (StageInvocation, StageResolver, isProviderUnconfiguredError)
    - src/core/ai/types.ts (OptimizedContext, ToolExecutionResult)
  </read_first>
  <behavior>
    - Test 1 (healthy): planner returns { action: 'answer', reasonCode: 'ok' } → exactly 2 service calls (PlannerService.plan spy + RendererService.render spy), output.reasonCode 'ok', streamedText from the renderer, toolResults [].
    - Test 2 (planner failure): PlannerService.plan throws → deterministic fallback: RendererService.render still runs with the failure context and reasonCode 'planner_failed' (NO second planner call — fetch count 1 for planning).
    - Test 3 (run_tool): planner returns { action: 'run_tool', toolName: 'get-provider-info' } → ExecutorService.execute called, toolResults length 1, then render.
    - Test 4 (caps): planner keeps returning run_tool → at plannerCap the loop terminates with reasonCode 'planner_cap_reached' (never exceeding the cap); at toolCap → 'tool_cap_reached'.
    - Test 5 (abort): abortSignal aborted before a call → throws DOMException AbortError.
    - Test 6 (streaming seam): a recorder onStreamDelta collects deltas and asserts at least one delta arrives BEFORE runAgentTurn resolves (deltas-before-completion fixture).
    - Test 7 (ask_clarification): planner returns ask_clarification → reasonCode 'ask_clarification', renderer still produces a final text.
    - Test 8 (provider unconfigured): the invocation resolver throws the typed unconfigured marker (isProviderUnconfiguredError narrows true — 03-05) → runAgentTurn returns the terminal { operationId, streamedText: '', toolResults: [], reasonCode: 'provider_unconfigured' } with ZERO model calls (no PlannerService.plan, no RendererService.render — fetch count 0); any OTHER invocation-resolution error is NOT swallowed (rethrows after debugLog).
  </behavior>
  <action>
    Create src/core/ai/AgentOrchestrator.ts — Appendix I VERBATIM body (the while loop, abort check, plannerCap/toolCap guards, finish()) with TWO documented optional deviations:
    - `AgentTurnInput` gains `onStreamDelta?: (delta: string) => void` and `invocation?: StageResolver` where `StageResolver = (tier: ModelTier) => Promise<StageInvocation>` (from 03-05). All verbatim fields unchanged — PINNED: `tier` is VERBATIM the caps shape `{ plannerCap: number; toolCap: number; mcpChaining: boolean }` (Appendix I lines 5527-5531), NOT a ModelContextTier field; the hook (03-08) populates it via capsForTier (below). The output struct `AgentTurnOutput { operationId, streamedText, toolResults, reasonCode }` stays verbatim (D-20 — no evidence machinery).
    - runAgentTurn: before PlannerService.plan, `const inv = await input.invocation('haiku')` → plan({ ..., callProviderJsonMode: inv.callProviderJsonMode, registeredToolNames }); finish() → `const renderInv = await input.invocation('flash')` → RendererService.render({ ..., invocation: renderInv, onDelta: (d) => { input.onStreamDelta?.(d); } }). When invocation is absent (unit tests), a default resolve stub may be injected by the test (the orchestrator NEVER imports ProviderRouter — it stays a pure Appendix-I orchestrator).
    - Provider-unconfigured terminal: wrap the first `await input.invocation(...)` — when `isProviderUnconfiguredError(err)` (03-05's typed marker, no uncanonicalized code) narrows true, return the terminal `{ operationId, streamedText: '', toolResults: [], reasonCode: 'provider_unconfigured' }` (NO model call — no plan, no render); any other invocation-resolution error is debugLogged (PLANNER_FAILED, redacted) and rethrown, never swallowed. Header notes this as the orchestrator-level mapping 03-05's design promises (terminal state, no model call).
    - Tier caps: `export function capsForTier(tier: ModelContextTier): { plannerCap: number; toolCap: number; mcpChaining: boolean }` — tiny 1/1/false, small 2/1/false, medium 3/2/true, large 5/3/true (§1.4 table); this helper maps the context tier → the VERBATIM AgentTurnInput.tier caps shape; the hook (03-08) calls capsForTier(context.tier) and passes the result as AgentTurnInput.tier (NOT a field-type deviation — see pinned contract above).
    - The stray-run_tool guard: when the planner schema had no run_tool branch (zero tools) and a decision with action 'run_tool' somehow arrives, ExecutorService's TOOL_REJECTED handles it (03-04) — orchestrator does not special-case.
    - Every catch debugLogs canonical codes (PLANNER_FAILED / STREAM_FAILED) — Golden Rule 9.
    Create tests/core/ai/AgentOrchestrator.test.ts per the behavior block (vi.spyOn/vi.fn on the three services OR injected stage-stubs; buildOptimizedContextFixture; the invocation resolver is a stub returning a fake StageInvocation with a stub callProviderJsonMode + a model the RendererService mock consumes — for orchestrator-level tests the services may be mocked as spies so the real SDK is NOT invoked here).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/AgentOrchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass — healthy = exactly 2 stage calls; planner_failed fallback has no second planner call; provider_unconfigured terminal has zero model calls; caps terminate; abort → AbortError; deltas arrive before completion; ask_clarification terminal.
    - grep -c "plannerCap\|toolCap" src/core/ai/AgentOrchestrator.ts >= 1 (caps enforced HERE).
    - grep -c "CompletionEvidence\|OutcomeVerifier\|trajectory" src/core/ai/AgentOrchestrator.ts == 0 (D-20 — no evidence machinery).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Bounded, honest, cap-enforcing orchestrator (Appendix I + the two documented deviations) with the full terminal-state suite (incl. provider_unconfigured) green.</done>
</task>

</tasks>

## Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-03 [empty + encoding] and AI-02 [concurrency] — recorded in `must_haves.flagged_assumptions`. All other edges stay with their owners (never silently dropped).

## Artifacts This Phase Produces

- src/core/ai/RendererService.ts (new): `RenderInput`, `RendererService.render()` (Seam 3, onDelta).
- src/core/ai/AgentOrchestrator.ts (new): `AgentTurnInput` (+ optional `onStreamDelta`, `invocation` — documented deviations), `AgentTurnOutput`, `runAgentTurn()`, `capsForTier()`, `StageResolver` type re-export.
- tests/core/ai/RendererService.test.ts, tests/core/ai/AgentOrchestrator.test.ts (new).

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer stream → UI path | Deltas cross here via onStreamDelta; honesty (done XOR error) is the contract |
| Orchestrator → stage services | The loop is the ONLY caller of PlannerService (Appendix I Rule); caps are the only budget control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | (entropy) | RendererService streaming honesty | critical | mitigate | finishReason awaited after the delta loop (Pitfall 5); error chunk → typed STREAM_FAILED throw → failed terminal; partial text is exactly the pre-failure deltas (test 2); the UI (03-08) renders failed state, never complete |
| T-03-06-02 | (cost) | loop termination / caps | high | mitigate | §1.4 plannerCap/toolCap enforced ONLY here (verbatim AgentTurnInput.tier caps shape); cap exhaustion → *_cap_reached terminal; planner failure → deterministic planner_failed (no re-invocation); provider-unconfigured invocation → provider_unconfigured terminal (no model call); abort → AbortError; healthy = exactly 2 calls (tests 1,2,4,5,8) |
| T-03-06-03 | DoS | aborted turn billing | medium | mitigate | abortSignal threaded unchanged into every stage call (Appendix I Rule); abort cancels generation — no orphaned request bills tokens (test 5, Renderer test 3) |
| T-03-06-04 | Tampering | evidence machinery leak | medium | mitigate | D-20: grep asserts zero CompletionEvidence/OutcomeVerifier/trajectory tokens in the orchestrator — Phase 3a rewires it |
| T-03-06-SC | Tampering | npm installs | high | mitigate | No new packages this plan |
</threat_model>

<verification>
- `npx vitest run tests/core/ai/RendererService.test.ts tests/core/ai/AgentOrchestrator.test.ts` green.
- `npx tsc --noEmit` green.
- D-20 grep: no evidence machinery in AgentOrchestrator.ts.
- Full AI suite so far: `npx vitest run tests/core/ai` (all wave-1..4 AI tests together).
</verification>

<success_criteria>
- Streaming honesty, cost discipline (exactly 2 calls), and loop termination all test-proven at the orchestrator level.
- The onStreamDelta + invocation seams exist for the UI hook (03-08) with deltas-before-completion proven.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-06-SUMMARY.md` when done
</output>
