---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 06
type: execute
wave: 4
depends_on: ["03-03", "03-04", "03-05"]
files_modified:
- src/core/ai/RendererService.ts
- src/core/ai/AgentOrchestrator.ts
- tests/core/ai/RendererService.test.ts
- tests/core/ai/AgentOrchestrator.test.ts
autonomous: true
requirements: [AI-02, AI-03, AI-04]

<!-- FINAL v2 (2026-08-10): + ordered Tasks section. P-4/F-5 — RendererService builds its streamText call
     from the Router invocation's messages[]+providerOptions shape, never system:string. D-20 verbatim intact
     (output struct unchanged; onStreamDelta/invocation are documented input-only deviations). -->

### must_haves:
truths:
- "RendererService.render streams via StreamAdapter (Seam 3): text deltas flow to the caller's onDelta callback live, then the accumulated final text is returned; streamText is consumed ONLY inside RendererService/StreamAdapter (AI-SPEC rule)."
- "RendererService builds its streamText call from the Router-provided invocation (F-5, P-4): it uses the `messages[]` + `providerOptions.anthropic.cacheControl` shape supplied by createStageInvocation (03-05) — NEVER `system: string`. The [SYSTEM] message is the byte-stable persona/prompt block from the OptimizedContext (03-07); RendererService does not compute cache strategy (03-03 owns strategy, 03-05 owns application) and does not assemble prompts (Golden Rule 3)."
- "Streaming honesty (Pitfall 5): a render ending with finishReason !== 'stop' or an error chunk produces a failed terminal — never a silently-truncated 'complete' text; abortSignal threaded so cancel stops generation (no orphaned billing)."
- "AgentOrchestrator.runAgentTurn is Appendix I VERBATIM (D-20): returns AgentTurnOutput { operationId, streamedText, toolResults, reasonCode } — NO trajectory states, NO CompletionEvidence, NO OutcomeVerifier (Phase 3a)."
- "§1.4 tier caps are enforced ONLY by AgentOrchestrator: plannerCap/toolCap/mcpChaining arrive via AgentTurnInput.tier — which is VERBATIM the Appendix I caps shape { plannerCap: number; toolCap: number; mcpChaining: boolean } (spec lines 5527-5531), NOT ModelContextTier; the hook (03-08) populates that verbatim field via capsForTier(context.tier) (tiny 1/1, small 2/1, medium 3/2, large 5/3); cap exhaustion terminates with planner_cap_reached / tool_cap_reached."
- "Every path terminates in a bounded terminal reasonCode: planner failure → deterministic 'planner_failed' fallback (no re-invocation), provider-unconfigured invocation resolution → 'provider_unconfigured' (no model call), abort → AbortError, caps → *_cap_reached, success → the planner's reasonCode or 'ask_clarification'."
- "A healthy turn is exactly 2 model calls (one planner + one renderer) regardless of provider (AI-SPEC cost dimension)."
- "Documented Phase-3 deviations from Appendix I (recorded in the §18 addendum by 03-09): runAgentTurn gains optional onStreamDelta?: (delta: string) => void and optional invocation?: StageResolver; the OUTPUT struct stays verbatim (D-20 intact). AgentTurnInput.tier is NOT a deviation — the spec's verbatim tier field is already the caps shape (plannerCap/toolCap/mcpChaining, lines 5527-5531); capsForTier is a hook-side helper that maps ModelContextTier → that verbatim shape, never a field-type change."
artifacts:
- src/core/ai/RendererService.ts
- src/core/ai/AgentOrchestrator.ts
- tests/core/ai/{RendererService,AgentOrchestrator}.test.ts
key_links:
- "runAgentTurn.finish() → RendererService.render({ ..., invocation, onDelta }) — the stream reaches the hook's ChunkBuffer via onStreamDelta (03-08); RendererService uses invocation's messages[]+providerOptions shape (F-5, from 03-05)."
- "The orchestrator resolves per-stage invocations via input.invocation (StageResolver from ProviderRouter 03-05) — PlannerService ctx + RendererService model + the F-5 call shape."
- "PlannerService/ExecutorService from 03-04 are the loop's stage services; onStreamDelta fixture asserts deltas arrive BEFORE completion."
flagged_assumptions:
- "AI-03 [empty]: Phase-3 turns are single-shot ([USER INPUT] only) — no conversation history sections; an empty user input is never sent (Sender disabled); empty stream (zero deltas) still terminates with done/error honestly."
- "AI-03 [encoding]: chunk boundaries are SDK delta boundaries; text accumulates in arrival order; the renderer never re-orders or normalizes."
- "AI-02 [concurrency]: runAgentTurn is sequential and single-surface; the loop is the only caller of PlannerService (no component/hook calls PlannerService directly — Appendix I Rule)."
- "F-5 [call shape ownership]: RendererService receives the messages[]+providerOptions shape from the Router invocation; it never builds system:string and never computes cacheControl — it threads the Router-supplied shape into streamText."
prohibitions:
- "No trajectory states / CompletionEvidence / OutcomeVerifier in the orchestrator (D-20 — Phase 3a owns them)."
- "No `system: string` on RendererService's constructed streamText (F-5 — use the invocation's messages[]+providerOptions shape)."
- "No cache-strategy computation or prompt assembly in RendererService (03-03 strategy / 03-05 application / Golden Rule 3)."
- "No maxSteps / SDK loops (bounded while loop only)."
- "No 'await result' destructuring (ai@4 not thenable)."
- "No consuming textStream without awaiting a terminal promise (Pitfall 5)."
- "No component/hook calling PlannerService directly (Appendix I Rule)."
- "No rendering un-await-verified text as complete (streaming honesty)."

Purpose: A healthy turn must cost exactly 2 model calls, stream honestly (done XOR error), and terminate in every path — the AI-SPEC eval dimensions "Cost discipline", "Streaming honesty", and "Task completion / loop termination". RendererService threads the F-5 messages[]+providerOptions shape so the byte-stable [SYSTEM] persona block actually caches on anthropic.
Output: RendererService + AgentOrchestrator + their §18 test files (spies assert exactly-2-calls; deltas-before-completion; terminal states; RendererService uses messages[] not system:string).
<execution_context>@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md</execution_context>

### Tasks (ordered — do not reorder; each maps to a truth/artifact)
1. **RendererService.ts (new).** RenderInput carries the Router invocation. render() consumes StreamAdapter (Seam 3): build streamText from the invocation's **messages[]+providerOptions** shape (F-5, never system:string); stream deltas → onDelta; accumulate + return final text; await finishReason.
2. **Streaming honesty.** finishReason !== 'stop' or an error chunk → typed STREAM_FAILED throw → failed terminal (partial text = pre-failure deltas). Thread abortSignal so cancel stops generation.
3. **AgentOrchestrator.ts (new) — Appendix I VERBATIM (D-20).** runAgentTurn returns AgentTurnOutput {operationId, streamedText, toolResults, reasonCode} — no trajectory/evidence/verifier. Add ONLY the input-only deviations: optional onStreamDelta?, optional invocation?: StageResolver. Re-export StageResolver type.
4. **capsForTier() helper.** Map ModelContextTier → the verbatim caps shape {plannerCap, toolCap, mcpChaining} (tiny 1/1, small 2/1, medium 3/2, large 5/3). AgentTurnInput.tier IS the caps shape (spec lines 5527-5531).
5. **Cap + terminal enforcement.** Enforce §1.4 caps ONLY here → *_cap_reached; planner failure → deterministic planner_failed (no re-invocation); provider-unconfigured invocation → provider_unconfigured (no model call); abort → AbortError; success → planner reasonCode or ask_clarification.
6. **RendererService.test.ts (new).** streaming honesty (done XOR error), abort-cancels-billing, AND **F-5**: constructed streamText uses messages[]+providerOptions (never system:string).
7. **AgentOrchestrator.test.ts (new).** exactly-2-calls (spies); terminal reasonCodes for every path; D-20 grep (zero CompletionEvidence/OutcomeVerifier/trajectory tokens); onStreamDelta deltas-before-completion fixture.
8. **Verify.** grep no `system:` string literal in RendererService streamText construction; grep no evidence tokens in AgentOrchestrator; test:ai green.

### Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-03 [empty + encoding] and AI-02 [concurrency] — recorded in must_haves.flagged_assumptions. All other edges stay with their owners (never silently dropped).

### Artifacts This Phase Produces
- src/core/ai/RendererService.ts (new): RenderInput (carries the Router invocation), RendererService.render() (Seam 3, onDelta) — builds streamText from the invocation's messages[]+providerOptions (F-5).
- src/core/ai/AgentOrchestrator.ts (new): AgentTurnInput (+ optional onStreamDelta, invocation — documented deviations), AgentTurnOutput, runAgentTurn(), capsForTier(), StageResolver type re-export.
- tests/core/ai/RendererService.test.ts (new): streaming honesty, abort, AND an F-5 assertion (messages[] not system:string).
- tests/core/ai/AgentOrchestrator.test.ts (new): exactly-2-calls, terminal reasonCodes, D-20 grep, onStreamDelta deltas-before-completion.
<threat_model>

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer stream → UI path | Deltas cross here via onStreamDelta; honesty (done XOR error) is the contract |
| Orchestrator → stage services | The loop is the ONLY caller of PlannerService (Appendix I Rule); caps are the only budget control |
| Router invocation → Renderer call | RendererService threads the Router-supplied messages[]+providerOptions shape (F-5); it never builds system:string or computes strategy |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | (entropy) | RendererService streaming honesty | critical | mitigate | finishReason awaited after the delta loop (Pitfall 5); error chunk → typed STREAM_FAILED → failed terminal; partial text is exactly the pre-failure deltas (test 2); the UI (03-08) renders failed state, never complete |
| T-03-06-02 | (cost) | loop termination / caps | high | mitigate | §1.4 plannerCap/toolCap enforced ONLY here (verbatim AgentTurnInput.tier caps shape); cap exhaustion → *_cap_reached; planner failure → planner_failed (no re-invocation); provider-unconfigured → provider_unconfigured (no model call); abort → AbortError; healthy = exactly 2 calls |
| T-03-06-03 | Tampering | F-5 call shape | medium | mitigate | RendererService uses the Router invocation's messages[]+providerOptions (never system:string) so the anthropic cacheControl breakpoint reaches the wire and the byte-stable [SYSTEM] caches; grep asserts no system:string literal |
| T-03-06-04 | DoS | aborted turn billing | medium | mitigate | abortSignal threaded unchanged into every stage call (Appendix I Rule); abort cancels generation — no orphaned request bills tokens |
| T-03-06-05 | Tampering | evidence machinery leak | medium | mitigate | D-20: grep asserts zero CompletionEvidence/OutcomeVerifier/trajectory tokens in the orchestrator — Phase 3a rewires it |
| T-03-06-SC | Tampering | pnpm installs | high | mitigate | No new packages this plan |
</threat_model>
<success_criteria>
- Streaming honesty, cost discipline (exactly 2 calls), and loop termination all test-proven at the orchestrator level.
- F-5 proven: RendererService constructs streamText via messages[]+providerOptions (never system:string).
- D-20 intact: output struct verbatim; onStreamDelta + invocation are the only (input-only) deviations; no evidence machinery.
- The onStreamDelta + invocation seams exist for the UI hook (03-08) with deltas-before-completion proven.
</success_criteria>
