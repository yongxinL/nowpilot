---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/core/ai/ChunkBuffer.ts
  - src/core/ai/StreamAdapter.ts
  - src/core/ai/PromptCacheAdapter.ts
  - src/core/ai/PromptCacheManager.ts
  - src/core/ai/toolSchemas.ts
  - tests/fixtures/optimizedContext.ts
  - tests/core/ai/ChunkBuffer.test.ts
  - tests/core/ai/PromptCacheAdapter.test.ts
  - tests/core/ai/toolSchemas.test.ts
autonomous: true
requirements: [AI-02, AI-03, AI-05]

must_haves:
  truths:
    - "ChunkBuffer (Appendix J.1 verbatim) throttles stream display at 8_000 bytes/s default with rAF/setTimeout flush; total text is never dropped (order + tail preserved)."
    - "StreamAdapter normalizes streamText deltas into LLMStreamChunk {type:'text'}… then a terminal {type:'done'} or {type:'error'} chunk; it ALWAYS awaits result.finishReason after the delta loop (Pitfall 5 — never render un-await-verified text); maxRetries: 0 on the constructed call (Pitfall 1)."
    - "PromptCacheAdapter (Appendix K verbatim) emits per-provider cache-hint strategies: 'anthropic-ephemeral' (≤4 breakpoints, cache_control on stable sections), 'gemini-cachedContent' (only ≥32,768 stable tokens — dormant at Phase-3 sizes; no CachedContent API client built), 'prefix-only' for openai/ollama; hashStableSections is FNV-1a byte-stable."
    - "toolSchemas.ts ships the ToolSchemaRef contract + exactly one safe built-in 'get-provider-info' (dangerous: no) + buildToolNameEnum that returns null for an empty list (D-05: z.enum([]) is rejected by Zod — the run_tool branch is omitted)."
    - "tests/fixtures/optimizedContext.ts (D-08) builds deterministic, typed §2.3 OptimizedContexts (fixed IDs, edge-parameterized on tier/budget/privacyMode/persona) — never crypto/Date.now."
  artifacts:
    - src/core/ai/ChunkBuffer.ts
    - src/core/ai/StreamAdapter.ts
    - src/core/ai/PromptCacheAdapter.ts
    - src/core/ai/PromptCacheManager.ts
    - src/core/ai/toolSchemas.ts
    - tests/fixtures/optimizedContext.ts
    - tests/core/ai/{ChunkBuffer,PromptCacheAdapter,toolSchemas}.test.ts
  key_links:
    - "StreamAdapter is the ONLY consumer of streamText besides getAISDKModel/ProviderRouter (Seam 3 boundary; RendererService in 03-06 consumes the adapter)."
    - "buildToolNameEnum output feeds PlannerDecisionSchema's run_tool branch (03-04) and ExecutorService's closed enum."
    - "PromptCacheAdapter hashes context.sections (persona block inside [SYSTEM]) — the byte-stability invariant PersonaInjector tests assert (03-07)."
  flagged_assumptions:
    - "AI-03 [empty]: Phase-3 chat is single-turn ([USER INPUT] only, no history sections); an empty conversation renders the empty-state cue and empty input disables send — no accumulation."
    - "AI-03 [encoding]: ChunkBuffer's byte-rate throttle counts JS string length (UTF-16 code units) per Appendix J.1; Bubble rendering is plain text — no grapheme normalization this phase."
    - "AI-05 [encoding]: byte-stability is JS string equality on UTF-16 code units; the persona block uses a fixed template with ordered joins (buildPersonaBlock, 03-07)."
    - "A1 [research]: Gemini cachedContent minimum stays the spec constant 32_768 tokens; the branch stays dormant at Phase-3 prompt sizes — the constant is spec-verbatim and NOT optimized."
  prohibitions:
    - "No 'await result' destructuring (ai@4 results are not thenable — Pitfall 2)."
    - "No consuming textStream without awaiting a terminal promise (Pitfall 5)."
    - "No experimental_repair* wiring (R-2)."
    - "No SDK tool()/tools/maxSteps machinery (R-4)."
    - "No z.enum([]) — run_tool branch omitted when zero tools (D-05)."
    - "No CachedContent API client this phase (Appendix K branch is hash/strategy only)."
    - "No motion-driven text reveals (spec §12.6) — ChunkBuffer rAF flushing is the only text animation."
---

<objective>
Deterministic runtime utilities: the streaming kit (ChunkBuffer J.1 + StreamAdapter), the prompt-cache kit (PromptCacheAdapter K + PromptCacheManager), the tool contract (toolSchemas.ts, D-04/D-05), and the deterministic fixture builder (D-08). All are pure/spec-verbatim modules — no SDK orchestration logic here.

Purpose: These are the shared primitives the Renderer/Orchestrator (03-06), Planner/Executor (03-04), and Persona tests (03-07) consume. Byte-stability (cache), chunk honesty (streaming), and the closed tool enum (execution governance) are all established here.
Output: Working streaming/cache/tool/fixture primitives with their unit tests.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/PRODUCT_SPEC_v0_1.md
@src/core/ai/types.ts
@src/core/prompts/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChunkBuffer (Appendix J.1) + StreamAdapter + ChunkBuffer test</name>
  <files>src/core/ai/ChunkBuffer.ts, src/core/ai/StreamAdapter.ts, tests/core/ai/ChunkBuffer.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md Appendix J.1 (line ~5596 — createChunkBuffer VERBATIM)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Pattern 5 (lines 329-349, streamTextToLLMChunks) + Code Examples "Streaming renderer + error surface" (lines 473-483) + Pitfall 2/Pitfall 5
    - src/core/ai/types.ts (LLMStreamChunk)
  </read_first>
  <behavior>
    - Test 1: enqueue('ab').enqueue('cd') → after flushNow, the flush callback receives 'abcd' (order preserved, tail never dropped).
    - Test 2: with fake timers, >8_000 bytes enqueued in one second schedules a 33ms setTimeout flush (Appendix J.1 throttle), not a rAF flush.
    - Test 3: reset() clears pending+full and cancels the scheduled flush.
    - Test 4: streamTextToLLMChunks over a mocked streamText (injected via a minimal fake — the adapter accepts the ai streamText result shape) yields text chunks then {type:'done'} and awaits finishReason.
    - Test 5: when the terminal promise rejects (finishReason non-'stop'), the generator yields {type:'error'} — never a done chunk.
  </behavior>
  <action>
    Create src/core/ai/ChunkBuffer.ts — Appendix J.1 VERBATIM (createChunkBuffer with enqueue/onFlush/flushNow/reset, pending/full, rAF schedule, byteRate throttle: >8_000 B/s measured over 1s → 33ms setTimeout, else rAF; 8_000 default kept per CONTEXT discretion).
    Create src/core/ai/StreamAdapter.ts — RESEARCH Pattern 5 VERBATIM-ish: `export async function* streamTextToLLMChunks(args: { model: LanguageModel; system: string; task: string; maxTokens: number; abortSignal: AbortSignal }): AsyncIterable<LLMStreamChunk>` — calls streamText({ model, system, prompt: task, maxTokens, maxRetries: 0, abortSignal }) (maxRetries: 0 — Router owns retries, D-17), yields { type:'text', content: delta } per delta, ALWAYS awaits result.finishReason, then yields { type:'done', content: '' }; catch → yield { type:'error', content: e instanceof Error ? e.message : String(e) } (caller debugLogs with a canonical code — Golden Rule 9). Header documents: never render un-await-verified text (Pitfall 5); ai@4 result is NOT thenable.
    Create tests/core/ai/ChunkBuffer.test.ts per the behavior block (fake timers for the throttle; pure Node env via @vitest-environment node precedent from RateLimiter.test.ts). For streamTextToLLMChunks, stub the ai module minimally OR route through the injected-fetch adapter pattern (getAISDKModel with a mock fetch) — prefer the real-SDK path per RESEARCH Pattern 4; if stubbing is required, mock only 'ai' inside this test file and document why.
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/ChunkBuffer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass.
    - grep -c "maxRetries: 0" src/core/ai/StreamAdapter.ts == 1.
    - grep -c "finishReason" src/core/ai/StreamAdapter.ts >= 1 (terminal await present).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>ChunkBuffer matches J.1; StreamAdapter is streaming-honest (done XOR error, finishReason awaited, maxRetries: 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PromptCacheAdapter (Appendix K) + PromptCacheManager + test</name>
  <files>src/core/ai/PromptCacheAdapter.ts, src/core/ai/PromptCacheManager.ts, tests/core/ai/PromptCacheAdapter.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md Appendix K (line ~5719 — applyCacheHints VERBATIM + hashStableSections FNV-1a + Rules)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Pattern 3 (Anthropic cache-hint wire path: providerOptions.anthropic.cacheControl on CoreSystemMessage) + Pitfall 5 (byte-stability)
    - src/core/ai/types.ts (ProviderId, PromptSection)
    - src/core/error/debugLog.ts + errorCodes.ts (for PromptCacheManager logging)
  </read_first>
  <behavior>
    - Test 1: applyCacheHints('anthropic', sections with 2 stable) marks only the first 4 stable sections with cache_control {type:'ephemeral'} (ANTHROPIC_MAX_BREAKPOINTS) and strategy === 'anthropic-ephemeral'.
    - Test 2: applyCacheHints('gemini', stable sections totaling < 32_768 tokens) → strategy 'prefix-only' (dormant branch); ≥ 32_768 → 'gemini-cachedContent' with cachedContent/inline split.
    - Test 3: applyCacheHints('openai', sections) → strategy 'prefix-only', stable sections ordered first.
    - Test 4: hashStableSections is deterministic — two calls with the same section texts produce the same 8-hex FNV-1a string; changing one stable text changes the hash.
    - Test 5: PromptCacheManager.logHit/logMiss calls debugLog with a canonical code and never throws.
  </behavior>
  <action>
    Create src/core/ai/PromptCacheAdapter.ts — Appendix K VERBATIM: CacheAdaptedPrompt, ANTHROPIC_MAX_BREAKPOINTS = 4, GEMINI_MIN_CACHED_TOKENS = 32_768, applyCacheHints(providerId, sections), stableFirst sort, hashStableSections (FNV-1a 32-bit, hex padStart 8). Header notes: gemini-cachedContent resource creation is a documented future phase (Research RQ4 — do NOT build the CachedContent API client); the anthropic cache_control marker is the adapter-level representation, and the actual wire hint rides providerOptions: { anthropic: { cacheControl: { type: 'ephemeral' } } } on a CoreSystemMessage (RESEARCH Pattern 3 — the ProviderRouter/StageInvocation layer attaches it, not this pure module).
    Create src/core/ai/PromptCacheManager.ts — thin orchestrator: `promptCacheManager` singleton (or getPromptCacheManager()) holding a per-surface Map<providerId, { hitCount, missCount }>; `recordHit(providerId)`, `recordMiss(providerId)` → debugLog with a canonical code (use a Phase-3 code from 03-01; if none fits, log via EVT_HANDLER — NO new uncanonicalized code, Golden Rule 9); never blocks, never throws. PHASE-3 reality: hits/misses are debugLog-only (no AITransactionLog until Phase 6 — DIAG-01).
    Create tests/core/ai/PromptCacheAdapter.test.ts per the behavior block (node env).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/PromptCacheAdapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass; hash determinism asserted.
    - applyCacheHints matches Appendix K byte-for-byte semantics (strategies, breakpoint cap, gemini minimum).
    - No CachedContent API client code exists (grep 'cachedContent' shows only the Appendix-K strategy branch).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Cache-hint emission + byte-stable hashing per Appendix K; manager logs hits/misses without blocking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: toolSchemas (D-04/D-05) + optimizedContext fixture (D-08) + tests</name>
  <files>src/core/ai/toolSchemas.ts, tests/fixtures/optimizedContext.ts, tests/core/ai/toolSchemas.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md Appendix C ToolSchemaRef (line ~4570), §1.2 planner decision note
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md D-04 (contract + one tool) + D-05 (empty registry rule)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md "tests/fixtures/optimizedContext.ts" (fixture conventions: seeded/fixed constants, never crypto/Date.now, edge variants first-class)
    - tests/fixtures/index.ts (builder conventions)
    - src/core/ai/types.ts (ToolSchemaRef, OptimizedContext, PromptSection, UserPreferences, ModelContextTier)
  </read_first>
  <behavior>
    - Test 1: buildToolNameEnum(['get-provider-info']) returns a z.enum accepting 'get-provider-info' and rejecting 'unknown-tool'.
    - Test 2: buildToolNameEnum([]) returns null (never z.enum([]) — Zod rejects).
    - Test 3: GET_PROVIDER_INFO_TOOL schema: name === 'get-provider-info', dangerous === false, source === 'builtin'.
    - Test 4: buildOptimizedContextFixture() is deterministic — two calls deep-equal; the fixture's sections include a stable system section, a task section, and a user_input section; tier/provenance/minimalMode present per §2.3.
    - Test 5: buildOptimizedContextFixture({ tier: 'tiny', inputBudget: 1000 }) overrides propagate.
  </behavior>
  <action>
    Create src/core/ai/toolSchemas.ts:
    - `export const GET_PROVIDER_INFO_TOOL: ToolSchemaRef` — name 'get-provider-info', description "Report the currently configured provider id, enabled state, and available models", jsonSchema `{ type: 'object', properties: { providerId: { type: 'string', enum: ['openai','anthropic','gemini','ollama'] } }, additionalProperties: false }`, dangerous: false, source: 'builtin' (D-04 — depends only on ProviderRegistry).
    - `export function buildToolNameEnum(toolNames: readonly string[])`: returns `z.enum(toolNames)` when toolNames.length > 0, else null (D-05 — the PlannerDecisionSchema builder omits the run_tool branch when null; z.enum([]) is rejected by Zod).
    - `export function registeredToolNames(tools: readonly ToolSchemaRef[]): string[]` — maps to names (the closed-enum source for ExecutorService).
    Create tests/fixtures/optimizedContext.ts (D-08 — test fixture, NOT a runtime module; header marks it Phase-3 create-list item):
    - `export function buildOptimizedContextFixture(overrides: Partial<OptimizedContext> = {}): OptimizedContext` — deterministic §2.3 shape: tier 'small' default, inputBudget 8192, outputBudget 2048, sections [] (stable system + task + user_input built from FIXED strings), provenance { sections: [], totalTokens: 0, minimalMode: false, workspaceId: 'fixture-ws', activeSurface: 'sidepanel' }, minimalMode false.
    - `export const FIXED_OPERATION_ID = 'op-fixture-0001'` and fixed persona/UserPreferences fixtures.
    Create tests/core/ai/toolSchemas.test.ts per the behavior block (+ a determinism assertion consuming buildOptimizedContextFixture).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/toolSchemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass; fixture determinism deep-equality asserted.
    - grep -c "z.enum" src/core/ai/toolSchemas.ts >= 1 and the empty-list path returns null (test-asserted, never z.enum([])).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Closed tool enum + single built-in contract + deterministic fixture builder all test-green.</done>
</task>

</tasks>

## Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-03 [empty + encoding] and AI-05 [encoding] — recorded in `must_haves.flagged_assumptions`. The remaining edges land with their requirement owners (AI-02 concurrency → 03-04; AI-03 streaming → 03-06; AI-04 → 03-05; AI-05 adjacency/empty/ordering/idempotency/concurrency → 03-07; AI-06 → 03-08). None silently dropped.

## Artifacts This Phase Produces

- src/core/ai/ChunkBuffer.ts (new): `ChunkBuffer` interface, `createChunkBuffer()` (Appendix J.1).
- src/core/ai/StreamAdapter.ts (new): `streamTextToLLMChunks()` async generator (Seam 3).
- src/core/ai/PromptCacheAdapter.ts (new): `CacheAdaptedPrompt`, `applyCacheHints()`, `hashStableSections()`, `ANTHROPIC_MAX_BREAKPOINTS`, `GEMINI_MIN_CACHED_TOKENS` (Appendix K).
- src/core/ai/PromptCacheManager.ts (new): `getPromptCacheManager()` / `promptCacheManager`, `recordHit()`, `recordMiss()`.
- src/core/ai/toolSchemas.ts (new): `GET_PROVIDER_INFO_TOOL`, `buildToolNameEnum()`, `registeredToolNames()`.
- tests/fixtures/optimizedContext.ts (new): `buildOptimizedContextFixture()`, `FIXED_OPERATION_ID`, fixed persona/preferences fixtures.
- tests/core/ai/ChunkBuffer.test.ts, tests/core/ai/PromptCacheAdapter.test.ts, tests/core/ai/toolSchemas.test.ts (new).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StreamAdapter → model provider | streamText deltas cross here; maxRetries: 0 + abortSignal are the only call settings the adapter adds (cost/loop control) |
| PromptCacheAdapter → provider request shape | Cache hints (anthropic cacheControl) alter the wire request; the persona block inside stable [SYSTEM] is hash-covered |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | (cost) | StreamAdapter / ChunkBuffer | high | mitigate | maxRetries: 0 on the constructed streamText (Pitfall 1 hidden 4th retry layer); explicit maxTokens; the terminal finishReason await (Pitfall 5) guarantees failed streams surface as error chunks — never a silently-truncated 'done' presented as complete |
| T-03-03-02 | Tampering | PromptCacheAdapter hash | medium | mitigate | FNV-1a hashStableSections is deterministic over stable section text; byte-stability tests (this plan) + PersonaInjector hash-equality tests (03-07) catch persona-block drift that would break provider caches (Pitfall 5, RICH-R-02) |
| T-03-03-03 | Elevation | toolSchemas closed enum | high | mitigate | buildToolNameEnum returns null for empty (D-05 — no z.enum([])); only 'get-provider-info' (dangerous: no) ships; ExecutorService (03-04) rejects anything outside the closed enum with TOOL_REJECTED |
| T-03-03-SC | Tampering | npm installs | high | mitigate | No new packages in this plan; existing approved set from 03-01 audit |
</threat_model>

<verification>
- `npx vitest run tests/core/ai/ChunkBuffer.test.ts tests/core/ai/PromptCacheAdapter.test.ts tests/core/ai/toolSchemas.test.ts` green.
- `npx tsc --noEmit` green.
- Seam-3 grep: streamText imported only in StreamAdapter.ts (+ getAISDKModel/ProviderRouter later).
</verification>

<success_criteria>
- Streaming kit, cache kit, tool contract, and fixture builder all test-green.
- No silent-truncation path (done XOR error), no hidden SDK retries, no empty z.enum, no unstable hashing.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-03-SUMMARY.md` when done
</output>
