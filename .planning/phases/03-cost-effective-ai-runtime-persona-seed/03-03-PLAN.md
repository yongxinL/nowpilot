---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
- src/core/ai/ChunkBuffer.ts
- src/core/ai/StreamAdapter.ts
- src/core/ai/PromptCacheAdapter.ts
- src/core/ai/PromptCacheManager.ts
- src/core/ai/toolSchemas.ts
- tests/fixtures/optimizedContext.ts
- tests/core/ai/ChunkBuffer.test.ts
- tests/core/ai/PromptCacheAdapter.test.ts
- tests/core/ai/toolSchemas.test.ts
autonomous: true
requirements: [AI-02, AI-03, AI-05]

<!-- FINAL v2 (2026-08-10): + ordered Tasks section. P-4 — StreamAdapter is the F-5 pass-through: builds
     streamText in messages[]+providerOptions form, passes providerOptions.anthropic.cacheControl THROUGH
     from the Router (03-05); never system:string, never computes strategy. P-3 — PromptSection from
     '@/core/ai/types'. -->

### must_haves:
truths:
- "ChunkBuffer (Appendix J.1 verbatim) throttles stream display at 8_000 bytes/s default with rAF/setTimeout flush; total text is never dropped (order + tail preserved)."
- "StreamAdapter normalizes streamText deltas into LLMStreamChunk {type:'text'}… then a terminal {type:'done'} or {type:'error'} chunk; it ALWAYS awaits result.finishReason after the delta loop (Pitfall 5 — never render un-await-verified text); maxRetries: 0 on the constructed call (Pitfall 1)."
- "StreamAdapter builds the streamText call in the `messages[]` form (F-5, P-4): the cached [SYSTEM] section is a CoreSystemMessage and any providerOptions.anthropic.cacheControl is passed THROUGH from the caller (Router / createStageInvocation, 03-05). StreamAdapter NEVER sets `system: string` and NEVER computes cache strategy itself — applyCacheHints (this plan) owns strategy; the Router owns application (03-05). PromptSection is imported from '@/core/ai/types' (D-07/P-3)."
- "PromptCacheAdapter (Appendix K verbatim) emits per-provider cache-hint strategies: 'anthropic-ephemeral' (≤4 breakpoints, cache_control on stable sections), 'gemini-cachedContent' (only ≥32,768 stable tokens — dormant at Phase-3 sizes; no CachedContent API client built), 'prefix-only' for openai/ollama; hashStableSections is FNV-1a byte-stable. applyCacheHints returns the strategy + the providerOptions payload the Router applies (F-5) — it does not itself construct a provider call."
- "toolSchemas.ts ships the ToolSchemaRef contract + exactly one safe built-in 'get-provider-info' (dangerous: no) + buildToolNameEnum that returns null for an empty list (D-05: z.enum([]) is rejected by Zod — the run_tool branch is omitted)."
- "tests/fixtures/optimizedContext.ts (D-08) builds deterministic, typed §2.3 OptimizedContexts (fixed IDs, edge-parameterized on tier/budget/privacyMode/persona) — never crypto/Date.now; PromptSection[] shape imported from '@/core/ai/types' (P-3)."
artifacts:
- src/core/ai/ChunkBuffer.ts
- src/core/ai/StreamAdapter.ts
- src/core/ai/PromptCacheAdapter.ts
- src/core/ai/PromptCacheManager.ts
- src/core/ai/toolSchemas.ts
- tests/fixtures/optimizedContext.ts
- tests/core/ai/{ChunkBuffer,PromptCacheAdapter,toolSchemas}.test.ts
key_links:
- "StreamAdapter is the ONLY consumer of streamText besides getAISDKModel/ProviderRouter (Seam 3 boundary; RendererService in 03-06 consumes the adapter)."
- "applyCacheHints output (strategy + providerOptions.anthropic.cacheControl payload) is applied by the Router/createStageInvocation (03-05, F-5) and passed through StreamAdapter into the constructed messages[] call."
- "buildToolNameEnum output feeds PlannerDecisionSchema's run_tool branch (03-04) and ExecutorService's closed enum."
- "PromptCacheAdapter hashes context.sections (persona block inside [SYSTEM]) — the byte-stability invariant PersonaInjector tests assert (03-07)."
flagged_assumptions:
- "AI-03 [empty]: Phase-3 chat is single-turn ([USER INPUT] only, no history sections); an empty conversation renders the empty-state cue and empty input disables send — no accumulation."
- "AI-03 [encoding]: ChunkBuffer's byte-rate throttle counts JS string length (UTF-16 code units) per Appendix J.1; Bubble rendering is plain text — no grapheme normalization this phase."
- "AI-05 [encoding]: byte-stability is JS string equality on UTF-16 code units; the persona block uses a fixed template with ordered joins (buildPersonaBlock, 03-07)."
- "F-5 [application boundary]: StreamAdapter is a PASS-THROUGH for providerOptions — it does not read np_persona, does not compute cacheControl, and does not choose the strategy; those belong to applyCacheHints (strategy) and the Router (application)."
- "A1 [research]: Gemini cachedContent minimum stays the spec constant 32_768 tokens; the branch stays dormant at Phase-3 prompt sizes — spec-verbatim, NOT optimized."
prohibitions:
- "No 'await result' destructuring (ai@4 results are not thenable — Pitfall 2)."
- "No consuming textStream without awaiting a terminal promise (Pitfall 5)."
- "No `system: string` on the constructed streamText call (F-5 — use messages[] + providerOptions so the anthropic cacheControl breakpoint reaches the wire)."
- "No cache-strategy computation inside StreamAdapter (applyCacheHints owns strategy; the Router owns application — P-4)."
- "No `PromptSection` import from '../context/ContextOptimizer' or a local re-declaration (P-3/D-07 — import from '@/core/ai/types')."
- "No experimental_repair* wiring (R-2)."
- "No SDK tool()/tools/maxSteps machinery (R-4)."
- "No z.enum([]) — run_tool branch omitted when zero tools (D-05)."
- "No CachedContent API client this phase (Appendix K branch is hash/strategy only)."
- "No motion-driven text reveals (spec §12.6) — ChunkBuffer rAF flushing is the only text animation."

Purpose: These are the shared primitives the Renderer/Orchestrator (03-06), Planner/Executor (03-04), and Persona tests (03-07) consume. Byte-stability (cache), chunk honesty (streaming), and the closed tool enum (execution governance) are all established here. StreamAdapter's messages[]+providerOptions pass-through (F-5) is what lets the byte-stable [SYSTEM] persona block actually cache on anthropic.
Output: Working streaming/cache/tool/fixture primitives with their unit tests, incl. an F-5 assertion that StreamAdapter emits messages[] (never system:string) and passes providerOptions through unchanged.
<execution_context>@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md</execution_context>

### Tasks (ordered — do not reorder; each maps to a truth/artifact)
1. **ChunkBuffer.ts (new).** ChunkBuffer interface + createChunkBuffer() (Appendix J.1 verbatim): 8_000 B/s rAF/setTimeout flush, order + tail preserved, flushNow() final flush.
2. **StreamAdapter.ts (new).** streamTextToLLMChunks() async generator (Seam 3): build the streamText call in **messages[]** form (F-5) — CoreSystemMessage for [SYSTEM] + pass providerOptions THROUGH; maxRetries: 0; await result.finishReason after the delta loop; emit {type:'text'}… then {type:'done'}|{type:'error'}. Import PromptSection from '@/core/ai/types'. NEVER system:string; NEVER compute strategy.
3. **PromptCacheAdapter.ts (new).** applyCacheHints() (Appendix K): return strategy ('anthropic-ephemeral' ≤4 breakpoints / 'gemini-cachedContent' ≥32_768 / 'prefix-only') + the providerOptions.anthropic.cacheControl payload the Router applies; hashStableSections() FNV-1a; ANTHROPIC_MAX_BREAKPOINTS, GEMINI_MIN_CACHED_TOKENS. No CachedContent client.
4. **PromptCacheManager.ts (new).** getPromptCacheManager()/promptCacheManager, recordHit(), recordMiss().
5. **toolSchemas.ts (new).** GET_PROVIDER_INFO_TOOL (dangerous:no) + ToolSchemaRef contract + buildToolNameEnum() (null for empty, no z.enum([])) + registeredToolNames().
6. **tests/fixtures/optimizedContext.ts (new).** buildOptimizedContextFixture(), FIXED_OPERATION_ID, fixed persona/preferences; PromptSection[] from '@/core/ai/types'; deterministic (no crypto/Date.now).
7. **Tests (new).** ChunkBuffer.test (rAF batching + flushNow tail); PromptCacheAdapter.test (FNV-1a byte-stability + **F-5**: strategy payload is a valid providerOptions.anthropic.cacheControl object); toolSchemas.test (empty→null, get-provider-info present).
8. **Verify.** grep no `system:` string literal in StreamAdapter's streamText construction; grep PromptSection import path = '@/core/ai/types'; test:ai green.

### Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-03 [empty + encoding] and AI-05 [encoding] — recorded in must_haves.flagged_assumptions. The remaining edges land with their requirement owners (AI-02 concurrency → 03-04; AI-03 streaming → 03-06; AI-04 → 03-05; AI-05 → 03-07; AI-06 → 03-08). None silently dropped.

### Artifacts This Phase Produces
- src/core/ai/ChunkBuffer.ts (new): ChunkBuffer interface, createChunkBuffer() (Appendix J.1).
- src/core/ai/StreamAdapter.ts (new): streamTextToLLMChunks() async generator (Seam 3) — messages[]+providerOptions pass-through (F-5), imports PromptSection from '@/core/ai/types'.
- src/core/ai/PromptCacheAdapter.ts (new): CacheAdaptedPrompt, applyCacheHints(), hashStableSections(), ANTHROPIC_MAX_BREAKPOINTS, GEMINI_MIN_CACHED_TOKENS (Appendix K).
- src/core/ai/PromptCacheManager.ts (new): getPromptCacheManager() / promptCacheManager, recordHit(), recordMiss().
- src/core/ai/toolSchemas.ts (new): GET_PROVIDER_INFO_TOOL, buildToolNameEnum(), registeredToolNames().
- tests/fixtures/optimizedContext.ts (new): buildOptimizedContextFixture(), FIXED_OPERATION_ID, fixed persona/preferences fixtures (PromptSection[] from '@/core/ai/types').
- tests/core/ai/ChunkBuffer.test.ts, tests/core/ai/PromptCacheAdapter.test.ts, tests/core/ai/toolSchemas.test.ts (new). PromptCacheAdapter.test asserts the F-5 shape.
<threat_model>

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StreamAdapter → model provider | streamText deltas cross here; maxRetries: 0 + abortSignal are the only call settings the adapter adds; providerOptions is passed through, never invented (F-5) |
| PromptCacheAdapter → provider request shape | Cache hints (anthropic cacheControl) alter the wire request; the persona block inside stable [SYSTEM] is hash-covered; application at the Router (03-05), strategy here |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | (cost) | StreamAdapter / ChunkBuffer | high | mitigate | maxRetries: 0 on the constructed streamText (Pitfall 1); explicit maxTokens; terminal finishReason await (Pitfall 5) guarantees failed streams surface as error chunks — never a silently-truncated 'done' |
| T-03-03-02 | Tampering | PromptCacheAdapter hash / F-5 shape | medium | mitigate | FNV-1a hashStableSections deterministic; byte-stability tests + PersonaInjector hash-equality (03-07) catch drift; StreamAdapter uses messages[]+providerOptions (never system:string) so the hint reaches the wire (Pitfall 5, RICH-R-02) |
| T-03-03-03 | Elevation | toolSchemas closed enum | high | mitigate | buildToolNameEnum returns null for empty (D-05 — no z.enum([])); only 'get-provider-info' (dangerous: no) ships; ExecutorService (03-04) rejects anything outside the enum with TOOL_REJECTED |
| T-03-03-SC | Tampering | pnpm installs | high | mitigate | No new packages; existing approved set from 03-01 audit |
</threat_model>
<success_criteria>
- Streaming kit, cache kit, tool contract, and fixture builder all test-green.
- F-5 proven: StreamAdapter builds messages[] (never system:string) and passes providerOptions.anthropic.cacheControl through unchanged; applyCacheHints returns the payload the Router applies (03-05).
- No silent-truncation path (done XOR error), no hidden SDK retries, no empty z.enum, no unstable hashing.
- PromptSection imported from '@/core/ai/types' (P-3) — no ContextOptimizer path, no re-declaration.
</success_criteria>
