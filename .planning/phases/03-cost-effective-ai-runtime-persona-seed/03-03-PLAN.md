---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/core/ai/providers/OpenAIProvider.ts
  - src/core/ai/providers/AnthropicProvider.ts
  - src/core/ai/providers/GeminiProvider.ts
  - src/core/ai/providers/OllamaProvider.ts
  - src/core/ai/providers/OpenAICompatProvider.ts
  - src/core/ai/StreamAdapter.ts
  - tests/core/ai/StreamAdapter.test.ts
  - tests/core/ai/fixtures/
autonomous: true
requirements: [RICH-R-10]
user_setup: []

assumption_delta_decision:
  detected: true
  transition: "pluralization — one legacy src/services/aiProvider.ts → five provider variants in src/core/ai/providers/"
  decision: "add-alongside with normalization, not promote-and-delete"
  rationale: >
    The new src/core/ai/ProviderId ('openai' | 'anthropic' | 'gemini' | 'ollama' | 'openai-compat',
    spec §8.5 line 208 + D-56) is the canonical RUNTIME identity. The legacy src/services/aiProvider.ts
    is retained: its streamChatResponse path is retired for production chat behind the DEMO_MODE+DEV gate
    (D-44/D-12), and its fetchModelsOrError/testProviderConnection semantics are reused by ProviderRegistry
    live discovery (D-52). The disk np_providers object keeps the Phase-2 shape whose CustomProviderId uses
    'claude' — the 'claude'→'anthropic' mapping happens ONLY at the D-49 normalization boundary (plan 03-05),
    in memory, disk untouched. No migration, no user-visible config change.
  supersedes: "D-30a's 'Phase 3 ProviderRegistry owns the disk migration' — migration is now normalization"

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All five provider adapters implement ILLMProvider (stream + requestJson) and fetch via Requester (src/core/http/Requester.ts) — never raw fetch, never an SDK"
    - "Each provider sets its JSON-mode flag natively (Appendix L rule): OpenAI/OpenAICompat response_format {type:'json_object'}, Gemini responseMimeType 'application/json', Ollama format 'json', Anthropic via tool-use/forced output (A6)"
    - "StreamAdapter parses all four wire families to canonical events: OpenAI [DONE], Anthropic event: types (content_block_delta/text_delta), Gemini candidates[].content.parts[].text, Ollama OpenAI-compatible NDJSON — missing terminator = STREAM_ERROR (REQ-R09)"
    - "Per-provider conformance fixtures exist in tests/core/ai/fixtures/ with exact wire bytes (D-48 matrix seed: OpenAI/Anthropic/Gemini/Ollama happy paths + missing terminator)"
  artifacts:
    - path: "src/core/ai/providers/OpenAIProvider.ts"
      provides: "OpenAI Chat Completions adapter (Bearer auth, SSE stream, response_format json_object)"
      exports: ["OpenAIProvider"]
    - path: "src/core/ai/providers/OpenAICompatProvider.ts"
      provides: "OpenAI-wire-compatible self-hosted adapter (LM Studio/vLLM/proxy) — operator endpoint from np_endpoint_overrides (D-56)"
      exports: ["OpenAICompatProvider"]
    - path: "src/core/ai/providers/OllamaProvider.ts"
      provides: "Ollama via OpenAI-compatible {base}/chat/completions (A4, §10.6 http://localhost:11434/v1), format 'json'"
      exports: ["OllamaProvider"]
    - path: "src/core/ai/providers/AnthropicProvider.ts"
      provides: "Anthropic Messages adapter (x-api-key + anthropic-version 2023-06-01, event-stream, JSON mode via A6)"
      exports: ["AnthropicProvider"]
    - path: "src/core/ai/providers/GeminiProvider.ts"
      provides: "Gemini generateContent adapter (key param/header auth, inline stream, responseMimeType application/json)"
      exports: ["GeminiProvider"]
    - path: "src/core/ai/StreamAdapter.ts"
      provides: "Extended: Anthropic event adapter, Gemini inline adapter, Ollama NDJSON/OpenAI-compat adapter, missing-terminator STREAM_ERROR"
      exports: ["createStreamAdapter", "parseOpenAIStream", "parseAnthropicStream", "parseGeminiStream", "parseOllamaStream"]
  key_links:
    - from: "src/core/ai/providers/OpenAIProvider.ts"
      to: "src/core/ai/StreamAdapter.ts"
      via: "provider streams raw bytes into the adapter; adapter emits canonical StreamEvents"
      pattern: "StreamAdapter|createStreamAdapter"
    - from: "src/core/ai/providers/AnthropicProvider.ts"
      to: "src/core/http/Requester.ts"
      via: "all provider I/O goes through Requester.request (canonical RATE_LIMITED/TIMEOUT/NETWORK codes map onto §20.10 retryability)"
      pattern: "Requester|request\\("
  prohibitions:
    - "No reuse of the proxy-coupled SSE parse pattern (data.textChunk/data.thoughtChunk reads) anywhere in src/core/ai"
    - "No SDK imports (openai/anthropic/@google/genai) — client-side fetch only (INTEGRATIONS.md)"
    - "No provider request starts without a resolved model from TierResolver (D-54a — ProviderRouter must never guess a model slug)"
flagged_assumptions:
  - "A2 (Gemini wire shape): candidates[].content.parts[].text + responseMimeType application/json [ASSUMED — ai.google.dev unreachable at research; conformance fixture must be re-verified against the live API at implementation]"
  - "A3 (OpenAI chunk envelope): parser keys on choices[0].delta.content + [DONE] and tolerates extra/missing envelope fields [ASSUMED]"
  - "A4 (Ollama path): production path is the OpenAI-compatible /v1/chat/completions; native /api/chat NDJSON is fixtures-only"
  - "A6 (Anthropic JSON mode): exact mechanism (tool-use-with-forced-choice vs output-format param) confirmed against the live API at implementation; the conformance fixture must match the implemented shape"
---

### Provider-contract discovery gate

Before editing a provider adapter, inspect the authoritative in-repository specification, `COVERAGE.md`, existing `src/services/aiProvider.ts`, and available provider fixtures. Treat only explicitly sourced request fields, auth placement, stream terminators, and minimum parsed field paths as authoritative. Ignore unrelated envelope fields. If a required JSON-mode or streaming contract remains marked ASSUMED after this inspection, STOP that provider subtask and report the unresolved contract; do not guess from model knowledge and do not require live credentials for automated completion.

<objective>
Build the five provider adapters (OpenAI, Anthropic, Gemini, Ollama, OpenAICompat) and expand StreamAdapter to normalize every provider's wire format into the canonical event union (D-47). This is the SSE rebuild's completion — replacing the private-proxy-coupled parser (the confirmed production bug) with per-provider wire-format conformance, all I/O through Requester (Phase 2), each provider setting its JSON-mode flag natively (Appendix L rule).

Purpose: Real providers must stream real text. The conformance fixtures (exact wire bytes per provider, D-48) are what make the Phase-3 test path deterministic without live keys.
Output: 5 provider modules + expanded StreamAdapter + StreamAdapter.test.ts + per-provider conformance fixtures.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §1.5/§20.10 (retryability), §10.6 (ENDPOINTS defaults), Appendix L (per-provider JSON-mode rule)
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md  # Code Examples (wire bytes 437-452), Pitfall 1, Don't Hand-Roll (TextDecoder line buffer), assumptions A2/A3/A4/A6, COVERAGE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md  # provider/StreamAdapter sections + auth-header patterns (aiProvider.ts:78-88)
@src/services/aiProvider.ts  # auth headers (78-88), fetchModelsOrError semantics (38-149) reused by 03-05
@src/core/http/Requester.ts  # request() wrapper — the ONLY fetch path
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/COVERAGE.md  # API capability matrix — INTEGRATE/OPT-OUT decisions
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAI-wire family — OpenAI, OpenAICompat, Ollama providers</name>
  <files>src/core/ai/providers/OpenAIProvider.ts, src/core/ai/providers/OpenAICompatProvider.ts, src/core/ai/providers/OllamaProvider.ts</files>
  <read_first>@src/core/http/Requester.ts (the ONLY fetch path — request() wrapper + canonical codes) · @src/core/ai/ILLMProvider.ts (created 03-01 — the interface to implement) · @src/core/ai/StreamAdapter.ts (created 03-01 — canonical events) · 03-RESEARCH.md Code Examples (wire bytes) + assumptions A3/A4 + D-56 (OpenAICompat) · 03-PATTERNS.md auth-header patterns (aiProvider.ts:78-88)</read_first>
  <action>Create three ILLMProvider implementations sharing the OpenAI wire shape. Each implements `stream(request, signal)` emitting canonical events and `requestJson(prompt, jsonSchema, signal)` per the interface from plan 03-01, and routes ALL fetch through `Requester.request` (src/core/http/Requester.ts) — never raw fetch. `OpenAIProvider`: base `https://api.openai.com/v1` (overridable), `Authorization: Bearer` header, POST {base}/chat/completions with `stream: true`, JSON mode sets `response_format: {type:'json_object'}`. `OpenAICompatProvider` (D-56): same wire shape, endpoint + auth from an operator-supplied config (np_endpoint_overrides key — merged at registry hydrate in 03-05); registered with NO tier default (tier-mapped only when the operator assigns models). `OllamaProvider` (A4): base `http://localhost:11434/v1`, POST {base}/chat/completions (OpenAI-compatible), JSON mode `format: 'json'`, no auth. Model id comes from the resolved route (never invented). Errors surface via RequesterError canonical codes only (RATE_LIMITED/TIMEOUT/NETWORK) plus provider-level AUTH/MODEL_UNKNOWN/PROVIDER_5XX mapping for the §20.10 table — no invented codes (D-38). Use debugLog for instrumentation, never console.log; never log URLs containing a key. Run `pnpm run verify:phase-3` (tsc must pass).</action>
  <acceptance_criteria>tsc passes strict-clean (zero strict-suppression markers); all three providers implement the full ILLMProvider surface (stream + requestJson); JSON-mode request shapes present (response_format json_object / format json); no raw `fetch(` call in any provider file (grep-assertable, comment-filtered); errors carry canonical codes only.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>OpenAIProvider, OpenAICompatProvider, OllamaProvider compile strict-clean; each implements the full ILLMProvider surface; JSON-mode flags set natively per provider; all I/O via Requester.</done>
</task>

<task type="auto">
  <name>Task 2: Native-wire family — Anthropic + Gemini providers</name>
  <files>src/core/ai/providers/AnthropicProvider.ts, src/core/ai/providers/GeminiProvider.ts</files>
  <read_first>@src/core/http/Requester.ts (fetch wrapper + canonical codes) · @src/core/ai/ILLMProvider.ts (interface) · @src/services/aiProvider.ts lines 78-88 (auth-header pattern: x-api-key + anthropic-version) · 03-RESEARCH.md assumptions A2 (Gemini wire) + A6 (Anthropic JSON mode) + COVERAGE.md rows 2-3 (Anthropic/Gemini INTEGRATE)</read_first>
  <action>Create the two native-wire ILLMProvider implementations. `AnthropicProvider`: base `https://api.anthropic.com` (overridable), headers `x-api-key` + `anthropic-version: 2023-06-01` (match aiProvider.ts:78-88), POST {base}/v1/messages with `stream: true`; JSON-mode mechanism per A6 — confirm against the live API at implementation (tool-use-with-forced-choice or the provider's current structured-output parameter), and the conformance fixture must match the implemented shape. `GeminiProvider`: base `https://generativelanguage.googleapis.com`, POST {base}/v1beta/models/{model}:streamGenerateContent?alt=sse — auth via `key=` param per INTEGRATIONS.md BUT prefer header auth where the provider allows it (CONCERNS.md flags query-param leakage); JSON mode `responseMimeType: 'application/json'`. Both map RequesterError codes onto the §20.10 canonical set; AUTH on 401/403, PROVIDER_5XX on 5xx, MODEL_UNKNOWN on 404/400 model-not-found. Never log URLs containing a key; debugLog for instrumentation. Run `pnpm run verify:phase-3` (tsc must pass).</action>
  <acceptance_criteria>tsc passes strict-clean; AnthropicProvider sends x-api-key + anthropic-version headers; GeminiProvider uses responseMimeType 'application/json' for JSON mode; A6 JSON-mode mechanism chosen and recorded in a code comment; no raw `fetch(` call (grep-assertable); no key-bearing URL reaches any log call.</acceptance_criteria>
  <verify>
      <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>AnthropicProvider and GeminiProvider compile strict-clean with the native request shapes; JSON-mode flag set natively; A6 mechanism chosen and recorded in a code comment.</done>
</task>

<task type="auto">
  <name>Task 3: StreamAdapter expansion — Anthropic/Gemini/Ollama wire adapters + conformance fixtures</name>
  <files>src/core/ai/StreamAdapter.ts, tests/core/ai/StreamAdapter.test.ts, tests/core/ai/fixtures/</files>
  <read_first>@src/core/ai/StreamAdapter.ts (created 03-01 — the OpenAI adapter to extend; file being modified) · @src/core/ai/types.ts (canonical StreamEvent union) · 03-RESEARCH.md Code Examples lines 437-452 (exact wire bytes per provider) + Pitfall 1 + REQ-R09 (missing terminator = error) · 03-PATTERNS.md StreamAdapter section</read_first>
  <action>Extend `src/core/ai/StreamAdapter.ts` (created in 03-01) with per-provider parse adapters sharing the incremental `TextDecoder({stream:true})` line-buffer discipline: `parseAnthropicStream` — handles `event: <type>` + `data: {...}` pairs, emits text from `content_block_delta`/`text_delta`, terminates on `message_stop`, surfaces `error` events, tolerates dispersed `ping`; `parseGeminiStream` — reads `data: {"candidates":[{"content":{"parts":[{"text":"..."}]}}]}` inline chunks; `parseOllamaStream` — NDJSON/OpenAI-compat `data:` lines (A4: production path is the /v1 OpenAI-compatible shape). EVERY adapter: missing terminator → STREAM_ERROR (REQ-R09); STREAM_COMPLETE emitted on terminator; canonical StreamEvent union emitted throughout (D-47, correlated with the Phase-1 OperationId). Write `tests/core/ai/StreamAdapter.test.ts` + conformance fixtures in tests/core/ai/fixtures/ with the EXACT wire bytes from RESEARCH Code Examples (lines 437-452): OpenAI `data: {"choices":[{"delta":{"content":"Hello"},"finish_reason":null}]}` + `data: [DONE]`; Anthropic `event: content_block_delta` / `data: {"type":"content_block_delta","index":0,"delta":{"type":"text_delta","text":"ello frien"}}` + `event: message_stop`; Gemini `data: {"candidates":[{"content":{"parts":[{"text":"..."}]}}]}`; Ollama NDJSON. Cases per provider: happy path (text accumulated → STREAM_COMPLETE), missing terminator (STREAM_ERROR), empty stream (STREAM_ERROR — REQ-R09 "missing terminator = error"). The fixture files also seed the D-48 golden matrix (provider failure / fallback / abort cases referenced by plan 03-05's ProviderRouter tests). Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>StreamAdapter.test.ts green: 4 providers × 3 cases (happy → STREAM_COMPLETE with accumulated text; missing terminator → STREAM_ERROR; empty → STREAM_ERROR); exact wire bytes asserted in fixtures; `pnpm run verify:phase-3` green.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>StreamAdapter.test.ts green for all four providers × 3 cases; conformance fixtures carry the exact wire bytes; every adapter errors on a missing terminator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider HTTP response → StreamAdapter | Untrusted provider network bytes parsed into canonical events — malformed/truncated streams must not crash the UI context |
| provider request URL (Gemini) | API key travels in the request — must never reach logs/errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-06 | Information Disclosure | Gemini key= query param | high | mitigate | Prefer header auth where the provider allows (CONCERNS.md); NEVER debugLog/console.error a URL containing the key; error strings built from status + server body only (T-01-10 contract) — the plan's debugLog discipline + error-construction rule are the concrete controls |
| T-3-07 | Tampering | StreamAdapter wire parsing | medium | mitigate | Incremental TextDecoder({stream:true}) line buffer (CRLF + multi-byte boundaries); missing terminator = STREAM_ERROR; zod-validated canonical events — malformed provider output cannot inject structure into the pipeline |
| T-3-08 | Information Disclosure | error surfaces in providers | medium | mitigate | Requester canonical codes only; provider errors map 401→AUTH, 5xx→PROVIDER_5XX, 400/404-model→MODEL_UNKNOWN — never raw bodies with keys; no invented codes (D-38/§21.6) |
| T-3-09 | Tampering | SDK supply chain | low | accept | No SDKs imported (fetch-only, INTEGRATIONS.md) — the SDK attack surface is structurally absent |
</threat_model>

<verification>
- `pnpm run verify:phase-3` green after every task commit.
- Conformance fixtures are the verification spine: exact wire bytes per provider, 3 cases each (happy / missing terminator / empty).
- Grep guard: no SDK imports (`grep -rn "from 'openai'\|from 'anthropic'\|@google/genai" src/core/ai` → zero matches, comment-filtered).
</verification>

<success_criteria>
1. All five adapters implement ILLMProvider and stream real wire formats — the SSE rebuild (REQ-R09) is complete.
2. JSON-mode flag set natively per provider (Appendix L rule) — structured output works against every provider.
3. Conformance fixtures prove per-provider parsing including the missing-terminator error path.
4. No raw fetch, no SDK, no proxy-coupled parsing anywhere in src/core/ai.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-3 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/ai/providers/OpenAIProvider.ts | OpenAI Chat Completions adapter | **[this plan]** |
| src/core/ai/providers/OpenAICompatProvider.ts | OpenAI-wire self-hosted adapter (D-56) | **[this plan]** |
| src/core/ai/providers/OllamaProvider.ts | Ollama /v1 OpenAI-compatible adapter | **[this plan]** |
| src/core/ai/providers/AnthropicProvider.ts | Anthropic Messages adapter | **[this plan]** |
| src/core/ai/providers/GeminiProvider.ts | Gemini generateContent adapter | **[this plan]** |
| src/core/ai/StreamAdapter.ts | extended: 4 wire-family parse adapters (OpenAI from 03-01) | 03-01 + **[this plan]** |
| src/core/ai/types.ts / ILLMProvider.ts | spine types + interface | 03-01 |
| src/core/ai/StructuredOutput.ts / PlannerService.ts | Appendix L + §1.2 | 03-01 |
| src/core/prompts/index.ts | Appendix A PROMPTS | 03-01 |
| src/core/ai/persona/*, UserPreferences.ts | persona foundation | 03-02 |
| src/core/ai/ProviderRegistry.ts / TierResolver.ts / ProviderRouter.ts | routing stack (consumes these providers) | 03-05 |
| src/core/ai/PromptCacheManager.ts / PromptCacheAdapter.ts | prompt assembly + cache hints | 03-04 |
| src/core/ai/ExecutorService.ts / RendererService.ts / toolSchemas.ts / ChunkBuffer.ts | pipeline stages + tools + buffer | 03-04 |
| src/core/ai/AgentOrchestrator.ts | runAgentTurn (Appendix I) | 03-06 |
| src/core/runtime/workerState.ts | + ActiveStreamState | 03-04 |
| src/types/storage.ts / useChatStreaming.ts / OptionsPage.tsx | persistence + chat + options | 03-07 |
| tests/core/ai/** | test files + fixtures/ (conformance seed here) | all plans |