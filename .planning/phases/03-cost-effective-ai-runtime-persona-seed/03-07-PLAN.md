---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 07
type: execute
wave: 5
depends_on: [03-06]
files_modified:
  - src/types/storage.ts
  - src/core/storage/WriteJournal.ts
  - src/components/chat/useChatStreaming.ts
  - src/components/chat/SidepanelChat.tsx
  - src/components/options/OptionsPage.tsx
  - tests/core/ai/chat-integration.test.ts
read_only_discovery:
  - src/core/storage/ChatHistoryDB.ts
autonomous: false
requirements: [RICH-R-09]
user_setup:
  - service: "Live AI providers (OpenAI/Anthropic/Gemini/Ollama)"
    why: "End-to-end streaming smoke test of the re-pointed chat path (manual checkpoint) — keys are user-configured at runtime, never in the repo"
    env_vars: []
    dashboard_config:
      - task: "Configure at least one provider in Options (provider key + model tier assignment fast/balanced)"
        location: "Options → Providers / Options → General (Phase 3 endpoint-override + tier fields)"
  - service: "npm registry"
    why: "No new deps this plan — nothing required"

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Chat runs the pipeline: useChatStreaming.handleSend routes through AgentOrchestrator.runAgentTurn (D-44) — streamChatResponse is retired for production chat (kept only behind the DEMO_MODE+DEV gate, D-12)"
    - "Completed user/assistant message pairs persist to ChatHistoryDB at turn end via the WriteJournal 'append-chat-turn' operation (D-45); mid-stream chunks live in memory + ChunkBuffer only; abort drops the partial (nothing persisted)"
    - "The 'append-chat-turn' union member + zod schema + registered JournalStep exist (additive literal-union extension of the §20.3 11-member union — backward-compatible); ChatHistoryDB v1 schema fits the pair write unchanged (D-45a stop-condition does not trigger)"
    - "Options exposes per-provider endpoint overrides writing np_endpoint_overrides (D-50) and manual fast/balanced tier assignment writing UserPreferences (D-54), with first-setup pre-fill suggestions that are UI-only until persisted (D-54a)"
  artifacts:
    - path: "src/types/storage.ts"
      provides: "WriteJournalOperation + WriteJournalOperationSchema extended with 'append-chat-turn'"
      exports: ["WriteJournalOperation", "WriteJournalOperationSchema"]
    - path: "src/core/storage/WriteJournal.ts"
      provides: "createChatTurnSteps factory + registered JournalStep for 'append-chat-turn' (boot, mirroring update-workspace wiring at WriteJournal.ts:303)"
      exports: ["createChatTurnSteps", "registerJournalSteps"]
    - path: "src/components/chat/useChatStreaming.ts"
      provides: "D-44 re-point: handleSend → runAgentTurn with abortControllerRef threading, ChunkBuffer consumption, turn-end persist via the journaled append; the per-chunk store-update path is gone"
      exports: ["useChatStreaming"]
    - path: "src/components/options/OptionsPage.tsx"
      provides: "D-50 endpoint-override fields (np_endpoint_overrides) + D-54 tier assignment (fastModel/balancedModel) + first-setup pre-fill (UI-only)"
      exports: []
  key_links:
    - from: "src/components/chat/useChatStreaming.ts"
      to: "src/core/ai/AgentOrchestrator.ts"
      via: "handleSend calls runAgentTurn({ userInput, tier, prefs, abortSignal, persistTurn }) — the D-44 re-point"
      pattern: "runAgentTurn"
    - from: "src/components/chat/useChatStreaming.ts"
      to: "src/core/storage/WriteJournal.ts"
      via: "persistTurn runs the journaled 'append-chat-turn' op at turn end (D-45)"
      pattern: "append-chat-turn"
    - from: "src/components/options/OptionsPage.tsx"
      to: "src/core/ai/UserPreferences.ts"
      via: "tier-assignment fields write through to useUserPreferencesStore (np_preferences)"
      pattern: "setFastModel|setBalancedModel|setPersonaOverrides"
  prohibitions:
    - "No per-chunk persistence: the delta/flush path performs no chrome.storage or IndexedDB writes (P2 write-rate, D-45)"
    - "No production fallback to streamChatResponse — the legacy path is DEMO_MODE+DEV-only (D-44/D-12)"
    - "No new transcript store invented — Phase 3 consumes the existing ChatHistoryDB (D-45a)"
flagged_assumptions:
  - "RICH-R-09 (unclassified probe — resolved here): chat shares the persona with the agent because useChatStreaming routes through the same AgentOrchestrator; no second prompt path exists"
  - "Open Q1 resolution (D-45 'via the WriteJournal'): option (a) — additive 'append-chat-turn' union extension + registered JournalStep (research recommendation); ChatHistoryDB v1 schema fits the pair write, so D-45a's stop-condition does not trigger"
  - "Open Q3 resolution: adapt useChatStreaming.ts in place (D-44) — no second hook module created; the Appendix J.2 np_active_stream lifecycle pattern is applied inside the existing hook"
---

<objective>
Wire the pipeline into production chat and persistence: re-point `useChatStreaming` at `AgentOrchestrator` (D-44), persist completed turns to ChatHistoryDB via the journaled `append-chat-turn` operation (D-45), and add the Options surfaces D-50 (endpoint overrides) + D-54 (tier assignment with first-setup pre-fill). Ends with a human smoke checkpoint over the live chat path.

Purpose: Chat is the pipeline's production exercise — Planner, Executor, and Renderer all fire on ordinary chat turns from this plan on. Persistence honors the D-45 write-rate contract (turn-end journaled append, never per chunk).
Output: storage union extension + JournalStep registration + hook re-point + Options fields + chat-integration test.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §10.6 (endpoints + np_endpoint_overrides), §15.1 (np_providers/np_preferences), §20.3 (WriteJournal op union 3117-3130), Appendix J.2 (np_active_stream lifecycle)
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md  # D-44/D-45/D-45a/D-50/D-54/D-54a, Open Questions 1/3, Pitfall 2 (write-rate)
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md  # useChatStreaming/OptionsPage/storage.ts modification sections
@src/types/storage.ts  # the 11-member union (46-57) + zod schema (89-101) being extended additively
@src/core/storage/WriteJournal.ts  # registerJournalSteps (76-88), update-workspace boot wiring (303), createWorkspaceWriteSteps factory pattern (213)
@src/core/storage/ChatHistoryDB.ts  # v1 schema (24-53) — fits the pair write, NO schema change (D-45a)
@src/components/chat/useChatStreaming.ts  # current streamChatResponse call site (line 75) being replaced
@src/components/options/OptionsPage.tsx  # provider-modal proxy fields (194-253) being re-pointed to np_endpoint_overrides
@src/services/aiProvider.ts  # streamChatResponse — retired for production (DEMO_MODE+DEV only, D-12/D-44)
</context>

<tasks>

### Mandatory pre-implementation storage contract check

1. Read `src/core/storage/ChatHistoryDB.ts`, `src/core/storage/WriteJournal.ts`, and `src/types/storage.ts`.
2. Confirm the existing append API can atomically persist one completed user/assistant pair without changing the ChatHistoryDB schema.
3. Verify whether the authoritative §20.3 contract permits extending `WriteJournalOperation` and whether an exact repository-approved chat append identifier already exists.
4. Only if the exact identifier `append-chat-turn` is already authorized by the reviewed context/spec may the task add that literal and its journal-step registration.
5. If authorization or schema fit is absent, STOP this task and report the D-45a boundary conflict. Do not edit ChatHistoryDB, invent a second store, or substitute an unjournaled write.
6. Record inspected signatures and the go/stop result in the execution summary before coding.

<task type="auto">
  <name>Task 1: append-chat-turn — additive union extension + zod schema + JournalStep registration</name>
  <files>src/types/storage.ts, src/core/storage/WriteJournal.ts</files>
  <read_first>@src/types/storage.ts (lines 46-101 — the union + zod schema being extended; file being modified) · @src/core/storage/WriteJournal.ts (registerJournalSteps 76-88, createWorkspaceWriteSteps 213, update-workspace boot wiring 303) · @src/core/storage/ChatHistoryDB.ts (v1 schema 24-53 — fits the pair write, NO schema change) · 03-RESEARCH.md Open Question 1 (option (a) resolution) + D-45/D-45a</read_first>
  <action>Extend `src/types/storage.ts` additively (D-45/Open Q1 option (a)): add the literal `'append-chat-turn'` to the `WriteJournalOperation` union (line 46-57 region) AND to `WriteJournalOperationSchema` (line 89-101 region) — a backward-compatible literal-union extension; do not reorder or remove existing members. In `src/core/storage/WriteJournal.ts`: add `createChatTurnSteps(deps)` — a curried factory mirroring the `createWorkspaceWriteSteps` pattern (line 213) whose steps persist the completed user/assistant pair to ChatHistoryDB via `openChatHistoryDB()` (idb put into the 'messages' store with role 'user'/'assistant', timestamp, metadata — the v1 schema at ChatHistoryDB.ts:24-53 fits this; NO ChatHistoryDB schema change, so the D-45a stop-condition does NOT trigger). Register the step at boot via `registerJournalSteps('append-chat-turn', createChatTurnSteps(deps)())` alongside the update-workspace registration (line 303 region) so `isSupportedOperation('append-chat-turn')` returns true for replay (mirror the CR-01 pattern). Keep the D-33 metadata-only entry discipline (no message bodies inside the WriteJournalEntry itself — bodies live in ChatHistoryDB). Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>tsc passes; the union + schema contain 'append-chat-turn' (11→12 members, existing members untouched); isSupportedOperation('append-chat-turn') returns true at boot; a runJournaled 'append-chat-turn' entry persists the pair into ChatHistoryDB messages store and replays idempotently; zero strict-suppression markers.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>The 11-member union is now 12 with 'append-chat-turn' in both the type and the zod schema; the JournalStep is registered at boot; tsc passes with zero strict-suppression markers.</done>
</task>

<task type="auto">
  <name>Task 2: useChatStreaming re-point at AgentOrchestrator (D-44/D-45/D-47) + chat-integration test</name>
  <files>src/components/chat/useChatStreaming.ts, src/components/chat/SidepanelChat.tsx, tests/core/ai/chat-integration.test.ts</files>
  <read_first>@src/components/chat/useChatStreaming.ts (file being modified — current streamChatResponse call site line 75) · @src/components/chat/SidepanelChat.tsx (consumer — lines 63, 278) · @src/core/ai/AgentOrchestrator.ts (created 03-06 — runAgentTurn contract) · @.planning/PRODUCT_SPEC_v0_1.md Appendix J.2 (np_active_stream lifecycle) · 03-RESEARCH.md D-44/D-45/D-47 + Pitfall 2 (write-rate) + Open Q3</read_first>
  <action>Modify `src/components/chat/useChatStreaming.ts` (D-44, Open Q3 — adapt in place, do NOT create a second hook): replace the `streamChatResponse` call (line 75) with `AgentOrchestrator.runAgentTurn({ userInput, sessionId, operationId, tier, prefs, abortSignal, persistTurn })`. Keep `abortControllerRef` threading (lines 66, 96-103) — the AbortSignal flows into runAgentTurn (Appendix I aborts on it). Mid-stream: consume canonical stream events via ChunkBuffer (Appendix J.2 pattern) and update the assistant message from the buffer — NO per-chunk store-persist calls (P2 write-rate, D-45; the per-chunk store-update path is removed). At turn end: invoke the `persistTurn` callback wiring `createChatTurnSteps` (Task 1) → ChatHistoryDB; on abort, drop the partial assistant message (nothing persisted). Apply the Appendix J.2 `np_active_stream` session lifecycle (write chrome.storage.session on stream start, clear in finally — boot-recovery check: stale operationId for the same conversationId → surface interrupted state and remove the key). If `SidepanelChat.tsx` needs signature adjustments for the new hook return shape, update it minimally (it consumes isGenerating/handleSend/handleStopGenerating at lines 63, 278). Keep the DEMO_MODE+DEV-only retirement of streamChatResponse intact elsewhere (do NOT delete the legacy function — D-12 gate remains). Write `tests/core/ai/chat-integration.test.ts`: with a fixture provider, drive handleSend → assert (a) runAgentTurn was the path (the legacy streaming path was not invoked — the call-site removal is grep-assertable); (b) persistTurn invoked exactly once at turn end with the pair; (c) abort mid-stream → nothing persisted; (d) no per-chunk storage write occurred (mock storage write counter stays 0 during the stream). Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>chat-integration.test.ts green (4 case groups); grep (comment-filtered) shows the per-chunk store-update call absent from useChatStreaming.ts and the legacy streaming call-site absent from the production chat path; abort drops the partial (persist spy count 0); `pnpm run verify:phase-3` green.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>chat-integration.test.ts green; the per-chunk store-update call is gone from useChatStreaming (comment-filtered grep == 0); abort drops the partial; legacy streamChatResponse remains only behind the DEMO_MODE gate.</done>
</task>

<task type="auto">
  <name>Task 3: OptionsPage — D-50 endpoint overrides + D-54 tier assignment with first-setup pre-fill</name>
  <files>src/components/options/OptionsPage.tsx</files>
  <read_first>@src/components/options/OptionsPage.tsx (file being modified — provider-modal proxy fields 194-253) · @src/core/ai/UserPreferences.ts (created 03-02 — setFastModel/setBalancedModel store actions) · @src/core/ai/ProviderRegistry.ts (created 03-05 — getEndpointFor/getCachedModels read surface) · 03-RESEARCH.md D-50/D-52/D-54/D-54a + §10.6</read_first>
  <action>Modify `src/components/options/OptionsPage.tsx` per D-50/D-54. D-50: re-point the provider-modal proxy fields (lines 194-253 region) so saving a custom proxy writes the per-provider endpoint override to `np_endpoint_overrides` (chrome.storage.local) instead of only mutating the in-memory ProviderConfig — the runtime endpoint = np_endpoint_overrides[providerId] ?? §10.6 default (merged at ProviderRegistry hydrate, plan 03-05); localhost:12380 is never a canonical default (D-12). D-54: add fast/balanced tier assignment fields (write-through to `useUserPreferencesStore.setFastModel`/`setBalancedModel` → np_preferences) rendered from live-discovered models (`fetchProviderModels` semantics, D-52); model discovery populates the FAST and BALANCED selectors but does not classify, preselect, or persist either value. Both remain unset until the operator explicitly selects values and confirms Save. If an authoritative deterministic classifier is later supplied, it may add UI-only suggestions without changing the runtime null contract. These fields are additive to the existing General section (full Options redesign is Phase 15). Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>tsc passes; saving a provider proxy writes np_endpoint_overrides (assertable via the mock storage map in tests/setup.ts); tier-assignment fields write through to np_preferences (fastModel/balancedModel present in the mock storage map after save); the pre-fill code path contains no setFastModel/setBalancedModel call (grep-assertable — UI-only suggestion); `pnpm run verify:phase-3` green.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>Endpoint overrides persist to np_endpoint_overrides; tier assignment writes through to np_preferences; pre-fill suggestions never persist without confirmation (assertable: no setFastModel/setBalancedModel call inside the pre-fill path).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify: live chat streaming through the pipeline + Options fields + persist across reload</name>
  <what-built>Phase 3 chat integration: useChatStreaming now runs the Planner → Executor → Renderer pipeline via AgentOrchestrator; Options exposes endpoint overrides + tier assignment; turn-end persistence via the journaled append</what-built>
  <how-to-verify>
    1. Load the extension (pnpm dev:ext) with at least one live provider configured in Options.
    2. In Options → General: set the fast and balanced tier models for that provider (pre-fill suggestion appears on first setup — confirm it does not persist until you save). Set a per-provider endpoint override and confirm it sticks across reload (np_endpoint_overrides).
    3. Open the Side Panel chat, send a message, and confirm: streamed tokens render (no empty response — the SSE rebuild works against the REAL provider wire format); a second message in the same session shows the persona-consistent assistant behavior (name/tone/brevity).
    4. Abort mid-stream (Stop) and confirm the partial message is dropped, not persisted.
    5. Confirm the completed turn appears after reload (ChatHistoryDB turn-end persist via WriteJournal).
  <resume-signal>Type "approved" or describe what failed (paste the error + provider used)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useChatStreaming → runAgentTurn | Chat user input crosses into the pipeline (same boundary as 03-06) |
| persistTurn → ChatHistoryDB | Completed turns cross into IndexedDB — the write-rate + crash-safety boundary |
| OptionsPage → np_endpoint_overrides / np_preferences | Operator config crosses into chrome.storage.local — zod-validated at write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-22 | DoS | per-chunk persistence regression | high | mitigate | The per-chunk store-update call is REMOVED (grep-gated); mid-stream chunks live in memory + ChunkBuffer only; persistTurn is journaled and runs once at turn end (D-45) — the P2 write-rate risk is structurally closed, asserted by the chat-integration test (d) |
| T-3-23 | Tampering | journaled turn-end persist | medium | mitigate | append-chat-turn runs through runJournaled (replay-safe, D-45); abort drops the partial (nothing enqueued); D-33 metadata-only entries keep bodies out of the journal |
| T-3-24 | Tampering | endpoint override injection | medium | mitigate | np_endpoint_overrides values zod-validated (http/https URLs only) at the Options write AND at registry hydrate (03-05) — a malformed value cannot reach the fetch layer |
| T-3-25 | Information Disclosure | legacy streamChatResponse | low | accept | Retained ONLY behind the DEMO_MODE+DEV gate (D-12/D-44) — unreachable in production builds; production chat has no fallback to the proxy-coupled path |
</threat_model>

<verification>
- `pnpm run verify:phase-3` green after every task commit.
- Grep gates (comment-filtered): per-chunk store-update call absent from useChatStreaming.ts; the legacy streaming call-site absent from the production chat path.
- Chat-integration test asserts: pipeline path used, persist once at turn end, abort drops, zero per-chunk writes.
- Human checkpoint verifies the live-provider smoke (streaming against a REAL provider wire format, Options fields, persist-across-reload).
</verification>

<success_criteria>
1. Chat switches to the pipeline (D-44) — both chat surfaces stream through AgentOrchestrator.
2. Turn-end persist via journaled append-chat-turn; abort drops partial (D-45) — no per-chunk writes.
3. Options endpoint overrides + tier assignment work; pre-fill is UI-only until persisted (D-50/D-54/D-54a).
4. Live smoke checkpoint passes: real provider streams text, persona-consistent behavior, persist across reload.
5. `pnpm run verify:phase-3` green.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-3 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/types/storage.ts | + 'append-chat-turn' (type + zod schema) | **[this plan]** |
| src/core/storage/WriteJournal.ts | + createChatTurnSteps + boot registration | **[this plan]** |
| src/components/chat/useChatStreaming.ts | re-point at runAgentTurn (D-44) | **[this plan]** |
| src/components/chat/SidepanelChat.tsx | minimal hook-shape adaptation | **[this plan]** |
| src/components/options/OptionsPage.tsx | D-50 endpoint overrides + D-54 tier assignment | **[this plan]** |
| src/core/ai/types.ts / ILLMProvider.ts / StreamAdapter.ts / StructuredOutput.ts / PlannerService.ts / prompts/index.ts | spine | 03-01 |
| src/core/ai/persona/*, UserPreferences.ts | persona foundation (Options writes prefs here) | 03-02 |
| src/core/ai/providers/*.ts | 5 providers | 03-03 |
| src/core/ai/PromptCacheManager.ts / PromptCacheAdapter.ts / ChunkBuffer.ts / toolSchemas.ts / ExecutorService.ts / RendererService.ts | prompt assembly + stages | 03-04 |
| src/core/runtime/workerState.ts | + ActiveStreamState | 03-04 |
| src/core/ai/ProviderRegistry.ts / TierResolver.ts / ProviderRouter.ts | routing stack | 03-05 |
| src/core/ai/AgentOrchestrator.ts | runAgentTurn (consumed here) | 03-06 |
| tests/core/ai/** | test files + fixtures/ | all plans |