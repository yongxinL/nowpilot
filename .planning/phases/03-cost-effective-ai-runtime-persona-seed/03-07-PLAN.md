---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 07
type: execute
wave: 4
depends_on: ["03-01", "03-03"]
files_modified:
  - src/core/ai/persona/PersonaProfile.ts
  - src/core/ai/persona/personaConfig.ts
  - src/core/ai/persona/PersonaInjector.ts
  - src/core/ai/contextHelper.ts
  - tests/core/ai/persona/PersonaProfile.test.ts
  - tests/core/ai/persona/PersonaInjector.test.ts
autonomous: true
requirements: [AI-05]

must_haves:
  truths:
    - "PersonaProfileSchema (Appendix N.1 verbatim) + DEFAULT_PERSONA ship; the np_persona accessor (D-09) is a small Setting.ts-backed reader (key ALREADY registered area:'local' at Setting.ts line 67 — NO Setting.ts modification), PersonaProfileSchema-validated; empty/invalid key → PERSONA_LOAD_FAILED → DEFAULT_PERSONA fallback, never a crash, never a blocked Sender."
    - "Persona is read-only this phase (D-10): the accessor injects into PersonaInjector as a CONFIG PROVIDER (not imported); Phase 5's PreferenceMemoryStore writer swaps only the injected provider."
    - "PersonaInjector (Appendix N.2 verbatim): resolvePersona merges prefs.personaOverrides deterministically; buildPersonaBlock uses a fixed template with ordered joins → byte-stable per persona (prompt caching preserved, §1.3/RICH-R-02); inject(stage, baseSystem, opts) accepts all 4 stages (planner/executor/renderer/memoryExtractor — D-11: memoryExtractor accepted + unit-tested, call site Phase 5) and prepends the persona block INSIDE the cached [SYSTEM]."
    - "contextHelper (D-02, +1 documented to §18, Phase-4 deletion target) builds the §2.3-shaped OptimizedContext from Phase-3 inputs (operationId, tier, budgets, userInput, persona block, tool schema refs) — Golden Rule 3: NO React-side prompt assembly anywhere; the [SYSTEM] section carries the byte-stable persona block (cache-eligible)."
    - "UserPreferences.personaOverrides (name/tone/brevity) apply without a code change (§18 DONE-when); changing np_persona changes behavior by data, not code (R-7/R-2 — persona is user config in np_persona, never the fact store)."
  artifacts:
    - src/core/ai/persona/PersonaProfile.ts
    - src/core/ai/persona/personaConfig.ts
    - src/core/ai/persona/PersonaInjector.ts
    - src/core/ai/contextHelper.ts
    - tests/core/ai/persona/{PersonaProfile,PersonaInjector}.test.ts
  key_links:
    - "np_persona accessor (Setting.ts read) → PersonaInjector config provider → byte-stable [SYSTEM] section in contextHelper-built OptimizedContext."
    - "contextHelper output feeds runAgentTurn (03-06) and the hook (03-08); its deletion target is recorded (D-02 — Phase 4 ContextOptimizer replaces it)."
    - "PromptCacheAdapter (03-03) hashes the stable [SYSTEM] section — byte-stability is the cache-hit invariant."
  flagged_assumptions:
    - "AI-05 [adjacency]: fallback-chain ordering is the Appendix D candidate order (cheapest first); configured providers sort by priority with JS stable-sort — equal-priority providers resolve first-registered (documented; no dedupe in Phase 3)."
    - "AI-05 [empty]: zero configured providers → resolveTier null → the Router surfaces the unconfigured terminal state; PersonaInjector still works (DEFAULT_PERSONA) — persona is independent of provider config."
    - "AI-05 [encoding]: byte-stability is UTF-16 JS string equality; buildPersonaBlock joins with '\n' in fixed template order — no locale/whitespace normalization."
    - "AI-05 [ordering]: persona block field order is the fixed N.2 template; personaOverrides merge is deterministic (name/tone/brevity only)."
    - "AI-05 [idempotency]: resolvePersona/buildPersonaBlock are pure — same (base, prefs) always produce the same block (hash-equality tested)."
    - "AI-05 [concurrency]: the np_persona read is a single chrome.storage.local read per session (Setting cache); no concurrent writer exists in Phase 3 (D-10 read-only)."
  prohibitions:
    - "No persona writes this phase (D-10 read-only; the writer is Phase 5 PreferenceMemoryStore)."
    - "No Setting.ts registry change for np_persona (already registered, area 'local')."
    - "No persona in the fact store (R-7 — np_persona only)."
    - "No unstable serialization (Pitfall 5 — ordered joins, fixed template)."
    - "No persona error UI (UI-SPEC: PERSONA_LOAD_FAILED → silent DEFAULT_PERSONA)."
    - "No React-side prompt assembly (Golden Rule 3 — contextHelper is the only builder)."
---

<objective>
The persona pipeline (AI-05): `PersonaProfile` (Appendix N.1) + the `np_persona` Setting-backed accessor (D-09), `PersonaInjector` (Appendix N.2, byte-stable 4-stage injection), and the D-02 `contextHelper` (§2.3-shaped OptimizedContext builder). Persona-aware from day one with prompt-cache-preserving byte-stability.

Purpose: "UserPreferences.personaOverrides (name/tone/brevity) apply without a code change" (§18 DONE-when). The persona block lives inside the cached [SYSTEM] and must be byte-identical per persona so provider prompt caches keep hitting (§1.3, RICH-R-02). The contextHelper enforces Golden Rule 3 in the UI path.
Output: Persona modules + accessor + contextHelper + their §18 test files (byte-stability hash-equality, stage coverage, DEFAULT_PERSONA fallback, §2.3 shape determinism).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/PRODUCT_SPEC_v0_1.md
@src/core/ai/types.ts
@src/core/storage/Setting.ts
@src/core/prompts/index.ts
@tests/fixtures/optimizedContext.ts
</context>

<tasks>

<!-- planner-discipline-allow: PROMPTS -->
<task type="auto" tdd="true">
  <name>Task 1: PersonaProfile (N.1) + np_persona accessor (D-09) + test</name>
  <files>src/core/ai/persona/PersonaProfile.ts, src/core/ai/persona/personaConfig.ts, tests/core/ai/persona/PersonaProfile.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md Appendix N.1 (line ~6036 — PersonaProfileSchema + DEFAULT_PERSONA VERBATIM)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md D-09 (accessor contract: Setting-backed, PersonaProfileSchema-validated, injected as config provider not imported) + D-10 (read-only, zero-change handoff) + D-11
    - src/core/storage/Setting.ts (settingRead signature + sanitize→null→fallback gate, lines 177-203; np_persona already registered at line 67 — NO registry change)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md "persona/PersonaProfile.ts" (Setting.ts validation-gate shape)
    - src/core/error/errorCodes.ts (PERSONA_LOAD_FAILED)
  </read_first>
  <behavior>
    - Test 1: a valid PersonaProfile object round-trips PersonaProfileSchema.safeParse.
    - Test 2: an invalid object (missing identity.name, bad tone enum) FAILS safeParse.
    - Test 3: DEFAULT_PERSONA is a valid profile and byte-stable (deep-equal to itself; the literal matches Appendix N.1).
    - Test 4: readPersona() with np_persona holding a valid profile (fakeBrowser) returns that profile.
    - Test 5: readPersona() with an empty/missing/invalid np_persona value returns DEFAULT_PERSONA and logs PERSONA_LOAD_FAILED (debugLog spy) — never throws.
    - Test 6: readPersona() returns a partial UserPreferences { personaOverrides } shape usable by resolvePersona (03-02 dependency: accessor returns UserPreferences | undefined).
  </behavior>
  <action>
    Create src/core/ai/persona/PersonaProfile.ts — Appendix N.1 VERBATIM: PersonaProfileSchema (z.object with identity/personalityCore/behavioralDrivers/languageStyle/emotionalRepertoire — exact field constraints), `export type PersonaProfile = z.infer<typeof PersonaProfileSchema>`, DEFAULT_PERSONA (verbatim literal — do not paraphrase).
    Create src/core/ai/persona/personaConfig.ts (+1 documented to §18 — D-09 accessor):
    - `export async function readPersona(): Promise<PersonaProfile>` — Setting.ts-backed read of the canonical 'np_persona' key (area 'local'; Setting registry ALREADY has it — do NOT touch Setting.ts); sanitize via PersonaProfileSchema.safeParse (sanitize→null→fallback pattern); invalid/empty → debugLog(ERROR_CODES.PERSONA_LOAD_FAILED, …) → DEFAULT_PERSONA; never throws (never-throw adapter precedent).
    - `export async function readPersonaPrefs(): Promise<UserPreferences | undefined>` — reads np_persona and returns the { personaOverrides } slice in a UserPreferences-shaped value (types.ts seed, 03-01) so PersonaInjector/contextHelper consumers receive config via injection (D-09).
    Header documents: injected as a CONFIG PROVIDER into PersonaInjector (never imported by it); read-only (D-10); Phase 5 PreferenceMemoryStore writer swaps only the injected provider.
    Create tests/core/ai/persona/PersonaProfile.test.ts per the behavior block (fakeBrowser + seeded np_persona; debugLog spy; fixtures from tests/fixtures).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/persona/PersonaProfile.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass; DEFAULT_PERSONA literal matches Appendix N.1 byte-for-byte (no paraphrase).
    - grep -c "np_persona" src/core/storage/Setting.ts unchanged from its current single registration (no new registry entries added — verify the diff touches only persona/*).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Persona schema + DEFAULT_PERSONA + Setting-backed accessor (fallback, never-throw) test-green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PersonaInjector (N.2) + contextHelper (D-02) + test</name>
  <files>src/core/ai/persona/PersonaInjector.ts, src/core/ai/contextHelper.ts, tests/core/ai/persona/PersonaInjector.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md Appendix N.2 (line ~6075 — resolvePersona/buildPersonaBlock/PersonaInjector VERBATIM)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md D-02 (contextHelper contract + Phase-4 deletion target) + D-07 (OptimizedContext seed) + D-11 (memoryExtractor stage)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Open Question 2 (UserPreferences home — types.ts seed, this plan's consumer) + Pitfall 5 (persona drift)
    - src/core/ai/types.ts (OptimizedContext, PromptSection, UserPreferences, ContextProvenanceManifest)
    - src/core/prompts/index.ts (byte-stable constants header discipline)
  </read_first>
  <behavior>
    - Test 1: buildPersonaBlock(DEFAULT_PERSONA) is deterministic — two calls are identical strings; the block starts with 'You are NowPilot —'.
    - Test 2: byte-stability across stages: inject('planner', PROMPTS.planner.system, { persona }) and inject('renderer', PROMPTS.renderer.system, { persona }) each are byte-stable across repeated calls, and a changed persona (different name) changes both blocks.
    - Test 3: overrides apply: resolvePersona(DEFAULT_PERSONA, { personaOverrides: { name: 'Chip', tone: 'concise', brevity: 'detailed' } }) → identity.name 'Chip', languageStyle.tone 'concise', brevity 'detailed'; unchanged fields stay DEFAULT.
    - Test 4: all 4 stages accepted: inject('memoryExtractor', PROMPTS.memoryExtractor.system, …) and inject('executor', '', …) produce persona-prepended output (D-11).
    - Test 5: hash-equality via PromptCacheAdapter.hashStableSections over the composed [SYSTEM] section — same persona → same hash across planner+renderer calls; persona change → hash changes (Pitfall 5 invariant).
    - Test 6: buildOptimizedContext (contextHelper) produces a §2.3-shape: tier, inputBudget, outputBudget, sections (system carrying the persona block + task + user_input), provenance, minimalMode false; deterministic (two calls deep-equal).
  </behavior>
  <action>
    Create src/core/ai/persona/PersonaInjector.ts — Appendix N.2 VERBATIM: `export type PipelineStage = 'planner' | 'executor' | 'renderer' | 'memoryExtractor'`; resolvePersona(base, prefs?) (deterministic overrides merge — name/tone/brevity); buildPersonaBlock(p) (fixed template, ordered joins — byte-stable); `export const PersonaInjector = { inject(stage, baseSystem, opts?: { persona?: PersonaProfile; prefs?: UserPreferences }): string }` returning `${block}\n\n${baseSystem}` (persona first — cacheable, then canonical stage system).
    Create src/core/ai/contextHelper.ts (+1 documented to §18 — D-02; header MUST record: Phase-4 deletion target — ContextOptimizer replaces this helper):
    - `export interface ContextHelperInput { operationId: string; tier: ModelContextTier; userInput: string; persona: PersonaProfile; preferences?: UserPreferences; toolSchemas: ToolSchemaRef[] }`.
    - `export function buildOptimizedContext(input: ContextHelperInput): OptimizedContext` — builds the §2.3 shape: tier, inputBudget/outputBudget from the §2.2 formula (inputBudget = floor(modelContextWindow * 0.70), outputBudget = floor(modelContextWindow * 0.20) with per-tier windows: tiny 4096 / small 16384 / medium 131072 / large 200000), sections = [system: PersonaInjector-built persona block — wait, the persona block alone is the stable [SYSTEM] core; the stage-specific PROMPTS composition happens in the stage services (03-06) so the CACHED [SYSTEM] stays byte-stable per persona across stages. Set sections[system] = buildPersonaBlock(persona) (stable: true, sourceId 'persona'), sections[tool_schemas] = sorted stable tool names, sections[task] + sections[user_input] = the user input (stable: false)], provenance (minimal ContextProvenanceManifest with the section entries), minimalMode false.
    Header documents: Golden Rule 3 — this is the ONLY context builder on the UI path (no React-side prompt assembly); PROMPTS constants are never imported by the hook (03-08).
    Create tests/core/ai/persona/PersonaInjector.test.ts per the behavior block (node env; uses PromptCacheAdapter.hashStableSections for the hash-equality invariants; buildOptimizedContextFixture where a full context is needed).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/persona/PersonaInjector.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass — byte-stability hash-equality across stages, overrides apply, 4 stages accepted, §2.3 shape determinism.
    - grep -c "PROMPTS" src/components/pages/useStreamingLLM.ts == 0 (verified in 03-08 — prompt assembly stays in core; this plan's grep: grep -c "PROMPTS" src/core/ai/contextHelper.ts == 0 — the helper receives the persona block, it does not import PROMPTS itself).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Byte-stable 4-stage persona injection + §2.3 context builder, test-proven (persona-aware from day one).</done>
</task>

</tasks>

## Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-05 [adjacency, empty, encoding, ordering, idempotency, concurrency] — ALL recorded in `must_haves.flagged_assumptions`. Every other edge stays with its owner (never silently dropped).

## Artifacts This Phase Produces

- src/core/ai/persona/PersonaProfile.ts (new): `PersonaProfileSchema`, `PersonaProfile`, `DEFAULT_PERSONA` (Appendix N.1).
- src/core/ai/persona/personaConfig.ts (new, +1 documented): `readPersona()`, `readPersonaPrefs()` (np_persona Setting-backed accessor, D-09).
- src/core/ai/persona/PersonaInjector.ts (new): `PipelineStage`, `resolvePersona()`, `buildPersonaBlock()`, `PersonaInjector.inject()` (Appendix N.2).
- src/core/ai/contextHelper.ts (new, +1 documented, Phase-4 deletion target): `ContextHelperInput`, `buildOptimizedContext()`.
- tests/core/ai/persona/PersonaProfile.test.ts, tests/core/ai/persona/PersonaInjector.test.ts (new).

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| np_persona storage → accessor → injector | User config crosses here; invalid/empty must degrade silently to DEFAULT_PERSONA (never crash, never block the Sender) |
| [USER INPUT] → cached [SYSTEM] | User input must never interpolate into the cached persona/[SYSTEM] section (injection surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07-01 | Tampering | persona/prompt injection | high | mitigate | buildPersonaBlock is a fixed template over schema-validated fields ONLY; user input never enters [SYSTEM] (contextHelper puts it in task/user_input sections, stable: false — AI-SPEC §4b prompt discipline); a persona-injection attempt changes only [USER INPUT], never the cached prefix (adversarial fixture in 03-07 tests via hash-change-only-on-persona-change assertion) |
| T-03-07-02 | (cost) | persona drift breaking caches | medium | mitigate | resolvePersona/buildPersonaBlock pure + deterministic; byte-stability hash-equality across stages and turns (behavior tests 1,2,5) — drift would kill provider prompt caches (Pitfall 5, RICH-R-02) |
| T-03-07-03 | Information Disclosure | np_persona read path | low | accept | Persona is non-secret user config in chrome.storage.local (area 'local'); no vault involvement; read-only this phase (D-10) |
| T-03-07-SC | Tampering | npm installs | high | mitigate | No new packages this plan |
</threat_model>

<verification>
- `npx vitest run tests/core/ai/persona` green (both test files).
- `npx tsc --noEmit` green.
- D-02 discipline grep: no PROMPTS import in contextHelper.ts; the only [SYSTEM]-building paths are core/ai (grep 'PersonaInjector' outside src/core/ai — zero until 03-08's hook which receives prefs via config, not prompt assembly).
- Full AI suite so far: `npx vitest run tests/core/ai` (wave 1-4 tests together).
</verification>

<success_criteria>
- Persona overrides apply without a code change; block byte-stable per persona (hash-equality proven).
- contextHelper produces the §2.3 shape deterministically; Golden Rule 3 holds on the UI path.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-07-SUMMARY.md` when done
</output>
