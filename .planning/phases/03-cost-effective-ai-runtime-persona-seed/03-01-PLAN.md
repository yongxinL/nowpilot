---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/ai/types.ts
  - src/core/ai/ILLMProvider.ts
  - src/core/ai/StreamAdapter.ts
  - src/core/ai/StructuredOutput.ts
  - src/core/ai/PlannerService.ts
  - src/core/prompts/index.ts
  - package.json
  - pnpm-lock.yaml
  - tests/core/ai/PlannerService.test.ts
  - tests/core/ai/StructuredOutput.test.ts
  - tests/core/ai/fixtures/
autonomous: true
requirements: [RICH-R-10]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PlannerService returns a zod-validated PlannerDecision parsed from real OpenAI SSE wire bytes (fixture) flowing through StreamAdapter → FixtureProvider → StructuredOutput.requestJson"
    - "Malformed JSON decisions are repaired exactly once via PROMPTS.repairJson.system; a second failure throws terminal code STRUCTURED_OUTPUT_FAILED with retryable false (Appendix L)"
    - "The planner stage system prompt comes from the canonical Appendix A PROMPTS.planner.system — persona-free and byte-stable (persona is prepended later by PromptCacheManager, D-59)"
    - "pnpm run verify:phase-3 is green: tsc --noEmit passes with no new strict-suppression markers and vitest run tests/core/ai passes"
  artifacts:
    - path: "src/core/ai/types.ts"
      provides: "Spine types: ProviderId, ModelTier, PlannerDecisionSchema (discriminated union), canonical StreamEvent union (D-47), PromptSection, ToolExecutionResult, RouterAttemptState"
      exports: ["ProviderId", "ModelTier", "PlannerDecisionSchema", "PlannerDecision", "StreamEvent", "PromptSection", "ToolExecutionResult", "RouterAttemptState"]
    - path: "src/core/ai/ILLMProvider.ts"
      provides: "Provider interface: stream(request, signal) → canonical events; requestJson for structured output (JSON-mode flag set natively per provider)"
      exports: ["ILLMProvider", "LLMStreamRequest", "LLMJsonRequest"]
    - path: "src/core/ai/StreamAdapter.ts"
      provides: "SSE rebuild (REQ-R09): incremental TextDecoder({stream:true}) line buffer + OpenAI wire adapter; missing terminator = error"
      exports: ["createStreamAdapter", "parseOpenAIStream", "StreamAdapter"]
    - path: "src/core/ai/StructuredOutput.ts"
      provides: "Appendix L requestJson: zodToJsonSchema + exactly one repair + terminal STRUCTURED_OUTPUT_FAILED"
      exports: ["requestJson", "StructuredOutputContext", "STRUCTURED_OUTPUT_FAILED"]
    - path: "src/core/ai/PlannerService.ts"
      provides: "§1.2 planner: fast tier, 3s timeout, decision via StructuredOutput, closed toolName enum supplied by ExecutorService"
      exports: ["PlannerService", "PlannerInput", "plan"]
    - path: "src/core/prompts/index.ts"
      provides: "Appendix A canonical prompts verbatim (planner/renderer/memoryExtractor/conversationSummarizer/repairJson); replaces the non-canonical repairJson stub"
      exports: ["PROMPTS"]
  key_links:
    - from: "src/core/ai/PlannerService.ts"
      to: "src/core/ai/StructuredOutput.ts"
      via: "plan() calls requestJson(PlannerDecisionSchema, ...) for the decision parse"
      pattern: "requestJson\\(PlannerDecisionSchema"
    - from: "src/core/ai/StructuredOutput.ts"
      to: "src/core/prompts/index.ts"
      via: "repair prompt = PROMPTS.repairJson.system + schema + broken JSON (Appendix L)"
      pattern: "PROMPTS\\.repairJson\\.system"
    - from: "src/core/ai/StreamAdapter.ts"
      to: "src/core/ai/types.ts"
      via: "emits the canonical StreamEvent union (STREAM_START/DELTA/COMPLETE/ERROR/ABORTED, D-47)"
      pattern: "STREAM_DELTA|StreamEvent"
  prohibitions:
    - "No new strict-mode suppression comments in any new file (NP_STRICT_CEILING 0, STATE.md decision 17)"
    - "No substitution of Zod 4 native z.toJSONSchema() for zod-to-json-schema 3.25.2 (Appendix L implementer note — deferred v0.2 cleanup)"
    - "No reuse of the proxy-coupled SSE parsing pattern that reads data.textChunk/data.thoughtChunk"

flagged_assumptions:
  - "RICH-R-10 (unclassified probe): per-stage persona-consistent system prompts are delivered by the D-59 choke-point (PromptCacheManager, plan 03-04) prepending the persona block FIRST inside [SYSTEM]; this plan supplies the canonical persona-free stage strings only"
  - "RICH-R-01 (empty/encoding probe): DEFAULT_PERSONA and the persona block are seeded verbatim (Appendix N.1) in plan 03-02; byte-stability is exact serialized-block equality — the tracer stage strings never interpolate volatile values"
---

**Zero-tool runtime specialization:** Preserve the authoritative three-variant base `PlannerDecisionSchema`. Add a runtime schema-construction contract that receives the registered tool-name list. When the list is empty, the production planner schema contains only `answer` and `ask_clarification`; never construct `z.enum([])` and never leave `toolName` as an unrestricted production string. The base `run_tool` variant remains available for direct Executor contract tests and later tool-owning phases.

<objective>
Prove the AI-runtime's riskiest architectural path end-to-end with a production-quality tracer slice: **real OpenAI SSE wire bytes → StreamAdapter → ILLMProvider (fixture) → StructuredOutput → PlannerService → zod-validated PlannerDecision**, with the canonical Appendix A prompts and the §21.6 closed error-code discipline in place.

Purpose: The SSE rebuild (REQ-R09) and the one-shot JSON repair (Appendix L) are the two confirmed-drift risks of this phase (the current parser returns empty text on real providers). Proving them on a single wire-through-decision path first catches an architectural dead-end after one commit instead of after ten. Everything this plan creates is the skeleton later expansion plans build on — the canonical event union (D-47), the provider interface, and the decision contract are written for keeps.
Output: `src/core/ai/types.ts`, `ILLMProvider.ts`, `StreamAdapter.ts`, `StructuredOutput.ts`, `PlannerService.ts`, canonical `src/core/prompts/index.ts`, plus `PlannerService.test.ts` / `StructuredOutput.test.ts` and the D-48 fixture library seed.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §1.2 (PlannerDecisionSchema, 272-286), §1.3 (section order), Appendix A (4092-4153), Appendix L (5848-5902)
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md  # Pattern 3, Pitfall 1, Code Examples, assumptions A3/A8
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md  # PlannerService/StructuredOutput/StreamAdapter analog sections
@src/core/http/Requester.ts  # canonical codes RATE_LIMITED|TIMEOUT|NETWORK, request() wrapper
@src/services/aiProvider.ts  # lines 405-446: the proxy-coupled SSE parser being replaced (negative analog)
@src/core/prompts/index.ts  # the 4-line stub being expanded
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 toolchain — install node_modules + zod-to-json-schema, baseline verify:phase-3</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>@package.json (the file being modified by pnpm) · 03-RESEARCH.md "Environment Availability" + "Package Legitimacy Audit" sections · CLAUDE.md "Build & Development"</read_first>
  <action>Run `pnpm install` (node_modules is currently ABSENT — RESEARCH.md Environment Availability) then `pnpm add zod-to-json-schema@3.25.2` (the only new dependency; registry-verified OK per the Package Legitimacy Audit — StefanTerdell repo, no postinstall, peerDeps `zod ^3.25.28 || ^4` compatible with installed zod ^4.4.3). Do NOT add any other dependency. Then run `pnpm run verify:phase-3` — it must pass: `tsc --noEmit` green (existing code) and `vitest run tests/core/ai tests/core/ai/persona` exits 0 with the existing `tests/core/ai/testProviderConnection.test.ts` remaining green — the eight §18 Phase-3 test files listed by the authoritative spec are additional files delivered in tasks 2-3. Do not modify any source file in this task.</action>
  <acceptance_criteria>node_modules/ directory exists; package.json dependencies contains "zod-to-json-schema": "^3.25.2"; `node -e "require('zod-to-json-schema')"` resolves; `pnpm run verify:phase-3` exits 0 (tsc clean) — `tests/core/ai/testProviderConnection.test.ts` already exists and MUST remain green; the eight §18 Phase-3 test files listed by the authoritative spec are additional files delivered by these plans.</acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!(p.dependencies&&p.dependencies['zod-to-json-schema'])) process.exit(1)" && node -e "require('zod-to-json-schema'); console.log('resolvable')" && pnpm run verify:phase-3</automated>
  </verify>
  <done>node_modules exists; zod-to-json-schema@3.25.2 is in package.json dependencies and resolves; `pnpm run verify:phase-3` exits 0.</done>
</task>

<task type="tracer">
  <name>Task 2: End-to-end planner slice — OpenAI wire bytes to a valid PlannerDecision</name>
  <files>src/core/ai/types.ts, src/core/ai/ILLMProvider.ts, src/core/ai/StreamAdapter.ts, src/core/ai/StructuredOutput.ts, src/core/ai/PlannerService.ts, src/core/prompts/index.ts, tests/core/ai/fixtures/openai-stream.ts, tests/core/ai/fixtures/FixtureProvider.ts, tests/core/ai/PlannerService.test.ts</files>
  <read_first>@src/core/prompts/index.ts (the 4-line stub being expanded — file being modified) · @.planning/PRODUCT_SPEC_v0_1.md §1.2 (PlannerDecisionSchema 272-286), Appendix A (4092-4153), Appendix L (5848-5902) — verbatim sources · 03-RESEARCH.md Code Examples (locked shapes) + Pattern 3 (Appendix L) + Pitfall 1 (SSE) · 03-PATTERNS.md PlannerService/StructuredOutput/StreamAdapter sections · @src/services/aiProvider.ts lines 405-446 (the proxy-coupled parser being replaced — negative analog) · @src/core/http/Requester.ts (canonical codes + request wrapper)</read_first>
  <action>Wire ONE entry point through every layer of the decision path. Create `src/core/ai/types.ts` with the spine: `ProviderId = 'openai' | 'anthropic' | 'gemini' | 'ollama' | 'openai-compat'` (spec §8.5 line 208 + D-56; disk 'claude' maps to 'anthropic' at the registry normalization boundary, plan 03-05); `ModelTier = 'fast' | 'balanced'`; `PlannerDecisionSchema` as the §1.2 discriminated union verbatim (action 'answer' | 'run_tool' | 'ask_clarification' with the exact field caps from spec 272-286); the canonical `StreamEvent` union STREAM_START/STREAM_DELTA/STREAM_COMPLETE/STREAM_ERROR/STREAM_ABORTED (D-47 — correlate with Phase-1 OperationId, no new id scheme); `PromptSection {kind, text, stable, tokens}` (A8 contract — exact field names so Phase-5 ContextOptimizer adopts it); `ToolExecutionResult<T>`; `RouterAttemptState` (spec 377-383). Zod-validate every cross-boundary shape (CLAUDE.md convention). Create `src/core/ai/ILLMProvider.ts` with `stream(request, signal)` returning an async-iterable/emitter of canonical events and `requestJson(prompt, jsonSchema, signal)` for JSON-mode calls — the interface MUST let StreamAdapter normalize per-provider wire formats; the legacy onChunk/onDone callback surface is NOT part of it (D-47). Create `src/core/ai/StreamAdapter.ts` with an incremental `TextDecoder({stream:true})` line-buffer SSE parser (REQ-R09) and the OpenAI wire adapter: parse `data: {"choices":[{"delta":{"content":"..."}}]}` lines, terminate on `data: [DONE]`, MISSING terminator = STREAM_ERROR (REQ-R09 contract). Create `src/core/prompts/index.ts` replacing the stub: add the Appendix A canonical prompt constants verbatim (planner, renderer, memoryExtractor, conversationSummarizer, repairJson from spec 4092-4153) — `repairJson.system` MUST be the canonical Appendix A text, NOT the current stub's paraphrase; stage strings stay persona-free and byte-stable per the Appendix A note at spec 4153. Create `src/core/ai/StructuredOutput.ts` per Appendix L verbatim: `requestJson<T>(schema, prompt, ctx)` = zodToJsonSchema(schema) → call ctx.callProviderJsonMode → safeParse → on failure build repairPrompt from PROMPTS.repairJson.system + schema + broken text → EXACTLY ONE repair attempt → second failure throws with code 'STRUCTURED_OUTPUT_FAILED', retryable false. Create `src/core/ai/PlannerService.ts` per §1.2: `plan(input)` where input carries { userInput, systemPrompt, provider (an ILLMProvider), timeoutMs (3s default), abortSignal } — the systemPrompt arrives pre-assembled from the caller (PromptCacheManager in production, tests pass PROMPTS.planner.system); decision parsed via StructuredOutput.requestJson(PlannerDecisionSchema, ...). Create the D-48 fixture seed: `tests/core/ai/fixtures/openai-stream.ts` with exact wire bytes (one happy path `data: {...content...}` + `data: [DONE]`, one missing-terminator case, one malformed-JSON case); `tests/core/ai/fixtures/FixtureProvider.ts` — an ILLMProvider implementation whose requestJson/stream parse the fixture wire bytes through StreamAdapter (so the tracer exercises the real SSE parser). Write the end-to-end case in `tests/core/ai/PlannerService.test.ts`: feed the happy-path fixture → assert the returned decision is a zod-validated PlannerDecision with action 'answer'; assert the malformed-JSON fixture flows through the one-shot repair to a valid decision. Run `pnpm run verify:phase-3` — must be green.</action>
  <acceptance_criteria>PlannerService.test.ts passes: happy-path fixture bytes → zod-validated PlannerDecision (action 'answer'); malformed fixture → repaired to a valid decision via exactly one PROMPTS.repairJson.system attempt; missing-terminator fixture → STREAM_ERROR surfaces; `pnpm run verify:phase-3` green.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>The single path works end-to-end and is committed: fixture OpenAI SSE bytes parse through StreamAdapter, a malformed decision is repaired exactly once to a zod-validated PlannerDecision, and `pnpm run verify:phase-3` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Expand the slice — repair-failure terminal path + structured-output edge tests + OpenAI conformance fixtures</name>
  <files>tests/core/ai/StructuredOutput.test.ts, tests/core/ai/fixtures/openai-stream.ts</files>
  <read_first>@src/core/ai/StructuredOutput.ts (the file under test — created task 2) · @.planning/PRODUCT_SPEC_v0_1.md Appendix L (5848-5902) — the exactly-one-repair invariant · 03-RESEARCH.md Pattern 3 (one-shot repair flow)</read_first>
  <action>Write `tests/core/ai/StructuredOutput.test.ts` covering the Appendix L contract: (a) valid JSON first attempt passes through with NO repair; (b) malformed JSON repaired exactly once — assert the repair prompt contains PROMPTS.repairJson.system and the broken text; (c) double-failure is terminal — second repair attempt fails → throws code 'STRUCTURED_OUTPUT_FAILED' with retryable false; (d) abort propagation — ctx.abortSignal aborted mid-attempt surfaces the abort (Requester maps caller abort to TIMEOUT per D-35 — the test asserts no hang and a typed failure). Extend `tests/core/ai/fixtures/openai-stream.ts` with: a JSON-mode response returning a valid answer decision, a malformed-JSON response, a repair-success response, and a repair-failure response (D-48 golden matrix subset for this slice: invalid JSON, repair success, repair failure). Do NOT add a second repair attempt to StructuredOutput — the exactly-one rule is the invariant. Run `pnpm run verify:phase-3` — must be green.</action>
  <acceptance_criteria>StructuredOutput.test.ts green: 4 case groups (no-repair pass, one-repair success, terminal STRUCTURED_OUTPUT_FAILED with retryable false, abort surfaces typed failure); fixtures carry the 4 response variants.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>StructuredOutput.test.ts passes all four cases; the fixture library seeds the D-48 matrix; verify:phase-3 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider wire → StreamAdapter | Untrusted provider network bytes cross into the pipeline here — malformed/truncated streams must not wedge the planner |
| provider JSON-mode response → StructuredOutput | Untrusted model output parsed as a decision — schema-validated, never trusted raw |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | DoS | StructuredOutput repair loop | medium | mitigate | Appendix L hard limit: EXACTLY ONE repair, then terminal 'STRUCTURED_OUTPUT_FAILED' (retryable false); planner timeout 3s (§1.2) + Requester 25s default bound the call |
| T-3-02 | Tampering | StreamAdapter SSE parse | medium | mitigate | Missing terminator = STREAM_ERROR (REQ-R09); incremental TextDecoder({stream:true}) line buffer handles CRLF/multi-byte boundaries; zod-validated canonical events (CLAUDE.md cross-boundary rule) |
| T-3-03 | Information Disclosure | error surfaces (StructuredOutput/PlannerService) | medium | mitigate | Errors built from provider status + server body only, never the apiKey (T-01-10 contract, aiProvider.ts:96-115); Requester canonical codes RATE_LIMITED/TIMEOUT/NETWORK only (§21.6, no invented codes D-38) |
| T-3-SC | Tampering | npm/pip/cargo installs (zod-to-json-schema) | high | mitigate | Package-legitimacy gate already passed (RESEARCH audit: StefanTerdell repo, 57.7M/wk, no postinstall, verdict OK); version pinned 3.25.2; no other new packages — install task is scoped to exactly this one dependency |
</threat_model>

<verification>
- `pnpm run verify:phase-3` green after every task commit (tsc --noEmit + vitest run tests/core/ai tests/core/ai/persona).
- Grep gate: no strict-suppression markers in new files (`grep -R "suppress" src/core/ai` — phrased to avoid literal echo; use `grep -rn "ts-expect-error" src/core/ai` with comment-line filtering if needed).
- End-to-end tracer test is a real path: fixture wire bytes → StreamAdapter → FixtureProvider → StructuredOutput → PlannerDecision.
</verification>

<success_criteria>
1. PlannerService returns zod-validated PlannerDecision objects with the closed §1.2 action union; the run_tool toolName is validated against the closed enum supplied at request time (ExecutorService, plan 03-04).
2. StructuredOutput one-shot repair verified: repair success and repair-failure terminal paths both tested (Appendix L).
3. Canonical Appendix A prompts land in src/core/prompts/index.ts verbatim — repairJson.system replaced (current stub differs).
4. `pnpm run verify:phase-3` passes; node_modules + zod-to-json-schema present.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-3 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/ai/types.ts | ProviderId, ModelTier, PlannerDecisionSchema, PlannerDecision, StreamEvent, PromptSection, ToolExecutionResult, RouterAttemptState | **[this plan]** |
| src/core/ai/ILLMProvider.ts | ILLMProvider (stream + requestJson) | **[this plan]** |
| src/core/ai/StreamAdapter.ts | createStreamAdapter, per-provider wire adapters (OpenAI here; Anthropic/Gemini/Ollama in 03-03) | this plan + 03-03 |
| src/core/ai/StructuredOutput.ts | requestJson, StructuredOutputContext, STRUCTURED_OUTPUT_FAILED | **[this plan]** |
| src/core/ai/PlannerService.ts | PlannerService.plan (§1.2) | **[this plan]** |
| src/core/prompts/index.ts | PROMPTS (Appendix A canonical: planner/renderer/memoryExtractor/conversationSummarizer/repairJson) | **[this plan]** |
| src/core/ai/providers/{OpenAI,Anthropic,Gemini,Ollama,OpenAICompat}Provider.ts | 5 ILLMProvider implementations | 03-03 |
| src/core/ai/ProviderRegistry.ts | hydrate/getEnabled/getById/getAll, endpoint-override merge, live-model cache | 03-05 |
| src/core/ai/TierResolver.ts | resolveTier (Appendix D mech, capability tiers, null contract) | 03-05 |
| src/core/ai/ProviderRouter.ts | route/retry/fallback/circuit breaker (§1.5/§20.10) | 03-05 |
| src/core/ai/PromptCacheManager.ts | buildSystemPrompt (D-59 single choke-point) | 03-04 |
| src/core/ai/PromptCacheAdapter.ts | applyCacheHints, hashStableSections (Appendix K) | 03-04 |
| src/core/ai/ExecutorService.ts | execute + closed toolName enum + TOOL_REJECTED | 03-04 |
| src/core/ai/RendererService.ts | render + 512-token cap | 03-04 |
| src/core/ai/AgentOrchestrator.ts | runAgentTurn (Appendix I, tier caps) | 03-06 |
| src/core/ai/toolSchemas.ts | ToolDefinition, ToolCapabilityManifest, zero tools (D-46) | 03-04 |
| src/core/ai/ChunkBuffer.ts | createChunkBuffer (Appendix J) | 03-04 |
| src/core/ai/persona/PersonaProfile.ts | PersonaProfileSchema, DEFAULT_PERSONA (Appendix N.1) | 03-02 |
| src/core/ai/persona/PersonaInjector.ts | resolvePersona, buildPersonaBlock, inject (Appendix N.2) | 03-02 |
| src/core/ai/UserPreferences.ts | minimal UserPreferences (fastModel/balancedModel/personaOverrides) + np_preferences | 03-02 |
| src/core/runtime/workerState.ts | + ActiveStreamState (§20.6) | 03-04 |
| src/types/storage.ts | + 'append-chat-turn' union member (D-45) | 03-07 |
| src/components/chat/useChatStreaming.ts | re-point at AgentOrchestrator (D-44) | 03-07 |
| src/components/options/OptionsPage.tsx | D-50 endpoint overrides + D-54 tier assignment | 03-07 |
| tests/core/ai/ | 8 spec-required test files + fixtures/ (D-48) | all plans |