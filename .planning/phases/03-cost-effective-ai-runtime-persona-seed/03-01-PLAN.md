---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - AGENTS.md
  - .planning/REQUIREMENTS.md
  - src/core/ai/types.ts
  - src/types/workspace.ts
  - src/core/error/errorCodes.ts
  - .planning/PRODUCT_SPEC_v0_1.md
autonomous: true
requirements: [AI-01, AI-07]

must_haves:
  truths:
    - "The four-ID ProviderId enum ('openai' | 'anthropic' | 'gemini' | 'ollama') has ONE canonical home in src/core/ai/types.ts; src/types/workspace.ts re-exports it via import (no second declaration, R-1)."
    - "The ai-sdk is installed at the exact locked versions via `pnpm add` per D-01 (pnpm-standardized repo — package-lock.json stays absent, pnpm-lock.yaml carries the pins): ai@4.3.19, @ai-sdk/openai@1.3.24, @ai-sdk/anthropic@1.2.12, @ai-sdk/google@1.2.22, zod-to-json-schema@^3; @ai-sdk/ollama is absent (npm 404)."
    - "package.json has scripts.test:ai = 'vitest run tests/core/ai' and a verify:phase-3 template will exist by wave 6 (eslint + prettier + tsc --noEmit + wxt build + vitest run + isolation check)."
    - "Every new Phase-3 error code exists in src/core/error/errorCodes.ts AND is canonical in spec Appendix C.2 (Golden Rule 9)."
    - "REQUIREMENTS.md documents AI-07 re-mapped to Phase 8 per D-06 (§18 is authoritative over the REQUIREMENTS.md row)."
  artifacts:
    - src/core/ai/types.ts
    - src/types/workspace.ts
    - src/core/error/errorCodes.ts
  key_links:
    - "src/types/workspace.ts imports ProviderId from '@/core/ai/types' (single canonical home)."
    - "src/core/error/errorCodes.ts Phase-3 block mirrors spec Appendix C.2 canonical codes."
  flagged_assumptions:
    - "AI-01 [unclassified — manual review]: provider identity semantics are locked as the four-ID ProviderId enum; a custom OpenAI-compatible baseURL is an OpenAICompatProvider factory config variant (D-12), NOT a 5th ID; 'fallback' is routing state inside RouterAttemptState, NOT a provider identity."
    - "AI-07 [unclassified — manual review]: re-mapped to Phase 8 per D-06 (dependency-blocked: PageContentService→4a, NotesDB→5, ClipboardHelper→8; must ship WITH ToolCapabilityManifest/verifiers Phase 8a). Phase 3 delivers only the REQUIREMENTS.md note."
    - "A5 [research]: browser Response/Headers/Request globals in the jsdom-align vitest env come from Node (undici); mock-fetch tests construct responses from the global, not window."
  prohibitions:
    - "No 5th ProviderId; no invented identifiers (R-1)."
    - "No '@ai-sdk/ollama' install (npm 404)."
    - "No 'latest' version ranges for the ai-sdk packages (Pitfall 3 version drift)."
    - "No duplicate type declarations (R-1 single home at any time)."

assumption_delta_decision:
  noun: "ProviderId"
  decision: "no-change"
  rationale: "custom baseURL is a D-12 factory config variant; fallback lives in RouterAttemptState, not a new provider identity."
---

<objective>
Foundation for the Phase-3 AI runtime: install the locked Vercel AI SDK packages, seed the canonical type home (`src/core/ai/types.ts`, D-07), swap the `ProviderId` declaration into it, and register the Phase-3 error-code block (Golden Rule 9). Every later plan compiles against these types and codes.

Purpose: The four-provider runtime, Router, Orchestrator, persona pipeline, and UI all import from `src/core/ai/types.ts` — nothing can compile until this plan lands (RESEARCH Wave-0 gap). The pinned SDK versions prevent the ai@5+/v7 API drift that breaks the typecheck (Pitfall 3).
Output: package.json with locked deps + `test:ai` script, AGENTS.md §7 amended, REQUIREMENTS.md AI-07 note, `src/core/ai/types.ts` seeded, `src/types/workspace.ts` ProviderId swap, `src/core/error/errorCodes.ts` Phase-3 block, spec Appendix C.2 canonicalization.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@src/core/ai/ProviderRegistry.ts
@src/types/workspace.ts
@src/core/error/errorCodes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install locked SDK deps + amend AGENTS.md/REQUIREMENTS.md</name>
  <files>package.json, AGENTS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - package.json (current dependencies + scripts sections)
    - AGENTS.md §7 Approved Stack (verify-then-fix: RESEARCH live-verified the list ALREADY reflects the amendment — `zod-to-json-schema ^3` present, no `@ai-sdk/ollama` line; only fix if a stale line appears during execution)
    - .planning/REQUIREMENTS.md lines 34-42 (AI-01..AI-07 block) + traceability table
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md "Standard Stack" + "Package Legitimacy Audit" (all packages approved — no [SUS]/[SLOP] remaining after review; no checkpoint gates required)
  </read_first>
  <action>
    Install the exact pinned versions (never "latest" — Pitfall 3) with pnpm per D-01 (pnpm-standardized repo: the lockfile is pnpm-lock.yaml only; package-lock.json was removed in Phase 1 and must stay absent — running npm install in a pnpm-symlinked node_modules can prune the .pnpm virtual-store layout and drift the lockfile, breaking `pnpm install --frozen-lockfile` in CI/verify):
    `pnpm add ai@4.3.19 @ai-sdk/openai@1.3.24 @ai-sdk/anthropic@1.2.12 @ai-sdk/google@1.2.22 zod-to-json-schema@^3`
    (zod-to-json-schema is for the prompt-mode [SYSTEM] schema text + Appendix L repair prompt only — native path passes the Zod schema directly.)
    DO NOT install @ai-sdk/ollama — it is not published (npm E404, verified twice).
    Then add the focused eval script: `pnpm pkg set scripts.test:ai="vitest run tests/core/ai"`.
    Confirm the install created no package-lock.json and pnpm-lock.yaml carries the new pins (the verify below asserts this).

    AGENTS.md §7: VERIFY the Approved Stack already reflects the amendment — RESEARCH live-verified it contains `zod-to-json-schema ^3` and contains no `@ai-sdk/ollama` line. If — and only if — a stale line appears during execution (e.g. a concurrent doc edit), remove the stale `@ai-sdk/ollama` entry and ensure `zod-to-json-schema ^3` is present; otherwise leave the list untouched (do not churn it). The `@ai-sdk/openai` line stays (it covers OpenAI + OpenAI-compatible + Ollama-local via createOpenAI per §10.2).

    REQUIREMENTS.md: annotate the AI-07 row (line 42) with a re-map note — e.g. "AI-07 re-mapped to Phase 8 per D-06 (§18 is authoritative over this row; MCP client + NowPilotMainServer + MCPRegistry ship governance-less-never with ToolCapabilityManifest/verifiers in Phase 8a)". Update the traceability table row for AI-07 from Phase 3 to Phase 8. Also annotate AI-04 (line 39) with "monthly aggregate deferred to Phase 6 per D-16; Phase 3 ships the no-op budgetGuard hook" (under-specified marker).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};if(d['ai']!=='4.3.19'||d['@ai-sdk/openai']!=='1.3.24'||d['@ai-sdk/anthropic']!=='1.2.12'||d['@ai-sdk/google']!=='1.2.22'||!d['zod-to-json-schema']){console.error('version mismatch',JSON.stringify(d));process.exit(1)};if(p.scripts['test:ai']!=='vitest run tests/core/ai'){console.error('test:ai missing');process.exit(1)};if(d['@ai-sdk/ollama']){console.error('ollama installed');process.exit(1)};if(fs.existsSync('package-lock.json')){console.error('package-lock.json must not exist (D-01 pnpm)');process.exit(1)};const lock=fs.readFileSync('pnpm-lock.yaml','utf8');const need=['ai@4.3.19','@ai-sdk/openai@1.3.24','@ai-sdk/anthropic@1.2.12','@ai-sdk/google@1.2.22','zod-to-json-schema@3.'];const miss=need.filter(s=>!lock.includes(s));if(miss.length){console.error('pnpm-lock.yaml missing pins',miss);process.exit(1)};console.log('deps ok, pnpm lockfile in sync')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies/devDependencies contain ai@4.3.19, @ai-sdk/openai@1.3.24, @ai-sdk/anthropic@1.2.12, @ai-sdk/google@1.2.22, zod-to-json-schema (^3), and NOT @ai-sdk/ollama.
    - package.json scripts.test:ai === "vitest run tests/core/ai" (set via `pnpm pkg set`).
    - package-lock.json does NOT exist (D-01: pnpm-only repo); pnpm-lock.yaml contains the ai@4.3.19 + @ai-sdk/* v1-line + zod-to-json-schema@3.x pins.
    - AGENTS.md §7 contains `zod-to-json-schema ^3` and contains no `@ai-sdk/ollama` line.
    - REQUIREMENTS.md AI-07 row + traceability row say Phase 8 / re-map note; AI-04 row has the D-16 deferral annotation.
    - The node -e verify command above exits 0.
  </acceptance_criteria>
  <done>Locked deps installed, scripts + doc amendments applied, verify command exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Seed src/core/ai/types.ts (D-07) + swap workspace.ts ProviderId</name>
  <files>src/core/ai/types.ts, src/types/workspace.ts</files>
  <read_first>
    - src/types/workspace.ts (current local ProviderId decl at lines 8-10 + NOTE)
    - .planning/PRODUCT_SPEC_v0_1.md §2.3 (lines 453-479 — OptimizedContext verbatim), Appendix C (line ~4234 — LLMMessage/LLMOptions/LLMStreamChunk/ModelInfo/ProviderConfig/ContentBlock/ToolExecutionResult), Appendix C PromptSection (~line 4583), toolSchemas ToolSchemaRef (~line 4570), §2.1 ModelContextTier + classifyModelContext (lines 415-432), ContextProvenanceManifest (line 521), §3.4/§3.5 UserPreferences + RetrievedMemory (lines 4543-4562, incl. personaOverrides)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md "Open Questions" #2 (UserPreferences home recommendation)
  </read_first>
  <action>
    Create src/core/ai/types.ts as the canonical AI type home (spec-verbatim headers: "Source: Appendix C / §2.3 (verbatim)"). Seed EXACTLY:
    - `export type ProviderId = 'openai' | 'anthropic' | 'gemini' | 'ollama';` (canonical — Appendix C)
    - ContentBlock, LLMMessage, LLMOptions, LLMStreamChunk, ModelInfo, ProviderConfig (Appendix C verbatim — ProviderConfig matches §10.3 field set: id, label, apiKey?, baseURL, customBaseURL?, models, contextWindow, supportsTools, enabled, priority, lastValidated?)
    - ToolExecutionResult<T> (Appendix C verbatim: toolName, ok, output?, error?, evidence?, durationMs)
    - ToolSchemaRef (Appendix C verbatim: name, description, jsonSchema, dangerous, source: 'builtin' | 'mcp' | 'skill' | 'servicenow')
    - PromptSection (Appendix C verbatim: kind 'system'|'tool_schemas'|'preferences'|'memory'|'context'|'task'|'user_input', text, tokens, stable, sourceId)
    - ModelContextTier + classifyModelContext (spec §2.1 verbatim — tiny/small/medium/large with the exact 4096/16384/131072 thresholds)
    - OptimizedContext + ContextOptimizerInput (§2.3 verbatim — D-07 seed; import type PageContext from '@/core/content/PageContext', import ToolSchemaRef from this same file)
    - ContextProvenanceManifest (spec line 521 verbatim)
    - RetrievedMemory + UserPreferences (spec §3.4/§3.5 verbatim incl. personaOverrides?: { name?: string; tone?: 'professional-warm'|'concise'|'friendly'; brevity?: 'brief'|'balanced'|'detailed' } and allowCloudFallbackFromLocal: boolean) — header comment marks BOTH as Phase-5 move targets (src/core/memory/types.ts re-exports later; R-1: single home at any time, per RESEARCH Open Question #2).
    Each group gets the "Source: … (verbatim, lines …)" header style used by src/core/prompts/index.ts. Do NOT add invented fields.
    Then modify src/types/workspace.ts: DELETE the local `export type ProviderId = ...` decl + NOTE (lines 8-10) and ADD `import type { ProviderId } from '@/core/ai/types';` at the top import block. WorkspaceState.activeProvider stays typed ProviderId — verify all consumers still compile (SidePanelShell, SidePanelRouter, standalone shell, entrypoints, tests).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (whole repo — the swap is type-clean).
    - src/types/workspace.ts contains `from '@/core/ai/types'` and no local `export type ProviderId`.
    - src/core/ai/types.ts contains the four-ID ProviderId, OptimizedContext (§2.3 field-for-field), PromptSection, UserPreferences (with personaOverrides + allowCloudFallbackFromLocal), ModelContextTier, classifyModelContext, ContextProvenanceManifest, RetrievedMemory, ToolSchemaRef, ToolExecutionResult, LLM types, ProviderConfig.
    - grep -c "^export type ProviderId" src/core/ai/types.ts == 1 and grep -c "ProviderId" src/types/workspace.ts shows only the import + usages (no redeclaration).
  </acceptance_criteria>
  <done>Canonical type home compiles; workspace.ts imports it; no second declaration anywhere (R-1).</done>
</task>

<task type="auto">
  <name>Task 3: Phase-3 error-code block + spec Appendix C.2 canonicalization</name>
  <files>src/core/error/errorCodes.ts, .planning/PRODUCT_SPEC_v0_1.md</files>
  <read_first>
    - src/core/error/errorCodes.ts (extend IN PLACE — header says later phases extend, never duplicate)
    - .planning/PRODUCT_SPEC_v0_1.md Appendix C.2 — Error Code Registry (line 5013+; the `# Runtime / provider` group at lines 5018-5024 already contains HOST_NOT_PERMITTED @5020, STRUCTURED_OUTPUT_FAILED @5022, STREAM_FAILED @5023 — VERIFY presence, never re-add; the 10 Phase-3 codes below are the only ones missing from C.2)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md "Wave 0 Gaps" (error-code list)
  </read_first>
  <action>
    Extend ERROR_CODES in place with a `// --- AI runtime (Phase 3) ---` block (placed before the Phase-2 block):
    TOOL_REJECTED, PERSONA_LOAD_FAILED, STRUCTURED_OUTPUT_FAILED, PLANNER_FAILED, STREAM_FAILED, SCHEMA_INVALID, NETWORK, TIMEOUT, RATE_LIMITED, PROVIDER_AUTH, PROVIDER_5XX, PROVIDER_MODEL_UNKNOWN, HOST_NOT_PERMITTED — each as the string literal of its own name (existing style: `TOOL_REJECTED: 'TOOL_REJECTED',`).
    Golden Rule 9 canonicalization (Appendix C.2 — Error Code Registry, spec line 5013+): verify ALL 13 Phase-3 codes are present in C.2. STREAM_FAILED (line 5023), STRUCTURED_OUTPUT_FAILED (line 5022), and HOST_NOT_PERMITTED (line 5020) ALREADY exist in the `# Runtime / provider` group — VERIFY presence only, do NOT add duplicates. ADD the 10 MISSING codes — TOOL_REJECTED, PERSONA_LOAD_FAILED, PLANNER_FAILED, SCHEMA_INVALID, NETWORK, TIMEOUT, RATE_LIMITED, PROVIDER_AUTH, PROVIDER_5XX, PROVIDER_MODEL_UNKNOWN — to the same `# Runtime / provider` group as a `# Phase 3 — AI runtime / provider (canonical additions, 03-01)` comment group placed immediately after STREAM_INTERRUPTED (Phase-1 group-comment precedent), so the code registry and spec stay in lockstep (truth #4).
    Do NOT touch any existing Phase-1/2 code entries or any other C.2 line; do NOT add any other code to the spec.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('src/core/error/errorCodes.ts','utf8');const spec=fs.readFileSync('.planning/PRODUCT_SPEC_v0_1.md','utf8');const need=['TOOL_REJECTED','PERSONA_LOAD_FAILED','STRUCTURED_OUTPUT_FAILED','PLANNER_FAILED','STREAM_FAILED','SCHEMA_INVALID','NETWORK','TIMEOUT','RATE_LIMITED','PROVIDER_AUTH','PROVIDER_5XX','PROVIDER_MODEL_UNKNOWN','HOST_NOT_PERMITTED'];const missSrc=need.filter(c=>!src.includes(\"'\"+c+\"'\"));const c2=spec.slice(spec.indexOf('### Appendix C.2'));const missSpec=need.filter(c=>!new RegExp('^'+c+'$','m').test(c2));if(missSrc.length||missSpec.length){console.error('missing in src:',missSrc,'missing in spec C.2:',missSpec);process.exit(1)}console.log('error codes ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All 13 codes are in src/core/error/errorCodes.ts (Phase-3 block) AND present in spec Appendix C.2 (STREAM_FAILED / STRUCTURED_OUTPUT_FAILED / HOST_NOT_PERMITTED verified as already canonical — no duplicate; the 10 missing codes added to the `# Runtime / provider` group).
    - `npx tsc --noEmit` still passes.
    - No Phase-1/2 entries modified or duplicated.
  </acceptance_criteria>
  <done>Phase-3 code registry canonicalized in code + spec (Golden Rule 9).</done>
</task>

</tasks>

## Edge Coverage Assumptions (specless probe fallback — 14 edges, ALL unresolved, surfaced not dropped)

Every unresolved edge from `$COVERAGE` is carried as an explicit flagged assumption in this plan's `must_haves.flagged_assumptions` where it belongs to this plan's scope (AI-01 identity semantics, AI-07 deferral). The remaining edges are owned by the plans that implement their requirements (AI-02 → 03-04, AI-03 → 03-03/03-06, AI-04 → 03-05, AI-05 → 03-07, AI-06 → 03-08). None are silently dropped.

## Artifacts This Phase Produces

- package.json: deps `ai@4.3.19`, `@ai-sdk/openai@1.3.24`, `@ai-sdk/anthropic@1.2.12`, `@ai-sdk/google@1.2.22`, `zod-to-json-schema@^3`; script `test:ai`.
- AGENTS.md §7: Approved Stack verified/amended to the amendment state (zod-to-json-schema ^3 present; no @ai-sdk/ollama line).
- .planning/REQUIREMENTS.md: AI-07 Phase-8 re-map note + traceability row; AI-04 D-16 deferral annotation.
- src/core/ai/types.ts (new): `ProviderId`, `ContentBlock`, `LLMMessage`, `LLMOptions`, `LLMStreamChunk`, `ModelInfo`, `ProviderConfig`, `ToolExecutionResult<T>`, `ToolSchemaRef`, `PromptSection`, `ModelContextTier`, `classifyModelContext()`, `ContextOptimizerInput`, `OptimizedContext`, `ContextProvenanceManifest`, `RetrievedMemory`, `UserPreferences`.
- src/types/workspace.ts (modified): local ProviderId decl removed, import from '@/core/ai/types'.
- src/core/error/errorCodes.ts (extended): 13-code Phase-3 block.
- .planning/PRODUCT_SPEC_v0_1.md (modified): the 10 missing Phase-3 codes canonicalized into Appendix C.2 (`# Runtime / provider` group, Phase-3 comment group); STREAM_FAILED / STRUCTURED_OUTPUT_FAILED / HOST_NOT_PERMITTED verified present — no duplicate added.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| type/registry layer → runtime | Canonical types and codes are consumed by every downstream module; an invented identifier here cascades (R-1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-01 | Tampering | package.json deps | high | mitigate | Exact-pinned versions only (`ai@4.3.19`, v1-line adapters); `@ai-sdk/ollama` absent (npm E404); RESEARCH Package Legitimacy Audit approved all 5 packages (official vercel/ai + StefanTerdell repos, 6-20M weekly downloads) — no checkpoint gates required per audit |
| T-03-01-02 | Tampering | src/core/ai/types.ts | high | mitigate | Spec-verbatim seeding only (R-1); the verify greps assert a single ProviderId home and no invented fields; downstream plans compile against these exact shapes |
| T-03-01-SC | Tampering | pnpm add installs | high | mitigate | Package legitimacy audit complete (RESEARCH §Package Legitimacy Audit — all OK/approved, `@ai-sdk/ollama` [SLOP] removed); versions verified on registry 2026-08-09; installs run via `pnpm add` only (D-01 — npm install would drift the pnpm-lock.yaml and create package-lock.json); the node -e verify asserts exact pins + pnpm-lock.yaml sync + package-lock.json absence |
</threat_model>

<verification>
- `npx tsc --noEmit` — full-repo typecheck (types.ts seed + workspace swap must not break Phase-1/2 consumers).
- The node -e dep/code verifies above (exact pins; 13 codes in code + spec).
- `npx vitest run tests/core/security tests/entrypoints tests/components/sidepanel` — the existing suites touching ProviderRegistry/workspace types stay green after the swap.
</verification>

<success_criteria>
- Repo compiles with the new type home and error codes; locked deps installed; AGENTS.md/REQUIREMENTS.md amended; spec C.2 canonicalized with the 10 missing Phase-3 codes (STREAM_FAILED / STRUCTURED_OUTPUT_FAILED / HOST_NOT_PERMITTED verified present — no duplicates).
- No existing test regression (registry/entrypoint/shell suites pass).
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-01-SUMMARY.md` when done
</output>
