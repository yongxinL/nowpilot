---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
- package.json
- AGENTS.md
- .planning/REQUIREMENTS.md
- src/core/ai/types.ts
- src/core/context/ModelContextTier.ts
- src/core/context/ContextProvenanceManifest.ts
- src/core/memory/types.ts
- src/types/workspace.ts
- src/core/error/errorCodes.ts
- .planning/PRODUCT_SPEC_v0_1.md
autonomous: true
requirements: [AI-01, AI-07]

<!-- FINAL v2 (2026-08-10): + ordered Tasks section (cost-effective-model executability). W-1 — C.2 check
     is a SCOPED, LINE-ANCHORED regex (not spec.includes). P-3 — PromptSection declared ONCE in
     src/core/ai/types.ts (D-07); spec Appendix C/K/L + §8.5 import from this home. -->

must_haves:
truths:
- "The four-ID ProviderId enum ('openai' | 'anthropic' | 'gemini' | 'ollama') has ONE canonical home in src/core/ai/types.ts; src/types/workspace.ts re-exports it via `export type { ProviderId } from '@/core/ai/types'` (no second declaration, R-1 — OnboardingModal imports ProviderId from '@/types/workspace', so the re-export is mandatory, not a bare import)."
- "PromptSection is declared ONCE, in src/core/ai/types.ts (D-07 canonical home); the spec's Appendix C/K/L and §8.5 ContextOptimizer import it from this AI home (P-3). NO '../context/ContextOptimizer' declaration or second copy exists (R-1). Phase-4 ContextOptimizer imports PromptSection from '@/core/ai/types'."
- "Context/type homes are seeded AT their §8.5/Appendix-C canonical paths (R-1), NOT flattened into ai/types.ts: `src/core/context/ModelContextTier.ts` (ModelContextTier + classifyModelContext), `src/core/context/ContextProvenanceManifest.ts` (ContextProvenanceManifest), `src/core/memory/types.ts` (UserPreferences + RetrievedMemory — spec C.1 note line 4775: 'UserPreferences and RetrievedMemory remain in @/core/memory/types'). ai/types.ts IMPORTS these (never re-declares). These are documented +N seeds to the §18 create-list, recorded in the 03-09 §18 addendum."
- "The ai-sdk is installed at the exact locked versions via pnpm add per D-01 (pnpm-standardized repo — package-lock.json stays absent, pnpm-lock.yaml carries the pins): ai@4.3.19, @ai-sdk/openai@1.3.24, @ai-sdk/anthropic@1.2.12, @ai-sdk/google@1.2.22, zod-to-json-schema@^3; @ai-sdk/ollama is absent (npm 404)."
- "package.json has scripts.test:ai = 'vitest run tests/core/ai' and a verify:phase-3 template will exist by wave 6 (eslint + prettier + tsc --noEmit + wxt build + vitest run + isolation check)."
- "Every new Phase-3 error code exists in src/core/error/errorCodes.ts AND is canonical in spec Appendix C.2 (Golden Rule 9)."
- "The C.2 canonicalization check is SCOPED + LINE-ANCHORED (W-1, NOT spec.includes): the verify extracts the Appendix C.2 block (slice from the 'Appendix C.2' heading to the next '###'/'Appendix' boundary) and asserts each required code matches /^CODE_NAME$/m WITHIN that slice — so the assertion FAILS pre-edit and only passes once the code is added to C.2 itself. A whole-file substring check is prohibited (it passes pre-edit because codes appear in prose elsewhere — the checkr W-1 false-green gate)."
- "REQUIREMENTS.md documents AI-07 re-mapped to Phase 8 per D-06 (§18 is authoritative over the REQUIREMENTS.md row)."
artifacts:
- src/core/ai/types.ts
- src/core/context/ModelContextTier.ts
- src/core/context/ContextProvenanceManifest.ts
- src/core/memory/types.ts
- src/types/workspace.ts
- src/core/error/errorCodes.ts
key_links:
- "src/types/workspace.ts re-exports ProviderId via `export type { ProviderId } from '@/core/ai/types'` (single canonical home — OnboardingModal keeps importing from '@/types/workspace')."
- "src/core/ai/types.ts is the single home for ProviderId, OptimizedContext, and PromptSection (D-07); PromptCacheAdapter (03-03), StructuredOutput (03-04), and ProviderRouter (03-05) all import PromptSection from here. ai/types.ts imports ModelContextTier/classifyModelContext, ContextProvenanceManifest, UserPreferences, RetrievedMemory from their §8.5/C.1 homes — never re-declares them (R-1)."
- "src/core/error/errorCodes.ts Phase-3 block mirrors spec Appendix C.2 canonical codes; the scoped-regex verify (W-1) proves each code is present in the C.2 slice."
flagged_assumptions:
- "AI-01 [unclassified — manual review]: provider identity semantics are locked as the four-ID ProviderId enum; a custom OpenAI-compatible baseURL is an OpenAICompatProvider factory config variant (D-12), NOT a 5th ID; 'fallback' is routing state inside RouterAttemptState, NOT a provider identity."
- "AI-07 [unclassified — manual review]: re-mapped to Phase 8 per D-06 (dependency-blocked: PageContentService→4a, NotesDB→5, ClipboardHelper→8; must ship WITH ToolCapabilityManifest/verifiers Phase 8a). Phase 3 delivers only the REQUIREMENTS.md note."
- "P-3 [type home]: PromptSection moves from the §8.5 ContextOptimizer block to the Appendix C src/core/ai/types.ts listing; §8.5 keeps a re-export note (import type { PromptSection } from '../ai/types')."
- "P-3b [type homes — canonical seeds]: ModelContextTier + classifyModelContext seed in `src/core/context/ModelContextTier.ts` (§8.5 line 2668), ContextProvenanceManifest in `src/core/context/ContextProvenanceManifest.ts` (§8.5 line 2673), UserPreferences + RetrievedMemory in `src/core/memory/types.ts` (Appendix C line 4542 / C.1 note line 4775) — these are the spec's canonical homes; ai/types.ts imports them (R-1), so Phase 4/5 create NO second declaration. Recorded as +N in the 03-09 §18 addendum."
- "A5 [research]: browser Response/Headers/Request globals in the jsdom-align vitest env come from Node (undici); mock-fetch tests construct responses from the global, not window."
prohibitions:
- "No 5th ProviderId; no invented identifiers (R-1)."
- "No '@ai-sdk/ollama' install (npm 404)."
- "No 'latest' version ranges for the ai-sdk packages (Pitfall 3 version drift)."
- "No duplicate type declarations (R-1 single home) — exactly one 'export interface PromptSection' in the repo (in src/core/ai/types.ts)."
- "No flattening of context/memory types into ai/types.ts (P-3b — ModelContextTier, ContextProvenanceManifest, UserPreferences, RetrievedMemory live at their §8.5/Appendix-C homes and are imported, never re-declared)."
- "No bare `import type { ProviderId }` in workspace.ts — the swap MUST be `export type { ProviderId } from '@/core/ai/types'` (OnboardingModal depends on the '@/types/workspace' export)."
- "No whole-file spec.includes() C.2 check (W-1 — must be the scoped, line-anchored regex)."

### assumption_delta_decision:
noun: "ProviderId"
decision: "no-change"
rationale: "custom baseURL is a D-12 factory config variant; fallback lives in RouterAttemptState, not a new provider identity."

Purpose: The four-provider runtime, Router, Orchestrator, persona pipeline, and UI all import from src/core/ai/types.ts — nothing can compile until this plan lands (RESEARCH Wave-0 gap). The pinned SDK versions prevent the ai@5+/v7 API drift that breaks the typecheck (Pitfall 3). PromptSection lands in this canonical home (P-3) so the F-4 structured-output seam (03-04/03-05) imports it cleanly. Context/memory types (ModelContextTier, ContextProvenanceManifest, UserPreferences, RetrievedMemory) are seeded at their §8.5/Appendix-C canonical homes (P-3b) so Phase 4/5 never re-declare them (R-1).
Output: package.json with locked deps + test:ai script, AGENTS.md §7 amended, REQUIREMENTS.md AI-07 note, src/core/ai/types.ts seeded (incl. PromptSection) + the three canonical context/memory home seeds, src/types/workspace.ts ProviderId re-export swap, src/core/error/errorCodes.ts Phase-3 block, spec Appendix C.2 canonicalization (scoped-regex verified), spec PromptSection home move.
<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

### Tasks (ordered — do not reorder; each maps to a truth/artifact)
1. **Install locked deps (D-01).** `pnpm add ai@4.3.19 @ai-sdk/openai@1.3.24 @ai-sdk/anthropic@1.2.12 @ai-sdk/google@1.2.22 zod-to-json-schema@^3`. Do NOT add @ai-sdk/ollama (npm 404). Verify: exact pins in package.json, pnpm-lock.yaml synced, package-lock.json absent.
2. **Add test:ai script.** package.json `scripts.test:ai = 'vitest run tests/core/ai'`.
3. **Seed src/core/ai/types.ts (new).** Declare ProviderId, ContentBlock, LLMMessage, LLMOptions, LLMStreamChunk, ModelInfo, ProviderConfig, ToolExecutionResult<T>, ToolSchemaRef, **PromptSection (P-3 canonical home)**, ContextOptimizerInput, OptimizedContext — spec-verbatim shapes only (R-1). Import ModelContextTier/classifyModelContext from '../context/ModelContextTier', ContextProvenanceManifest from '../context/ContextProvenanceManifest', UserPreferences/RetrievedMemory from '../memory/types' (P-3b canonical homes — NEVER re-declare).
4. **Seed canonical context/memory homes (new, +N to §18, P-3b).** Create `src/core/context/ModelContextTier.ts` (ModelContextTier + classifyModelContext — spec §8.5 line 2668/Appendix C verbatim), `src/core/context/ContextProvenanceManifest.ts` (ContextProvenanceManifest — §8.5 line 2673), `src/core/memory/types.ts` (UserPreferences + RetrievedMemory — Appendix C line 4542 / C.1 note line 4775). ai/types.ts imports from these; no second copy anywhere (R-1).
5. **Swap ProviderId in src/types/workspace.ts.** Remove the local ProviderId decl; re-export with `export type { ProviderId } from '@/core/ai/types'` (NOT a bare import — OnboardingModal imports ProviderId from '@/types/workspace').
6. **Extend src/core/error/errorCodes.ts.** Add the 13-code Phase-3 block (canonical codes only, Golden Rule 9).
7. **Spec P-3 home move (PRODUCT_SPEC_v0_1.md).** Relocate the `export interface PromptSection` from the §8.5 ContextOptimizer block into the Appendix C types.ts listing; change Appendix K and Appendix L import lines to `import type { PromptSection } from './types';`; leave a §8.5 re-export note (`import type { PromptSection } from '../ai/types'`).
8. **Spec C.2 canonicalization (PRODUCT_SPEC_v0_1.md).** Add the 10 missing Phase-3 codes to Appendix C.2; verify STREAM_FAILED / STRUCTURED_OUTPUT_FAILED / HOST_NOT_PERMITTED already present (no duplicate).
9. **Wire the SCOPED C.2 verify (W-1).** Implement the check as: slice the Appendix C.2 block (heading → next '###'/'Appendix'), assert each required code `/^CODE$/m` WITHIN the slice. Prohibit `spec.includes(code)`.
10. **Amend AGENTS.md §7 + REQUIREMENTS.md.** §7 stack: zod-to-json-schema ^3 present, no @ai-sdk/ollama line. REQUIREMENTS: AI-07 → Phase 8 (D-06) note + traceability row; AI-04 D-16 deferral annotation.
11. **Verify green.** Run tsc --noEmit + test:ai; grep-assert exactly one ProviderId home AND one `export interface PromptSection`; zero `context/ContextOptimizer` PromptSection imports under src/core/ai/**; grep-assert single declarations of ModelContextTier / ContextProvenanceManifest / UserPreferences / RetrievedMemory at their §8.5/C.1 homes (P-3b) with ai/types.ts importing (not re-declaring); grep `export type { ProviderId }` in workspace.ts; no existing-suite regression.

### Edge Coverage Assumptions (specless probe fallback — 14 edges, ALL unresolved, surfaced not dropped)

Every unresolved edge from $COVERAGE is carried as an explicit flagged assumption in this plan's must_haves.flagged_assumptions where it belongs to this plan's scope (AI-01 identity semantics, AI-07 deferral). The remaining edges are owned by the plans that implement their requirements (AI-02 → 03-04, AI-03 → 03-03/03-06, AI-04 → 03-05, AI-05 → 03-07, AI-06 → 03-08). None are silently dropped.

### Artifacts This Phase Produces
- package.json: deps ai@4.3.19, @ai-sdk/openai@1.3.24, @ai-sdk/anthropic@1.2.12, @ai-sdk/google@1.2.22, zod-to-json-schema@^3; script test:ai.
- AGENTS.md §7: Approved Stack verified/amended (zod-to-json-schema ^3 present; no @ai-sdk/ollama line).
- .planning/REQUIREMENTS.md: AI-07 Phase-8 re-map note + traceability row; AI-04 D-16 deferral annotation.
- src/core/ai/types.ts (new): ProviderId, ContentBlock, LLMMessage, LLMOptions, LLMStreamChunk, ModelInfo, ProviderConfig, ToolExecutionResult<T>, ToolSchemaRef, PromptSection (D-07 canonical home — moved here from §8.5), ContextOptimizerInput, OptimizedContext. Imports ModelContextTier/ContextProvenanceManifest/UserPreferences/RetrievedMemory from their §8.5/C.1 homes (P-3b) — never re-declares them.
- src/core/context/ModelContextTier.ts (new, +N to §18, P-3b): ModelContextTier, classifyModelContext() (§8.5 line 2668).
- src/core/context/ContextProvenanceManifest.ts (new, +N to §18, P-3b): ContextProvenanceManifest (§8.5 line 2673).
- src/core/memory/types.ts (new, +N to §18, P-3b): UserPreferences, RetrievedMemory (Appendix C line 4542 / C.1 note line 4775).
- src/types/workspace.ts (modified): local ProviderId decl removed, re-export `export type { ProviderId } from '@/core/ai/types'`.
- src/core/error/errorCodes.ts (extended): 13-code Phase-3 block.
- .planning/PRODUCT_SPEC_v0_1.md (modified): (a) 10 missing Phase-3 codes canonicalized into Appendix C.2 (no duplicates); (b) P-3 PromptSection home move (Appendix C/K/L + §8.5).
<threat_model>

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| type/registry layer → runtime | Canonical types and codes are consumed by every downstream module; an invented identifier here cascades (R-1) |
| verify gate → spec truth | The C.2 scoped-regex check guards that a claimed code actually lives in Appendix C.2 (W-1); a substring check would false-pass |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-01 | Tampering | package.json deps | high | mitigate | Exact-pinned versions only (ai@4.3.19, v1-line adapters); @ai-sdk/ollama absent (npm E404); RESEARCH Package Legitimacy Audit approved all 5 packages (official vercel/ai + StefanTerdell repos, 6-20M weekly downloads) |
| T-03-01-02 | Tampering | src/core/ai/types.ts | high | mitigate | Spec-verbatim seeding only (R-1); the verify greps assert a single ProviderId home AND a single PromptSection declaration (P-3) and no invented fields |
| T-03-01-03 | Tampering | C.2 verification method | high | mitigate | W-1: scoped line-anchored regex (/^CODE$/m within the extracted C.2 slice) — a whole-file spec.includes() is prohibited (false-green); the check fails until the code is added to C.2 itself |
| T-03-01-SC | Tampering | pnpm add installs | high | mitigate | Package legitimacy audit complete (all OK/approved, @ai-sdk/ollama [SLOP] removed); versions verified on registry 2026-08-09; installs run via pnpm add only (D-01); the node -e verify asserts exact pins + pnpm-lock.yaml sync + package-lock.json absence |
</threat_model>
<success_criteria>
- Repo compiles with the new type homes and error codes; locked deps installed; AGENTS.md/REQUIREMENTS.md amended.
- Spec C.2 canonicalized with the 10 missing Phase-3 codes, proven by the SCOPED line-anchored regex (W-1) — not spec.includes; STREAM_FAILED / STRUCTURED_OUTPUT_FAILED / HOST_NOT_PERMITTED verified present (no duplicates).
- P-3 done: exactly one 'export interface PromptSection' (in src/core/ai/types.ts); Appendix K/L + §8.5 import from './types'; zero 'context/ContextOptimizer' PromptSection imports under src/core/ai/**.
- P-3b done: ModelContextTier/classifyModelContext, ContextProvenanceManifest, UserPreferences/RetrievedMemory each declared exactly once at their §8.5/Appendix-C homes; ai/types.ts imports (never re-declares) them — no existing-suite regression (OnboardingModal still resolves ProviderId via the '@/types/workspace' re-export).
</success_criteria>
