---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 09
type: execute
wave: 6
depends_on: ["03-08"]
files_modified:
  - src/entrypoints/sidepanel/main.tsx
  - src/entrypoints/standalone/main.tsx
  - package.json
  - tests/isolation/check-content-bundle.mjs
  - .planning/PRODUCT_SPEC_v0_1.md
  - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-09-SUMMARY.md
autonomous: true
requirements: [AI-01, AI-03, AI-04, AI-05, AI-06]

must_haves:
  truths:
    - "Both surfaces (Side Panel + Standalone) fire runAIRuntimeInit at mount: provider configs load from the vault (np_providers.<id> per-provider envelopes via Setting.read + KeyVault.decryptSecret), decrypt failure → PROVIDER_KEY_UNREADABLE → ProviderRegistry.markProviderKeyUnreadable (enabled: false, treated as unconfigured — D-21, no auto-wipe); configured providers register; np_persona loads via the accessor; every step wrapped (debugLog + fall-through, Golden Rule 9)."
    - "R-3: the AI runtime init runs ONLY on the sidepanel/standalone surfaces — the background SW never imports ai packages or touches the vault (isolation-grep verified)."
    - "The content bundle excludes ai/@ai-sdk/streamText symbols — verify:phase-3's isolation check extends the W-16 forbidden-token list to the ai-sdk (Pitfall 6)."
    - "verify:phase-3 runs eslint + prettier --check + tsc --noEmit + wxt build + vitest run + the content-bundle isolation check — the full phase gate (Phase-1/2 template)."
    - "The §18 Phase-3 addendum documents the two Appendix-I deviations (onStreamDelta + invocation seams) and the D-02 contextHelper as a Phase-4 deletion target; the +1 documented files (contextHelper, personaConfig, useStreamingLLM) are listed."
  artifacts:
    - src/entrypoints/sidepanel/main.tsx
    - src/entrypoints/standalone/main.tsx
    - package.json (verify:phase-3 script)
    - tests/isolation/check-content-bundle.mjs
    - .planning/PRODUCT_SPEC_v0_1.md (Phase-3 addendum)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-09-SUMMARY.md
  key_links:
    - "Vault (np_providers.<id> envelope) → wiring decrypt → ProviderRegistry.registerProvider/markProviderKeyUnreadable → Router.configure → hook createStageInvocation → runAgentTurn."
    - "getProviderRouter().configure({ configuredProviders, privacyMode }) is called by the wiring BEFORE any send — the privacyMode gate (D-13) derives from UserPreferences.allowCloudFallbackFromLocal via privacyModeFromPrefs."
  flagged_assumptions:
    - "AI-01 [unclassified — manual review]: wiring resolves per-provider vault envelopes (np_providers.<id>, encrypted) — the Phase-2 single storage model; provider config presence + unreadable state flow through the registry boundary as typed data, never raw keys."
    - "AI-04 [concurrency]: the Router's in-memory per-surface state (D-14) is re-created per surface load; no cross-surface sharing this phase."
  prohibitions:
    - "No ai/provider/vault imports in the background SW (R-3)."
    - "No raw API keys/prompt bodies in any wiring debugLog (TraceRedactor, R-10)."
    - "No auto-wipe/auto-regenerate on PROVIDER_KEY_UNREADABLE (02-CONTEXT D-04)."
    - "No new uncanonicalized error codes in the wiring (Golden Rule 9)."
    - "No AI init blocking the mount — failures degrade gracefully (Golden Rule 9)."
---

<objective>
The final wiring + gate: `runAIRuntimeInit` on both surfaces (vault → registry → persona → router configuration), the `verify:phase-3` script, the content-bundle isolation extension (ai packages), the §18 addendum documentation, and the full phase gate.

Purpose: Without this plan the runtime exists but nothing loads it — the user could never chat. This is the R-3 boundary enforcement (AI in Side Panel/Standalone only) and the phase's "every phase ends green" gate (Golden Rule 10).
Output: Wired surfaces, verify:phase-3 green, §18 addendum + plan SUMMARY recorded.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/PRODUCT_SPEC_v0_1.md
@src/entrypoints/sidepanel/main.tsx
@src/entrypoints/standalone/main.tsx
@src/core/ai/ProviderRegistry.ts
@src/core/ai/ProviderRouter.ts
@src/core/ai/persona/personaConfig.ts
@src/core/storage/Setting.ts
@src/core/security/KeyVault.ts
@tests/isolation/check-content-bundle.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: runAIRuntimeInit wiring on both surfaces (R-3)</name>
  <files>src/entrypoints/sidepanel/main.tsx, src/entrypoints/standalone/main.tsx</files>
  <read_first>
    - src/entrypoints/sidepanel/main.tsx (runStorageBootstrap at lines 74-133 — the EXACT template for runAIRuntimeInit; module-scope fire-and-forget chain at lines 208-243)
    - src/entrypoints/standalone/main.tsx (same pattern)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md "src/entrypoints/{sidepanel,standalone}/main.tsx" (wiring pattern) + "ProviderRegistry ↔ KeyVault" (D-21 error-emission half)
    - src/core/ai/ProviderRegistry.ts (registerProvider, markProviderKeyUnreadable, getProviderInfo from 03-02)
    - src/core/ai/ProviderRouter.ts (getProviderRouter, configure from 03-05)
    - src/core/ai/persona/personaConfig.ts (readPersonaPrefs from 03-07)
    - src/core/storage/Setting.ts (settingRead for np_providers.<id> envelopes) + src/core/security/KeyVault.ts (decryptSecret, PROVIDER_KEY_UNREADABLE state)
  </read_first>
  <action>
    Add `runAIRuntimeInit(): Promise<void>` to BOTH entrypoints, mirroring runStorageBootstrap's wrapped-step structure (Golden Rule 9 — each step try/catch + debugLog with a canonical Phase-3 code + fall-through, NEVER rejects the mount):
    1. Read the registered provider ids (ProviderRegistry presences or a wiring-owned list from Setting — the canonical Phase-2 storage model is per-provider envelopes np_providers.<id>, encrypted; enumerate the known four ids and read each envelope via Setting's settingRead gate (resolveKeyPermission honors np_providers.<id> → { local, encrypted: true } — DO NOT touch Setting.ts, it already resolves the per-provider keys per WR-10).
    2. For each present envelope: await getKeyVault().decryptSecret(envelope) → parse ProviderConfig (ProviderConfigSchema from types.ts — Zod-validate at the boundary); decrypt failure → getProviderRegistry().markProviderKeyUnreadable(id) + debugLog(ERROR_CODES.PROVIDER_KEY_UNREADABLE, …) (D-21 — no auto-wipe, no regenerate); success → getProviderRegistry().registerProvider({ providerId: id, models, enabled }).
    3. Load persona prefs: const prefs = await readPersonaPrefs() (PERSONA_LOAD_FAILED → DEFAULT_PERSONA already handled inside the accessor).
    4. Configure the router: getProviderRouter().configure({ configuredProviders: <from the registry, enabled only>, privacyMode: privacyModeFromPrefs(prefs) }) — privacyModeFromPrefs from TierResolver (03-02); the D-13 gate is live from first send.
    5. Fire it in the module-scope chain alongside runStorageBootstrap (void runAIRuntimeInit() with catch → debugLog), honoring R-3 (only sidepanel + standalone; the background SW never imports these modules — verify the background entrypoint has no ai imports).
    Header comment on both entrypoints documents the 02-11 precedent + R-3 boundary.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - grep -c "runAIRuntimeInit" src/entrypoints/sidepanel/main.tsx >= 1 and src/entrypoints/standalone/main.tsx >= 1 (both surfaces wire it).
    - grep -rn "@ai-sdk\|from 'ai'" src/entrypoints/background/* 2>/dev/null → zero matches (R-3 — background never touches the AI runtime).
    - Each wiring step is wrapped in try/catch with a canonical code (no raw throws past the mount).
  </acceptance_criteria>
  <done>Both surfaces wire vault→registry→persona→router at mount; the background SW stays AI-free (R-3).</done>
</task>

<task type="auto">
  <name>Task 2: verify:phase-3 script + content-bundle isolation extension + §18 addendum</name>
  <files>package.json, tests/isolation/check-content-bundle.mjs, .planning/PRODUCT_SPEC_v0_1.md</files>
  <read_first>
    - package.json (verify:phase-1/verify:phase-2 script templates at lines 18-19)
    - tests/isolation/check-content-bundle.mjs (W-16 forbidden-token list at lines 27-37 — 'antd', 'defuddle', 'yaml', etc.)
    - .planning/PRODUCT_SPEC_v0_1.md §18 Phase 3 (lines 2605-2652 — add the addendum after the DONE-when list)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Pitfall 6 (content-bundle isolation must extend to the ai packages)
  </read_first>
  <action>
    package.json: add `"verify:phase-3": "eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run && node tests/isolation/check-content-bundle.mjs"` (byte-template of verify:phase-1/2).
    tests/isolation/check-content-bundle.mjs: extend the W-16 forbidden-token list with the ai-sdk tokens — add 'ai-sdk' and 'streamText' (and '@ai-sdk' if the tokenizer is substring-based; keep the existing entries untouched). Rationale comment: Phase-3 AI packages must never reach the content-script bundle (R-3/Pitfall 6 — AI + vault live in Side Panel/Standalone only).
    .planning/PRODUCT_SPEC_v0_1.md §18 Phase 3: append the Phase-3 addendum block after the DONE-when list (verbatim documentation, precedent: Phase-1/2 plan-deviation notes):
      - ChatPage 3-state lifecycle (skeleton → minimal → full; Phase 7 promotes the hook to src/hooks/useStreamingLLM.ts).
      - contextHelper (src/core/ai/contextHelper.ts) is a Phase-4 DELETION TARGET — ContextOptimizer replaces it (D-02); listed as a documented +1 to the create-list.
      - runAgentTurn optional deviations: `onStreamDelta?: (delta: string) => void` and `invocation?: StageResolver` — output struct stays verbatim (D-20).
      - toolSchemas.ts = ToolSchemaRef contract + exactly one tool (get-provider-info, D-04); run_tool branch omitted when zero tools (D-05).
      - np_persona accessor (personaConfig.ts) + co-located useStreamingLLM.ts are documented +1 files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8'));if(p.scripts['verify:phase-3']!=='eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run && node tests/isolation/check-content-bundle.mjs'){console.error('verify:phase-3 missing/mismatched');process.exit(1)};const iso=fs.readFileSync('tests/isolation/check-content-bundle.mjs','utf8');if(!iso.includes('ai-sdk')){console.error('isolation missing ai-sdk token');process.exit(1)};const spec=fs.readFileSync('.planning/PRODUCT_SPEC_v0_1.md','utf8');if(!spec.includes('Phase-3 addendum')||!spec.includes('contextHelper')){console.error('spec addendum missing');process.exit(1)};console.log('gate config ok')"</automated>
  </verify>
  <acceptance_criteria>
    - verify:phase-3 script present with the exact Phase-1/2 template shape.
    - check-content-bundle.mjs forbids the ai-sdk tokens in the content bundle.
    - Spec §18 Phase 3 contains the addendum (ChatPage lifecycle, contextHelper deletion target, runAgentTurn deviations, toolSchemas note, +1 files).
    - The node -e verify above exits 0.
  </acceptance_criteria>
  <done>Gate script + isolation extension + spec addendum all in place and config-verified.</done>
</task>

<task type="auto">
  <name>Task 3: Full verify:phase-3 gate + phase SUMMARY</name>
  <files>.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-09-SUMMARY.md</files>
  <read_first>
    - The full phase test surface: tests/core/ai/** (8 §18 files + ProviderRegistry + ChunkBuffer/PromptCacheAdapter/toolSchemas + persona), tests/components/pages/*, tests/fixtures/optimizedContext.ts
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-VALIDATION.md (Sampling Rate + Per-Task Verification Map)
  </read_first>
  <action>
    Run the full phase gate `pnpm run verify:phase-3`. Fix any regression (files from this phase only; if a cross-phase regression appears, debugLog + document it in the SUMMARY, do NOT modify Phase-1/2 files without a documented reason). When green:
    Write `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-09-SUMMARY.md` covering: what shipped (all 9 plans), the documented deviations (onStreamDelta/invocation seams, D-02 deletion target), the +1 documented files (contextHelper, personaConfig, useStreamingLLM), open items deferred (monthly budget D-16 → Phase 6; MCP D-06 → Phase 8; trajectory/evidence D-20 → Phase 3a; RICH D-03 → Phase 7; circuit-breaker persistence D-14), and gate results (each verify:phase-3 stage: eslint/prettier/tsc/wxt build/vitest/isolation).
  </action>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run verify:phase-3` exits 0 (eslint + prettier --check + tsc --noEmit + wxt build + vitest run + isolation check all green).
    - The SUMMARY file exists and records deviations, +1 files, deferred items, and gate results.
  </acceptance_criteria>
  <done>Phase 3 ends green (Golden Rule 10) with the SUMMARY recorded.</done>
</task>

</tasks>

## Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-01 [unclassified — wiring review] and AI-04 [concurrency — per-surface router state] — recorded in `must_haves.flagged_assumptions`. Every other edge has its owner plan (never silently dropped).

## Artifacts This Phase Produces

- src/entrypoints/sidepanel/main.tsx (modified): `runAIRuntimeInit()` + module-scope wiring.
- src/entrypoints/standalone/main.tsx (modified): `runAIRuntimeInit()` + module-scope wiring.
- package.json (modified): `verify:phase-3` script.
- tests/isolation/check-content-bundle.mjs (modified): ai-sdk forbidden tokens (W-16 extension).
- .planning/PRODUCT_SPEC_v0_1.md (modified): §18 Phase-3 addendum.
- .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-09-SUMMARY.md (new).

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault (np_providers.<id> envelopes) → surface wiring | Decrypted API keys cross here — the ONLY place vault decrypts for the AI runtime happen (R-3) |
| Surface → background | The background SW must never import the AI runtime or vault (isolation boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09-01 | Information Disclosure | wiring decrypt path | critical | mitigate | Decrypted keys exist only inside the wiring function scope; decrypt failure → PROVIDER_KEY_UNREADABLE (typed, enabled:false) — no key material ever debugLogged (TraceRedactor R-10); no auto-wipe (02-CONTEXT D-04) |
| T-03-09-02 | (boundary) | background SW AI-free | high | mitigate | verify:phase-3 isolation check forbids ai/@ai-sdk symbols in the content bundle; the background entrypoint grep asserts zero ai imports (R-3, Pitfall 6) |
| T-03-09-03 | Tampering | mount degradation | medium | mitigate | runAIRuntimeInit steps are individually wrapped (Golden Rule 9) — a vault/router/persona failure degrades gracefully (debugLog + fall-through), never blocks the mount |
| T-03-09-04 | Spoofing | provider config parse | medium | mitigate | ProviderConfigSchema Zod-validates every decrypted envelope at the boundary (V5 Input Validation) before registerProvider |
| T-03-09-SC | Tampering | pnpm installs | high | mitigate | No new packages this plan; approved set from 03-01 |
</threat_model>

<verification>
- `pnpm run verify:phase-3` — the full phase gate (eslint + prettier + tsc + wxt build + vitest + isolation).
- Background-free grep: no ai/@ai-sdk imports in src/entrypoints/background.
- Full AI suite: `npx vitest run tests/core/ai` + `npx vitest run tests/components/pages` green.
</verification>

<success_criteria>
- Both surfaces wire the AI runtime at mount; provider config + persona + privacyMode live from first send.
- verify:phase-3 exits 0 — Phase 3 ends green (Golden Rule 10).
- §18 addendum + SUMMARY record all deviations, +1 files, and deferrals for the next phase.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-09-SUMMARY.md` when done
</output>
