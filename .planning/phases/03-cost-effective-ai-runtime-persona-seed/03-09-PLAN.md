---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 09
type: execute
wave: 6
depends_on: ["03-08"]
files_modified:
- src/entrypoints/sidepanel/main.tsx
- src/entrypoints/standalone/main.tsx
- package.json
- tests/isolation/check-content-bundle.mjs
- .planning/PRODUCT_SPEC_v0_1.md
- .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-09-SUMMARY.md
autonomous: true
requirements: [AI-01, AI-03, AI-04, AI-05, AI-06]

<!-- FINAL v2 (2026-08-10): + ordered Tasks section. P-5 — the §18 addendum reconciles "8 canonical §18 test
     files" vs the full tests/core/ai/** suite (canonical 8 + documented +N + the D-08 fixture); verify:phase-3
     runs the FULL suite and MUST NOT assert an exact count of 8. Also records the P-3/F-4/F-5 corrections. -->

### must_haves:
truths:
- "Both surfaces (Side Panel + Standalone) fire runAIRuntimeInit at mount: provider configs load from the vault (np_providers.* per-provider envelopes via Setting.read + KeyVault.decryptSecret), decrypt failure → PROVIDER_KEY_UNREADABLE → ProviderRegistry.markProviderKeyUnreadable (enabled: false, treated as unconfigured — D-21, no auto-wipe); configured providers register; np_persona loads via the accessor; every step wrapped (debugLog + fall-through, Golden Rule 9)."
- "R-3: the AI runtime init runs ONLY on the sidepanel/standalone surfaces — the background SW never imports ai packages or touches the vault (isolation-grep verified)."
- "The content bundle excludes ai/@ai-sdk/streamText symbols — verify:phase-3's isolation check extends the W-16 forbidden-token list to the ai-sdk (Pitfall 6)."
- "verify:phase-3 runs eslint + prettier --check + tsc --noEmit + wxt build + vitest run + the content-bundle isolation check — the full phase gate (Phase-1/2 template)."
- "Test-file count reconciliation (P-5): §18 names 8 canonical Phase-3 test files; Phase 3 additionally ships documented +N test files (ChunkBuffer, PromptCacheAdapter, toolSchemas, ProviderRegistry, StructuredOutput, PlannerService, ExecutorService, ProviderRouter, RendererService, AgentOrchestrator, PersonaProfile, PersonaInjector — the full tests/core/ai/** set) plus tests/fixtures/optimizedContext.ts (D-08, a +1 fixture, not one of the 8). verify:phase-3 runs the FULL tests/core/ai/** set (and tests/components/**), NOT a fixed count of 8; the '8' figure is the canonical §18 subset. The verify MUST NOT assert an exact test-file count of 8."
- "The §18 Phase-3 addendum documents: (a) the two Appendix-I deviations (onStreamDelta + invocation seams) and the D-02 contextHelper as a Phase-4 deletion target; (b) the +1 documented files (contextHelper, personaConfig, useStreamingLLM) and the +1 fixture (optimizedContext.ts); (c) the P-3 PromptSection home move (Appendix C/K/L + §8.5 → src/core/ai/types.ts); (d) the F-4 structured-output signature (requestJson/callProviderJsonMode take PromptSection[]) and the F-5 cache-hint application (messages[]+providerOptions, owned by the Router); (e) the P-5 test-count reconciliation; (f) the P-3b canonical type-home seeds — src/core/context/ModelContextTier.ts, src/core/context/ContextProvenanceManifest.ts, src/core/memory/types.ts as +N §18 additions whose types are imported (never re-declared) by src/core/ai/types.ts, so Phase 4/5 create no second declaration (R-1)."
artifacts:
- src/entrypoints/sidepanel/main.tsx
- src/entrypoints/standalone/main.tsx
- package.json (verify:phase-3 script)
- tests/isolation/check-content-bundle.mjs
- .planning/PRODUCT_SPEC_v0_1.md (Phase-3 addendum)
- .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-09-SUMMARY.md
key_links:
- "Vault (np_providers.* envelope) → wiring decrypt → ProviderRegistry.registerProvider/markProviderKeyUnreadable → Router.configure → hook createStageInvocation → runAgentTurn."
- "getProviderRouter().configure({ configuredProviders, privacyMode }) is called by the wiring BEFORE any send — the privacyMode gate (D-13) derives from UserPreferences.allowCloudFallbackFromLocal via privacyModeFromPrefs (false → 'prefer-local', true → 'cloud-ok'; 'local-only' reserved)."
- "The §18 addendum is the single record of Phase-3 deviations/extras/corrections consumed by the next phase's planner (P-3/P-3b/P-5/F-4/F-5 all captured here)."
flagged_assumptions:
- "AI-01 [unclassified — manual review]: wiring resolves per-provider vault envelopes (np_providers.*, encrypted) — the Phase-2 single storage model; provider config presence + unreadable state flow through the registry boundary as typed data, never raw keys."
- "AI-04 [concurrency]: the Router's in-memory per-surface state (D-14) is re-created per surface load; no cross-surface sharing this phase."
- "P-5 [count semantics]: '8 §18 test files' is a documentation subset marker, not a verify assertion; the gate runs the whole suite. A future phase adding/removing a test must update the addendum list, not a magic number."
- "P-3b [type homes]: the three canonical type-home seed files (context/ModelContextTier.ts, context/ContextProvenanceManifest.ts, memory/types.ts) are +N §18 additions; their types are imported by src/core/ai/types.ts (never re-declared) — Phase 4/5 extend these files in place, so the next planner must NOT recreate the declarations (R-1)."
prohibitions:
- "No ai/provider/vault imports in the background SW (R-3)."
- "No raw API keys/prompt bodies in any wiring debugLog (TraceRedactor, R-10)."
- "No auto-wipe/auto-regenerate on PROVIDER_KEY_UNREADABLE (02-CONTEXT D-04)."
- "No new uncanonicalized error codes in the wiring (Golden Rule 9)."
- "No verify:phase-3 assertion of an exact test-file count of 8 (P-5 — run the full suite; the '8' is a §18 subset marker only)."
- "No AI init blocking the mount — failures degrade gracefully (Golden Rule 9)."

Purpose: Without this plan the runtime exists but nothing loads it — the user could never chat. This is the R-3 boundary enforcement (AI in Side Panel/Standalone only) and the phase's "every phase ends green" gate (Golden Rule 10). The §18 addendum is also where every Phase-3 deviation/extra/correction (P-3, P-3b, P-5, F-4, F-5, the two Appendix-I seams) is recorded for the next planner.
Output: Wired surfaces, verify:phase-3 green (full suite, no magic count), §18 addendum + plan SUMMARY recorded.
<execution_context>@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md</execution_context>

### Tasks (ordered — do not reorder; each maps to a truth/artifact)
1. **runAIRuntimeInit() — sidepanel/main.tsx (modify).** At mount: Setting.read np_providers.* envelopes → KeyVault.decryptSecret; decrypt failure → PROVIDER_KEY_UNREADABLE → ProviderRegistry.markProviderKeyUnreadable (enabled:false, no auto-wipe); register configured providers; load np_persona via the accessor. Wrap every step (debugLog + fall-through, Golden Rule 9) — never block the mount.
2. **Router.configure() before first send.** getProviderRouter().configure({ configuredProviders, privacyMode: privacyModeFromPrefs(prefs) }) — false→'prefer-local', true→'cloud-ok'.
3. **standalone/main.tsx (modify).** Same runAIRuntimeInit() + module-scope wiring.
4. **check-content-bundle.mjs (modify).** Extend the W-16 forbidden-token list to ai/@ai-sdk/streamText so the content bundle excludes them (R-3, Pitfall 6); background entrypoint grep asserts zero ai imports.
5. **verify:phase-3 script (package.json).** eslint + prettier --check + tsc --noEmit + wxt build + vitest run + the isolation check. Run the FULL tests/core/ai/** + tests/components/** suite; **do NOT assert an exact count of 8** (P-5).
6. **§18 Phase-3 addendum (PRODUCT_SPEC_v0_1.md).** Record: (a) the two Appendix-I input-only deviations (onStreamDelta + invocation) + contextHelper as a Phase-4 deletion target; (b) the +1 files (contextHelper, personaConfig, useStreamingLLM) + the +1 fixture (optimizedContext.ts); (c) P-3 PromptSection home move; (d) F-4 signature + F-5 application owner; (e) P-5 count reconciliation; (f) P-3b canonical type-home seeds (ModelContextTier.ts / ContextProvenanceManifest.ts / memory/types.ts — +N, imported by ai/types.ts, R-1).
7. **03-09-SUMMARY.md (new).** Record all deviations, +1 files/fixture, deferrals, and the F-4/F-5/P-3/P-3b/P-5 corrections for the next phase.
8. **Verify green.** verify:phase-3 exits 0 (full suite); background-SW isolation grep clean; both surfaces wire the runtime at mount.

### Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-01 [unclassified — wiring review] and AI-04 [concurrency — per-surface router state] — recorded in must_haves.flagged_assumptions. Every other edge has its owner plan (never silently dropped).

### Artifacts This Phase Produces
- src/entrypoints/sidepanel/main.tsx (modified): runAIRuntimeInit() + module-scope wiring.
- src/entrypoints/standalone/main.tsx (modified): runAIRuntimeInit() + module-scope wiring.
- package.json (modified): verify:phase-3 script (full suite; no exact-count assertion).
- tests/isolation/check-content-bundle.mjs (modified): ai-sdk forbidden tokens (W-16 extension).
- .planning/PRODUCT_SPEC_v0_1.md (modified): §18 Phase-3 addendum (deviations, +1 files/fixture, P-3 home move, P-3b canonical seeds, F-4/F-5 seam notes, P-5 count reconciliation).
- .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-09-SUMMARY.md (new).
<threat_model>

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault (np_providers.* envelopes) → surface wiring | Decrypted API keys cross here — the ONLY place vault decrypts for the AI runtime happen (R-3) |
| Surface → background | The background SW must never import the AI runtime or vault (isolation boundary) |
| §18 addendum → next planner | The addendum is the authoritative record of Phase-3 deviations/extras/corrections; an omission here silently drifts the next phase |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09-01 | Information Disclosure | wiring decrypt path | critical | mitigate | Decrypted keys exist only inside the wiring function scope; decrypt failure → PROVIDER_KEY_UNREADABLE (typed, enabled:false) — no key material ever debugLogged (TraceRedactor R-10); no auto-wipe (02-CONTEXT D-04) |
| T-03-09-02 | (boundary) | background SW AI-free | high | mitigate | verify:phase-3 isolation check forbids ai/@ai-sdk symbols in the content bundle; the background entrypoint grep asserts zero ai imports (R-3, Pitfall 6) |
| T-03-09-03 | Tampering | mount degradation | medium | mitigate | runAIRuntimeInit steps are individually wrapped (Golden Rule 9) — a vault/router/persona failure degrades gracefully (debugLog + fall-through), never blocks the mount |
| T-03-09-04 | Spoofing | provider config parse | medium | mitigate | ProviderConfigSchema Zod-validates every decrypted envelope at the boundary (V5 Input Validation) before registerProvider |
| T-03-09-05 | Tampering | addendum completeness | low | mitigate | P-5: the addendum enumerates the canonical 8 + extras + fixture and the F-4/F-5/P-3 corrections; verify runs the full suite so an unlisted test still executes (no silent drop), and the count is never asserted as a magic 8 |
| T-03-09-SC | Tampering | pnpm installs | high | mitigate | No new packages this plan; approved set from 03-01 |
</threat_model>
<success_criteria>
- Both surfaces wire the AI runtime at mount; provider config + persona + privacyMode live from first send.
- verify:phase-3 exits 0 running the FULL tests/core/ai/** + tests/components/** suite — Phase 3 ends green (Golden Rule 10); no exact-count-of-8 assertion (P-5).
- §18 addendum records all deviations, +1 files/fixture, the P-3 PromptSection home move, the F-4/F-5 seam corrections, and the P-5 count reconciliation for the next phase.
</success_criteria>
