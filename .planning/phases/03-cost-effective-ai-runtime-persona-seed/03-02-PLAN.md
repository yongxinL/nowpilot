---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/core/ai/ILLMProvider.ts
  - src/core/ai/providers/OpenAIProvider.ts
  - src/core/ai/providers/AnthropicProvider.ts
  - src/core/ai/providers/GeminiProvider.ts
  - src/core/ai/providers/OllamaProvider.ts
  - src/core/ai/providers/OpenAICompatProvider.ts
  - src/core/ai/TierResolver.ts
  - src/core/ai/ProviderRegistry.ts
  - tests/core/ai/ProviderRegistry.test.ts
autonomous: true
requirements: [AI-01]

must_haves:
  truths:
    - "The four provider adapters (openai/anthropic/gemini/ollama) implement the §10.1 ILLMProvider contract; getAISDKModel is the single factory switch and the ONLY place provider packages are imported (Seam 1)."
    - "Ollama rides createOpenAI({ apiKey: 'ollama', baseURL: 'http://localhost:11434/v1', compatibility: 'compatible' }) — no @ai-sdk/ollama package (§10.2, RESEARCH verified)."
    - "OpenAICompatProvider is a factory createOpenAICompatProvider({ baseURL }) returning a provider with id: 'openai' + custom baseURL — NOT a 5th ProviderId (D-12)."
    - "TierResolver.resolveTier returns the cheapest capable candidate and never invents a model name; returns null when no candidate matches; the 'local-only' filter branch stays reserved/unused in Phase 3 (D-13)."
    - "ProviderRegistry extension (D-21): a PROVIDER_KEY_UNREADABLE emission marks the provider enabled:false (treated as unconfigured); no auto-wipe, no auto-regenerate; the registry stays dependency-free (no zustand/react imports)."
  artifacts:
    - src/core/ai/ILLMProvider.ts
    - src/core/ai/providers/{OpenAI,Anthropic,Gemini,Ollama,OpenAICompat}Provider.ts
    - src/core/ai/TierResolver.ts
    - src/core/ai/ProviderRegistry.ts
    - tests/core/ai/ProviderRegistry.test.ts
  key_links:
    - "getAISDKModel is the only import site of @ai-sdk/* packages (grep-verified)."
    - "ProviderRegistry feeds ExecutorService's get-provider-info tool (03-04) and the surface-mount wiring (03-09); the PROVIDER_KEY_UNREADABLE typed error crosses the wiring boundary (D-21)."
  flagged_assumptions:
    - "A6 [research]: the fetch test seam is an OPTIONAL cfg field on getAISDKModel (documented extension of the §10.1 signature, per AI-SPEC)."
    - "A4 [research]: model ids pass through getAISDKModel to the provider factory's callable with the provider's own id naming (deepseek-chat via the openai factory)."
  prohibitions:
    - "No 5th ProviderId; OpenAICompat stays a factory (D-12)."
    - "No provider package imported outside ILLMProvider.ts (Seam 1)."
    - "No zustand/react import into ProviderRegistry (Pitfall 4 dependency-free)."
    - "No auto-wipe / auto-regenerate on PROVIDER_KEY_UNREADABLE (02-CONTEXT D-04)."
    - "No 'local-only' filter usage in resolveTier this phase (D-13 reserved)."
---

<objective>
Provider layer: the §10.1 `ILLMProvider` contract, the `getAISDKModel` single factory switch (Seam 1), the five adapter files (openai / anthropic / gemini / ollama / openai-compatible), the Appendix D `TierResolver` (+ `privacyModeFromPrefs`, D-13), and the in-place `ProviderRegistry` extension (D-21).

Purpose: AI-01 — four-provider support with an OpenAI-compatible custom baseURL. Every Router/Planner/Renderer call and the surface wiring depends on these adapters and the registry's config-presence + unreadable-state semantics.
Output: The provider adapter family + tier resolution + extended registry, with the registry's PROVIDER_KEY_UNREADABLE behavior proven by test.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/PRODUCT_SPEC_v0_1.md
@src/core/ai/types.ts
@src/core/ai/ProviderRegistry.ts
@src/core/security/KeyVault.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ILLMProvider contract + getAISDKModel + five adapter files</name>
  <files>src/core/ai/ILLMProvider.ts, src/core/ai/providers/OpenAIProvider.ts, src/core/ai/providers/AnthropicProvider.ts, src/core/ai/providers/GeminiProvider.ts, src/core/ai/providers/OllamaProvider.ts, src/core/ai/providers/OpenAICompatProvider.ts</files>
  <read_first>
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Pattern 1 (lines 247-274, verified getAISDKModel) + Code Examples "Constructing the four provider models" (lines 426-440) + Assumption A6
    - .planning/PRODUCT_SPEC_v0_1.md §10.1 (ILLMProvider interface, line ~1530), §10.2 (four-provider table + Ollama apiKey 'ollama' + resolvedBaseURL rule), §10.3 (ProviderConfigSchema)
    - src/core/ai/types.ts (ProviderId, ProviderConfig, LLMMessage, LLMOptions, LLMStreamChunk, ModelInfo)
  </read_first>
  <action>
    Create src/core/ai/ILLMProvider.ts with:
    - `export interface ILLMProvider` — §10.1 verbatim: `id: ProviderId; name: string; chat(messages: LLMMessage[], options: LLMOptions): AsyncIterable<LLMStreamChunk>; getModels(): Promise<ModelInfo[]>; validateConfig(config: ProviderConfig): Promise<boolean>; getAISDKModel(model: string): LanguageModel;`
    - `export function getAISDKModel(providerId: ProviderId, model: string, cfg: { apiKey?: string; baseURL?: string; fetch?: typeof globalThis.fetch }): LanguageModel` — the RESEARCH Pattern 1 switch, VERBATIM (openai → createOpenAI compatibility:'compatible'; anthropic → createAnthropic; gemini → createGoogleGenerativeAI; ollama → createOpenAI apiKey 'ollama', baseURL ?? 'http://localhost:11434/v1', compatibility 'compatible'). This is the ONLY place @ai-sdk/* packages are imported (Seam 1). Header notes the optional `fetch` cfg is the documented test seam (A6).
    Create the five adapters, each implementing ILLMProvider and delegating model construction to getAISDKModel:
    - OpenAIProvider.ts — `createOpenAIProvider(config: ProviderConfig): ILLMProvider` — id 'openai', default baseURL 'https://api.openai.com/v1' (computed resolvedBaseURL = customBaseURL ?? baseURL per §10.2), chat() delegates to a StreamAdapter-shaped consumer (declare the loop over getAISDKModel(model).chat? No — adapters expose the §10.1 contract; the actual SDK invocation lives in getAISDKModel + StreamAdapter; chat() can be implemented as an async generator over the SDK's streamText via the same pattern as StreamAdapter (03-03) — for Phase 3 the adapter's chat/getModels/validateConfig are thin; getModels() returns ModelInfo[] from config.models mapped to { id, label: id, contextWindow, supportsTools, group: id==='ollama' ? 'local' : 'cloud' }).
    - AnthropicProvider.ts — createAnthropicProvider(config) — id 'anthropic', default baseURL 'https://api.anthropic.com'.
    - GeminiProvider.ts — createGeminiProvider(config) — id 'gemini', default baseURL 'https://generativelanguage.googleapis.com'.
    - OllamaProvider.ts — createOllamaProvider(config) — id 'ollama', baseURL forced 'http://localhost:11434/v1' (customBaseURL ignored per §10.2), apiKey 'ollama' always.
    - OpenAICompatProvider.ts — `createOpenAICompatProvider(opts: { baseURL: string; apiKey?: string }): ILLMProvider` — returns an OpenAI-config provider with `id: 'openai'` (the four-ID rule, D-12) and the custom baseURL; header comment: this is a factory/config variant of OpenAIProvider, NEVER a 5th ProviderId (Appendix D maps deepseek-chat → providerId 'openai').
    Every catch calls debugLog(ERROR_CODES.X, …) per Golden Rule 9 (no empty catches; reuse PROVIDER_AUTH / NETWORK / PROVIDER_5XX / STREAM_FAILED as applicable — codes from 03-01).
    Do NOT import any provider package outside ILLMProvider.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/core/ai --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - grep '@ai-sdk/' across src/core/ai/ shows imports ONLY in ILLMProvider.ts (grep -rln "@ai-sdk/" src/core/ai/ | grep -v ILLMProvider.ts yields nothing).
    - getAISDKModel handles all four ProviderId cases; ollama case pins apiKey 'ollama' + baseURL default 'http://localhost:11434/v1'.
    - createOpenAICompatProvider returns id 'openai' (never a 5th value).
  </acceptance_criteria>
  <done>Four-ID adapter family + single factory switch compile; Seam 1 import boundary holds.</done>
</task>

<task type="auto">
  <name>Task 2: TierResolver (Appendix D) + privacyModeFromPrefs (D-13)</name>
  <files>src/core/ai/TierResolver.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md Appendix D (line ~5110 — TIER_TO_MODEL_CANDIDATES, TierResolveInput, TierResolveResult, resolveTier verbatim + Rules)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md D-13 (privacyModeFromPrefs mapping + 'local-only' reserved)
    - src/core/ai/types.ts (ProviderId)
  </read_first>
  <action>
    Create src/core/ai/TierResolver.ts — Appendix D verbatim:
    - `export type ModelTier = 'haiku' | 'flash';`
    - `export interface TierCandidate { providerId: ProviderId; model: string; }`
    - `export const TIER_TO_MODEL_CANDIDATES: Record<ModelTier, TierCandidate[]>` — haiku: [anthropic/claude-haiku-4-latest, openai/deepseek-chat, ollama/llama3.2:3b]; flash: [gemini/gemini-2.5-flash, anthropic/claude-haiku-4-latest, openai/deepseek-chat, ollama/qwen2.5:7b] (verbatim, as const).
    - `export type PrivacyMode = 'local-only' | 'prefer-local' | 'cloud-ok';`
    - `export interface TierResolveInput { tier: ModelTier; configuredProviders: Array<{ id: ProviderId; models: string[]; enabled: boolean; priority: number }>; privacyMode: PrivacyMode; }`
    - `export interface TierResolveResult { providerId: ProviderId; model: string; fallbackChain: TierCandidate[]; }`
    - `resolveTier(input): TierResolveResult | null` — verbatim algorithm (filter candidates by 'local-only', sort enabled by priority, pick first configured+capable candidate, fallbackChain = rest). Do NOT change the 'local-only' branch — it stays (reserved, unused in Phase 3 per D-13).
    - `export function privacyModeFromPrefs(prefs?: { allowCloudFallbackFromLocal?: boolean }): PrivacyMode` — pure helper (D-13): `false → 'prefer-local'`, `true → 'cloud-ok'`, undefined → 'prefer-local'. NEVER returns 'local-only' (reserved for a future explicit privacy toggle). Header comment documents D-13 rationale (the §1.5 boolean governs fallback DIRECTION, not global cloud-disable; the no-silent-local→cloud guarantee is enforced in ProviderRouter, never by filtering here).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - resolveTier('haiku', {openai/deepseek-chat enabled, priority 0}, 'cloud-ok') returns providerId 'openai', model 'deepseek-chat'; an enabled ollama with higher priority wins under 'prefer-local' when the haiku chain contains it.
    - resolveTier returns null when no configured provider is enabled+capable.
    - privacyModeFromPrefs({allowCloudFallbackFromLocal:false}) === 'prefer-local'; ({allowCloudFallbackFromLocal:true}) === 'cloud-ok'; (undefined) === 'prefer-local'; no call path returns 'local-only'.
    - Candidate table matches Appendix D byte-for-byte (model names never invented).
  </acceptance_criteria>
  <done>Tier resolution + privacy mapping implemented per Appendix D / D-13; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3: ProviderRegistry extension (D-21) + test</name>
  <files>src/core/ai/ProviderRegistry.ts, tests/core/ai/ProviderRegistry.test.ts</files>
  <read_first>
    - src/core/ai/ProviderRegistry.ts (current file — preserve registerActiveProvider/hasActiveProvider/getActiveProvider/subscribe/clear + lazy singleton; it imports only core/error)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md D-21 + D-14; 02-CONTEXT D-04 (no auto-wipe)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md "ProviderRegistry (modify — registry, event-driven)" (KeyVault error-emission analog)
    - tests/core/security/KeyVault.test.ts (fakeBrowser + fixture conventions, lines 13-31)
  </read_first>
  <action>
    Extend ProviderRegistry IN PLACE (01-CONTEXT B3; the file header already declares Phase 3 extends it). Keep it dependency-free: imports only core/error + type-only imports from '@/core/ai/types' (NO zustand/react — Pitfall 4). Add:
    - `type ProviderConfigPresence = { providerId: ProviderId; enabled: boolean; models: string[]; unreadable: boolean }` (internal Map<ProviderId, ProviderConfigPresence>).
    - `registerProvider(config: { providerId: ProviderId; models: string[]; enabled: boolean })`: idempotent upsert — sets presence, notifies; invalid id → REGISTRY_INIT debugLog + no-op (existing never-throws style).
    - `markProviderKeyUnreadable(providerId: ProviderId)`: sets unreadable: true + enabled: false, notifies, debugLog(ERROR_CODES.PROVIDER_KEY_UNREADABLE, …) — ONE shared state (decrypt failure / installSecret cleared / tampered ciphertext = same transition, per 02-CONTEXT D-04). NO auto-wipe, NO auto-regenerate.
    - `hasActiveProvider()`: true only when at least one registered provider is enabled AND not unreadable (D-21: unreadable = treated as unconfigured). `getActiveProvider()` unchanged semantics (returns an enabled non-unreadable providerId or undefined).
    - `getProviderInfo(): { providerId: ProviderId; models: string[]; enabled: boolean } | undefined` — the read path for ExecutorService's get-provider-info tool (D-04, consumed in 03-04).
    Create tests/core/ai/ProviderRegistry.test.ts (new file — the existing suites only exercise it indirectly):
    - registerProvider then hasActiveProvider() true; getProviderInfo returns models.
    - markProviderKeyUnreadable('openai') → hasActiveProvider() false (treated as unconfigured), getProviderInfo shows enabled:false + unreadable:true, and the transition logs PROVIDER_KEY_UNREADABLE (spy on debugLog).
    - unreadable + clear() → still no auto-regenerate (no provider re-enables itself).
    - invalid registerProvider input → REGISTRY_INIT logged, no throw.
    - dependency-free assertion: module imports contain no 'react'/'zustand' (static grep on the source file).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/ProviderRegistry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All test cases pass (register / unreadable-disables / no-auto-wipe / invalid-input / dependency-free grep).
    - `npx tsc --noEmit` passes.
    - grep -c "from 'react'\|from 'zustand'" src/core/ai/ProviderRegistry.ts == 0.
    - Existing consumers (SidePanelShell, SidePanelRouter gate on hasActiveProvider) still pass: `npx vitest run tests/components/sidepanel tests/entrypoints`.
  </acceptance_criteria>
  <done>Registry surfaces config presence + PROVIDER_KEY_UNREADABLE (enabled:false, unconfigured-treated); dependency-free; tests green.</done>
</task>

</tasks>

## Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-01 [unclassified — manual review] resolved here at the provider layer — the identity semantics are the four-ID enum; `custom baseURL` is the OpenAICompat factory variant (D-12); `fallback` is RouterAttemptState routing state. Both recorded in `must_haves.flagged_assumptions`.

## Artifacts This Phase Produces

- src/core/ai/ILLMProvider.ts (new): `ILLMProvider` interface, `getAISDKModel()` factory switch (Seam 1).
- src/core/ai/providers/OpenAIProvider.ts (new): `createOpenAIProvider(config)`.
- src/core/ai/providers/AnthropicProvider.ts (new): `createAnthropicProvider(config)`.
- src/core/ai/providers/GeminiProvider.ts (new): `createGeminiProvider(config)`.
- src/core/ai/providers/OllamaProvider.ts (new): `createOllamaProvider(config)` (apiKey 'ollama', baseURL 'http://localhost:11434/v1').
- src/core/ai/providers/OpenAICompatProvider.ts (new): `createOpenAICompatProvider({ baseURL, apiKey? })` (id stays 'openai').
- src/core/ai/TierResolver.ts (new): `ModelTier`, `TierCandidate`, `TIER_TO_MODEL_CANDIDATES`, `PrivacyMode`, `TierResolveInput`, `TierResolveResult`, `resolveTier()`, `privacyModeFromPrefs()`.
- src/core/ai/ProviderRegistry.ts (extended): `registerProvider()`, `markProviderKeyUnreadable()`, `getProviderInfo()`; `hasActiveProvider()` semantics updated for unreadable-disabled.
- tests/core/ai/ProviderRegistry.test.ts (new): registry gate + unreadable + dependency-free cases.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider adapters → provider endpoints | Vault-decrypted API keys cross here on every configured-provider call (R-3: surface-side only) |
| Registry ↔ wiring layer | PROVIDER_KEY_UNREADABLE typed error crosses this boundary; the registry never touches the vault (Pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | provider adapters / getAISDKModel | high | mitigate | Adapters never log apiKey/baseURL; every catch debugLogs with canonical codes (PROVIDER_AUTH/NETWORK/PROVIDER_5XX/STREAM_FAILED) and debugLog auto-routes through TraceRedactor (Phase-2, R-10); keys are held only in the config object passed to the SDK factory |
| T-03-02-02 | Spoofing | custom baseURL (OpenAICompat) | medium | accept | User-configured and deliberate (D-12); no PROXY_FETCH involved (direct surface→endpoint); host permissioning applies to the background proxy path only — documented behavior, not a bypass (RESEARCH Security Domain) |
| T-03-02-03 | Tampering | ProviderRegistry unreadable state | high | mitigate | markProviderKeyUnreadable is the single transition for decrypt-failure/cleared-secret/tampered-ciphertext; enabled:false + hasActiveProvider() false prevents the router from ever calling a broken provider; no auto-wipe/regenerate (02-CONTEXT D-04); test-asserted |
| T-03-02-SC | Tampering | npm installs | high | mitigate | All five packages approved by RESEARCH Package Legitimacy Audit (official repos, 6-20M/wk downloads); no [SUS]/[SLOP] remain; `@ai-sdk/ollama` [SLOP] already removed — not installed |
</threat_model>

<verification>
- `npx vitest run tests/core/ai/ProviderRegistry.test.ts` green.
- `npx vitest run tests/components/sidepanel tests/entrypoints` green (registry consumers unaffected).
- `npx tsc --noEmit` green.
- Seam-1 import grep: only ILLMProvider.ts imports @ai-sdk/*.
</verification>

<success_criteria>
- Four-provider adapter family + single factory switch compile and pass the import-boundary grep.
- resolveTier + privacyModeFromPrefs match Appendix D / D-13 exactly.
- Registry marks unreadable providers disabled without auto-recovery; dependency-free verified by grep + test.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-02-SUMMARY.md` when done
</output>
