---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/ai/persona/PersonaProfile.ts
  - src/core/ai/persona/PersonaInjector.ts
  - src/core/ai/UserPreferences.ts
  - tests/core/ai/persona/PersonaProfile.test.ts
  - tests/core/ai/persona/PersonaInjector.test.ts
autonomous: true
requirements: [RICH-R-01, RICH-R-02, RICH-R-10]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "DEFAULT_PERSONA is the Appendix N.1 constant verbatim (id 'nowpilot-default', tagline 'Your ServiceNow support co-pilot', personalityCore ['privacy-first','helpful','precise','humble'], behavioralDrivers ['prefers asking clarifying questions over guessing','cites sources when available'], tone 'professional-warm', brevity 'brief') — RICH-R-01"
    - "PersonaInjector.inject(stage, baseSystem, {prefs}) merges UserPreferences.personaOverrides (name/tone/brevity) at render time with ?? precedence — partial overrides leave seeded fields — RICH-R-02 + DONE-when 5"
    - "The persona block is prepended FIRST: inject returns `${block}\n\n${baseSystem}` — byte-stable per resolved persona (prompt-cache preserved, §1.3 / D-59) — RICH-R-10"
    - "UserPreferences.personaOverrides persist under chrome.storage.local key np_preferences via the chromeStorageAdapter pattern — overrides apply without a code change"
  artifacts:
    - path: "src/core/ai/persona/PersonaProfile.ts"
      provides: "PersonaProfileSchema (zod) + PersonaProfile type + DEFAULT_PERSONA (Appendix N.1 verbatim)"
      exports: ["PersonaProfileSchema", "PersonaProfile", "DEFAULT_PERSONA"]
    - path: "src/core/ai/persona/PersonaInjector.ts"
      provides: "PipelineStage union, resolvePersona (data-merge), buildPersonaBlock (byte-stable), PersonaInjector.inject (persona-first prepend)"
      exports: ["PipelineStage", "resolvePersona", "buildPersonaBlock", "PersonaInjector"]
    - path: "src/core/ai/UserPreferences.ts"
      provides: "Minimal UserPreferences zod schema (fastModel/balancedModel/personaOverrides {name?, tone?, brevity?}) + useUserPreferencesStore persisting to np_preferences"
      exports: ["UserPreferencesSchema", "UserPreferences", "useUserPreferencesStore", "PERSONA_TONE_ENUM", "PERSONA_BREVITY_ENUM"]
  key_links:
    - from: "src/core/ai/persona/PersonaInjector.ts"
      to: "src/core/ai/UserPreferences.ts"
      via: "resolvePersona reads prefs.personaOverrides for the data-merge"
      pattern: "personaOverrides|UserPreferences"
    - from: "src/core/ai/persona/PersonaInjector.ts"
      to: "src/core/ai/persona/PersonaProfile.ts"
      via: "DEFAULT_PERSONA is the base profile; buildPersonaBlock serializes the resolved profile"
      pattern: "DEFAULT_PERSONA|buildPersonaBlock"
  prohibitions:
    - "No persona text hard-coded into the Appendix A stage constants (planner/renderer/memoryExtractor) — they stay persona-free and byte-stable (Appendix A note, spec 4153)"
    - "No persistence of the profile itself (np_persona is Phase 8 RICH-R-05 — Phase 3 seeds the profile in code only)"
flagged_assumptions:
  - "RICH-R-01 (empty/encoding probe): PersonaProfileSchema validates field shapes; an empty-string override (e.g. override.name: '') is NOT applied — the data-merge uses ?? which treats '' as a value, so the plan mandates override fields be undefined-or-nonempty via z.string().min(1).optional() to keep seeded values authoritative"
  - "RICH-R-02 (adjacency/empty/ordering probe): inject() with no prefs or empty personaOverrides returns the base block unchanged; merge precedence is field-level (name/tone/brevity independently), never whole-profile replacement"
  - "RICH-R-10 (unclassified probe): byte-stability = exact string equality of buildPersonaBlock output for an identical resolved profile — the profile-version-keyed cache hash (PromptCacheAdapter.hashStableSections, plan 03-04) re-derives [SYSTEM] when overrides change"
---

<objective>
Seed the persona runtime foundation: the spec-verbatim `PersonaProfile` (RICH-R-01), the data-merge `PersonaInjector` (RICH-R-02, D-58), and the minimal `UserPreferences` shape + `np_preferences` persistence that makes `personaOverrides` apply without a code change (DONE-when 5). The injection contract established here — persona block prepended FIRST, byte-stable per persona — is what PromptCacheManager (plan 03-04) consumes as its single choke-point (D-59).

Purpose: Every AI call in the phase builds its system prompt from this foundation; chat and agent share it because there is exactly one injection path.
Output: `persona/PersonaProfile.ts`, `persona/PersonaInjector.ts`, `UserPreferences.ts` + the two persona test files required by spec §18.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §17.7 RICH-R-01/02/09/10, §21.6 PersonaProfile interface (3412-3419), Appendix N.1 (6084-6099) + N.2 (6105-6141) — verbatim, "do not paraphrase"
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md  # Pattern 2, D-57/D-58/D-59, assumptions A1 (UserPreferences shape)
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md  # PersonaProfile/PersonaInjector sections + shared patterns (zod cross-boundary, chromeStorageAdapter)
@src/core/theme/chromeStorageAdapter.ts  # chromeStorageAdapter('local') persist pattern
@src/core/workspace/WorkspaceStore.ts  # zustand+immer+persist+partialize analog (lines 89-199)
</context>

<tasks>

<task type="auto">
  <name>Task 1: PersonaProfile — Appendix N.1 verbatim + schema test</name>
  <files>src/core/ai/persona/PersonaProfile.ts, tests/core/ai/persona/PersonaProfile.test.ts</files>
  <read_first>@.planning/PRODUCT_SPEC_v0_1.md Appendix N.1 (6084-6099 — the verbatim DEFAULT_PERSONA, "do not paraphrase") + §21.6 PersonaProfile interface (3412-3419) · 03-RESEARCH.md Phase Requirements RICH-R-01 row + D-57 note (spec block is authoritative over the CONTEXT paraphrase) · 03-PATTERNS.md PersonaProfile section</read_first>
  <action>Create `src/core/ai/persona/PersonaProfile.ts` per Appendix N.1 verbatim (spec 6084-6099, "Do not paraphrase"): a zod `PersonaProfileSchema` + inferred `PersonaProfile` type with fields identity {name, tagline, domain}, personalityCore (string array), behavioralDrivers (string array), languageStyle {tone, vocabulary, brevity}, emotionalRepertoire (string array), plus `id: string`. The tone field must be zod-validated against the locked §21.6 enum 'professional-warm' | 'concise' | 'friendly' and brevity against 'brief' | 'balanced' | 'detailed' (D-58). Export `DEFAULT_PERSONA` with the EXACT Appendix N.1 values: id 'nowpilot-default'; identity.name 'NowPilot'; tagline 'Your ServiceNow support co-pilot'; domain 'ServiceNow support engineering, technical troubleshooting, and knowledge management'; personalityCore ['privacy-first','helpful','precise','humble']; behavioralDrivers ['prefers asking clarifying questions over guessing','cites sources when available']; languageStyle.tone 'professional-warm', vocabulary 'technical but accessible to support engineers', brevity 'brief'; emotionalRepertoire ['empathy','encouragement','curiosity']. NOTE: D-57's CONTEXT summary paraphrases tagline/behavioralDrivers — the spec block is authoritative. Do NOT persist the profile (np_persona is Phase 8). Write `tests/core/ai/persona/PersonaProfile.test.ts`: assert DEFAULT_PERSONA fields verbatim (string equality for every field), assert schema rejects an invalid tone and invalid brevity, and asserts partial-profile construction is impossible without all required fields. Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>PersonaProfile.test.ts green: DEFAULT_PERSONA field-for-field string equality with Appendix N.1; schema rejects invalid tone ('casual') and invalid brevity ('chatty'); missing required field fails parse; `pnpm run verify:phase-3` green.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>PersonaProfile.test.ts green; DEFAULT_PERSONA matches Appendix N.1 field-for-field; schema enforces the locked tone/brevity enums.</done>
</task>

<task type="auto">
  <name>Task 2: Minimal UserPreferences + np_preferences persistence</name>
  <files>src/core/ai/UserPreferences.ts</files>
  <read_first>@src/core/workspace/WorkspaceStore.ts lines 89-199 (zustand+immer+persist+chromeStorageAdapter analog) · @src/core/theme/chromeStorageAdapter.ts (adapter usage) · 03-RESEARCH.md Open Question 2 (UserPreferences location recommendation) + D-54/D-58 field contract · 03-PATTERNS.md "Store persistence via Zustand" shared pattern</read_first>
  <action>Create `src/core/ai/UserPreferences.ts` (RESEARCH Open Q2 — separate file so plans 01/02 stay parallel-safe on types.ts): a zod `UserPreferencesSchema` with `fastModel?: string`, `balancedModel?: string` (D-54 write-through targets), and `personaOverrides?: { name?: string; tone?: 'professional-warm' | 'concise' | 'friendly'; brevity?: 'brief' | 'balanced' | 'detailed' }` — override strings z.string().min(1).optional() so empty strings are rejected (empty-string override must NOT clobber the seeded value — flagged assumption). Export `PERSONA_TONE_ENUM` and `PERSONA_BREVITY_ENUM` as const arrays. Create a `useUserPreferencesStore` zustand store (create + persist + chromeStorageAdapter('local') + partialize + version 1) persisted under the key `np_preferences` (chrome.storage.local, non-secrets per §15.2 partition rules). Add a code comment marking this minimal shape as the Phase-8/10 supersession point (memory phases own the full UserPreferences). The store exposes `setFastModel`, `setBalancedModel`, `setPersonaOverrides` actions + a `hydrate()` used at boot. No secrets live here (apiKey stays in encrypted np_providers). Run `pnpm run verify:phase-3` (tsc must pass).</action>
  <acceptance_criteria>tsc passes; UserPreferencesSchema parses {fastModel, balancedModel, personaOverrides {name, tone, brevity}} and REJECTS an empty-string override ('' fails z.string().min(1)); store persists under key 'np_preferences' via chromeStorageAdapter('local'); version field = 1.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>UserPreferencesSchema validates the three fields; the store persists under np_preferences via chromeStorageAdapter('local'); an empty-string override is rejected by the schema.</done>
</task>

<task type="auto">
  <name>Task 3: PersonaInjector — data-merge + byte-stable persona-first prepend</name>
  <files>src/core/ai/persona/PersonaInjector.ts, tests/core/ai/persona/PersonaInjector.test.ts</files>
  <read_first>@.planning/PRODUCT_SPEC_v0_1.md Appendix N.2 (6105-6141 — resolvePersona/buildPersonaBlock/inject semantics) · 03-RESEARCH.md Pattern 2 (persona-first cached [SYSTEM]) + D-58 (data-merge enums) + D-59 (choke-point) · @src/core/ai/persona/PersonaProfile.ts (created task 1) · @src/core/ai/UserPreferences.ts (created task 2 — the import target replacing the non-existent @/core/memory/types)</read_first>
  <action>Create `src/core/ai/persona/PersonaInjector.ts` per Appendix N.2 verbatim semantics (spec 6105-6141): export `PipelineStage = 'planner' | 'executor' | 'renderer' | 'memoryExtractor'` (memoryExtractor reserved for the future owner — Phase 3 never integrates it, D-59); `resolvePersona(base: PersonaProfile, prefs?: UserPreferences): PersonaProfile` — pure data-merge: override.name → identity.name, override.tone → languageStyle.tone, override.brevity → languageStyle.brevity, each with `??` so partial overrides leave seeded fields (D-58, confirmed against spec 656-660 / §21.6 / §17.7.5); `buildPersonaBlock(persona): string` — serializes the resolved profile into a byte-stable block (same persona → identical bytes, §1.3); `PersonaInjector.inject(stage, baseSystem, opts?: { persona?: PersonaProfile; prefs?: UserPreferences }): string` — resolves persona (default DEFAULT_PERSONA), builds the block, returns `${block}\n\n${baseSystem}` (persona FIRST, inside the cached [SYSTEM] section per D-59). Import `UserPreferences` from `../UserPreferences` (the Appendix N.2 `@/core/memory/types` target does not exist — this is the Phase-3 supply point, flagged assumption A1). Write `tests/core/ai/persona/PersonaInjector.test.ts`: (a) no prefs → base block + baseSystem unchanged; (b) full overrides (name/tone/brevity) are reflected in the block; (c) partial overrides leave unset fields from the seed; (d) byte-stability — two calls with identical inputs return identical strings; different overrides produce different blocks; (e) persona-first ordering — the block is the string PREFIX of the returned prompt; (f) per-stage — inject works for all four PipelineStage values (RICH-R-10). Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>PersonaInjector.test.ts green (6 case groups); resolvePersona({...base}, {personaOverrides:{tone:'concise'}}) returns tone 'concise' with name/tagline from the seed (partial merge); inject output startsWith(buildPersonaBlock(resolved)); identical inputs → identical full-string output; all four PipelineStage values accepted; `pnpm run verify:phase-3` green.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>PersonaInjector.test.ts green (6 case groups); data-merge semantics match D-58; persona block is a byte-stable prefix of the returned system prompt for every PipelineStage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserPreferences → PersonaInjector | Operator-controlled override values (name/tone/brevity) cross into every system prompt — the only untrusted-ish input in this plan, and it is operator-owned (not host-page data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-04 | Tampering | personaOverrides → system prompt | low | mitigate | Overrides are zod-validated against the locked tone/brevity enums + non-empty string min(1) (empty strings rejected); the profile is a seeded constant; Phase 3 has NO host-page/content-script data path into persona (extraction is Phase 6) — prompt-injection via persona is structurally impossible this phase |
| T-3-05 | Information Disclosure | np_preferences storage | low | accept | Non-secret preferences (fastModel/balancedModel/personaOverrides) in chrome.storage.local per §15.2 partition rules; API keys remain in encrypted np_providers (Phase 2) — no key can reach this store |
</threat_model>

<verification>
- `pnpm run verify:phase-3` green after every task commit.
- PersonaInjector test asserts byte-stability (identical inputs → identical bytes) — the prompt-cache preservation invariant (§1.3).
- Grep guard: `src/core/prompts/index.ts` is NOT touched by this plan (stage constants stay persona-free — verified in plan 03-01; cross-check `grep -c 'NowPilot' src/core/prompts/index.ts == 0`).
<!-- planner-discipline-allow: NowPilot -->
</verification>

<success_criteria>
1. DEFAULT_PERSONA is spec-verbatim (RICH-R-01) — test-asserted field-for-field.
2. PersonaInjector data-merges overrides without mutating the base profile; partial overrides preserved (RICH-R-02, D-58).
3. Overrides apply via UserPreferences.personaOverrides + np_preferences — no code change (DONE-when 5).
4. The inject contract (persona-first, byte-stable) is what plan 03-04's PromptCacheManager consumes (RICH-R-10).
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-3 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/ai/persona/PersonaProfile.ts | PersonaProfileSchema, PersonaProfile, DEFAULT_PERSONA (Appendix N.1) | **[this plan]** |
| src/core/ai/persona/PersonaInjector.ts | PipelineStage, resolvePersona, buildPersonaBlock, PersonaInjector.inject (Appendix N.2) | **[this plan]** |
| src/core/ai/UserPreferences.ts | UserPreferencesSchema, UserPreferences, useUserPreferencesStore (np_preferences), PERSONA_TONE_ENUM, PERSONA_BREVITY_ENUM | **[this plan]** |
| src/core/ai/types.ts | spine types (ProviderId/ModelTier/PlannerDecisionSchema/StreamEvent/…) | 03-01 |
| src/core/ai/ILLMProvider.ts | provider interface | 03-01 |
| src/core/ai/StreamAdapter.ts | SSE rebuild + wire adapters | 03-01 + 03-03 |
| src/core/ai/StructuredOutput.ts | Appendix L requestJson | 03-01 |
| src/core/ai/PlannerService.ts | §1.2 planner | 03-01 |
| src/core/prompts/index.ts | Appendix A canonical PROMPTS | 03-01 |
| src/core/ai/providers/*.ts | 5 provider implementations | 03-03 |
| src/core/ai/ProviderRegistry.ts / TierResolver.ts / ProviderRouter.ts | routing stack | 03-05 |
| src/core/ai/PromptCacheManager.ts / PromptCacheAdapter.ts | prompt assembly + cache hints | 03-04 |
| src/core/ai/ExecutorService.ts / RendererService.ts / toolSchemas.ts / ChunkBuffer.ts | pipeline stages + tools + buffer | 03-04 |
| src/core/ai/AgentOrchestrator.ts | runAgentTurn (Appendix I) | 03-06 |
| src/core/runtime/workerState.ts | + ActiveStreamState (§20.6) | 03-04 |
| src/types/storage.ts | + 'append-chat-turn' | 03-07 |
| src/components/chat/useChatStreaming.ts | re-point at AgentOrchestrator | 03-07 |
| src/components/options/OptionsPage.tsx | D-50/D-54 fields | 03-07 |
| tests/core/ai/** | 8 test files + fixtures/ | all plans |