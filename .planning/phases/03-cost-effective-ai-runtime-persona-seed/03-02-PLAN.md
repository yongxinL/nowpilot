---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
- src/core/ai/ILLMProvider.ts
- src/core/ai/providers/OpenAIProvider.ts
- src/core/ai/providers/AnthropicProvider.ts
- src/core/ai/providers/GeminiProvider.ts
- src/core/ai/providers/OllamaProvider.ts
- src/core/ai/providers/OpenAICompatProvider.ts
- src/core/ai/TierResolver.ts
- src/core/ai/ProviderRegistry.ts
- tests/core/ai/ProviderRegistry.test.ts
autonomous: true
requirements: [AI-01]

<!-- FINAL v2 (2026-08-10): + ordered Tasks section. No fix drift — F-1 ('compatible' everywhere) already
     correct; providers/registry/TierResolver clean. privacyModeFromPrefs = false→'prefer-local', true→'cloud-ok'. -->

### must_haves:
truths:
- "The four provider adapters (openai/anthropic/gemini/ollama) implement the §10.1 ILLMProvider contract; getAISDKModel is the single factory switch and the ONLY place provider packages are imported (Seam 1). ILLMProvider.ts imports `ProviderId` from './types' (src/core/ai/types.ts canonical home, R-1) — it does NOT re-declare the four-ID enum the way the §10.1 reference block shows (that block is a shape sketch; the single canonical declaration is 03-01's ai/types.ts)."
- "All openai-id endpoints use compatibility: 'compatible' (F-1 — local/OpenAI-compatible only; the official OpenAI endpoint is not targeted); Ollama rides createOpenAI({ apiKey: 'ollama', baseURL: 'http://localhost:11434/v1', compatibility: 'compatible' }) — no @ai-sdk/ollama package (§10.2, RESEARCH verified)."
- "OpenAICompatProvider is a factory createOpenAICompatProvider() returning a provider with id: 'openai' + custom baseURL — NOT a 5th ProviderId (D-12)."
- "TierResolver.resolveTier returns the cheapest capable candidate and never invents a model name; returns null when no candidate matches; the 'local-only' filter branch stays reserved/unused in Phase 3 (D-13). privacyModeFromPrefs maps allowCloudFallbackFromLocal false → 'prefer-local', true → 'cloud-ok' (Q5); 'local-only' reserved."
- "ProviderRegistry extension (D-21): a PROVIDER_KEY_UNREADABLE emission marks the provider enabled:false (treated as unconfigured); no auto-wipe, no auto-regenerate; the registry stays dependency-free (no zustand/react imports)."
artifacts:
- src/core/ai/ILLMProvider.ts
- src/core/ai/providers/{OpenAI,Anthropic,Gemini,Ollama,OpenAICompat}Provider.ts
- src/core/ai/TierResolver.ts
- src/core/ai/ProviderRegistry.ts
- tests/core/ai/ProviderRegistry.test.ts
key_links:
- "getAISDKModel is the only import site of @ai-sdk/* packages (grep-verified)."
- "ProviderRegistry feeds ExecutorService's get-provider-info tool (03-04) and the surface-mount wiring (03-09); the PROVIDER_KEY_UNREADABLE typed error crosses the wiring boundary (D-21)."
flagged_assumptions:
- "A6 [research]: the fetch test seam is an OPTIONAL cfg field on getAISDKModel (documented extension of the §10.1 signature, per AI-SPEC)."
- "A4 [research]: model ids pass through getAISDKModel to the provider factory's callable with the provider's own id naming (deepseek-chat via the openai factory)."
prohibitions:
- "No 5th ProviderId; OpenAICompat stays a factory (D-12)."
- "No provider package imported outside ILLMProvider.ts (Seam 1)."
- "No ProviderId re-declaration in ILLMProvider.ts — import from './types' (src/core/ai/types.ts, R-1)."
- "No compatibility: 'strict' on any openai-id endpoint (F-1 — 'compatible' everywhere)."
- "No zustand/react import into ProviderRegistry (Pitfall 4 dependency-free)."
- "No auto-wipe / auto-regenerate on PROVIDER_KEY_UNREADABLE (02-CONTEXT D-04)."
- "No 'local-only' filter usage in resolveTier this phase (D-13 reserved)."

Purpose: AI-01 — four-provider support with an OpenAI-compatible custom baseURL. Every Router/Planner/Renderer call and the surface wiring depends on these adapters and the registry's config-presence + unreadable-state semantics.
Output: The provider adapter family + tier resolution + extended registry, with the registry's PROVIDER_KEY_UNREADABLE behavior proven by test.
<execution_context>@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md</execution_context>

### Tasks (ordered — do not reorder; each maps to a truth/artifact)
1. **ILLMProvider.ts (new).** Define the §10.1 ILLMProvider interface + getAISDKModel() single factory switch (Seam 1) — the ONLY import site of @ai-sdk/* packages. Accept the optional `cfg.fetch` test seam (A6).
2. **OpenAIProvider.ts (new).** createOpenAIProvider(config) via createOpenAI({ apiKey, baseURL, compatibility: 'compatible' }) (F-1).
3. **AnthropicProvider.ts (new).** createAnthropicProvider(config) via createAnthropic.
4. **GeminiProvider.ts (new).** createGeminiProvider(config) via createGoogleGenerativeAI.
5. **OllamaProvider.ts (new).** createOllamaProvider(config): createOpenAI({ apiKey: 'ollama', baseURL: 'http://localhost:11434/v1', compatibility: 'compatible' }) (§10.2) — no @ai-sdk/ollama.
6. **OpenAICompatProvider.ts (new).** createOpenAICompatProvider({ baseURL, apiKey? }) returning id:'openai' + custom baseURL (D-12 factory, NOT a 5th id).
7. **TierResolver.ts (new).** ModelTier, TierCandidate, TIER_TO_MODEL_CANDIDATES, PrivacyMode, TierResolveInput/Result, resolveTier() (cheapest-capable, null when none; 'local-only' branch present but unused), privacyModeFromPrefs() (false→'prefer-local', true→'cloud-ok'). Match Appendix D / D-13 verbatim.
8. **ProviderRegistry.ts (extend).** registerProvider(), markProviderKeyUnreadable() (enabled:false, no auto-wipe), getProviderInfo(); update hasActiveProvider() for unreadable-disabled. Keep dependency-free (no zustand/react).
9. **ProviderRegistry.test.ts (new).** Registry gate + PROVIDER_KEY_UNREADABLE-disabled + dependency-free cases.
10. **Verify.** Import-boundary grep (no @ai-sdk/* outside ILLMProvider.ts); grep no `compatibility: 'strict'`; resolveTier/privacyModeFromPrefs match Appendix D/D-13; test:ai green.

### Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-01 [unclassified — manual review] resolved here at the provider layer — the identity semantics are the four-ID enum; custom baseURL is the OpenAICompat factory variant (D-12); fallback is RouterAttemptState routing state. Both recorded in must_haves.flagged_assumptions.

### Artifacts This Phase Produces
- src/core/ai/ILLMProvider.ts (new): ILLMProvider interface, getAISDKModel() factory switch (Seam 1).
- src/core/ai/providers/OpenAIProvider.ts (new): createOpenAIProvider(config) (compatibility: 'compatible', F-1).
- src/core/ai/providers/AnthropicProvider.ts (new): createAnthropicProvider(config).
- src/core/ai/providers/GeminiProvider.ts (new): createGeminiProvider(config).
- src/core/ai/providers/OllamaProvider.ts (new): createOllamaProvider(config) (apiKey 'ollama', baseURL 'http://localhost:11434/v1', compatibility 'compatible').
- src/core/ai/providers/OpenAICompatProvider.ts (new): createOpenAICompatProvider({ baseURL, apiKey? }) (id stays 'openai').
- src/core/ai/TierResolver.ts (new): ModelTier, TierCandidate, TIER_TO_MODEL_CANDIDATES, PrivacyMode, TierResolveInput, TierResolveResult, resolveTier(), privacyModeFromPrefs().
- src/core/ai/ProviderRegistry.ts (extended): registerProvider(), markProviderKeyUnreadable(), getProviderInfo(); hasActiveProvider() semantics updated for unreadable-disabled.
- tests/core/ai/ProviderRegistry.test.ts (new): registry gate + unreadable + dependency-free cases.
<threat_model>

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider adapters → provider endpoints | Vault-decrypted API keys cross here on every configured-provider call (R-3: surface-side only) |
| Registry ↔ wiring layer | PROVIDER_KEY_UNREADABLE typed error crosses this boundary; the registry never touches the vault (Pitfall 4) |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | provider adapters / getAISDKModel | high | mitigate | Adapters never log apiKey/baseURL; every catch debugLogs with canonical codes (PROVIDER_AUTH/NETWORK/PROVIDER_5XX/STREAM_FAILED) auto-routed through TraceRedactor (Phase-2, R-10); keys held only in the config object passed to the SDK factory |
| T-03-02-02 | Spoofing | custom baseURL (OpenAICompat) | medium | accept | User-configured and deliberate (D-12); no PROXY_FETCH (direct surface→endpoint); host permissioning applies to the background proxy path only — documented, not a bypass |
| T-03-02-03 | Tampering | ProviderRegistry unreadable state | high | mitigate | markProviderKeyUnreadable is the single transition for decrypt-failure/cleared-secret/tampered-ciphertext; enabled:false + hasActiveProvider() false prevents the router from ever calling a broken provider; no auto-wipe/regenerate (02-CONTEXT D-04); test-asserted |
| T-03-02-SC | Tampering | pnpm installs | high | mitigate | All five packages approved by RESEARCH Package Legitimacy Audit; @ai-sdk/ollama [SLOP] already removed — not installed |
</threat_model>
<success_criteria>
- Four-provider adapter family + single factory switch compile and pass the import-boundary grep; no compatibility: 'strict' anywhere (F-1).
- resolveTier + privacyModeFromPrefs match Appendix D / D-13 exactly (false→'prefer-local', true→'cloud-ok').
- Registry marks unreadable providers disabled without auto-recovery; dependency-free verified by grep + test.
</success_criteria>
