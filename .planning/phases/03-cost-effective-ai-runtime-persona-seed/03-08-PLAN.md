---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 08
type: execute
wave: 5
depends_on: ["03-06", "03-07"]
files_modified:
  - src/core/i18n/strings.ts
  - src/components/pages/useStreamingLLM.ts
  - src/components/pages/ChatPage.tsx
  - src/components/sidepanel/SidePanelShell.tsx
  - tests/components/pages/useStreamingLLM.test.tsx
  - tests/components/pages/ChatPage.test.tsx
autonomous: true
requirements: [AI-03, AI-06]

must_haves:
  truths:
    - "ChatPage renders Ant Design X Bubble/Bubble.List + Sender (D-01, approved §7.2): user bubble right (variant filled, colorPrimaryBg light / colorPrimary @18% dark, radius 12), assistant bubble left (colorBgContainer, 1px colorBorder, name 'NowPilot' — never the persona name), BubbleList role='log' aria-live='polite' (spec §17.6)."
    - "Streaming surface state machine (UI-SPEC): idle (empty → 'Start a conversation' STR.chat.empty one-liner) / streaming (assistant Bubble appended immediately with streaming caret colorPrimary @60%, text grows via ChunkBuffer — NEVER a spinner, never motion-driven reveals §12.6) / completed (final text, caret removed) / failed (partial text retained + 'Provider error.' colorError line + Retry colorPrimary text action STR.chat.retry; §C.2 code goes to debugLog only — never raw in the UI) / offline (NETWORK-class after router retries → 'No network. Retrying when back online.' STR.chat.offline muted 12px notice)."
    - "Sender: controlled value/onChange, placeholder STR.chat.askPlaceholder, circular SendOutlined send button (colorPrimary when input non-empty AND idle, disabled during stream — one stream per session §17.5, no stop control this phase), focus ring colorPrimary 2px; retry re-sends the last user input through the same runAgentTurn path with a NEW operationId (failed bubble's partial text replaced)."
    - "The co-located hook (D-01 useStreamingLLM.ts, Phase-7 promotion target to src/hooks/) sends through runAgentTurn with a contextHelper-built OptimizedContext (D-02) — NOT React-side prompt assembly (Golden Rule 3); it maps LLMStreamChunk deltas into ChunkBuffer; abort() cancels the generation so no orphaned request bills tokens."
    - "The hook does NOT write np_active_stream (Setting registry declares it writeAllowed: false — D-11); Phase-3 stream state is in-memory per-surface (D-03/D-14); the J.2 session-key persistence is the Phase-7 full-hook behavior."
    - "Provider-unconfigured gate (D-21): ChatPage is not rendered without an enabled provider — the shell Alert 'Configure an AI provider in Settings first.' (STR.chat.noProvider) shows; PROVIDER_KEY_UNREADABLE-disabled providers collapse into the same gate. The shell's Phase-1 disabled Input footer is REPLACED by ChatPage's Sender when a provider is active (no double composer)."
    - "RICH fencing (D-03): NO welcome cards, Prompts, clarification/follow-up chips, persona header, stage indicators, chat-history persistence, Agent toggle, or action panels."
  artifacts:
    - src/core/i18n/strings.ts
    - src/components/pages/useStreamingLLM.ts
    - src/components/pages/ChatPage.tsx
    - src/components/sidepanel/SidePanelShell.tsx
    - tests/components/pages/{useStreamingLLM,ChatPage}.test.tsx
  key_links:
    - "ChatPage.onSubmit → hook.send(userInput) → contextHelper (03-07) → ProviderRouter.createStageInvocation (03-05) → runAgentTurn (03-06) → onStreamDelta → ChunkBuffer → growing Bubble text."
    - "SidePanelShell/StandaloneShell render the SAME ChatPage; the Sender lives inside ChatPage (D-01) — one composer per surface."
  flagged_assumptions:
    - "AI-06 [unclassified — manual review]: the Phase-3 surface is the UI-SPEC minimal subset (Bubble/Sender streaming) — the 16 UI-SPEC 'covered' state rows are lifted as truths above; the 7 'dismissed' rows (partial/empty/loading/overflow/zero-one-many on E2/E3/E5) are structurally inapplicable per UI-SPEC and not built."
    - "AI-03 [empty]: an empty user input never sends (Sender disabled); the empty conversation renders the centered cue; no message-store persistence this phase (D-03 — ChatHistoryDB consumers land Phase 7)."
    - "AI-03 [encoding]: Bubble text is plain text (no rich markdown until Phase 7's DOMPurify pipeline); ChunkBuffer throttle counts JS string length."
  prohibitions:
    - "No RICH elements (welcome/prompts/chips/persona header/stage indicators/history/Agent toggle/action panels — D-03)."
    - "No React-side prompt assembly — the hook imports contextHelper, never PROMPTS (Golden Rule 3)."
    - "No np_active_stream writes (writeAllowed: false — D-11)."
    - "No raw §C.2 codes or error messages in the UI (debugLog only, R-10)."
    - "No spinner/typewriter text reveals (§12.6 — ChunkBuffer rAF + Bubble streaming caret only)."
    - "No [Switch Provider] token rendering (Phase-7 Options; the STR.chat.errorRetry string stays untouched — Golden Rule 2)."
    - "No persona name in the UI (assistant identity is always 'NowPilot'; persona header fenced)."
---

<objective>
The minimal functional streaming chat surface (AI-03 + AI-06): the co-located `useStreamingLLM` hook (D-01) + the ChatPage upgrade to AntD X Bubble/Sender with the UI-SPEC stream state machine, the two new STR keys, and the SidePanelShell composer reconciliation.

Purpose: The user's first real chat experience — send a message, watch it stream through ChunkBuffer into the Bubble, see honest failure states (error + Retry, offline notice), all through the mandated runAgentTurn path (Golden Rule 3). Everything RICH is fenced to Phase 7 (D-03).
Output: The streaming hook + upgraded ChatPage + shell reconciliation + their component tests.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-UI-SPEC.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md
@.planning/PRODUCT_SPEC_v0_1.md
@src/components/pages/ChatPage.tsx
@src/components/sidepanel/SidePanelShell.tsx
@src/core/i18n/strings.ts
@src/core/ai/AgentOrchestrator.ts
@src/core/ai/contextHelper.ts
@src/core/ai/ChunkBuffer.ts
</context>

<tasks>

<!-- planner-discipline-allow: np_active_stream -->
<task type="auto" tdd="true">
  <name>Task 1: STR keys + co-located useStreamingLLM hook + test</name>
  <files>src/core/i18n/strings.ts, src/components/pages/useStreamingLLM.ts, tests/components/pages/useStreamingLLM.test.tsx</files>
  <read_first>
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-UI-SPEC.md Copywriting Contract (new keys STR.chat.send / STR.chat.retry — verbatim) + Interaction Contract (stream state machine) + Appendix (J.2 reference shape)
    - .planning/PRODUCT_SPEC_v0_1.md Appendix J.2 (line ~5653 — reference hook; Phase-3 keeps the minimal 3-state subset idle/streaming/completed/failed — no np_active_stream writes, no preparing/waiting/aborting variants)
    - src/core/i18n/strings.ts (chat block — add keys, never reword existing)
    - src/core/ai/contextHelper.ts (buildOptimizedContext — the ONLY context builder the hook may use)
    - src/core/ai/AgentOrchestrator.ts (runAgentTurn + capsForTier + StageResolver)
    - src/core/ai/ProviderRouter.ts (getProviderRouter — createStageInvocation for the renderer stage)
    - src/core/ai/persona/personaConfig.ts (readPersona, readPersonaPrefs)
    - src/core/ai/ChunkBuffer.ts (createChunkBuffer)
    - src/core/error/errorCodes.ts + debugLog.ts
  </read_first>
  <behavior>
    - Test 1: send('hello') sets state streaming, invokes runAgentTurn with a contextHelper-built context (spy), and deltas fed through onStreamDelta reach the ChunkBuffer listener (text state grows) before completion; on resolve, state becomes completed and text === streamedText.
    - Test 2: runAgentTurn rejects (provider error) → state failed with code/message; partial streamed text retained.
    - Test 3: abort() cancels the in-flight generation (AbortController signal observed by the runAgentTurn spy) and the turn rejects with AbortError → state failed (or completed-if-already-done) — never a hung streaming state.
    - Test 4: send() while streaming aborts the previous stream first (one stream per session §17.5).
    - Test 5: the hook imports contextHelper + runAgentTurn + ChunkBuffer, and NEVER imports PROMPTS or assembles prompt text (static grep).
  </behavior>
  <action>
    Modify src/core/i18n/strings.ts — add exactly two verbatim keys to the chat block: `send: 'Send',` and `retry: 'Retry',` (UI-SPEC Copywriting Contract — Golden Rule 2; do not reword any existing string).
    Create src/components/pages/useStreamingLLM.ts (co-located with ChatPage — D-01; header documents the 3-state lifecycle skeleton→minimal→full and the Phase-7 promotion to src/hooks/useStreamingLLM.ts):
    - `export type ChatStreamState = { state: 'idle' } | { state: 'streaming'; operationId: string } | { state: 'completed'; operationId: string; text: string } | { state: 'failed'; operationId: string; code: string; message: string }` (minimal subset of Appendix J.2's ActiveStreamState — no preparing/waiting/aborting, D-03).
    - `export function useStreamingLLM()` returns { text, state, send, abort, clearConversation } where:
      - send(userInput): guards empty input (no-op); aborts any in-flight stream (one stream per session §17.5); creates a fresh AbortController; resets the ChunkBuffer; sets state streaming with crypto.randomUUID() operationId; awaits readPersona()/readPersonaPrefs(); builds the context via buildOptimizedContext({ operationId, tier: 'small', userInput, persona, preferences, toolSchemas: [] }) (D-02 — Golden Rule 3); resolves the renderer-stage invocation via getProviderRouter().createStageInvocation({ tier: 'flash', operationId, configuredProviders, privacyMode }); calls runAgentTurn({ operationId, userInput, context, abortSignal, tier: capsForTier('small'), invocation: stageResolver, onStreamDelta: (d) => bufferRef.current.enqueue(d) }); on resolve: bufferRef.current.flushNow(), set state completed with the accumulated streamedText; on reject: map to state failed { code, message } (debugLog already fired downstream — do not double-log raw bodies).
      - abort(): abortRef.current?.abort() — cancels generation (no orphaned billing).
      - The ChunkBuffer onFlush(setText) subscription lives in a useEffect (Appendix J.2 pattern).
      - NO chrome.storage.session writes (np_active_stream is writeAllowed: false — D-11); stream state is in-memory per-surface (D-14).
    Create tests/components/pages/useStreamingLLM.test.tsx per the behavior block (React Testing Library renderHook in the jsdom-align env; runAgentTurn/contextHelper/personaConfig vi.mock'd or spies; ChunkBuffer real — fake timers for flush assertions).
  </action>
  <verify>
    <automated>npx vitest run tests/components/pages/useStreamingLLM.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass (streaming/completed/failed/abort/one-stream-per-session).
    - grep -c "from '@/core/prompts'" src/components/pages/useStreamingLLM.ts == 0 (Golden Rule 3 — no prompt assembly in the hook).
    - grep -c "np_active_stream" src/components/pages/useStreamingLLM.ts == 0 (no session writes).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Streaming hook sends via runAgentTurn + contextHelper, streams into ChunkBuffer, aborts cleanly; no prompt assembly, no session writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChatPage Bubble/Sender upgrade + SidePanelShell composer reconciliation + test</name>
  <files>src/components/pages/ChatPage.tsx, src/components/sidepanel/SidePanelShell.tsx, tests/components/pages/ChatPage.test.tsx</files>
  <read_first>
    - src/components/pages/ChatPage.tsx (current empty-state shell to replace)
    - src/components/sidepanel/SidePanelShell.tsx (current unconditional disabled Input footer at lines 67-75 — the UI-SPEC composer reconciliation)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-UI-SPEC.md (ChatPage composition, Stream state machine table, Copywriting Contract, Color/Spacing/Accent sections, Interaction Contract, Fenced-out list)
    - @ant-design/x installed types (Bubble, Bubble.List, Sender — READ node_modules/@ant-design/x/dist/index.d.ts for the exact props: streaming, placement, variant, shape, avatar/name; Sender value/onChange/onSubmit/disabled/loading)
    - src/core/i18n/strings.ts (STR.chat.send/retry from Task 1)
  </read_first>
  <behavior>
    - Test 1: empty state renders the centered 'Start a conversation' one-liner and a Sender with the send button disabled (empty input).
    - Test 2: typing enables the send button; onSubmit with non-empty input calls the hook's send (spy) and the user bubble renders right.
    - Test 3: while streaming, the assistant Bubble has streaming=true (caret path) and the send button is disabled.
    - Test 4: on failed state, the assistant bubble retains partial text + renders 'Provider error.' (colorError line) + a Retry action; clicking Retry re-sends the last user input (send spy called with the same text).
    - Test 5: offline notice renders for NETWORK-class failures.
    - Test 6: the bubble list has role='log' and aria-live='polite'; no fenced elements (no 'Welcome', no 'Prompts' chip, no persona name render — the assistant name is always 'NowPilot').
  </behavior>
  <action>
    Rewrite src/components/pages/ChatPage.tsx (keep ErrorBoundary wrapper; Card/Empty/ChatPageSkeleton replaced):
    - Compose: Bubble.List (role='log', aria-live='polite', overflow-y auto, overflow-anchor none) mapping the in-memory message list from the hook (user + assistant entries); assistant Bubble: placement='start', variant='filled', style colorBgContainer + 1px colorBorder, radius 12, name 'NowPilot' (never the persona name — UI-SPEC persona-copy rule); user Bubble: placement='end', variant='filled', colorPrimaryBg light / colorPrimary @18% dark.
    - Streaming: the assistant bubble under stream has `streaming` prop true (X Bubble caret, colorPrimary @60% — NEVER a spinner) while the hook state is 'streaming'; text grows via the hook's ChunkBuffer-flushed state (no typewriter, §12.6).
    - Failed: keep partial text; below it an inline 'Provider error.' line in colorError + a Retry text action (colorPrimary link, STR.chat.retry) that calls hook.send(lastUserInput) (new operationId — Retry re-sends the last input, partial text replaced per UI-SPEC). The §C.2 code + message go to debugLog (already done in the runtime) — NEVER rendered raw.
    - Offline: when the hook's failure code is NETWORK-class, render the muted 12px notice STR.chat.offline above the Sender.
    - Empty: when zero bubbles, the centered one-liner STR.chat.empty (colorTextTertiary).
    - Sender: controlled value/onChange, placeholder STR.chat.askPlaceholder, onSubmit → hook.send(value); send button = circular SendOutlined bottom-right (STR.chat.send aria-label + tooltip 'Send'), colorPrimary when value non-empty AND idle, disabled otherwise (incl. streaming); focus ring colorPrimary 2px; no stop/abort control this phase (UI-SPEC fenced).
    Modify src/components/sidepanel/SidePanelShell.tsx: when hasProvider, the disabled Input footer is REMOVED (ChatPage owns the Sender — no double composer, UI-SPEC); when !hasProvider, keep the Alert STR.chat.noProvider + the disabled Input placeholder (unchanged gate).
    Create tests/components/pages/ChatPage.test.tsx per the behavior block (renderHook-style harness: mock the hook module OR drive a real hook with stubbed runAgentTurn; AntD X components render in jsdom-align per Phase-1 component-test conventions).
  </action>
  <verify>
    <automated>npx vitest run tests/components/pages/ChatPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass (empty/streaming/failed/retry/offline/gate states + aria roles + fencing).
    - grep -c "dangerouslySetInnerHTML\|innerHTML" src/components/pages/ChatPage.tsx == 0 (Phase-1 no-innerHTML rule).
    - grep -c "Welcome\|Prompts\|clarification\|chips\|stage" src/components/pages/ChatPage.tsx == 0 (D-03 fencing — no RICH elements).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Minimal streaming chat surface per UI-SPEC with honest states, Retry, offline notice, and full RICH fencing.</done>
</task>

</tasks>

## UI Considerations Lift (UI-SPEC §UI Considerations — 16 covered rows → truths, 7 dismissed, 0 backstop, 0 unresolved)

The 16 `✅ covered` rows are lifted verbatim into this plan's `must_haves.truths` (empty/loading/error/populated/partial/overflow/zero-one-many on E1; empty/loading/error/long-text on E2; error/partial/overflow on E3; overflow/long-text on E4). The 7 `⚪ dismissed` rows (E2 partial; E3 empty/loading/populated/zero-one-many; E5 overflow/long-text) are structurally inapplicable per UI-SPEC — no build, no backstop needed. No backstop/unresolved rows exist.

## Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-06 [unclassified — manual review] and AI-03 [empty + encoding] — recorded in `must_haves.flagged_assumptions`. All other edges stay with their owners (never silently dropped).

## Artifacts This Phase Produces

- src/core/i18n/strings.ts (modified): `STR.chat.send`, `STR.chat.retry` (verbatim Copywriting Contract).
- src/components/pages/useStreamingLLM.ts (new, +1 documented — D-01 co-located hook, Phase-7 promotion target): `ChatStreamState`, `useStreamingLLM()`.
- src/components/pages/ChatPage.tsx (rewritten): Bubble/Bubble.List + Sender minimal streaming surface + stream state machine.
- src/components/sidepanel/SidePanelShell.tsx (modified): disabled Input footer removed when a provider is active (single composer).
- tests/components/pages/useStreamingLLM.test.tsx, tests/components/pages/ChatPage.test.tsx (new).

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook → runAgentTurn | The UI path boundary: prompt assembly must NOT happen here (Golden Rule 3) |
| Provider stream → UI text | Streamed model output renders as plain text in Bubble (XSS surface minimal this phase; DOMPurify pipeline is Phase 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08-01 | (entropy) | ChatPage streaming honesty | high | mitigate | The failed state retains partial text + visible 'Provider error.' + Retry — never renders a truncated stream as complete (Pitfall 5 → UI-SPEC failed row); the §C.2 code goes to debugLog, never raw into the UI (R-10) |
| T-03-08-02 | XSS | streamed text in Bubble | medium | mitigate | Phase-3 renders PLAIN TEXT only (no markdown/HTML until Phase 7's DOMPurify/PortableMarkdown pipeline); no dangerouslySetInnerHTML/innerHTML (grep-asserted); renderer prompt forbids inventing content (RESEARCH Security Domain) |
| T-03-08-03 | Information Disclosure | error surfaces | low | accept | Failed-bubble copy is the canonical STR.chat.errorRetry string (verbatim, no [Switch Provider] token); no raw codes/messages — debugLog redacts (R-10) |
| T-03-08-04 | Tampering | RICH fencing | medium | mitigate | D-03 grep asserts no welcome/prompts/chips/stage elements ship; persona name never renders (identity is 'NowPilot') |
| T-03-08-SC | Tampering | npm installs | high | mitigate | No new packages — @ant-design/x ^2.9.0 + @ant-design/icons ^6.3.2 already installed (UI-SPEC verified) |
</threat_model>

<verification>
- `npx vitest run tests/components/pages/useStreamingLLM.test.tsx tests/components/pages/ChatPage.test.tsx` green.
- `npx vitest run tests/components/sidepanel` green (SidePanelShell change — composer reconciliation).
- `npx tsc --noEmit` green.
- Fencing greps (no welcome/prompts/chips/stage; no innerHTML; no PROMPTS import in the hook).
</verification>

<success_criteria>
- Send → stream → complete / fail-honestly works end-to-end on the minimal surface with one composer per surface.
- All UI-SPEC covered state rows rendered; RICH elements absent.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-08-SUMMARY.md` when done
</output>
