---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 13
type: execute
wave: 7
depends_on: ["03-02"]
files_modified:
  - src/core/ai/ProviderRegistry.ts
  - tests/core/ai/ProviderRegistry.test.ts
autonomous: true
gap_closure: true
requirements: [AI-01]
must_haves:
  truths:
    - "The D-07/D-21 provider gate is 'any usable provider configured'"
  artifacts:
    - "src/core/ai/ProviderRegistry.ts — hasActiveProvider() iterates ALL provider entries and returns true if ANY is enabled && !keyUnreadable (was: last-registered-only via activeProviderId)"
    - "tests/core/ai/ProviderRegistry.test.ts — multi-provider gate cases: earlier usable provider keeps the gate open when the LAST registered is disabled/unreadable; all-disabled → false; legacy registerActiveProvider without an entry → false"
  key_links:
    - "hasActiveProvider() ↔ registerProvider (L118 activeProviderId = last registration wins) ↔ markProviderKeyUnreadable (D-21 enabled:false) — the gate must survive a disabled LAST provider in the fixed openai→anthropic→gemini→ollama registration order"
  flagged_assumptions:
    - "AI-01 [probe unclassified]: the four-ID enum stays primary; custom baseURL remains a config variant of 'openai' (D-12) — the gate fix does not pluralize the identity model (assumption-delta no-change recorded in 03-10)"
    - "Legacy registerActiveProvider (Phase-1 primitive): it sets only activeProviderId with NO registry entry — under any-usable semantics it no longer opens the gate; the D-07 gate is entry-based (documented behavior change, asserted in tests)"
---
<objective>
Close VERIFICATION.md gap 4 (WR-01, partial): `hasActiveProvider()` checks ONLY the last-registered provider (`activeProviderId`, last registration wins) instead of any usable provider as its docstring claims. In the fixed `openai → anthropic → gemini → ollama` registration order (main.tsx), a stored envelope with `enabled: false` (or `keyUnreadable`) for the LAST provider closes the entire chat surface even when earlier providers are usable.

Purpose: implement the 03-REVIEW.md WR-01 fix — iterate all provider entries, return true if any `entry.enabled && !entry.keyUnreadable`. Preserve `getActiveProvider()` (last-registered accessor) unchanged; do not change registerProvider, markProviderKeyUnreadable, or any other contract.

Output: any-usable gate semantics + regression tests including the legacy registerActiveProvider behavior change.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-VERIFICATION.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-REVIEW.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-02-SUMMARY.md
@src/core/ai/ProviderRegistry.ts
@tests/core/ai/ProviderRegistry.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 — hasActiveProvider() returns true when ANY usable provider is configured</name>
  <files>src/core/ai/ProviderRegistry.ts</files>
  <read_first>
    - src/core/ai/ProviderRegistry.ts L178-193 (hasActiveProvider L184-189 — the DEFECTIVE last-registered-only gate; getActiveProvider L191-193 must stay untouched)
    - 03-REVIEW.md WR-01 (L97-112) — the fix sketch verbatim
    - 03-PATTERNS.md WR-01 (L195-219) — the fix sketch + the "Preserve getActiveProvider() unchanged" note
    - src/core/ai/ProviderRegistry.ts L118 (activeProviderId = last registration wins — the root cause)
  </read_first>
  <action>
    Replace `hasActiveProvider()` (ProviderRegistry.ts L184-189) with the 03-REVIEW.md WR-01 sketch (exact target state, prose form — the full implementation is in 03-REVIEW.md L104-111 and 03-PATTERNS.md L211-217):
    1. The gate is now "any usable provider exists" — iterate `this.providers.values()`; return true on the FIRST entry that is `enabled` AND NOT `keyUnreadable`; return false when the map is empty or every entry is disabled/unreadable.
    2. Update the JSDoc comment above it (L178-183) to state the any-usable semantics: the fixed openai→anthropic→gemini→ollama registration order can no longer close the gate when an EARLIER provider is usable; a legacy `registerActiveProvider(id)` with no registry entry no longer opens the gate (entry-based gate).
    3. Do NOT touch: `getActiveProvider()` (L191-193 — stays the last-registered accessor), `registerProvider` (L95-124), `markProviderKeyUnreadable` (L136-166), `getProviderInfos`/`getProviderInfo`, `clear`, `subscribe`, or the singleton.
    4. Do NOT change the D-21 semantics: a provider marked keyUnreadable is disabled and treated as unconfigured (T-03-02-03 still holds — a single unreadable provider never opens the gate).
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/ai/ProviderRegistry.test.ts --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/ai/ProviderRegistry.ts` L184-189 iterates `this.providers.values()` and returns true only for `entry.enabled && !entry.keyUnreadable`
    - `getActiveProvider()` unchanged (still returns `this.activeProviderId`)
    - `pnpm vitest run tests/core/ai/ProviderRegistry.test.ts --passWithNoTests` exits 0 after Task 2's test updates
  </acceptance_criteria>
  <done>The gate reflects 'any usable provider' — a disabled/unreadable last-registered provider can no longer close the chat surface.</done>
</task>

<task type="auto">
  <name>Task 2: WR-01 — regression tests (multi-provider gate + legacy behavior change)</name>
  <files>tests/core/ai/ProviderRegistry.test.ts</files>
  <read_first>
    - tests/core/ai/ProviderRegistry.test.ts L18-30 (freshConfig helper), L37-86 (D-07 gate suite — note the EXISTING test at L79-84 'keeps the Phase-1 registerActiveProvider primitive working' asserts `hasActiveProvider() === true` after `registerActiveProvider('openai')` — this asserts the PRE-FIX converse bug and MUST be updated)
    - 03-PATTERNS.md "WR-01 — extend tests" (L340-341)
    - tests/core/ai/ProviderRegistry.test.ts L95-139 (D-21 unreadable suite — all existing assertions must keep passing)
  </read_first>
  <action>
    Update `tests/core/ai/ProviderRegistry.test.ts`:
    1. UPDATE the existing backward-compat test (L79-84): after the fix, `registerActiveProvider('openai')` alone (no registry entry) does NOT open the gate. Assert: `hasActiveProvider() === false` while `getActiveProvider() === 'openai'`. Rename it to reflect the new semantics (e.g. "legacy registerActiveProvider sets the active id but does NOT open the entry-based D-07 gate").
    2. Add a new test 'an EARLIER usable provider keeps the gate open when the LAST registered is disabled': `registerProvider(freshConfig({ id: 'openai', enabled: true }))` then `registerProvider(freshConfig({ id: 'ollama', enabled: false }))` → `hasActiveProvider() === true` (openai usable; the last-registered ollama is disabled).
    3. Add 'a keyUnreadable LAST provider does not close the gate when an earlier one is usable': register openai (enabled), register anthropic, then `markProviderKeyUnreadable('anthropic')` → `hasActiveProvider() === true`.
    4. Add 'all providers disabled/unreadable → false': register openai + anthropic, mark BOTH keyUnreadable (or register with enabled:false) → `hasActiveProvider() === false`.
    5. Add 'any-usable survives the D-21 transition on a NON-last provider': register openai, register anthropic, mark openai unreadable → gate still true via anthropic.
    6. Keep every other existing assertion untouched (empty gate false, unknown-id rejected, apiKey strip, D-21 single-transition, re-registration reset).
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/ai/ProviderRegistry.test.ts --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - The L79-84 test now asserts `hasActiveProvider() === false` for a bare `registerActiveProvider('openai')` with `getActiveProvider() === 'openai'`
    - 4 new any-usable gate cases (disabled-last, unreadable-last, all-disabled, non-last-unreadable) assert the boolean outcomes above
    - `pnpm vitest run tests/core/ai/ProviderRegistry.test.ts --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>The any-usable gate is pinned by regression tests covering multi-provider and legacy paths; no existing D-21 assertions regress.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Details |
|----------|------|---------|
| `ProviderRegistry.hasActiveProvider()` | modified logic | iterates all entries; true iff any `enabled && !keyUnreadable` |
| `tests/core/ai/ProviderRegistry.test.ts` | updated | backward-compat test re-asserted (legacy primitive no longer opens gate) + 4 new any-usable cases |

Note: no shell/entrypoint test changes are required — the executed shell-gate suites (SidePanelShell/StandaloneShell/entrypoints) register via `registerProvider`/`registerActiveProvider` with real entries in their fixtures; if the full gate run surfaces a fixture using a bare `registerActiveProvider` with no entry expecting the gate open, convert that fixture to `registerProvider(freshConfig(...))` (source-of-truth helper in ProviderRegistry.test.ts L18-30). That is a fixture-consistency fix, not a scope change.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault decrypt / wiring layer → ProviderRegistry | provider envelopes cross in; D-21 marks entries unreadable |
| ProviderRegistry → shells (SidePanelShell/StandaloneShell) | hasActiveProvider() decides whether the chat surface is reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13-01 | DoS | hasActiveProvider() gate | high | mitigate | any-usable iteration means a single poisoned (disabled/unreadable) LAST registration can no longer lock users out of a usable provider; tests cover disabled-last, unreadable-last, all-disabled, non-last-unreadable |
| T-03-13-02 | Spoofing | legacy registerActiveProvider path | medium | mitigate | entry-based gate: a bare activeProviderId without a registry entry returns false (the REVIEW's converse bug is fixed); the updated backward-compat test pins it. Production wiring (runAIRuntimeInit) uses registerProvider/markProviderKeyUnreadable only — grep-verified zero production callers of registerActiveProvider |
| T-03-13-03 | Info Disclosure | getProviderInfos/registry snapshot | low | accept | unchanged from the executed plan; apiKey is stripped at registration (R-10) — the gate fix reads only enabled/keyUnreadable flags |

The gate fix touches no secrets and no network surface. T-03-13-01 (a locked-out chat surface) is the high item, mitigated by the iteration fix + 4 regression cases.
</threat_model>

<verification>
- `pnpm vitest run tests/core/ai/ProviderRegistry.test.ts --passWithNoTests`
- `pnpm vitest run tests/components/sidepanel tests/components/standalone tests/entrypoints --passWithNoTests` (shell-gate suites must stay green)
- `pnpm run verify:phase-3` (full gate) must stay green
</verification>

<success_criteria>
- hasActiveProvider() = any usable provider (enabled && !keyUnreadable)
- Multi-provider configurations with a disabled/unreadable last provider keep the chat surface usable
- Legacy registerActiveProvider without an entry no longer opens the gate
- Full `verify:phase-3` gate green
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-13-SUMMARY.md` when done
</output>
