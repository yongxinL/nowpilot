---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - src/core/ai/ProviderRegistry.ts
  - src/core/ai/TierResolver.ts
  - src/core/ai/ProviderRouter.ts
  - tests/core/ai/ProviderRouter.test.ts
  - entrypoints/sidepanel/main.tsx
  - entrypoints/standalone/main.tsx
autonomous: true
requirements: [RICH-R-09]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ProviderRegistry hydrates the Phase-2 np_providers object shape (providers Record + openAiKey/geminiKey) and exposes normalized sync reads getEnabled()/getById(id)/getAll() — the disk shape is untouched (D-49), 'claude' maps to 'anthropic' only in memory"
    - "np_endpoint_overrides (chrome.storage.local) merge over the §10.6 ENDPOINTS defaults at load (D-50) — localhost:12380 is never a canonical default"
    - "TierResolver resolves fast/balanced from UserPreferences.fastModel/balancedModel + live-discovered models (D-52/D-53/D-54) and returns null for an unresolved tier (D-54a) — it never invents a model name"
    - "ProviderRouter implements the §20.10 locked table: TIMEOUT/PROVIDER_5XX/NETWORK retryable pre-first-token + CB vote 1; RATE_LIMITED retryable w/ jitter + vote 0; AUTH non-retryable + vote 3 (open immediately); 3 votes within 60 s → provider open for 5 min; never switches after hasStreamedFirstToken === true"
    - "Provider fallback works: one provider down → the next enabled provider is tried (DONE-when 2)"
  artifacts:
    - path: "src/core/ai/ProviderRegistry.ts"
      provides: "D-49 normalize (np_providers object → normalized in-memory), D-50 endpoint-override merge, D-51 declarative registration of the 5 providers + sync reads, D-52 live-model session cache"
      exports: ["ProviderRegistry", "hydrate", "getEnabled", "getById", "getAll", "registerProvider", "getEndpointFor", "getCachedModels"]
    - path: "src/core/ai/TierResolver.ts"
      provides: "Appendix D mechanism verbatim (priority sort + privacyMode) with D-53 capability-tiers-only candidates and D-54/D-54a null contract"
      exports: ["TierResolver", "resolveTier", "TIER_TO_MODEL_CANDIDATES"]
    - path: "src/core/ai/ProviderRouter.ts"
      provides: "§1.5/§20.10: RouterAttemptState, retry/fallback + circuit breaker (3 votes/60s → open 5min), hasStreamedFirstToken never-switch, allowCloudFallbackFromLocal rule"
      exports: ["ProviderRouter", "route", "ProviderAttempt", "RouterError", "CIRCUIT_BREAKER_VOTES", "CIRCUIT_BREAKER_WINDOW_MS", "CIRCUIT_OPEN_MS"]
  key_links:
    - from: "src/core/ai/ProviderRouter.ts"
      to: "src/core/ai/ProviderRegistry.ts"
      via: "route() picks the next enabled provider from the registry's normalized list"
      pattern: "getEnabled|ProviderRegistry"
    - from: "src/core/ai/TierResolver.ts"
      to: "src/core/ai/UserPreferences.ts"
      via: "resolveTier reads fastModel/balancedModel from np_preferences"
      pattern: "fastModel|balancedModel"
    - from: "src/core/ai/ProviderRegistry.ts"
      to: "src/core/ai/providers/"
      via: "the five provider modules import and register themselves declaratively at module load (D-51)"
      pattern: "registerProvider"
  prohibitions:
    - "No model-slug guessing: when TierResolver returns null, ProviderRouter MUST NOT infer/auto-assign/substitute a model (D-54a — the caller surfaces a configuration-required state and starts no provider request)"
    - "No invented error codes beyond §21.6 (D-38) — router errors map onto the closed set only"
    - "No hard-coded vendor model slugs anywhere in the routing stack (D-53 — capability tiers only)"
flagged_assumptions:
  - "RICH-R-09 (unclassified probe): chat and agent share the same persona BECAUSE they share the same routed pipeline — this plan's router is tier-agnostic and persona-agnostic by design; persona consistency is asserted end-to-end in the AgentOrchestrator test (03-06)"
  - "A1 (UserPreferences location): fastModel/balancedModel live in src/core/ai/UserPreferences.ts (plan 03-02) — TierResolver imports from there"
---

<objective>
Build the provider routing stack: `ProviderRegistry` (D-49 normalize-in-memory, D-50 endpoint overrides, D-51 sync reads + declarative registration, D-52 live-model session cache), `TierResolver` (Appendix D mechanism with D-53 capability-tiers-only and the D-54/D-54a null contract), and `ProviderRouter` (§1.5/§20.10 retry/fallback + circuit breaker). Wire registry hydration into both surface boots.

Purpose: This is where tier-resolved routing (fast / balanced) becomes real: a per-stage tier → a concrete (providerId, model) → a provider call, with fallback when one provider is down and a circuit breaker that stops hammering a failing provider.
Output: 3 routing modules + boot hydration wiring + ProviderRouter.test.ts (fallback + circuit breaker).
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §1.5 (retry/fallback rules 367-389), §10.6 (ENDPOINTS + np_endpoint_overrides), §20.10 (locked table 3215-3229), Appendix D (TierResolver reference)
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md  # Pattern 4 (locked table), Pitfalls 2/6, D-49..D-56, assumptions A1
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md  # ProviderRegistry/TierResolver/ProviderRouter sections
@src/types/index.ts  # ProviderConfig object shape (94-137) — the disk shape D-49 keeps
@src/store/useExtensionStore.ts  # np_providers read path (774-775) — the encrypted blob the registry decrypts via Phase-2 EncryptedStorage
@src/services/aiProvider.ts  # fetchModelsOrError/fetchProviderModels semantics (38-149) reused for D-52 live discovery
@entrypoints/sidepanel/main.tsx  # boot (line 65: useWorkspaceStore.persist.rehydrate())
@entrypoints/standalone/main.tsx  # boot (line 65)
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProviderRegistry — D-49 normalize, D-50 endpoint overrides, D-51 sync reads, D-52 live-model cache + boot hydration</name>
  <files>src/core/ai/ProviderRegistry.ts, entrypoints/sidepanel/main.tsx, entrypoints/standalone/main.tsx</files>
  <read_first>@src/types/index.ts lines 94-137 (ProviderConfig/CustomProviderId/CustomProviderDetail — the disk shape D-49 keeps) · @src/store/useExtensionStore.ts lines 774-775 (np_providers read path) · @src/services/aiProvider.ts lines 38-149 (fetchModelsOrError/fetchProviderModels semantics reused for D-52) · @entrypoints/sidepanel/main.tsx + @entrypoints/standalone/main.tsx (boot — line 65 both, files being modified) · 03-RESEARCH.md D-49/D-50/D-51/D-52 + §10.6 ENDPOINTS table</read_first>
  <action>Create `src/core/ai/ProviderRegistry.ts` per D-49/D-50/D-51/D-52. Hydration (`hydrate()`): read the encrypted np_providers blob via the Phase-2 EncryptedStorage path (the same read useExtensionStore.ts:774-775 performs — no new crypto, V6), decode the Phase-2 object shape `{ providers: Record<CustomProviderId, CustomProviderDetail>, openAiKey, geminiKey }` (src/types/index.ts:94-137) into a normalized in-memory registry; map disk 'claude' → runtime 'anthropic' at this boundary ONLY (disk shape untouched — D-49); read np_endpoint_overrides (chrome.storage.local, D-50) and merge over the §10.6 ENDPOINTS defaults (openai https://api.openai.com/v1, anthropic https://api.anthropic.com, gemini https://generativelanguage.googleapis.com, ollama http://localhost:11434/v1 — localhost:12380 is NEVER a canonical default, D-12); expose `getEndpointFor(providerId)` returning the merged endpoint. Registration (D-51): `registerProvider(provider: ILLMProvider)` — the five provider modules (plan 03-03) call this at module load; keep the registration + hydration separate so tests can register fixtures. Sync read surface (D-51): `getEnabled()`, `getById(id)`, `getAll()` — no async in the read surface; hydration must complete before reads (call `await ProviderRegistry.hydrate()` at boot). Live-model cache (D-52): keep the `fetchProviderModels` semantics from src/services/aiProvider.ts (reuse `fetchModelsOrError`) for Options refresh + connection test, caching the fetched list per provider in memory for the session (`getCachedModels(providerId)`). Wire boot hydration: in `entrypoints/sidepanel/main.tsx` and `entrypoints/standalone/main.tsx`, alongside the existing `useWorkspaceStore.persist.rehydrate()` call (line 65 both), add `await ProviderRegistry.hydrate()` BEFORE UI renders (D-51 "hydration once at boot"). Use debugLog for instrumentation. Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>tsc passes; hydrate() maps disk 'claude' to runtime 'anthropic' with the disk np_providers object unchanged (assertable: registry reads don't write storage); getEndpointFor returns §10.6 defaults + np_endpoint_overrides merged; both boots call `await ProviderRegistry.hydrate()` before UI render; getEnabled/getById/getAll are synchronous.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>ProviderRegistry compiles; hydrate() normalizes the Phase-2 shape with 'claude'→'anthropic' in memory only; endpoint overrides merge over §10.6 defaults; both boots call hydrate() before UI renders.</done>
</task>

<task type="auto">
  <name>Task 2: TierResolver — Appendix D mechanism + capability tiers + null contract</name>
  <files>src/core/ai/TierResolver.ts</files>
  <read_first>@.planning/PRODUCT_SPEC_v0_1.md Appendix D (TierResolver reference implementation — mechanism verbatim, D-53 overrides the slug approach) · 03-RESEARCH.md D-53/D-54/D-54a + Pitfall 6 (null contract) · @src/core/ai/UserPreferences.ts (created 03-02 — fastModel/balancedModel read target)</read_first>
  <action>Create `src/core/ai/TierResolver.ts` per Appendix D mechanism verbatim with the D-53/D-54/D-54a overrides. Export `TIER_TO_MODEL_CANDIDATES` as a capability-tier table ONLY: keys 'fast' | 'balanced', values are capability descriptors (operator-selected low-cost vs higher-capability), NOT vendor model slugs (D-53 — no stale names, no vendor churn; OpenAICompat has NO default entry, D-56). `resolveTier(tier: ModelTier, opts): { providerId, model } | null` — resolves the concrete (providerId, model) from `UserPreferences.fastModel`/`balancedModel` (np_preferences via plan 03-02's store) validated against the live-discovered model cache (D-52); "never invents a model name" (Appendix D rule); the priority-sort + privacyMode handling from Appendix D remain verbatim. NULL CONTRACT (D-54a): until BOTH fastModel and balancedModel are explicitly persisted, resolveTier returns null for the unresolved tier — no inference, no auto-assignment, no substitution, no guessing; the caller (AgentOrchestrator, 03-06) surfaces a configuration-required state and starts NO provider request. Suggestions shown during provider setup are UI-only and MUST NOT affect runtime routing until confirmed and persisted (D-54). Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>tsc passes; TIER_TO_MODEL_CANDIDATES contains exactly the keys 'fast' and 'balanced' with NO vendor model slugs (assertable via grep for known slug patterns — comment-filtered); resolveTier returns null when either fastModel or balancedModel is absent (D-54a); resolveTier returns a (providerId, model) pair only from persisted prefs validated against cached models.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>TierResolver compiles; resolveTier returns null until both preferences persist; TIER_TO_MODEL_CANDIDATES contains zero vendor slugs.</done>
</task>

<task type="auto">
  <name>Task 3: ProviderRouter — §1.5/§20.10 retry/fallback + circuit breaker + fallback tests</name>
  <files>src/core/ai/ProviderRouter.ts, tests/core/ai/ProviderRouter.test.ts</files>
  <read_first>@.planning/PRODUCT_SPEC_v0_1.md §1.5 (367-389) + §20.10 (3215-3229 — the LOCKED retryability table) · 03-RESEARCH.md Pattern 4 (locked table + rules) · @src/core/ai/ProviderRegistry.ts (created task 1 — getEnabled) · @src/core/ai/types.ts (ProviderId, RouterAttemptState) · tests/core/ai/fixtures/FixtureProvider.ts (created 03-01 — fixture providers for the tests)</read_first>
  <action>Create `src/core/ai/ProviderRouter.ts` per §1.5/§20.10. `RouterAttemptState { operationId, attempts, hasStreamedFirstToken, circuitBreakerOpen: Record<ProviderId, number> }` (spec 377-383) — correlation reuses the Phase-1 OperationId (Flag C). `route(input)` where input carries { tier, systemPrompt, providerCandidates (from ProviderRegistry.getEnabled), abortSignal, allowCloudFallbackFromLocal } — implement the LOCKED §20.10 table verbatim (spec 3215-3229): TIMEOUT retryable pre-first-token YES / CB vote 1; PROVIDER_5XX retryable YES / CB vote 1; NETWORK retryable YES / CB vote 1; RATE_LIMITED retryable YES (with jitter) / CB vote 0; AUTH retryable NO / CB vote 3 (open immediately); MODEL_UNKNOWN retryable NO / CB vote 0; SCHEMA_INVALID retryable NO / CB vote 0; HOST_NOT_PERMITTED retryable NO / CB vote 0. Circuit breaker: 3 votes within 60 s (export CIRCUIT_BREAKER_VOTES = 3, CIRCUIT_BREAKER_WINDOW_MS = 60_000) → provider open for 5 min (CIRCUIT_OPEN_MS = 300_000), skipped while open. §1.5 rules: one provider → retry once only for retryable pre-first-token failures; NEVER switch after `hasStreamedFirstToken === true`; never silently switch local→cloud when allowCloudFallbackFromLocal=false. Record every attempt via debugLog only (AITransactionLog is Phase 11). Write `tests/core/ai/ProviderRouter.test.ts` (DONE-when 2): (a) one provider down (fixture throwing NETWORK) → next enabled provider is tried and succeeds; (b) AUTH failure opens the breaker immediately (vote 3); (c) 3 NETWORK votes within 60 s → provider open, subsequent route skips it; (d) no switch after first token (fixture streaming a token then failing → the stream is NOT re-routed); (e) allowCloudFallbackFromLocal=false blocks local→cloud switch. Fixture providers implement ILLMProvider from plan 03-01. Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>ProviderRouter.test.ts green (5 case groups: fallback to next enabled provider, AUTH opens breaker immediately, 3-vote open + skip, no post-first-token switch, no silent local→cloud); the locked §20.10 table implemented verbatim (each code's retryable + CB vote asserted in test (c) coverage); constants exported (CIRCUIT_BREAKER_VOTES=3, CIRCUIT_BREAKER_WINDOW_MS=60000, CIRCUIT_OPEN_MS=300000).</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>ProviderRouter.test.ts green (5 case groups); the locked §20.10 table is implemented verbatim; circuit breaker opens on 3 votes and after AUTH.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| routing decision → provider call | A wrong route (guessed model, switched mid-stream, fallen-back silently) is the failure the router exists to prevent |
| np_endpoint_overrides → endpoint | Operator-provided endpoint strings reach the fetch layer — zod-validated at read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-14 | DoS | circuit breaker bypass | medium | mitigate | 3 votes within 60 s → open 5 min is enforced in route() (CIRCUIT_BREAKER_* constants); AUTH = 3 votes opens immediately — a failing provider cannot be hammered |
| T-3-15 | Spoofing | TierResolver null → guessed model | high | mitigate | D-54a null contract enforced: resolveTier returns null, ProviderRouter raises configuration-required, NO provider request starts — no inference/substitution path exists in code (covered by the plan's prohibition + AgentOrchestrator test 03-06) |
| T-3-16 | Tampering | endpoint override strings | low | mitigate | np_endpoint_overrides values zod-validated at hydrate (URL scheme http/https only); merged over §10.6 defaults — an operator cannot inject a non-URL value into the fetch layer |
| T-3-17 | Information Disclosure | attempt logging | low | mitigate | debugLog records codes + providerId only, never request bodies or keys (TraceRedactor discipline; AITransactionLog is Phase 11) |
</threat_model>

<verification>
- `pnpm run verify:phase-3` green after every task commit.
- ProviderRouter.test.ts is the DONE-when 2 gate (one provider down → next enabled provider).
- Grep guard: no vendor model slugs in the routing stack (comment-filtered negative gate on `TIER_TO_MODEL_CANDIDATES` region).
</verification>

<success_criteria>
1. Provider fallback + circuit breaker tests pass (DONE-when 2).
2. TierResolver returns null until both preferences persist — no silent wrong-model request (D-54a, Pitfall 6).
3. Registry normalizes the Phase-2 disk shape with zero migration risk (D-49).
4. Both surfaces hydrate the registry at boot before UI renders.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-3 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/ai/ProviderRegistry.ts | hydrate, getEnabled/getById/getAll, registerProvider, getEndpointFor, getCachedModels | **[this plan]** |
| src/core/ai/TierResolver.ts | resolveTier, TIER_TO_MODEL_CANDIDATES (capability tiers) | **[this plan]** |
| src/core/ai/ProviderRouter.ts | route, RouterAttemptState, ProviderAttempt, circuit-breaker constants | **[this plan]** |
| entrypoints/sidepanel/main.tsx, entrypoints/standalone/main.tsx | + ProviderRegistry.hydrate() at boot | **[this plan]** |
| src/core/ai/types.ts / ILLMProvider.ts / StreamAdapter.ts / StructuredOutput.ts / PlannerService.ts / prompts/index.ts | spine | 03-01 |
| src/core/ai/persona/*, UserPreferences.ts | persona foundation (TierResolver consumes prefs) | 03-02 |
| src/core/ai/providers/*.ts | 5 providers (registered here) | 03-03 |
| src/core/ai/PromptCacheManager.ts / PromptCacheAdapter.ts / ChunkBuffer.ts / toolSchemas.ts / ExecutorService.ts / RendererService.ts | prompt assembly + stages | 03-04 |
| src/core/runtime/workerState.ts | + ActiveStreamState | 03-04 |
| src/core/ai/AgentOrchestrator.ts | runAgentTurn (consumes router/tier/registry) | 03-06 |
| src/types/storage.ts / useChatStreaming.ts / OptionsPage.tsx | persistence + chat + options | 03-07 |
| tests/core/ai/** | test files + fixtures/ | all plans |