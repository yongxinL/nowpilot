---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
- src/core/ai/ProviderRouter.ts
- tests/core/ai/ProviderRouter.test.ts
autonomous: true
requirements: [AI-01, AI-04]

<!-- FINAL v2 (2026-08-10): + ordered Tasks section. P-2/F-4 — callProviderJsonMode closure is
     (sections: PromptSection[], jsonSchema, signal); the Router owns the cached→system / task→prompt
     mapping (joinSections). P-4/F-5 — the Router-constructed calls use messages[]+providerOptions.anthropic.cacheControl
     (from applyCacheHints), never system:string. P-3 — PromptSection from '@/core/ai/types'. -->

### must_haves:
truths:
- "ProviderRouter is the FIRST retry layer (D-17): retryable pre-first-token codes (TIMEOUT, PROVIDER_5XX, NETWORK, RATE_LIMITED) get exactly ONE router retry per provider step; non-retryable (PROVIDER_AUTH, PROVIDER_MODEL_UNKNOWN, SCHEMA_INVALID, HOST_NOT_PERMITTED) never retry; attempts never exceed the non-multiplying budget (R-2) — the three non-multiplying retry bounds land here (D-15, §1.6.1). Canonical C.2 code names are used (PROVIDER_AUTH / PROVIDER_MODEL_UNKNOWN per Appendix C.2 + errorCodes.ts Phase-3 block from 03-01) — the spec §1.6.1 prose shorthand 'AUTH'/'MODEL_UNKNOWN' maps to these canonical codes; never invent a new code name (Golden Rule 9)."
- "Circuit breaker: 3 consecutive failures for a provider within 60s open it for 5 minutes (in-memory per-surface RouterAttemptState, D-14); cheapest-capable routing + fallback + circuit breaker are this plan's D-15 cost governors."
- "The D-13 privacy gate is enforced HERE during fallback-chain traversal (never a resolveTier filter): under privacyMode 'prefer-local' (default), a failing local provider NEVER hops to a cloud provider — the turn terminates in a visible provider-failure state and the hop refusal is debugLogged (redacted). 'local-only' stays reserved/unused this phase."
- "Every SDK call the Router constructs passes maxRetries: 0 (Pitfall 1 — ai@4.3.19 silently defaults to 2) and an explicit maxTokens (planner/repair 256, renderer 512)."
- "ProviderRouter constructs callProviderJsonMode (D-18, F-4 signature): the closure is `(sections: PromptSection[], jsonSchema: unknown, signal: AbortSignal) => Promise<string>`; it resolves per-provider jsonMode — Ollama → 'prompt' (model-dependent), openai/anthropic/gemini → 'native' — and maps cached kinds (system/tool_schemas/preferences/memory) → provider `system` and task kinds (context/task/user_input) → `prompt` via a pure joinSections helper (NO `prompt.split`). After a failover only the Router knows the new (providerId, model, jsonMode) triple, so only the Router hands out the matching closure."
- "F-5 cache-hint APPLICATION (P-4): every streamText/generateObject call the Router constructs is built in the `messages[]` form with a CoreSystemMessage carrying `providerOptions.anthropic.cacheControl` sourced from applyCacheHints (03-03) — NEVER `system: string` (ai@4 drops the breakpoint on the string form). Below a provider's cache minimum the marker is inert; emission + byte-stability are the asserted invariants, live hits are Phase-4+."
- "`PromptSection` is imported from `@/core/ai/types` (D-07 canonical home), never re-declared (R-1)."
- "The no-op budgetGuard hook (D-16) exists on the Router (Phase 6 wires the monthly ledger pre-flight without a rebuild)."
- "StageInvocation resolution: createStageInvocation() resolves the cheapest-capable candidate via resolveTier + getAISDKModel, builds the F-4 callProviderJsonMode closure (sections-in) AND the F-5 messages[]+providerOptions call shape, and returns the (providerId, model: LanguageModel, jsonMode, callProviderJsonMode) bundle — the seam the hook/orchestrator (03-06/03-08) consume."
artifacts:
- src/core/ai/ProviderRouter.ts
- tests/core/ai/ProviderRouter.test.ts
key_links:
- "createStageInvocation → getAISDKModel (03-02) + resolveTier (03-02); the returned F-4 callProviderJsonMode (sections-in) feeds StructuredOutput.requestJson via PlannerService (03-04)."
- "applyCacheHints (03-03) output populates providerOptions.anthropic.cacheControl on the Router-constructed calls (F-5 application owner = Router / createStageInvocation)."
- "RouterAttemptState is the per-surface cost/privacy ledger the eval fixtures assert (3 fails/60s → open 5min; ≤3 non-multiplying attempts; prefer-local blocks cloud)."
flagged_assumptions:
- "AI-04 [concurrency]: RouterAttemptState is in-memory per-surface (D-14) — it dies on panel close; no cross-surface race on the single-writer bus; persistence/sharing is a later-phase ADR."
- "AI-04 [idempotency]: router state is not persisted; a re-send after failure creates a fresh operationId + fresh attempt state (Retry semantics, UI-SPEC)."
- "F-4 [section mapping]: joinSections maps cached kinds → `system` and task kinds → `prompt`; the mapping lives ONLY in the Router closure so Planner/Renderer stay pure (D-18/D-19)."
- "D-14 confirmed: hasStreamedFirstToken freezes the provider mid-stream (§1.5) — never switch after the first token; enforced inside the renderer-stage invocation path."
prohibitions:
- "No SDK-internal retries (maxRetries: 0 on every constructed call — hidden 4th layer)."
- "No `system: string` on a constructed call when a cache hint applies (F-5 — use messages[]+providerOptions)."
- "No joined-string prompt or `prompt.split()` in the callProviderJsonMode closure (F-4 — sections in, joinSections maps)."
- "No silent local→cloud switch under 'prefer-local' (D-13)."
- "No resolveTier-level privacy filtering (D-13 — the gate lives here)."
- "No 'local-only' privacyMode usage this phase (reserved)."
- "No nested retry loops (R-2 — exactly one router retry per retryable step, 3 layers total across the app)."
- "No raw prompt/key bodies in debugLog (TraceRedactor, R-10)."

Purpose: The Router owns every cost multiplier (maxRetries: 0, three non-multiplying layers, explicit maxTokens) and the privacy boundary ("no silent local→cloud switch"). It is also the F-4 mapping owner (sections → system/prompt) and the F-5 cache-hint application owner (messages[]+providerOptions). This is the AI-SPEC eval suite's centerpiece (Cost discipline + Privacy boundary honesty dimensions, Critical/High).
Output: ProviderRouter.ts + its eval test file (fallback chains, breaker timing, privacy gate, maxRetries: 0 assertions, F-4 sections-in closure, F-5 messages[] cache shape, TraceRedactor adversarial fixtures).
<execution_context>@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md</execution_context>

### Tasks (ordered — do not reorder; each maps to a truth/artifact)
1. **ProviderRouter.ts scaffolding (new).** Types: JsonMode, RouterAttemptState, StageInvocation, CallProviderJsonMode = `(sections: PromptSection[], jsonSchema, signal) => Promise<string>` (F-4), CreateStageInvocationInput. Import PromptSection from '@/core/ai/types'. getProviderRouter() lazy singleton; isProviderUnconfiguredError() guard.
2. **joinSections helper (F-4).** CACHED_KINDS = [system, tool_schemas, preferences, memory] → `system`; TASK_KINDS = [context, task, user_input] → `prompt`. Pure; the ONLY section→string mapping site. NO prompt.split.
3. **callProviderJsonMode closure (D-18/F-4).** Resolve jsonMode per provider (ollama→'prompt', others→'native'); native = generateObject({schema: zodSchema, messages:[system+task], maxRetries:0}); prompt = generateText. Close over resolved (providerId, model).
4. **F-5 message builder (P-4).** All constructed streamText/generateObject calls use messages[] with a CoreSystemMessage carrying providerOptions.anthropic.cacheControl from applyCacheHints (03-03). NEVER system:string.
5. **Retry + breaker (D-17/D-14/D-15).** One router retry per retryable pre-first-token code; non-retryable never retry; breaker 3 fails/60s → open 5min; maxRetries:0 + explicit maxTokens on every call; hasStreamedFirstToken freezes the provider mid-stream.
6. **Privacy gate (D-13).** During fallback traversal under 'prefer-local': dead local → terminate in visible provider-failure, debugLog redacted refusal, NO cloud fetch. 'cloud-ok' → logged hop. No resolveTier filtering.
7. **budgetGuard hook (D-16).** Optional no-op hook Phase 6 wires; default pass-through.
8. **createStageInvocation().** resolveTier + getAISDKModel → build the F-4 closure + F-5 call shape → return {providerId, model, jsonMode, callProviderJsonMode}.
9. **ProviderRouter.test.ts (new).** 8 eval fixtures (healthy/privacy/breaker/maxRetries/auth/budgetGuard/secrets) + **F-4**: a multi-line persona block in a cached section maps whole to `system` (never split) + **F-5**: constructed anthropic call uses messages[] with providerOptions.anthropic.cacheControl (not system:string).
10. **Verify.** grep no `prompt.split(`, no `system:` string literal on constructed calls; PromptSection import = '@/core/ai/types'; test:ai green.

### Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-04 [idempotency + concurrency] — recorded in must_haves.flagged_assumptions (router state per-surface in-memory, D-14; fresh operationId per retry). All other edges stay with their owners (never silently dropped).

### Artifacts This Phase Produces
- src/core/ai/ProviderRouter.ts (new): JsonMode, RouterAttemptState, StageInvocation, CallProviderJsonMode (F-4 `(sections: PromptSection[], jsonSchema, signal)`), joinSections helper (cached→system / task→prompt), CreateStageInvocationInput, ProviderRouter class (createStageInvocation(), configure(), classifyProviderError(), budgetGuard hook, breaker state, applyCacheHints-sourced messages[] builder), getProviderRouter() lazy singleton, isProviderUnconfiguredError() guard. Imports PromptSection from '@/core/ai/types' (D-07).
- tests/core/ai/ProviderRouter.test.ts (new): 8 eval fixtures + F-4 sections-mapping assertion + F-5 messages[] cache-shape assertion.
<threat_model>

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router → provider endpoint | The privacy gate lives HERE: under 'prefer-local', a failing local provider must never transmit to a cloud endpoint (D-13) |
| Router → debugLog | Every refusal/retry/breaker event logs through TraceRedactor (R-10) — zero raw secrets/prompts |
| Sections → constructed call | The Router maps PromptSection kinds → system/prompt (F-4) and attaches cacheControl via messages[] (F-5); the cached [SYSTEM] must stay byte-stable |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | fallback traversal | critical | mitigate | D-13 gate in the Router (never resolveTier): prefer-local + dead local → visible provider-failure, hop refusal debugLogged redacted, NO cloud fetch (test 2); cloud-ok → legitimate logged hop (test 3) |
| T-03-05-02 | (cost) | retry multiplication | critical | mitigate | maxRetries: 0 on every constructed SDK call (Pitfall 1); one router retry per retryable step (D-17); breaker 3/60s → 5min (D-14); non-retryable never retry; attempts bounded (tests 4-6) |
| T-03-05-03 | Tampering | F-4 section mapping / F-5 cache stability | high | mitigate | joinSections maps whole cached sections → `system` (a multi-line persona block is never split — the old prompt.split bug); messages[] cacheControl carries the applyCacheHints hint so the byte-stable [SYSTEM] actually caches (Pitfall 5, §1.3); F-4/F-5 fixtures |
| T-03-05-04 | Information Disclosure | debugLog paths | high | mitigate | TraceRedactor boundary (R-10); adversarial-secrets fixture asserts the api key literal never appears in any captured log (test 8) |
| T-03-05-05 | Spoofing | custom baseURL endpoint | medium | accept | User-configured deliberate endpoint (D-12); direct surface→endpoint TLS; documented |
| T-03-05-SC | Tampering | pnpm installs | high | mitigate | No new packages this plan; approved set from 03-01 |
</threat_model>
<success_criteria>
- Privacy boundary honesty proven: prefer-local never cloud-hops; redaction proven on every router log path.
- Cost discipline proven: healthy turn 1 call per stage, maxRetries: 0, breaker + attempt budget enforced.
- F-4 proven: callProviderJsonMode takes sections; joinSections maps a multi-line cached section wholly to `system` (no split). F-5 proven: constructed anthropic call uses messages[]+providerOptions.anthropic.cacheControl, never system:string.
- StageInvocation + budgetGuard seams exist for 03-06/03-08 and Phase 6.
</success_criteria>
