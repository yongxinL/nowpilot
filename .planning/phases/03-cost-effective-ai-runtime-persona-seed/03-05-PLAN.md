---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/core/ai/ProviderRouter.ts
  - tests/core/ai/ProviderRouter.test.ts
autonomous: true
requirements: [AI-01, AI-04]

must_haves:
  truths:
    - "ProviderRouter is the FIRST retry layer (D-17): retryable pre-first-token codes (TIMEOUT, PROVIDER_5XX, NETWORK, RATE_LIMITED) get exactly ONE router retry per provider step; non-retryable (AUTH, MODEL_UNKNOWN, SCHEMA_INVALID, HOST_NOT_PERMITTED) never retry; attempts never exceed the non-multiplying budget (R-2) — the three non-multiplying retry bounds land here (D-15, §1.6.1)."
    - "Circuit breaker: 3 consecutive failures for a provider within 60s open it for 5 minutes (in-memory per-surface RouterAttemptState, D-14); cheapest-capable routing + fallback + circuit breaker are this plan's D-15 cost governors."
    - "The D-13 privacy gate is enforced HERE during fallback-chain traversal (never a resolveTier filter): under privacyMode 'prefer-local' (default), a failing local provider NEVER hops to a cloud provider — the turn terminates in a visible provider-failure state and the hop refusal is debugLogged (redacted)."
    - "Every SDK call the Router constructs passes maxRetries: 0 (Pitfall 1 — ai@4.3.19 silently defaults to 2) and an explicit maxTokens (planner/repair 256, renderer 512)."
    - "ProviderRouter constructs callProviderJsonMode (D-18): per-provider jsonMode resolution — Ollama → 'prompt' (model-dependent), openai/anthropic/gemini → 'native'; the closure encapsulates the fallback chain + breaker so after a failover only the Router knows the new (providerId, model, jsonMode) triple."
    - "The no-op budgetGuard hook (D-16) exists on the Router (Phase 6 wires the monthly ledger pre-flight without a rebuild)."
    - "StageInvocation resolution: createStageInvocation({ tier }) resolves the cheapest-capable candidate via resolveTier + getAISDKModel, builds the callProviderJsonMode closure, and returns the (providerId, model: LanguageModel, jsonMode, callProviderJsonMode) bundle — the seam the hook/orchestrator (03-06/03-08) consume."
  artifacts:
    - src/core/ai/ProviderRouter.ts
    - tests/core/ai/ProviderRouter.test.ts
  key_links:
    - "createStageInvocation → getAISDKModel (03-02) + resolveTier (03-02); the returned callProviderJsonMode feeds StructuredOutput.requestJson via PlannerService (03-04)."
    - "RouterAttemptState is the per-surface cost/privacy ledger the eval fixtures assert (3 fails/60s → open 5min; ≤3 non-multiplying attempts; prefer-local blocks cloud)."
  flagged_assumptions:
    - "AI-04 [concurrency]: RouterAttemptState is in-memory per-surface (D-14) — it dies on panel close; no cross-surface race on the single-writer bus; persistence/sharing is a later-phase ADR."
    - "AI-04 [idempotency]: router state is not persisted; a re-send after failure creates a fresh operationId + fresh attempt state (Retry semantics, UI-SPEC)."
    - "D-14 confirmed: hasStreamedFirstToken freezes the provider mid-stream (§1.5) — never switch after the first token; enforced inside the renderer-stage invocation path."
  prohibitions:
    - "No SDK-internal retries (maxRetries: 0 on every constructed call — hidden 4th layer)."
    - "No silent local→cloud switch under 'prefer-local' (D-13)."
    - "No resolveTier-level privacy filtering (D-13 — the gate lives here)."
    - "No 'local-only' privacyMode usage this phase (reserved)."
    - "No nested retry loops (R-2 — exactly one router retry per retryable step, 3 layers total across the app)."
    - "No raw prompt/key bodies in debugLog (TraceRedactor, R-10)."
---

<objective>
The cost/privacy core of the phase: `ProviderRouter` with the §1.5 fallback + circuit-breaker + retry policy (D-17), the D-13 privacy gate, the D-18 `callProviderJsonMode` construction (Seam 2), `StageInvocation` resolution, and the D-16 no-op `budgetGuard` hook — all proven by the injected-fetch test suite (AI-01 + AI-04).

Purpose: The Router owns every cost multiplier (maxRetries: 0, three non-multiplying layers, explicit maxTokens) and the privacy boundary ("no silent local→cloud switch"). This is the AI-SPEC eval suite's centerpiece (Cost discipline + Privacy boundary honesty dimensions, Critical/High).
Output: ProviderRouter.ts + its eval test file (fallback chains, breaker timing, privacy gate, maxRetries: 0 assertions, TraceRedactor adversarial fixtures).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/PRODUCT_SPEC_v0_1.md
@src/core/ai/types.ts
@src/core/ai/TierResolver.ts
@src/core/ai/ILLMProvider.ts
@src/core/error/debugLog.ts
@src/core/error/errorCodes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProviderRouter implementation (D-17/D-18/D-13/D-16/D-14)</name>
  <files>src/core/ai/ProviderRouter.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md §1.5 (Routing and Fallback + RouterAttemptState + retry/breaker policy, lines ~356-378), §1.6.1 (three non-multiplying retry layers, lines ~404-425)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md D-13 (privacy gate location), D-14 (state scope), D-16 (budgetGuard), D-17 (retry layering), D-18 (callProviderJsonMode ownership)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Pattern 2 (retry layering, lines 278-291) + Pattern 6 (native vs prompt JSON mode, lines 351-354) + AI-SPEC §4 Seam 2 sketch (buildCallProviderJsonMode, lines 383-431)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md File Classification row for `src/core/ai/ProviderRouter.ts` (role-match analog: `src/core/http/Requester.ts` bounded-retry/never-throw/error-classification + `src/core/utils/RateLimiter.ts` per-instance state) + the ProviderRouter section lines 132-163 (bounded-retry pattern Requester.ts lines 99-111; error classification Requester.ts lines 26-37; per-instance state shape RateLimiter.ts lines 25-46)
    - src/core/ai/TierResolver.ts (resolveTier, privacyModeFromPrefs, TierCandidate)
    - src/core/ai/ILLMProvider.ts (getAISDKModel — the ONLY provider-package import site, Seam 1)
    - src/core/error/errorCodes.ts (Phase-3 block) + debugLog.ts (redacted)
  </read_first>
  <action>
    Create src/core/ai/ProviderRouter.ts:
    - `export type JsonMode = 'native' | 'prompt';`
    - `export interface RouterAttemptState { operationId: string; attempts: number; hasStreamedFirstToken: boolean; circuitBreakerOpen: Partial<Record<ProviderId, number>> }` — per-surface in-memory (D-14), created per operationId via `createAttemptState(operationId)`.
    - `export interface StageInvocation { providerId: ProviderId; model: LanguageModel; jsonMode: JsonMode; callProviderJsonMode: CallProviderJsonMode }` where `CallProviderJsonMode = (prompt: string, jsonSchema: unknown, signal: AbortSignal) => Promise<string>`.
    - `export interface CreateStageInvocationInput { tier: ModelTier; operationId: string; configuredProviders: TierResolveInput['configuredProviders']; privacyMode: PrivacyMode }`.
    - `export class ProviderRouter { createStageInvocation(input): Promise<StageInvocation> }` — resolveTier(input) → null → throw a typed provider-unconfigured error (router code, e.g. PROVIDER_AUTH? NO — use a distinct canonical code; if none fits, reuse PROVIDER_KEY_UNREADABLE for unconfigured? NO — unconfigured ≠ unreadable; log + throw an Error with code 'PLANNER_FAILED'? NO. Decision: throw Error with code = existing Phase-3 code `NETWORK`? Also wrong. The cleanest: an unconfigured router surfaces reasonCode 'provider_unconfigured' at the ORCHESTRATOR level (terminal state, no model call) — the Router resolves it via a typed error `isProviderUnconfiguredError()` carrying no code but a `kind: 'unconfigured'` marker; debugLog uses the existing REGISTRY_INIT code. Document this in the file header: no new uncanonicalized code (Golden Rule 9).); then getAISDKModel(providerId, model, { apiKey, baseURL, fetch }) — apiKey/baseURL from the configured provider's resolved config (resolvedBaseURL = customBaseURL ?? baseURL per §10.2) — the Router receives the decrypted configs from the wiring layer (03-09), NEVER decrypts itself (R-3/Pitfall 4); builds the callProviderJsonMode closure (D-18): 'native' → generateObject({ model, schema: zodSchema(schema), mode: 'auto', system: (cached [SYSTEM] section text), prompt: (task-section text), maxTokens: 256, temperature: 0, maxRetries: 0, abortSignal }) returning JSON.stringify(object); 'prompt' → generateText({ model, system: 'Return JSON only, matching this schema exactly:\n' + JSON.stringify(jsonSchema), prompt, maxTokens: 256, maxRetries: 0, abortSignal }) returning text (D-19 — requestJson does the fence-strip + one repair).
    - Retry/breaker classification (D-17, REQUIRED): `classifyProviderError(err): { code, retryable }` — APICallError.statusCode 429 → RATE_LIMITED (retryable), 5xx → PROVIDER_5XX (retryable), fetch rejection → NETWORK (retryable), 401/403 → PROVIDER_AUTH (non-retryable), 404 → PROVIDER_MODEL_UNKNOWN (non-retryable), NoObjectGeneratedError.isInstance → SCHEMA_INVALID (non-retryable). The closure advances through the fallback chain on retryable failures (ONE router retry per step per §1.5 "retry once only for retryable pre-first-token failures"), honors the breaker (3 fails/60s → open 5min — `circuitBreakerOpen[providerId] = now + 300_000`), and NEVER retries non-retryable codes.
    - D-13 privacy gate: `privacyModeFromPrefs` is NOT consulted inside resolveTier (03-02 owns it); the ROUTER's fallback traversal checks: when the current provider is 'ollama' (local) and it fails AND privacyMode === 'prefer-local' → the chain terminates (NO cloud hop); debugLog the refusal (redacted) with REGISTRY_INIT or the router's canonical code; under 'cloud-ok' a local→cloud hop is legitimate and logged. NEVER a resolveTier filter.
    - `hasStreamedFirstToken` freeze (§1.5): once the renderer streamed its first token, the invocation is fixed — the renderer-stage path never re-resolves (enforced by the orchestrator contract: renderer invocation resolved once at finish() start).
    - D-16: `budgetGuard: (info: { operationId: string; providerId: ProviderId; model: string; estTokens: number }) => Promise<void>` — optional no-op hook (default no-op; Phase 6 wires the ledger pre-flight). Header documents the D-16 deferral.
    - `export function getProviderRouter(): ProviderRouter` — lazy singleton (ProviderRegistry precedent); the wiring layer (03-09) injects configuredProviders + prefs via `configure({ configuredProviders, privacyMode })`.
    Every catch debugLogs a canonical code with redacted extra (R-10). No raw prompt/key/tool bodies in any log line.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - grep -c "maxRetries: 0" src/core/ai/ProviderRouter.ts >= 2 (native + prompt paths).
    - grep -c "budgetGuard" src/core/ai/ProviderRouter.ts >= 1 (no-op seam exists).
    - grep -c "@ai-sdk\|from 'ai'" src/core/ai/ProviderRouter.ts >= 1 AND those imports are generateObject/generateText/zodSchema only (no provider packages — those stay in ILLMProvider.ts, Seam 1).
    - The unconfigured path throws the typed marker (no new uncanonicalized error strings beyond the documented marker).
  </acceptance_criteria>
  <done>Router implements D-13/D-14/D-16/D-17/D-18: privacy gate, breaker, maxRetries: 0, budgetGuard seam, StageInvocation construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProviderRouter eval tests (injected fetch, AI-SPEC §5)</name>
  <files>tests/core/ai/ProviderRouter.test.ts</files>
  <read_first>
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Pattern 4 (deterministic provider tests via injected fetch, lines 311-327) + Assumption A5 (browser Response/Headers/Request globals in jsdom-align come from Node/undici — mock-fetch tests must construct responses from the SAME global constructor the adapter calls; there is NO constructor-identity check in tests/setup.ts — A5 is a test-write-time concern, verify once per test file) + Validation Architecture "Phase Requirements → Test Map" AI-01/AI-04 rows + AI-SPEC §5 dimensions (Privacy boundary honesty, Cost discipline, Task completion)
    - src/core/ai/ProviderRouter.ts (the implementation from Task 1)
    - src/core/ai/TierResolver.ts
  </read_first>
  <behavior>
    - Test 1 (healthy): resolveStage('haiku') with openai/deepseek-chat configured → callProviderJsonMode(prompt, jsonSchema, signal) drives the REAL adapter via injected fetch → POST https://mock.local/v1/chat/completions; returns the JSON string; exactly 1 model call (fetch count).
    - Test 2 (privacy gate): configured = [ollama (dead host), openai (healthy cloud)], privacyMode 'prefer-local' → resolveStage for a tier whose first candidate is ollama fails → NO cloud hop (fetch to the openai URL count === 0) → the stage fails with the provider-failure marker; the refusal is debugLogged (redacted spy).
    - Test 3 (legitimate hop): same config, privacyMode 'cloud-ok' → dead ollama hops to openai → fetch to openai URL count === 1, result from openai.
    - Test 4 (breaker): 3 consecutive PROVIDER_5XX failures for openai within 60s (fake timers) → circuitBreakerOpen['openai'] set (now + 300_000); a 4th call is refused without a fetch (breaker hit).
    - Test 5 (maxRetries: 0): the mock fetch inspects the request body/headers of the SDK call and asserts maxRetries is 0 (assert via the constructed call settings — or assert the fetch receives exactly ONE attempt on a NETWORK flake: no SDK-internal retries).
    - Test 6 (non-retryable): a 401 AUTH response is NOT retried (single fetch) and classified PROVIDER_AUTH.
    - Test 7 (budgetGuard no-op): calling budgetGuard resolves without error.
    - Test 8 (adversarial secrets): a provider response body contains a fake api key 'sk-ADVERSARIAL-SECRET'; a captured debugLog sink over the router path contains no occurrence of that literal (TraceRedactor R-10).
  </behavior>
  <action>
    Create tests/core/ai/ProviderRouter.test.ts per the behavior block. Pattern: injected `fetch` into getAISDKModel (RESEARCH Pattern 4 — the REAL adapter against a fake transport; NO vi.mock of ai packages); mockFetch returns minimal OpenAI chat.completions JSON (and error statuses 429/401/5xx/network-reject for the failure fixtures); construct Response/Headers from the Node global (A5); fake timers for the 60s/5min breaker window; debugLog spied to assert redacted refusals. Provider configs come from a small fixture map (fixed apiKey 'sk-test', baseURL 'https://mock.local/v1'). Consume buildOptimizedContextFixture where a context is needed (03-03).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/ProviderRouter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass (healthy 1 call; prefer-local blocks cloud; cloud-ok hops; breaker 3/60s→5min; maxRetries: 0 asserted; 401 not retried; budgetGuard no-op; secrets never logged).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>AI-SPEC §5 cost/privacy eval suite green against the real SDK via injected fetch.</done>
</task>

</tasks>

## Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-04 [idempotency + concurrency] — recorded in `must_haves.flagged_assumptions` (router state per-surface in-memory, D-14; fresh operationId per retry). All other edges stay with their owners (never silently dropped).

## Artifacts This Phase Produces

- src/core/ai/ProviderRouter.ts (new): `JsonMode`, `RouterAttemptState`, `StageInvocation`, `CallProviderJsonMode`, `CreateStageInvocationInput`, `ProviderRouter` class (`createStageInvocation()`, `configure()`, `classifyProviderError()`, `budgetGuard` hook, breaker state), `getProviderRouter()` lazy singleton, `isProviderUnconfiguredError()` narrowing guard.
- tests/core/ai/ProviderRouter.test.ts (new): 8 eval fixtures (healthy/privacy/breaker/maxRetries/auth/budgetGuard/secrets).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router → provider endpoint | The privacy gate lives HERE: under 'prefer-local', a failing local provider must never transmit to a cloud endpoint (D-13) |
| Router → debugLog | Every refusal/retry/breaker event logs through TraceRedactor (R-10) — zero raw secrets/prompts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | fallback traversal | critical | mitigate | D-13 gate in the Router (never resolveTier): prefer-local + dead local → turn terminates in a visible provider-failure state, hop refusal debugLogged redacted, NO cloud fetch (test 2); cloud-ok → legitimate logged hop (test 3) |
| T-03-05-02 | (cost) | retry multiplication | critical | mitigate | maxRetries: 0 on every constructed SDK call (Pitfall 1); one router retry per retryable step (D-17); breaker 3/60s → 5min (D-14); non-retryable codes never retry; attempts bounded by the non-multiplying budget (tests 4-6) |
| T-03-05-03 | Information Disclosure | debugLog paths | high | mitigate | TraceRedactor boundary (R-10); adversarial-secrets fixture asserts the api key literal never appears in any captured log (test 8) |
| T-03-05-04 | Spoofing | custom baseURL endpoint | medium | accept | User-configured deliberate endpoint (D-12); direct surface→endpoint TLS; documented behavior (RESEARCH Security Domain) |
| T-03-05-SC | Tampering | pnpm installs | high | mitigate | No new packages this plan; approved set from 03-01 |
</threat_model>

<verification>
- `npx vitest run tests/core/ai/ProviderRouter.test.ts` green.
- `npx tsc --noEmit` green.
- Seam boundary: provider packages imported only in ILLMProvider.ts (grep).
</verification>

<success_criteria>
- Privacy boundary honesty proven: prefer-local never cloud-hops; redaction proven on every router log path.
- Cost discipline proven: healthy turn 1 call per stage, maxRetries: 0, breaker + attempt budget enforced.
- StageInvocation + budgetGuard seams exist for 03-06/03-08 and Phase 6.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-05-SUMMARY.md` when done
</output>
