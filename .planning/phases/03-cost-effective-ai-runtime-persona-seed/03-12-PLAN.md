---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 12
type: execute
wave: 7
depends_on: ["03-05", "03-06"]
files_modified:
  - src/core/ai/RendererService.ts
  - tests/core/ai/RendererService.test.ts
autonomous: true
gap_closure: true
requirements: [AI-03, AI-04]
must_haves:
  truths:
    - "Circuit breaker + stream-freeze guard protect the streaming path (§1.5 / D-14: never switch provider after the first token; mid-stream failures vote the breaker)"
  artifacts:
    - "src/core/ai/RendererService.ts — render() catch + non-stop finish branches call getProviderRouter().recordFailure(providerId, ERROR_CODES.STREAM_FAILED, err) for provider-originated failures; the delta loop calls markStreamedFirstToken(operationId) once (firstTokenMarked flag)"
    - "tests/core/ai/RendererService.test.ts — regression: mid-stream rejection votes the breaker; first delta marks the first token; user aborts do NOT vote"
  key_links:
    - "RendererService.render → getProviderRouter() singleton (lazy, ProviderRouter.ts L736-746) — the ONLY production wiring of recordFailure + markStreamedFirstToken (currently ZERO callers, VERIFICATION.md L21)"
    - "isAbortError guard in the catch — a user abort must not vote the breaker (only provider-originated failures vote)"
  flagged_assumptions:
    - "AI-03 [probe: 'empty, single-element, or null input']: ChunkBuffer byte-rate uses UTF-16 code-unit string length (ChunkBuffer.ts L8 header already documents this as the AI-03 [encoding] flagged assumption); empty deltas simply don't grow accumulated text; null never reaches the delta loop (TypeScript contract, onDelta receives strings only)"
    - "AI-04 [probe unclassified]: mid-stream failure now accrues 1 breaker vote (BREAKER_VOTES.STREAM_FAILED → falls through the table to 0 in the CURRENT constants — see threat register T-03-12-01 for the exact accounting decision; the STREAM_FAILED code votes per the classifier's mapped code, never a hardcoded double-count)"
---
<objective>
Close VERIFICATION.md gap 2 (WR-02): `recordFailure()` and `markStreamedFirstToken()` have ZERO production callers — `RendererService.render` throws `STREAM_FAILED` without voting the breaker, and `hasStreamedFirstToken` stays false forever, so the §1.5 stream_frozen guard can never fire and a provider failing mid-stream accrues no breaker votes (retried every turn).

Purpose: wire the two Router-side streaming contracts (03-05 plan truth "D-14 confirmed: hasStreamedFirstToken freezes the provider mid-stream") into the ONLY production path that needs them — `RendererService.render()` (03-REVIEW.md WR-02 fix sketch). The catch and non-stop finish branches vote the breaker for provider-originated failures; the delta loop marks the first streamed token exactly once. A user abort (AbortError) must NOT vote the breaker — only provider failures do (per the PATTERNS planner note reconciling the REVIEW sketch's unconditional recordFailure with the isAbortError guard).

Output: wired breaker/stream-freeze in RendererService + regression tests in RendererService.test.ts.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-VERIFICATION.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-REVIEW.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-05-SUMMARY.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-06-SUMMARY.md
@src/core/ai/RendererService.ts
@src/core/ai/ProviderRouter.ts
@tests/core/ai/RendererService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-02 — wire recordFailure + markStreamedFirstToken into RendererService.render()</name>
  <files>src/core/ai/RendererService.ts</files>
  <read_first>
    - src/core/ai/RendererService.ts L72-117 (render() — the DEFECTIVE pattern: catch L95-103 and non-stop branch L104-116 throw STREAM_FAILED with NO recordFailure; delta loop L89-92 never calls markStreamedFirstToken)
    - 03-REVIEW.md WR-02 (L114-132) — the fix sketch
    - 03-PATTERNS.md WR-02 (L135-147) — the fix sketch + the planner note: reconcile with isAbortError so a user abort does NOT vote
    - src/core/ai/ProviderRouter.ts L470-477 (markStreamedFirstToken), L517-523 (recordFailure), L732-734 (isAbortError name-match helper)
    - src/core/error/errorCodes.ts (ERROR_CODES.STREAM_FAILED)
  </read_first>
  <action>
    In `RendererService.render()` (RendererService.ts L72-117):
    1. Import `getProviderRouter` from `@/core/ai/ProviderRouter` (extend the existing import at L23 — currently only `buildStageMessages` + the `StageInvocation` type).
    2. Add a local `isAbortError(err: unknown): boolean` helper (name-match pattern: `err instanceof Error && err.name === 'AbortError'`) — or import the existing pattern; do NOT introduce a new module import beyond the router singleton.
    3. Add `let firstTokenMarked = false;` before the delta loop.
    4. Inside the `for await (const delta of result.textStream)` loop, AFTER `input.onDelta?.(delta)`: `if (!firstTokenMarked) { getProviderRouter().markStreamedFirstToken(input.operationId); firstTokenMarked = true; }`.
    5. In the catch (L95-103): BEFORE the debugLog, guard with `if (!isAbortError(e))` and call `getProviderRouter().recordFailure(input.invocation.providerId, ERROR_CODES.STREAM_FAILED, err)` — a user abort must NOT vote the breaker (only provider-originated mid-stream failures do). Keep the existing debugLog + `throw streamFailed(...)` unchanged.
    6. In the non-stop finish branch (L104-116): call `getProviderRouter().recordFailure(input.invocation.providerId, ERROR_CODES.STREAM_FAILED, err)` alongside the existing debugLog (a content-filter/length finish is a provider behavior, never a user abort — no guard needed), then keep the throw.
    7. Do NOT change: RENDERER_MAX_TOKENS, the RenderInput contract, buildStageMessages usage, maxRetries: 0, finishReason awaiting, the StreamFailedError carrier, or partialText semantics.
    8. Update the module header doc (L12-18) with one line noting the breaker/stream-freeze wiring (WR-02) and the isAbortError guard.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/ai/RendererService.test.ts --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recordFailure" src/core/ai/RendererService.ts` returns exactly 2 (catch + non-stop branch)
    - `grep -c "markStreamedFirstToken" src/core/ai/RendererService.ts` returns exactly 1 (inside the delta loop, guarded by firstTokenMarked)
    - The catch's recordFailure is preceded by an `isAbortError` guard
    - `pnpm vitest run tests/core/ai/RendererService.test.ts --passWithNoTests` exits 0 (existing suite passes unmodified — the mockStream seam is untouched)
  </acceptance_criteria>
  <done>The streaming path is wired: mid-stream provider failures vote the breaker, the first token freezes the provider, and user aborts never vote.</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 — regression tests (breaker votes + first-token freeze + abort exclusion)</name>
  <files>tests/core/ai/RendererService.test.ts</files>
  <read_first>
    - tests/core/ai/RendererService.test.ts L32-38 (vi.mock('ai') stub-only streamText), L58-68 (baseInput), L70-89 (mockStream seam), L46-56 (invocationStub)
    - 03-PATTERNS.md "WR-02 — extend tests" (L343-344) — the mock-router pattern (vi.mock '@/core/ai/ProviderRouter' returning { recordFailure, markStreamedFirstToken } — the useStreamingLLM.test.tsx L45-47 pattern, but see the caveat below)
    - src/core/ai/RendererService.ts imports buildStageMessages from '@/core/ai/ProviderRouter' — a WHOLE-module mock would break the F-5 shape; use importOriginal spread (ProviderRouter.test.ts L46-53 pattern) to keep real exports and stub ONLY getProviderRouter
  </read_first>
  <action>
    Extend `tests/core/ai/RendererService.test.ts`:
    1. Add a partial module mock for the router singleton that preserves the real ProviderRouter exports (RendererService imports buildStageMessages from it): `vi.mock('@/core/ai/ProviderRouter', async (importOriginal) => { const actual = await importOriginal<typeof import('@/core/ai/ProviderRouter')>(); return { ...actual, getProviderRouter: () => routerMock }; })` with a `vi.hoisted` `routerMock = { recordFailure: vi.fn(), markStreamedFirstToken: vi.fn() }`.
    2. beforeEach: `vi.clearAllMocks()` already runs; add `routerMock.recordFailure.mockClear(); routerMock.markStreamedFirstToken.mockClear();` to the existing beforeEach (L92-94 area).
    3. New tests:
       a. 'mid-stream rejection votes the breaker': `mockStream({ streamThrows: new Error('boom') })` → `render(baseInput())` rejects with a StreamFailedError (isStreamFailedError true) → `routerMock.recordFailure` called once with `(invocation.providerId, 'STREAM_FAILED')` — assert the exact first arg equals the baseInput invocation's providerId ('anthropic') and the code 'STREAM_FAILED'; assert `routerMock.markStreamedFirstToken` called ONCE (deltas 'Hel','lo' streamed before the throw).
       b. 'first streamed delta marks the provider frozen (never switches)': `mockStream({ deltas: ['a'], finishReason: 'stop' })` → render resolves → `markStreamedFirstToken` called exactly once with the operationId; `recordFailure` NOT called (clean stop path).
       c. 'a user abort does NOT vote the breaker': `mockStream({ streamThrows: new DOMException('aborted', 'AbortError') })` → render rejects → `recordFailure` NOT called (isAbortError guard), but `markStreamedFirstToken` IS called once (the first token legitimately streamed before the abort).
       d. 'non-stop finish votes the breaker': `mockStream({ deltas: ['x'], finishReason: 'length' })` → render rejects → `recordFailure` called once with `(providerId, 'STREAM_FAILED')`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/core/ai/RendererService.test.ts --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - The file contains the partial `vi.mock('@/core/ai/ProviderRouter', ...)` with importOriginal spread + hoisted routerMock
    - 4 new tests assert: mid-stream rejection votes (providerId + 'STREAM_FAILED'), first delta marks once, user abort does NOT vote, non-stop finish votes
    - `pnpm vitest run tests/core/ai/RendererService.test.ts --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>The breaker/stream-freeze wiring is pinned by regression tests covering the four streaming outcomes (fail, clean stop, user abort, non-stop finish).</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Details |
|----------|------|---------|
| `RendererService.render()` | modified | catch + non-stop branches call `getProviderRouter().recordFailure(providerId, ERROR_CODES.STREAM_FAILED, err)` (abort-guarded in the catch); delta loop calls `markStreamedFirstToken(operationId)` once via firstTokenMarked |
| `isAbortError` helper | new local | name-match guard in RendererService.ts distinguishing user aborts from provider failures |
| `tests/core/ai/RendererService.test.ts` | updated | partial router mock (importOriginal spread) + 4 regression tests |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider SDK stream → RendererService | mid-stream errors/finish reasons cross in; origin (provider vs user abort) must be discriminated before breaker voting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12-01 | DoS | recordFailure breaker voting | high | mitigate | ONLY provider-originated failures vote: the catch guards with isAbortError (user abort never votes); the non-stop finish branch (content-filter/length) votes as a provider behavior. recordFailure delegates to voteBreaker whose table (BREAKER_VOTES) and window (3/60s → open 5min) bound the blast radius |
| T-03-12-02 | Tampering | markStreamedFirstToken freeze | medium | mitigate | firstTokenMarked flag ensures exactly ONE mark per render; after it, createStageInvocation throws stream_frozen — the D-14 no-switch invariant is enforced (asserted by test b) |
| T-03-12-03 | Info Disclosure | err object passed to recordFailure → voteBreaker debugLog | low | mitigate | debugLog auto-routes through TraceRedactor (R-10); the err is `err instanceof Error ? err : undefined` — never raw stream text or provider keys (RendererService already logs only `partialTokens` counts; the wired path adds no new body logging) |
| T-03-12-04 | Repudiation | breaker state is in-memory per-surface (D-14) | low | accept | documented D-14 scope: state dies on panel close; no persistence claim is made |

No new network surface, no new storage, no new secrets. The security delta is the breaker-vote origin guard (T-03-12-01) — the single high-severity item, mitigated by the isAbortError guard + voteBreaker's existing bounds.
</threat_model>

<verification>
- `pnpm vitest run tests/core/ai/RendererService.test.ts --passWithNoTests`
- `pnpm vitest run tests/core/ai/AgentOrchestrator.test.ts --passWithNoTests` (orchestrator's render call site must not regress)
- `pnpm run verify:phase-3` (full gate) must stay green
- Grep: `grep -rn "recordFailure\|markStreamedFirstToken" src/ | grep -v ProviderRouter.ts | grep -v node_modules` — RendererService.ts appears as the production caller
</verification>

<success_criteria>
- A provider failing mid-stream accrues breaker votes (recordFailure wired in both failure branches)
- The first streamed token marks the provider frozen — the stream_frozen guard in createStageInvocation is reachable
- User aborts never vote the breaker (isAbortError guard)
- Full `verify:phase-3` gate green
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-12-SUMMARY.md` when done
</output>
