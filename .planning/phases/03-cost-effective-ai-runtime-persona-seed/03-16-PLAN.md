---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 16
type: execute
wave: 10
depends_on: ["03-15"]
files_modified:
  - src/core/ai/StructuredOutput.ts
  - src/core/ai/ProviderRouter.ts
  - tests/core/ai/ProviderRouter.test.ts
  - tests/core/ai/StructuredOutput.timeoutRetry.test.ts
autonomous: true
gap_closure: true
requirements: [AI-04]
must_haves:
  truths:
    - "WR-03A closed: a production timeout (per-attempt abort) reaches the router's retry decision as the TimeoutError carrier — the D-17 retry fires EXACTLY once on a FRESH non-aborted signal (generateObject called 2×, retryCount === 1, ledger attempts[0].errorCode === 'TIMEOUT')"
    - "The retried call is never futile by construction: each attempt derives its own controller, so the retry's signal is not the already-aborted timeout signal"
    - "The planner_failed fallback source is intact: when the D-17 retry also fails, requestJson still rejects with the TimeoutError carrier (planOnce converts it to the visible planner_failed answer — never a silent idle, never a re-invocation, R-2)"
  artifacts:
    - "src/core/ai/StructuredOutput.ts — the per-attempt timeout callback (L94-97) calls ac.abort(timeoutError(ctx.timeoutMs)) so signal.reason IS the TimeoutError carrier"
    - "src/core/ai/ProviderRouter.ts — buildCallProviderJsonMode's attempt() catches the SDK's bare AbortError, checks isTimeoutError(signal.reason) FIRST and rethrows the carrier BEFORE classification; each attempt derives a fresh controller (the D-17 retry runs on a non-aborted signal, never the aborted timeout signal)"
    - "tests/core/ai/ProviderRouter.test.ts — the defective L354-373 carrier-injection test is reframed to the production arrival pattern (abort with carrier reason → SDK rejects AbortError → closure recovers the carrier → retry)"
    - "tests/core/ai/StructuredOutput.timeoutRetry.test.ts — NEW end-to-end regression through requestJson + a REAL Router: 25 ms timeout, SDK mocked to reject AbortError on signal abort, asserting generateObject 2×, retryCount 1, ledger errorCode TIMEOUT, retry signal non-aborted"
  key_links:
    - "StructuredOutput ac.abort(timeoutError(...)) → ac.signal.reason = carrier → SDK rejects bare AbortError → closure attempt() catch: isTimeoutError(signal.reason) → throw carrier → classifyProviderError → TIMEOUT/retryable → D-17 retry with a fresh derived signal"
    - "retry success → requestJson returns the parsed decision; retry failure → the attempt catch's timedOut branch still throws the carrier → planOnce's planner_failed fallback (unchanged, AgentOrchestrator L188-201)"
  flagged_assumptions:
    - "AI-02 [concurrency] + AI-04 [idempotency] (carried from 03-04's must_haves): requestJson is a single sequential loop (first attempt + one repair — never concurrent); requestJson is NOT idempotent — every call is a fresh paid model call, but the D-17 router retry is the ONE retry layer, bounded (exactly one per retryable failure, retryCount ≤ ROUTER_MAX_ATTEMPTS, R-2). The new end-to-end test asserts 2× (never 3+) on the timeout path"
    - "AI-07 [unclassified] stays ACCOUNTED-DEFERRED (Phase 8 D-06, recorded in 03-10) — never re-opened"
---
<objective>
Close the empirically-confirmed residual WR-03A: the D-17 retry on TIMEOUT never fires on the production path. `StructuredOutput.attempt`'s per-attempt timeout aborts the SAME signal threaded into the router closure, so the SDK rejects with a bare AbortError INSIDE the closure — `classifyProviderError` maps it to UNKNOWN (not retryable) before the retry decision, and the TimeoutError carrier is born only in `attempt`'s catch AFTER the closure returned. The unit test at ProviderRouter.test.ts L354-373 injects the carrier as the SDK mock rejection — an arrival pattern production never produces — and even a carrier reaching the closure would retry with the same already-aborted signal (futile by construction).

Purpose: implement the locked fix option (b) — the timeout-origin carrier rides the abort reason. `ac.abort(timeoutError(ctx.timeoutMs))` makes `signal.reason === <TimeoutError carrier>`; the router closure catches the abort-origin rejection, checks `isTimeoutError(signal.reason)` FIRST, and rethrows the carrier so classification maps TIMEOUT/retryable and the D-17 retry fires exactly once. The retried call gets a fresh non-aborted signal (per-attempt derived controller) so the retry can actually succeed. The `planner_failed` fallback answer semantics (WR-03's primary user-visible fix) must NOT regress.

Output: `ac.abort(timeoutError(...))` in StructuredOutput, the closure's `isTimeoutError(signal.reason)` recovery + fresh-signal per-attempt controller in ProviderRouter, the reframed unit test, and the new end-to-end `StructuredOutput.timeoutRetry.test.ts` regression.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-VERIFICATION.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-REVIEW.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-11-PLAN.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-11-SUMMARY.md
@src/core/ai/StructuredOutput.ts
@src/core/ai/ProviderRouter.ts
@src/core/error/TimeoutError.ts
@tests/core/ai/StructuredOutput.test.ts
@tests/core/ai/ProviderRouter.test.ts
@tests/core/ai/AgentOrchestrator.budget.test.ts
@src/core/ai/AgentOrchestrator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-03A — carrier as abort reason + closure timeout-awareness with fresh-signal retry</name>
  <files>src/core/ai/StructuredOutput.ts, src/core/ai/ProviderRouter.ts</files>
  <read_first>
    - src/core/ai/StructuredOutput.ts L89-107 (attempt(): the DEFECTIVE pattern — L94-97 bare `ac.abort()` aborts the signal the closure consumes; the carrier is born only in the L100-102 catch AFTER the closure's retry decision)
    - src/core/ai/ProviderRouter.ts L586-663 (buildCallProviderJsonMode: the closure; attempt() L611-647 reuses the SAME `signal` for the retry at L658 — futile by construction; the outer retry decision L648-661), L670-712 (invokeJsonMode consumes `signal` as abortSignal at L692/L709), L782-784 (isAbortError — name-match)
    - src/core/error/TimeoutError.ts L30-40 (isTimeoutError guard + timeoutError factory — the carrier shape; R-10: carries only timeoutMs)
    - 03-REVIEW.md WR-03A (the four-part defect chain: bare AbortError inside the closure → UNKNOWN → no retry; carrier born after the retry decision; unit test injects an unreal arrival pattern; retry reuses the aborted signal)
    - 03-VERIFICATION.md gap 3 reason + missing (option (b) is the locked path)
  </read_first>
  <action>
    In `src/core/ai/StructuredOutput.ts` (attempt L89-107):
    1. Change the setTimeout callback (L94-97) from bare `ac.abort()` to `ac.abort(timeoutError(ctx.timeoutMs))` — the carrier becomes `ac.signal.reason`. Keep `timedOut = true` BEFORE the abort (the flag still guards the L100-102 catch, unchanged).
    2. Update the WR-03 comment block (L83-88) with one WR-03A line: the timeout-origin abort carries the typed carrier as the abort reason so the router closure can recover it inside the retry decision point.
    3. Do NOT change: the catch (L100-102 `if (timedOut) throw timeoutError(ctx.timeoutMs); throw e;` — the retry-failed-with-non-timeout case is intentionally masked as the carrier, which is what planOnce converts to planner_failed), the outer-abort re-parenting (L90-92, L105), or the requestJson flow (first + one repair).
    In `src/core/ai/ProviderRouter.ts` (buildCallProviderJsonMode L586-663):
    4. Inside `attempt(isRetry)` (L611-647): before `this.invokeJsonMode`, create a per-attempt derived controller and re-parent the incoming signal to it:
       - `const derived = new AbortController();`
       - `const onParentAbort = () => derived.abort(signal.reason);` (the reason is preserved — for a timeout it is the carrier, for a user abort a DOMException)
       - `signal.addEventListener('abort', onParentAbort);`
       - pass `derived.signal` to `invokeJsonMode` instead of `signal`
       - add a `finally` that `signal.removeEventListener('abort', onParentAbort)` (no listener leaks across the first + repair closure invocations)
       - NOTE: for the retried call the parent signal is ALREADY aborted — `addEventListener` on an aborted signal never fires, so the retry's derived controller stays fresh (this is the intended "fresh non-aborted signal"; the retry is bounded to exactly one, R-2)
    5. In `attempt()`'s catch (L634-646), BEFORE `const cls = this.classifyProviderError(e)`: recover the timeout origin — `if (isAbortError(e) && isTimeoutError(signal.reason)) throw signal.reason;` — the SDK rejects with a bare AbortError and drops the reason, so the carrier must be recovered from the incoming signal's reason. Throwing the carrier FIRST means the ledger records TIMEOUT (recordAttempt at L636-643 sees cls.code === 'TIMEOUT' via the outer classify) and the D-17 retry decision (L656) fires.
    6. Update the D-17 header comment (L578-585) and the module header (L1-26) with WR-03A lines: timeout-origin aborts are recovered from signal.reason inside the closure; each attempt derives a fresh controller so the retry is never futile.
    7. Do NOT change: RETRYABLE_CODES, BREAKER_VOTES (Task 1 of 03-15 added STREAM_FAILED), ROUTER_MAX_ATTEMPTS, the budget gates, recordAttempt/retryCount semantics (CR-01), or classifyProviderError's ordering.
    8. Do NOT copy the literal `ac.abort(timeoutError(ctx.timeoutMs))` form into comment prose.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/core/ai/StructuredOutput.test.ts tests/core/ai/ProviderRouter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '94,97p' src/core/ai/StructuredOutput.ts | grep -c "timeoutError"` returns 1 (the abort carries the carrier — `ac.abort(timeoutError(ctx.timeoutMs))`)
    - `sed -n '94,97p' src/core/ai/StructuredOutput.ts | grep -c "timedOut = true"` returns 1 (the flag still precedes the abort)
    - `sed -n '611,647p' src/core/ai/ProviderRouter.ts | grep -c "isTimeoutError(signal.reason)"` returns 1 (the recovery guard) and `grep -n "new AbortController()" src/core/ai/ProviderRouter.ts` shows a per-attempt derivation inside the closure (L586-663 region)
    - `grep -n "removeEventListener" src/core/ai/ProviderRouter.ts` returns at least 1 (the re-parent listener is cleaned up in a finally)
    - `./node_modules/.bin/vitest run tests/core/ai/StructuredOutput.test.ts tests/core/ai/ProviderRouter.test.ts` exits 0 (both existing suites stay green — the direct-injection carrier test at ProviderRouter.test.ts L354-373 still passes after the source change; Task 2 reframes it)
    - `./node_modules/.bin/tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The timeout path now reaches the D-17 retry: the carrier rides signal.reason, the closure recovers it before classification, and the retry runs on a fresh non-aborted derived controller.</done>
</task>

<task type="auto">
  <name>Task 2: WR-03A — reframe the defective unit test to the production arrival pattern</name>
  <files>tests/core/ai/ProviderRouter.test.ts</files>
  <read_first>
    - tests/core/ai/ProviderRouter.test.ts L354-373 (the DEFECTIVE test 'retries EXACTLY once on a TimeoutError carrier (WR-03, D-17) then succeeds' — feeds timeoutError(3_000) as the generateObject mock rejection, an arrival pattern production never produces)
    - tests/core/ai/ProviderRouter.test.ts L336-352 (the 5xx retry test — the shape to mirror: mockResolveTier + createStageInvocation + generateObjectMock once-reject/once-resolve + callProviderJsonMode)
    - tests/core/ai/ProviderRouter.test.ts L39 (import { timeoutError }), L340-347 (section() + makeInput helpers)
    - 03-VERIFICATION.md gap 3 artifacts ("tests/core/ai/ProviderRouter.test.ts L354-373 retry-on-TimeoutError test feeds the carrier at the SDK boundary — not reachable from the production timeout flow")
  </read_first>
  <action>
    Rewrite the test at `tests/core/ai/ProviderRouter.test.ts` L354-373 so it exercises the PRODUCTION arrival pattern:
    1. Keep the name intent but rename to the production framing: 'retries EXACTLY once on a production timeout — abort with the carrier reason, SDK rejects AbortError (WR-03A, D-17)'.
    2. Keep `mockResolveTier({ providerId: 'openai', model: 'deepseek-chat', fallbackChain: [] })`, `new ProviderRouter()`, `createStageInvocation(makeInput())`.
    3. The SDK mock: FIRST call uses `mockImplementationOnce` with a promise that registers an abort listener on the abortSignal it receives and rejects with `new DOMException('aborted', 'AbortError')` when it fires (`if (sig.aborted) reject(...)` guard included for determinism); SECOND call `mockResolvedValueOnce({ object: { answer: '42' } })`.
    4. Drive a real timeout-origin abort: create the controller, `setTimeout(() => ac.abort(timeoutError(3_000)), 0)`, and pass `ac.signal` to `callProviderJsonMode` — the per-attempt-style timeout that production produces.
    5. Assert: `generateObjectMock` called 2× (initial + exactly ONE D-17 retry — never 3+, T-03-11-02), `out === '{"answer":"42"}'`, `router.getAttemptState('op-test-0001')?.attempts[0].errorCode === 'TIMEOUT'` (the ledger records the recovered code — the WR-03A ledger requirement), `retryCount === 1`, and the retried SDK call ran on a NON-aborted signal (`generateObjectMock.mock.calls[1][0].abortSignal.aborted === false` — the fresh-signal proof; pre-fix this was the same aborted signal, i.e. a futile retry).
    6. Keep the import of `timeoutError` (L39) and `section()`/`makeInput()` helpers — no new imports beyond what the file already has.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/core/ai/ProviderRouter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The L354-373 test no longer passes `timeoutError(...)` as a `mockRejectedValue`/`mockRejectedValueOnce` direct rejection (`grep -n "mockRejectedValueOnce(timeoutError" tests/core/ai/ProviderRouter.test.ts` returns 0 lines)
    - The test drives the abort via `setTimeout(() => ac.abort(timeoutError(3_000)), 0)` with the SDK mocked to reject AbortError on signal abort — the production arrival pattern
    - The test asserts retryCount === 1 AND the retried call's `abortSignal.aborted === false` (fresh signal — a futile retry now fails this test)
    - `./node_modules/.bin/vitest run tests/core/ai/ProviderRouter.test.ts` exits 0 (the reframed test + all existing Router tests green)
  </acceptance_criteria>
  <done>The D-17 retry-on-timeout unit test now reproduces the production arrival pattern end-to-end at the unit level and would catch a regression to the same-aborted-signal futile retry.</done>
</task>

<task type="auto">
  <name>Task 3: WR-03A — NEW end-to-end regression: StructuredOutput.timeoutRetry.test.ts (requestJson + REAL Router)</name>
  <files>tests/core/ai/StructuredOutput.timeoutRetry.test.ts</files>
  <read_first>
    - tests/core/ai/AgentOrchestrator.budget.test.ts L1-111 (the real-Router precedent: `new ProviderRouter()` per test, REAL resolveTier via configuredProviders + getModel: () => fakeModel, ONLY the ai-sdk call sites stubbed with the real error classes kept via importOriginal)
    - tests/core/ai/StructuredOutput.test.ts L13-56 (requestJson + DecisionSchema + makeContext shape; the abort-listener responder pattern at L172-198 — the production arrival pattern for the SDK mock)
    - src/core/ai/StructuredOutput.ts L77-141 (requestJson — first + one repair; the attempt's timeout + catch)
    - src/core/ai/ProviderRouter.ts L586-663 (the closure the retry lives in), L502-537 (classifyProviderError)
    - 03-VERIFICATION.md gap 3 missing + the empirical probe parameters ("real requestJson + real Router closure, 25 ms timeout, SDK mocked to reject AbortError on aborted signal")
  </read_first>
  <action>
    Create `tests/core/ai/StructuredOutput.timeoutRetry.test.ts` (NEW file) — the WR-03A permanent end-to-end regression:
    1. Module mock: `vi.mock('ai', async (importOriginal) => { const actual = await importOriginal<typeof import('ai')>(); return { ...actual, generateObject: vi.fn(), generateText: vi.fn(), streamText: vi.fn() }; })` — real error classes kept (APICallError), only the SDK call sites stubbed (the budget-test precedent).
    2. Fixtures: a local `DecisionSchema` (z.object with `action: z.literal('answer')`, `reasonCode: z.string().max(64)`, `confidence: z.number().min(0).max(1)` — the StructuredOutput.test.ts shape); `buildOptimizedContextFixture()` sections; a `fakeModel` LanguageModel cast (budget-test L54-58).
    3. Helper `buildRealInvocation()`: `const router = new ProviderRouter(); const inv = router.createStageInvocation({ operationId, tier: 'haiku', privacyMode: 'prefer-local', maxTokens: 256, configuredProviders: [{ id: 'openai', models: ['deepseek-chat'], enabled: true, priority: 1 }], getModel: () => fakeModel });` — REAL resolveTier, REAL closure, fresh router + unique operationId per test.
    4. Helper `abortSensitiveReject()`: returns a `mockImplementationOnce` fn for generateObject that registers `(args.abortSignal).addEventListener('abort', () => reject(new DOMException('aborted', 'AbortError')))` (with an `if (sig.aborted)` immediate-reject guard) — the production arrival pattern: the SDK drops the reason and rejects bare AbortError.
    5. Tests:
       a. 'the D-17 retry fires END-TO-END on a production timeout — 2 SDK calls, retryCount 1, ledger TIMEOUT, fresh retry signal': `generateObjectMock` = abortSensitiveReject() then `mockResolvedValueOnce({ object: { action: 'answer', reasonCode: 'ok', confidence: 0.9 } })`; call `requestJson(DecisionSchema, fixture.sections, { operationId, providerId: inv.providerId, model: inv.model.modelId, timeoutMs: 25, abortSignal: new AbortController().signal, callProviderJsonMode: inv.callProviderJsonMode })`. Assert: resolves to the decision; `generateObjectMock` called 2× (initial + exactly one retry — never 3+); `router.getAttemptState(operationId)` — `retryCount === 1`, `attempts[0]` matches `{ outcome: 'failed', errorCode: 'TIMEOUT' }`; the SECOND call's `abortSignal.aborted === false` (fresh signal at call time).
       b. 'when the D-17 retry ALSO fails, requestJson rejects with the TimeoutError carrier — the planner_failed fallback source is intact': `generateObjectMock` = abortSensitiveReject() then `mockRejectedValueOnce(new APICallError({ message: 'upstream 500', url: 'https://fixture.example/v1/responses', requestBodyValues: {}, statusCode: 500 }))`; `requestJson(...)` rejects; assert `isTimeoutError(caught) === true` (the L100-102 timedOut rethrow — this carrier is what planOnce converts to the visible planner_failed answer, AgentOrchestrator L188-201); assert `generateObjectMock` called exactly 2× (the retry is bounded — never 3+).
       c. 'a healthy first call never retries — 1 SDK call, retryCount 0': `mockResolvedValueOnce` valid object; assert 1 call, `retryCount === 0`, resolves.
    6. Header comment: this file reproduces VERIFICATION.md WR-03A EXACTLY (the empirical probe: real requestJson + real Router closure, 25 ms timeout — pre-fix generateObject 1×, retryCount 0, ledger 'UNKNOWN') and pins the post-fix contract (2×, retryCount 1, ledger TIMEOUT).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/core/ai/StructuredOutput.timeoutRetry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and drives a REAL `new ProviderRouter()` closure through `requestJson` (never a mocked callProviderJsonMode) with a 25 ms timeout
    - `grep -c "retryCount" tests/core/ai/StructuredOutput.timeoutRetry.test.ts` returns at least 2 (test a and test c assert the retry budget)
    - `grep -c "TIMEOUT" tests/core/ai/StructuredOutput.timeoutRetry.test.ts` returns at least 1 (the ledger errorCode assertion)
    - Test a asserts: generateObject 2×, retryCount 1, attempts[0].errorCode 'TIMEOUT', retry signal non-aborted; test b asserts isTimeoutError on the rejection with exactly 2 calls; test c asserts 1 call / retryCount 0
    - `./node_modules/.bin/vitest run tests/core/ai/StructuredOutput.timeoutRetry.test.ts` exits 0
  </acceptance_criteria>
  <done>The D-17 timeout retry is permanently pinned END-TO-END through the production path — a regression to the pre-fix behavior (1 call, retryCount 0, ledger UNKNOWN) fails this suite immediately.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Details |
|----------|------|---------|
| `ac.abort(timeoutError(ctx.timeoutMs))` | behavior change | StructuredOutput.ts L94-97 — the timeout-origin abort carries the typed carrier as `signal.reason` (previously a bare abort whose reason the SDK dropped) |
| closure `isTimeoutError(signal.reason)` recovery | behavior change | ProviderRouter.ts attempt() catch — before classification, a bare AbortError rejection whose parent signal carries the carrier is rethrown as the carrier (the SDK drops the reason; the carrier must be recovered from the signal) |
| per-attempt fresh derived controller | behavior change | ProviderRouter.ts attempt() — each call derives its own AbortController re-parented to the incoming signal with a finally cleanup; the D-17 retry runs on a fresh non-aborted signal (pre-fix: the same already-aborted signal — a futile retry by construction) |
| reframed unit test | test update | ProviderRouter.test.ts L354-373 — abort with carrier reason → SDK rejects AbortError → closure maps to TIMEOUT → exactly one retry on a fresh signal; asserts retryCount 1 + retry signal non-aborted |
| `tests/core/ai/StructuredOutput.timeoutRetry.test.ts` | NEW file | end-to-end regression through `requestJson` + a REAL Router: 25 ms timeout, production arrival pattern, asserting generateObject 2×, retryCount 1, ledger errorCode TIMEOUT, fresh retry signal, and the carrier surfacing when the retry also fails (planner_failed source intact) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StructuredOutput per-attempt controller → Router closure | the timeout-origin abort crosses here as `signal.reason`; the SDK drops the reason and rejects AbortError, so the closure recovers the carrier from the incoming signal |
| Router closure → SDK call (retry) | the D-17 retry's abortSignal is a fresh derived controller — the user-cancel intent no longer propagates to the retry (bounded single retry) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-16-01 | DoS | D-17 retry on timeout | high | mitigate | the retry is bounded EXACTLY once per retryable failure (R-2: retried flag + retryCount ≤ ROUTER_MAX_ATTEMPTS gate L612, never a nested loop); the end-to-end test asserts 2× never 3+ (T-03-11-02 preserved). This makes the intended §20.10 single retry LIVE — it does not add a retry layer |
| T-03-16-02 | DoS | retried call has no per-attempt timeout (fresh signal) | low | accept | the retry is a single bounded call under the R-2 cap; this mirrors the pre-existing 5XX-retry semantics (same signal, no re-armed timeout) — no NEW unbounded exposure is introduced; a user cancel during the single retry does not abort it (documented accepted gap, the parent signal is already aborted at retry time) |
| T-03-16-03 | Tampering | signal.reason spoof → forced TIMEOUT classification | low | accept | the carrier is produced only by StructuredOutput's own timeout callback (trusted internal code); isTimeoutError requires a real `Error` with name 'TimeoutError'; the worst outcome of a spoof is one bounded retry under the retryCount cap |
| T-03-16-04 | Info Disclosure | carrier propagation | low | mitigate | the TimeoutError carrier carries ONLY timeoutMs (R-10, T-03-11-03 — never prompt bodies or keys); the recovery path rethrows the existing carrier, adding no new payload |

No new network surface, no new storage, no new secrets, no new dependencies (approved stack §7 untouched).
</threat_model>

<verification>
- `./node_modules/.bin/vitest run tests/core/ai/StructuredOutput.test.ts tests/core/ai/ProviderRouter.test.ts tests/core/ai/StructuredOutput.timeoutRetry.test.ts` — exits 0
- `./node_modules/.bin/vitest run tests/core/ai/AgentOrchestrator.budget.test.ts` — exits 0 (the D-17 retry + R-2 budget accounting must not regress — the retried call still consumes retryCount exactly once)
- `./node_modules/.bin/vitest run tests/core/ai/RendererService.test.ts tests/core/ai/RendererService.streamBreakdown.test.ts` — exits 0 (03-15's suites stay green — this plan touches the same ProviderRouter.ts file)
- `./node_modules/.bin/tsc --noEmit` — exits 0
- Grep proof: `sed -n '94,97p' src/core/ai/StructuredOutput.ts | grep "timeoutError"` shows the carrier abort; `grep -n "isTimeoutError(signal.reason)" src/core/ai/ProviderRouter.ts` shows the closure recovery
- Full gate (if the pnpm wrapper is healthy in the executor environment): `pnpm run verify:phase-3` stays green; otherwise run its components via the direct binaries above
</verification>

<success_criteria>
- A production timeout (25 ms, abort-on-signal SDK) fires the D-17 retry exactly once on a fresh non-aborted signal — generateObject 2×, retryCount 1, ledger attempts[0].errorCode TIMEOUT
- The `planner_failed` fallback still surfaces when the retry also fails (requestJson rejects with the TimeoutError carrier — never a silent idle, never a re-invocation)
- The unit test reproduces the production arrival pattern; the end-to-end regression pins the contract
- User aborts still classify UNKNOWN/never-retried (existing tests untouched and green)
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-16-SUMMARY.md` when done
</output>
