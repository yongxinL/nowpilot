---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 14
type: execute
wave: 7
depends_on: ["03-08"]
files_modified:
  - src/components/pages/ChatPage.tsx
  - tests/components/pages/ChatPage.test.tsx
autonomous: true
gap_closure: true
requirements: [AI-03, AI-05, AI-06]
must_haves:
  truths:
    - "Retry re-sends the last user input through the same runAgentTurn path with a NEW operationId"
  artifacts:
    - "src/components/pages/ChatPage.tsx — the Retry footer renders ONLY on the LATEST assistant bubble (m.id === messages[messages.length - 1]?.id); handleRetry guards to the last message"
    - "tests/components/pages/ChatPage.test.tsx — regression: after a failure + a new completed send, the OLD failed bubble has NO Retry footer; Retry on the latest bubble does not wipe the newest message"
  key_links:
    - "ChatPage footer (L129-146) ↔ handleRetry (L77-85) ↔ useStreamingLLM.retry() (re-sends lastUserInputRef with a NEW operationId, useStreamingLLM.ts L197-200)"
    - "Gate condition: `m.id === messages[messages.length - 1]?.id && (m.status === 'failed' || m.status === 'offline')` — UI-SPEC failed-row semantics"
  flagged_assumptions:
    - "AI-06 [probe unclassified]: RICH chat polish (Welcome/Prompts/clarification chips/action panels) stays fenced to Phase 7 (D-03); the Phase-3 surface is Bubble/Bubble.List + Sender only — the retry fix touches none of the fence"
    - "AI-05 [probe: merge/collide/equality — persona override semantics]: resolvePersona merges DEFAULT_PERSONA with prefs.personaOverrides per-field (prefs win); empty overrides → DEFAULT_PERSONA (PersonaProfile.ts); buildPersonaBlock is a fixed-order byte-stable template (N.2) — no merge/collide ambiguity exists because persona is a per-field override, not a set operation. Verified-satisfied in the executed phase; no gap task touches it"
    - "AI-03 [probe: empty/single-element/null input]: empty user input is trimmed and rejected at handleSend (ChatPage L57-58); a single message renders normally; null never reaches the list (state is typed ChatMessage[])"
---
<objective>
Close VERIFICATION.md gap 5 (WR-04, partial): `ChatPage.handleRetry` always operates on `prev[prev.length - 1]` and re-sends the newest input, while the footer renders a Retry button on EVERY failed/offline bubble. After a failure the Sender re-enables; sending a new message then clicking Retry on an OLDER failed bubble wipes the NEWEST assistant message and re-runs the newest input — the wrong turn is retried and the current answer is destroyed.

Purpose: implement the 03-REVIEW.md WR-04 fix — only offer/execute retry on the latest assistant bubble. Gate the footer on `m.id === messages[messages.length - 1]?.id` (per the UI-SPEC failed-row semantics: Retry is a recovery action for the current turn, not a history control), and keep handleRetry's last-message guard so the retry always re-sends the correct (latest) input through `useStreamingLLM.retry()` with a NEW operationId.

Output: gated Retry footer + regression tests in ChatPage.test.tsx.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-VERIFICATION.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-REVIEW.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-08-SUMMARY.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-UI-SPEC.md
@src/components/pages/ChatPage.tsx
@src/components/pages/useStreamingLLM.ts
@tests/components/pages/ChatPage.test.tsx
</context>

<assumption_delta_decision>
- Noun that stays primary: **ProviderId** (four-ID enum, D-07/D-12). OpenAI-compatible baseURL remains a config variant of `openai`, NOT a 5th id.
- Decision: **no-change** — the D-17 fallback chain and D-12 custom-baseURL variant were decided at discuss-phase; this replan only fixes their buggy implementations (WR-04 retry targeting). It does NOT pluralize the identity model and does NOT change resolveTier/fallback semantics.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: WR-04 — scope the Retry footer to the latest assistant bubble</name>
  <files>src/components/pages/ChatPage.tsx</files>
  <read_first>
    - src/components/pages/ChatPage.tsx L77-85 (handleRetry — operates on prev[prev.length-1]; keep its last-message guard), L107-149 (items useMemo — the DEFECTIVE footer at L129-146 renders Retry on EVERY failed/offline bubble), L44-50 (state/hook wiring)
    - 03-REVIEW.md WR-04 (L163-183) — the fix sketch with the footer gate
    - 03-PATTERNS.md WR-04 (L223-254) — the fix sketch + planner note (retry() re-sends lastUserInputRef with a NEW operationId — UI contract is 'Retry only on the latest failed/offline assistant bubble')
    - src/components/pages/useStreamingLLM.ts L197-200 (retry — the hook-side contract, unchanged)
    - 03-UI-SPEC.md L226 "## UI Considerations" (failed-row Retry semantics — recovery for the current turn, not history)
  </read_first>
  <action>
    In `ChatPage.tsx`, gate the Retry footer inside the `items` useMemo (L129-146):
    1. Change the footer condition from `m.status === 'failed' || m.status === 'offline' ? (...)` to:
       `m.id === messages[messages.length - 1]?.id && (m.status === 'failed' || m.status === 'offline') ? (...) : undefined`
    2. Keep the entire footer JSX (FAILED_PREFIX text, Retry Button with onClick={handleRetry}, offline omission of the error prefix) unchanged — only the condition gains the latest-bubble gate.
    3. Keep `handleRetry` (L77-85) as-is — it already targets `prev[prev.length - 1]` with a `role !== 'assistant'` guard, which is now CORRECT because the footer only renders on the latest bubble; add a one-line comment noting the footer gate makes last-message semantics sound.
    4. Update the `items` useMemo dependency array if needed (messages is already a dependency; verify no new deps are required).
    5. Do NOT change: handleSend, the 5-state effect (L90-105), Bubble.List composition, Sender, FAILED_PREFIX, STR keys, or the streaming caret.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/pages/ChatPage.test.tsx --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/pages/ChatPage.tsx` footer condition contains `m.id === messages[messages.length - 1]?.id && (m.status === 'failed' || m.status === 'offline')`
    - The Retry Button render is inside that gated branch (grep the footer block: the `STR.chat.retry` Button is only reachable when the gate holds)
    - `pnpm vitest run tests/components/pages/ChatPage.test.tsx --passWithNoTests` exits 0 (existing suite's single-failed-bubble tests still pass — a lone failed bubble IS the latest)
  </acceptance_criteria>
  <done>Retry is offered/executed only for the latest failed/offline assistant bubble; older bubbles no longer expose the destructive action.</done>
</task>

<task type="auto">
  <name>Task 2: WR-04 — regression tests (stale-bubble retry cannot wipe the newest message)</name>
  <files>tests/components/pages/ChatPage.test.tsx</files>
  <read_first>
    - tests/components/pages/ChatPage.test.tsx L50-60 (hookMock vi.hoisted), L62-77 (setStream/forceUpdate helpers), L130-160 (existing failed/retry tests — the base for the new cases), L21-41 (IntersectionObserver stubs)
    - 03-PATTERNS.md "WR-04 — extend tests" (L350-351)
  </read_first>
  <action>
    Extend `tests/components/pages/ChatPage.test.tsx`:
    1. New test 'Retry is NOT offered on a stale failed bubble after a newer message': render → `setStream('failed', 'partial')` → forceUpdate (failed bubble A) → send a new message via the Sender path (fireEvent on the Sender input + submit, or drive `handleSend` through the Sender onSubmit) → `setStream('completed', 'new answer')` → forceUpdate. Assert: exactly ONE 'Provider error.' prefix / retry-button set total OR the OLD bubble A's footer does not render `STR.chat.retry`; the newest assistant bubble has the new answer text intact (`screen.getByText('new answer')`).
    2. New test 'clicking Retry on the latest failed bubble still works': render → fail → forceUpdate → assert `STR.chat.retry` IS present (latest = the failed bubble) → click it → `hookMock.retry` called once.
    3. New test 'Retry on the latest bubble replaces only the latest content': fail turn 1, send new message (streaming), then make the NEW turn fail → forceUpdate → assert there are TWO bubbles and the footer (Retry button) appears exactly once (on the latest), and clicking it does not wipe the first answer.
    4. Keep all existing tests (single-failed-bubble, offline) passing unmodified — a lone failed bubble is the latest, so the gate does not change their outcome.
  </action>
  <verify>
    <automated>pnpm vitest run tests/components/pages/ChatPage.test.tsx --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - The stale-bubble test asserts the OLD failed bubble renders NO Retry footer after a newer completed send (count of `STR.chat.retry` buttons == 0 when the latest is completed, or per-bubble assertions)
    - The latest-failed-bubble test asserts `STR.chat.retry` present and `hookMock.retry` called on click
    - `pnpm vitest run tests/components/pages/ChatPage.test.tsx --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>Retry targeting is pinned: stale bubbles are inert, the latest failed bubble still offers recovery, and the newest answer is never wiped.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Details |
|----------|------|---------|
| `ChatPage.tsx` footer condition | modified | `m.id === messages[messages.length - 1]?.id && (m.status === 'failed' || m.status === 'offline')` gate on the Retry footer |
| `ChatPage.tsx` handleRetry comment | modified | documents why last-message semantics are now sound (footer gate) |
| `tests/components/pages/ChatPage.test.tsx` | updated | 3 new regression tests: stale-bubble no-retry, latest-bubble retry works, latest-replaces-only-latest |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChatPage UI → useStreamingLLM.retry | a user click crosses here; the retry re-sends the latest input through runAgentTurn |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14-01 | Tampering | Retry footer / handleRetry | medium | mitigate | footer gated to the latest assistant bubble — a stale-bubble click can no longer wipe the newest message or re-run the wrong input; handleRetry keeps the `role !== 'assistant'` guard; regression tests pin both |
| T-03-14-02 | Info Disclosure | ChatPage message content rendering | low | accept | unchanged from the executed plan — plain-text Bubble rendering, no HTML injection (T-03-08-02 already mitigates; the retry fix adds no new rendering path) |
| T-03-14-03 | DoS | rapid retry clicks | low | accept | `if (isStreaming) return` guard in handleRetry (L78) already prevents re-entry mid-stream; one stream per session (§17.5) |

Client-side UI fix only — no secrets, no network surface, no storage changes.
</threat_model>

<verification>
- `pnpm vitest run tests/components/pages/ChatPage.test.tsx --passWithNoTests`
- `pnpm vitest run tests/components/pages/useStreamingLLM.test.tsx --passWithNoTests` (hook retry contract unchanged)
- `pnpm run verify:phase-3` (full gate) must stay green
</verification>

<success_criteria>
- Retry renders only on the latest failed/offline assistant bubble
- Clicking Retry re-sends the latest user input with a NEW operationId — never a stale turn's input
- The newest assistant message is never wiped by a stale-bubble Retry
- Full `verify:phase-3` gate green
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-14-SUMMARY.md` when done
</output>
