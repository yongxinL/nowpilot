---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
- src/core/ai/StructuredOutput.ts
- src/core/ai/PlannerService.ts
- src/core/ai/ExecutorService.ts
- tests/core/ai/StructuredOutput.test.ts
- tests/core/ai/PlannerService.test.ts
- tests/core/ai/ExecutorService.test.ts
autonomous: true
requirements: [AI-02]

<!-- FINAL v2 (2026-08-10): + ordered Tasks section. P-1/F-4 — requestJson & callProviderJsonMode take
     PromptSection[] (not a joined string); the one repair PRESERVES the cached [SYSTEM] sections byte-stable
     (appends the repair as a user_input PromptSection). P-3 — PromptSection from '@/core/ai/types'. -->

### must_haves:
truths:
- "StructuredOutput.requestJson (Appendix L verbatim, F-4 signature) takes `(schema, sections: PromptSection[], ctx)` and performs exactly ONE repair: first safeParse fail → keep the cached sections byte-stable and APPEND the repair as a single `{ kind:'user_input', text: PROMPTS.repairJson.system + 'Schema:…' + 'Broken:…', stable:false }` PromptSection → second safeParse fail → throw STRUCTURED_OUTPUT_FAILED { retryable: false, raw: { first, second } } — never a third attempt, never a joined-string rebuild, never hand-parsed JSON (Golden Rule 4, D-19)."
- "The Seam-2 callback signature is `callProviderJsonMode: (sections: PromptSection[], jsonSchema: unknown, signal: AbortSignal) => Promise<string>` (F-4) — StructuredOutput threads sections through unchanged; the Router-supplied closure (03-05) maps cached kinds (system/tool_schemas/preferences/memory) → provider `system` and task kinds (context/task/user_input) → `prompt`. StructuredOutput NEVER string-splits (no `prompt.split('\\n\\n')`)."
- "`PromptSection` is imported from `@/core/ai/types` (D-07 canonical home) — NOT from '../context/ContextOptimizer'; StructuredOutput.ts adds no second declaration (R-1)."
- "PlannerDecisionSchema is a closed z.discriminatedUnion('action', …) with answer | run_tool | ask_clarification; when zero tools are registered the run_tool branch is OMITTED (D-05) and a stray run_tool is rejected with TOOL_REJECTED."
- "PlannerService.plan stays pure (D-19): it calls `requestJson(PlannerDecisionSchema, context.sections, ctx)` where `context` is the OptimizedContext (D-02/D-07 shape) and `ctx.callProviderJsonMode` is supplied by the caller (Router-constructed) — PlannerService never imports ProviderRouter and never assembles or joins a prompt string (Golden Rule 3)."
- "ExecutorService is deterministic (R-4): it validates toolName against the closed z.enum, rejects unknowns with TOOL_REJECTED, runs get-provider-info via ProviderRegistry, and returns ToolExecutionResult { toolName, ok, output | error, durationMs } — no SDK tool machinery."
- "requestJson re-parents abortSignal into a per-attempt AbortController with timeoutMs; the outer abortSignal abort propagates (Appendix L)."
artifacts:
- src/core/ai/StructuredOutput.ts
- src/core/ai/PlannerService.ts
- src/core/ai/ExecutorService.ts
- tests/core/ai/{StructuredOutput,PlannerService,ExecutorService}.test.ts
key_links:
- "ctx.callProviderJsonMode is the Router-constructed Seam-2 callback (03-05) with the F-4 `(sections: PromptSection[], …)` signature — consumers stay pure (D-18/D-19)."
- "PlannerService passes `context.sections` (PromptSection[] from the contextHelper-built OptimizedContext, 03-07) into requestJson — no React-side or service-side prompt joining."
- "ExecutorService.get-provider-info reads ProviderRegistry presence (03-02) — the only Phase-3 tool."
- "PlannerDecisionSchema's run_tool branch is driven by buildToolNameEnum (03-03)."
flagged_assumptions:
- "AI-02 [concurrency]: a single runAgentTurn executes sequentially on one surface; no parallel planner/executor calls in Phase 3 (D-14 per-surface in-memory state; the loop is the only caller)."
- "AI-04 [idempotency]: requestJson is intentionally not idempotent — every call is a fresh paid model call (planner + repair max 2); idempotency is a Phase-6 ledger concern (D-16), not a retry-safety mechanism here."
- "F-4 [repair section kind]: the appended repair section uses kind 'user_input' (a task kind) so it maps to the provider `prompt`, never the cached `system` — the cached [SYSTEM] stays byte-identical across attempt 1 and the repair (prompt-cache stability, §1.3)."
prohibitions:
- "No hand-parsing JSON with regex (Golden Rule 4)."
- "No double-repair / nested repair (D-19, R-2)."
- "No joined-string prompt or `prompt.split()` [SYSTEM] recovery (F-4 — sections in, mapping owned by the Router closure)."
- "No `PromptSection` import from '../context/ContextOptimizer' or a local re-declaration (P-3/D-07 — import from '@/core/ai/types')."
- "No SDK tool()/tools/maxSteps (R-4 — generateObject has no tools param in ai@4)."
- "No unvalidated toolName reaching execution (TOOL_REJECTED boundary)."
- "No free-form error strings (Golden Rule 9 — canonical codes only)."

Purpose: Every planner decision must be Zod-validated before anything executes; the Executor must reject anything outside the closed tool enum (D-04/D-05). The AI-SPEC's "Decision validity" eval dimension (Critical) is proven entirely by this plan's tests. F-4 makes the structured-output seam section-based so the cached [SYSTEM] block is never mis-sliced and stays cache-stable across the repair.
Output: Three service modules + their test files (first fail → repair → success / second fail → STRUCTURED_OUTPUT_FAILED; unknown tool → TOOL_REJECTED; F-4 sections-in signature + byte-stable repair asserted).
<execution_context>@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md</execution_context>

### Tasks (ordered — do not reorder; each maps to a truth/artifact)
1. **StructuredOutput.ts (new).** Implement Appendix L verbatim with the F-4 signature: `StructuredOutputContext.callProviderJsonMode: (sections: PromptSection[], jsonSchema, signal) => Promise<string>`; `requestJson(schema, sections: PromptSection[], ctx)`. Import PromptSection from '@/core/ai/types'.
2. **Repair path (byte-stable, F-4).** On first safeParse fail: `cached = sections.filter(s => !TASK_KINDS.includes(s.kind))`; append `{ kind:'user_input', text: PROMPTS.repairJson.system + '\nSchema: ' + JSON.stringify(jsonSchema) + '\nBroken: ' + first, tokens: Math.ceil(len/4), stable:false, sourceId:'structured-output-repair' }` (VERBATIM the spec Appendix L F-4 repairText — `\nSchema: ${JSON.stringify(jsonSchema)}\nBroken: ${first}`, never a paraphrase or dropped newline); re-attempt with `[...cached, repairSection]`; second fail → throw STRUCTURED_OUTPUT_FAILED {retryable:false, raw:{first,second}}. NO joined-string rebuild, NO prompt.split.
3. **Abort re-parenting.** Per-attempt AbortController with ctx.timeoutMs; outer abortSignal propagates (Appendix L). Add isStructuredOutputFailed() guard.
4. **PlannerService.ts (new).** buildPlannerDecisionSchema() (closed discriminatedUnion; run_tool omitted when buildToolNameEnum → null, D-05); PlanInput carries the OptimizedContext; `plan()` calls `requestJson(schema, context.sections, ctx)`. Never import ProviderRouter; never join a prompt (Golden Rule 3).
5. **ExecutorService.ts (new).** execute(): validate toolName against the closed z.enum → TOOL_REJECTED on unknown; run get-provider-info via ProviderRegistry (03-02); return ToolExecutionResult. No SDK tool machinery (R-4).
6. **StructuredOutput.test.ts (new).** Assert: sections-in signature; the repair appends a user_input section AND the cached-section text is byte-identical attempt-1 vs repair (hash-equality); one-repair-max (call count = 2); STRUCTURED_OUTPUT_FAILED shape.
7. **PlannerService.test.ts + ExecutorService.test.ts (new).** first-fail→repair→success; second-fail→STRUCTURED_OUTPUT_FAILED; zero-tools→run_tool omitted; stray run_tool→TOOL_REJECTED; get-provider-info runs.
8. **Verify.** grep no `prompt.split(`; grep PromptSection import = '@/core/ai/types'; test:ai green.

### Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-02 [concurrency] and AI-04 [idempotency for requestJson] — recorded in must_haves.flagged_assumptions. All other edges stay with their owners (never silently dropped).

### Artifacts This Phase Produces
- src/core/ai/StructuredOutput.ts (new): StructuredOutputContext (F-4 `callProviderJsonMode: (sections: PromptSection[], …)`), requestJson(schema, sections, ctx) with section-preserving one-repair, isStructuredOutputFailed(); imports PromptSection from '@/core/ai/types' (D-07).
- src/core/ai/PlannerService.ts (new): buildPlannerDecisionSchema(), PlanInput (carries the OptimizedContext), PlannerService.plan() calling requestJson(schema, context.sections, ctx), PlannerDecision type.
- src/core/ai/ExecutorService.ts (new): ExecuteInput, ExecutorService.execute(), ToolExecutionResult production.
- tests/core/ai/StructuredOutput.test.ts (new): sections-in signature; repair appends a user_input section and leaves cached-section text byte-identical; one-repair-max; failure shape.
- tests/core/ai/PlannerService.test.ts, tests/core/ai/ExecutorService.test.ts (new).
<threat_model>

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model output → decision | Raw model text crosses here; only Zod-validated decisions pass (Golden Rule 4) |
| Planner decision → tool execution | Unvalidated toolName must never reach execution; the closed enum + TOOL_REJECTED are the gate |
| Sections → provider call | The Router closure maps PromptSection kinds → system/prompt (F-4); StructuredOutput never joins or splits strings |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-01 | Tampering / Elevation | requestJson → PlannerDecision | critical | mitigate | One Zod-validated decision, exactly one repair, then STRUCTURED_OUTPUT_FAILED — never hand-parsed, never double-repaired (Golden Rule 4, D-19); test-asserted repair count = 2 max |
| T-03-04-02 | Tampering | F-4 cached-section stability | high | mitigate | Repair appends a task-kind (user_input) section only; cached (system/tool_schemas/preferences/memory) section text is byte-identical across attempt 1 and repair (hash-equality test) — a joined-string rebuild would silently drift the cached prefix and kill the provider prompt cache (Pitfall 5, §1.3) |
| T-03-04-03 | Elevation | ExecutorService toolName | critical | mitigate | Closed z.enum via buildToolNameEnum; zero-tools → run_tool branch omitted AND stray run_tool → TOOL_REJECTED (D-05); input-schema + dangerous-flag validation before any run; deterministic (R-4) |
| T-03-04-04 | DoS | requestJson per-attempt timeout | medium | mitigate | Per-attempt AbortController with ctx.timeoutMs (planner 3s §1.2) + outer-abort re-parenting (Appendix L) |
| T-03-04-05 | Information Disclosure | debugLog on failures | medium | mitigate | debugLog auto-routes through TraceRedactor (R-10); codes + module, never raw model output bodies |
| T-03-04-SC | Tampering | pnpm installs | high | mitigate | No new packages this plan |
</threat_model>
<success_criteria>
- Decision validity proven end-to-end: one repair max, closed enum, TOOL_REJECTED on stray run_tool, get-provider-info runs.
- F-4 proven: requestJson takes sections; the repair leaves the cached [SYSTEM] section byte-identical (hash-equality); no `prompt.split`/joined-string path; PromptSection imported from '@/core/ai/types'.
- All three §18 test files for these services green.
</success_criteria>
