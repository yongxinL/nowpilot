---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/core/ai/ChunkBuffer.ts
  - src/core/ai/PromptCacheAdapter.ts
  - src/core/ai/PromptCacheManager.ts
  - src/core/ai/toolSchemas.ts
  - src/core/ai/ExecutorService.ts
  - src/core/ai/RendererService.ts
  - src/core/runtime/workerState.ts
  - tests/core/ai/ExecutorService.test.ts
  - tests/core/ai/RendererService.test.ts
autonomous: true
requirements: [RICH-R-02, RICH-R-10]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PromptCacheManager.buildSystemPrompt(stage, opts) is the ONLY system-prompt assembly point in the phase — it calls PersonaInjector.inject and returns the persona-first, byte-stable [SYSTEM] (D-59 single choke-point, RICH-R-02/RICH-R-10)"
    - "PromptCacheAdapter.applyCacheHints implements Appendix K verbatim: anthropic cache_control ephemeral on ≤4 stable sections (ANTHROPIC_MAX_BREAKPOINTS — 5th → 400), gemini cachedContent only when stableTokens ≥ 32768 (GEMINI_MIN_CACHED_TOKENS) else prefix-only, openai/ollama stableFirst prefix-only; hashStableSections = FNV-1a 32-bit"
    - "ExecutorService rejects every direct or test-injected run_tool decision because Phase 3 registers zero tools, returning TOOL_REJECTED (D-46, §21.6). The production Planner schema excludes run_tool while the registry is empty. Do not construct z.enum([]); use the zero-tool specialization defined by 03-01."
    - "RendererService.render caps output at 512 tokens by default (§1.3) with a per-feature override param; it never invents facts"
    - "ActiveStreamState (§20.6) exists in src/core/runtime/workerState.ts — the canonical stream events map onto it (STREAM_START→preparing/streaming, STREAM_COMPLETE→completed, STREAM_ERROR→failed, STREAM_ABORTED→aborting)"
  artifacts:
    - path: "src/core/ai/ChunkBuffer.ts"
      provides: "Appendix J createChunkBuffer: enqueue/onFlush/flushNow/reset, rAF batching with 8 kB/s → 33 ms upgrade rule"
      exports: ["createChunkBuffer", "ChunkBuffer"]
    - path: "src/core/ai/PromptCacheAdapter.ts"
      provides: "Appendix K applyCacheHints(providerId, sections) + hashStableSections (FNV-1a 32-bit)"
      exports: ["applyCacheHints", "hashStableSections", "ANTHROPIC_MAX_BREAKPOINTS", "GEMINI_MIN_CACHED_TOKENS"]
    - path: "src/core/ai/PromptCacheManager.ts"
      provides: "§1.3 buildSystemPrompt(stage, {prefs?, persona?}): [SYSTEM: cached, canonical] persona-first via PersonaInjector (D-59); stable/unstable section tagging; 5-consecutive-miss → 60s disable rule; profile-version-keyed cache hash"
      exports: ["PromptCacheManager", "buildSystemPrompt", "CACHE_DISABLE_MISS_THRESHOLD"]
    - path: "src/core/ai/toolSchemas.ts"
      provides: "D-46: ToolDefinition, ToolCapabilityManifest, closed-enum generation contract; ZERO tools registered"
      exports: ["ToolDefinition", "ToolCapabilityManifest", "RegisteredToolNameSchema"]
    - path: "src/core/ai/ExecutorService.ts"
      provides: "§1.2 execute: closed z.enum from registered tools, input validation, TOOL_REJECTED for unknown toolName"
      exports: ["ExecutorService", "execute", "TOOL_REJECTED"]
    - path: "src/core/ai/RendererService.ts"
      provides: "§1.2/§1.3 render: streams final answer (fast tier), 512-token cap default + override, no invented facts"
      exports: ["RendererService", "render", "DEFAULT_MAX_OUTPUT_TOKENS"]
    - path: "src/core/runtime/workerState.ts"
      provides: "ADD ActiveStreamState (§20.6) — discriminated union with ActiveSurface (imported from WorkspaceStore)"
      exports: ["ActiveStreamState"]
  key_links:
    - from: "src/core/ai/PromptCacheManager.ts"
      to: "src/core/ai/persona/PersonaInjector.ts"
      via: "buildSystemPrompt is the single call site of PersonaInjector.inject (D-59)"
      pattern: "PersonaInjector\\.inject"
    - from: "src/core/ai/PromptCacheManager.ts"
      to: "src/core/ai/PromptCacheAdapter.ts"
      via: "stable sections hashed with hashStableSections for the cache key; applyCacheHints drives per-provider [SYSTEM] caching"
      pattern: "hashStableSections|applyCacheHints"
    - from: "src/core/ai/ExecutorService.ts"
      to: "src/core/ai/toolSchemas.ts"
      via: "the closed toolName enum is generated from the tool registry at request time"
      pattern: "RegisteredToolNameSchema|z\\.enum"
  prohibitions:
    - "No chrome.storage writes inside the ChunkBuffer flush path (P2 write-rate — mid-stream chunks live in memory only, D-45)"
    - "Stages never assemble their own system prompts — the PromptCacheManager choke-point is the only assembly path (D-59)"
    - "No invented error codes — ExecutorService uses only §21.6 codes (TOOL_REJECTED); Renderer surfaces canonical failures"
flagged_assumptions:
  - "RICH-R-09 (unclassified probe): chat/agent persona sharing is guaranteed BY CONSTRUCTION — every stage system prompt in the phase comes from buildSystemPrompt (one choke-point); the AgentOrchestrator test (plan 03-06) asserts the end-to-end result"
  - "RICH-R-10 (unclassified probe): per-stage prompts = byte-stable persona block + stage-specific canonical Appendix A string; stage strings are persona-free (plan 03-01); cache-key re-derivation on override change = profile-version hash (hashStableSections over the persona block)"
  - "A8 (PromptSection): the {kind, text, stable, tokens} field contract declared in types.ts (plan 03-01) is consumed here — Phase-5 ContextOptimizer adopts the same shape"
---

<objective>
Build the prompt-assembly layer (PromptCacheManager — the D-59 single choke-point where PersonaInjector is called; PromptCacheAdapter — Appendix K cache hints) and the remaining pipeline stages: toolSchemas (D-46 declaration), ExecutorService (closed-enum TOOL_REJECTED), RendererService (512-token cap). Add the §20.6 ActiveStreamState machine to workerState.ts and the Appendix J ChunkBuffer that streams render output to the UI.

Purpose: With this plan, every AI call in the phase gets a persona-consistent, byte-stable, cache-hinted system prompt through exactly one code path, and the Executor/Renderer stages enforce the DONE-when contracts (TOOL_REJECTED, output caps).
Output: 6 new modules + workerState.ts addition + ExecutorService.test.ts / RendererService.test.ts.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRODUCT_SPEC_v0_1.md  # §1.2 (Executor/Renderer rules 290-330), §1.3 (section order + 512-cap note), §20.6 (3170-3179), §21.6 (closed codes), Appendix J (5630-5678), Appendix K (5753-5821)
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md  # Pattern 2, Pitfalls 3/4/5, Open Q4 (512-cap mechanism), Open Q5 (cache invalidation)
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md  # ChunkBuffer/PromptCacheAdapter/PromptCacheManager/toolSchemas/ExecutorService/RendererService sections
@src/core/runtime/workerState.ts  # existing BackgroundWorkerState — ADD ActiveStreamState alongside (do not modify existing exports)
@src/core/workspace/WorkspaceStore.ts  # ActiveSurface export (line 11)
@src/core/registry/Registry.ts  # register/get/getAll pattern for toolSchemas
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChunkBuffer (Appendix J) + PromptCacheAdapter (Appendix K) + ActiveStreamState (§20.6)</name>
  <files>src/core/ai/ChunkBuffer.ts, src/core/ai/PromptCacheAdapter.ts, src/core/runtime/workerState.ts</files>
  <read_first>@src/core/runtime/workerState.ts (file being modified — existing BackgroundWorkerState; ADD ActiveStreamState alongside) · @src/core/workspace/WorkspaceStore.ts line 11 (ActiveSurface export) · @.planning/PRODUCT_SPEC_v0_1.md Appendix J (5630-5678), Appendix K (5753-5821), §20.6 (3170-3179) — verbatim sources · 03-RESEARCH.md Pitfalls 4/5 (Gemini minimum, Anthropic 4-breakpoint)</read_first>
  <action>Create `src/core/ai/ChunkBuffer.ts` per Appendix J verbatim (spec 5630-5678): `createChunkBuffer` with enqueue/onFlush/flushNow/reset; rAF batching with the 8_000 bytes/s → 33 ms setTimeout upgrade rule — no chrome.storage access anywhere in this module (P2 write-rate prohibition; mid-stream chunks live in memory only, D-45). Create `src/core/ai/PromptCacheAdapter.ts` per Appendix K verbatim (spec 5753-5821): `applyCacheHints(providerId, sections)` — anthropic: cache_control {type:'ephemeral'} on at most 4 stable sections (export `ANTHROPIC_MAX_BREAKPOINTS = 4`; a 5th breakpoint → 400, Pitfall 5); gemini: cachedContent split ONLY when stableTokens >= 32768 (export `GEMINI_MIN_CACHED_TOKENS = 32_768`), else inline + prefix-only (Pitfall 4); openai/ollama/default: stableFirst sort + prefix-only. Export `hashStableSections(sections)` = FNV-1a 32-bit over joined stable texts (verbatim). Consume the `PromptSection {kind, text, stable, tokens}` shape from src/core/ai/types.ts (A8). Modify `src/core/runtime/workerState.ts`: ADD the `ActiveStreamState` discriminated union verbatim from §20.6 (spec 3170-3179 — idle / preparing / streaming / waiting-for-permission / aborting / completed / failed), importing `ActiveSurface` from `../workspace/WorkspaceStore` (exported at line 11); do NOT alter the existing BackgroundWorkerState exports. Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>tsc passes; ChunkBuffer has zero chrome.storage references (grep-assertable); PromptCacheAdapter exports ANTHROPIC_MAX_BREAKPOINTS=4 and GEMINI_MIN_CACHED_TOKENS=32768 with the verbatim branch logic; hashStableSections is FNV-1a 32-bit; ActiveStreamState compiles in workerState.ts importing ActiveSurface; existing workerState exports unchanged.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>ChunkBuffer implements the Appendix J batching rules; PromptCacheAdapter enforces the 4-breakpoint / 32768-token limits verbatim; ActiveStreamState compiles in workerState.ts with ActiveSurface imported.</done>
</task>

<task type="auto">
  <name>Task 2: PromptCacheManager (D-59 choke-point) + toolSchemas (D-46) + ExecutorService (TOOL_REJECTED)</name>
  <files>src/core/ai/PromptCacheManager.ts, src/core/ai/toolSchemas.ts, src/core/ai/ExecutorService.ts, tests/core/ai/ExecutorService.test.ts</files>
  <read_first>@src/core/ai/persona/PersonaInjector.ts (created 03-02 — inject contract) · @src/core/prompts/index.ts (created 03-01 — canonical stage strings) · @src/types/storage.ts lines 46-101 (declare-now full-union analog) · @src/core/registry/Registry.ts (register/getAll pattern) · @.planning/PRODUCT_SPEC_v0_1.md §1.2 (290-330), §1.3 (section order), §21.6 (closed codes) · 03-RESEARCH.md Pattern 2 + Open Q5 (cache invalidation)</read_first>
  <action>Create `src/core/ai/toolSchemas.ts` per D-46 (declare-now/populate-later, mirroring src/types/storage.ts's full-union-declare pattern): `ToolDefinition` (id, name, description, inputSchema, risk/side-effect fields per TOL-01 shape), `ToolCapabilityManifest`, and `RegisteredToolNameSchema` — the closed-enum generation contract built from the registry (z.enum over registered tool names); register ZERO tools in Phase 3 (real tools arrive with owning phases — no fake tools, no governance surface). Create `src/core/ai/PromptCacheManager.ts` — the D-59 single choke-point: `buildSystemPrompt(stage, opts?: { prefs?, persona? })` calls `PersonaInjector.inject(stage, canonicalStageString, {prefs, persona})` where canonicalStageString comes from PROMPTS (plan 03-01) — persona block prepended FIRST inside the cached [SYSTEM] section, byte-stable per persona; assemble the §1.3 canonical section order ([SYSTEM: cached, canonical] → [TOOL SCHEMAS] → [USER PREFERENCES: compact] → [TASK] → [USER INPUT]); tag sections stable/unstable; key the cached [SYSTEM] on the profile-version hash = `hashStableSections([personaBlock])` (Open Q5 — override change → new hash → new byte-stable block, no explicit invalidation API); drive the 5-consecutive-cache-miss → 60 s disable rule (export `CACHE_DISABLE_MISS_THRESHOLD = 5`). Create `src/core/ai/ExecutorService.ts` per §1.2/D-46: `execute(input)` where input carries { toolName, inputData, systemPrompt, provider, abortSignal } — narrow toolName against the closed enum from toolSchemas; unknown toolName → throw/return error with code 'TOOL_REJECTED' (§21.6 closed set — no invented codes, D-38); with zero registered tools every run_tool is rejected; validate input against the tool's inputSchema when a tool exists (future phases). Write `tests/core/ai/ExecutorService.test.ts`: (a) any run_tool → TOOL_REJECTED (zero tools); (b) the rejection is typed (code field present, not a generic Error); (c) the closed enum generation contract is asserted (RegisteredToolNameSchema is a zod enum, currently empty). Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>ExecutorService.test.ts green (3 case groups: TOOL_REJECTED on every run_tool, typed code, empty closed enum); `grep -rn "PersonaInjector.inject" src/ | wc -l` == 1 (the D-59 invariant); zero tools registered (registry empty); tsc passes strict-clean.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>ExecutorService.test.ts green (TOOL_REJECTED on every run_tool); buildSystemPrompt is the only PersonaInjector call site in src/ (grep-verifiable); zero tools registered.</done>
</task>

<task type="auto">
  <name>Task 3: RendererService — 512-token cap + no invented facts</name>
  <files>src/core/ai/RendererService.ts, tests/core/ai/RendererService.test.ts</files>
  <read_first>@src/core/ai/ChunkBuffer.ts (created task 1 — the buffer render feeds) · @src/core/ai/types.ts (canonical StreamEvent) · @.planning/PRODUCT_SPEC_v0_1.md §1.2/§1.3 (renderer rules + 512-cap note, spec ~290-330) · 03-RESEARCH.md Open Q4 (cap mechanism — data, not hard-coded)</read_first>
  <action>Create `src/core/ai/RendererService.ts` per §1.2/§1.3: `render(input)` where input carries { systemPrompt, provider, tier, abortSignal, maxOutputTokens? } — streams the final answer (fast tier per D-55), enforces the "Max normal output: 512 tokens unless the feature overrides" cap (export `DEFAULT_MAX_OUTPUT_TOKENS = 512`; Open Q4 — cap is DATA, an override param on render, not hard-coded in the loop), "no invented facts" (the renderer only relays model output — no synthesis). Consume the canonical stream events via the provider's ILLMProvider.stream; emit events into a ChunkBuffer (plan 03-04 task 1) for UI consumption. Write `tests/core/ai/RendererService.test.ts`: (a) default cap 512 enforced (a fixture stream longer than 512 tokens is truncated); (b) override param raises/lowers the cap; (c) the streamed text equals the model output verbatim (no invented content); (d) abort mid-stream surfaces STREAM_ABORTED and stops. Run `pnpm run verify:phase-3`.</action>
  <acceptance_criteria>RendererService.test.ts green (4 case groups: 512 default cap truncation, override param, verbatim relay, STREAM_ABORTED on abort); DEFAULT_MAX_OUTPUT_TOKENS = 512 is the single cap constant; tsc passes strict-clean.</acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-3</automated>
  </verify>
  <done>RendererService.test.ts green (cap + override + verbatim + abort); DEFAULT_MAX_OUTPUT_TOKENS = 512 is the single cap constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExecutorService ← tool input | run_tool input crosses here — validated against the closed enum + input schema (zero tools in Phase 3 → always rejected) |
| RendererService ← model output | Model text crosses into the UI — capped, relayed verbatim, never synthesized |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-10 | Spoofing | ExecutorService closed enum | low | mitigate | toolName narrowed to the zod closed enum at request time; unknown → TOOL_REJECTED (§21.6) — a tool cannot be spoofed into existence when zero are registered (D-46) |
| T-3-11 | DoS | RendererService streaming | medium | mitigate | 512-token default cap (DEFAULT_MAX_OUTPUT_TOKENS) bounds output; abortSignal threaded; ChunkBuffer rAF/33 ms batching bounds render churn (§22.1); provider call bounded by Requester 25 s default |
| T-3-12 | Tampering | PromptCacheManager section assembly | low | mitigate | Stage strings come from canonical PROMPTS constants (plan 03-01, persona-free); the only variable input is operator-owned personaOverrides (zod-validated, plan 03-02); no host-page data reaches [SYSTEM] in Phase 3 |
| T-3-13 | Information Disclosure | prompt cache key/hash | low | accept | hashStableSections is FNV-1a over stable section text — a non-reversible 32-bit hash of operator-owned prompt text; no secrets can reach [SYSTEM] (apiKey stays in encrypted np_providers) |
</threat_model>

<verification>
- `pnpm run verify:phase-3` green after every task commit.
- Grep: exactly ONE `PersonaInjector.inject` call site in src/ (`grep -rn "PersonaInjector.inject" src/ | wc -l` == 1 — the D-59 invariant).
- Grep: no chrome.storage access in ChunkBuffer.ts (comment-filtered negative gate).
</verification>

<success_criteria>
1. PromptCacheManager is the single system-prompt assembly path — persona applied to every stage (RICH-R-02/RICH-R-10).
2. Appendix K cache hints verbatim — 4-breakpoint / 32768-token limits enforced.
3. Executor rejects unknown tools with TOOL_REJECTED (DONE-when 1); Renderer respects output caps (DONE-when).
4. ActiveStreamState (§20.6) exists; ChunkBuffer batches per Appendix J.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-3 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/ai/ChunkBuffer.ts | createChunkBuffer (Appendix J) | **[this plan]** |
| src/core/ai/PromptCacheAdapter.ts | applyCacheHints, hashStableSections (Appendix K) | **[this plan]** |
| src/core/ai/PromptCacheManager.ts | buildSystemPrompt (D-59 choke-point) | **[this plan]** |
| src/core/ai/toolSchemas.ts | ToolDefinition, ToolCapabilityManifest, zero tools | **[this plan]** |
| src/core/ai/ExecutorService.ts | execute + TOOL_REJECTED | **[this plan]** |
| src/core/ai/RendererService.ts | render + 512-cap | **[this plan]** |
| src/core/runtime/workerState.ts | + ActiveStreamState (§20.6) | **[this plan]** |
| src/core/ai/types.ts / ILLMProvider.ts / StreamAdapter.ts / StructuredOutput.ts / PlannerService.ts / prompts/index.ts | spine (03-01) | 03-01 |
| src/core/ai/persona/*, UserPreferences.ts | persona foundation (consumed here) | 03-02 |
| src/core/ai/providers/*.ts | 5 providers | 03-03 |
| src/core/ai/ProviderRegistry.ts / TierResolver.ts / ProviderRouter.ts | routing stack | 03-05 |
| src/core/ai/AgentOrchestrator.ts | runAgentTurn (consumes stages + PromptCacheManager) | 03-06 |
| src/types/storage.ts / useChatStreaming.ts / OptionsPage.tsx | persistence + chat + options | 03-07 |
| tests/core/ai/** | test files + fixtures/ | all plans |