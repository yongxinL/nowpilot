---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/core/ai/StructuredOutput.ts
  - src/core/ai/PlannerService.ts
  - src/core/ai/ExecutorService.ts
  - tests/core/ai/StructuredOutput.test.ts
  - tests/core/ai/PlannerService.test.ts
  - tests/core/ai/ExecutorService.test.ts
autonomous: true
requirements: [AI-02]

must_haves:
  truths:
    - "StructuredOutput.requestJson (Appendix L verbatim) performs exactly ONE repair: first safeParse fail → PROMPTS.repairJson + 'Schema: <zodToJsonSchema>' + 'Broken: <first>' → second safeParse fail → throw STRUCTURED_OUTPUT_FAILED { retryable: false, raw: { first, second } } — never a third attempt, never hand-parsed JSON (Golden Rule 4)."
    - "PlannerDecisionSchema is a closed z.discriminatedUnion('action', …) with answer | run_tool | ask_clarification; when zero tools are registered the run_tool branch is OMITTED (D-05) and a stray run_tool is rejected with TOOL_REJECTED."
    - "PlannerService.plan stays pure (D-19): it calls requestJson(PlannerDecisionSchema, plannerPrompt, ctx) where ctx.callProviderJsonMode is supplied by the caller (Router-constructed) — PlannerService never imports ProviderRouter."
    - "ExecutorService is deterministic (R-4): it validates toolName against the closed z.enum, rejects unknowns with TOOL_REJECTED, runs get-provider-info via ProviderRegistry, and returns ToolExecutionResult { toolName, ok, output | error, durationMs } — no SDK tool machinery."
    - "requestJson re-parents abortSignal into a per-attempt AbortController with timeoutMs; the outer abortSignal abort propagates (Appendix L)."
  artifacts:
    - src/core/ai/StructuredOutput.ts
    - src/core/ai/PlannerService.ts
    - src/core/ai/ExecutorService.ts
    - tests/core/ai/{StructuredOutput,PlannerService,ExecutorService}.test.ts
  key_links:
    - "ctx.callProviderJsonMode is the Router-constructed Seam-2 callback (03-05) — consumers stay pure (D-18/D-19)."
    - "ExecutorService.get-provider-info reads ProviderRegistry presence (03-02) — the only Phase-3 tool."
    - "PlannerDecisionSchema's run_tool branch is driven by buildToolNameEnum (03-03)."
  flagged_assumptions:
    - "AI-02 [concurrency]: a single runAgentTurn executes sequentially on one surface; no parallel planner/executor calls in Phase 3 (D-14 per-surface in-memory state; the loop is the only caller)."
    - "AI-04 [idempotency]: requestJson is intentionally not idempotent — every call is a fresh paid model call (planner + repair max 2); idempotency is a Phase-6 ledger concern (D-16), not a retry-safety mechanism here."
  prohibitions:
    - "No hand-parsing JSON with regex (Golden Rule 4)."
    - "No double-repair / nested repair (D-19, R-2)."
    - "No SDK tool()/tools/maxSteps (R-4 — generateObject has no tools param in ai@4)."
    - "No unvalidated toolName reaching execution (TOOL_REJECTED boundary)."
    - "No free-form error strings (Golden Rule 9 — canonical codes only)."
---

<objective>
The decision-validity core: Appendix L `requestJson` (Zod + exactly one repair), the closed `PlannerDecisionSchema` + pure `PlannerService`, and the deterministic `ExecutorService` (closed z.enum, get-provider-info). This is AI-02's Planner→Executor half and the enforcement point for Golden Rule 4.

Purpose: Every planner decision must be Zod-validated before anything executes; the Executor must reject anything outside the closed tool enum (D-04/D-05). The AI-SPEC's "Decision validity" eval dimension (Critical) is proven entirely by this plan's tests.
Output: Three service modules + their test files (first fail → repair → success / second fail → STRUCTURED_OUTPUT_FAILED; unknown tool → TOOL_REJECTED).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md
@.planning/PRODUCT_SPEC_v0_1.md
@src/core/ai/types.ts
@src/core/ai/toolSchemas.ts
@src/core/ai/ProviderRegistry.ts
@src/core/prompts/index.ts
@src/core/error/debugLog.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StructuredOutput.requestJson (Appendix L) + test</name>
  <files>src/core/ai/StructuredOutput.ts, tests/core/ai/StructuredOutput.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md Appendix L (line ~5795 — requestJson VERBATIM + Rules)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-RESEARCH.md Pattern 6 (native vs prompt JSON mode, lines 351-354) + AI-SPEC §4b "Structured Outputs with Zod"
    - src/core/ai/types.ts (ProviderId)
    - src/core/prompts/index.ts (PROMPTS.repairJson.system — byte-verbatim, quoted in Appendix L)
  </read_first>
  <behavior>
    - Test 1: with a callProviderJsonMode returning valid JSON first → requestJson returns the parsed data.
    - Test 2: first attempt returns malformed JSON → ONE repair (the callback is called with a repair prompt containing 'Schema:' + the zodToJsonSchema text) → second attempt valid → data returned; callback called exactly 2 times.
    - Test 3: both attempts invalid → throws an error with code === ERROR_CODES.STRUCTURED_OUTPUT_FAILED, retryable === false, raw { first, second } present; callback called exactly 2 times (never a third attempt).
    - Test 4: outer abortSignal aborts mid-attempt → the per-attempt controller aborts (the callback's signal aborts).
  </behavior>
  <action>
    Create src/core/ai/StructuredOutput.ts — Appendix L VERBATIM:
    - `export interface StructuredOutputContext { operationId: string; providerId: ProviderId; model: string; timeoutMs: number; callProviderJsonMode: (prompt: string, jsonSchema: unknown, signal: AbortSignal) => Promise<string>; abortSignal: AbortSignal }`.
    - `export async function requestJson<T>(schema: z.ZodSchema<T>, prompt: string, ctx: StructuredOutputContext): Promise<T>` — verbatim algorithm: zodToJsonSchema(schema); attempt(p) with per-attempt AbortController + ctx.timeoutMs + outer-abort re-parenting; safeParse (fence-strip ```json fences, JSON.parse, schema.safeParse — NEVER regex JSON surgery); repairPrompt = `${PROMPTS.repairJson.system}\nSchema: ${JSON.stringify(jsonSchema)}\nBroken: ${first}`; second attempt; on second failure throw an Error with code STRUCTURED_OUTPUT_FAILED, retryable false, raw { first, second } (typed-error factory + isStructuredOutputFailed() narrowing guard — EncryptedStorage D-03 precedent, PATTERNS Shared Patterns).
    - The adapter sets the provider's JSON mode natively via ctx.callProviderJsonMode (D-18) — requestJson itself never touches the SDK.
    Create tests/core/ai/StructuredOutput.test.ts per the behavior block (node env; callProviderJsonMode is a stub closure; timeoutMs via fake timers).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/StructuredOutput.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass — repair called at most 2×, STRUCTURED_OUTPUT_FAILED thrown with code/retryable/raw, abort propagates.
    - grep -c "STRUCTURED_OUTPUT_FAILED" src/core/ai/StructuredOutput.ts >= 1 (the code, from 03-01).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>One-repair structured output with canonical failure, test-proven (Golden Rule 4).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlannerDecisionSchema + PlannerService + test</name>
  <files>src/core/ai/PlannerService.ts, tests/core/ai/PlannerService.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md §1.2 (Planner rules), Appendix L (requestJson usage), AI-SPEC §4b (PlannerDecisionSchema discriminated union, lines 507-532)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md D-05 (empty registry rule) + D-19 (consumers stay pure)
    - src/core/ai/toolSchemas.ts (buildToolNameEnum, registeredToolNames)
    - src/core/prompts/index.ts (PROMPTS.planner)
  </read_first>
  <behavior>
    - Test 1: plan() with a callProviderJsonMode returning { action: 'answer', reasonCode: 'ok' } → the returned decision passes PlannerDecisionSchema.safeParse and has action 'answer'.
    - Test 2: decision { action: 'ask_clarification', question, options } (options default []) parses with ≤4 options of ≤60 chars.
    - Test 3: when zero tools are registered, the generated schema's safeParse REJECTS { action: 'run_tool' } (branch omitted — the caller surfaces TOOL_REJECTED).
    - Test 4: with get-provider-info registered, { action: 'run_tool', toolName: 'get-provider-info', input: {} } parses.
    - Test 5: malformed decision JSON → the requestJson flow (covered by StructuredOutput tests) → plan() propagates STRUCTURED_OUTPUT_FAILED.
  </behavior>
  <action>
    Create src/core/ai/PlannerService.ts:
    - `export function buildPlannerDecisionSchema(registeredToolNames: readonly string[]): z.ZodDiscriminatedUnion<'action', [...]>` — discriminated union: answer { reasonCode: z.string().max(64) }, run_tool { toolName: z.string().max(64), input: z.unknown() } (branch included ONLY when registeredToolNames.length > 0 — D-05; the toolName member is the closed z.enum from buildToolNameEnum), ask_clarification { question: z.string().max(200), options: z.array(z.string().max(60)).max(4).default([]) }.
    - `export interface PlanInput { operationId: string; userInput: string; context: OptimizedContext; toolResults: ToolExecutionResult<unknown>[]; abortSignal: AbortSignal; callProviderJsonMode: StructuredOutputContext['callProviderJsonMode']; registeredToolNames: string[]; }`
    - `export const PlannerService = { async plan(input: PlanInput): Promise<PlannerDecision>` — composes the planner prompt from the context's stable [SYSTEM] section (the byte-stable persona block, built by contextHelper in 03-07) PREPENDED to PROMPTS.planner.system (persona-first join matching PersonaInjector.inject's N.2 order), then calls requestJson(buildPlannerDecisionSchema(input.registeredToolNames), prompt, { operationId, providerId, model, timeoutMs: 3_000 (§1.2 planner timeout), callProviderJsonMode, abortSignal }). PURE — no ProviderRouter import (D-19); debugLog on the requestJson failure with the canonical code, then rethrow.
    Header documents: the run_tool branch is schema-built per turn (zero tools → omitted); the caller (AgentOrchestrator 03-06) maps a stray run_tool past the schema to TOOL_REJECTED.
    Create tests/core/ai/PlannerService.test.ts per the behavior block (node env; callProviderJsonMode stub closures; buildOptimizedContextFixture from 03-03).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/PlannerService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass (including the zero-tools branch-omission and closed-enum rejection).
    - grep -c "z.discriminatedUnion" src/core/ai/PlannerService.ts == 1.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Closed, per-turn-built planner schema + pure planner service, test-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ExecutorService (deterministic, closed enum) + test</name>
  <files>src/core/ai/ExecutorService.ts, tests/core/ai/ExecutorService.test.ts</files>
  <read_first>
    - .planning/PRODUCT_SPEC_v0_1.md §1.2 (Executor rules: validate name → input schema → permission → capability → timeout → output; ToolExecutionResult), §18 Phase 3 DONE-when ("Executor rejects unknown tools")
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-CONTEXT.md D-04 (get-provider-info) + D-05 (TOOL_REJECTED)
    - src/core/ai/toolSchemas.ts (GET_PROVIDER_INFO_TOOL, buildToolNameEnum)
    - src/core/ai/ProviderRegistry.ts (getProviderInfo from 03-02)
    - .planning/phases/03-cost-effective-ai-runtime-persona-seed/03-PATTERNS.md "ExecutorService" (Registry closed-id validation analog)
  </read_first>
  <behavior>
    - Test 1: execute({ toolName: 'unknown-tool', … }) → result.ok === false, result.error.code === 'TOOL_REJECTED'.
    - Test 2: execute({ toolName: 'get-provider-info', input: {} }) with a registered openai provider → result.ok === true, output.providerId === 'openai', output.models present, output.enabled true.
    - Test 3: input schema validation — a non-object input fails with the schema-invalid error path (not TOOL_REJECTED).
    - Test 4: result shape is ToolExecutionResult { toolName, ok, output|error, durationMs } with durationMs >= 0.
  </behavior>
  <action>
    Create src/core/ai/ExecutorService.ts — deterministic, zero SDK imports (R-4):
    - `export interface ExecuteInput { operationId: string; toolName: string; input: unknown; abortSignal: AbortSignal }`.
    - `export const ExecutorService = { async execute(input: ExecuteInput): Promise<ToolExecutionResult<unknown>> }` — validation chain (verbatim §1.2 order): (1) toolName in the closed z.enum of registered tools (buildToolNameEnum; absent → return { toolName, ok: false, error: { code: 'TOOL_REJECTED', message, retryable: false }, durationMs }) — D-05; (2) input schema validation (JSON-schema check for the tool's jsonSchema); (3) permission/capability (dangerous flag — get-provider-info is dangerous: no); (4) run with timeout (25s max, Requester precedent) honoring abortSignal; (5) output validation → ToolExecutionResult. get-provider-info reads getProviderRegistry().getProviderInfo() (03-02) — never the vault (Pitfall 4; wiring decrypts).
    - Every failure debugLogs a canonical code (TOOL_REJECTED / SCHEMA_INVALID / TIMEOUT / etc. from 03-01) — Golden Rule 9.
    Create tests/core/ai/ExecutorService.test.ts per the behavior block (fakeBrowser for the registry; node env).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/ExecutorService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass — unknown tool → TOOL_REJECTED, get-provider-info returns registry info, schema-invalid ≠ TOOL_REJECTED, ToolExecutionResult shape.
    - grep -c "@ai-sdk\|from 'ai'" src/core/ai/ExecutorService.ts == 0 (deterministic — no SDK).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Deterministic executor with closed-enum rejection + working get-provider-info, test-proven (R-4).</done>
</task>

</tasks>

## Edge Coverage Assumptions (specless probe fallback)

Edges owned by this plan: AI-02 [concurrency] and AI-04 [idempotency for requestJson] — recorded in `must_haves.flagged_assumptions`. All other edges stay with their owners (never silently dropped).

## Artifacts This Phase Produces

- src/core/ai/StructuredOutput.ts (new): `StructuredOutputContext`, `requestJson()`, `isStructuredOutputFailed()`.
- src/core/ai/PlannerService.ts (new): `PlannerDecisionSchema` builder `buildPlannerDecisionSchema()`, `PlanInput`, `PlannerService.plan()`, `PlannerDecision` type.
- src/core/ai/ExecutorService.ts (new): `ExecuteInput`, `ExecutorService.execute()`, `ToolExecutionResult` production.
- tests/core/ai/StructuredOutput.test.ts, tests/core/ai/PlannerService.test.ts, tests/core/ai/ExecutorService.test.ts (new).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model output → decision | Raw model text crosses here; only Zod-validated decisions pass (Golden Rule 4) |
| Planner decision → tool execution | Unvalidated toolName must never reach execution; the closed enum + TOOL_REJECTED are the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-01 | Tampering / Elevation | requestJson → PlannerDecision | critical | mitigate | One Zod-validated decision, exactly one repair, then STRUCTURED_OUTPUT_FAILED { retryable: false } — never hand-parsed, never double-repaired (Golden Rule 4, D-19); test-asserted repair count = 2 max |
| T-03-04-02 | Elevation | ExecutorService toolName | critical | mitigate | Closed z.enum via buildToolNameEnum; zero-tools → run_tool branch omitted from the schema AND stray run_tool → TOOL_REJECTED (D-05); input-schema + dangerous-flag validation before any run; deterministic — no SDK tool machinery (R-4) |
| T-03-04-03 | DoS | requestJson per-attempt timeout | medium | mitigate | Per-attempt AbortController with ctx.timeoutMs (planner 3s §1.2) + outer-abort re-parenting (Appendix L) — a hung provider cannot stall the turn; abortSignal threaded through every call |
| T-03-04-04 | Information Disclosure | debugLog on failures | medium | mitigate | debugLog auto-routes through TraceRedactor (R-10); logs carry codes + module, never raw model output bodies |
| T-03-04-SC | Tampering | pnpm installs | high | mitigate | No new packages this plan |
</threat_model>

<verification>
- `npx vitest run tests/core/ai/StructuredOutput.test.ts tests/core/ai/PlannerService.test.ts tests/core/ai/ExecutorService.test.ts` green.
- `npx tsc --noEmit` green.
- Executor determinism grep: no ai imports in ExecutorService.ts.
</verification>

<success_criteria>
- Decision validity proven end-to-end: one repair max, closed enum, TOOL_REJECTED on stray run_tool, get-provider-info runs.
- All three §18 test files for these services green.
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-04-SUMMARY.md` when done
</output>
