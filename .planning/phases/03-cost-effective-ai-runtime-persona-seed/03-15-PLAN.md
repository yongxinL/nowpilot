---
phase: 03-cost-effective-ai-runtime-persona-seed
plan: 15
type: execute
wave: 9
depends_on: ["03-05", "03-12"]
files_modified:
  - src/core/ai/ProviderRouter.ts
  - src/core/ai/RendererService.ts
  - tests/core/ai/RendererService.test.ts
  - tests/core/ai/RendererService.streamBreakdown.test.ts
autonomous: true
gap_closure: true
requirements: [AI-04]
must_haves:
  truths:
    - "WR-02A closed: a provider failing mid-stream accrues REAL breaker votes — 3 provider-originated streaming failures within 60 s open the breaker (isBreakerOpen(providerId) === true); user aborts never vote"
    - "The 03-12 accounting intent is honored: the catch votes the classifier's mapped code (never a hardcoded STREAM_FAILED double-count), and the no-error non-stop finish keeps STREAM_FAILED with a real vote (BREAKER_VOTES.STREAM_FAILED = 1)"
  artifacts:
    - "src/core/ai/ProviderRouter.ts — BREAKER_VOTES (L186-195) gains STREAM_FAILED: 1; header comment documents the streaming-path vote"
    - "src/core/ai/RendererService.ts — the mid-stream catch (L128-132) classifies the underlying provider error via getProviderRouter().classifyProviderError(err) and votes recordFailure(providerId, cls.code, err); the non-stop finish branch (L146) keeps ERROR_CODES.STREAM_FAILED"
    - "tests/core/ai/RendererService.streamBreakdown.test.ts — NEW permanent real-Router regression (only ai-sdk call sites stubbed): 3 non-stop finishes → isBreakerOpen === true; 3 mid-stream PROVIDER_5XX failures → open; AbortError/user-cancel paths never open"
    - "tests/core/ai/RendererService.test.ts — routerMock gains a classifyProviderError delegate (real classifier); the WR-02 mid-stream vote test asserts the mapped-code flow"
  key_links:
    - "RendererService.render catch → getProviderRouter().classifyProviderError(err) → cls.code → recordFailure → voteBreaker — voteBreaker no longer early-returns 0 for streaming failures (BREAKER_VOTES[STREAM_FAILED] ?? 0 = 1, PROVIDER_5XX/NETWORK/TIMEOUT = 1)"
  flagged_assumptions:
    - "AI-04 [probe unclassified] (carried from 03-12's must_haves): the STREAM_FAILED accounting resolution — the catch votes per the classifier's mapped code, never a hardcoded double-count; UNKNOWN-classifiable stream errors vote 0 by design (BREAKER_VOTES.UNKNOWN is absent) and STREAM_FAILED: 1 covers the no-error non-stop finish"
    - "AI-07 [unclassified] stays ACCOUNTED-DEFERRED (Phase 8 D-06, recorded in 03-10) — never re-opened"
---
<objective>
Close the empirically-confirmed residual WR-02A: the streaming-path circuit breaker is INERT. `BREAKER_VOTES` (ProviderRouter.ts L186-195) has no `STREAM_FAILED` entry and `voteBreaker` (L760-762) early-returns on a 0 vote, so `RendererService.render`'s two failure branches (mid-stream catch L128-132, non-stop finish L146) call `recordFailure` with a code that accrues zero votes — a provider failing mid-stream is still retried every turn. The WR-02 regression tests mock the Router singleton and assert invocation only, so they cannot see the 0-vote.

Purpose: make the streaming-path breaker live per VERIFICATION.md's `missing` guidance (both halves): add `STREAM_FAILED: 1` to `BREAKER_VOTES` so the no-error non-stop finish branch votes, AND switch the mid-stream catch to classify the underlying provider error and vote its mapped code (honoring 03-12's stated intent "votes per the classifier's mapped code, never a hardcoded double-count"). Prove it with a permanent real-Router regression suite that asserts breaker STATE, not invocation.

Output: `BREAKER_VOTES.STREAM_FAILED = 1`, classifier-mapped catch voting in RendererService, updated WR-02 wiring tests, and the new `RendererService.streamBreakdown.test.ts` real-Router regression.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-VERIFICATION.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-REVIEW.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-12-PLAN.md
@.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-12-SUMMARY.md
@src/core/ai/ProviderRouter.ts
@src/core/ai/RendererService.ts
@tests/core/ai/RendererService.test.ts
@tests/core/ai/AgentOrchestrator.budget.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-02A — give streaming failures a real breaker vote (BREAKER_VOTES.STREAM_FAILED + classifier-mapped catch voting)</name>
  <files>src/core/ai/ProviderRouter.ts, src/core/ai/RendererService.ts</files>
  <read_first>
    - src/core/ai/ProviderRouter.ts L181-195 (BREAKER_VOTES — the DEFECTIVE constant table: no STREAM_FAILED entry, so voteBreaker L760-762 early-returns on `BREAKER_VOTES[code] ?? 0 = 0`)
    - src/core/ai/ProviderRouter.ts L502-537 (classifyProviderError — the D-17 classifier the catch must call), L543-545 (recordFailure), L760-779 (voteBreaker — 3 votes within 60 s opens 5 min)
    - src/core/ai/RendererService.ts L122-140 (mid-stream catch — recordFailure at L127-133 hardcodes ERROR_CODES.STREAM_FAILED), L141-156 (non-stop finish — recordFailure at L146)
    - 03-VERIFICATION.md gap 2 reason + missing (the exact fix mandate: add STREAM_FAILED: 1 AND/OR classify; the locked decision takes BOTH halves)
    - 03-12-PLAN.md Task 1 + must_haves flagged_assumption ("the STREAM_FAILED code votes per the classifier's mapped code, never a hardcoded double-count")
  </read_first>
  <action>
    In `src/core/ai/ProviderRouter.ts`:
    1. Add `STREAM_FAILED: 1,` to the BREAKER_VOTES object (L186-195) — place it after `NETWORK: 1` and before `RATE_LIMITED: 0`, so the no-error non-stop finish branch's vote is real. Do NOT touch any other entry or the RETRYABLE_CODES array (STREAM_FAILED stays OUT of RETRYABLE_CODES — the streaming path never retries, §1.5).
    2. Update the JSDoc above BREAKER_VOTES (L181-185) with one line: STREAM_FAILED votes 1 (mid-stream failures open the breaker after 3 within the window) and the catch branch votes the classifier's mapped code.
    3. Update the module header comment (L1-26) with one WR-02A line noting the streaming-path vote is live.
    In `src/core/ai/RendererService.ts`:
    4. In the mid-stream catch (L127-133), replace the hardcoded `ERROR_CODES.STREAM_FAILED` argument with the classifier's mapped code — keep the `if (!isAbortError(e))` guard:
       ```
       const cls = getProviderRouter().classifyProviderError(err);
       getProviderRouter().recordFailure(input.invocation.providerId, cls.code, err);
       ```
       (One `getProviderRouter()` read per statement is fine — the singleton is cheap; do NOT add a shared local that complicates the guard.) This honors 03-12's "never a hardcoded double-count": 5XX/NETWORK/TIMEOUT map to 1-vote codes; an UNKNOWN-classifiable stream error votes 0 by design (BREAKER_VOTES.UNKNOWN absent).
    5. Leave the non-stop finish branch (L146) on `ERROR_CODES.STREAM_FAILED` — it now votes via the new table entry. Leave RENDERER_MAX_TOKENS, the RenderInput contract, buildStageMessages usage, maxRetries: 0, finishReason awaiting, the StreamFailedError carrier, partialText semantics, and the `debugLog` calls unchanged.
    6. Update the WR-02 header comment block (L20-25) with one WR-02A line: the catch votes the classifier's mapped code; the non-stop branch votes STREAM_FAILED (1 vote each).
    7. Do NOT copy the literal `STREAM_FAILED: 1` into any comment prose (the acceptance grep is region-scoped).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/core/ai/ProviderRouter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '186,196p' src/core/ai/ProviderRouter.ts | grep -c "STREAM_FAILED: 1"` returns 1 (the BREAKER_VOTES table entry is the only occurrence in the region)
    - `sed -n '186,196p' src/core/ai/ProviderRouter.ts | grep -c "RATE_LIMITED: 0"` returns 1 (the existing table is otherwise untouched)
    - `grep -c "STREAM_FAILED" src/core/ai/ProviderRouter.ts` is unchanged from the pre-task value (0 new literal occurrences outside BREAKER_VOTES — header prose refers to it by name only, never the literal table form)
    - `sed -n '127,133p' src/core/ai/RendererService.ts | grep -c "classifyProviderError"` returns 1 and `sed -n '127,133p' src/core/ai/RendererService.ts | grep -c "cls.code"` returns 1 (the catch votes the mapped code)
    - `sed -n '141,156p' src/core/ai/RendererService.ts | grep -c "STREAM_FAILED"` returns 1 (the non-stop branch keeps its code)
    - `./node_modules/.bin/tsc --noEmit` exits 0 (typecheck only — see Task 2 for the suite gate; the WR-02 vote test at RendererService.test.ts L264-285 is EXPECTED to fail until Task 2 updates its assertion)
    - `./node_modules/.bin/vitest run tests/core/ai/ProviderRouter.test.ts` exits 0 (BREAKER_VOTES change is test-transparent — no test deep-equals the table)
  </acceptance_criteria>
  <done>Streaming failures now vote: STREAM_FAILED: 1 exists in BREAKER_VOTES, and the mid-stream catch votes the classifier's mapped code — voteBreaker can no longer early-return 0 for a streaming failure.</done>
</task>

<task type="auto">
  <name>Task 2: WR-02A — update the existing WR-02 wiring tests (classifier delegate + mapped-code assertion)</name>
  <files>tests/core/ai/RendererService.test.ts</files>
  <read_first>
    - tests/core/ai/RendererService.test.ts L30-45 (vi.hoisted routerMock + partial vi.mock('@/core/ai/ProviderRouter') with importOriginal spread — the mock must now ALSO expose classifyProviderError)
    - tests/core/ai/RendererService.test.ts L263-327 (the WR-02 describe: mid-stream vote L264-285, clean-stop L287-296, user-abort L298-312, non-stop finish L314-326)
    - src/core/ai/ProviderRouter.ts L502-537 (classifyProviderError — pure method, uses no `this`, safe to delegate as a method reference)
    - 03-REVIEW.md WR-02A ("the regression tests ... assert only that recordFailure was INVOKED, never that a vote accrued or the breaker state changed")
  </read_first>
  <action>
    Extend `tests/core/ai/RendererService.test.ts`:
    1. Add `classifyProviderError: vi.fn()` to the vi.hoisted routerMock object (L35-40). The partial module mock already preserves the real ProviderRouter class via the importOriginal spread.
    2. AFTER the imports (module level, near the routerMock usage), delegate the stub to the real classifier: `routerMock.classifyProviderError = new ProviderRouter().classifyProviderError;` — the real method is `this`-free (verify: it reads no instance state), so an unbound reference is safe. Add `import { ProviderRouter } from '@/core/ai/ProviderRouter';` to the import block (the partial mock keeps the real export).
    3. Rewrite the mid-stream vote test (L264-285): change `mockStream({ streamThrows: new Error('boom') })` to throw a CLASSIFIABLE provider error — `new APICallError({ message: 'upstream 500', url: 'https://fixture.example/v1/responses', requestBodyValues: {}, statusCode: 500 })` (import `APICallError` from 'ai'; the existing vi.mock('ai') spread keeps the real class). Keep the isStreamFailedError assertion and the markStreamedFirstToken-once assertions. Replace the recordFailure expectation: `expect(routerMock.recordFailure).toHaveBeenCalledTimes(1)` and `expect(routerMock.recordFailure).toHaveBeenCalledWith('anthropic', 'PROVIDER_5XX', expect.any(Error))` — the mapped code flows from the real classifier. Add `expect(routerMock.classifyProviderError).toHaveBeenCalledTimes(1)`.
    4. Do NOT change: the user-abort test (L298-312, recordFailure not called — the isAbortError guard still excludes it), the clean-stop test (L287-296), or the non-stop finish test (L314-326, recordFailure('anthropic', 'STREAM_FAILED') — unchanged, the code is preserved).
    5. Update the WR-02 describe header comment to note the classifier-mapped vote (WR-02A).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/core/ai/RendererService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "classifyProviderError" tests/core/ai/RendererService.test.ts` returns 2 (the routerMock stub field + the delegation line) — plus the new expectation lines assert the mapped code
    - The mid-stream vote test asserts `recordFailure` called with `('anthropic', 'PROVIDER_5XX', expect.any(Error))` — a 1-vote code, proving the wiring (a 0-vote code like STREAM_FAILED/UNKNOWN would leave the breaker inert)
    - `./node_modules/.bin/vitest run tests/core/ai/RendererService.test.ts` exits 0 (all 4 WR-02 tests + the streaming-honesty suite green)
  </acceptance_criteria>
  <done>The existing WR-02 wiring tests now assert the mapped-code vote flow through the real classifier — a 0-vote regression is detectable again.</done>
</task>

<task type="auto">
  <name>Task 3: WR-02A — NEW permanent real-Router regression: RendererService.streamBreakdown.test.ts asserts breaker STATE</name>
  <files>tests/core/ai/RendererService.streamBreakdown.test.ts</files>
  <read_first>
    - tests/core/ai/AgentOrchestrator.budget.test.ts L1-111 (the real-Router precedent: construct `new ProviderRouter()` per test, stub ONLY the ai-sdk call sites, keep real error classes via importOriginal — this suite is the pattern for the new file)
    - tests/core/ai/RendererService.test.ts L30-106 (the module-mock shape: partial `vi.mock('@/core/ai/ProviderRouter')` with importOriginal spread overriding getProviderRouter; the vi.mock('ai') stub-only-streamText; the mockStream seam and baseInput fixture — copy these)
    - src/core/ai/ProviderRouter.ts L548-550 (isBreakerOpen), L186-195 (BREAKER_VOTES — the vote table the test's assertions depend on)
    - 03-VERIFICATION.md gap 2 missing ("extend RendererService.test.ts with a real-Router test: 3 STREAM_FAILED failures → isBreakerOpen(providerId) === true")
  </read_first>
  <action>
    Create `tests/core/ai/RendererService.streamBreakdown.test.ts` (NEW file) — a permanent real-Router regression:
    1. Module mocks (both partial, importOriginal spread):
       - `vi.mock('@/core/ai/ProviderRouter', ...)` returning `{ ...actual, getProviderRouter: () => holder.current }` where `holder` is a vi.hoisted `{ current: null as unknown }` placeholder (a fresh real Router is injected per test — the getProviderRouter() call sites in RendererService get the SAME instance the test asserts on).
       - `vi.mock('ai', ...)` spreading the real module and stubbing ONLY `streamText: vi.fn()` (real APICallError class kept — the classifier's instanceof checks must stay meaningful).
    2. beforeEach: `holder.current = new ProviderRouter(); vi.clearAllMocks(); getPromptCacheManager().reset();` (hints enabled — buildStageMessages emits providerOptions; the fixture expects it).
    3. Copy the mockStream seam + baseInput fixture from RendererService.test.ts (L59-106): baseInput defaults providerId 'anthropic'; keep operationId 'op-render-0001'.
    4. Tests (each drives RendererService.render through the REAL router, then asserts `holder.current.isBreakerOpen('anthropic')`):
       a. '3 non-stop finishes open the breaker — STREAM_FAILED votes 1 each (WR-02A)': loop 3× `mockStream({ deltas: ['x'], finishReason: 'length' })` + `await expect(render(baseInput())).rejects.toSatisfy(isStreamFailedError)`; after the loop `expect(holder.current.isBreakerOpen('anthropic')).toBe(true)`. Also assert a SINGLE non-stop finish does NOT open (`isBreakerOpen === false`) — threshold semantics.
       b. '3 mid-stream PROVIDER_5XX failures open the breaker — the catch votes the classifier's mapped code (WR-02A)': loop 3× `mockStream({ streamThrows: new APICallError({ message: 'upstream 500', url: 'https://fixture.example/v1/responses', requestBodyValues: {}, statusCode: 500 }) })` + render rejects; `isBreakerOpen('anthropic') === true` after 3. The deltas ('Hel','lo') stream before the throw — markStreamedFirstToken fires on the real router too (operation entry created).
       c. 'user aborts never open the breaker': loop 3× `mockStream({ streamThrows: new DOMException('aborted', 'AbortError') })` + render rejects; `isBreakerOpen('anthropic') === false`.
       d. 'a clean stop never votes — the breaker stays closed': `mockStream({ deltas: ['a'], finishReason: 'stop' })` + render resolves; `isBreakerOpen('anthropic') === false`.
    5. Header comment: this file reproduces VERIFICATION.md WR-02A EXACTLY (the empirical probe: 3×recordFailure('openai','STREAM_FAILED') → isBreakerOpen === false, pre-fix) and replaces the throwaway probe with a permanent real-Router assertion — only ai-sdk call sites stubbed, never the Router singleton.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/core/ai/RendererService.streamBreakdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The file exists with a partial `vi.mock('@/core/ai/ProviderRouter')` whose `getProviderRouter` returns the per-test holder's real `new ProviderRouter()` instance (NEVER a mock with vi.fn() recordFailure)
    - `grep -c "isBreakerOpen" tests/core/ai/RendererService.streamBreakdown.test.ts` returns at least 4 (one state assertion per test — the file asserts breaker STATE, not invocation)
    - Test a asserts both: 3 non-stop finishes open AND a single one does not (threshold)
    - Test b drives the REAL classifier through the catch with a real APICallError(500) → PROVIDER_5XX votes
    - Test c asserts AbortError/DOMException paths leave the breaker closed after 3 attempts
    - `./node_modules/.bin/vitest run tests/core/ai/RendererService.streamBreakdown.test.ts` exits 0
  </acceptance_criteria>
  <done>The WR-02A observable consequence is permanently pinned: a provider failing mid-stream (3×) now opens the breaker — a regression back to a 0-vote table fails this suite immediately.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Details |
|----------|------|---------|
| `BREAKER_VOTES.STREAM_FAILED = 1` | constant-table entry | ProviderRouter.ts L186-195 — the no-error non-stop finish branch's vote becomes real; voteBreaker no longer early-returns 0 on STREAM_FAILED |
| classifier-mapped catch voting | behavior | RendererService.ts L128-132 — the mid-stream catch calls `getProviderRouter().classifyProviderError(err)` and votes `recordFailure(providerId, cls.code, err)` (03-12 intent: never a hardcoded double-count) |
| `routerMock.classifyProviderError` delegate | test seam | RendererService.test.ts — the real `classifyProviderError` method reference bound into the WR-02 routerMock; the mid-stream vote test asserts the mapped code (`PROVIDER_5XX`) |
| `tests/core/ai/RendererService.streamBreakdown.test.ts` | NEW file | permanent real-Router regression: 3 non-stop finishes → open, 3 mid-stream PROVIDER_5XX → open, abort paths never open, clean stop never votes |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider SDK stream → RendererService | mid-stream errors/finish reasons cross in; origin (provider vs user abort) is discriminated by the isAbortError guard before any breaker vote |
| RendererService catch → Router classifier | the underlying error object is now passed to `classifyProviderError` and its mapped code drives `voteBreaker` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15-01 | DoS | BREAKER_VOTES.STREAM_FAILED + mapped-code voting | high | mitigate | The breaker opening is the intended cost-governance outcome (a failing provider is NOT retried every turn); voteBreaker's existing bounds (3 votes within 60 s → open 5 min, §1.5) cap the blast radius. This change makes the pre-existing breaker mechanism LIVE on the streaming path — no new unbounded state |
| T-03-15-02 | Tampering | classifier input (untrusted stream error) | medium | mitigate | `classifyProviderError` maps only canonical C.2 codes (no free-form strings reach BREAKER_VOTES lookups — unknown inputs fall to UNKNOWN which votes 0); the code+vote path records no provider body text (R-10) |
| T-03-15-03 | Spoofing | user-abort misattribution | medium | mitigate | the isAbortError guard (name-match, prototype-chain agnostic) still runs BEFORE classification in the catch — a user abort (AbortError) never reaches recordFailure; only provider-originated errors classify and vote |
| T-03-15-04 | Info Disclosure | err object → debugLog | low | mitigate | the vote path logs only `code` + `providerId` + (optionally) the error via debugLog, which auto-routes through TraceRedactor (R-10); RendererService already logs only `partialTokens` counts — no new body logging is introduced |

No new network surface, no new storage, no new secrets, no new dependencies (approved stack §7 untouched).
</threat_model>

<verification>
- `./node_modules/.bin/vitest run tests/core/ai/RendererService.test.ts tests/core/ai/RendererService.streamBreakdown.test.ts` — exits 0
- `./node_modules/.bin/vitest run tests/core/ai/ProviderRouter.test.ts` — exits 0 (BREAKER_VOTES change is test-transparent)
- `./node_modules/.bin/vitest run tests/core/ai/AgentOrchestrator.budget.test.ts` — exits 0 (the renderer call site must not regress the real-Router orchestrator suite)
- `./node_modules/.bin/tsc --noEmit` — exits 0
- Grep proof: `sed -n '186,196p' src/core/ai/ProviderRouter.ts | grep "STREAM_FAILED"` shows the entry; `grep -rn "recordFailure" src/core/ai/RendererService.ts` shows the two call sites (catch mapped-code + non-stop STREAM_FAILED)
- Full gate (if the pnpm wrapper is healthy in the executor environment): `pnpm run verify:phase-3` stays green; otherwise run its components via the direct binaries above
</verification>

<success_criteria>
- 3 mid-stream streaming failures (provider-originated) open the provider's breaker — the WR-02A observable consequence is closed
- The catch votes the classifier's mapped code; the no-error non-stop finish votes STREAM_FAILED (now 1 vote)
- User aborts and clean stops never open the breaker
- The permanent real-Router regression file asserts breaker STATE and would fail if BREAKER_VOTES lost the entry
</success_criteria>

<output>
Create `.planning/phases/03-cost-effective-ai-runtime-persona-seed/03-15-SUMMARY.md` when done
</output>
