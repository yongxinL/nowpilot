---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/setup.ts
  - tests/core/storage/harness-smoke.test.ts
autonomous: true
requirements: [REQ-R03, REQ-R07]
user_setup: []
estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The vitest environment provides a working global `indexedDB` (fake-indexeddb, per RESEARCH empirical probe) so idb-based tests are hermetic."
    - "The vitest environment provides a Map-backed `chrome.storage.session` mock mirroring the existing local/sync mocks (RESEARCH Pitfall 5 — the session mock is currently missing and WorkspaceElection tests need it)."
    - "`idb` and `fake-indexeddb` are installed with versions matching STATE.md watch item VAI-04 (idb 8.0.3, fake-indexeddb 6.2.5 — re-queried 2026-08-23 in RESEARCH.md)."
    - "All pre-existing Phase-1 test files still pass after the harness changes (no regression against the 166-test verify:phase-1 suite)."
  artifacts:
    - "package.json with idb@^8 (dependency) and fake-indexeddb@^6 (devDependency)"
    - "tests/setup.ts with fake-indexeddb auto-import, `__resetIndexedDB()` global helper, and the chrome.storage.session mock (`__chromeStorageSession` + `__chromeSessionMap`)"
    - "tests/core/storage/harness-smoke.test.ts"
    - "tests/core/security/ and tests/core/utils/ directories (required by the verify:phase-2 vitest path args)"
  key_links:
    - "tests/setup.ts chrome.storage.session mock ↔ WorkspaceElection CAS/heartbeat tests (plan 02-06)"
    - "global indexedDB ↔ idb openDB in WriteJournalDB/ErrorStore/IndexedDBMigrator tests (plans 02-04, 02-05)"
  assumptions: []
  prohibitions:
    - "Changing the behavior of the existing chrome.storage.local / sync mocks (the session area is appended alongside, never mutating the local/sync Map semantics)"
    - "Hand-rolling an IndexedDB mock instead of fake-indexeddb (RESEARCH Don't-Hand-Roll: a custom mock misses upgrade/transaction/key-range semantics)"
---

<objective>
Wave-0 test infrastructure: install the two Phase-2 packages, extend `tests/setup.ts` with the two missing test-env capabilities (IndexedDB + `chrome.storage.session`), and prove the harness end-to-end before any Phase-2 test file depends on it.

Purpose: RESEARCH.md empirically verified this session that `indexedDB` is **undefined** in the current jsdom test env and that `chrome.storage.session` is **not mocked** in `tests/setup.ts:142-145` — both are hard blockers for the IndexedDBMigrator / WriteJournal / WorkspaceElection / WorkspacePersistence tests every later plan in this phase writes. This plan removes both blockers first so later plans' `<verify>` commands are runnable on day one.
Output: working test harness (deps + setup.ts extensions + smoke proof), zero Phase-1 regression.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md
@tests/setup.ts
@package.json
@tests/core/storage/chromeStorageAdapter.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Install Phase-2 packages and extend tests/setup.ts with IndexedDB + chrome.storage.session harness</name>
  <files>package.json, tests/setup.ts</files>
  <read_first>
    - tests/setup.ts (the file being modified — current mocks for local/sync/BroadcastChannel at lines 45-187)
    - 02-RESEARCH.md lines 544-551 (fake-indexeddb harness), 243-250 (session mock shape), 661-677 (Environment Availability)
    - 02-PATTERNS.md lines 491-504 (tests/setup.ts modification spec)
    - package.json (the file being modified — scripts + dependency blocks)
  </read_first>
  <action>
    Install the two spec-pinned packages with pnpm: `pnpm add idb@^8` (resolves 8.0.3 per VAI-04/RESEARCH) and `pnpm add -D fake-indexeddb@^6` (resolves 6.2.5). Both passed the RESEARCH Package Legitimacy Audit (OK verdict, no postinstall, well-known repos) — no checkpoint required.

    Extend `tests/setup.ts` with exactly three additive changes (append-only; do not alter the existing local/sync/BroadcastChannel mock semantics):
    1. Add `import 'fake-indexeddb/auto';` at the top of the file (before the existing `vi.stubGlobal` calls) so a global `indexedDB` exists in every test file.
    2. Add a global reset helper: `(globalThis as any).__resetIndexedDB = () => { (globalThis as any).indexedDB = new IDBFactory(); };` (import `IDBFactory` from 'fake-indexeddb') and invoke it once at module scope so the global is a fresh factory per test run. Expose the helper so tests can call it in `beforeEach`.
    3. Add a Map-backed `chrome.storage.session` mock mirroring the existing `chromeStorageLocal` mock (tests/setup.ts lines 45-95): a `chromeStorageSession` object with `get`/`set`/`remove`/`clear` `vi.fn` implementations over a module-level `sessionMap = new Map<string, string>()`, exposed as `(globalThis as any).__chromeStorageSession` and `(globalThis as any).__chromeSessionMap`, and wired into `(globalThis as any).chrome.storage.session = chromeStorageSession` in the existing `chrome.storage` assignment (line 142-145).
    4. Create the verify-path directories `tests/core/security/` and `tests/core/utils/` (mkdir -p) — verify:phase-2's vitest path args require them to exist, and wave-1 plans 02-02/03 write their test files into them. File writes self-heal via mkdir -p semantics regardless, but doing it here lands the dirs at the earliest point in the wave (checker info 02-02/02-03 dir-hazard fix).
  </action>
  <verify>
    <automated>pnpm lint && pnpm list idb fake-indexeddb 2>&1 | grep -E "idb@8|fake-indexeddb@6"</automated>
  </verify>
  <done>
    Both packages appear in package.json with the expected major versions; tests/setup.ts contains the fake-indexeddb auto-import, `__resetIndexedDB`, and the session mock wired into `chrome.storage.session`; `pnpm lint` (tsc --noEmit) passes.
  </done>
  <acceptance_criteria>
    - `pnpm list idb fake-indexeddb` shows idb 8.x and fake-indexeddb 6.x installed.
    - tests/setup.ts contains the literal `import 'fake-indexeddb/auto'` line and an assignment of `chrome.storage.session` to a mock object.
    - `pnpm lint` exits 0.
    - The existing local/sync mock implementations are byte-for-byte unchanged apart from the appended session wiring.
    - The `tests/core/security/` and `tests/core/utils/` directories exist (mkdir -p, step 4).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Prove the harness end-to-end with a smoke test and run the Phase-1 regression gate</name>
  <files>tests/core/storage/harness-smoke.test.ts</files>
  <read_first>
    - tests/core/storage/chromeStorageAdapter.test.ts (test conventions: describe/it/expect/beforeEach/vi, beforeEach clears maps + `__test__.resetPendingState()`)
    - tests/setup.ts (the mocks the smoke test exercises)
    - 02-RESEARCH.md lines 544-551 (fake-indexeddb reset pattern)
  </read_first>
  <action>
    Create `tests/core/storage/harness-smoke.test.ts` with three `it` cases:
    1. IndexedDB availability: `expect(indexedDB).toBeDefined()`; open a throwaway DB via `openDB('SmokeDB', 1, { upgrade(db) { db.createObjectStore('t'); } })` (import `openDB` from 'idb'), put/get a value round-trip, `db.close()`, `indexedDB.deleteDatabase('SmokeDB')`.
    2. Session mock round-trip: `await chrome.storage.session.set({ np_workspace_primary: { tabId: 1, surface: 'sidepanel', electedAt: 123 } })` then `get` returns the same record; assert `(globalThis as any).__chromeSessionMap` has the key.
    3. Isolation: call `(globalThis as any).__resetIndexedDB()` and assert the global `indexedDB` is replaced with a fresh factory instance (not reference-equal to the previous one).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/core/storage/harness-smoke.test.ts && pnpm run verify:phase-1</automated>
  </verify>
  <done>
    The smoke test passes (all 3 cases); `pnpm run verify:phase-1` stays green (tsc + the full Phase-1 vitest suite + the tailwind gate) — the harness additions introduce zero Phase-1 regression.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/storage/harness-smoke.test.ts` exits 0 with 3 passing tests.
    - `pnpm run verify:phase-1` exits 0 (proves the 166-test Phase-1 suite plus the tailwind/no-tailwind grep gate still pass).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `package.json` — deps: `idb` (^8), devDeps: `fake-indexeddb` (^6)
- `tests/setup.ts` — additions: `import 'fake-indexeddb/auto'`; `__resetIndexedDB()` global; `chrome.storage.session` mock (`__chromeStorageSession`, `__chromeSessionMap`); `sessionMap`
- `tests/core/storage/harness-smoke.test.ts` — new test file (3 cases: IDB round-trip, session round-trip, reset isolation)
- Directories `tests/core/security/`, `tests/core/utils/`

## Flagged Assumptions (spec-less probe fallback)

None of the 5 probe rows land in this plan (REQ-R06 → plan 02-04; REQ-R07 → plan 02-06; REQ-R03 → plan 02-07).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | package installs cross this boundary (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-SC | Tampering | idb / fake-indexeddb installs | high | mitigate | RESEARCH.md Package Legitimacy Audit completed this session: both packages OK verdict, no `postinstall` script, well-known author repos, versions re-queried from the registry (VAI-04). No [ASSUMED]/[SUS] verdicts — no blocking checkpoint needed. |
| T-02-01-01 | Tampering | tests/setup.ts mocks | low | accept | Mock fidelity is best-effort by design; fake-indexeddb passes 82.8% of the WPT IDB suite and the session mock mirrors the already-shipped local mock shape — residual divergence surfaces as test failures, not silent production risk. |
</threat_model>

<verification>
- Harness correctness: `pnpm exec vitest run tests/core/storage/harness-smoke.test.ts` (3 cases).
- Phase-1 regression: `pnpm run verify:phase-1` green.
- Deps sanity: `pnpm list idb fake-indexeddb`.
</verification>

<success_criteria>
- Both Phase-2 packages installed at the researched versions.
- `tests/setup.ts` provides global `indexedDB` (with per-test reset) and `chrome.storage.session`.
- Every pre-existing Phase-1 test file passes unchanged — the harness is purely additive.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-01-SUMMARY.md` when done
</output>