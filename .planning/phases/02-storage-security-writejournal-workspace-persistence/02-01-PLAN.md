---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - tests/setup.ts
  - tests/core/storage/smoke.test.ts
autonomous: true
requirements:
  - STOR-05

must_haves:
  truths:
    - "idb v8.0.3 is installed and importable from TypeScript"
    - "chrome.storage.local mock is available in tests with get, set, remove, getBytesInUse"
    - "crypto.subtle mock or native support is verified via smoke test"
  artifacts:
    - "package.json (updated with idb dependency)"
    - "tests/setup.ts (updated with chrome.storage.local + crypto mocks)"
    - "tests/core/storage/smoke.test.ts"
  key_links:
    - "npm install idb@^8.0.3 → package.json → import { openDB } from 'idb'"
    - "tests/setup.ts chrome.stubGlobal → vitest → all storage tests"
---

<objective>
Install the `idb` v8.0.3 dependency and update the test infrastructure to support IndexedDB, chrome.storage.local, and Web Crypto API mocking for all Phase 2 storage tests.

Purpose: All subsequent Phase 2 plans depend on `idb` being installable and the test mocks being in place. Without this, no storage test can run.

Output: Updated package.json with idb dependency, updated tests/setup.ts with chrome.storage.local and crypto.subtle mocks, smoke test verifying the infrastructure works.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md
@.planning/PROJECT.md
@tests/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install idb v8.0.3</name>
  <files>package.json</files>
  <read_first>
    @package.json
    @.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md (lines 110-121 for version verification)
  </read_first>
  <action>
    Install the `idb` package at version 8.0.3. Run `npm install idb@^8.0.3` from the project root. After installation, verify the package was added by checking that `package.json` contains `"idb"` in dependencies with version `^8.0.3`. Run `npx tsc --noEmit` to confirm the `idb` types are resolved and there are no new TypeScript compilation errors. Verify the installed version with `npm ls idb` — the output must show `idb@8.0.3`.
  </action>
  <acceptance_criteria>
    - `package.json` dependencies includes `"idb": "^8.0.3"`
    - `npm ls idb` outputs `idb@8.0.3`
    - `npx tsc --noEmit` exits 0 (no new type errors from idb installation)
    - `node -e "require('idb')"` exits 0 (module is resolvable at runtime)
  </acceptance_criteria>
  <verify>
    <automated>npm ls idb 2>&1 | grep -q "idb@8.0.3" && npx tsc --noEmit</automated>
  </verify>
  <done>idb v8.0.3 is installed in node_modules, package.json is updated, TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests/setup.ts with chrome.storage.local + crypto.subtle mocks</name>
  <files>tests/setup.ts, tests/core/storage/smoke.test.ts</files>
  <read_first>
    @tests/setup.ts
    @.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md (lines 580-612 for setup.ts modifications)
    @.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md (lines 776-779 for crypto.subtle smoke test concern)
    @tests/core/workspaceStore.test.ts
    @tests/core/keymapRegistry.test.ts
  </read_first>
  <action>
    Modify `tests/setup.ts` to add new mocks needed by Phase 2 storage tests:

    1. Add `chrome.storage.local` entry inside the existing `vi.stubGlobal('chrome', {...})` call, at the same indentation level as `sync` and `session`. The `local` object must have: `get: vi.fn().mockResolvedValue({})`, `set: vi.fn().mockResolvedValue(undefined)`, `remove: vi.fn().mockResolvedValue(undefined)`, `getBytesInUse: vi.fn().mockResolvedValue(0)`.

    2. Add `chrome.runtime.id` inside the existing `runtime` object: `id: 'test-extension-id'`.

    3. Add `crypto` stub on `globalThis` using `vi.stubGlobal('crypto', {...})` AFTER the chrome stub. The crypto stub must include: `getRandomValues: vi.fn((arr: Uint8Array) => { for (let i = 0; i < arr.length; i++) arr[i] = Math.floor(Math.random() * 256); return arr; })` and `subtle: { encrypt: vi.fn(), decrypt: vi.fn(), deriveKey: vi.fn(), importKey: vi.fn(), generateKey: vi.fn() }`. Wrap this in a conditional that only stubs if `globalThis.crypto` is not available: `if (typeof globalThis.crypto === 'undefined' || !globalThis.crypto.subtle) { vi.stubGlobal('crypto', ...); }`.

    4. Create new file `tests/core/storage/smoke.test.ts` with a single test that verifies: (a) `chrome.storage.local.get` can be called and returns a mock value, (b) `crypto.getRandomValues` can be called and fills a Uint8Array, (c) `crypto.subtle.encrypt` can be called. Use vitest `describe`/`it`/`expect` pattern matching existing tests. Import path pattern: `import { describe, it, expect, vi } from 'vitest';`.

    5. Run `npx vitest run tests/core/storage/smoke.test.ts` to confirm all smoke assertions pass.
  </action>
  <acceptance_criteria>
    - `tests/setup.ts` contains `chrome.storage.local` with keys `get`, `set`, `remove`, `getBytesInUse`
    - `tests/setup.ts` contains `chrome.runtime.id` with value `'test-extension-id'`
    - `tests/setup.ts` contains `crypto.getRandomValues` (functional stub filling Uint8Array)
    - `tests/setup.ts` contains `crypto.subtle` with keys `encrypt`, `decrypt`, `deriveKey`, `importKey`, `generateKey`
    - `tests/core/storage/smoke.test.ts` exists with 3 passing assertions (local.get, getRandomValues, subtle.encrypt)
    - `npx vitest run tests/core/storage/smoke.test.ts` exits 0 with all tests passing
    - Existing tests continue to pass: `npx vitest run tests/core/workspaceStore.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/storage/smoke.test.ts && npx vitest run tests/core/workspaceStore.test.ts</automated>
  </verify>
  <done>Test infrastructure supports chrome.storage.local, crypto.subtle, and crypto.getRandomValues in vitest+jsdom. Smoke test passes. Existing tests unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → project | Package installation — `idb` fetched from public npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm install of `idb` | high | mitigate | Verified via Package Legitimacy Audit in RESEARCH.md — approved (8+ yrs maintenance, 17.5M/wk downloads, Jake Archibald/Google). Locked to version 8.0.3 via `^8.0.3` range. |
</threat_model>

<verification>
- `npm install idb` completed without errors
- `package.json` has idb dependency
- `tests/setup.ts` has all required mocks
- `tests/core/storage/smoke.test.ts` passes
- All existing tests still pass
</verification>

<success_criteria>
1. idb v8.0.3 is installed and TypeScript compiles cleanly
2. Test mocks for chrome.storage.local and Web Crypto API are functional
3. Smoke test verifies the test infrastructure is ready for Phase 2 implementation
4. No existing tests are broken by the setup.ts modifications
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `chrome.storage.local` mock (get, set, remove, getBytesInUse) | Test infra | tests/setup.ts |
| `chrome.runtime.id` mock (`'test-extension-id'`) | Test infra | tests/setup.ts |
| `crypto.getRandomValues` stub | Test infra | tests/setup.ts |
| `crypto.subtle` stub (encrypt, decrypt, deriveKey, importKey, generateKey) | Test infra | tests/setup.ts |
| Smoke test suite | Test | tests/core/storage/smoke.test.ts |
