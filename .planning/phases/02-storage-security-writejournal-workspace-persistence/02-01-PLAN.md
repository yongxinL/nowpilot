---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/setup.ts
  - src/types/storage.ts
  - src/core/error/errorCodes.ts
  - src/core/i18n/strings.ts
  - tests/fixtures/index.ts
  - tests/fixtures/fixtures.test.ts
  - .planning/PRODUCT_SPEC_v0_1.md
autonomous: true
requirements: [STORAGE-02, STORAGE-03, STORAGE-04, STORAGE-05]
must_haves:
  truths:
    - "Tests can open real IndexedDB (idb CRUD + version migrations) inside the jsdom-align env via fake-indexeddb — no polyfills beyond crypto.subtle/structuredClone (already present)."
    - "The six Phase-2 canonical codes (VAULT_DECRYPT_FAILED, PROVIDER_KEY_UNREADABLE, IDB_MIGRATION_FAILED, SYNC_QUOTA_EXCEEDED, WRITE_JOURNAL_FAILED, WRITE_JOURNAL_ROLLBACK_FAILED) exist in src/core/error/errorCodes.ts AND in spec Appendix C.2."
    - "WriteJournalEntry and the locked 11-op WriteJournalOperation union are typed in src/types/storage.ts and importable via '@/types/storage' (the spec O.11 import path, R-1)."
    - "Six deterministic typed fixture builders live under tests/fixtures/ (vault-roundtrip, cross-install, journal-recovery, migration, quota-shadow, redaction) and are never imported by src/."
    - "verify:phase-2 script exists in package.json (eslint + prettier + tsc --noEmit + wxt build + vitest run + isolation check)."
  artifacts:
    - package.json
    - tests/setup.ts
    - src/types/storage.ts
    - src/core/error/errorCodes.ts
    - src/core/i18n/strings.ts
    - tests/fixtures/index.ts
    - tests/fixtures/fixtures.test.ts
  key_links:
    - "errorCodes.ts ↔ PRODUCT_SPEC Appendix C.2 (canonical codes in both places, CONTEXT line 84)"
    - "tests/fixtures/index.ts ↔ src/types/storage.ts (typed builders compile against the canonical types)"
    - "tests/setup.ts ↔ vitest.config.ts (fake-indexeddb registered before any idb store test runs)"
---

<objective>
Wave-0 foundation for the whole phase: install the storage stack (idb, fflate, fake-indexeddb), register the IndexedDB test harness, ship the canonical storage types, extend the canonical error-code registry, add the Phase-2 canonical user strings, and build the six deterministic fixture builders that every later plan's unit and integration tests share (D-20/D-21).

Purpose: Everything downstream (vault, journal, stores, migrator, import/export, sync-shadow) imports types, codes, fixtures, and the test harness created here. Without this plan, every later plan would duplicate harness wiring and fixtures would drift.
Output: package.json + tests/setup.ts + src/types/storage.ts + errorCodes.ts extension + i18n strings + tests/fixtures/ + verify:phase-2 script.

Flagged assumptions (specless probe — all five Phase-2 requirements were `unresolved`; authoring the resolved contract here):
- A-01: "API keys AES-GCM at rest, never plaintext in chrome.storage" is the vault contract (STORAGE-03) — enforced by the cross-install/redaction fixtures.
- A-02: "No message bodies in chrome.storage.local" is a hard invariant (STORAGE-01) — enforced by the phase-11 privacy gate; fixtures never seed bodies into KV builders.
- A-03: "installSecret never in sync, never in exports" (D-02) — the fixture builders model the key registry so later export tests can prove exclusion.
- A-04: PBKDF2-100k iterations stay locked (D-02) — the vault-roundtrip fixture pins the derivation parameters verbatim.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md (D-02, D-03, D-09, D-10, D-11, D-13, D-15, D-20, D-21)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md (Pattern 1 fake-indexeddb + Pattern 3 crypto probes; Wave 0 Gaps; Package Legitimacy Audit)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md (Shared Pattern 3 debugLog + errorCodes; Pattern 8 fake-indexeddb harness; Pattern 9 fixture builders)
@.planning/PRODUCT_SPEC_v0_1.md §15.1/§15.2 (lines 1920-1984), §20.3 (lines 3183-3207), Appendix C (WriteJournalEntry lines 4592-4608), Appendix C.2 (lines 4918+), Appendix O.11 (line 6592 import path), Appendix B (canonical strings, line 4029)
@tests/setup.ts (existing harness — add fake-indexeddb import, Pattern 8)
@src/core/error/errorCodes.ts (existing registry — extend IN PLACE)
@src/core/i18n/strings.ts (existing STR object — add Phase-2 keys)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install storage deps, register fake-indexeddb harness, add verify:phase-2 script</name>
  <read_first>
    - package.json (scripts + deps blocks)
    - tests/setup.ts (existing beforeEach/afterEach structure)
    - 02-RESEARCH.md "Primary recommendation" + Pattern 1 + "Wave 0 Gaps"
  </read_first>
  <files>
    package.json
    tests/setup.ts
  </files>
  <action>
    Install the approved-stack storage dependencies exactly as RESEARCH.md specifies: `pnpm add idb@^8 fflate@^0.8` and `pnpm add -D fake-indexeddb@^6` (devDep, test-only). Do not add any other package (banned list, §0.2 package hygiene).

    In tests/setup.ts, add `import 'fake-indexeddb/auto';` at the top of the import block so the global indexedDB/IDBKeyRange/IDBCursor exist for every test (verified compatible with the jsdom-align env + threads pool — RESEARCH Pattern 1). Keep the existing fakeBrowser.reset() beforeEach and RTL cleanup afterEach untouched. Note in a header comment (one line, referencing RESEARCH Pattern 1) that crypto.subtle and structuredClone are already available so no further polyfills are required.

    In package.json scripts, add `"verify:phase-2": "eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run && node tests/isolation/check-content-bundle.mjs"` — the Phase-1 extended template per RESEARCH Validation Architecture (spec §24 minimum is `tsc --noEmit && vitest run tests/core/storage tests/core/security tests/core/utils tests/core/workspace/WorkspacePersistence.test.ts`, satisfied by the full run). Keep verify:phase-1 untouched.
  </action>
  <acceptance_criteria>
    - `pnpm ls idb fflate fake-indexeddb` lists all three; fake-indexeddb under devDependencies, idb+fflate under dependencies
    - `grep -c "fake-indexeddb/auto" tests/setup.ts` returns 1
    - `grep -c '"verify:phase-2"' package.json` returns 1 and the script contains the six-step chain (eslint, prettier, tsc, wxt build, vitest run, check-content-bundle)
    - `pnpm vitest run tests/theme tests/workspace --reporter=dot` still passes (existing suite unaffected by the harness change)
  </acceptance_criteria>
  <verify>
    <automated>pnpm ls idb fflate fake-indexeddb && grep -c "fake-indexeddb/auto" tests/setup.ts && grep -c '"verify:phase-2"' package.json && pnpm vitest run tests/core/theme tests/core/workspace --reporter=dot</automated>
  </verify>
  <done>Deps installed at pinned versions, harness registered, verify:phase-2 script added, existing test suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/types/storage.ts + extend errorCodes.ts + spec C.2 + Phase-2 i18n strings</name>
  <read_first>
    - PRODUCT_SPEC §20.3 (lines 3183-3207, locked WriteJournalOperation union verbatim)
    - PRODUCT_SPEC Appendix C WriteJournalEntry (lines 4592-4608, verbatim)
    - PRODUCT_SPEC Appendix O.11 (line 6592 — imports WriteJournalEntry from '@/types/storage')
    - 02-CONTEXT.md line 84 (six codes must be canonical in both errorCodes.ts and spec C.2)
    - src/types/workspace.ts (per-domain types-file precedent — header comment style, §21.5 verbatim)
    - src/core/error/errorCodes.ts (block-comment grouping style, IN-PLACE extension)
    - 02-PATTERNS.md Shared Pattern 3 (debugLog + canonical error codes)
  </read_first>
  <files>
    src/types/storage.ts
    src/core/error/errorCodes.ts
    src/core/i18n/strings.ts
    .planning/PRODUCT_SPEC_v0_1.md
  </files>
  <action>
    Create src/types/storage.ts following the src/types/workspace.ts per-domain precedent (header comment citing §20.3/Appendix C, D-05). Export exactly:
    - `type WriteJournalOperation` = the locked 11-op union verbatim from §20.3 (lines 3186-3197): append-memory-message, evict-conversation, archive-conversation, compact-conversation, save-note-with-links, update-user-memory, export-data, update-workspace, sync-note-file, delete-note-file, restore-notes-batch. No invented ops, no edits (D-05: declared-but-unwired vocabulary).
    - `interface WriteJournalEntry` verbatim from Appendix C (lines 4594-4607): id, operation, status ('pending'|'applying'|'completed'|'failed'|'rolled-back'), createdAt, updatedAt, attempts, targetIds: Record<string,string>, steps: Array<{name; status:'pending'|'completed'|'failed'; error?}>.

    Extend src/core/error/errorCodes.ts IN PLACE with a new block-comment group `--- Storage / vault / journal (Phase 2) ---` adding six codes, each `'CODE': 'CODE'` string entries matching the existing style: VAULT_DECRYPT_FAILED, PROVIDER_KEY_UNREADABLE, IDB_MIGRATION_FAILED, SYNC_QUOTA_EXCEEDED, WRITE_JOURNAL_FAILED, WRITE_JOURNAL_ROLLBACK_FAILED. Do not duplicate any existing code; do not re-export elsewhere.

    In .planning/PRODUCT_SPEC_v0_1.md, canonicalize the same six codes into the Appendix C.2 error-code registry (the Phase-1 block ends ~line 5097): append a "Phase 2 — storage/vault/journal" block listing the six verbatim codes. Use a scoped Edit that only touches the C.2 section end — never rewrite the file.

    In src/core/i18n/strings.ts, add a `storage:` group (canonical copy verbatim from CONTEXT D-12/D-04 wording — do not paraphrase):
    - `degradedBanner: 'Storage failed to upgrade — data is read-only. Use Import/Export to back up.'`
    - `providerKeyRequired: 'Key required — re-enter'`
    Add a one-line header note citing Appendix B + CONTEXT D-12/D-04.
  </action>
  <acceptance_criteria>
    - `tsc --noEmit` passes (types file compiles; WriteJournalOperation union has exactly 11 members)
    - `grep -c "VAULT_DECRYPT_FAILED\|PROVIDER_KEY_UNREADABLE\|IDB_MIGRATION_FAILED\|SYNC_QUOTA_EXCEEDED\|WRITE_JOURNAL_FAILED\|WRITE_JOURNAL_ROLLBACK_FAILED" src/core/error/errorCodes.ts` returns 6 distinct codes
    - Same 6 codes present in .planning/PRODUCT_SPEC_v0_1.md Appendix C.2 section (`grep -c "IDB_MIGRATION_FAILED" .planning/PRODUCT_SPEC_v0_1.md` >= 1 and the five siblings likewise)
    - `grep -c "degradedBanner" src/core/i18n/strings.ts` returns 1; `grep -c "providerKeyRequired" src/core/i18n/strings.ts` returns 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "WRITE_JOURNAL_ROLLBACK_FAILED" src/core/error/errorCodes.ts && grep -c "restore-notes-batch" src/types/storage.ts && grep -c "degradedBanner" src/core/i18n/strings.ts</automated>
  </verify>
  <done>Canonical types compile via '@/types/storage'; six codes live in errorCodes.ts AND spec C.2; the two Phase-2 banner strings ship verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Build the six deterministic fixture builders (D-20/D-21) + determinism smoke test</name>
  <read_first>
    - 02-CONTEXT.md D-20/D-21 (six named builders, determinism + typed + edge-variant requirements)
    - 02-PATTERNS.md Pattern 9 (fixture builders)
    - src/types/storage.ts (WriteJournalEntry shape for the journal-recovery builder)
    - PRODUCT_SPEC §15.2 (PBKDF2 100k/SHA-256 pin for vault builders)
    - tests/core/workspace/WorkspaceStore.test.ts (test-file import/cleanup conventions)
  </read_first>
  <files>
    tests/fixtures/index.ts
    tests/fixtures/fixtures.test.ts
  </files>
  <action>
    Create tests/fixtures/index.ts exporting six named, typed, deterministic builders (D-20/D-21). Rules: seeded pseudo-randomness or fixed constants only — never real crypto.getRandomValues or Date.now; parameterized on edges (workspaceId, version, secret); edge/failure variants first-class; no imports from src/ except type-only imports (D-21 direction: tests → fixtures → src/types). Builders:

    1. `buildVaultRoundtripFixture(overrides?)` — returns { installSecret, extensionId, plaintext, salt, iv } with fixed byte arrays (e.g. salt 16×0x11, iv 12×0x22, installSecret a fixed 32-byte pattern base64) for AES-GCM-256 roundtrip (STORAGE-03 decrypt posture).
    2. `buildCrossInstallFixture(overrides?)` — returns { secretA, secretB, envelopeA } where envelopeA was "encrypted on install A"; used to prove decrypt-B → PROVIDER_KEY_UNREADABLE and NO wipe (D-04 cross-install road).
    3. `buildJournalRecoveryFixture(overrides?)` — returns WriteJournalEntry[] in statuses pending/applying/completed with fixed workspaceId/version in targetIds; edge variants: crash-before-completed, different-workspaceId entry, unknown operation string (forward-compat skip case) (D-05/D-07, WR-10).
    4. `buildMigrationFixture(overrides?)` — returns { dbName, v1Rows } for the synthetic v1→v2 proof: fixed rows for the legacy store plus expected v2 schema facts (add-store + add-index + data-carry + idempotency + throws→degraded) (D-13/D-14).
    5. `buildQuotaShadowFixture(overrides?)` — returns { key ('np_theme'|'np_theme_pack'|'np_language'), value, syncRejectError } modeling a rejected chrome.storage.sync write (quota or rate) for the shadow-promote/clear dance (D-15).
    6. `buildRedactionFixture(overrides?)` — returns error/message strings and a structured object containing secret-shaped values (sk-…, Bearer …, JSESSIONID=, sysparm_ck, g_ck) plus a password-like field that must be dropped, not masked (D-16/R-10; Appendix O.13 patterns).

    Create tests/fixtures/fixtures.test.ts (a real test — Nyquist) asserting determinism per builder: two calls with identical args deep-equal (fixed IDs/timestamps), no NaN/undefined in required fields, and the redaction fixture's password field is a distinct key the redaction tests can assert absence of.
  </action>
  <acceptance_criteria>
    - `tsc --noEmit` passes with tests/fixtures included
    - `grep -c "buildVaultRoundtripFixture\|buildCrossInstallFixture\|buildJournalRecoveryFixture\|buildMigrationFixture\|buildQuotaShadowFixture\|buildRedactionFixture" tests/fixtures/index.ts` returns all 6 export names
    - `pnpm vitest run tests/fixtures/fixtures.test.ts --reporter=dot` passes (determinism smoke green)
    - `grep -rn "tests/fixtures" src/` returns nothing (D-21: fixtures never imported from src/)
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm vitest run tests/fixtures/fixtures.test.ts --reporter=dot</automated>
  </verify>
  <done>Six deterministic typed builders live under tests/fixtures/, proven deterministic by a real test, and are import-ready for every later plan.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol | Kind | Location |
|--------|------|----------|
| `WriteJournalOperation` (11-op union) | type | src/types/storage.ts |
| `WriteJournalEntry` | interface | src/types/storage.ts |
| `ERROR_CODES.VAULT_DECRYPT_FAILED` + 5 siblings | const | src/core/error/errorCodes.ts |
| `STR.storage.degradedBanner`, `STR.storage.providerKeyRequired` | const | src/core/i18n/strings.ts |
| `buildVaultRoundtripFixture` / `buildCrossInstallFixture` / `buildJournalRecoveryFixture` / `buildMigrationFixture` / `buildQuotaShadowFixture` / `buildRedactionFixture` | fn | tests/fixtures/index.ts |
| verify:phase-2 script | script | package.json |
| fake-indexeddb harness registration | side-effect import | tests/setup.ts |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json deps → build/runtime | New dependencies (idb, fflate, fake-indexeddb) enter the supply chain here |
| tests/setup.ts → all tests | A global test harness change touches every future test file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01-01 | Tampering | pnpm installs (idb, fflate, fake-indexeddb) | high | mitigate | RESEARCH.md Package Legitimacy Audit rates all three OK (no [SUS]/[SLOP]); no blocking checkpoint required. Pin within the approved ranges (`idb@^8`, `fflate@^0.8`, devDep `fake-indexeddb@^6`); never add beyond the approved stack (AGENTS.md §7). |
| T-2-01-02 | Tampering | errorCodes.ts ↔ spec C.2 | high | mitigate | The six codes are added in BOTH files in the same task (CONTEXT line 84); acceptance greps prove both. A code present in one file only is a canonicalization failure caught by the task's automated checks. |
| T-2-01-03 | Information Disclosure | fixture builders | low | accept | Fixtures contain secret-shaped test values only (never real keys); they live under tests/ and are never imported by src/ (D-21), so they cannot reach a bundle. Accepted: test-only exposure. |
| T-2-01-04 | Denial of Service | fake-indexeddb in test env | medium | mitigate | fake-indexeddb 6.2.5 is verified exit-0 with the jsdom-align env + threads pool (RESEARCH Pattern 1); the upgrade-abort landmine is avoided by the raw-open migrator pattern (02-06) — never idb openDB with a throwing upgrade. |
</threat_model>

<verification>
- `pnpm typecheck` clean (fixtures + types + codes compile together)
- `pnpm vitest run tests/fixtures tests/core/theme tests/core/workspace --reporter=dot` green (fixture determinism + no regression in existing suites)
- `pnpm eslint src/types tests/fixtures src/core/error/errorCodes.ts` clean; `pnpm prettier --check` on the changed files clean
- verify:phase-2 script exists and its chain is valid (full gate runs in 02-11)
</verification>

<success_criteria>
- Every later plan can import `@/types/storage`, the six canonical codes, and the six fixture builders without re-deriving them.
- The test harness opens real IndexedDB in the jsdom-align env with zero additional polyfills.
- No production code imports tests/fixtures (dependency direction enforced).
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-01-SUMMARY.md` when done.
</output>
