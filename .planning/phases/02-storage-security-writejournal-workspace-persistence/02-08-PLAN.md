---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 08
type: execute
wave: 4
depends_on: [02-05]
files_modified:
  - src/core/storage/Setting.ts
  - src/core/theme/ThemeStore.ts
  - tests/core/storage/Setting.test.ts
  - .planning/PRODUCT_SPEC_v0_1.md
autonomous: true
requirements: [STORAGE-02]
must_haves:
  truths:
    - "A failed chrome.storage.sync write to a cosmetic key (np_theme/np_theme_pack/np_language; quota or rate) catches, debugLogs SYNC_QUOTA_EXCEEDED, and writes the same key to chrome.storage.local as a shadow (D-15)."
    - "Reads are sync-first, then local; a local shadow wins on read AND triggers a re-attempt to write sync; a successful sync write deletes the local shadow — sync/local never silently diverge (D-15)."
    - "Cosmetic writes are debounced to avoid tripping the sync rate limit (120 writes/min)."
    - "ThemeStore persists np_theme/np_theme_pack THROUGH Setting.ts sync-first (rewire — D-15's live consumer, RESEARCH Pattern 5 landmine), keeping its read-validate idiom."
    - "The spec touch is applied: APPR-03's single-source invariant reworded from 'sync is the ONLY store' to 'sync is the CANONICAL/preferred store; local is a transient fallback shadow reconciled back to sync when possible.'"
  artifacts:
    - src/core/storage/Setting.ts (sync-shadow extension)
    - src/core/theme/ThemeStore.ts (persistence rewire)
    - tests/core/storage/Setting.test.ts (quota-shadow cases)
    - .planning/PRODUCT_SPEC_v0_1.md (APPR-03 one-line touch)
  key_links:
    - "Setting.ts sync-shadow ↔ ThemeStore.setMode/setPack (rewired through Setting)"
    - "local shadow ↔ sync promote/delete (reconciliation loop closes)"
---

<objective>
Wire the chrome.storage.sync quota/rate fallback (D-15) into Setting.ts — the shadow machinery (sync fail → local shadow, sync-first reads, promote-and-delete) — and rewire ThemeStore's np_theme/np_theme_pack persistence through Setting.ts so D-15 has a live consumer (RESEARCH Pattern 5: Phase-1 ThemeStore writes these to local; spec §15.1 says sync; the rewire closes the divergence, same precedent as D-06).

Purpose: Cosmetic settings (theme, language) must never silently stop syncing on quota/rate limits; sync and local must never diverge without reconciliation.
Output: Setting.ts shadow extension + ThemeStore rewire + quota-shadow tests (fixture from 02-01) + the one-line APPR-03 spec touch.

Flagged assumptions (specless probe):
- A-17: fakeBrowser does NOT enforce sync quotas — rejections must be mocked (RESEARCH Pattern 4); the quota-shadow fixture models both size and rate rejections under the same SYNC_QUOTA_EXCEEDED handling.
- A-18: The local write-rate limit is undocumented (RESEARCH A1) — debounce aggressiveness is agent discretion; only sync's documented constants (120/min) are hard-coded.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md D-15 (sync quota fallback full contract) and D-09 (Setting.ts owns the fallback machinery)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md Pattern 5 (sync-quota local shadow verified facts + the ThemeStore landmine + the APPR-03 spec touch)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md (Setting.ts adaptation notes — ThemeStore isValidMode read-validate idiom)
@src/core/storage/Setting.ts (02-05 — the permission table + serialized writes to extend)
@src/core/theme/ThemeStore.ts (lines 68-148 — init/setMode/setPack write chrome.storage.local directly; the rewire points)
@src/core/error/errorCodes.ts (SYNC_QUOTA_EXCEEDED, from 02-01)
@tests/fixtures/index.ts (quota-shadow builder, from 02-01)
@.planning/PRODUCT_SPEC_v0_1.md §17.1a APPR-03 (line ~1947 area — the one-line wording touch)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Setting.ts sync-shadow machinery (D-15)</name>
  <read_first>
    - 02-CONTEXT.md D-15 (full contract: catch both size and rate errors under SYNC_QUOTA_EXCEEDED, same-key local shadow, sync-first reads, shadow wins on read + triggers re-attempt, delete shadow on successful sync write, small debounce)
    - 02-RESEARCH.md Pattern 5 (verified facts: quota/rate rejection surfaces as a rejected promise; sync constants 8KB/item, 120/min; local rate undocumented — A1)
    - src/core/storage/Setting.ts (the module to extend — permission table + write mutex from 02-05)
  </read_first>
  <files>
    src/core/storage/Setting.ts
  </files>
  <action>
    Extend src/core/storage/Setting.ts with the D-15 sync-shadow machinery. Add:
    - `COSMETIC_SYNC_KEYS = ['np_theme', 'np_theme_pack', 'np_language']` const — the keys whose area is 'sync'.
    - `SYNC_KEYS_WITH_SHADOW`: a derived set — any key with area 'sync' in STORAGE_KEY_REGISTRY.
    - In settingWrite (or a wrapper `settingWriteSync<T>(key, value)`): for area 'sync' keys, attempt chrome.storage.sync.set first; on rejection (quota OR rate — both surface as rejected promises; catch broadly and log SYNC_QUOTA_EXCEEDED with the intent of THEME_STORAGE_UNAVAILABLE reuse per D-15), fall back to chrome.storage.local.set of the same key (the local shadow). A successful sync write deletes any local shadow for that key (chrome.storage.local.remove).
    - In settingRead (or `settingReadSync<T>(key, sanitize, fallback)`): read sync FIRST; if absent/undefined, read local; if the local shadow exists, it wins AND triggers a re-attempt to write sync (fire the sync write; on success delete the local shadow). Shadow presence = local value exists while sync does not.
    - Debounce: a module-level per-key debounce (small window, e.g. 100ms, agent discretion per RESEARCH A1) on cosmetic-key writes so rapid theme toggles do not trip the 120 writes/min sync limit; the last value always wins.
    - Header note: D-15 contract + "sync is CANONICAL/preferred; local is a transient fallback shadow" framing.
  </action>
  <acceptance_criteria>
    - `grep -c "COSMETIC_SYNC_KEYS" src/core/storage/Setting.ts` returns >= 1
    - `grep -c "SYNC_QUOTA_EXCEEDED" src/core/storage/Setting.ts` returns >= 1
    - `grep -c "shadow" src/core/storage/Setting.ts` returns >= 1 (shadow concept present)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "SYNC_QUOTA_EXCEEDED" src/core/storage/Setting.ts</automated>
  </verify>
  <done>Sync writes fall back to a local shadow under SYNC_QUOTA_EXCEEDED; reads are sync-first with promote-and-delete reconciliation.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire ThemeStore persistence through Setting.ts (sync-first + shadow)</name>
  <read_first>
    - 02-RESEARCH.md Pattern 5 landmine (Phase-1 ThemeStore writes np_theme/np_theme_pack to local; spec §15.1 says sync; D-15 needs a live consumer — the rewire is the recommended fix)
    - src/core/theme/ThemeStore.ts (lines 68-148 — init reads local + onChanged, setMode/setPack write local)
    - src/core/storage/Setting.ts (the sync-first API from Task 1)
    - 02-PATTERNS.md Shared Pattern 4 (remove-then-add onChanged listener — keep T-1-11)
  </read_first>
  <files>
    src/core/theme/ThemeStore.ts
  </files>
  <action>
    Modify src/core/theme/ThemeStore.ts per D-15 (same Phase-1-file-edit precedent as D-06):
    - init(): replace the direct `chrome.storage.local.get(STORAGE_KEYS)` with settingReadSync for np_theme and np_theme_pack (sync-first, local shadow fallback), keeping the isValidMode / isThemePackId read-validate idiom (T-1-10 — never trust raw storage).
    - setMode / setPack: replace the direct `chrome.storage.local.set` with settingWriteSync (sync-first; the shadow machinery handles quota/rate failure; the write must still never throw to the UI — keep the try/catch + debugLog(THEME_WRITE) shape).
    - onChanged listener: keep the remove-then-add pattern but broaden the area handling — a sync-area change for np_theme/np_theme_pack must ALSO propagate (the current handler ignores non-local areas); listen for both 'local' and 'sync' changes of these two keys with the same validation.
    - Header comment: D-15 rewire note — persistence flows through Setting.ts sync-first with local shadow; ThemeStore stays the read-validate owner.
  </action>
  <acceptance_criteria>
    - `grep -c "settingReadSync\|settingWriteSync" src/core/theme/ThemeStore.ts` returns >= 1 (rewired through Setting)
    - `grep -c "isValidMode" src/core/theme/ThemeStore.ts` returns >= 1 (read-validate idiom preserved)
    - `grep -c "chrome.storage.local.set" src/core/theme/ThemeStore.ts` returns 0 (direct local writes gone — all through Setting)
    - `grep -c "area !== 'local'" src/core/theme/ThemeStore.ts` returns 0 (the old local-only onChanged gate replaced)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "settingReadSync" src/core/theme/ThemeStore.ts</automated>
  </verify>
  <done>ThemeStore persists np_theme/np_theme_pack through Setting.ts sync-first; no direct chrome.storage.local.set remains in the file.</done>
</task>

<task type="auto">
  <name>Task 3: quota-shadow tests + the APPR-03 one-line spec touch</name>
  <read_first>
    - tests/fixtures/index.ts (buildQuotaShadowFixture — key, value, syncRejectError)
    - 02-RESEARCH.md Pattern 5 (fakeBrowser mocks sync area; quota NOT enforced — mock the set() rejection)
    - tests/core/storage/Setting.test.ts (02-05 conventions — extend, do not rewrite)
    - .planning/PRODUCT_SPEC_v0_1.md §17.1a APPR-03 (the single-source invariant line — find it via the 'sync is the ONLY' wording)
  </read_first>
  <files>
    tests/core/storage/Setting.test.ts
    .planning/PRODUCT_SPEC_v0_1.md
  </files>
  <action>
    Extend tests/core/storage/Setting.test.ts with quota-shadow cases (fixture: quota-shadow — D-15):
    1. Sync write fails (mock chrome.storage.sync.set to reject with a quota/rate error per the fixture) → settingWriteSync writes the SAME key to chrome.storage.local (shadow present) and logs SYNC_QUOTA_EXCEEDED.
    2. Read with a local shadow: seed local-only value + no sync value → settingReadSync returns the local (shadow wins) AND triggers a re-attempt to write sync.
    3. Reconciliation: after a successful sync write, the local shadow is deleted (chrome.storage.local no longer holds the key) — sync/local never diverge.
    4. Happy path: sync write succeeds → no local shadow ever created.

    Apply the one-line APPR-03 spec touch to .planning/PRODUCT_SPEC_v0_1.md §17.1a via a scoped Edit: reword the single-source invariant so 'sync is the CANONICAL/preferred store; local is a transient fallback shadow reconciled back to sync when possible' replaces the wording that claims sync is the only store. Do NOT touch any other line.
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/storage/Setting.test.ts --reporter=dot` passes (existing 02-05 cases + new quota-shadow cases)
    - `grep -c "SYNC_QUOTA_EXCEEDED" tests/core/storage/Setting.test.ts` returns >= 1
    - `grep -c "CANONICAL/preferred" .planning/PRODUCT_SPEC_v0_1.md` returns >= 1 (spec touch applied)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/storage/Setting.test.ts --reporter=dot && grep -c "CANONICAL/preferred" .planning/PRODUCT_SPEC_v0_1.md</automated>
  </verify>
  <done>Quota-shadow fallback proven (fail→shadow, shadow-wins read, promote-and-delete); the APPR-03 spec line reflects sync-canonical + local-shadow semantics.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol | Kind | Location |
|--------|------|----------|
| `COSMETIC_SYNC_KEYS` | const | src/core/storage/Setting.ts |
| `settingWriteSync<T>(key, value)` / `settingReadSync<T>(key, sanitize, fallback)` | fn (D-15 machinery) | src/core/storage/Setting.ts |
| per-key debounce for cosmetic writes | module-level helper | src/core/storage/Setting.ts |
| ThemeStore init/setMode/setPack/onChanged rewire | behavior change | src/core/theme/ThemeStore.ts |
| quota-shadow tests (4 cases) | test | tests/core/storage/Setting.test.ts |
| APPR-03 sync-canonical wording | spec edit | .planning/PRODUCT_SPEC_v0_1.md |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Setting sync writes → chrome.storage.sync | Quota/rate failures cross here — must fall back, never fail silently |
| local shadow → read path | A shadow winning a read must trigger reconciliation, not silent divergence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-08-01 | Availability | sync quota/rate failure silently drops cosmetic settings | high | mitigate | SYNC_QUOTA_EXCEEDED debugLog + local shadow on ANY sync rejection (both size and rate caught); the shadow wins reads so the value is never lost (D-15, test cases 1-2). |
| T-2-08-02 | Tampering | sync/local divergence after reconciliation | medium | mitigate | Promote-and-delete: a successful sync write removes the local shadow (test case 3) — the loop closes and the two areas cannot silently disagree. |
| T-2-08-03 | Denial of Service | rate-limit self-inflicted by rapid theme toggles | medium | mitigate | Per-key debounce on cosmetic writes keeps bursts under the documented 120 writes/min sync cap (D-15; RESEARCH A1 keeps local-rate aggressiveness at discretion). |
| T-2-08-04 | Information Disclosure | theme writes leaking via shadow to an unexpected area | low | accept | Theme keys are cosmetic (no secrets); the shadow lives in chrome.storage.local (extension-private). Accepted: minimal surface, no sensitive value involved. |
</threat_model>

<verification>
- `pnpm vitest run tests/core/storage/Setting.test.ts tests/core/theme/ThemeStore.test.ts` green (Setting extension + existing ThemeStore tests still pass after the rewire)
- `pnpm typecheck` clean; eslint/prettier clean on touched files
- APPR-03 spec line updated (grep-proven)
</verification>

<success_criteria>
- D-15 satisfied: sync-first cosmetic writes with local shadow fallback and reconciliation, no silent divergence, debounce respected.
- ThemeStore is the live D-15 consumer — spec §15.1's sync placement of theme keys is now honored.
- No regression in ThemeStore's existing read-validate + onChanged behavior.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-08-SUMMARY.md` when done.
</output>
