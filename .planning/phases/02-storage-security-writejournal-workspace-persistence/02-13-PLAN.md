---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 13
type: execute
wave: 8
depends_on: [02-10, 02-12]
files_modified:
  - package.json
  - .planning/phases/02-storage-security-writejournal-workspace-persistence/02-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CORE-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`verify:phase-2` names every live Phase 2 suite explicitly and carries a self-derived path-resolution preflight that exits non-zero naming any declared path that does not resolve (the `verify:phase-1` pattern, reused rather than reinvented)."
    - "The stale expectation is gone: `tests/core/utils` appears nowhere in `verify:phase-2`, and no `RateLimiter` suite exists to satisfy it (D2-26/D2-28)."
    - "The preflight has teeth: a temporarily misspelled suite path makes the gate fail with the path named, and restoring the path makes it green (observed, then restored)."
    - "The manifest gate runs inside the phase gate, so the `unlimitedStorage` addition is asserted by the same command that accepts the phase."
    - "`pnpm run build:ext` runs before the gate, because the manifest gate fails rather than skips when the built artifact is absent."
    - "The phase gate is green end to end after the build, and `verify:phase-1` still passes, so no completed phase regressed."
    - "The phase record states what was verified and what remains outstanding: WINDOWS #5, #7 and #8 stay `open` and deferred to the Phase 15 consolidated Real-Chrome cycle, with Phase 2 recorded as automated contract coverage only."
    - "D2-29's documentation follow-up is recorded with an owner: the PRODUCT_SPEC §18 Phase 2 Create-list inconsistency is not edited in Phase 2, the owner is named verbatim as `Phase 3 planner`, and the Phase 3 roadmap entry carries the inherited constraint that `Requester`/`RateLimiter` are Phase 3-owned under `CORE-03` per D2-26/D2-27."
  artifacts:
    - "package.json — corrected `verify:phase-2` script with the explicit suite list and the self-derived preflight"
    - "02-VALIDATION.md — completed sign-off block and observed row statuses"
    - ".planning/STATE.md — phase position/status updated for the completed phase, plus the D2-29 follow-up record with its owner"
    - ".planning/ROADMAP.md — the Phase 3 entry's inherited-constraints note (D2-29)"
  key_links:
    - "The preflight reads the script's own text from `package.json`, so a renamed or deleted suite is caught by the gate rather than silently ignored by Vitest (WINDOWS #21 records the silent-ignore defect the preflight exists to prevent)."
    - "The gate composes `tsc`, the preflight, the suites and `tests/isolation` (OQ-7), which is what forces the manifest constant and `wxt.config.ts` to move together."
  prohibitions:
    - "The gate must not be weakened to pass: no empty-directory requirement, no dummy test file, no removed suite, no watch flag, and no skipping of the manifest or isolation gates."
    - "No deferred window may be marked fixed, waived or closed by this phase."
---

<objective>
Correct the phase gate and take the phase's final evidence: an explicit, preflight-checked `verify:phase-2` and a green full run after the build.

Purpose: D2-28 corrects a gate that currently ignores three non-existent paths and exits zero — a defect the RESEARCH probe reproduced — and the phase is not done until the corrected gate is green against the real suites.
Output: the rewritten `verify:phase-2`, the observed preflight-teeth proof, a green full gate plus a green `verify:phase-1`, and the phase record with the outstanding verifications named.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@package.json
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-VALIDATION.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md
@.planning/STATE.md
@.planning/WINDOWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `verify:phase-2` with the explicit suite list and the self-derived preflight</name>
  <files>package.json</files>
  <read_first>
    - package.json (the existing `verify:phase-1` script — copy its `node -e` preflight expression shape verbatim in structure; and the defective `verify:phase-2` script that names `tests/core/utils`)
    - .planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md D2-28 (the corrected gate's requirements) and .planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md § Open Questions OQ-7, § Executed probe: the current `verify:phase-2` defect
    - .planning/phases/02-storage-security-writejournal-workspace-persistence/02-VALIDATION.md § Test Infrastructure and § Sampling Rate
  </read_first>
  <action>Rewrite the `verify:phase-2` script in `package.json` as: `tsc --noEmit`, then the self-derived path preflight, then `vitest run` over the explicit Phase 2 suite list, then the isolation suites — all joined with `&amp;&amp;` so any stage's failure fails the gate. The preflight must be the `verify:phase-1` pattern reused in structure: it reads `require('./package.json').scripts['verify:phase-2']`, extracts every `tests/...` and `scripts/...` token with the same regex shape, de-duplicates, exits 1 printing the unresolved paths when any `fs.existsSync` check fails, and otherwise prints a resolution count. Enumerate the Phase 2 suites as explicit file paths — `tests/core/security/KeyVault.test.ts`, `tests/core/security/EncryptedStorage.test.ts`, `tests/core/security/credentialStorePort.test.ts`, `tests/core/security/redactSensitive.test.ts`, `tests/core/storage/NowPilotDB.test.ts`, `tests/core/storage/IndexedDBMigrator.test.ts`, `tests/core/storage/ChatHistoryDB.test.ts`, `tests/core/storage/WriteJournal.test.ts`, `tests/core/storage/ErrorStore.test.ts`, `tests/core/storage/legacyChatMigration.test.ts`, `tests/core/storage/Setting.test.ts`, `tests/core/workspace/WorkspacePersistence.test.ts`, `tests/core/workspace/WriterElection.test.ts`, `tests/core/store/useExtensionStore.test.ts`, `tests/integration/workspaceHandoff.integration.test.ts`, `tests/integration/onboardingTwoSurface.integration.test.ts`, `tests/components/Phase2Notices.test.tsx` — plus the phase-touched component suites (`tests/components/SidePanelShell.test.tsx`, `tests/components/StandaloneShell.test.tsx`, `tests/components/MirrorBanner.test.tsx`, `tests/components/OnboardingFlow.test.tsx`) and the directory `tests/isolation`. Do not include `tests/core/utils` anywhere and do not create a `RateLimiter` suite. Then prove the preflight has teeth: temporarily misspell one enumerated path, run the gate's preflight stage alone and observe exit 1 naming that path, restore the correct path, and re-run to observe the resolution count printed. Record both observations in the summary.</action>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts['verify:phase-2'];const paths=[...new Set(s.match(/(?:tests\/[\w./-]+|scripts\/[\w.-]+)/g)||[])];const fs=require('fs');const missing=paths.filter(p=>!fs.existsSync(p));if(missing.length){console.error('verify:phase-2: unresolved declared path(s): '+missing.join(', '));process.exit(1)};const bad=s.includes('tests/core/utils')||s.includes('RateLimiter');if(bad){console.error('verify:phase-2 still declares the stale deferred surface');process.exit(1)};console.log('verify:phase-2 declares '+paths.length+' resolvable path(s)')"</automated>
    <fails_when>non-zero exit, a printed "unresolved declared path(s)" line naming any path, or a printed line about the stale deferred surface — the gate still points at a missing suite or at the deferred RateLimiter surface.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every enumerated path resolves on disk, and the list contains no directory-only entry for the new storage/security suites.
    - The script contains no `tests/core/utils` token and no RateLimiter file is created anywhere.
    - The preflight stage alone exits non-zero and names the path when one enumerated path is misspelled (observed and reported), and exits zero after restoration.
    - No `--watch`-style flag appears in the script.
  </acceptance_criteria>
  <done>`verify:phase-2` declares the live Phase 2 suites explicitly and fails loudly when any declared path is missing or misspelled.</done>
</task>

<task type="auto">
  <name>Task 2: Full gate run, phase-1 regression, and the phase record</name>
  <files>.planning/phases/02-storage-security-writejournal-workspace-persistence/02-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/02-storage-security-writejournal-workspace-persistence/02-VALIDATION.md (the Sampling Rate, the Manual-Only Verifications table and the Validation Sign-Off checklist)
    - .planning/STATE.md (the Current Position block, the Progress line, § Accumulated Context → Decisions and § Verification Deferrals)
    - .planning/ROADMAP.md (the Phase 3 entry that receives the D2-29 inherited-constraints note — do not touch the Phase 2 plan list)
    - .planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md D2-05, D2-26, D2-27, D2-28 and D2-29 (Phase 3's inherited credential-entry ownership contract, the deferral, Phase 3's inherited constraints, and the recorded source conflict)
    - .planning/WINDOWS.md (read-only — entries 5, 7, 8 and 22 must stay `open`)
  </read_first>
  <action>Run the phase gate in the required order: `pnpm run build:ext` first (the manifest gate fails, never skips, when `.output/chrome-mv3/manifest.json` is absent), then `pnpm run verify:phase-2`, then `pnpm run verify:phase-1` as the cross-phase regression, then `bash scripts/verify-no-tailwind.sh` and `node -e` assertion that the built manifest still carries exactly the four authorised permissions with `connect-src 'none'`. Record the observed results in `02-VALIDATION.md`: tick the Validation Sign-Off items that are genuinely true, fill each Per-Task Verification Map row's status with its observed result, and leave `nyquist_compliant` honest — set it true only if every sampled row passed; the Manual-Only row for the two 400 px backstops stays outstanding and routed to the Phase 15 cycle. Update `STATE.md`'s Current Position, Progress and Per-Plan metrics for the completed phase, and confirm § Verification Deferrals still records WINDOWS #5, #7 and #8 as deferred with the Phase 2 automated-coverage note from 02-12. If any gate stage is red, fix the defect it names in this task rather than weakening the gate; a red stage that cannot be fixed in scope is recorded as an explicit blocker in the summary, not silently accepted. Then record the D2-29 follow-up on the two planning surfaces it names. In `STATE.md` § Accumulated Context → Decisions, add a Phase 2 bullet that records the conflict (PRODUCT_SPEC §18's Phase 2 Create list names Requester and RateLimiter, while the Phase 2 ROADMAP goal and completion criteria require no outbound HTTP and the first production consumers belong to Phase 3), states that both modules are deferred to Phase 3 per D2-26, and names the documentation follow-up owner verbatim as `Phase 3 planner` — the canonical Product Specification is not edited in Phase 2. In `ROADMAP.md`, append to the Phase 3 entry an `**Inherited constraints (from Phase 2 D2-26/D2-29)**:` line stating that `src/core/http/Requester.ts` and `src/core/utils/RateLimiter.ts` are Phase 3-owned under requirement `CORE-03`, that the Phase 3 requirement/plan/task mapping must carry them together with D2-27's constraints (AbortController-based cancellation and timeout, no generic retries, per-provider limiter, no module-level mutable singleton), and that PRODUCT_SPEC §18's Phase 2 Create list is stale pending that follow-up — keep that existing line verbatim, because the gate's token assertion reads it. Alongside it, append a companion `**Inherited constraints (from Phase 2 D2-05)**:` line to the same Phase 3 entry recording Phase 3's credential-entry ownership contract: both approved credential-entry surfaces (onboarding and Options provider configuration) are wired by Phase 3 as transient input → real provider validation through the approved Requester and ProviderRouter → validation success → storage through the Phase 2 `CredentialStorePort` → transient input cleared → provider metadata records configured/validated state without the secret; on validation failure the credential is not persisted, a redacted canonical error is returned and the provider is not marked ready. In `STATE.md` § Accumulated Context → Decisions, record D2-05's Phase 3 inheritance on the same terms as the D2-26/D2-29 bullet (a companion bullet is fine), so the Phase 3 inheritance note stays accurate and non-vacuous. Do not edit `PRODUCT_SPEC.md`, do not renumber or reword the Phase 2 plan list, and do not mark any window fixed or waived.</action>
  <verify>
    <automated>pnpm run build:ext &amp;&amp; pnpm run verify:phase-2 &amp;&amp; pnpm run verify:phase-1 &amp;&amp; node -e "const fs=require('fs');const r=fs.readFileSync('.planning/ROADMAP.md','utf8');const s=fs.readFileSync('.planning/STATE.md','utf8');const p=fs.readFileSync('.planning/product/PRODUCT_SPEC.md','utf8');const needR=['Inherited constraints (from Phase 2 D2-26/D2-29)','Requester','RateLimiter','CORE-03'];const needS=['D2-29','Requester','RateLimiter','Phase 3 planner'];const needP=['src/core/utils/RateLimiter.ts','src/core/http/Requester.ts'];const mr=needR.filter(t=>!r.includes(t));const ms=needS.filter(t=>!s.includes(t));const mp=needP.filter(t=>!p.includes(t));if(mr.length||ms.length||mp.length){console.error('D2-29 record missing — roadmap: '+mr.join(', ')+' | state: '+ms.join(', ')+' | spec left stale: '+mp.join(', '));process.exit(1)};console.log('D2-29 follow-up recorded with owner Phase 3 planner; PRODUCT_SPEC left stale')"</automated>
    <fails_when>non-zero exit at any stage, a Vitest failing count in the output, a "generated manifest not found" error (the build did not run first), a preflight "unresolved declared path(s)" line, or a printed "D2-29 record missing" line naming a token absent from `ROADMAP.md` or `STATE.md` — the decision's follow-up or its owner was not recorded.</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm run verify:phase-2` is green after `pnpm run build:ext`, and `pnpm run verify:phase-1` is green in the same run.
    - The built manifest assertion confirms the four authorised permissions and the unchanged CSP.
    - `02-VALIDATION.md`'s sign-off block reflects observed truth, with the Manual-Only backstop row still outstanding.
    - `STATE.md` records the phase position and the deferred windows unchanged.
    - `STATE.md` names the D2-29 follow-up owner verbatim as `Phase 3 planner`, and `ROADMAP.md`'s Phase 3 entry carries the `Inherited constraints (from Phase 2 D2-26/D2-29)` line mapping Requester/RateLimiter to Phase 3 / `CORE-03`.
    - `ROADMAP.md`'s Phase 3 entry also carries the companion `Inherited constraints (from Phase 2 D2-05)` line naming Phase 3's credential-entry ownership contract (transient input → real provider validation through Requester/ProviderRouter → storage through `CredentialStorePort` → transient input cleared; no persistence and no provider-ready state on validation failure), and `STATE.md` § Accumulated Context → Decisions records the same D2-05 inheritance alongside the D2-26/D2-29 bullet — so the Phase 3 inheritance note names D2-05 explicitly rather than being vacuous.
    - `PRODUCT_SPEC.md` is left deliberately stale rather than silently patched: its §18 Phase 2 Create list still names `src/core/utils/RateLimiter.ts` and `src/core/http/Requester.ts` (asserted by content, not by an empty diff), and the Phase 2 plan list in `ROADMAP.md` is untouched.
    - Any red stage that could not be fixed is named as a blocker in the summary rather than absorbed.
  </acceptance_criteria>
  <done>The corrected phase gate is green end to end after the build, `verify:phase-1` still passes, and the phase record states what remains outstanding — including the Phase 3 inheritance record naming D2-05 alongside D2-26/D2-29.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate script → verification claim | A gate that passes without resolving its paths produces a false green. |
| Build artifact → manifest gate | The gate reads a generated file; a missing artifact must fail, never skip. |
| Phase record → release decision | The record is what later gates read about outstanding work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-68 | Repudiation | gate silently ignoring paths | critical | mitigate | The self-derived preflight resolves every declared path and exits non-zero naming the unresolved ones; the teeth observation is recorded. |
| T-02-69 | Repudiation | weakened gate | high | mitigate | No dummy suite, no directory-only substitution for the new suites, no watch flag; the assertion rejects the stale `tests/core/utils` token. |
| T-02-70 | Tampering | manifest drift | high | mitigate | The manifest gate and the isolation suites run inside the phase gate after the build, and a `node -e` assertion re-checks the built artifact. |
| T-02-71 | Repudiation | over-claimed closure | high | mitigate | The Manual-Only row stays outstanding, no window changes status, and the record states automated coverage only. |
| T-02-72 | Denial of Service | build-order failure | low | mitigate | The gate is documented and run after `build:ext`; a missing artifact fails loudly with the build command named. |
| T-02-SC | Tampering | npm installs | high | mitigate | No install in this plan. |
</threat_model>

## Artifacts this phase produces (this plan's subset)

| File | Symbols created |
|---|---|
| `package.json` | corrected `verify:phase-2` script (explicit suite list + self-derived preflight) |
| `02-VALIDATION.md` | completed sign-off block and observed row statuses |
| `.planning/STATE.md` | phase position, progress and metrics updated; the D2-29 follow-up record with its owner (`Phase 3 planner`) |
| `.planning/ROADMAP.md` | the Phase 3 entry's `Inherited constraints (from Phase 2 D2-26/D2-29)` note (Phase 2 plan list untouched) |

<verification>
- `pnpm run build:ext` → `pnpm run verify:phase-2` → `pnpm run verify:phase-1` all green in one run.
- The preflight teeth observation (red on a misspelling, green after restore) is recorded in the phase summary.
- The D2-29 token assertion passes, `PRODUCT_SPEC.md`'s §18 Phase 2 Create list still names `RateLimiter`/`Requester` (the spec is left stale on purpose), and `ROADMAP.md`'s Phase 2 plan list is byte-unchanged.
- No window status changed; the Manual-Only backstop row remains outstanding.
</verification>

<success_criteria>
- The corrected gate is the phase's acceptance instrument and is green against the live suites.
- Cross-phase regression is proved by `verify:phase-1` passing.
- The phase record is accurate about what was verified automatically and what remains for the Phase 15 cycle.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-13-SUMMARY.md` when done.
</output>
