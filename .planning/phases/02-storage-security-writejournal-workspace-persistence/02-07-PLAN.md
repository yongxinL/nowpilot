---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 07
type: execute
wave: 4
depends_on: [02-02, 02-04, 02-05, 02-06]
files_modified:
  - src/core/workspace/WorkspaceStore.ts
  - src/entrypoints/sidepanel/main.tsx
  - src/entrypoints/standalone/main.tsx
  - src/components/chat/SidepanelChat.tsx
  - src/components/common/MirrorBanner.tsx
  - tests/core/workspace/WorkspacePersistence.test.ts
autonomous: true
requirements: [REQ-R03]
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WorkspaceStore.isPrimaryWriter() is a pure read of the election result (state 'primary' or 'solo') — the Phase-1 stub is gone (D-24, the D-16 swap point)."
    - "WorkspaceStore's persist config uses the journalingAdapter and the canonical `np_workspace` key; a legacy `np_workspace_store` blob is lifted once (plan 02-05 adapter behavior) — no message loss on reload (ROADMAP success criterion 5)."
    - "Workspace state persists across a simulated page reload (re-hydrate from chrome.storage.local) and across cross-surface handoff with no message loss (ROADMAP success criterion 5)."
    - "Primary surfaces journaled-persist np_workspace; secondary surfaces skip persist entirely (mirror only — D-27) and their mirror state derives from inbound WORKSPACE_UPDATED/WORKSPACE_HEARTBEAT."
    - "recoverJournal() runs on sidepanel/standalone boot and replays any pending/applying update-workspace entries before the store hydrates (D-31)."
    - "The D-22 300ms trailing debounce, election heartbeats (20/min session writes, NOT debounced), and immediate journal-entry writes compose under the ≤30 writes/min steady-state assertion (D-43 / REQ-R03)."
    - "Side Panel mirroring triggers on election-secondary state (handoff OR election demotion) with the Phase-1 MirrorBanner visual contract unchanged (UI-SPEC Component Inventory — wiring only); 'Refocus here' triggers a CAS re-election."
    - "Options boot decrypt-on-read: `hydrateProviderSecrets()` merges decrypted np_providers values into the in-memory config (empty-field-only, read-only), so a fresh boot renders the masked placeholder and an untouched `Save Provider` preserves the stored key — never re-encrypts '' (checker key-link: the concrete read-path function; RESEARCH Pattern 3 read path)."
  artifacts:
    - "src/core/workspace/WorkspaceStore.ts — isPrimaryWriter pure read, journalingAdapter storage, np_workspace persist name"
    - "src/entrypoints/{sidepanel,standalone}/main.tsx — boot sequences and adapter error-reporter registration. Do not create an Options entrypoint. Provider migration/hydration is owned by the established Standalone bootstrap or existing Options route initialization discovered in the repository."
    - "src/components/chat/SidepanelChat.tsx, src/components/common/MirrorBanner.tsx — wiring-only changes (no visual change)"
    - "tests/core/workspace/WorkspacePersistence.test.ts (spec-required)"
  key_links:
    - "WorkspaceStore.isPrimaryWriter ↔ WorkspaceElection (plan 02-06) ↔ journalingAdapter gate (plan 02-05)"
    - "journalingAdapter ↔ np_workspace key ↔ chromeStorageAdapter debounce ↔ D-43 write-rate budget"
    - "boot wiring ↔ recoverJournal (WriteJournalDB) ↔ IndexedDBMigrator.bootstrap ↔ migrateProviderSecrets + hydrateProviderSecrets (plan 02-02 decrypt-on-read)"
  assumptions:
    - "REQ-R03 boundary (probe row): the debounce thresholds are D-22 frozen — STORAGE_DEBOUNCE_MS = 300ms trailing, flush on beforeunload/visibilitychange, no leading edge, timer reset per write; Phase 2 verifies interplay only (D-43 — no re-open)."
    - "REQ-R03 precision (probe row): the write-rate budget is exact — heartbeat session writes (1 per 3s = 20/min) + journal-entry writes (immediate, sparse) + debounced np_workspace persists (coalesced) must hold ≤30 writes/min steady-state; asserted by test."
    - "A3 (RESEARCH): the journalingAdapter is the composition point — already built in plan 02-05; this plan installs it into the persist config."
    - "Boot order per RESEARCH Open Question 3 recommendation: sidepanel/standalone = IndexedDBMigrator bootstrap for required databases → recoverJournal for update-workspace entries → hydrate WorkspaceStore → start exactly one WorkspaceElection instance → register cleanup on surface unload; provider migration/hydration (migrateProviderSecrets → hydrateProviderSecrets decrypt-on-read, plan 02-02 → encrypted providers read) is owned by the established Standalone bootstrap or existing Options route initialization discovered in the repository."
  prohibitions:
    - "Re-opening or modifying the D-22 debounce (D-43 — verification only; STORAGE_DEBOUNCE_MS stays 300)"
    - "User-facing copy for STORAGE_QUOTA / STORAGE_RATE_LIMIT / IDB_MIGRATION_FAILED (UI-SPEC — ErrorStore + debugLog only; the modal must not claim false success)"
    - "Bare-verb CTA labels in the provider modal (Save Provider / Check Connection — shipped in plan 02-02, not reverted here)"
    - "Secondary surfaces writing np_workspace (election-gated persist — D-27)"
    - "New `@ts-expect-error NP-STRICT` markers (Pitfall 9)"
---

<objective>
Workspace persistence integration: WorkspaceStore's election-gated journaled persist (the phase's end-to-end tracer slice), per-surface boot wiring (migrator bootstrap → recovery → election / provider migration-hydration), the MirrorBanner wiring-only trigger broadening, and the spec-required WorkspacePersistence integration test.

Purpose: ROADMAP success criterion 5 — workspace persists across reload and cross-surface handoff with no message loss; D-27/D-31/D-34 make the journaled persist the real production path (not test-only); RESEARCH Open Question 3's boot wiring makes recovery + election + secret migration run on every surface boot. This plan closes the loop the earlier plans built piecewise: election (02-06) × journal/adapter (02-05) × secrets migration (02-02) × IDB framework (02-04).
Output: the working, integrated persist path + the WorkspacePersistence test that gates the whole phase (verify:phase-2).
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md
@src/core/workspace/WorkspaceStore.ts
@src/core/workspace/WorkspaceSync.ts
@src/core/workspace/WorkspaceElection.ts
@src/core/workspace/journalingAdapter.ts
@src/core/theme/chromeStorageAdapter.ts
@src/entrypoints/sidepanel/main.tsx
@src/entrypoints/standalone/main.tsx
@src/components/chat/SidepanelChat.tsx
@src/components/common/MirrorBanner.tsx
@tests/core/workspace/WorkspaceStore.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: WorkspaceStore integration — isPrimaryWriter pure read + journaled np_workspace persist + first persistence test</name>
  <files>src/core/workspace/WorkspaceStore.ts, tests/core/workspace/WorkspacePersistence.test.ts</files>
  <read_first>
    - src/core/workspace/WorkspaceStore.ts (the file being modified — isPrimaryWriter stub at 18-20, persist config at 147-171, workspaceMigrate at 77-82)
    - 02-PATTERNS.md lines 420-443 (WorkspaceStore modification spec: isPrimaryWriter swap + journalingAdapter storage swap + persist config to preserve)
    - src/core/workspace/WorkspaceElection.ts + src/core/workspace/journalingAdapter.ts (plan 02-05/02-06 outputs this task consumes)
    - tests/core/workspace/WorkspaceStore.test.ts (test conventions: beforeEach reset via store actions, storage map clearing)
  </read_first>
  <behavior>
    - Test 1 (happy-path tracer): setWorkspaceId on a primary surface → the np_workspace key is written through the journaled adapter (a 'pending' entry appears in WriteJournalDB, then 'completed') → a simulated reload (in-memory reset + rehydrate from the storage map) hydrates the same workspaceId — no message loss.
    - Test 2 (predicate): isPrimaryWriter() === true when the election state is 'primary' or 'solo', false when 'secondary'.
  </behavior>
  <action>
    Modify `src/core/workspace/WorkspaceStore.ts`:
    1. Replace the `isPrimaryWriter()` stub (lines 18-20) with a delegation: `import { isPrimaryWriter as electionIsPrimaryWriter } from './WorkspaceElection'` and `export function isPrimaryWriter(): boolean { return electionIsPrimaryWriter(); }` — a pure read of the in-memory election result (D-24). Keep the export signature identical (every later phase's write gating reads through it).
    2. Replace the persist `storage:` (line 155): `storage: createJSONStorage(() => createJournalingAdapter({ inner: chromeStorageAdapter, isPrimary: isPrimaryWriter, putEntry: (e) => openWriteJournalDB().then(db => db.put('entries', e)), persistEntry: (e) => openWriteJournalDB().then(db => db.put('entries', e)), emitUpdate: notifyWorkspaceUpdate }))` — the exact dependency injection is planner/executor discretion within this signature; the point is the adapter from plan 02-05 is installed as the store's storage.
    3. Change the persist `name` (line 148) from 'np_workspace_store' to 'np_workspace' (the canonical key per PATTERNS Workspace-key migration rule; the one-time legacy lift lives in the adapter's getItem — plan 02-05).
    4. Keep `partialize`, `version: 1`, and `workspaceMigrate` unchanged (no schema change; D-22 version axis untouched).
    5. Add a code comment at the swap point (replacing the Phase-1 SWAP POINT comment at lines 9-17) documenting that the election is live and `isPrimaryWriter()` is a pure read.

    Create `tests/core/workspace/WorkspacePersistence.test.ts` starting with the two behaviors above (RED first). Use the fake-indexeddb + session mocks from plan 02-01; start the election (or stub its state to 'solo') so the persist path is primary; simulate reload by resetting the in-memory store and calling the persist rehydrate against the storage map.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/workspace/WorkspacePersistence.test.ts</automated>
  </verify>
  <done>
    The happy-path persist→journal→reload-hydrate slice works end-to-end (journal entry pending→completed, np_workspace written, rehydrate restores state); isPrimaryWriter delegates to the election module.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/workspace/WorkspacePersistence.test.ts` exits 0 with the tracer behaviors passing.
    - Source assertion: `grep -n "return true" src/core/workspace/WorkspaceStore.ts` matches nothing inside isPrimaryWriter (the stub is gone); `grep -n "np_workspace_store" src/core/workspace/WorkspaceStore.ts` matches nothing (canonical key only).
    - Existing WorkspaceStore.test.ts still green (no regression from the persist-config change).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Boot wiring — migrator bootstrap → recoverJournal → election (sidepanel/standalone), provider migration/hydration, adapter reporter registration, MirrorBanner trigger</name>
  <files>src/entrypoints/sidepanel/main.tsx, src/entrypoints/standalone/main.tsx, src/components/chat/SidepanelChat.tsx, src/components/common/MirrorBanner.tsx</files>
  <read_first>
    - src/entrypoints/sidepanel/main.tsx, src/entrypoints/standalone/main.tsx (the files being modified — current bootstrap shape, useWorkspaceStore usage)
    - 02-RESEARCH.md lines 136-153 (boot sequence diagram: IndexedDBMigrator bootstrap for required databases → recoverJournal for update-workspace entries → hydrate WorkspaceStore → start exactly one WorkspaceElection instance → register cleanup on surface unload → migrateProviderSecrets), 651-660 (Open Question 3 recommendation), 137-150 (system architecture flow)
    - src/components/chat/SidepanelChat.tsx lines 195-244 (demotion on WORKSPACE_HANDOFF — the trigger to broaden)
    - src/components/common/MirrorBanner.tsx (visual contract frozen — do not modify visuals)
    - 02-CONTEXT.md D-25 (one election instance per surface, dispose on unload), D-31 (recoverJournal on surface boot), D-39 (reporter ownership)
  </read_first>
  <action>
    Wire per-surface boot sequences (async, ordered, each step awaited):
    - `src/entrypoints/sidepanel/main.tsx` and `src/entrypoints/standalone/main.tsx`: (1) `await IndexedDBMigrator.bootstrap()` for required databases; (2) `await recoverJournal(loadEntriesFromWriteJournalDB, replayEntry)` — load pending/applying entries from WriteJournalDB and replay only supported operations ('update-workspace'); unsupported ops are skipped with a `debugLog` line (D-32 replay contract); (3) hydrate WorkspaceStore (rehydrate from chrome.storage.local after recovery); (4) create exactly one WorkspaceElection instance via `startElection(surface)` during WorkspaceStore bootstrap and dispose it on surface unload (D-25 one-instance rule); (5) register the adapter error reporter ONCE: `setStorageErrorReporter((entry) => { void ErrorStore.record({ code: entry.code, message: entry.message, context: entry.context }); debugLog(entry.code, entry.message); })` (D-39 ownership — the adapter emits, this wiring records exactly one entry).
    - Provider migration/hydration is owned by the established Standalone bootstrap or existing Options route initialization discovered in the repository (no new Options entrypoint): (1) `await IndexedDBMigrator.bootstrap()`; (2) `await migrateProviderSecrets()` (from useExtensionStore — plan 02-02; idempotent — a legacy np_store with plaintext keys migrates once, re-runs are no-ops); (3) `await hydrateProviderSecrets()` (from useExtensionStore — plan 02-02; the decrypt-on-read boot step: reads np_providers, derives the key per non-empty secret field via KeyVault, decrypts, and merges into the in-memory config empty-field-only — read-only, never writes storage. This is what makes the masked placeholder render and the untouched-save path preserve the stored key after a reload — RESEARCH Pattern 3 read path); (4) register the same adapter error reporter; (5) providers read through the encrypted np_providers path (no election needed — the Options surface does not write workspace state, per RESEARCH Open Question 3).
    - `src/components/chat/SidepanelChat.tsx`: broaden the mirror trigger from WORKSPACE_HANDOFF-only to 'side panel is secondary' — show the mirror state when the election reports secondary (handoff OR election demotion, D-24/D-26). Use the election state via a small subscription (the WorkspaceElection module's inbound-handler already updates state; the component reads `getState()` and re-renders on change). Keep the existing `mirrored` visual behavior; do not change MirrorBanner's visuals.
    - `src/components/common/MirrorBanner.tsx`: wiring only — the `onRefocus` handler (passed from SidepanelChat) triggers a CAS re-election on `np_workspace_primary` (attempt to become primary again). No JSX/style change (UI-SPEC: visual contract frozen).
    - No new UI copy, no storage-error toasts (UI-SPEC Copywriting Contract — debug-only surfacing).
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/workspace/WorkspacePersistence.test.ts tests/core/storage/WriteJournal.test.ts tests/core/security/secrets-inspection.test.ts</automated>
  </verify>
  <done>
    The sidepanel/standalone entrypoints run their boot sequences and provider migration/hydration (including hydrateProviderSecrets — decrypt-on-read) is owned by the established Standalone bootstrap or existing Options route initialization discovered in the repository; the adapter reporter is registered once per surface; SidepanelChat mirrors on election-secondary; MirrorBanner visuals byte-identical (wiring-only); tsc passes across entrypoints; the secrets reload test stays green through the boot wiring.
  </done>
  <acceptance_criteria>
    - `pnpm lint` exits 0 (the boot wiring compiles against the Phase-1 entrypoint shells).
    - Boot-level decrypt-on-read contract: after a simulated reload (in-memory config reset, np_store stripped, np_providers holding ciphertext), `hydrateProviderSecrets()` populates in-memory `config.providers.*.apiKey` — asserted by the reload test in tests/core/security/secrets-inspection.test.ts (plan 02-02 Task 2 Test 5), which the verify above runs: the masked placeholder renders (02-02 Task 3 AC-3), an untouched `Save Provider` preserves the stored key (persistProviderConfigEncrypted reuses the existing ciphertext — never re-encrypts ''), `handleCheckConnection` reads the decrypted in-memory key, and `migrateProviderSecrets()` remains idempotent.
    - Source assertion: `grep -R -n "hydrateProviderSecrets" src/entrypoints src/components/options src/store` matches (the decrypt-on-read step is wired into a bootstrap path); `git diff --stat src/components/common/MirrorBanner.tsx` shows only prop/type-level or zero changes (visual contract intact); `grep -n "setStorageErrorReporter" src/entrypoints/*/main.tsx` matches in the sidepanel/standalone entrypoints.
    - Source assertion: exactly one `startElection` call per sidepanel/standalone entrypoint.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WorkspacePersistence expansion — reload, handoff, election-gated mirror skip, journal recovery, write-rate budget</name>
  <files>tests/core/workspace/WorkspacePersistence.test.ts</files>
  <read_first>
    - tests/core/workspace/WorkspacePersistence.test.ts (the file being extended — Task 1 slice)
    - 02-VALIDATION.md lines 41-57 (per-task verification map: 02-05-02 handoff no-message-loss, 02-05-01 election-gated persist + mirror skip, write-rate assertion)
    - 02-RESEARCH.md lines 155-164 (D-34 flow), 612-615 (D-43 write-rate budget shared pattern)
    - tests/setup.ts (BroadcastChannel mock + __broadcast helper for cross-surface simulation)
  </read_first>
  <behavior>
    - Test 1 (reload): primary persist → reset in-memory → rehydrate from the storage map → the workspace state (workspaceId/conversationId) is identical — no message loss (success criterion 5).
    - Test 2 (handoff): standalone (primary) and sidepanel (secondary) surfaces — standalone's WORKSPACE_UPDATED reaches the sidepanel via the BroadcastChannel; the sidepanel's mirror state updates; a subsequent sidepanel setItem creates no journal entry and no storage write (election-gated, D-27).
    - Test 3 (journal recovery): a leftover 'pending' update-workspace entry in WriteJournalDB is replayed by recoverJournal on boot → np_workspace restored (crash recovery on a production path — D-31).
    - Test 4 (write-rate budget): over a simulated steady-state window, the combined session-write count (heartbeats 1/3s) + journal-entry writes + debounced np_workspace persists stays ≤ 30 writes/min (D-43 / REQ-R03 precision contract).
  </behavior>
  <action>
    Extend `tests/core/workspace/WorkspacePersistence.test.ts` with the four behaviors:
    - Use the BroadcastChannel mock's multi-instance semantics + the `__broadcast` helper to simulate two surfaces (standalone + sidepanel) with separate election instances.
    - Election-gated mirror skip: assert that a secondary surface's WorkspaceStore setItem produces neither a WriteJournalDB entry nor an np_workspace storage write.
    - Journal recovery: write a synthetic 'pending' entry directly into WriteJournalDB, run `recoverJournal` with the real update-workspace replay, assert np_workspace is restored.
    - Write-rate budget: with `vi.useFakeTimers()`, count `chrome.storage.session.set` invocations (heartbeat ticks) + WriteJournalDB puts + debounced local writes over a simulated 60s window; assert ≤ 30.
    - Keep every test hermetic: `__resetIndexedDB()`, `__chromeSessionMap.clear()`, `__chromeStorageMap.clear()`, and the adapter `__test__.resetPendingState()` in beforeEach.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/workspace/WorkspacePersistence.test.ts</automated>
  </verify>
  <done>
    All four expansion behaviors green; the full WorkspacePersistence test suite (Task 1 + Task 3) passes; `pnpm run verify:phase-2` (tsc + tests/core/storage + tests/core/security + tests/core/utils + tests/core/workspace) is green end-to-end.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/workspace/WorkspacePersistence.test.ts` exits 0 with the 4 expansion behaviors passing.
    - `pnpm run verify:phase-2` exits 0 — the full phase gate (tsc --noEmit + all Phase-2 test dirs) is green.
    - The write-rate test asserts a numeric bound ≤ 30/min (source assertion — no vacuous test).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `src/core/workspace/WorkspaceStore.ts` — `isPrimaryWriter()` (pure read, delegates to WorkspaceElection), persist `storage:` → journalingAdapter, persist `name: 'np_workspace'`
- `src/entrypoints/sidepanel/main.tsx`, `src/entrypoints/standalone/main.tsx` — boot: IndexedDBMigrator.bootstrap → recoverJournal → hydrate WorkspaceStore → startElection(surface) → setStorageErrorReporter; dispose on unload
- Provider migration/hydration (migrateProviderSecrets → hydrateProviderSecrets decrypt-on-read → encrypted providers read) is owned by the established Standalone bootstrap or existing Options route initialization discovered in the repository (no new Options entrypoint)
- `src/components/chat/SidepanelChat.tsx` — mirror trigger broadened to election-secondary (wiring only)
- `src/components/common/MirrorBanner.tsx` — onRefocus → CAS re-election (wiring only, visuals frozen)
- `tests/core/workspace/WorkspacePersistence.test.ts` — reload, handoff, election-gated mirror skip, journal recovery, write-rate budget

## Flagged Assumptions (spec-less probe fallback)

- **REQ-R03 boundary (probe row):** D-22 debounce thresholds are frozen (300ms trailing + flush hooks, no leading edge, timer reset per write) — Phase 2 verifies interplay only (D-43).
- **REQ-R03 precision (probe row):** the write-rate budget is exact — heartbeat (20/min) + journal entries (immediate, sparse) + debounced persists (coalesced) ≤ 30/min steady-state, asserted numerically.
Both surfaced in frontmatter `must_haves.assumptions`.

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorkspaceStore persist → journalingAdapter → chrome.storage.local / WriteJournalDB | the election-gated, journaled write path |
| primary surface ↔ secondary surface (BroadcastChannel) | mirror state propagation; secondary must not write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Integrity | secondary surface overwriting primary state (write conflict) | medium | mitigate | election-gated persist: non-primary setItem no-ops (D-27); mirror-only semantics asserted in the integration test |
| T-02-07-02 | Availability | workspace state loss on reload / handoff / SW suspension | high | mitigate | journaled persist (pending→completed) + recoverJournal on boot + legacy-key lift; reload and handoff paths asserted with no message loss (success criterion 5) |
| T-02-07-03 | Integrity | legacy np_workspace_store blob divergence after the key rename | low | mitigate | one-time lift (read→write→verify→delete) idempotent and adapter-owned (plan 02-05); canonical np_workspace key only |
| T-02-07-04 | Information Disclosure | storage errors surfaced to the user UI | low | mitigate | UI-SPEC contract: STORAGE_QUOTA/STORAGE_RATE_LIMIT/IDB_MIGRATION_FAILED → ErrorStore + debugLog only; the provider modal never claims false success |
| T-02-07-05 | Denial of Service | write-rate blowup exceeding chrome.storage throttle | medium | mitigate | D-43 budget: heartbeat 20/min + journal immediate + debounce 300ms; ≤30/min steady-state assertion in the integration test |
</threat_model>

<verification>
- Automated: `pnpm lint && pnpm exec vitest run tests/core/workspace/WorkspacePersistence.test.ts` then the full gate `pnpm run verify:phase-2` (tsc --noEmit + tests/core/storage + tests/core/security + tests/core/utils + tests/core/workspace).
- Stub audit: `grep -n "return true" src/core/workspace/WorkspaceStore.ts` — the Phase-1 stub is gone.
- Visual freeze: MirrorBanner.tsx diff is wiring-only (no JSX/style changes).
</verification>

<success_criteria>
- Workspace persists across reload + cross-surface handoff with no message loss (success criterion 5).
- Secondary surfaces mirror only; primary surfaces journaled-persist through the real production path (D-27/D-31).
- Boot wiring runs IndexedDBMigrator bootstrap → recoverJournal → hydrate WorkspaceStore → election (sidepanel/standalone); provider migration/hydration is owned by the established Standalone bootstrap or existing Options route initialization discovered in the repository.
- `pnpm run verify:phase-2` green — the phase's verification gate closes.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-07-SUMMARY.md` when done
</output>