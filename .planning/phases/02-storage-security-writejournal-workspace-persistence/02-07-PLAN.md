---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 07
type: execute
wave: 3
depends_on: [02-01]
files_modified:
  - src/core/storage/ChatHistoryDB.ts
  - src/core/storage/NotesDB.ts
  - src/core/storage/MemoryDB.ts
  - tests/core/storage/ChatHistoryDB.test.ts
  - tests/core/storage/NotesDB.test.ts
  - tests/core/storage/MemoryDB.test.ts
autonomous: true
requirements: [STORAGE-01]
must_haves:
  truths:
    - "ChatHistoryDB, NotesDB, and MemoryDB open via idb with strict DBSchema typing (RESEARCH Pattern 1) and expose typed CRUD (STORAGE-01)."
    - "Data models follow §21 verbatim: ChatSession/ChatMessage (§21.1), Note + concepts (§21.2), MemoryMessage (keyPath [conversationId, seq]) + Fact + conversationSummaries (§21.3/§21.4)."
    - "Message bodies live in IndexedDB ONLY — never in chrome.storage.local (§0.2, Pitfall 4)."
    - "NotesDB exports getNoteByTitle() (§15.1)."
    - "Every store declares a numeric DB_VERSION; initial opens use a non-throwing idb openDB upgrade (no migration history yet — RESEARCH note on the migrator happy path)."
  artifacts:
    - src/core/storage/ChatHistoryDB.ts
    - src/core/storage/NotesDB.ts
    - src/core/storage/MemoryDB.ts
    - tests/core/storage/ChatHistoryDB.test.ts
    - tests/core/storage/NotesDB.test.ts
    - tests/core/storage/MemoryDB.test.ts
  key_links:
    - "stores ↔ IndexedDBMigrator (02-06): future schema changes register a DBVersionMigration; initial opens stay plain"
    - "MemoryDB messages keyPath [conversationId, seq] ↔ §20.2 idempotency key (save memory body = conversationId + seq)"
    - "stores ↔ ErrorStore (02-06): migration failures on these DBs degrade + record"
---

<objective>
Deliver the three message-body IndexedDB stores (STORAGE-01): ChatHistoryDB (§21.1 sessions + messages), NotesDB (§21.2 notes + concepts), and MemoryDB (§21.3/§21.4 messages/userFacts/conversationSummaries) — all via idb with strict DBSchema typing, keyPaths and indexes per §15.1, and typed CRUD.

Purpose: Message bodies and note content must live in IndexedDB, never chrome.storage.local (10MB quota + §0.2 forbids bodies in local). These are the stores Phase 3 (chat/memory) and Phase 5 (notes/knowledge) build on.
Output: three typed stores + unit tests.

Flagged assumptions (specless probe):
- A-15: No message body ever reaches chrome.storage.local — these stores are the ONLY home for bodies; the phase-11 privacy gate re-asserts the invariant on the whole src/ tree.
- A-16: Store internal keyPaths/indexes follow §15.1 + §21 verbatim (agent discretion limited to index naming choices like 'by-session'/'by-timestamp' per RESEARCH Pattern 1).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md §15.1 (lines 1950-1974 — store layouts), §21.1/§21.2/§21.3/§21.4 (lines 3325-3434 — data models verbatim), §0.2 (lines 80-110 — bodies never in local)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md D-12/D-14 (stores register DB_VERSION with the migrator later)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md Pattern 1 (DBSchema + openDB verified template)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md (four-IDB-store adaptation notes)
@src/types/storage.ts (02-01 — WriteJournalEntry already there; note: ChatSession/ChatMessage/Note/Fact co-locate with their stores per PATTERNS note)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatHistoryDB.ts — sessions + messages stores with typed CRUD</name>
  <read_first>
    - PRODUCT_SPEC §21.1 (lines 3329-3353 — ChatSession/ChatMessage verbatim: id, title, created, updated, starred, preview; ChatMessage id/sessionId/role/content/timestamp/metadata)
    - PRODUCT_SPEC §15.1 (lines 1950-1953 — sessions + messages stores)
    - 02-RESEARCH.md Pattern 1 (DBSchema interface + openDB + index 'by-session'/'by-timestamp' verified)
  </read_first>
  <files>
    src/core/storage/ChatHistoryDB.ts
    tests/core/storage/ChatHistoryDB.test.ts
  </files>
  <action>
    Create src/core/storage/ChatHistoryDB.ts per §21.1/§15.1. Export:
    - `interface ChatSession` and `interface ChatMessage` — VERBATIM §21.1 (co-located with the store per the PATTERNS placement note; header comment citing §21.1).
    - `interface ChatHistoryDBSchema extends DBSchema { sessions: { key: string; value: ChatSession }; messages: { key: string; value: ChatMessage; indexes: { 'by-session': string; 'by-timestamp': number } } }`.
    - `DB_VERSION = 1` const.
    - `openChatHistoryDB(): Promise<IDBPDatabase<ChatHistoryDBSchema>>` — idb openDB('ChatHistoryDB', DB_VERSION) with a NON-throwing upgrade creating sessions (keyPath 'id') + messages (keyPath 'id', indexes 'by-session' on sessionId, 'by-timestamp' on timestamp).
    - Typed CRUD: `putSession(s)`, `getSession(id)`, `listSessions()`, `putMessage(m)`, `getMessagesForSession(sessionId)` (via the 'by-session' index), `deleteSession(id)` (removes the session AND its messages — iterate the index — since bodies are orphaned otherwise).
    - Every catch → debugLog(STORE_READ/STORE_WRITE) (Golden Rule 9).

    Create tests/core/storage/ChatHistoryDB.test.ts (fake-indexeddb — fresh DB per test or IDBFactory reset in afterEach, RESEARCH Pattern 8): putSession + putMessage round-trip; getMessagesForSession returns only that session's messages ordered by timestamp; deleteSession removes the session AND its orphaned messages (index iteration verified).
  </action>
  <acceptance_criteria>
    - `grep -c "ChatHistoryDBSchema" src/core/storage/ChatHistoryDB.ts` returns >= 1; `grep -c "'by-session'" src/core/storage/ChatHistoryDB.ts` returns >= 1
    - `grep -c "ChatSession\|ChatMessage" src/core/storage/ChatHistoryDB.ts` returns >= 2
    - `grep -c "openDB" src/core/storage/ChatHistoryDB.ts` returns >= 1
    - `pnpm vitest run tests/core/storage/ChatHistoryDB.test.ts --reporter=dot` passes (round-trip + index ordering + orphan cleanup)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "'by-session'" src/core/storage/ChatHistoryDB.ts && pnpm vitest run tests/core/storage/ChatHistoryDB.test.ts --reporter=dot</automated>
  </verify>
  <done>ChatHistoryDB opens with typed schema + indexes; sessions and messages CRUD via idb; store CRUD round-trip proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotesDB.ts — notes + concepts stores, getNoteByTitle()</name>
  <read_first>
    - PRODUCT_SPEC §21.2 (lines 3357-3384 — Note verbatim incl. LLM-Wiki optional fields + concepts shape)
    - PRODUCT_SPEC §15.1 (lines 1954-1958 — notes/concepts stores + getNoteByTitle)
    - 02-RESEARCH.md Pattern 1 (DBSchema template)
  </read_first>
  <files>
    src/core/storage/NotesDB.ts
    tests/core/storage/NotesDB.test.ts
  </files>
  <action>
    Create src/core/storage/NotesDB.ts per §21.2/§15.1. Export:
    - `interface Note` and `interface Concept` — VERBATIM §21.2 (Note includes the LLM-Wiki optional fields summary/categoryPath/summaryGeneratedAt/tagsGeneratedAt and aiMeta; Concept = { slug, label, summary, noteIds[], aliases[], updatedAt }).
    - `interface NotesDBSchema extends DBSchema { notes: { key: string; value: Note; indexes: { 'by-updated': number; 'by-tags': string } }; concepts: { key: string; value: Concept } }` (index choices at agent discretion within §15.1's shape).
    - `DB_VERSION = 1` const.
    - `openNotesDB(): Promise<IDBPDatabase<NotesDBSchema>>` — non-throwing upgrade creating notes (keyPath 'id') + concepts (keyPath 'slug') with the indexes.
    - Typed CRUD: `putNote(n)`, `getNote(id)`, `listNotes()`, `deleteNote(id)`, `getNoteByTitle(title): Promise<Note | undefined>` (via an index or an in-memory scan of listNotes — the §15.1 contract is the lookup by title), `putConcept(c)`, `getConcept(slug)`, `listConcepts()`.
    - Every catch → debugLog (Golden Rule 9).

    Create tests/core/storage/NotesDB.test.ts (fake-indexeddb — fresh DB per test or IDBFactory reset in afterEach, RESEARCH Pattern 8): putNote/getNote/listNotes round-trip; getNoteByTitle finds by exact title; deleteNote removes; putConcept/getConcept/listConcepts round-trip.
  </action>
  <acceptance_criteria>
    - `grep -c "getNoteByTitle" src/core/storage/NotesDB.ts` returns >= 1
    - `grep -c "Concept" src/core/storage/NotesDB.ts` returns >= 2
    - `grep -c "NotesDBSchema" src/core/storage/NotesDB.ts` returns >= 1
    - `pnpm vitest run tests/core/storage/NotesDB.test.ts --reporter=dot` passes (round-trip + title lookup)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "getNoteByTitle" src/core/storage/NotesDB.ts && pnpm vitest run tests/core/storage/NotesDB.test.ts --reporter=dot</automated>
  </verify>
  <done>NotesDB stores notes + concepts with typed CRUD and the §15.1 getNoteByTitle lookup; store CRUD round-trip proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MemoryDB.ts — [conversationId, seq] messages + userFacts + summaries</name>
  <read_first>
    - PRODUCT_SPEC §21.3 (lines 3391-3407 — ConversationMeta + MemoryMessage keyPath [conversationId, seq]) and §21.4 (lines 3413-3419 — Fact)
    - PRODUCT_SPEC §15.1 (lines 1959-1962 — messages/userFacts/conversationSummaries stores)
    - 02-RESEARCH.md Pattern 1 (composite keyPath syntax verified in idb 8)
  </read_first>
  <files>
    src/core/storage/MemoryDB.ts
    tests/core/storage/MemoryDB.test.ts
  </files>
  <action>
    Create src/core/storage/MemoryDB.ts per §21.3/§21.4/§15.1. Export:
    - `interface MemoryMessage { conversationId: string; seq: number; role: LLMMessage['role']; content: string; timestamp: number }` — VERBATIM §21.3 (the composite keyPath [conversationId, seq]).
    - `interface Fact { id, content, confidence, source: 'extracted'|'explicit', created }` — VERBATIM §21.4. Also export `ConversationSummary { conversationId, summary, updatedAt }` for the conversationSummaries store.
    - `interface MemoryDBSchema extends DBSchema { messages: { key: [string, number]; value: MemoryMessage; indexes: { 'by-conversation': string } }; userFacts: { key: string; value: Fact }; conversationSummaries: { key: string; value: ConversationSummary } }` (indexes per §15.1's implied lookups).
    - `DB_VERSION = 1` const.
    - `openMemoryDB(): Promise<IDBPDatabase<MemoryDBSchema>>` — non-throwing upgrade creating messages with keyPath array ['conversationId','seq'] + the 'by-conversation' index (conversationId), userFacts (keyPath 'id'), conversationSummaries (keyPath 'conversationId').
    - Typed CRUD: `putMemoryMessage(m)`, `getMessagesForConversation(conversationId)` (via the index, ordered by seq), `putFact(f)`, `getFact(id)`, `listFacts()`, `putConversationSummary(s)`, `getConversationSummary(conversationId)`.
    - Every catch → debugLog (Golden Rule 9).

    Create tests/core/storage/MemoryDB.test.ts (fake-indexeddb — fresh DB per test or IDBFactory reset in afterEach, RESEARCH Pattern 8): putMemoryMessage with the composite key [conversationId, seq] — two conversations interleave and getMessagesForConversation returns ONLY the requested conversation, ordered by seq (the composite-key + index contract); putFact/getFact/listFacts round-trip; putConversationSummary/getConversationSummary round-trip.
  </action>
  <acceptance_criteria>
    - `grep -c "conversationId" src/core/storage/MemoryDB.ts` returns >= 3 (composite keyPath usage)
    - `grep -c "MemoryDBSchema" src/core/storage/MemoryDB.ts` returns >= 1
    - `grep -c "userFacts" src/core/storage/MemoryDB.ts` returns >= 1
    - `grep -c "conversationSummaries" src/core/storage/MemoryDB.ts` returns >= 1
    - `pnpm vitest run tests/core/storage/MemoryDB.test.ts --reporter=dot` passes (composite-key isolation + ordering + round-trips)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "conversationSummaries" src/core/storage/MemoryDB.ts && pnpm vitest run tests/core/storage/MemoryDB.test.ts --reporter=dot</automated>
  </verify>
  <done>MemoryDB stores composite-keyed messages + facts + summaries with typed CRUD; store CRUD round-trip proven.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol | Kind | Location |
|--------|------|----------|
| `ChatSession` / `ChatMessage` | interface (verbatim §21.1) | src/core/storage/ChatHistoryDB.ts |
| `ChatHistoryDBSchema` / `DB_VERSION = 1` / `openChatHistoryDB()` | interface/const/fn | src/core/storage/ChatHistoryDB.ts |
| `putSession` / `getSession` / `listSessions` / `putMessage` / `getMessagesForSession` / `deleteSession` | fn | src/core/storage/ChatHistoryDB.ts |
| `Note` / `Concept` | interface (verbatim §21.2) | src/core/storage/NotesDB.ts |
| `NotesDBSchema` / `openNotesDB()` / `putNote` / `getNote` / `listNotes` / `deleteNote` / `getNoteByTitle` / `putConcept` / `getConcept` / `listConcepts` | fn | src/core/storage/NotesDB.ts |
| `MemoryMessage` / `Fact` / `ConversationSummary` | interface (verbatim §21.3/§21.4) | src/core/storage/MemoryDB.ts |
| `MemoryDBSchema` / `openMemoryDB()` / `putMemoryMessage` / `getMessagesForConversation` / `putFact` / `getFact` / `listFacts` / `putConversationSummary` / `getConversationSummary` | fn | src/core/storage/MemoryDB.ts |
| store CRUD tests | test | tests/core/storage/{ChatHistoryDB,NotesDB,MemoryDB}.test.ts |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message/note content → IndexedDB | Conversation bodies and note text cross here — their ONLY permitted persistence home |
| store CRUD → callers | Phase 3/5 consumers will read these bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-07-01 | Information Disclosure | message bodies written to chrome.storage.local | critical | mitigate | These stores are the only body home (§0.2); nothing in this plan's code touches chrome.storage for bodies; the phase-11 privacy gate greps the write boundaries and asserts no body ever lands in local (Pitfall 4). |
| T-2-07-02 | Tampering | composite-key index collisions in MemoryDB | medium | mitigate | keyPath [conversationId, seq] + 'by-conversation' index verified by the interleaving test — a cross-conversation leak would fail case ordering assertions. |
| T-2-07-03 | Information Disclosure | orphaned bodies after deleteSession | medium | mitigate | deleteSession removes messages via the index with the session row (single atomic tx) — ChatHistoryDB test asserts no orphans remain. |
| T-2-07-04 | Availability | store open failure at Phase 3/5 init | medium | mitigate | Every store open/catch routes through debugLog (Golden Rule 9); migration failures on these DBs flow to the 02-06 degraded mode + banner state (D-12). |
</threat_model>

<verification>
- `pnpm vitest run tests/core/storage/ChatHistoryDB.test.ts tests/core/storage/NotesDB.test.ts tests/core/storage/MemoryDB.test.ts` green
- `pnpm vitest run tests/core/storage` full green (all storage suites together)
- `pnpm typecheck` clean; eslint/prettier clean on touched files
</verification>

<success_criteria>
- STORAGE-01 satisfied: four IDB stores (three here + ErrorStore in 02-06) work via idb with strict typing.
- Data models are §21-verbatim; bodies live exclusively in IndexedDB.
- The stores are read-ready for Phase 3 (chat/memory) and Phase 5 (notes) consumers.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-07-SUMMARY.md` when done.
</output>
