---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/workspace/WorkspaceElection.ts
  - tests/core/workspace/WorkspaceElection.test.ts
autonomous: true
gap_closure: true
requirements:
  - REQ-R03
user_setup: []

estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A primary or solo surface's runHeartbeatTick publishes a WORKSPACE_HEARTBEAT carrying its surface + workspaceId on the np_workspace BroadcastChannel every 3 s tick (CR-02: previously zero call sites)."
    - "A higher-priority foreign surface (standalone) that starts AFTER an established sidepanel primary eventually wins the election: sidepanel demotes to secondary when it receives the standalone's production heartbeat, and standalone re-elects to primary on the next tick."
    - "The two-surface handoff regression test relies on the PRODUCTION heartbeat tick (via notifyWorkspaceHeartbeat from runHeartbeatTick), NOT a manually-injected publish(_sender:'foreign-instance-id')."
    - "The two-surface test models distinct surface JS contexts via SEPARATE MODULE INSTANCES (vi.resetModules + two dynamic imports of WorkspaceElection) — each import has its own election `activeInstance` AND its own BroadcastBus instanceId, so the sidepanel instance is NOT disposed when standalone starts and cross-surface production-tick heartbeats are deliverable via the shared BroadcastChannel mock (matching real MV3 multi-context delivery)."
  artifacts:
    - src/core/workspace/WorkspaceElection.ts (startElection opts + runHeartbeatTick publish)
    - tests/core/workspace/WorkspaceElection.test.ts (strengthened Test 2 + production-tick two-surface test via module copies)
  key_links:
    - "runHeartbeatTick -> notifyWorkspaceHeartbeat -> publish(np_workspace) -> onWorkspaceSync demotion rule (WorkspaceElection.ts inbound handler)"
  flagged_assumptions:
    - "REQ-R03 boundary (edge-probe unresolved): the heartbeat publishes once per 3 s tick for primary/solo; the session-write budget (≤30/min) still holds because heartbeats are session writes counted in WorkspacePersistence Test 6. Assumed no boundary change one step either side of the 3 s interval."
    - "The heartbeat's workspaceId is informational-only for the Phase-2 demotion rule (which consumes msg.surface); the entrypoints pass the real workspaceId getter in the dependent plan 02-08, so production heartbeats carry the real value there. Until 02-08 lands, the default getter returns '' (receiver ignores it)."

# Flagged per the gap-closure probe fallback: the following edge-probe rows were
# classified 'unresolved' (assumed, not silently dropped):
#   REQ-R03 boundary/precision — see flagged_assumptions above.
#   REQ-R06, REQ-R07 — not in this plan's scope (covered by existing plans 02-04/02-02).
prohibitions: []
---

<objective>
Fix CR-02 (BLOCKER, from 02-VERIFICATION.md truth 5 / 02-REVIEW.md CRITICAL-02): the election heartbeat is never published, so the standalone-wins tie-break demotion and the lone-surface 'solo' detection are dead in production.

Purpose: `runHeartbeatTick` (WorkspaceElection.ts:246-284) writes the session record but NEVER calls `notifyWorkspaceHeartbeat` (zero call sites). `WORKSPACE_HEARTBEAT` is therefore never broadcast, `foreignSurfacesEverSeen` stays false, and the standalone-wins demotion rule (WorkspaceElection.ts:348-363) — and the election-demotion mirror path (SidepanelChat.tsx:226-241) — never fire. The existing WorkspaceElection.test.ts Test 5 bypasses this by manually injecting `publish(..., _sender:'foreign-instance-id')`, so the suite is green while production never sends a heartbeat. This plan publishes the heartbeat from the production tick and proves the two-surface handoff through the real tick.

Output:
  - `runHeartbeatTick` publishes `WORKSPACE_HEARTBEAT` for primary/solo surfaces via `notifyWorkspaceHeartbeat`.
  - `startElection(surface, opts?)` accepts an optional `getWorkspaceId` getter (threaded into the tick; default `() => ''`).
  - A two-surface regression test (Sidepanel primary → Standalone starts later → Standalone wins) driven entirely by the production tick, using two separate WorkspaceElection module instances (`vi.resetModules()` + dynamic imports) so each surface has its own election state and BroadcastBus instance identity.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-VERIFICATION.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-REVIEW.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md

src/core/workspace/WorkspaceElection.ts (source of truth — runHeartbeatTick, startElection, inbound demotion rule, __test__ timer seam)
src/core/workspace/WorkspaceSync.ts (notifyWorkspaceHeartbeat — the zero-call-site function this plan wires)
src/core/runtime/BroadcastBus.ts (publish/subscribe, INSTANCE_ID self-suppression — read-only reference; each module copy has its own instance id)
src/core/workspace/WorkspaceStore.ts (ActiveSurface type, useWorkspaceStore.getState().workspaceId)
tests/setup.ts (BroadcastChannel mock — dispatches postMessage to ALL registered instances, so cross-surface delivery works across module copies)
tests/core/workspace/WorkspaceElection.test.ts (Test 2 to be strengthened + Test 5 to be corrected)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish WORKSPACE_HEARTBEAT from runHeartbeatTick for primary/solo surfaces + strengthen Test 2 to assert the publish</name>
  <files>src/core/workspace/WorkspaceElection.ts, tests/core/workspace/WorkspaceElection.test.ts</files>
  <read_first>
    src/core/workspace/WorkspaceElection.ts
    src/core/workspace/WorkspaceSync.ts
    src/core/workspace/WorkspaceStore.ts
    tests/core/workspace/WorkspaceElection.test.ts (Test 2 to be strengthened)
  </read_first>
  <action>
    Modify `src/core/workspace/WorkspaceElection.ts`:

    1. Import `notifyWorkspaceHeartbeat` from `./WorkspaceSync` (the file already imports `onWorkspaceSync` from the same module — extend that import statement; do NOT add a second import line for the same module).

    2. Change the public `startElection` signature from `startElection(surface: ActiveSurface): Promise<ElectionInstance>` to `startElection(surface: ActiveSurface, opts?: { getWorkspaceId?: () => string }): Promise<ElectionInstance>`. Derive `const getWorkspaceId = opts?.getWorkspaceId ?? (() => '');` at the top of the function body. Keep the existing double-start guard and cross-surface dispose behavior intact.

    3. Thread `getWorkspaceId` into the heartbeat tick: in the `heartbeatTimer = timerFactory(() => {...})` callback, add `getWorkspaceId` to the argument list passed to `runHeartbeatTick(...)`. Update `runHeartbeatTick`'s signature to accept `getWorkspaceId: () => string` as an additional parameter.

    4. Inside `runHeartbeatTick`, after the `writeRecord(record)` call in the `cur.state === 'primary' || cur.state === 'solo' || cur.state === 'election-in-progress'` branch, publish the heartbeat for the primary/solo states ONLY (per the VERIFICATION's required fix):
       - When `cur.state === 'primary'` or `cur.state === 'solo'`, call `notifyWorkspaceHeartbeat(surface, getWorkspaceId())`.
       - Do NOT publish for `election-in-progress` (that branch still refreshes the record but emits no heartbeat, matching the documented intent).
       - Place the publish call AFTER the `writeRecord` so the session record is refreshed before the broadcast, and keep the existing lone-surface trap (`if (cur.state === 'primary' && !foreignSeen()) setState({state:'solo', ...})`) ordering unchanged.

    5. Do NOT change the inbound demotion rule (WorkspaceElection.ts:348-363), the CAS logic, the `SURFACE_PRIORITY` table, `HEARTBEAT_INTERVAL_MS`, or the `__test__` timer seam. The heartbeat publish is purely additive to the tick.

    6. Strengthen `tests/core/workspace/WorkspaceElection.test.ts` Test 2 so it asserts the heartbeat is actually PUBLISHED, not just that `electedAt` advanced. The current Test 2 only checks the session record's `electedAt` moved, which passes WITHOUT the publish (record refresh happens in the same branch). Because a single surface's own heartbeat is self-suppressed in its own BroadcastBus, observe the publish through a DISTINCT-identity subscriber: create an observer module copy of BroadcastBus via `vi.resetModules()` + a dynamic `import('../../../src/core/runtime/BroadcastBus')` (fresh instanceId ≠ the election module's), `subscribe('np_workspace', ...)`, then start the sidepanel election in the main module, advance one `HEARTBEAT_INTERVAL_MS`, and assert the observer received a `{ type: 'WORKSPACE_HEARTBEAT', surface: 'sidepanel', workspaceId: ... }`. Restore the module cache / re-import after this test so later tests get a clean module. Keep the existing `electedAt`-advanced assertion as a secondary check.

    Constraint: the heartbeat publish must go through `notifyWorkspaceHeartbeat` (the production primitive) — never a hand-rolled `publish` in the election module.
  </action>
  <verify>
    <automated>pnpm test -- tests/core/workspace/WorkspaceElection.test.ts -t "Test 2" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "notifyWorkspaceHeartbeat" src/core/workspace/WorkspaceElection.ts` returns a call inside `runHeartbeatTick` guarded by `cur.state === 'primary' || cur.state === 'solo'`.
    - `startElection('sidepanel')` (no opts) still compiles and returns an instance — the opts param is optional.
    - The strengthened Test 2 asserts a DISTINCT-identity subscriber actually receives a WORKSPACE_HEARTBEAT after advancing one `HEARTBEAT_INTERVAL_MS` (the publish is verified, not just the record refresh) AND keeps the `electedAt`-advanced check.
    - `pnpm run verify:phase-2` is green after all tasks (this task's tsc must compile).
  </acceptance_criteria>
  <done>
    A primary/solo surface's production heartbeat tick publishes a WORKSPACE_HEARTBEAT with its surface and workspaceId; the election module imports only `notifyWorkspaceHeartbeat` (no raw publish).
  </done>
  <reversibility rating="reversible">
    The `startElection` opts param is additive (optional) and the tick publish is additive; removing them restores the prior contract with no data migration.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-surface regression test — Sidepanel primary → Standalone starts later → Standalone wins, via the production tick (two module instances)</name>
  <files>tests/core/workspace/WorkspaceElection.test.ts</files>
  <read_first>
    tests/core/workspace/WorkspaceElection.test.ts
    src/core/workspace/WorkspaceElection.ts
    src/core/workspace/WorkspaceSync.ts
    src/core/runtime/BroadcastBus.ts (read-only — confirms INSTANCE_ID self-suppression is per-module-copy)
    tests/setup.ts (BroadcastChannel mock — dispatches postMessage to ALL registered instances)
  </read_first>
  <behavior>
    - Test A (production-tick demotion, two module instances): model two surfaces as TWO SEPARATE WorkspaceElection module copies (each `import()` after `vi.resetModules()` gets its own `activeInstance` AND its own BroadcastBus `instanceId`). Sidepanel (copy 1) starts first → primary. Standalone (copy 2) starts immediately (same 3 s CAS window) → wins the tie-break by higher priority → primary. Advancing fake timers one `HEARTBEAT_INTERVAL_MS` at a time: standalone's production tick emits WORKSPACE_HEARTBEAT(standalone) which reaches sidepanel's distinct BroadcastBus instance (mock dispatches cross-instance), sidepanel demotes to secondary. Final state: standalone primary, sidepanel secondary. MUST rely on `notifyWorkspaceHeartbeat` from `runHeartbeatTick` — NO `publish(..., _sender:'foreign-instance-id')` anywhere in the test.
    - Test B (single-surface self-suppression preserved): a lone sidepanel surface publishing its own heartbeat never sees it as foreign; it transitions primary → solo on the first tick (unchanged existing behavior).
  </behavior>
  <action>
    Replace the existing `WorkspaceElection.test.ts` Test 5 (lines ~163-201, the "demotion" test that manually injects `publish(..., _sender:'foreign-instance-id')`) with a production-tick two-surface test that uses TWO SEPARATE MODULE INSTANCES. Do NOT leave the manual-injection test in the file.

    CRITICAL (BLOCKER-1 fix): the module-level `activeInstance` singleton in WorkspaceElection.ts (lines 103, 307-308) DISPOSES the previous instance when a second instance starts on a different surface. In a SINGLE module, starting standalone would dispose the sidepanel instance, so sidepanel could never receive standalone's heartbeat and never demote — and standalone would "win" only via the stale-record re-election path, NOT the heartbeat-demotion path (exactly the wiring bypass the verifier flagged). The fix is to give each surface its OWN module instance so they coexist:

    Test flow (replace the old Test 5):
      1. `const sidepanelMod = await import('../../../src/core/workspace/WorkspaceElection')` (module copy 1 — its own `activeInstance` + BroadcastBus `instanceId`).
      2. `const sidepanel = await sidepanelMod.startElection('sidepanel', { getWorkspaceId: () => 'ws-test' })`. Assert `sidepanelMod.getState().state === 'primary'`.
      3. `vi.resetModules()` — clears the module cache so the NEXT import is a fresh copy (do this BEFORE the standalone import so the two copies coexist). The already-running sidepanel instance keeps its own timer + subscription.
      4. `const standaloneMod = await import('../../../src/core/workspace/WorkspaceElection')` (module copy 2 — fresh `activeInstance`, fresh BroadcastBus `instanceId`). `const standalone = await standaloneMod.startElection('standalone', { getWorkspaceId: () => 'ws-test' })`. Because standalone starts within the same 3 s CAS window (no time advanced since sidepanel started), its higher priority wins the tie-break → standalone is primary (assert `standaloneMod.getState().state === 'primary'`).
      5. Advance `HEARTBEAT_INTERVAL_MS` ticks (loop 2-3 ticks). On each tick standalone (primary) publishes its production heartbeat; the mock dispatches it to sidepanel's distinct BroadcastChannel instance (different instanceId → not self-suppressed) → sidepanel's inbound handler demotes to secondary (standalone priority > sidepanel). Without the CR-02 publish, sidepanel would remain primary forever — so the demotion IS the CR-02-dependent assertion.
      6. Assert FINAL state: `sidepanelMod.getState()` is `secondary` with `primarySurface === 'standalone'`; `standaloneMod.getState()` is `primary` with `surface === 'standalone'`; `standaloneMod.isPrimaryWriter()` is true.
      7. `sidepanel.dispose(); standalone.dispose();`

    The test MUST NOT contain the literal string `_sender: 'foreign-instance-id'` or any manual `publish('np_workspace', {type:'WORKSPACE_HEARTBEAT', ...})` call — the heartbeat must originate from `runHeartbeatTick` → `notifyWorkspaceHeartbeat`.

    NOTE: `vi.resetModules()` also re-evaluates the statically-imported `../../../src/core/workspace/WorkspaceElection` module used by the OTHER tests; subsequent tests' static imports re-import a clean module, which is correct isolation. The two-surface test must dispose both instances in a `finally` and not rely on the shared module-level `getState()`/`isPrimaryWriter()` (each instance is read via its own module handle).
  </action>
  <verify>
    <automated>pnpm test -- tests/core/workspace/WorkspaceElection.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "foreign-instance-id" tests/core/workspace/WorkspaceElection.test.ts` returns 0 (the manual-injection test is gone).
    - The new two-surface test creates TWO module instances (sidepanelMod + standaloneMod via `vi.resetModules()` + dynamic imports) and asserts the FINAL state has standalone primary and sidepanel demoted to secondary, with the heartbeat originating from the production tick (NOT the stale-record re-election path).
    - `pnpm test -- tests/core/workspace/WorkspaceElection.test.ts` passes all tests (Tests 1-4, strengthened Test 2, Test 6, new two-surface test).
    - `pnpm run verify:phase-2` passes (tsc + all phase-2 tests, 109 existing + new).
  </acceptance_criteria>
  <done>
    The two-surface "Standalone wins when it starts after an established Sidepanel primary" scenario is proven through the real production heartbeat tick with two coexisting module instances; no manual heartbeat injection remains in the test file, and the heartbeat-demotion path (not the stale-record path) is what drives sidepanel's demotion.
  </done>
</task>

</tasks>

## Artifacts this phase produces

Symbols/behavior created or changed by this plan:

- `src/core/workspace/WorkspaceElection.ts`
  - `startElection(surface: ActiveSurface, opts?: { getWorkspaceId?: () => string }): Promise<ElectionInstance>` — new optional `opts.getWorkspaceId` getter.
  - `runHeartbeatTick(...)` — now takes `getWorkspaceId: () => string` and, for `state === 'primary' | 'solo'`, calls `notifyWorkspaceHeartbeat(surface, getWorkspaceId())`.
- `tests/core/workspace/WorkspaceElection.test.ts`
  - Strengthened Test 2 → asserts the WORKSPACE_HEARTBEAT publish via a distinct-identity observer module copy (not just `electedAt`).
  - Corrected Test 5 → production-tick two-surface demotion test using two module instances (`vi.resetModules()` + dynamic imports), proving sidepanel demotes via the real heartbeat, not a manual `_sender` injection.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| surface↔surface (BroadcastChannel) | Election heartbeats cross the `np_workspace` BroadcastChannel between sidepanel and standalone surfaces. Any surface on the channel can emit a `WORKSPACE_HEARTBEAT`; the demotion rule trusts the `surface` field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09-01 | Spoofing | `WORKSPACE_HEARTBEAT.surface` field | medium | mitigate | The demotion rule only demotes to `secondary` (read-only mirror, D-27) — it never grants write access. A spoofed standalone heartbeat can at most demote a real primary to mirror, which is a benign, recoverable state (the demoted surface stops writing; the spoofed surface cannot write without the election record). No privilege escalation. Heartbeat `workspaceId` is not trusted by any consumer in Phase 2. |
| T-02-09-02 | Tampering | session record `np_workspace_primary` | high | mitigate | Election CAS + 6 s stale threshold already re-elect on a tampered/stale record; the fix does not weaken this. The heartbeat publish is additive; self-suppression by instance id prevents a surface from observing its own heartbeat as foreign. Mitigation is unchanged from the existing CAS design. |
| T-02-09-03 | DoS | heartbeat flood on `np_workspace` | low | accept | One heartbeat per 3 s tick per primary/solo surface (≤ 1/3s/surface), far under the ~120/min chrome.storage boundary and the D-43 write-rate budget. Broadcast is in-memory; no persistence cost. Accepted as within normal traffic. |
| T-02-09-04 | Information Disclosure | workspaceId in heartbeat payload | low | accept | `workspaceId` is a non-secret coordination identifier already present in `np_workspace` and broadcast messages; it is not a credential. Accepted. |
| T-02-09-SC | Tampering | npm installs (no new deps) | low | mitigate | No new package installed by this plan; package-legitimacy gate N/A. |

No high-severity threat is left unmitigated; the two high rows (T-02-09-02) rely on the pre-existing CAS/stale-re-election design which this plan does not weaken.
</threat_model>

<verification>
- `pnpm test -- tests/core/workspace/WorkspaceElection.test.ts` — all election tests pass (incl. the strengthened Test 2 and corrected Test 5).
- `pnpm run verify:phase-2` — tsc + all 109 existing phase-2 tests + new tests green.
- Grep gates: `grep -c "foreign-instance-id" tests/core/workspace/WorkspaceElection.test.ts` returns 0; `grep -rn "notifyWorkspaceHeartbeat" src/core/workspace/WorkspaceElection.ts` returns a call guarded by primary/solo.
</verification>

<success_criteria>
Measurable completion:
- `runHeartbeatTick` publishes a WORKSPACE_HEARTBEAT for primary/solo surfaces via `notifyWorkspaceHeartbeat` (previously zero call sites).
- The strengthened Test 2 verifies the publish through a distinct-identity observer; the corrected Test 5 proves "Sidepanel primary → Standalone starts later → Standalone wins" through the real production tick with two coexisting module instances, with zero manual `_sender:'foreign-instance-id'` injection.
- No changes to `src/core/runtime/BroadcastBus.ts` — distinct surface contexts are modeled by separate module instances (each with its own instanceId), matching real MV3 multi-context delivery; default single-surface behavior and all existing tests are unchanged.
- `pnpm run verify:phase-2` passes.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-09-SUMMARY.md` when done.
</output>
