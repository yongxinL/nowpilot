---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 09
type: execute
wave: 4
depends_on: [02-02, 02-04, 02-05, 02-07]
files_modified:
  - src/core/storage/ImportExport.ts
  - tests/core/storage/ImportExport.test.ts
autonomous: true
requirements: [STORAGE-05]
must_haves:
  truths:
    - "Export produces JSON as the canonical format plus ZIP via fflate (zipSync/strToU8) for multi-group full exports (D-17)."
    - "Export groups mirror export-data { scopes: string[] }: chat-history, notes, memory, workspace, settings — each group sanitized via redactSensitive BEFORE serialization (D-17)."
    - "No secret material ever leaves: np_providers apiKey ciphertext, np_install_secret, and all secret-bearing values are EXCLUDED from every export (D-01)."
    - "A manifest { exportedAt, appVersion, schemaVersion } accompanies every export (D-17)."
    - "Import is per-group MERGE/upsert by id — existing records win on conflict by default, with a 'restore overwrites' toggle; NEVER full wipe-and-replace (D-18)."
    - "Full-vault ZIP restore runs journaled via runJournaled (restore-batch-style op) so a mid-restore crash leaves a consistent state (D-18)."
    - "Phase 2 ships the CORE ONLY — the Options → Advanced → Import/ExportPanel UI is Phase 7 (D-19)."
  artifacts:
    - src/core/storage/ImportExport.ts
    - tests/core/storage/ImportExport.test.ts
  key_links:
    - "ImportExport ↔ runJournaled (02-04) — journaled full-vault restore"
    - "ImportExport ↔ redactSensitive (02-02) — sanitize-before-serialize"
    - "ImportExport ↔ the four IDB stores (02-06/02-07) + Setting.ts (02-05) — group data sources"
---

<objective>
Deliver STORAGE-05's core import/export module (D-17/D-18/D-19): JSON canonical + ZIP via fflate, scoped groups mirroring export-data's `{ scopes: string[] }`, redactSensitive-sanitized groups, a manifest, per-group MERGE/upsert restore (existing wins, 'restore overwrites' toggle), and journaled full-vault restore. This file is a documented +1 to the §18 create-list (RESEARCH Q1 recommendation — D-09's folding rule forbids only StorageLayer/StorageSession names).

Purpose: Users can back up/restore their data (sanitized JSON/ZIP) — the §18 DONE-when "user can import/export" is satisfied by the verified core + export-data plumbing; the UI lands in Phase 7.
Output: ImportExport.ts + tests.

Flagged assumptions (specless probe):
- A-19: Export excludes np_providers (ciphertext AND plaintext) and np_install_secret entirely — 'settings' group carries non-secret keys only (np_theme, np_theme_pack, np_language, np_addon_settings, np_flags, np_schema_version, np_persona minus secrets).
- A-20: The restore op uses the locked 'restore-notes-batch' operation value for the journaled full-vault restore entry (D-18 'restore-batch-style op'; the vocabulary is pre-locked — this wires a consumer, not a new op). **User-confirmed 2026-08-09:** the 02-CONTEXT.md Deferred Ideas exception note records that D-18 supersedes the declared-but-unwired wording for `restore-notes-batch` — the live Phase-2 consumer wiring is intended and validated, not a plan invention.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md export-data tool (line ~1598 — { scopes: string[] }, 'Export bundle (no API keys)'), §9.3 (Import/Export sanitised JSON/ZIP)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md D-01, D-17, D-18, D-19
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md Pattern 7 (fflate zipSync/unzipSync verified) + Open Question Q1 (the +1 file recommendation) + Assumption A4
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md (ImportExport adaptation notes)
@src/core/storage/WriteJournal.ts (runJournaled, from 02-04)
@src/core/security/redactSensitive.ts (02-02)
@src/core/storage/Setting.ts (02-05/02-08 — settings group source)
@src/core/storage/ChatHistoryDB.ts / NotesDB.ts / MemoryDB.ts (02-07 — group sources)
@tests/fixtures/index.ts (redaction fixture — sanitize proof)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ImportExport.ts — scoped groups, manifest, JSON+ZIP, merge/upsert restore</name>
  <read_first>
    - 02-CONTEXT.md D-17 (JSON canonical + ZIP via fflate; scopes chat-history/notes/memory/workspace/settings; sanitize before serialize; manifest { exportedAt, appVersion, schemaVersion }) and D-18 (per-group MERGE/upsert; existing wins; 'restore overwrites' toggle; journaled full-vault restore) — plus the Deferred Ideas exception note (line ~149): `restore-notes-batch` is a user-confirmed live Phase-2 consumer (D-18 supersedes declared-but-unwired)
    - 02-RESEARCH.md Pattern 7 (fflate API: zipSync/unzipSync/strToU8/strFromU8 verified round-trip) + Q1 (ImportExport.ts +1 file with a Rule-3 deviation note)
    - src/core/storage/WriteJournal.ts (runJournaled signature from 02-04)
    - src/core/security/redactSensitive.ts (02-02)
  </read_first>
  <files>
    src/core/storage/ImportExport.ts
  </files>
  <action>
    Create src/core/storage/ImportExport.ts (documented +1 to the §18 create-list — header comment: Rule-3 deviation note per RESEARCH Q1/A4, consistent with D-09's folding rule which forbids only StorageLayer/StorageSession names). Export:
    - `EXPORT_GROUPS = ['chat-history', 'notes', 'memory', 'workspace', 'settings'] as const` and `type ExportGroup = (typeof EXPORT_GROUPS)[number]` — the export-data `{ scopes: string[] }` vocabulary (D-17).
    - `interface ExportManifest { exportedAt: number; appVersion: string; schemaVersion: number }`.
    - `buildManifest(): ExportManifest` — exportedAt = Date.now(), appVersion from a module const (e.g. '0.1.0'), schemaVersion = 1 (aligns with np_schema_version, 02-05).
    - `collectGroup(group: ExportGroup): Promise<Record<string, unknown>>` — gathers the group's data: chat-history from ChatHistoryDB (sessions + messages), notes from NotesDB, memory from MemoryDB, workspace from np_workspace via Setting, settings from Setting's non-secret keys (EXCLUDING np_providers, np_install_secret, and the session tokens — D-01/D-11: an explicit EXCLUDED_KEYS const).
    - `sanitizeGroup(group, data): unknown` — runs redactSensitive over the group payload before serialization (D-17); assert the result carries no secret-shaped values.
    - `exportJson(groups: ExportGroup[]): Promise<string>` — canonical JSON: { manifest, groups: { [group]: sanitizedData } } serialized with JSON.stringify.
    - `exportZip(groups: ExportGroup[]): Promise<Uint8Array>` — fflate zipSync with 'manifest.json' + 'groups/<group>.json' entries (strToU8), level 6.
    - `parseImportPayload(payload: string | Uint8Array): Promise<{ manifest; groups }>` — JSON.parse for strings; unzipSync + strFromU8 for ZIP payloads.
    - `mergeGroup(group, incoming, opts: { overwrite?: boolean }): Promise<{ upserted: number; kept: number }>` — per-group MERGE/upsert by id: for each incoming record, if the local id exists → keep local unless overwrite:true; if absent → insert (D-18; existing wins by default). The merge writes go through the relevant store (ChatHistoryDB/NotesDB/MemoryDB/Setting) via upsert puts.
    - `restoreFullVault(payload, opts): Promise<{ upserted; kept }>` — journaled: builds a single WriteJournalEntry { operation: 'restore-notes-batch' (A-20 — user-confirmed live Phase-2 consumer per the CONTEXT deferred-list exception), targetIds: { scope: 'full-vault' }, steps: one step per group merge } and runs runJournaled so a mid-restore crash replays/re-rolls back to a consistent state (D-18).
    - Every catch → debugLog with canonical codes (STORE_READ/WRITE_JOURNAL_FAILED etc., Golden Rule 9).
  </action>
  <acceptance_criteria>
    - `grep -c "EXPORT_GROUPS" src/core/storage/ImportExport.ts` returns >= 1
    - `grep -c "manifest" src/core/storage/ImportExport.ts` returns >= 2 (manifest type + build)
    - `grep -c "zipSync\|unzipSync" src/core/storage/ImportExport.ts` returns >= 1 (fflate)
    - `grep -c "restore-notes-batch" src/core/storage/ImportExport.ts` returns >= 1 (journaled restore op)
    - `grep -c "np_install_secret\|np_providers" src/core/storage/ImportExport.ts` returns >= 2 (explicit exclusion refs)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "zipSync" src/core/storage/ImportExport.ts && grep -c "restore-notes-batch" src/core/storage/ImportExport.ts</automated>
  </verify>
  <done>Export collects sanitized scoped groups into JSON/ZIP with a manifest; restore merges per-group by id with journaled full-vault recovery; secrets are explicitly excluded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImportExport.test.ts — round-trip, no-secrets, merge semantics, journaled restore</name>
  <read_first>
    - tests/fixtures/index.ts (redaction + vault fixtures — sanitize + exclusion proof)
    - 02-RESEARCH.md Pattern 7 (fflate round-trip verified — 'Maximum call stack' was the leaked-rejection cascade, fflate itself is clean)
    - tests/core/storage/WriteJournal.test.ts (fake-indexeddb conventions — the store groups live in IDB)
    - src/core/storage/ImportExport.ts (the module under test)
  </read_first>
  <files>
    tests/core/storage/ImportExport.test.ts
  </files>
  <action>
    Create tests/core/storage/ImportExport.test.ts (STORAGE-05 coverage, per RESEARCH validation map):
    1. JSON round-trip: seed chat-history + notes groups via the stores, exportJson, parseImportPayload, mergeGroup into a fresh set of stores → data survives with equal ids.
    2. ZIP round-trip: exportZip, unzipSync parse, same equality (fflate strToU8/strFromU8 — A5 level 6).
    3. No-secrets: seed np_providers (ciphertext) + np_install_secret in storage, export all groups, assert the serialized payload contains NEITHER the ciphertext NOR the plaintext secret NOR any secret-shaped value (redaction fixture values) — D-01 exclusion proven; the manifest is present with exportedAt/appVersion/schemaVersion.
    4. Merge semantics: seed local record with id X, import payload whose same id differs → local wins (existing-wins default); with overwrite:true → incoming wins; new ids insert (no wipe of unrelated records — D-18).
    5. Journaled restore: full-vault restore writes a 'restore-notes-batch' journal entry; a mid-restore crash simulation (fail a group step) leaves the journal entry in a recoverable state and recoverJournal can replay it (reuse the 02-04 recovery harness).
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/storage/ImportExport.test.ts --reporter=dot` passes (all cases)
    - `grep -c "manifest" tests/core/storage/ImportExport.test.ts` returns >= 1
    - `grep -c "restore-notes-batch" tests/core/storage/ImportExport.test.ts` returns >= 1 (journaled restore asserted)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/storage/ImportExport.test.ts --reporter=dot</automated>
  </verify>
  <done>JSON+ZIP round-trip, no-secret export, existing-wins merge, and journaled full-vault restore are all proven.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol | Kind | Location |
|--------|------|----------|
| `EXPORT_GROUPS` / `ExportGroup` | const/type | src/core/storage/ImportExport.ts |
| `ExportManifest` / `buildManifest()` | interface/fn | src/core/storage/ImportExport.ts |
| `EXCLUDED_KEYS` | const (np_providers, np_install_secret, session tokens) | src/core/storage/ImportExport.ts |
| `collectGroup(group)` / `sanitizeGroup(group, data)` | fn | src/core/storage/ImportExport.ts |
| `exportJson(groups)` / `exportZip(groups)` / `parseImportPayload(payload)` | fn | src/core/storage/ImportExport.ts |
| `mergeGroup(group, incoming, opts)` / `restoreFullVault(payload, opts)` | fn (MERGE/upsert + journaled) | src/core/storage/ImportExport.ts |
| ImportExport tests (5 cases) | test | tests/core/storage/ImportExport.test.ts |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export serialization → file bytes | Secret material must never cross this boundary (D-01) |
| import payload → stores | Untrusted (possibly tampered) data enters the extension here — merge, never wipe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-09-01 | Information Disclosure | API keys / installSecret leaking into an export | critical | mitigate | EXCLUDED_KEYS removes np_providers + np_install_secret + session tokens before collectGroup; sanitizeGroup runs redactSensitive before serialization (D-01/D-17); test case 3 asserts the payload contains neither ciphertext nor plaintext secrets. |
| T-2-09-02 | Tampering | hostile import payload (malformed/tampered JSON/ZIP) | high | mitigate | parseImportPayload validates the manifest + group shapes (typed; unknown groups rejected); mergeGroup upserts per-id with existing-wins — a hostile record cannot delete or overwrite local data without the overwrite toggle (D-18). |
| T-2-09-03 | Availability | mid-restore crash leaving a half-restored vault | high | mitigate | Full-vault restore runs journaled via runJournaled ('restore-notes-batch', D-18) — a crash leaves a recoverable entry; recoverJournal (02-04) replays to consistency; test case 5 proves the crash/replay path. |
| T-2-09-04 | Information Disclosure | sanitized-but-shape-echoing groups (metadata leak) | low | accept | Groups are sanitized and carry no secret values; metadata (timestamps, ids) is intentionally exported for restore fidelity. Accepted per D-17's inspectable/diffable contract. |
</threat_model>

<verification>
- `pnpm vitest run tests/core/storage/ImportExport.test.ts` green
- `pnpm vitest run tests/core/storage` full green (all storage suites together)
- `pnpm typecheck` clean; eslint/prettier clean on touched files
- No UI component is added (D-19 core-only) — grep `src/components` for ImportExportPanel: no match
</verification>

<success_criteria>
- STORAGE-05 satisfied: sanitized JSON/ZIP import/export + backup/restore work via the verified core (UI deferred to Phase 7, D-19).
- No secret material ever leaves the machine through an export (D-01).
- Restore is additive/merge semantics — a partial restore can never destroy data (D-18).
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-09-SUMMARY.md` when done.
</output>
