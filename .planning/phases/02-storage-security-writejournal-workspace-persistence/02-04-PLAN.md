---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/types/storage.ts
  - src/core/storage/IndexedDBMigrator.ts
  - src/core/storage/Setting.ts
  - src/core/storage/ChatHistoryDB.ts
  - src/core/storage/MemoryDB.ts
  - src/core/storage/NotesDB.ts
  - src/core/storage/WriteJournalDB.ts
  - src/core/storage/ErrorStore.ts
  - wxt.config.ts
  - tests/core/storage/IndexedDBMigrator.test.ts
  - tests/core/storage/ErrorStore.test.ts
autonomous: true
requirements: [REQ-R06, REQ-R07]
user_setup: []
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The v1→v2 migration fixture passes: (a) opening at v2 migrates v1 data intact (backward-compatible), (b) running the migration twice is a no-op (idempotent), (c) a fresh open at v2 applies both v1 and v2 steps (conditional-block correctness) — ROADMAP success criterion 4."
    - "All five production IndexedDB databases (ChatHistoryDB, MemoryDB, NotesDB, WriteJournalDB, ErrorStore) bootstrap at DB_VERSION = 1 with their §15.1 store lists (D-41/D-42)."
    - "Every database bump is carried by an `IndexedDBMigration` entry (fromVersion/toVersion/description/migrate per §20.4); production DBs stay at v1 until owning phases bump them."
    - "Migration/open failure of one database degrades only that database (IDB_MIGRATION_FAILED recorded, IDB_BLOCKED on blocked upgrade) and does not block workspace persistence or provider configuration (D-41 degraded-mode contract)."
    - "ErrorStore persists typed errors to IndexedDB with FIFO eviction at max 100 entries and redacts context at the write boundary (D-39, §15.1)."
    - "wxt.config.ts permissions become exactly ['sidePanel', 'storage', 'tabs', 'unlimitedStorage'] (D-40 / REQ-R06 / ADR-STACK-02)."
  artifacts:
    - "src/types/storage.ts — WriteJournalEntry + related persisted types (A4: O.11 imports from @/types/storage)"
    - "src/core/storage/IndexedDBMigrator.ts — IndexedDBMigration registry + openVersionedDB + bootstrap"
    - "src/core/storage/{ChatHistoryDB,MemoryDB,NotesDB,WriteJournalDB,ErrorStore,Setting}.ts"
    - "wxt.config.ts — unlimitedStorage permission"
    - "tests/core/storage/IndexedDBMigrator.test.ts, tests/core/storage/ErrorStore.test.ts"
  key_links:
    - "IndexedDBMigrator.openVersionedDB ↔ each DB module's open*DB (blocked → IDB_BLOCKED; failure → IDB_MIGRATION_FAILED → ErrorStore + degraded mode)"
    - "WriteJournalDB.entries ↔ WriteJournalEntry type (src/types/storage.ts) ↔ WriteJournal (plan 02-05)"
    - "ErrorStore.record ↔ adapter error pipeline (plan 02-06) and migrator blocked/failure callbacks"
  assumptions:
    - "REQ-R06 edge (unclassified probe row, resolved manually): the boundary is the manifest permission set — exactly `['sidePanel','storage','tabs','unlimitedStorage']`. `unlimitedStorage` exempts ONLY the extension origin's storage.local quota + IndexedDB origin quota/eviction; the chrome.storage.session 10MB cap is NOT lifted (RESEARCH Pitfall 7 — session records stay tiny)."
    - "Execution-weight watch (scope monitor, checker warning): 11 files modified across 3 tasks ≈ 70% of the context budget. All phase estimates are uncalibrated (confidence low, 0 prior actuals). If Task 2 (DB bootstrap) runs hot, split the three DB modules into a second sub-step within this plan (the module template is shared — a clean split point). The estimate is kept as-is and re-checked after the first phase with real actuals recalibrates the factor."
    - "Field-level fidelity: keyPaths follow §15.1 / Appendix C; where Appendix C is silent (e.g. ChatHistoryDB messages keyPath), the repo UUID-identity convention applies (keyPath 'id' + index 'sessionId'). No invented schema fields."
    - "ErrorStore.record is best-effort: internal try/catch with debugLog fallback, never rethrown into the caller (RESEARCH Open Question 4)."
  prohibitions:
    - "Inventing error codes (only existing canonical codes are used: IDB_BLOCKED, IDB_MIGRATION_FAILED — REQ-R07/D-38 closed-set rule)"
    - "Awaiting non-IDB work (fetch/crypto) inside an idb transaction (RESEARCH Pitfall 3 — transaction auto-close)"
    - "Unconditional store/field creation in upgrades (conditional `if (oldVersion < N)` blocks + skip-if-present guards — Pitfall 8)"
    - "Renumbering DB_VERSION without a registered IndexedDBMigration (D-41 forward-migration contract)"
    - "New `@ts-expect-error NP-STRICT` markers (Pitfall 9)"
---

<objective>
IndexedDB foundation: the shared `IndexedDBMigrator` versioning framework, the `WriteJournalEntry` type home, all five production databases bootstrapped at v1 with §15.1 schema fidelity, the ErrorStore (FIFO-100 debug sink), the declare-now `Setting<T>` utility, and the `unlimitedStorage` manifest permission.

Purpose: ROADMAP success criterion 4 (v1→v2 fixture) + REQ-R06 (unlimitedStorage per ADR-STACK-02) + the IDB half of REQ-R07's error surfacing (IDB_BLOCKED / IDB_MIGRATION_FAILED → ErrorStore). Every later phase's IDB usage opens through this framework (D-41/D-42). Setting.ts ships declare-now (the §13 serialized-write rule is type-only in Phase 2 — planner discretion).
Output: versioned, idempotent, degraded-mode-safe IndexedDB layer; unlimitedStorage in the manifest.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md
@src/core/registry/Registry.ts
@src/core/commands/CommandRegistry.ts
@src/core/log/debugLog.ts
@wxt.config.ts
@tests/setup.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: WriteJournalEntry types + IndexedDBMigrator framework + v1→v2 fixture proof</name>
  <files>src/types/storage.ts, src/core/storage/IndexedDBMigrator.ts, tests/core/storage/IndexedDBMigrator.test.ts</files>
  <read_first>
    - 02-RESEARCH.md lines 353-380 (Pattern 4: IndexedDBMigration interface verbatim, idb openDB mechanics, conditional blocks, v1→v2 fixture spec), 516-542 (idb openDB example), 465-470 (Pitfall 8)
    - 02-PATTERNS.md lines 218-266 (migrator registry pattern from CommandRegistry), 508-531 (src/types/storage.ts WriteJournalEntry shape)
    - src/core/registry/Registry.ts and src/core/commands/CommandRegistry.ts (Map-registry conventions)
  </read_first>
  <behavior>
    - Test 1 (backward-compatible): a DB opened at v1 with data, then opened at v2 via the framework's migrations, retains the v1 data intact.
    - Test 2 (idempotent): running the migration path twice is a no-op (no ConstraintError, no duplicate stores).
    - Test 3 (fresh-open-at-v2): a DB never opened before, opened directly at v2, has both the v1 and v2 stores/fields (conditional-block correctness).
    - Test 4 (type fidelity): WriteJournalEntry carries id/operation/status/attempts/steps/createdAt with status 'pending'|'applying'|'completed'|'rolled-back' (Appendix C / O.11).
  </behavior>
  <action>
    Create `src/types/storage.ts` (A4 — the O.11 type home): `WriteJournalEntryStatus`, `WriteJournalStepRecord { name, status: 'completed' | 'rolled-back' }`, `WriteJournalEntry { id, operation, status, attempts, steps, createdAt }` — `operation` is typed by a forward-compatible `WriteJournalOperation` string-union placeholder declared IN THIS FILE (all 11 ops, per D-32). This file is the canonical declaration home: WriteJournal.ts (plan 02-05) re-exports the union from `@/types/storage`. Importing the union FROM WriteJournal.ts in this plan is FORBIDDEN — that is a forward reference into a wave-3 plan and breaks tsc in this wave-2 plan; the placeholder here is the mandatory typing path. Add a zod schema for WriteJournalEntry (CLAUDE.md cross-boundary validation convention) exported as `WriteJournalEntrySchema`.

    Create `src/core/storage/IndexedDBMigrator.ts`:
    - `export interface IndexedDBMigration { fromVersion: number; toVersion: number; description: string; migrate(db: IDBPDatabase, tx: IDBPTransaction): Promise<void> }` (spec §20.4 verbatim).
    - A per-DB migration registry (Map<dbName, IndexedDBMigration[]>) with `registerMigration(dbName, migration)` and `getMigrations(dbName)` (CommandRegistry Map pattern).
    - `openVersionedDB<T extends DBSchema>(dbName: string, targetVersion: number, opts?: { blocked?: () => void }): Promise<IDBPDatabase<T>>` — calls `idb.openDB(dbName, targetVersion, { upgrade(db, oldVersion) { for (const m of migrations.filter(m => m.toVersion > oldVersion)) await m.migrate(db, tx) — conditional application; }, blocked })`. Migration failures propagate so the caller records IDB_MIGRATION_FAILED.
    - `bootstrap(): Promise<void>` — opens all five DBs via their module open functions (ChatHistoryDB/MemoryDB/NotesDB/WriteJournalDB/ErrorStore), recording IDB_BLOCKED via the per-DB blocked callback and IDB_MIGRATION_FAILED → ErrorStore + degraded mode per DB (a failed DB is disabled for the session; other DBs continue — D-41).
    - Use `import { openDB, type DBSchema, type IDBPDatabase, type IDBPTransaction } from 'idb'`.

    Create `tests/core/storage/IndexedDBMigrator.test.ts` using a FIXTURE DB name ('FixtureDB') + an in-test migration pair (v1: create 'items' store; v2: create 'tags' store + an index on items) per D-42 — production DBs stay at v1. Cover the four behaviors with `__resetIndexedDB()` per test. Commit RED first, then GREEN.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/storage/IndexedDBMigrator.test.ts</automated>
  </verify>
  <done>
    The v1→v2 fixture passes all three framework proofs (backward-compatible, idempotent, fresh-open-at-v2); WriteJournalEntry types + zod schema compile; the migrator registry applies migrations conditionally.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/storage/IndexedDBMigrator.test.ts` exits 0 with the fixture behaviors passing.
    - `pnpm lint` exits 0 with zero new NP-STRICT markers.
    - A fresh open at v2 yields both stores (asserted in test 3); a second migration run raises no ConstraintError (test 2).
  </acceptance_criteria>
  <reversibility rating="costly">D-41 (user-locked): DB_VERSION and store schemas are a persisted contract — renumbering later requires migration churn. Flagged only.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap ChatHistoryDB + MemoryDB + NotesDB at v1 + declare-now Setting.ts</name>
  <files>src/core/storage/ChatHistoryDB.ts, src/core/storage/MemoryDB.ts, src/core/storage/NotesDB.ts, src/core/storage/Setting.ts, tests/core/storage/IndexedDBMigrator.test.ts</files>
  <read_first>
    - 02-RESEARCH.md lines 368-376 (§15.1 verbatim store lists: ChatHistoryDB sessions/messages; NotesDB notes/concepts + getNoteByTitle; MemoryDB messages keyPath [conversationId, seq] + userFacts + conversationSummaries), 354-366 (migrator policy)
    - 02-PATTERNS.md lines 269-280 (DB module template: DB_NAME const, DB_VERSION = 1, DBSchema, open*DB)
    - 02-CONTEXT.md lines 68-72 (the agent's Discretion: Setting.ts may be declare-now; schema detail follows §15.1/Appendix C)
  </read_first>
  <behavior>
    - Test 1: bootstrap() opens ChatHistoryDB/MemoryDB/NotesDB at version 1 with the §15.1 stores present (sessions + messages; messages keyPath [conversationId, seq] + userFacts + conversationSummaries; notes + concepts).
    - Test 2: opening the same DB twice does not re-create stores (idempotent open — no ConstraintError).
    - Test 3: Setting<T> serializes writes (a write queue guarantees the §13 serialized-write rule at the type level).
  </behavior>
  <action>
    Create the three DB modules following the shared template from `IndexedDBMigrator`:
    - `ChatHistoryDB.ts` — `CHAT_HISTORY_DB`, `CHAT_HISTORY_DB_VERSION = 1`; `ChatHistoryDBV1 extends DBSchema` with `sessions` (keyPath 'id'; fields per §15.1: id/title/created/updated/starred/preview) and `messages` (keyPath 'id', index 'sessionId'; fields: sessionId/role/content/timestamp/metadata); `openChatHistoryDB()` via `openVersionedDB`.
    - `MemoryDB.ts` — `MEMORY_DB`, `MEMORY_DB_VERSION = 1`; `MemoryDBV1` with `messages` keyPath `[conversationId, seq]` (spec-explicit) + `userFacts` (UserMemoryFact[]) + `conversationSummaries` (keyPath 'conversationId'); `openMemoryDB()`.
    - `NotesDB.ts` — `NOTES_DB`, `NOTES_DB_VERSION = 1`; `NotesDBV1` with `notes` (keyPath 'id'; fields per §15.1: id/title/content/created/updated/tags[]/links[]/source/aiMeta/version) and `concepts` (keyPath 'slug'; fields: slug/label/summary/noteIds[]/aliases[]/updatedAt); declare `getNoteByTitle()` per §15.1; `openNotesDB()`.
    - Bootstrap each DB at v1 with the standard `if (oldVersion < 1)` conditional block; `blocked` callback records IDB_BLOCKED via the injected onBlocked path (no circular import: ErrorStore is imported by the migrator's bootstrap wiring, not by the DB modules).
    - Create `Setting.ts` declare-now (planner discretion per CONTEXT): export a `Setting<T>` generic (get/set/delete) built on a serialized-write queue — a module-level promise chain guaranteeing the §13 "never write two Setting<T> keys concurrently" rule at the type level. No production consumer in Phase 2; document declare-now/populate-later in a comment.
    - Extend `tests/core/storage/IndexedDBMigrator.test.ts` with the bootstrap-open cases (behaviors 1-2) and a small Setting serialization case (behavior 3).
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/storage/IndexedDBMigrator.test.ts</automated>
  </verify>
  <done>
    Three DB modules compile and open at v1 with the §15.1 store lists; bootstrap() opens them; Setting<T> provides the serialized-write queue; the new test cases pass.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/storage/IndexedDBMigrator.test.ts` exits 0 including the bootstrap cases (stores present, idempotent re-open).
    - Each DB module exports `DB_NAME`/`DB_VERSION = 1`/typed `DBSchema`/`open*DB()` per the PATTERNS template.
    - MemoryDB's messages store declares keyPath `[conversationId, seq]` (spec-explicit, asserted by inspection of MemoryDB.ts).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WriteJournalDB + ErrorStore (FIFO-100) + unlimitedStorage manifest permission</name>
  <files>src/core/storage/WriteJournalDB.ts, src/core/storage/ErrorStore.ts, wxt.config.ts, tests/core/storage/ErrorStore.test.ts</files>
  <read_first>
    - 02-RESEARCH.md lines 516-542 (WriteJournalDB openDB example: entries store keyPath 'id', blocked → IDB_BLOCKED), 593-610 (ErrorStore FIFO-100 write with redaction), 719-740 (Security Domain), 455-463 (Pitfall 7: session cap NOT lifted)
    - 02-PATTERNS.md lines 133-181 (ErrorStore = persistent sibling of debugLog), 485-489 (wxt.config.ts modification)
    - 02-CONTEXT.md D-33 (journal entries in WriteJournalDB idb store, metadata-only), D-39 (ErrorStore FIFO 100, debug only), D-40 (unlimitedStorage)
    - src/core/log/debugLog.ts (module shape + MAX_LOG_ENTRIES pattern)
  </read_first>
  <behavior>
    - Test 1: ErrorStore.record writes a typed error to the 'errors' store and evicts the oldest entry when the count exceeds 100 (FIFO).
    - Test 2: ErrorStore redacts sensitive context keys at the write boundary (apiKey values emptied via redactSensitive).
    - Test 3: ErrorStore.record never rethrows — when the IDB write fails, it falls back to debugLog (best-effort, RESEARCH Open Question 4).
    - Test 4: wxt.config.ts permissions include 'unlimitedStorage' (grep gate) and the set is exactly the four Phase-2 permissions.
  </behavior>
  <action>
    Create `src/core/storage/WriteJournalDB.ts` — `WRITE_JOURNAL_DB`, `WRITE_JOURNAL_DB_VERSION = 1`; `WriteJournalDBV1 extends DBSchema` with `entries` (keyPath 'id', value `WriteJournalEntry` from @/types/storage); `openWriteJournalDB()` via `openVersionedDB` with the standard v1 conditional block and a blocked callback recording IDB_BLOCKED. Entries are metadata-only (no message bodies — D-33).

    Create `src/core/storage/ErrorStore.ts`:
    - `export interface NowPilotErrorRecord { code: string; message: string; context?: Record<string, unknown>; timestamp: number }` (RESEARCH lines 593-610).
    - `record(err: { code: string; message: string; context?: Record<string, unknown> }): Promise<void>` — opens ErrorStore (autoIncrement 'errors' store), adds the entry with `context: redactSensitive(err.context)` and current timestamp, counts, and deletes the oldest entry via cursor when the count exceeds 100; `await tx.done` as commit signal. Wrapped in an internal try/catch that falls back to `debugLog(code, message)` and NEVER rethrows into the caller (best-effort — Open Question 4).
    - `queryRecent(limit: number): Promise<NowPilotErrorRecord[]>` for the debug-only surface (Phase-11 Diagnostics reads it).
    - Own open path via the migrator with a no-op blocked handler (its own failure falls back to debugLog — no circular import).
    - NEVER writes into the user UI; debug-only per §15.1.

    Modify `wxt.config.ts`: `permissions: ['sidePanel', 'storage', 'tabs', 'unlimitedStorage']` (line 36) and update the D-19a comment block (lines 29-35) to record the Phase-2 addition per ADR-STACK-02/REQ-R06 — noting unlimitedStorage does NOT lift the chrome.storage.session 10MB cap (Pitfall 7) and that Phase 19's CWS review must justify it.

    Create `tests/core/storage/ErrorStore.test.ts` covering behaviors 1-3; verify behavior 4 with a grep gate.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/storage/ErrorStore.test.ts tests/core/storage/IndexedDBMigrator.test.ts && grep -c "unlimitedStorage" wxt.config.ts</automated>
  </verify>
  <done>
    WriteJournalDB opens at v1 with the entries store; ErrorStore records + evicts + redacts + never rethrows (tests green); wxt.config.ts carries exactly the four Phase-2 permissions with the D-19a comment updated.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/storage/ErrorStore.test.ts` exits 0 with FIFO-100, redaction, and best-effort behaviors passing.
    - `grep -c "unlimitedStorage" wxt.config.ts` ≥ 1 and the permissions array is exactly `['sidePanel', 'storage', 'tabs', 'unlimitedStorage']` (source assertion).
    - WriteJournalDB's entries store schema references WriteJournalEntry (compiles strict-clean).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `src/types/storage.ts` — `WriteJournalEntryStatus`, `WriteJournalStepRecord`, `WriteJournalEntry`, `WriteJournalEntrySchema` (zod)
- `src/core/storage/IndexedDBMigrator.ts` — `IndexedDBMigration`, per-DB migration registry (`registerMigration`, `getMigrations`), `openVersionedDB`, `bootstrap`
- `src/core/storage/ChatHistoryDB.ts` — `CHAT_HISTORY_DB`, `CHAT_HISTORY_DB_VERSION`, `ChatHistoryDBV1`, `openChatHistoryDB`
- `src/core/storage/MemoryDB.ts` — `MEMORY_DB`, `MEMORY_DB_VERSION`, `MemoryDBV1` (messages keyPath `[conversationId, seq]`), `openMemoryDB`
- `src/core/storage/NotesDB.ts` — `NOTES_DB`, `NOTES_DB_VERSION`, `NotesDBV1`, `getNoteByTitle`, `openNotesDB`
- `src/core/storage/WriteJournalDB.ts` — `WRITE_JOURNAL_DB`, `WRITE_JOURNAL_DB_VERSION`, `WriteJournalDBV1`, `openWriteJournalDB`
- `src/core/storage/ErrorStore.ts` — `NowPilotErrorRecord`, `record`, `queryRecent`
- `src/core/storage/Setting.ts` — `Setting<T>` (serialized-write queue, declare-now)
- `wxt.config.ts` — `unlimitedStorage` in manifest permissions
- `tests/core/storage/IndexedDBMigrator.test.ts` (extended), `tests/core/storage/ErrorStore.test.ts`

## Flagged Assumptions (spec-less probe fallback)

- **REQ-R06 (unclassified → resolved manually):** the edge is the manifest boundary — exactly `['sidePanel','storage','tabs','unlimitedStorage']`; session 10MB cap not lifted. Surfaced in frontmatter `must_haves.assumptions`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension-origin IndexedDB ↔ migrator/DB modules | schema upgrade and data migration cross here |
| manifest permissions → Chrome | unlimitedStorage grants quota/eviction exemption |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Tampering | IDB upgrade replay / duplicate store creation | medium | mitigate | conditional `if (oldVersion < N)` blocks + skip-if-present guards; v1→v2 fixture proves idempotency (Pitfall 8) |
| T-02-04-02 | Denial of Service | IDB quota/eviction of large bodies | medium | mitigate | `unlimitedStorage` permission (REQ-R06/ADR-STACK-02) exempts the origin; `navigator.storage.persist()` is NOT a substitute |
| T-02-04-03 | Denial of Service | blocked or failed DB migration blocking the extension | medium | mitigate | IDB_BLOCKED / IDB_MIGRATION_FAILED → ErrorStore + degraded mode: only the failed DB is disabled, other stores continue (D-41) |
| T-02-04-04 | Information Disclosure | sensitive context persisted in ErrorStore | low | mitigate | redactSensitive at the ErrorStore write boundary (D-39/§16.5); debug-only store |
| T-02-04-05 | Tampering | session-storage cap overflow (unlimitedStorage misread) | low | accept | `unlimitedStorage` does not lift the session 10MB cap (Pitfall 7); np_workspace_primary records stay tiny — documented in the wxt.config comment |
</threat_model>

<verification>
- Automated: `pnpm lint && pnpm exec vitest run tests/core/storage` (IndexedDBMigrator fixture + bootstrap + ErrorStore) and `grep -c "unlimitedStorage" wxt.config.ts`.
- Permission audit: source assertion that the manifest permissions array is exactly the four Phase-2 entries.
</verification>

<success_criteria>
- v1→v2 fixture passes (success criterion 4): backward-compatible, idempotent, fresh-open-at-v2.
- All five DBs bootstrap at v1 via the shared framework; degraded mode per DB on failure.
- ErrorStore FIFO-100 + redaction proven; unlimitedStorage in the manifest.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-04-SUMMARY.md` when done
</output>