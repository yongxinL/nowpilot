---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 06
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/core/workspace/WorkspaceElection.ts
  - src/core/workspace/WorkspaceSync.ts
  - src/core/theme/chromeStorageAdapter.ts
  - package.json
  - tests/core/workspace/WorkspaceElection.test.ts
  - tests/core/storage/chromeStorageAdapter.test.ts
autonomous: true
requirements: [REQ-R03, REQ-R07]
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WorkspaceElection implements the complete primary-writer election (D-24/D-25, spec §20.11): startup CAS on `np_workspace_primary` in chrome.storage.session, 3s heartbeat, 2-missed-heartbeats → re-election, Standalone view tie-break priority."
    - "WorkspaceElection owns the ONLY 3s tick in the workspace layer (D-26); WorkspaceSync stays timer-free and grows only the `WorkspaceSyncMessage` union with `WORKSPACE_HEARTBEAT`."
    - "A lone surface (no other surface seen — BroadcastBus suppresses self-messages) resolves to solo/primary using the session record's `electedAt` freshness; a stale record or a foreign heartbeat triggers re-election (RESEARCH Pitfall 4)."
    - "The election record lives in chrome.storage.session ONLY (spec §15.1 — transient coordination state), written on every tick (1/3s = 20/min, NOT debounced per D-43)."
    - "Error-state transitions emit the existing canonical codes WORKSPACE_ELECTION_TIMEOUT / WORKSPACE_STORAGE_UNAVAILABLE via debugLog/ErrorStore — no invented codes (D-38)."
    - "chromeStorageAdapter surfaces storage write failures as exactly two new canonical codes: STORAGE_QUOTA (QUOTA/QUOTA_BYTES message text) and STORAGE_RATE_LIMIT (MAX_WRITE_OPERATIONS message text); anything else keeps STORAGE_DEBOUNCE_FLUSH_FAILED — never swallowed (D-38/D-39, REQ-R07)."
    - "The adapter emits typed errors through a single reporter hook; exactly one ErrorStore entry is recorded by the registered reporter (D-39 ownership rule — the adapter does not import ErrorStore directly)."
  artifacts:
    - "src/core/workspace/WorkspaceElection.ts — WorkspaceCoordinationState machine, CAS, heartbeat, re-election, dispose"
    - "src/core/workspace/WorkspaceSync.ts — WORKSPACE_HEARTBEAT union variant + notifyWorkspaceHeartbeat"
    - "src/core/theme/chromeStorageAdapter.ts — classifyStorageError + error reporter hook in both flush catches"
    - "tests/core/workspace/WorkspaceElection.test.ts, tests/core/storage/chromeStorageAdapter.test.ts (extended)"
  key_links:
    - "WorkspaceElection heartbeat ↔ notifyWorkspaceHeartbeat (WorkspaceSync) ↔ np_workspace BroadcastChannel ↔ BroadcastBus self-suppression"
    - "WorkspaceElection isPrimaryWriter() ↔ WorkspaceStore persist gate (plan 02-07)"
    - "chromeStorageAdapter reporter hook ↔ ErrorStore.record registration (plan 02-07 boot wiring)"
  assumptions:
    - "Dependency trim (checker blocker resolution): the original `depends_on: [02-01, 02-04]` was conservative — this plan consumes NO 02-04 artifact. WorkspaceElection tests use the 02-01 session mock + BroadcastBus + fake timers; the adapter tests extend the existing Phase-1 test file; nothing imports IndexedDBMigrator / WriteJournalDB / ErrorStore / src/types/storage.ts (the adapter must NOT import ErrorStore per D-39 — recording lands with the 02-07 reporter registration). depends_on is [02-01]; wave stays 2."
    - "REQ-R07 boundary (probe row): classification boundary is the message-text regex contract — /QUOTA|QUOTA_BYTES/i → STORAGE_QUOTA (both QUOTA_BYTES and QUOTA_BYTES_PER_ITEM land here); /MAX_WRITE_OPERATIONS/i → STORAGE_RATE_LIMIT; no match → STORAGE_DEBOUNCE_FLUSH_FAILED (fallback preserved)."
    - "REQ-R07 precision (probe row): classification is case-insensitive, QUOTA is checked before MAX_WRITE_OPERATIONS, and the fallback code is never dropped — exact contract asserted by the adapter tests."
    - "A5 (RESEARCH): heartbeat session writes at 1/3s = 20/min are safely under the ~120/min boundary; combined steady-state ≤30/min is asserted in plan 02-07."
  prohibitions:
    - "A second heartbeat timer anywhere (D-26 — WorkspaceElection owns the only 3s tick)"
    - "Writing np_workspace_primary to chrome.storage.local (session only — §15.1)"
    - "Inventing error codes (exactly two additions: STORAGE_QUOTA, STORAGE_RATE_LIMIT — D-38 closed-set rule)"
    - "Swallowing storage write failures into debugLog without classification (D-39 — REQ-R07)"
    - "New `@ts-expect-error NP-STRICT` markers (Pitfall 9)"
---

<objective>
Primary-writer election (D-24/D-25/D-26/D-27) + storage error surfacing (D-38/D-39, REQ-R07): the WorkspaceElection state machine with its single 3s heartbeat, and the chromeStorageAdapter's STORAGE_QUOTA / STORAGE_RATE_LIMIT classification pipeline.

Purpose: replaces the Phase-1 `isPrimaryWriter()` stub (the D-16 swap point) with a real election whose predicate gates the journaled np_workspace persist (plan 02-07); and fixes the CONCERNS "unhandled QUOTA_BYTES in the adapter" by surfacing storage failures as the two REQ-R07 canonical codes into ErrorStore + debugLog — never swallowed, never user-facing.
Output: tested election state machine + tested adapter error classification; both wired in plan 02-07.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md
@src/core/workspace/WorkspaceSync.ts
@src/core/theme/chromeStorageAdapter.ts
@src/core/runtime/BroadcastBus.ts
@src/core/log/debugLog.ts
@tests/setup.ts
@tests/core/storage/chromeStorageAdapter.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: WorkspaceElection state machine + WORKSPACE_HEARTBEAT — CAS, heartbeat, re-election, tie-break, solo</name>
  <files>src/core/workspace/WorkspaceElection.ts, src/core/workspace/WorkspaceSync.ts, tests/core/workspace/WorkspaceElection.test.ts</files>
  <read_first>
    - 02-RESEARCH.md lines 218-250 (Pattern 1: state union verbatim, election rules verbatim, lone-surface trap, session-mock need, __test__ timer seam), 553-563 (session record shape + CAS)
    - 02-PATTERNS.md lines 349-416 (WorkspaceElection composite pattern: state union, timer seam from chromeStorageAdapter, heartbeat publish, WorkspaceSync union change)
    - src/core/workspace/WorkspaceSync.ts (the file being modified — union at lines 5-8, notifyWorkspaceUpdate at 18-24)
    - src/core/runtime/BroadcastBus.ts (self-message suppression at line 27 — the lone-surface trap)
    - src/core/theme/chromeStorageAdapter.ts lines 33-42, 231-247 (the __test__ timer seam pattern to mirror)
    - tests/setup.ts (the chrome.storage.session mock from plan 02-01)
  </read_first>
  <behavior>
    - Test 1 (CAS): two surfaces start concurrently → exactly one becomes primary (Standalone tie-break wins when both attempt at the same electedAt window).
    - Test 2 (heartbeat): advancing fake timers by 3000ms publishes WORKSPACE_HEARTBEAT and refreshes the session record's electedAt.
    - Test 3 (2-miss re-election): a primary that misses 2 heartbeat windows (its session record goes stale) is re-elected by another surface; a stale-record primary demotes.
    - Test 4 (solo): a lone surface (no inbound heartbeats — BroadcastBus self-suppression) resolves to state 'solo' and isPrimaryWriter() === true.
    - Test 5 (demotion): a secondary surface receiving a foreign heartbeat marks the primary alive and stays secondary (isMirroring true).
    - Test 6 (dispose): dispose() clears the timer and unsubscribes — no further heartbeats.
  </behavior>
  <action>
    Modify `src/core/workspace/WorkspaceSync.ts`: add `| { type: 'WORKSPACE_HEARTBEAT'; surface: ActiveSurface; workspaceId: string }` to the `WorkspaceSyncMessage` union and export `notifyWorkspaceHeartbeat(surface: ActiveSurface, workspaceId: string): void` mirroring `notifyWorkspaceUpdate` (publish on the existing 'np_workspace' channel — no timer, no channel change, D-26).

    Create `src/core/workspace/WorkspaceElection.ts`:
    - `export type WorkspaceCoordinationState` — the spec §20.11 union verbatim (solo/primary/secondary/election-in-progress/error with codes 'ELECTION_TIMEOUT' | 'STORAGE_UNAVAILABLE').
    - A module-level current-state holder + `export function getState(): WorkspaceCoordinationState`, `export function isPrimaryWriter(): boolean` (pure read — true when state is 'primary' or 'solo'; D-24 the WorkspaceStore delegates to this).
    - `export interface ElectionInstance { start(surface: ActiveSurface): Promise<void>; dispose(): void }` + `export async function startElection(surface: ActiveSurface): Promise<ElectionInstance>`:
      - CAS on `np_workspace_primary` {tabId, surface, electedAt} in chrome.storage.session: read → if empty or stale (electedAt older than 2 heartbeat windows) → write own record (Standalone wins the concurrent tie-break — compare surface priority when both attempt in the same window); if a fresh foreign record exists → secondary.
      - Heartbeat loop via a `__test__`-seamed timer (mirror chromeStorageAdapter's timerFactory/timerClear pattern): every 3000ms publish `notifyWorkspaceHeartbeat(surface, workspaceId)` AND refresh the session record's electedAt (D-43 — session write NOT debounced; 20/min).
      - Inbound `WORKSPACE_HEARTBEAT` (only from other surfaces — BroadcastBus suppresses self): mark primary alive; if we were primary → demote to 'secondary'.
      - 2-missed-heartbeat detection: if no foreign heartbeat for 2 intervals AND the session record's electedAt is stale (not ours / too old) → re-election CAS (lone-surface trap resolution: no-foreign-heartbeat + fresh-own-record ⇒ 'solo').
      - Error paths use the existing canonical codes via debugLog: WORKSPACE_ELECTION_TIMEOUT, WORKSPACE_STORAGE_UNAVAILABLE (never invented).
      - `dispose()` clears the timer and unsubscribes the inbound handler.
    - Lifecycle rule (D-25): exactly one instance per surface; the boot wiring (plan 02-07) creates it during WorkspaceStore bootstrap and disposes on surface unload — enforce with a guard (throw if a second instance starts without dispose).

    Create `tests/core/workspace/WorkspaceElection.test.ts` covering the six behaviors with `vi.useFakeTimers()` + the `__test__` timer seam + the session mock (`__chromeSessionMap` reset per test). Commit RED first, then GREEN.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/workspace/WorkspaceElection.test.ts</automated>
  </verify>
  <done>
    Election tests green (CAS/tie-break, heartbeat, 2-miss re-election, solo, demotion, dispose); WorkspaceSync union carries WORKSPACE_HEARTBEAT; exactly one 3s timer owner; no invented error codes.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/workspace/WorkspaceElection.test.ts` exits 0 with the 6 behaviors passing.
    - Source assertion: `grep -n "setInterval\|setTimeout" src/core/workspace/` matches only WorkspaceElection.ts's single heartbeat timer (WorkspaceSync stays timer-free).
    - Session-record writes never target chrome.storage.local (source assertion: np_workspace_primary appears only in session calls).
  </acceptance_criteria>
  <reversibility rating="costly">D-24 (user-locked): the election replaces the frozen Phase-1 predicate contract; every later phase's write gating reads through isPrimaryWriter(). Flagged only.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chromeStorageAdapter error surfacing — STORAGE_QUOTA / STORAGE_RATE_LIMIT classification + reporter hook</name>
  <files>src/core/theme/chromeStorageAdapter.ts, tests/core/storage/chromeStorageAdapter.test.ts, package.json</files>
  <read_first>
    - src/core/theme/chromeStorageAdapter.ts (the file being modified — flush catches at lines 154-156 and 210-212; STORAGE_DEBOUNCE_MS at line 14; __test__ seam at 231-247)
    - 02-RESEARCH.md lines 453-463 (Pitfall 6: quota errors arrive as message text, not typed codes — classification by matching), 444-452 (Pitfall 5)
    - 02-PATTERNS.md lines 446-464 (adapter modification spec: classify QUOTA* → STORAGE_QUOTA; MAX_WRITE_OPERATIONS* → STORAGE_RATE_LIMIT; else STORAGE_DEBOUNCE_FLUSH_FAILED; keep the debounce + flush behavior intact)
    - 02-CONTEXT.md D-38/D-39 (exactly two codes; adapter emits typed errors, caller records one ErrorStore entry)
    - tests/core/storage/chromeStorageAdapter.test.ts (the file being extended — mock set must be able to reject)
  </read_first>
  <behavior>
    - Test 1: a flush whose chrome.storage.local.set rejects with message text containing 'QUOTA_BYTES' → the reporter receives code STORAGE_QUOTA.
    - Test 2: a flush rejecting with 'MAX_WRITE_OPERATIONS_PER_MINUTE' → STORAGE_RATE_LIMIT.
    - Test 3: a flush rejecting with an unrelated message → STORAGE_DEBOUNCE_FLUSH_FAILED (fallback preserved — no code dropped).
    - Test 4: exactly one reporter invocation per failed flush (no duplicate persistence — D-39 ownership rule); the reporter is a registered hook, not an ErrorStore import in the adapter.
  </behavior>
  <action>
    Modify `src/core/theme/chromeStorageAdapter.ts`:
    1. Add `export function classifyStorageError(err: unknown): string` — pure classifier: read `err?.message ?? String(err)`; if `/QUOTA|QUOTA_BYTES/i` matches → return 'STORAGE_QUOTA'; else if `/MAX_WRITE_OPERATIONS/i` matches → return 'STORAGE_RATE_LIMIT'; else return 'STORAGE_DEBOUNCE_FLUSH_FAILED' (case-insensitive; QUOTA checked first — REQ-R07 boundary/precision contract).
    2. Add a module-level error reporter hook: `let errorReporter: ((entry: { code: string; message: string; context?: Record<string, unknown> }) => void) | null = null` with `export function setStorageErrorReporter(fn)` and a default no-op. The adapter does NOT import ErrorStore (D-39 ownership rule — the boot wiring in plan 02-07 registers the reporter = ErrorStore.record + debugLog; tests register spies).
    3. Replace BOTH flush catch bodies (setItem lines 154-156 and syncStorageAdapter.setItem lines 210-212): classify via `classifyStorageError(err)`, then invoke the reporter with `{ code, message: err?.message ?? String(err), context: { key: /* the batch keys being flushed */ } }` (context passes through redactSensitive at the ErrorStore boundary, not here). Never swallow — the failure is always surfaced. Keep `flushPendingWrites()` behavior stable for the beforeunload path (D-43 — debounce untouched).
    4. Keep STORAGE_DEBOUNCE_MS = 300, the `__test__` seam, and D-22 semantics fully intact.

    Extend `tests/core/storage/chromeStorageAdapter.test.ts`: the mocked `chrome.storage.local.set` must be able to reject — use `vi.mocked(...).mockRejectedValueOnce(new Error('QUOTA_BYTES quota exceeded'))` and `new Error('MAX_WRITE_OPERATIONS_PER_MINUTE exceeded')`; register a spy reporter via `setStorageErrorReporter`; assert the four behaviors + exactly-one-invocation. Restore the reporter to no-op in afterEach.

    Modify `package.json`: extend the `verify:phase-2` workspace path from `tests/core/workspace/WorkspacePersistence.test.ts` to `tests/core/workspace` so the phase's WorkspaceElection.test.ts runs in the gate (the phase-required election tests must be part of the phase gate; existing WorkspaceStore/WorkspaceRouter tests remain covered).
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/storage/chromeStorageAdapter.test.ts tests/core/workspace/WorkspaceElection.test.ts</automated>
  </verify>
  <done>
    Adapter classification tests green (STORAGE_QUOTA, STORAGE_RATE_LIMIT, fallback, exactly-one-reporter-call); the reporter hook is registered by wiring, not an ErrorStore import; the debounce and flush behavior are unchanged; verify:phase-2 now includes all workspace tests.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/storage/chromeStorageAdapter.test.ts` exits 0 with the 4 new behaviors passing (plus the existing D-22 tests still green).
    - Source assertion: `grep -n "ErrorStore" src/core/theme/chromeStorageAdapter.ts` matches nothing (the adapter emits typed errors only — D-39 ownership rule).
    - package.json verify:phase-2 runs `tests/core/workspace` (source assertion).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `src/core/workspace/WorkspaceElection.ts` — `WorkspaceCoordinationState` (spec union), `getState()`, `isPrimaryWriter()`, `ElectionInstance`, `startElection()`, `__test__` timer seam
- `src/core/workspace/WorkspaceSync.ts` — `WORKSPACE_HEARTBEAT` union variant, `notifyWorkspaceHeartbeat()`
- `src/core/theme/chromeStorageAdapter.ts` — `classifyStorageError()`, `setStorageErrorReporter()`, updated flush catches
- `package.json` — verify:phase-2 workspace path widened to `tests/core/workspace`
- `tests/core/workspace/WorkspaceElection.test.ts`, `tests/core/storage/chromeStorageAdapter.test.ts` (extended)

## Flagged Assumptions (spec-less probe fallback)

- **REQ-R07 boundary (probe row):** the classification threshold contract — both QUOTA_BYTES and QUOTA_BYTES_PER_ITEM map to STORAGE_QUOTA; MAX_WRITE_OPERATIONS* maps to STORAGE_RATE_LIMIT; no match keeps STORAGE_DEBOUNCE_FLUSH_FAILED. Case-insensitive, QUOTA-first precedence.
- **REQ-R07 precision (probe row):** exact classification contract asserted by tests (regex set, precedence, fallback never dropped).
Both surfaced in frontmatter `must_haves.assumptions`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| surface ↔ chrome.storage.session | the np_workspace_primary election record (transient coordination state) |
| surface ↔ chrome.storage.local/sync | debounced data writes whose failures are classified here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Spoofing | stale/false election primary record | medium | mitigate | electedAt freshness check + 2-miss re-election + CAS on np_workspace_primary; record lives in session (cleared on browser close) — §20.11 rules unit-tested |
| T-02-06-02 | Denial of Service | silent storage write loss (quota/rate-limit) | high | mitigate | classifyStorageError → STORAGE_QUOTA / STORAGE_RATE_LIMIT → ErrorStore + debugLog; never swallowed (REQ-R07/D-39); D-22 debounce keeps steady-state ≤30/min (asserted in plan 02-07) |
| T-02-06-03 | Spoofing | lone-surface heartbeat ambiguity | low | mitigate | solo detection via session-record freshness (Pitfall 4); 1-surface and 2-surface cases unit-tested |
| T-02-06-04 | Integrity | invented error codes breaking the closed-set audit | medium | mitigate | exactly two additions (STORAGE_QUOTA, STORAGE_RATE_LIMIT); election errors reuse WORKSPACE_ELECTION_TIMEOUT / WORKSPACE_STORAGE_UNAVAILABLE; grep audit in verification |
</threat_model>

<verification>
- Automated: `pnpm lint && pnpm exec vitest run tests/core/workspace/WorkspaceElection.test.ts tests/core/storage/chromeStorageAdapter.test.ts`.
- Timer audit: `grep -rn "setInterval\|setTimeout" src/core/workspace/` shows exactly the one heartbeat timer in WorkspaceElection.ts.
- Code-set audit: `grep -rn "STORAGE_QUOTA\|STORAGE_RATE_LIMIT\|WORKSPACE_ELECTION_TIMEOUT\|WORKSPACE_STORAGE_UNAVAILABLE" src/core/` matches only the intended additions/usages.
</verification>

<success_criteria>
- Election state machine proven (CAS, 3s heartbeat, 2-miss re-election, Standalone tie-break, solo, dispose).
- Adapter surfaces the two REQ-R07 codes + fallback, never swallows; exactly one ErrorStore record per failure via the registered reporter.
- verify:phase-2 gate includes the election tests.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-06-SUMMARY.md` when done
</output>