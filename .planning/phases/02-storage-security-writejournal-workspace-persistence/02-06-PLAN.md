---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/core/storage/IndexedDBMigrator.ts
  - src/core/storage/ErrorStore.ts
  - tests/core/storage/IndexedDBMigrator.test.ts
  - tests/core/storage/ErrorStore.test.ts
autonomous: true
requirements: [STORAGE-01]
must_haves:
  truths:
    - "IndexedDBMigrator opens version-change connections via RAW indexedDB.open + sync dispatch + idb wrap() — never idb openDB with a throwing upgrade (fake-indexeddb double-settle landmine, RESEARCH Pitfall 1)."
    - "The §20.4 IndexedDBMigration interface is kept verbatim: { fromVersion, toVersion, description, migrate(db, tx) }."
    - "A migration failure records IDB_MIGRATION_FAILED in ErrorStore and enters degraded mode = read-only for the affected DB + module-level degraded state { db, reason } — in-memory fallback REJECTED (D-12)."
    - "The synthetic v1→v2 fixture migration adds a store + adds an index + carries data, and is idempotent (D-13/D-14)."
    - "ErrorStore is an IndexedDB store (FIFO max 100, debug-only) with redaction-before-write (R-10/D-16) and is the IDB_MIGRATION_FAILED sink (D-12)."
  artifacts:
    - src/core/storage/IndexedDBMigrator.ts
    - src/core/storage/ErrorStore.ts
    - tests/core/storage/IndexedDBMigrator.test.ts
    - tests/core/storage/ErrorStore.test.ts
  key_links:
    - "IndexedDBMigrator ↔ ErrorStore (IDB_MIGRATION_FAILED sink + degraded state)"
    - "IndexedDBMigrator raw-open pattern ↔ vitest exit-code gate (a leaked rejection fails verify:phase-2)"
    - "ErrorStore redaction-before-write ↔ redactSensitive (02-02)"
---

<objective>
Ship the IndexedDB migration framework (D-14) with the synthetic v1→v2 proof (D-13) and the ErrorStore debug sink with redaction-before-write (R-10/D-16). The migrator's raw-open + sync-dispatch pattern is THE phase landmine solution (RESEARCH Pitfall 1) — a throwing migration must not leak an unhandled rejection that fails CI.

Purpose: Future schema changes (e.g. Phase 5a's v4 notes_backup_config) extend the migration registry rather than hand-patching stores; degraded mode guarantees reads still work and writes are blocked with a typed error instead of silently splitting brain.
Output: IndexedDBMigrator + ErrorStore + their tests (fixture: migration).

Flagged assumptions (specless probe):
- A-13: In-memory fallback is REJECTED for degraded mode (D-12) — a migration failure never shadows writes to memory; the DB stays read-only at v(n-1).
- A-14: The original migration error is captured inside the upgrade callback before the abort propagates (RESEARCH Pitfall 3 — the AbortError swallows the original message otherwise).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md §20.4 (lines 3209-3225 — IndexedDBMigration interface + migration-failure policy), §15.1 (line 1963 — ErrorStore FIFO max 100 debug-only)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md D-12, D-13, D-14
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md Pattern 2 (raw-open + wrap — the critical verified pattern), Pitfalls 1-3 (fake-indexeddb double-settle, async-upgrade tx close, swallowed error)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md (IndexedDBMigrator + ErrorStore adaptation notes)
@src/core/security/redactSensitive.ts (02-02 — ErrorStore redaction-before-write)
@src/core/error/errorCodes.ts (IDB_MIGRATION_FAILED, from 02-01)
@tests/fixtures/index.ts (migration builder, from 02-01)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IndexedDBMigrator.ts — raw-open + wrap, verbatim §20.4 interface, degraded mode</name>
  <read_first>
    - 02-RESEARCH.md Pattern 2 (the verified migratorOpen skeleton — raw indexedDB.open, sync dispatch in onupgradeneeded, wrap() on success, reject on error) + Pitfalls 1-3 (the landmine, tx lifetime, swallowed error)
    - PRODUCT_SPEC §20.4 (lines 3209-3224 — IndexedDBMigration interface VERBATIM; failure policy; v4 note as the future extension example)
    - 02-CONTEXT.md D-12 (degraded mode semantics), D-14 (framework + seeding)
    - 02-RESEARCH.md lines 265-271 (data-carry via IDBRequest chaining — never await inside the upgrade callback)
  </read_first>
  <files>
    src/core/storage/IndexedDBMigrator.ts
  </files>
  <action>
    Create src/core/storage/IndexedDBMigrator.ts. Export:
    - `interface IndexedDBMigration { fromVersion: number; toVersion: number; description: string; migrate(db: IDBPDatabase, tx: IDBPTransaction): Promise<void> }` — VERBATIM §20.4.
    - `interface DBVersionMigration { dbName: string; dbVersion: number; migrations: IndexedDBMigration[] }` — the per-DB registry shape (D-14: real stores register their current version + future migrations).
    - `runMigrations(spec: DBVersionMigration): Promise<IDBPDatabase>` — the runner: opens via the RAW `indexedDB.open(name, spec.dbVersion)` pattern (RESEARCH Pattern 2 — onupgradeneeded dispatches each migration whose fromVersion matches the transition, called SYNCronously; onsuccess → wrap(req.result); onerror → reject). Never idb openDB for version-change opens.
    - Migration dispatch detail: inside onupgradeneeded, wrap(db) and wrap(tx) and call `migrate(wrappedDb, wrappedTx)` WITHOUT awaiting (request-chaining for data-carry keeps the tx alive — RESEARCH Pitfall 2); the migrate call is wrapped in try/catch so the ORIGINAL error is captured (store it in a closure) before the throw aborts the tx (Pitfall 3).
    - Failure path: on rejection (AbortError), call `onMigrationFailed(dbName, originalError)` — the injected failure callback (defaults to a debugLog + degraded-state recorder) → records IDB_MIGRATION_FAILED + message + dbName into ErrorStore (Task 2) and marks degraded mode: export `getDegradedDbs(): Array<{ db: string; reason: string }>` (module-level state for the Phase-7 banner, D-12) and `isDbDegraded(dbName: string): boolean` (a store can gate its writes on this). Reads still work at v(n-1) — the runner never deletes data.
    - `assertWritable(dbName): void` — throws a typed error when isDbDegraded(dbName) (write-block gate; in-memory fallback explicitly REJECTED per D-12 — header comment).
    - Header comment: RESEARCH Pattern 2 source + the fake-indexeddb double-settle rationale.
  </action>
  <acceptance_criteria>
    - `grep -c "indexedDB.open" src/core/storage/IndexedDBMigrator.ts` returns >= 1 (raw open, NOT openDB)
    - `grep -c "IndexedDBMigration" src/core/storage/IndexedDBMigrator.ts` returns >= 1
    - `grep -c "getDegradedDbs" src/core/storage/IndexedDBMigrator.ts` returns >= 1; `grep -c "isDbDegraded" src/core/storage/IndexedDBMigrator.ts` returns >= 1
    - `grep -c "IDB_MIGRATION_FAILED" src/core/storage/IndexedDBMigrator.ts` returns >= 1
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "indexedDB.open" src/core/storage/IndexedDBMigrator.ts && grep -c "IDB_MIGRATION_FAILED" src/core/storage/IndexedDBMigrator.ts</automated>
  </verify>
  <done>Migrator runs version-change opens on raw indexedDB.open with sync dispatch; failures record IDB_MIGRATION_FAILED + degraded state; no in-memory fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ErrorStore.ts — FIFO max 100 IndexedDB store with redaction-before-write</name>
  <read_first>
    - PRODUCT_SPEC §15.1 (line 1963 — ErrorStore, debug only, FIFO max 100), §16.5 (lines 2044-2046 — TraceRedactor MUST run before writing to ErrorStore)
    - 02-CONTEXT.md D-16 (every ErrorStore.write routes through the redaction hook)
    - 02-RESEARCH.md Pattern 1 (idb DBSchema + openDB for the store)
    - src/core/security/redactSensitive.ts (02-02 — the redaction primitive)
  </read_first>
  <files>
    src/core/storage/ErrorStore.ts
  </files>
  <action>
    Create src/core/storage/ErrorStore.ts — an IndexedDB store (DB name 'ErrorStore', version 1, object store `errors` keyPath 'id') per §15.1, FIFO max 100, debug-only. Export:
    - `interface ErrorEntry { id: string; ts: number; code: string; message: string; module?: string }` — internal shape (agent discretion beyond FIFO 100 + redaction-before-write).
    - `openErrorStore(): Promise<IDBPDatabase<ErrorStoreSchema>>` — idb openDB (happy-path initial open may use idb openDB with a NON-throwing upgrade per RESEARCH note — no migration history yet).
    - `writeError(code: string, message: string, module?: string): Promise<void>` — REDACTS before persist: run message + code through redactSensitive/TraceRedactor first (R-10, D-16 — assert in tests the persisted entry never contains the raw secret), then put; then enforce FIFO: count entries and delete the oldest by ts when the store exceeds 100 (an index by ts, or a count+getAll-keyed-by-id ordering, agent discretion on mechanics; the 100 cap is the contract).
    - `getErrors(limit?: number): Promise<ErrorEntry[]>` — newest-first read for the Phase-7 Diagnostics surface (read-only).
    - The IDB_MIGRATION_FAILED sink: `recordMigrationFailure(dbName: string, cause: string): Promise<void>` — writes a code 'IDB_MIGRATION_FAILED' entry with dbName + redacted cause (D-12). If ErrorStore itself fails to open, fall back to debugLog (console sink, redacted) + the degraded state stays in-memory (RESEARCH Q4 answer).
    - Every catch → debugLog (Golden Rule 9).
  </action>
  <acceptance_criteria>
    - `grep -c "writeError" src/core/storage/ErrorStore.ts` returns >= 1; `grep -c "getErrors" src/core/storage/ErrorStore.ts` returns >= 1
    - `grep -c "recordMigrationFailure" src/core/storage/ErrorStore.ts` returns >= 1
    - `grep -c "100" src/core/storage/ErrorStore.ts` returns >= 1 (FIFO cap)
    - `grep -c "redactSensitive" src/core/storage/ErrorStore.ts` returns >= 1 (redaction-before-write import)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "recordMigrationFailure" src/core/storage/ErrorStore.ts && grep -c "redactSensitive" src/core/storage/ErrorStore.ts</automated>
  </verify>
  <done>ErrorStore persists redacted debug errors with FIFO 100 and is the IDB_MIGRATION_FAILED sink.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IndexedDBMigrator.test.ts + ErrorStore.test.ts — v1→v2 fixture, degraded, FIFO, redaction</name>
  <read_first>
    - tests/fixtures/index.ts (migration builder — fixed rows + expected v2 schema facts)
    - 02-RESEARCH.md Pattern 2 failure-path verification (lines 426-435 — AbortError name, version stays v(n-1), data intact)
    - tests/core/storage/WriteJournal.test.ts (fake-indexeddb test conventions from 02-04)
    - 02-RESEARCH.md lines 246-270 (the vitest exit-code landmine — the migrator pattern exists to keep `vitest run` exit 0)
  </read_first>
  <files>
    tests/core/storage/IndexedDBMigrator.test.ts
    tests/core/storage/ErrorStore.test.ts
  </files>
  <action>
    Create tests/core/storage/IndexedDBMigrator.test.ts (fixture: migration — D-13 synthetic v1→v2):
    1. Seed a v1 DB (raw indexedDB.open or openDB at version 1) with the fixture's initial store + rows (buildMigrationFixture v1Rows).
    2. runMigrations with the v1→v2 migration (adds a store + adds an index + carries rows via request-chaining) → new DB version is 2, the new store and index exist, carried rows are readable (data survives).
    3. Idempotency: running the runner again at version 2 is a no-op (no double-store, no version bump).
    4. Throws→degraded: a migration that throws → the rejection is an AbortError-named error, isDbDegraded(dbName) is true, getDegradedDbs() lists { db, reason }, the DB still opens at v(n-1) with the original data intact, and assertWritable throws (writes blocked).
    5. The WHOLE FILE runs clean — `vitest run` must exit 0 (the landmine regression: no unhandled rejection from the raw-open path).
    6. ErrorStore recorded IDB_MIGRATION_FAILED via the failure callback (mock/inject recordMigrationFailure or assert the real store entry).

    Create tests/core/storage/ErrorStore.test.ts:
    1. FIFO cap: write 105 entries → getErrors() returns at most 100, oldest dropped.
    2. Redaction-before-write: writeError with a secret-shaped message (buildRedactionFixture secret string) → getErrors() returns the entry with the literal [REDACTED] token and WITHOUT the raw secret substring.
    3. recordMigrationFailure writes a code 'IDB_MIGRATION_FAILED' entry whose cause is redacted.
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/storage/IndexedDBMigrator.test.ts tests/core/storage/ErrorStore.test.ts --reporter=dot` passes — exit code 0 is guaranteed by vitest's own exit status (a leaked unhandled rejection from the raw-open path fails the run; no extra echo needed)
    - Both files import from tests/fixtures (shared builders, D-21)
    - `grep -c "isDbDegraded" tests/core/storage/IndexedDBMigrator.test.ts` returns >= 1
    - `grep -c "\[REDACTED\]" tests/core/storage/ErrorStore.test.ts` returns >= 1 (redaction assertion)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/storage/IndexedDBMigrator.test.ts tests/core/storage/ErrorStore.test.ts --reporter=dot</automated>
  </verify>
  <done>Synthetic v1→v2 migration proves add-store/add-index/data-carry/idempotency/throws→degraded; ErrorStore proves FIFO 100 + redaction-before-write + migration-failure sink.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol | Kind | Location |
|--------|------|----------|
| `IndexedDBMigration` | interface (verbatim §20.4) | src/core/storage/IndexedDBMigrator.ts |
| `DBVersionMigration` | interface (per-DB registry) | src/core/storage/IndexedDBMigrator.ts |
| `runMigrations(spec)` | fn (raw-open runner) | src/core/storage/IndexedDBMigrator.ts |
| `getDegradedDbs()` / `isDbDegraded(dbName)` / `assertWritable(dbName)` | fn | src/core/storage/IndexedDBMigrator.ts |
| `ErrorEntry` / `ErrorStoreSchema` | type/interface | src/core/storage/ErrorStore.ts |
| `openErrorStore()` / `writeError(code, message, module?)` / `getErrors(limit?)` / `recordMigrationFailure(dbName, cause)` | fn | src/core/storage/ErrorStore.ts |
| v1→v2 + degraded + FIFO + redaction tests | test | tests/core/storage/IndexedDBMigrator.test.ts, tests/core/storage/ErrorStore.test.ts |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration code → IndexedDB upgrade tx | Throwing/aborting migrations cross here — the fake-indexeddb double-settle surface |
| error messages → ErrorStore | Debug-only persisted sink — must never receive raw secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-06-01 | Integrity | idb openDB with throwing upgrade | critical | mitigate | Migrator uses raw indexedDB.open + sync dispatch + wrap() (RESEARCH Pattern 2) — verified exit-0 on both happy and failure paths; test 5 is the regression guard for `vitest run` exit code. |
| T-2-06-02 | Information Disclosure | raw secrets reaching ErrorStore | high | mitigate | writeError redacts via redactSensitive BEFORE put (test case 2 asserts [REDACTED] present and raw secret absent); recordMigrationFailure redacts the cause. |
| T-2-06-03 | Availability | migration failure → silent data loss | critical | mitigate | Atomic rollback (version stays v(n-1), data intact — test case 4) + degraded read-only + IDB_MIGRATION_FAILED record (D-12); in-memory fallback explicitly rejected so no split-brain shadow writes. |
| T-2-06-04 | Availability | degraded DB writes silently succeeding | high | mitigate | assertWritable(dbName) throws a typed error when degraded (D-12); store consumers gate writes on isDbDegraded — verified in test case 4. |
| T-2-06-05 | Tampering | original migration error swallowed | medium | mitigate | try/catch inside onupgradeneeded captures the ORIGINAL error before the abort propagates (RESEARCH Pitfall 3) — IDB_MIGRATION_FAILED records the real cause, not the generic AbortError. |
</threat_model>

<verification>
- `pnpm vitest run tests/core/storage/IndexedDBMigrator.test.ts tests/core/storage/ErrorStore.test.ts` green + exit 0 (no unhandled rejection)
- `pnpm vitest run --reporter=dot` full suite green (landmine regression check across ALL files)
- `pnpm typecheck` clean; eslint/prettier clean on touched files
</verification>

<success_criteria>
- STORAGE-01's migration layer + ErrorStore ship: the synthetic v1→v2 migration passes (§18 DONE-when "Migration from v1 → v2 fixture passes").
- Migration failures always record IDB_MIGRATION_FAILED and degrade to read-only with the banner state ready for Phase 7.
- ErrorStore is FIFO-100, redacted, and debug-only.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-06-SUMMARY.md` when done.
</output>
