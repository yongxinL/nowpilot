---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/utils/RateLimiter.ts
  - src/core/http/Requester.ts
  - tests/core/utils/RateLimiter.test.ts
  - tests/core/utils/Requester.test.ts
autonomous: true
requirements: [REQ-R07]
user_setup: []
estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "RateLimiter implements a per-instance token bucket `{ capacity, refillPerSecond }`; `acquire()` returns true while tokens remain and false (never throws) when empty (D-36, spec §13 per-instance rule)."
    - "Requester is a UI-context fetch wrapper (never the background SW — §0.2 boundary) with AbortController threading, a 25s default timeout (matching PROXY_FETCH's Promise.race per §10.7), and structured error codes from the existing canonical set: TIMEOUT / NETWORK / RATE_LIMITED (D-35/D-37/D-38)."
    - "Requester accepts an OPTIONAL injected RateLimiter; with none passed, no throttling occurs (D-37 — no default limiter, keeps aiProvider's streaming path uninstrumented)."
    - "A rate-limited request surfaces RATE_LIMITED; an aborted/timeout request surfaces TIMEOUT; a network failure surfaces NETWORK — each is an existing canonical §21.6 code (no invented codes)."
  artifacts:
    - "src/core/utils/RateLimiter.ts — RateLimiter class (capacity, refillPerSecond, acquire)"
    - "src/core/http/Requester.ts — request() with RequesterOptions { timeoutMs?, rateLimiter? }"
    - "tests/core/utils/RateLimiter.test.ts, tests/core/utils/Requester.test.ts"
  key_links:
    - "Requester.request ← injected RateLimiter (D-37) ← Phase 3 aiProvider consumer"
    - "Requester error classification ↔ canonical §21.6 codes (RATE_LIMITED/TIMEOUT/NETWORK reused — REQ-R07's closed-set rule)"
  assumptions:
    - "Requester has no production consumer in Phase 2 (D-35 — Phase 3 wires aiProvider); the module is verified by unit tests only. Residual risk of an untested integration seam is accepted and revisited at Phase 3 wiring."
  prohibitions:
    - "Inventing error codes (only existing canonical codes RATE_LIMITED / TIMEOUT / NETWORK are used — REQ-R07/D-38 closed-set rule)"
    - "Running fetch from the background service worker (Requester is UI-context only — §0.2 boundary)"
    - "New `@ts-expect-error NP-STRICT` markers (Pitfall 9 — strict-clean from the start)"
---

<objective>
Per-instance RateLimiter (token bucket) + UI-side Requester fetch wrapper — the two standalone utilities from the spec §18 Create list that Phase 3's `aiProvider` consumes.

Purpose: D-35..D-37. Requester is the UI-context HTTP client with AbortController threading, 25s default timeout, optional injected limiter, and canonical error codes — declared now, consumed in Phase 3. RateLimiter proves the token-bucket contract (`RateLimiter.test.ts` is a spec-required test).
Output: two strict-clean modules + two unit test files; no consumer changes (Phase 3's job).
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md
@src/services/aiProvider.ts
@tests/core/storage/chromeStorageAdapter.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RateLimiter + Requester wired — request() with an injected real limiter, proven end-to-end</name>
  <files>src/core/utils/RateLimiter.ts, src/core/http/Requester.ts, tests/core/utils/RateLimiter.test.ts, tests/core/utils/Requester.test.ts</files>
  <read_first>
    - 02-RESEARCH.md lines 565-591 (RateLimiter + Requester shape verbatim, including the canonical error-code mapping), 719-740 (Security Domain V5/V6)
    - 02-PATTERNS.md lines 289-346 (RateLimiter + Requester pattern assignments; aiProvider fetch/error patterns)
    - src/services/aiProvider.ts (the module Requester will serve in Phase 3 — fetch + AbortSignal + error classification conventions, lines ~344-382)
    - tests/core/storage/chromeStorageAdapter.test.ts (test conventions)
  </read_first>
  <behavior>
    - Test 1: RateLimiter with capacity 3, refillPerSecond 1 — three immediate acquire() calls return true, the fourth returns false (burst consumption).
    - Test 2: after refill time elapses, acquire() returns true again (elapsed-time refill; injectable clock for determinism).
    - Test 3: request() with a limiter whose bucket is empty rejects with code RATE_LIMITED before touching fetch.
    - Test 4: request() resolves the underlying fetch response on success (fetch mocked via vi.stubGlobal).
  </behavior>
  <action>
    Create `src/core/utils/RateLimiter.ts`: export `interface RateLimiterOptions { capacity: number; refillPerSecond: number }` and `class RateLimiter`. Constructor stores the options and initializes the token count to capacity; `acquire(): boolean` refills tokens using elapsed-time math (no timers — a `now()` injectable via constructor or `__test__` seam keeps tests deterministic per RESEARCH lines 565-572) and returns true if at least one token is available (consuming it) or false otherwise. Never throws — the caller maps false to the RATE_LIMITED code (D-36).

    Create `src/core/http/Requester.ts`: export `interface RequesterOptions { timeoutMs?: number; rateLimiter?: RateLimiter }` and `async function request(url: string, init: RequestInit, opts: RequesterOptions = {}): Promise<Response>`:
    - Build an AbortController; `setTimeout(() => controller.abort(), opts.timeoutMs ?? 25_000)` (the 25s default matches PROXY_FETCH's Promise.race — §10.7).
    - If `opts.rateLimiter` is present and `acquire()` returns false → throw an Error with `code: 'RATE_LIMITED'` (existing canonical code).
    - `return await fetch(url, { ...init, signal: controller.signal })`.
    - Catch: `AbortError` (name check) → throw Error with `code: 'TIMEOUT'`; any other error → throw with `code: 'NETWORK'`. `finally` clears the timeout.
    - Codes TIMEOUT / NETWORK / RATE_LIMITED are all existing §21.6 canonical codes — do NOT invent (D-38).

    Create `tests/core/utils/RateLimiter.test.ts` and `tests/core/utils/Requester.test.ts` covering the four behaviors; mock `globalThis.fetch` via `vi.stubGlobal` for Requester tests; use `vi.useFakeTimers()` for the timeout path. Commit RED first, then GREEN.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/utils/RateLimiter.test.ts tests/core/utils/Requester.test.ts</automated>
  </verify>
  <done>
    Both test files green (burst, refill, RATE_LIMITED, success, timeout, network); the modules compile strict-clean with zero new NP-STRICT markers; no production consumer was touched.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/utils/RateLimiter.test.ts tests/core/utils/Requester.test.ts` exits 0 with all behaviors passing.
    - `pnpm lint` exits 0; `git grep -c "NP-STRICT" src/core/utils src/core/http` counts zero new markers.
    - request() with no `opts.rateLimiter` performs zero rate checks (D-37 default-free).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expansion — timeout/abort and network-failure paths + refill-boundary precision tests</name>
  <files>src/core/http/Requester.ts, src/core/utils/RateLimiter.ts, tests/core/utils/Requester.test.ts, tests/core/utils/RateLimiter.test.ts</files>
  <read_first>
    - src/core/http/Requester.ts (the file being extended — the catch classification from Task 1)
    - 02-RESEARCH.md lines 334-345 (AbortError discrimination pattern from aiProvider), 585-591 (classification precedence)
    - 02-PATTERNS.md lines 334-345 (aiProvider AbortError check)
  </read_first>
  <behavior>
    - Test 1: a fetch that never resolves → advance fake timers past the 25s default → request() rejects with code TIMEOUT (AbortError path).
    - Test 2: a custom timeoutMs (e.g. 1000) is honored (timeout precision contract).
    - Test 3: fetch rejects (network failure) → request() rejects with code NETWORK.
    - Test 4: RateLimiter refill is precise at the boundary — acquire() false at exactly 0 tokens, true again strictly after the refill interval for one token (precision: fractional-token accumulation, no rounding drift).
  </behavior>
  <action>
    Extend `Requester.test.ts` with the timeout (fake timers + never-resolving fetch), custom-timeout, and network-rejection cases; extend `RateLimiter.test.ts` with the refill-boundary precision case (elapsed-time math must accumulate fractional tokens — e.g. refillPerSecond 0.5 with 2s elapsed yields exactly 1 token; assert no rounding drift). Adjust `RateLimiter.ts`/`Requester.ts` only if the new cases expose a contract bug (e.g. AbortError misclassification when the caller aborts vs the timeout aborts — both map to TIMEOUT per the locked D-35 contract).
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/utils/RateLimiter.test.ts tests/core/utils/Requester.test.ts</automated>
  </verify>
  <done>
    All expansion cases green; the TIMEOUT/NETWORK/RATE_LIMITED classification is stable under caller-abort vs timeout-abort (both TIMEOUT per D-35); refill precision holds with fractional refill rates.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/utils/RateLimiter.test.ts tests/core/utils/Requester.test.ts` exits 0 (now covering 8+ cases).
    - A caller-provided AbortSignal abort and the internal timeout abort both classify as TIMEOUT (asserted in the test).
    - No new error codes appear in either module (grep the two source files for `code:` assignments — only RATE_LIMITED/TIMEOUT/NETWORK).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `src/core/utils/RateLimiter.ts` — `RateLimiterOptions`, `class RateLimiter` (`constructor`, `acquire()`, injected `now()` seam)
- `src/core/http/Requester.ts` — `RequesterOptions`, `request()` (AbortController, 25s default timeout, optional injected limiter, code classification)
- `tests/core/utils/RateLimiter.test.ts`, `tests/core/utils/Requester.test.ts`

## Flagged Assumptions (spec-less probe fallback)

No probe rows land in this plan (REQ-R06 → plan 02-04; REQ-R07 boundary/precision → plan 02-06; REQ-R03 → plan 02-07). The no-consumer-until-Phase-3 assumption is surfaced in frontmatter `must_haves.assumptions`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → fetch | outbound HTTP from UI contexts (sidepanel/standalone only) — no consumer until Phase 3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Denial of Service | unbounded request lifetime | low | mitigate | 25s default timeout via AbortController (matches PROXY_FETCH's Promise.race per §10.7); timeout path unit-tested |
| T-02-03-02 | Denial of Service | rate-limit bypass / throttling absence | low | mitigate | optional injected per-instance RateLimiter (D-37); acquire() gate precedes fetch; RATE_LIMITED surfaced with a canonical code |
| T-02-03-03 | Information Disclosure | error context leaking request/secret details | low | mitigate | classification keeps only the code + generic message; no body echo; no consumers until Phase 3 (residual seam risk accepted — revisit at Phase 3 wiring) |
</threat_model>

<verification>
- Automated: `pnpm lint && pnpm exec vitest run tests/core/utils/RateLimiter.test.ts tests/core/utils/Requester.test.ts` (burst, refill, boundary precision, RATE_LIMITED, success, timeout, custom timeout, network).
- Code-set audit: `grep -n "code:" src/core/utils/RateLimiter.ts src/core/http/Requester.ts` lists only RATE_LIMITED / TIMEOUT / NETWORK.
</verification>

<success_criteria>
- RateLimiter token-bucket contract proven (RateLimiter.test.ts green — spec-required test).
- Requester fetch wrapper proven (timeout, network, rate-limit paths) with no invented error codes.
- Both modules strict-clean (zero new NP-STRICT markers).
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-03-SUMMARY.md` when done
</output>