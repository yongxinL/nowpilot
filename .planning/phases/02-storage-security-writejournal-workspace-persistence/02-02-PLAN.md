---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/core/security/TraceRedactor.ts
  - src/core/security/redactSensitive.ts
  - tests/core/security/redactSensitive.test.ts
autonomous: true
requirements: [STORAGE-01, STORAGE-03]
must_haves:
  truths:
    - "TraceRedactor.redact(s) replaces every Appendix O.13 secret pattern with the literal [REDACTED] token — sk-…, key-…, Bearer …, JSESSIONID=…, sysparm_ck, g_ck."
    - "redactSensitive(value) recursively redacts string fields of storage-bound objects and DROPS password-like fields (never masks them)."
    - "The vault ciphertext envelope is not re-redacted by redactSensitive (already encrypted)."
    - "Every ErrorStore.write / journal persist / debugLog string routes through the redaction hook before it touches a sink."
  artifacts:
    - src/core/security/TraceRedactor.ts
    - src/core/security/redactSensitive.ts
    - tests/core/security/redactSensitive.test.ts
  key_links:
    - "TraceRedactor.redact ↔ debugLog.ts (stable signature, zero caller churn)"
    - "redactSensitive ↔ ErrorStore.ts (02-06) and journal persist (02-04) — the write-boundary consumers"
---

<objective>
Implement real redaction (D-16): replace the Phase-1 pass-through placeholder body of `TraceRedactor` with the Appendix O.13 regex list (R-10), and ship the field-level `redactSensitive` module that storage-bound write paths call before persisting to ErrorStore, the journal, or export serialization.

Purpose: ErrorStore, WriteJournalDB, and vault ciphertext are born in this phase — the redaction hook must exist before any of those sinks write, so Phase 6 only adds consumers, never the hook itself.
Output: TraceRedactor with its real body + redactSensitive + unit tests using the `redaction` fixture builder from 02-01.

Flagged assumptions (specless probe):
- A-05: Password-like values are DROPPED, never masked (the spec's [REDACTED] token is for inline strings; field-level password fields are omitted entirely).
- A-06: redactSensitive operates on plaintext-before-encryption and non-secret metadata; it never runs on the already-encrypted vault envelope.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md Appendix O.13 (lines 6680-6718, REDACTION_PATTERNS verbatim), §16.5 (lines 2044-2046, redaction-before-sink list)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md D-16
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md Pattern 6 (redaction at every write boundary)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md (redactSensitive analog + adaptation notes)
@src/core/security/TraceRedactor.ts (the file being replaced — keep the signature)
@src/core/error/debugLog.ts (the existing consumer — signature stability is mandatory)
@tests/fixtures/index.ts (buildRedactionFixture from 02-01)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace TraceRedactor pass-through body with the O.13 regex body</name>
  <read_first>
    - PRODUCT_SPEC Appendix O.13 (lines 6686-6694 — REDACTION_PATTERNS verbatim, do not invent patterns)
    - src/core/security/TraceRedactor.ts (current placeholder — keep the exported `redact(s: string): string` signature exactly)
    - src/core/error/debugLog.ts (existing caller — verify the signature match so no caller changes)
  </read_first>
  <files>
    src/core/security/TraceRedactor.ts
  </files>
  <action>
    Replace the placeholder body of TraceRedactor.ts with the real redaction implementation per Appendix O.13. Keep the module shape and the exported `redact(value: string): string` signature byte-identical so debugLog and every existing caller need zero changes (stable-signature contract from the PATTERNS map).

    Implementation: a module-level `REDACTION_PATTERNS: RegExp[]` array containing the six patterns verbatim from O.13 (sk-…, key-…, Bearer … case-insensitive, JSESSIONID=…, sysparm_ck, g_ck), then a reduce that replaces every match with the literal token `[REDACTED]`. Update the file header comment: remove the TODO(security-phase) placeholder note and state that this is the canonical R-10 redaction body sourced from Appendix O.13, kept in sync with the spec.
  </action>
  <acceptance_criteria>
    - `grep -c "REDACTION_PATTERNS" src/core/security/TraceRedactor.ts` returns >= 1
    - `grep -c "\[REDACTED\]" src/core/security/TraceRedactor.ts` returns >= 1
    - `grep -rn "from '@/core/security/TraceRedactor'" src/core/ | wc -l` returns 1 — the debugLog.ts import baseline (src/core/error/debugLog.ts is the only caller; callers untouched)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "REDACTION_PATTERNS" src/core/security/TraceRedactor.ts</automated>
  </verify>
  <done>TraceRedactor.redact scrubs all six O.13 patterns to [REDACTED]; the pass-through placeholder is gone; no caller file changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create redactSensitive.ts — field-level redaction with password DROP</name>
  <read_first>
    - 02-CONTEXT.md D-16 (field-level for storage-bound values; password-like DROPPED not masked)
    - 02-RESEARCH.md Pattern 6 "Design note" (ciphertext must NOT be re-redacted)
    - 02-PATTERNS.md redactSensitive adaptation notes
    - src/core/security/TraceRedactor.ts (the string primitive it builds on)
  </read_first>
  <files>
    src/core/security/redactSensitive.ts
  </files>
  <action>
    Create src/core/security/redactSensitive.ts: a field-level redactor for storage-bound values (D-16). Export `redactSensitive(value: unknown): unknown`.

    Behavior contract:
    - For a plain string: apply TraceRedactor.redact (the O.13 patterns).
    - For a plain object: recurse into enumerable own string properties; redact each string value; DROP any property whose key matches a password-like matcher (normalize keys: lowercase, strip non-alphanumerics, then match against the set 'password', 'apikey' — apikey is redacted inline not dropped, 'secret', 'token', 'authorization'); the dropped-property set must be exported as `SENSITIVE_FIELD_KEYS` so tests can assert on it.
    - For an array: recurse per element.
    - For primitives: pass through unchanged.
    - Do NOT redact the vault ciphertext envelope: when the object carries the envelope marker shape `{ salt, iv, ciphertext }` (byte-array-like values), return it structurally unchanged — the DESIGN NOTE in RESEARCH Pattern 6 forbids re-redaction of already-encrypted data. Export an `isVaultEnvelope(value): boolean` guard used internally.
    - Header comment citing §16.5 + D-16 + RESEARCH Pattern 6 design note.
  </action>
  <acceptance_criteria>
    - `grep -c "export function redactSensitive" src/core/security/redactSensitive.ts` returns 1
    - `grep -c "SENSITIVE_FIELD_KEYS" src/core/security/redactSensitive.ts` returns >= 1
    - `grep -c "isVaultEnvelope" src/core/security/redactSensitive.ts` returns >= 1
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "redactSensitive" src/core/security/redactSensitive.ts</automated>
  </verify>
  <done>redactSensitive recursively redacts storage-bound objects, drops password-like fields, and leaves the vault envelope untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: redactSensitive.test.ts — the redaction fixture proves patterns, DROP, and envelope passthrough</name>
  <read_first>
    - tests/fixtures/index.ts (buildRedactionFixture from 02-01)
    - tests/core/workspace/WorkspaceStore.test.ts (test conventions — fakeBrowser + afterEach reset)
    - src/core/security/redactSensitive.ts (the module under test)
  </read_first>
  <files>
    tests/core/security/redactSensitive.test.ts
  </files>
  <action>
    Create tests/core/security/redactSensitive.test.ts. Use the `buildRedactionFixture` builder from 02-01 (D-20/21: same builder the integration paths use). Cover, via the shared fixture:
    1. String redaction: a string containing a key-shaped value, a bearer token, a JSESSIONID pair, a sysparm_ck pair, and a g_ck pair → all replaced with the literal token [REDACTED] and never the original substring.
    2. Field-level: a nested object whose string fields contain secret-shaped values → redacted; the object's password-like key (e.g. 'password') → the KEY is absent from the result (dropped, not present as masked value).
    3. Envelope passthrough: an object shaped like the vault envelope { salt, iv, ciphertext } with Uint8Array fields → returned structurally unchanged (isVaultEnvelope true, no pattern applied).
    4. Error-message routing precedent: a message string with an embedded secret → redacted (this is the string form ErrorStore/journal persist will run).
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/security/redactSensitive.test.ts --reporter=dot` passes (all cases)
    - The test imports from tests/fixtures (shared builder, D-21)
    - `grep -c "\[REDACTED\]" tests/core/security/redactSensitive.test.ts` returns >= 1 (assertion literal present)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/security/redactSensitive.test.ts --reporter=dot</automated>
  </verify>
  <done>Redaction behavior is proven: O.13 patterns scrub, password fields drop, ciphertext envelope passes through unmodified.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol | Kind | Location |
|--------|------|----------|
| `REDACTION_PATTERNS` | const (RegExp[]) | src/core/security/TraceRedactor.ts |
| `redact(value: string): string` | fn (body replaced, signature stable) | src/core/security/TraceRedactor.ts |
| `redactSensitive(value: unknown): unknown` | fn | src/core/security/redactSensitive.ts |
| `SENSITIVE_FIELD_KEYS` | const (string set) | src/core/security/redactSensitive.ts |
| `isVaultEnvelope(value): boolean` | fn | src/core/security/redactSensitive.ts |
| redaction behavior tests (4 cases) | test | tests/core/security/redactSensitive.test.ts |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage-bound values → sinks | Any string/object about to be persisted (ErrorStore, journal, export) crosses here |
| debugLog strings → console/persist | Logged strings must be scrubbed before any sink (R-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02-01 | Information Disclosure | TraceRedactor.redact | high | mitigate | O.13 patterns applied verbatim; [REDACTED] token literal is asserted in tests; a pattern regression (e.g. Bearer losing case-insensitivity) fails task-3 case 1. |
| T-2-02-02 | Information Disclosure | redactSensitive password handling | high | mitigate | Password-like keys are DROPPED (key absent), never masked — the DROP contract is the task-3 case-2 assertion; masking would leak the value shape into storage. |
| T-2-02-03 | Information Disclosure | vault envelope double-redaction | medium | mitigate | isVaultEnvelope guard returns the ciphertext structurally unchanged — re-running patterns over encrypted bytes is harmless but wasteful; the guard is asserted in task-3 case 3. |
| T-2-02-04 | Tampering | redaction bypass via new sinks | medium | mitigate | Every Phase-2 write boundary (ErrorStore 02-06, journal 02-04, export 02-09) imports redactSensitive at its sink — each consumer plan carries its own redaction-before-write acceptance check. |
</threat_model>

<verification>
- `pnpm vitest run tests/core/security/redactSensitive.test.ts` green
- `pnpm typecheck` clean
- `pnpm eslint src/core/security/TraceRedactor.ts src/core/security/redactSensitive.ts` clean; prettier clean
- Baseline caller count for TraceRedactor unchanged (no caller churn)
</verification>

<success_criteria>
- Every string debugLog logs is scrubbed of the six O.13 patterns before any sink.
- Every storage-bound object ErrorStore/journal/export will write passes redactSensitive first (proven per-consumer in later plans).
- Password-like fields never appear in persisted output, not even masked.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-02-SUMMARY.md` when done.
</output>
