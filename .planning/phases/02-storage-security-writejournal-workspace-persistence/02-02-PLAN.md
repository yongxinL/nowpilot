---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/security/KeyVault.ts
  - src/core/security/redactSensitive.ts
  - src/core/storage/EncryptedStorage.ts
  - src/store/useExtensionStore.ts
  - src/components/options/OptionsPage.tsx
  - tests/core/storage/EncryptedStorage.test.ts
  - tests/core/security/secrets-inspection.test.ts
autonomous: true
requirements: [REQ-R07]
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An API key encrypted with EncryptedStorage round-trips: encrypt → persist to chrome.storage.local → read → decrypt returns the original value (ROADMAP success criterion 2)."
    - "Decryption with a wrong key or tampered ciphertext rejects (AES-GCM authenticated — RESEARCH Code Examples / MDN)."
    - "`np_install_secret` is generated exactly once (32 random bytes) and reused across all per-key derivations (D-29, spec §15.2)."
    - "Provider secrets are persisted ONLY in `np_providers` as AES-GCM ciphertext blobs; the `np_store` blob carries no secret-bearing field values (D-28/D-30, ROADMAP success criterion 3)."
    - "A legacy `np_store` blob with plaintext apiKey/openAiKey/geminiKey is migrated once: encrypted `np_providers` written first, plaintext stripped from `np_store` second; re-running the migration is a no-op (crash-safe + idempotent, RESEARCH Pattern 3)."
    - "The provider modal renders the §8.7 masked placeholder for a provider with a saved key and never renders the decrypted value; the primary CTA is `Save Provider`, the connection-check button is `Check Connection` (UI-SPEC Dimension 1 + E1 partial row)."
    - "A provider save with a known key value leaves no plaintext substring of that value in the mocked `chrome.storage.local` map (inspection gate, ROADMAP success criterion 3)."
  artifacts:
    - "src/core/security/KeyVault.ts — installSecret lifecycle + per-key PBKDF2 derivation"
    - "src/core/storage/EncryptedStorage.ts — AES-GCM-256 encrypt/decrypt + provider-config helpers"
    - "src/core/security/redactSensitive.ts — storage-side redaction primitive"
    - "src/store/useExtensionStore.ts — partialize secret-strip + `migrateProviderSecrets()` boot step + `persistProviderConfigEncrypted()`"
    - "src/components/options/OptionsPage.tsx — provider modal encrypted read/write + masked field + CTA renames"
    - "tests/core/storage/EncryptedStorage.test.ts, tests/core/security/secrets-inspection.test.ts"
  key_links:
    - "KeyVault.ensureInstallSecret ↔ np_install_secret (chrome.storage.local) ↔ EncryptedStorage per-key derivation"
    - "useExtensionStore.partialize ↔ np_store blob (secret fields emptied) ; persistProviderConfigEncrypted ↔ np_providers (ciphertext-only)"
    - "OptionsPage handleSaveProviderModal ↔ np_providers write order (encrypt-first, then in-memory update) ↔ UI-SPEC masked-field contract"
  assumptions:
    - "A1 (RESEARCH): `chrome.runtime.id` is stable per install and never changes on browser update — valid PBKDF2 input; spec §15.2 forbids navigator.userAgent. If wrong, decryption fails deterministically and the round-trip test catches it."
    - "A6 (RESEARCH, refined): in-memory `config` keeps plaintext apiKey fields so Phase-1 consumers (aiProvider etc.) keep working; only persisted blobs are stripped/encrypted. The UI never reads the decrypted value into the field (masked contract)."
    - "D-29 is user-locked one-way: reinstall loses provider keys (documented, accepted; no recovery path in v0.1)."
    - "Criterion-3 'no message body in chrome.storage.local': satisfied for all Phase-2-created data paths — journal entries are metadata-only (plan 02-05), np_workspace payloads carry no bodies (plans 02-05/02-07), np_providers/np_install_secret carry no bodies. Legacy np_store.sessions message bodies remain until the ChatHistoryDB write path + data migration land in their owning phase per D-41."
  prohibitions:
    - "Hand-rolled cryptography (WebCrypto crypto.subtle only — AES-GCM + PBKDF2 per spec §15.2)"
    - "Using navigator.userAgent or any browser-update-sensitive value in key derivation (§15.2)"
    - "Async crypto work inside zustand persist.migrate (Pitfall 2 — migrate is synchronous; migration must be an async boot step)"
    - "Rendering or echoing a stored key value into the API-key field, placeholder, aria-label, hint, or save/error copy (UI-SPEC Anti-Pattern Gate)"
    - "New `@ts-expect-error NP-STRICT` markers (Pitfall 9 — ceiling is 0; new code must be strict-clean)"
    - "Raw console.log (CLAUDE.md — debugLog + redactSensitive only)"
---

<objective>
Encrypt-at-rest foundation: KeyVault (install-secret lifecycle + per-key derivation), EncryptedStorage (AES-GCM-256), redactSensitive, the crash-safe `np_store` → `np_providers` migration, and the Options provider-modal wiring that closes the CONCERNS "API keys stored plaintext" finding.

Purpose: ROADMAP success criteria 2 and 3. D-28 wires live encryption (not primitives-only): provider config moves to an encrypted `np_providers` key, the one-time migration strips plaintext from `np_store`, and the Options modal stops pre-filling the decrypted key (closes `OptionsPage.tsx:198`). D-29 locks the §15.2 derivation scheme (no recovery — one-way, user-ratified).
Output: secrets encrypted at rest; migration idempotent + crash-safe; UI masked-field contract; inspection gate proves no raw secret in chrome.storage.local.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-UI-SPEC.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md
@src/store/useExtensionStore.ts
@src/types/index.ts
@src/components/options/OptionsPage.tsx
@src/core/theme/chromeStorageAdapter.ts
@tests/setup.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: KeyVault + EncryptedStorage — the encrypt→persist→decrypt path, proven by round-trip test</name>
  <files>src/core/security/KeyVault.ts, src/core/storage/EncryptedStorage.ts, tests/core/storage/EncryptedStorage.test.ts</files>
  <read_first>
    - 02-RESEARCH.md lines 487-513 (locked crypto scheme verbatim: 12-byte IV, PBKDF2(installSecret + extensionId, salt, 100000, SHA-256) → AES-GCM-256), 333-351 (Pattern 3), 195-214 (Pattern 1 KeyVault lifecycle)
    - 02-PATTERNS.md lines 183-215 (EncryptedStorage + KeyVault pattern assignment)
    - tests/core/storage/chromeStorageAdapter.test.ts (test conventions)
    - src/core/log/debugLog.ts (module shape + error-code discipline)
  </read_first>
  <behavior>
    - Test 1: encrypt(known plaintext) with a derived key → decrypt → equals the original plaintext (round-trip through a chrome.storage.local persist).
    - Test 2: decrypt with a wrong key rejects; decrypt of tampered ciphertext rejects (GCM authentication).
    - Test 3: ensureInstallSecret() called twice returns the same persisted secret (np_install_secret written once, 32 bytes).
    - Test 4: two encryptions of the same plaintext produce different ciphertext blobs (fresh salt + IV per key).
  </behavior>
  <action>
    Create `src/core/security/KeyVault.ts`:
    - `ensureInstallSecret(): Promise<Uint8Array>` — read `np_install_secret` from chrome.storage.local (via chromeStorageAdapter.getItem); if absent, generate `crypto.getRandomValues(new Uint8Array(32))`, persist, return. Never regenerate once persisted (D-29).
    - `deriveKey(installSecret: Uint8Array, extensionId: string, salt: Uint8Array): Promise<CryptoKey>` — PBKDF2(installSecret + extensionId, salt, 100000, SHA-256) → AES-GCM-256 key with ['encrypt','decrypt'] usages. Deterministic single encoding for the concatenation input (document the chosen encoding in a code comment per RESEARCH note). `extensionId` comes from `chrome.runtime.id` — never navigator.userAgent (§15.2).
    - `getExtensionId(): string` — `chrome.runtime.id` with a fallback constant for non-extension test contexts (documented; RESEARCH A1).
    - Export a module-level `__test__` seam only if needed for salt/IV injection (follow the chromeStorageAdapter `__test__` convention).

    Create `src/core/storage/EncryptedStorage.ts`:
    - `EncryptedBlob = { salt: string; iv: string; ciphertext: string }` (base64-encoded fields).
    - `encrypt(plaintext: string, key: CryptoKey): Promise<EncryptedBlob>` — fresh 16-byte salt + 12-byte IV per call.
    - `decrypt(blob: EncryptedBlob, key: CryptoKey): Promise<string>` — rejects on tamper/wrong key.
    - `encryptProviderConfig(config, key): Promise<ProviderConfig>` and `decryptProviderConfig(config, key)` — operate on the existing scaffold `ProviderConfig` object shape (D-30/D-30a: keep the object shape, do NOT implement the §15.1 array form — that is Phase 3). Secret-bearing fields: `providers.*.apiKey`, `openAiKey`, `geminiKey`. Encrypt every non-empty secret field to an `EncryptedBlob`; empty fields stay empty strings.

    Create `tests/core/storage/EncryptedStorage.test.ts` covering the four behaviors above. Use the existing chrome.storage.local mock (`__chromeStorageMap`); `crypto.subtle` is empirically available in this vitest env (RESEARCH Pitfall 1 — no polyfill). Commit RED first, then GREEN.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/storage/EncryptedStorage.test.ts</automated>
  </verify>
  <done>
    EncryptedStorage.test.ts green (round-trip, wrong-key/tamper rejection, once-only install secret, salt/IV freshness); KeyVault + EncryptedStorage compile under strict mode with zero new NP-STRICT markers; the code comments document the deterministic derivation-encoding choice and the extensionId fallback.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/storage/EncryptedStorage.test.ts` exits 0 with the 4 behaviors passing.
    - `pnpm lint` exits 0 and `git grep -c "NP-STRICT" src/core/security src/core/storage` counts zero new markers (ceiling is 0).
    - `ensureInstallSecret` persists `np_install_secret` of length 32 bytes; a second call reads the same value.
  </acceptance_criteria>
  <reversibility rating="costly">Implements user-locked D-29: the derivation scheme is the encryption root (one-way by the locked decision — changing it invalidates all persisted ciphertext). The one-way confirmation already happened at discuss time (D-29 ratified: "no rotation, no master password, no recovery/export path — a documented, accepted trade-off"); re-confirming would re-open a locked decision, so this plan flags the cost only and does not insert a checkpoint.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: redactSensitive + np_store→np_providers migration (partialize strip + async boot step) + inspection gate</name>
  <files>src/core/security/redactSensitive.ts, src/store/useExtensionStore.ts, tests/core/security/secrets-inspection.test.ts</files>
  <read_first>
    - src/store/useExtensionStore.ts lines 20-87 (DEFAULT_CONFIG with plaintext keys), 500-562 (persist config: partialize at 525-528, npStoreMigrate at 555-562)
    - src/types/index.ts lines 103-137 (CustomProviderDetail.apiKey, ProviderConfig.openAiKey/geminiKey — the D-30 shape to keep)
    - 02-RESEARCH.md lines 333-351 (Pattern 3: async boot-step order — write np_providers FIRST, strip np_store SECOND), 476-481 (Pitfall 10: partialize omission), 593-610 (redactSensitive usage at boundaries)
    - 02-PATTERNS.md lines 467-483 (useExtensionStore modification spec), 283-285 (redactSensitive)
  </read_first>
  <behavior>
    - Test 1: after `migrateProviderSecrets()` on a legacy np_store blob carrying plaintext apiKey/openAiKey/geminiKey, `np_providers` holds encrypted blobs and the np_store blob's secret fields are empty.
    - Test 2: running the migration twice is a no-op (idempotent — no re-encryption, D-29 one-way preserved).
    - Test 3: crash-order — if the process dies after np_providers is written but before np_store is stripped, a re-run completes the strip (write-first/strip-second).
    - Test 4 (inspection gate): after a provider save with a known key value, the serialized `__chromeStorageMap` entries (np_store, np_providers) contain no substring equal to the known value (ROADMAP success criterion 3).
  </behavior>
  <action>
    Create `src/core/security/redactSensitive.ts`: export `redactSensitive(context?: Record<string, unknown>): Record<string, unknown>` — deep-copies the input, empties known secret keys (`apiKey`, `openAiKey`, `geminiKey`, plus any key matching /key|token|secret|authorization/i case-insensitively), and truncates message-body-shaped string values to a fixed preview length. It is the storage-side redaction primitive (the full TraceRedactor + AITransactionLog is Phase 11 — do not build that here).

    Modify `src/store/useExtensionStore.ts`:
    1. Extend `partialize` (lines 525-528) so the persisted np_store blob never carries secret field values: keep the existing exclusions (activeSession/activeAttachments/availableTabs) and additionally empty `config.providers.*.apiKey`, `config.openAiKey`, `config.geminiKey` to `''` before returning the persisted subset (Pitfall 10). The in-memory state keeps its plaintext values (A6 — Phase-1 consumers unaffected).
    2. Add an exported async boot-step `migrateProviderSecrets(): Promise<void>` (D-28, Pattern 3 — NEVER inside the sync `npStoreMigrate`, which stays a no-op): (a) read np_store via chromeStorageAdapter.getItem; (b) parse; if no legacy plaintext in `config` OR `np_providers` already exists with content → return (idempotent); (c) `KeyVault.ensureInstallSecret()` + derive a key per secret field (fresh 16-byte salt); (d) encrypt the three secret categories; (e) write `np_providers` (whole ProviderConfig shape, ciphertext fields) via chromeStorageAdapter.setItem — commit the new key FIRST; (f) strip the plaintext fields in the np_store object and re-persist np_store — strip SECOND.
    3. Add an exported `persistProviderConfigEncrypted(config: ProviderConfig): Promise<void>` helper: builds the encrypted np_providers blob (reusing the stored ciphertext for any provider whose apiKey is unchanged from the current np_providers blob — the UI untouched-field preserve path) and writes it. Throws on write failure so the caller can avoid claiming false success (STORAGE_QUOTA surfaces via the plan-02-06 error pipeline — never user-facing).

    Create `tests/core/security/secrets-inspection.test.ts` covering the four behaviors, driving the migration against the Map-backed storage mock and asserting substring absence via map inspection (grep the serialized entries for the known value).
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/security/secrets-inspection.test.ts tests/core/storage/EncryptedStorage.test.ts</automated>
  </verify>
  <done>
    Migration tests green (idempotent, crash-order, strip-completion); inspection gate proves the known key value never appears as a substring in np_store or np_providers persisted blobs; partialize empties all three secret categories; npStoreMigrate remains a sync no-op.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/security/secrets-inspection.test.ts` exits 0 with all 4 behaviors passing.
    - The persisted np_store blob after a provider save contains empty-string secret fields (asserted by inspecting `__chromeStorageMap.get('np_store')`).
    - `migrateProviderSecrets` runs to completion without re-encrypting on second invocation (np_providers blob byte-identical across runs).
  </acceptance_criteria>
  <reversibility rating="costly">D-28 (user-locked): provider config moves out of np_store into np_providers; the strip is one-way once keys leave the old blob. Flagged only — the decision is locked.</reversibility>
</task>

<task type="auto">
  <name>Task 3: OptionsPage provider modal — encrypted np_providers read/write + masked saved-key field + CTA renames</name>
  <files>src/components/options/OptionsPage.tsx</files>
  <read_first>
    - src/components/options/OptionsPage.tsx lines 161-289 (modal state + handleOpenProviderModal + handleSaveProviderModal + handleCheckConnection), 1455-1500 (modal JSX: Input.Password field + footer buttons)
    - 02-UI-SPEC.md lines 17-20 (touched surface), 91-105 (Copywriting Contract: `Save Provider` / `Check Connection`), 125-135 (E1 partial + backstop), 182-193 (Visual Anchors)
    - 02-PATTERNS.md lines 387-388 (anti-pattern: `setModalApiKey(detail.apiKey || '')` at OptionsPage.tsx:198 must be removed)
  </read_first>
  <action>
    Modify `src/components/options/OptionsPage.tsx` to route provider config through the encrypted np_providers path (D-28/D-30) while keeping the UI-SPEC contract exactly:

    1. Remove the plaintext pre-fill: delete `setModalApiKey(detail.apiKey || '')` (line 198). On modal open, `modalApiKey` starts as `''` always.
    2. Masked saved-key state: when `detail.apiKey` is non-empty (a key is saved — in-memory plaintext per A6), render the API-key `Input.Password` with placeholder `••••••••••••••••` (fixed bullet run, §8.7). The decrypted/stored value is NEVER placed into the input `value`, `placeholder`, `aria-label`, or any hint string (UI-SPEC E1 partial + backstop). When `detail.apiKey` is empty, the placeholder is empty (E1 empty row — existing behavior).
    3. `handleSaveProviderModal` (lines 210-238): `apiKey: modalApiKey.trim().length > 0 ? modalApiKey : currentDetail?.apiKey ?? ''` (untouched field preserves the stored key); `isConfigured` stays true when a stored key exists even if the field is untouched. On save: if the field held a new value, call `persistProviderConfigEncrypted(updatedConfig)` (from useExtensionStore — re-encrypts); if untouched, it reuses the existing ciphertext blobs. Then `updateConfig(updatedConfig)` (in-memory + np_store partialized-stripped persist). If `persistProviderConfigEncrypted` throws (e.g. STORAGE_QUOTA), do NOT show the success toast (no false success — UI-SPEC E1 error row: the failure surfaces to ErrorStore/debugLog only, no user copy) and keep the modal open.
    4. Rename the primary CTA `Save` → `Save Provider` and the connection-check button `Check` → `Check Connection` (Dimension 1 verb+noun contract). Keep the existing toasts (`{name} settings saved`, `{name} enabled/disabled`) verbatim.
    5. `handleCheckConnection` (lines 256-289): when `modalApiKey` is empty and the provider has a stored key, use the stored in-memory value transiently for the connection call (never write it into `modalApiKey` or any rendered attribute).
    6. Top-level `openAiKey`/`openAiBaseUrl` writes (lines 232-233) continue into the config that `persistProviderConfigEncrypted` encrypts.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/security tests/core/storage/EncryptedStorage.test.ts && grep -n "Save Provider" src/components/options/OptionsPage.tsx && grep -n "Check Connection" src/components/options/OptionsPage.tsx</automated>
  </verify>
  <done>
    tsc passes; the modal never pre-fills the stored key (line 198 removed); `Save Provider` and `Check Connection` labels present; the masked placeholder path renders for saved keys; the save path preserves the stored key on an untouched field and skips the success toast on persist failure.
  </done>
  <acceptance_criteria>
    - `grep -n "setModalApiKey(detail.apiKey" src/components/options/OptionsPage.tsx` matches nothing (the plaintext pre-fill is gone).
    - `grep -n "Save Provider"` and `grep -n "Check Connection"` each match exactly the CTA labels; no bare `>Save<` / `>Check<` button label remains in the modal footer.
    - The modal's password input renders `••••••••••••••••` as placeholder when the provider has a stored key and the field value/aria-label/hint carry no stored-key content (source assertion + the UI backstop in verification).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `src/core/security/KeyVault.ts` — `ensureInstallSecret()`, `deriveKey()`, `getExtensionId()` (+ `__test__` seam if needed)
- `src/core/storage/EncryptedStorage.ts` — `EncryptedBlob`, `encrypt()`, `decrypt()`, `encryptProviderConfig()`, `decryptProviderConfig()`
- `src/core/security/redactSensitive.ts` — `redactSensitive()`
- `src/store/useExtensionStore.ts` — `migrateProviderSecrets()` (async boot step), `persistProviderConfigEncrypted()`, extended `partialize`
- `src/components/options/OptionsPage.tsx` — masked saved-key field, `Save Provider` / `Check Connection` CTAs, encrypted np_providers write path
- `tests/core/storage/EncryptedStorage.test.ts`, `tests/core/security/secrets-inspection.test.ts`
- Storage keys created: `np_install_secret`, `np_providers` (both chrome.storage.local)

## Flagged Assumptions (spec-less probe fallback)

No probe rows land in this plan (REQ-R06 → plan 02-04; REQ-R07 boundary/precision → plan 02-06; REQ-R03 → plan 02-07). The RESEARCH A1/A6 assumptions and the criterion-3 message-body scope note are surfaced in frontmatter `must_haves.assumptions`.

## UI Considerations lift (UI-SPEC §UI Considerations)

Covered ✅ rows lifted as truths (frontmatter): E1 empty (empty field, no bullets), E1 partial (masked placeholder, never-echo, typing-replaces, untouched-field preserves), E1 loading (Check Connection in-flight → inline result), E1 error (inline colorError, never interpolating the key), E1 error (STORAGE_QUOTA/STORAGE_RATE_LIMIT → ErrorStore + debugLog, never user-facing; modal does not claim false success).
Backstop 🧪 (held-out, verify-time → human_needed if no explicit evidence):
`{ statement: "Opening the provider modal for a provider with a saved key renders the masked placeholder and does NOT contain the decrypted key value anywhere in the input DOM (value, placeholder, aria-label, or hint)", verification: backstop }`

## Prohibitions (recalled, descriptor-less, flagged-unverified)

Listed in frontmatter `must_haves.prohibitions` — recalled from CONTEXT D-28/D-29/D-30, RESEARCH Pitfalls 2/9/10, UI-SPEC Anti-Pattern Gates, spec §15.2.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory config (plaintext keys) → chrome.storage.local | secrets cross here on every persist — ciphertext-only after this plan |
| OptionsPage modal field ↔ stored key | the field must never receive or render the decrypted value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | plaintext API keys at rest (CONCERNS finding) | critical | mitigate | AES-GCM-256 via WebCrypto (KeyVault + EncryptedStorage); np_store partialize empties secret fields; np_providers ciphertext-only; inspection gate asserts substring absence in the storage map |
| T-02-02-02 | Information Disclosure | secret leakage into logs / ErrorStore | medium | mitigate | redactSensitive() runs at every persist/log boundary (D-39/§16.5); key values never passed to debugLog |
| T-02-02-03 | Tampering | persisted ciphertext | medium | mitigate | AES-GCM authenticated encryption — decrypt rejects on any modification; wrong-key/tamper rejection test |
| T-02-02-04 | Availability | key loss on reinstall | high | accept | D-29 user-locked one-way trade-off: no rotation/recovery/export in v0.1; np_install_secret never duplicated (documented, accepted) |
| T-02-02-05 | Information Disclosure | stored key echoed into Options field/aria/hint/copy | medium | mitigate | UI-SPEC masked-field contract: plaintext pre-fill removed, `••••••••••••••••` placeholder only, never-echo backstop test |
| T-02-02-06 | Tampering | key derivation instability | medium | mitigate | stable inputs only (installSecret + chrome.runtime.id per §15.2); navigator.userAgent forbidden |
</threat_model>

<verification>
- Automated: `pnpm lint && pnpm exec vitest run tests/core/storage/EncryptedStorage.test.ts tests/core/security` — round-trip, tamper rejection, migration idempotency/crash-order, inspection gate.
- UI backstop (held-out, human at verify time): open Options → provider modal for a saved-key provider → masked placeholder renders, decrypted value absent from value/placeholder/aria/hint (manual-only per VALIDATION.md).
</verification>

<success_criteria>
- EncryptedStorage AES-GCM round-trip test green (success criterion 2).
- Secrets live only in np_providers as ciphertext; np_store carries empty secret fields; inspection gate green (success criterion 3).
- Migration idempotent + crash-safe (write-first/strip-second).
- Options modal masked-field contract implemented; CTA renames shipped.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-02-SUMMARY.md` when done
</output>