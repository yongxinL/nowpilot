---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-04]
files_modified:
  - src/core/storage/WriteJournal.ts
  - src/core/workspace/journalingAdapter.ts
  - tests/core/storage/WriteJournal.test.ts
  - tests/core/workspace/journalingAdapter.test.ts
autonomous: true
requirements: [REQ-R03]
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WriteJournal implements the spec Appendix O.11 shape verbatim: `JournalStep { name, apply, rollback }` (apply MUST be idempotent), `runJournaled(entry, steps, persist)` (pending → applying → completed; rollback in reverse on step failure → rolled-back), and `recoverJournal(load, replay)` (replays only pending/applying entries)."
    - "The full 11-op `WriteJournalOperation` union is declared (§20.3: append-memory-message, evict-conversation, archive-conversation, compact-conversation, save-note-with-links, update-user-memory, export-data, update-workspace, sync-note-file, delete-note-file, restore-notes-batch); only `update-workspace` has a registered JournalStep in Phase 2 (D-32 declare-now/populate-later)."
    - "A simulated SW kill mid-write (a pending/applying entry left in WriteJournalDB) is recovered by `recoverJournal` replaying the write so state is restored without loss — ROADMAP success criterion 1."
    - "Journal entries persist in the WriteJournalDB IndexedDB `entries` store (NOT chrome.storage.session) so recovery survives SW restarts (D-33); entries are metadata-only — no message bodies."
    - "recoverJournal skips entries whose operation is not `update-workspace` with a debugLog instrumentation line — no placeholder handlers for the remaining 10 ops (D-32 replay contract)."
    - "The `journalingAdapter` wraps chromeStorageAdapter (RESEARCH Pattern 2): primary surfaces journal the np_workspace write (immediate entry put → runJournaled [debounced inner setItem, WORKSPACE_UPDATED emit] → completed); non-primary surfaces no-op setItem (mirror only, D-27); getItem/removeItem pass through."
    - "Journal-entry writes bypass the debounce (immediate, single IDB put) so recovery ordering is exact; the np_workspace data write stays on the D-22 300ms trailing debounce (D-34/D-43)."
    - "The np_workspace legacy key (`np_workspace_store`) is lifted once to `np_workspace` (read → write → verify → delete; idempotent) so journal/recovery/tests target the single canonical key (PATTERNS Workspace-key migration rule; RESEARCH Open Question 1 resolved)."
  artifacts:
    - "src/core/storage/WriteJournal.ts — WriteJournalOperation, JournalStep, runJournaled, recoverJournal"
    - "src/core/workspace/journalingAdapter.ts — createJournalingAdapter"
    - "tests/core/storage/WriteJournal.test.ts, tests/core/workspace/journalingAdapter.test.ts"
  key_links:
    - "WriteJournal.runJournaled/recoverJournal ↔ WriteJournalDB entries store (plan 02-04) ↔ src/types/storage.ts WriteJournalEntry"
    - "journalingAdapter.setItem ↔ isPrimaryWriter() (plan 02-06 election) ↔ chromeStorageAdapter debounce ↔ WorkspaceStore persist (plan 02-07)"
    - "journalingAdapter getItem legacy-key lift ↔ np_workspace canonical key (plan 02-07 persist name)"
  assumptions:
    - "PATTERNS Workspace-key migration rule (RESEARCH Open Question 1 resolved in PATTERNS.md): after the one-time lift, `np_workspace` is the sole source of truth — all Phase-2 journal/recovery code, tests, and plan 02-07's persist config target `np_workspace` only."
    - "A3 (RESEARCH): the journalingAdapter wrapper (not a store-level hook) is the composition point — zustand persist has no onWrite hook; the adapter is the shared choke point."
    - "REQ-R03 boundary (probe row, surfaced here): the journal-entry write is immediate while the data write is debounced — the exact interleaving is the D-34 contract; verified in plan 02-07's write-rate assertion."
  prohibitions:
    - "Storing message bodies or any user content in journal entries (metadata-only — D-33)"
    - "Routing journal-entry writes through the debounced adapter (they must be immediate IDB puts — D-34)"
    - "Registering placeholder handlers for the other 10 WriteJournalOperation values (D-32 — unsupported ops are skipped with debugLog)"
    - "A second heartbeat/timer anywhere in the workspace layer (D-26 — this plan adds no timers)"
    - "New `@ts-expect-error NP-STRICT` markers (Pitfall 9)"
    - "Renaming the persisted workspace key without the one-time lift migration (the lift is the migration — PATTERNS rule)"
---

<objective>
WriteJournal (spec Appendix O.11, verbatim) + the journalingAdapter that composes election-gating, journaling, and the D-22 debounce into the single seam WorkspaceStore's persist config uses (D-31/D-32/D-33/D-34).

Purpose: ROADMAP success criterion 1 — a simulated SW kill mid-write must be recovered by replay with zero state loss. D-31 wires the real `np_workspace` update through `runJournaled`; D-32 declares the 11-op union (one step implemented); D-33 persists entries in WriteJournalDB; D-34 composes election-gate + journal + debounced step. This plan delivers the journal core + the adapter; the WorkspaceStore swap and boot-time recovery land in plan 02-07.
Output: crash-safe journaling primitive + production-shaped adapter, both proven by tests.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md
@src/core/theme/chromeStorageAdapter.ts
@src/core/workspace/WorkspaceStore.ts
@src/core/workspace/WorkspaceSync.ts
@src/core/storage/WriteJournalDB.ts
@src/core/log/debugLog.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: WriteJournal core — runJournaled/recoverJournal per O.11 + recovery test (SW-kill replay)</name>
  <files>src/core/storage/WriteJournal.ts, tests/core/storage/WriteJournal.test.ts</files>
  <read_first>
    - 02-RESEARCH.md lines 262-331 (O.11 verbatim: JournalStep, runJournaled, recoverJournal, the 11-op union, update-workspace ordering), 305-319 (union verbatim)
    - 02-PATTERNS.md lines 46-131 (WriteJournal pattern assignment + Workspace-key migration rule)
    - src/core/log/debugLog.ts (WRITE_JOURNAL_FAILED / WRITE_JOURNAL_ROLLBACK_FAILED are spec-internal debugLog codes — NOT registry additions, D-38)
    - src/core/storage/WriteJournalDB.ts and src/types/storage.ts (plan 02-04 outputs — the entries store + WriteJournalEntry)
  </read_first>
  <behavior>
    - Test 1 (success criterion 1): a simulated SW kill mid-write — an entry left 'pending' (and one left 'applying') in WriteJournalDB → recoverJournal replays the update-workspace write → the storage value is restored.
    - Test 2 (idempotent replay): replaying the same entry twice produces the same final state (JournalStep.apply must be safe to run twice).
    - Test 3 (rollback): a step whose apply() throws → the already-done steps roll back in reverse, the entry is marked 'rolled-back', and WRITE_JOURNAL_FAILED is emitted via debugLog.
    - Test 4 (replay contract): an entry with an unsupported operation (e.g. 'save-note-with-links') is skipped with a debugLog instrumentation line — no placeholder handler runs.
  </behavior>
  <action>
    Create `src/core/storage/WriteJournal.ts` implementing the spec Appendix O.11 shape verbatim (RESEARCH lines 262-303):
    - `export type WriteJournalOperation = 'append-memory-message' | 'evict-conversation' | 'archive-conversation' | 'compact-conversation' | 'save-note-with-links' | 'update-user-memory' | 'export-data' | 'update-workspace' | 'sync-note-file' | 'delete-note-file' | 'restore-notes-batch'` (declare all 11 — D-32).
    - `export interface JournalStep { name: string; apply(): Promise<void>; rollback(): Promise<void> }` — apply MUST be idempotent (comment it).
    - `export async function runJournaled(entry: WriteJournalEntry, steps: JournalStep[], persist: (e: WriteJournalEntry) => Promise<void>): Promise<void>` — sets entry.status='applying', increments attempts, persists; applies steps in order, recording completed step names; on success marks 'completed' and persists; on throw emits `debugLog('WRITE_JOURNAL_FAILED', ...)` with the failed step name, rolls back the done steps in reverse (each rollback failure emits `debugLog('WRITE_JOURNAL_ROLLBACK_FAILED', ...)` and does not mask the original error), marks 'rolled-back', persists, and rethrows.
    - `export async function recoverJournal(load: () => Promise<WriteJournalEntry[]>, replay: (e: WriteJournalEntry) => Promise<void>): Promise<void>` — replays only entries with status 'pending' or 'applying'.
    - A `journalSteps` registry Map<WriteJournalOperation, JournalStep[]> with `registerJournalSteps(operation, steps)`; Phase 2 registers ONLY 'update-workspace'. A `createWorkspaceWriteSteps({ write, emit })` factory: `write` = the debounced chromeStorageAdapter setItem for the np_workspace key, `emit` = the WORKSPACE_UPDATED broadcast (injected — no import of WorkspaceSync here, keeps the journal decoupled; the adapter wires the real emit).
    - The unsupported-op skip lives in the recovery wiring (plan 02-07 boot), not here — but expose `isSupportedOperation(op)` so the boot wiring can skip cleanly with debugLog (D-32 replay contract).

    Create `tests/core/storage/WriteJournal.test.ts` covering the four behaviors against WriteJournalDB (fake-indexeddb harness + `__resetIndexedDB()` per test). Commit RED first, then GREEN.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/storage/WriteJournal.test.ts</automated>
  </verify>
  <done>
    WriteJournal.test.ts green: recovery replay restores state (criterion 1), replay is idempotent, rollback marks 'rolled-back' with WRITE_JOURNAL_FAILED logged, unsupported ops skip with instrumentation.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/storage/WriteJournal.test.ts` exits 0 with the 4 behaviors passing.
    - The 11-op union is declared (source assertion); only 'update-workspace' has registered steps (grep the registry registrations).
    - `pnpm lint` exits 0 with zero new NP-STRICT markers.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: journalingAdapter — election-gated, journaled, debounced compose seam + legacy-key lift</name>
  <files>src/core/workspace/journalingAdapter.ts, tests/core/workspace/journalingAdapter.test.ts</files>
  <read_first>
    - src/core/theme/chromeStorageAdapter.ts (the inner adapter being wrapped — setItem debounce lines 141-158, getItem 127-139, removeItem 160-170)
    - src/core/workspace/WorkspaceSync.ts (notifyWorkspaceUpdate — the emit the adapter wires)
    - 02-RESEARCH.md lines 252-331 (Pattern 2: journaling adapter wrapper, key-name discrepancy note + PATTERNS migration rule), 155-164 (D-34 flow diagram)
    - 02-PATTERNS.md lines 420-443 (WorkspaceStore persist config: name np_workspace_store at line 148 — the key the lift migrates FROM)
  </read_first>
  <behavior>
    - Test 1 (primary path): setItem('np_workspace', value) when isPrimary → a 'pending' journal entry is created in WriteJournalDB immediately, runJournaled applies the debounced write + the WORKSPACE_UPDATED emit, the entry ends 'completed'.
    - Test 2 (secondary path): setItem when NOT primary → no journal entry, no storage write (mirror only — D-27).
    - Test 3 (legacy lift): getItem('np_workspace') with no np_workspace key but a legacy 'np_workspace_store' value → the payload is copied to np_workspace, np_workspace_store removed, and the value returned (one-time, idempotent — a second getItem finds np_workspace directly).
    - Test 4 (passthrough): getItem/removeItem for non-workspace keys delegate to the inner adapter unchanged.
  </behavior>
  <action>
    Create `src/core/workspace/journalingAdapter.ts` exporting `createJournalingAdapter(deps: { inner: StateStorage; isPrimary: () => boolean; putEntry: (e: WriteJournalEntry) => Promise<void>; persistEntry: (e: WriteJournalEntry) => Promise<void>; emitUpdate: (workspaceId: string, conversationId: string | null) => void; }): StateStorage`:
    - `setItem(name, value)`:
      - If name !== 'np_workspace' → delegate to `inner.setItem` (only the workspace key is journaled in Phase 2 — defensive passthrough).
      - If `!isPrimary()` → return (no journal entry, no write — D-27 mirror only).
      - Else: parse `value` JSON → extract `workspaceId` + `conversationId`; build a WriteJournalEntry (`id: crypto.randomUUID()`, `operation: 'update-workspace'`, `status: 'pending'`, `attempts: 0`, `steps: []`, `createdAt: Date.now()`); `await putEntry(entry)` (IMMEDIATE IDB put — bypasses the debounce, D-34); then `runJournaled(entry, [ { name: 'write-np-workspace', apply: () => inner.setItem(name, value), rollback: () => inner.removeItem(name) }, { name: 'emit-workpace-updated', apply: async () => emitUpdate(workspaceId, conversationId), rollback: async () => {} } ], persistEntry)`.
    - `getItem(name)` — if name === 'np_workspace' and the inner getItem returns null: check the legacy key 'np_workspace_store'; if present, copy its payload to np_workspace via `inner.setItem`, verify, `inner.removeItem('np_workspace_store')`, return the payload (PATTERNS one-time lift — idempotent). Otherwise delegate to `inner.getItem`.
    - `removeItem(name)` — delegate to `inner.removeItem`.
    - The adapter is the seam where the D-22 debounce (inner) + immediate journal entry writes + election gating (isPrimary) compose — do NOT add any timer here (D-26).
    - Step names are exact strings: 'write-np-workspace' and 'emit-workspace-updated' (the step records stored in the journal entry must be stable identifiers — they appear in `entry.steps[].name`).

    Create `tests/core/workspace/journalingAdapter.test.ts` covering the four behaviors (fake-indexeddb + WriteJournalDB for the entry store; a stub inner adapter with a spy setItem; a stub isPrimary). Commit RED first, then GREEN.
  </action>
  <verify>
    <automated>pnpm lint && pnpm exec vitest run tests/core/workspace/journalingAdapter.test.ts tests/core/storage/WriteJournal.test.ts</automated>
  </verify>
  <done>
    The adapter proves primary-journaling, secondary-skip, legacy-key lift, and passthrough; journal-entry writes are immediate (spy asserts the IDB put precedes the debounced write); the adapter adds no timer.
  </done>
  <acceptance_criteria>
    - `vitest run tests/core/workspace/journalingAdapter.test.ts` exits 0 with all 4 behaviors passing.
    - Spy assertion: on the primary path, `putEntry` is awaited before `inner.setItem` is called (immediate-entry ordering, D-34).
    - Source assertion: journalingAdapter.ts contains no `setInterval`/`setTimeout` (D-26 — no second timer).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `src/core/storage/WriteJournal.ts` — `WriteJournalOperation` (11-op union), `JournalStep`, `runJournaled`, `recoverJournal`, `registerJournalSteps`, `isSupportedOperation`, `createWorkspaceWriteSteps`
- `src/core/workspace/journalingAdapter.ts` — `createJournalingAdapter` (setItem/getItem/removeItem; election gate; immediate entry put; debounced inner write; legacy-key lift)
- `tests/core/storage/WriteJournal.test.ts`, `tests/core/workspace/journalingAdapter.test.ts`

## Flagged Assumptions (spec-less probe fallback)

- **REQ-R03 boundary (probe row):** the exact interleaving of immediate journal-entry writes vs the 300ms-debounced data write is the D-34 contract; verified behaviorally in the adapter test (entry-before-write ordering) and the steady-state write-rate assertion lands in plan 02-07. Surfaced in frontmatter `must_haves.assumptions`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zustand persist → journalingAdapter → WriteJournalDB (IDB) | journal entries cross into IndexedDB here; crash-recovery depends on ordering |
| journalingAdapter → chromeStorageAdapter | the debounced np_workspace data write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Availability | SW suspension mid-write loses workspace state | high | mitigate | pending→applying→completed journal ordering (O.11 verbatim) + recoverJournal on boot (plan 02-07); idempotent replay; rollback on step failure — proven by the SW-kill recovery test (criterion 1) |
| T-02-05-02 | Information Disclosure | message bodies or user content in journal entries | medium | mitigate | entries are metadata-only (D-33); the entries store schema carries id/operation/status/attempts/steps/createdAt only |
| T-02-05-03 | Integrity | recovery replay ordering skew | low | mitigate | journal-entry writes are immediate (bypass debounce) so recovery sees exact ordering (D-34) |
| T-02-05-04 | Availability | residual ≤300ms debounce crash window | low | accept | documented residual (T-01-12): flush hooks cover beforeunload/visibilitychange; a hard crash inside the window remains accepted risk — entry may sit 'applying' and replay next boot |
| T-02-05-05 | Integrity | dual workspace keys (np_workspace_store vs np_workspace) diverging | low | mitigate | one-time legacy-key lift (read→write→verify→delete) makes np_workspace the sole key; idempotent and tested |
</threat_model>

<verification>
- Automated: `pnpm lint && pnpm exec vitest run tests/core/storage/WriteJournal.test.ts tests/core/workspace/journalingAdapter.test.ts`.
- Ordering proof: adapter test spy asserts the IDB entry put precedes the debounced inner setItem.
- No-timer proof: `grep -n "setInterval\|setTimeout" src/core/workspace/journalingAdapter.ts` matches nothing.
</verification>

<success_criteria>
- WriteJournal recovery test green (success criterion 1): simulated SW kill mid-write → replay restores state.
- Journal entries metadata-only, in WriteJournalDB, immediate writes; 11-op union declared, update-workspace implemented.
- journalingAdapter composes election-gate + journal + debounce; legacy key lifted once to np_workspace.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-05-SUMMARY.md` when done
</output>