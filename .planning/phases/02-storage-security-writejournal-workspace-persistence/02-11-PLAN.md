---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 11
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06, 02-07, 02-08, 02-09, 02-10]
files_modified:
  - tests/isolation/check-content-bundle.mjs
  - src/entrypoints/sidepanel/main.tsx
  - src/entrypoints/standalone/main.tsx
  - tests/core/storage/no-secrets-in-storage.test.ts
autonomous: true
requirements: [STORAGE-01, STORAGE-02, STORAGE-03, STORAGE-04, STORAGE-05]
must_haves:
  truths:
    - "verify:phase-2 runs green: eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run && node tests/isolation/check-content-bundle.mjs (Phase-1 extended template, spec §24)."
    - "No plaintext secret (API key / sk-… / Bearer … / JSESSIONID=) and NO message body ever appears in chrome.storage.local — proven by an automated test that writes via the real store/vault paths then dumps storage.local (privacy gate)."
    - "The content bundle isolation check still passes with idb/fflate/vault tokens forbidden (R-3: AI+IndexedDB+vault stay in panels)."
    - "The storage layer initializes on surface mount: KeyVault first-run (installSecret), migrate-on-read (np_schema_version), and the IDB migrator run from both entrypoints (sidepanel + standalone)."
    - "All five §18 DONE-when criteria for Phase 2 are demonstrably green: WriteJournal recovery test passes; API key encryption round-trip passes; no message body in chrome.storage.local; migration v1→v2 fixture passes; workspace persists across reload + cross-surface handoff."
  artifacts:
    - tests/isolation/check-content-bundle.mjs (extended FORBIDDEN_TOKENS)
    - src/entrypoints/sidepanel/main.tsx (storage-layer init wiring)
    - src/entrypoints/standalone/main.tsx (storage-layer init wiring)
    - tests/core/storage/no-secrets-in-storage.test.ts
  key_links:
    - "entrypoint init order ↔ KeyVault (02-03) + runMigrateOnRead (02-05) + IDB migrator (02-06) — the mount-time persistence bootstrap"
    - "privacy test ↔ the vault (02-03), stores (02-06/07), and Setting (02-05) — proves no secret/body in local"
    - "check-content-bundle.mjs tokens ↔ R-3 isolation (vault/IDB never in content bundles)"
---

<objective>
Close the phase with the verify:phase-2 gate and the privacy/security hygiene: extend the content-bundle isolation tokens (idb/fflate/vault awareness), wire the storage-layer initialization (KeyVault first-run, migrate-on-read, IDB migrator) into both entrypoints' mount sequence, add the automated no-secrets-in-storage privacy test, and run the full Phase-2 gate green (eslint + prettier + tsc + wxt build + vitest + isolation) so the phase ends per Golden Rule 10.

Purpose: "Every phase ends green (verify:phase-N)" — this plan is the evidence gate for all five STORAGE requirements plus the §18 DONE-when criteria.
Output: entrypoint init wiring, isolation-token extension, privacy test, and a green verify:phase-2 run.

Flagged assumptions (specless probe — the prohibitions become asserted truths here):
- A-22: No secrets in chrome.storage (API keys AES-GCM at rest) — asserted by the privacy test.
- A-23: No message bodies in chrome.storage.local — asserted by the privacy test.
- A-24: installSecret never in sync/exports — asserted by the privacy test (sync area dump) + 02-09 exclusion tests.
- A-25: No auto-wipe on decrypt failure / no auto-regenerate of installSecret — asserted by 02-03 cross-install tests.
- A-26: Password values DROPPED not masked — asserted by 02-02 redaction tests.
- A-27: In-memory fallback rejected for degraded mode — asserted by 02-06 tests.
- A-28: Unknown-op journal replay never throws — asserted by 02-04 tests.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md §18 Phase-2 DONE-when (lines 2597-2603), §24 (line ~3666 verify gate minimum), §0.5 Golden Rule 10
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md D-12 (degraded banner state), D-16 (redaction at every write boundary), D-10 (migrate-on-read at init)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-RESEARCH.md Validation Architecture (phase gate chain), Wave 0 Gaps (entrypoint wiring), §24 (line 3666)
@tests/isolation/check-content-bundle.mjs (the gate file to extend)
@src/entrypoints/sidepanel/main.tsx + src/entrypoints/standalone/main.tsx (mount init sequence)
@src/core/security/KeyVault.ts (02-03), src/core/storage/Setting.ts (02-05), src/core/storage/IndexedDBMigrator.ts (02-06)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire storage-layer init into both entrypoints (KeyVault first-run + migrate-on-read + IDB migrator)</name>
  <read_first>
    - src/entrypoints/sidepanel/main.tsx and src/entrypoints/standalone/main.tsx (existing init chains — where WorkspaceStore.init() etc. fire on mount)
    - 02-CONTEXT.md Integration Points (lines 127-131 — storage-layer init fires on surface mount) + D-10 (migrate-on-read at init)
    - src/core/security/KeyVault.ts (getInstallSecret/getKeyVault, 02-03), src/core/storage/Setting.ts (runMigrateOnRead, 02-05), src/core/storage/IndexedDBMigrator.ts (runMigrations/degraded state, 02-06), src/core/storage/ErrorStore.ts (recordMigrationFailure, 02-06)
  </read_first>
  <files>
    src/entrypoints/sidepanel/main.tsx
    src/entrypoints/standalone/main.tsx
  </files>
  <action>
    Modify both entrypoint mount sequences (sidepanel/main.tsx + standalone/main.tsx — R-3: these are the ONLY surfaces where storage/AI/IDB init may run; the background SW never does) to add a storage-layer bootstrap step BEFORE the existing init() chain completes. The bootstrap:
    1. KeyVault first-run: call getInstallSecret() (generates np_install_secret once, read-then-write-if-absent, D-02) — wrap in try/catch → debugLog(VAULT_DECRYPT_FAILED/PROVIDER_KEY_UNREADABLE as applicable, Golden Rule 9).
    2. Migrate-on-read: call runMigrateOnRead with the registered sanitizers (np_workspace/np_providers/np_addon_settings via their existing sanitizers, D-10) — normalizes old KV shapes before consumers read them.
    3. IDB migrator + ErrorStore: open ErrorStore and run the registered DBVersionMigration specs (each real store's current version — ChatHistoryDB/NotesDB/MemoryDB/WriteJournalDB at their DB_VERSION); on any migration failure the migrator records IDB_MIGRATION_FAILED and the degraded state becomes visible via getDegradedDbs (D-12 — the banner string STR.storage.degradedBanner renders in Phase 7, the state ships now).
    Keep the bootstrap non-blocking to the UI: failures degrade gracefully (debugLog + fall through to the existing init), never reject the mount (Golden Rule 9). Update the file header comments to note the storage bootstrap.
  </action>
  <acceptance_criteria>
    - `grep -c "getInstallSecret" src/entrypoints/sidepanel/main.tsx src/entrypoints/standalone/main.tsx` returns 2 (one per entrypoint)
    - `grep -c "runMigrateOnRead" src/entrypoints/sidepanel/main.tsx src/entrypoints/standalone/main.tsx` returns 2
    - `grep -c "runMigrations" src/entrypoints/sidepanel/main.tsx src/entrypoints/standalone/main.tsx` returns 2
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "runMigrateOnRead" src/entrypoints/sidepanel/main.tsx src/entrypoints/standalone/main.tsx && grep -c "runMigrations" src/entrypoints/sidepanel/main.tsx src/entrypoints/standalone/main.tsx</automated>
  </verify>
  <done>Both surfaces run KeyVault first-run, migrate-on-read, and the IDB migrator at mount; failures degrade gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Extend check-content-bundle.mjs isolation tokens + add the no-secrets-in-storage privacy test</name>
  <read_first>
    - tests/isolation/check-content-bundle.mjs (FORBIDDEN_TOKENS array — add idb/fflate/vault awareness, R-3)
    - 02-RESEARCH.md Security Domain (no-secrets-in-storage must be an AUTOMATED check — "a test that writes via EncryptedStorage then dumps storage.local and asserts redaction") + Validation Architecture security-sensitive checks
    - 02-CONTEXT.md D-16 (redaction at every write boundary) — the privacy test proves it end-to-end
    - src/core/security/KeyVault.ts + src/core/storage/ChatHistoryDB.ts (write paths the test drives)
  </read_first>
  <files>
    tests/isolation/check-content-bundle.mjs
    tests/core/storage/no-secrets-in-storage.test.ts
  </files>
  <action>
    Extend tests/isolation/check-content-bundle.mjs: append tokens to FORBIDDEN_TOKENS so the content bundle can never include the vault/IDB/network stack — add 'idb', 'fflate', 'KeyVault', 'EncryptedStorage', 'fake-indexeddb' (keep the existing tokens; one per line; update the header comment's token-set note).

    Create tests/core/storage/no-secrets-in-storage.test.ts — the automated privacy gate (security-sensitive check per RESEARCH):
    1. Secrets never plaintext in storage.local: encrypt a secret via the real KeyVault path (getInstallSecret + encryptSecret with the vault-roundtrip fixture secret), then dump chrome.storage.local and assert: np_install_secret exists (base64), the apiKey ciphertext envelope exists, but the PLAINTEXT secret substring (the fixture's secret value) appears NOWHERE in the dump; no secret-shaped pattern (sk-… / Bearer … / JSESSIONID=) appears in any local value.
    2. Secrets never in chrome.storage.sync: after the above, dump the sync area and assert it is empty of the secret + installSecret (D-02 never-sync).
    3. No message bodies in chrome.storage.local: write a message via ChatHistoryDB (putMessage) and a memory message via MemoryDB, then dump storage.local and assert no np_* key holds the body text (bodies live in IndexedDB only — A-23/§0.2); assert the store keys exist only as IndexedDB (fake-indexeddb), not as chrome.storage.local entries.
    4. Journal entry hygiene: after a journaled workspace write (02-04 path), dump the journal's persisted entries and assert their step errors/strings contain no secret-shaped values (redaction at the journal boundary, D-16).
  </action>
  <acceptance_criteria>
    - `grep -c "'idb'\|'fflate'\|'KeyVault'\|'EncryptedStorage'" tests/isolation/check-content-bundle.mjs` returns >= 4 (tokens added)
    - `pnpm vitest run tests/core/storage/no-secrets-in-storage.test.ts --reporter=dot` passes (all 4 cases)
    - `grep -c "storage.local" tests/core/storage/no-secrets-in-storage.test.ts` returns >= 2 (dump assertion present)
    - `pnpm node tests/isolation/check-content-bundle.mjs` exits 0 (isolation check still clean after token additions)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/storage/no-secrets-in-storage.test.ts --reporter=dot && node tests/isolation/check-content-bundle.mjs</automated>
  </verify>
  <done>Content bundle forbids the vault/IDB stack; the privacy test proves no plaintext secret, no installSecret-in-sync, no body-in-local, and journal hygiene.</done>
</task>

<task type="auto">
  <name>Task 3: Run verify:phase-2 green + DONE-when hygiene sweep</name>
  <read_first>
    - PRODUCT_SPEC §18 Phase-2 DONE-when (lines 2597-2603 — the five criteria this task proves)
    - 02-RESEARCH.md Validation Architecture (gate chain + sampling rate: per-wave vitest, phase gate full)
    - package.json (verify:phase-2 script from 02-01)
    - The five required-test files (lines 2587-2595): WriteJournal, EncryptedStorage, IndexedDBMigrator, RateLimiter, WorkspacePersistence — all must pass
  </read_first>
  <files>
    (no new files — verification-only task; fixes land in the files they belong to)
  </files>
  <action>
    Run the full Phase-2 gate and fix every failure until green:
    1. `pnpm run verify:phase-2` — the six-step chain (eslint . && prettier --check . && tsc --noEmit && wxt build && vitest run && node tests/isolation/check-content-bundle.mjs). Every failure is fixed in the owning file (lint/prettier/type/build/test/isolation) until the chain exits 0.
    2. DONE-when hygiene sweep against §18 lines 2597-2603, each demonstrably green:
       - "WriteJournal recovery test passes" → tests/core/storage/WriteJournal.test.ts + WorkspacePersistence.test.ts green.
       - "API key encryption round-trip passes" → EncryptedStorage.test.ts + KeyVault.test.ts green.
       - "No message body appears in chrome.storage.local" → no-secrets-in-storage.test.ts case 3 green.
       - "Migration from v1 → v2 fixture passes" → IndexedDBMigrator.test.ts green.
       - "Workspace state persists across page reload and cross-surface handoff" → WorkspacePersistence.test.ts cases 1/4 green.
    3. Golden Rule 9 hygiene: grep new Phase-2 catch sites for debugLog calls (no bare empty catches in the new storage/security/http/utils files).
  </action>
  <acceptance_criteria>
    - `pnpm run verify:phase-2` exits 0 (the complete chain)
    - `pnpm vitest run` full suite green (all phases' tests together — no regression from the D-06/02-08 rewires)
    - `grep -rn "catch" src/core/storage src/core/security src/core/http src/core/utils --include="*.ts" -A1 | grep -v debugLog` returns no bare empty catch bodies (Golden Rule 9 sweep)
    - All five §18 DONE-when criteria green (mapped tests above pass)
  </acceptance_criteria>
  <verify>
    <automated>pnpm run verify:phase-2 && pnpm vitest run --reporter=dot</automated>
  </verify>
  <done>verify:phase-2 chain exits 0; full suite green; all five §18 DONE-when criteria are demonstrably met; no empty catches in the new code.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol | Kind | Location |
|--------|------|----------|
| storage-layer bootstrap (KeyVault first-run + migrate-on-read + IDB migrator) | behavior wiring | src/entrypoints/sidepanel/main.tsx, src/entrypoints/standalone/main.tsx |
| extended FORBIDDEN_TOKENS (idb, fflate, KeyVault, EncryptedStorage, fake-indexeddb) | const | tests/isolation/check-content-bundle.mjs |
| no-secrets-in-storage privacy test (4 cases) | test | tests/core/storage/no-secrets-in-storage.test.ts |
| green verify:phase-2 gate | evidence | (run output; no new file) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| surface mount → storage layer | First-run secret generation + migrations cross here on every launch |
| built content bundles | The R-3 isolation boundary — vault/IDB/network stack must never reach content scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-11-01 | Information Disclosure | plaintext secrets in chrome.storage.local | critical | mitigate | Automated privacy test (task 2 case 1) writes via the REAL KeyVault path then dumps local — any plaintext secret fails the gate; A-22 asserted as a truth. |
| T-2-11-02 | Information Disclosure | installSecret or bodies reaching sync/local | high | mitigate | Privacy test cases 2-3 assert sync is secret-free and bodies live in IndexedDB only (A-23/A-24) — the §18 DONE-when "no message body in chrome.storage.local" is machine-checked. |
| T-2-11-03 | Spoofing | vault/IDB code bundled into content scripts (R-3) | high | mitigate | FORBIDDEN_TOKENS extended with idb/fflate/KeyVault/EncryptedStorage/fake-indexeddb — check-content-bundle.mjs exits non-zero on any violation; the gate runs as the final verify:phase-2 step. |
| T-2-11-04 | Tampering | init bootstrap failing silently, degrading the whole phase | medium | mitigate | Every bootstrap step is wrapped: debugLog with canonical codes + graceful fall-through (Golden Rule 9); degraded state via getDegradedDbs + banner string shipped (D-12) for the Phase-7 UI. |
| T-2-11-05 | Integrity | empty catch bodies hiding storage errors | medium | mitigate | Task-3 Golden Rule 9 sweep greps new catch sites for debugLog presence — a bare catch fails the gate. |
</threat_model>

<verification>
- `pnpm run verify:phase-2` exits 0 (eslint + prettier + tsc + wxt build + vitest + isolation)
- `pnpm vitest run` full suite green (regression check across phases)
- `pnpm node tests/isolation/check-content-bundle.mjs` exits 0 with the extended tokens
- Privacy test green (4 cases)
</verification>

<success_criteria>
- Phase 2 ends green (Golden Rule 10): verify:phase-2 chain exits 0.
- All five STORAGE-01..05 requirements are evidenced green, and every §18 Phase-2 DONE-when criterion maps to a passing test.
- All eight specless-probe prohibitions (A-22..A-28) are asserted by an automated test somewhere in the phase.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-11-SUMMARY.md` when done.
</output>
