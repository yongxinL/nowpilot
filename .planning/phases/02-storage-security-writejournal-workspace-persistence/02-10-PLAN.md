---
phase: 02-storage-security-writejournal-workspace-persistence
plan: 10
type: execute
wave: 4
depends_on: [02-01]
files_modified:
  - src/core/utils/RateLimiter.ts
  - src/core/http/Requester.ts
  - tests/core/utils/RateLimiter.test.ts
  - tests/core/http/Requester.test.ts
autonomous: true
requirements: [§18]  # §18 create-list infrastructure (RateLimiter/Requester), NOT a STORAGE-N requirement — see objective
must_haves:
  truths:
    - "RateLimiter is a per-instance token-bucket limiter keyed by addonId (spec §10.7/§13) — functional primitive shipped now, add-on consumers land Phase 8."
    - "Requester wraps PROXY_FETCH via chrome.runtime.sendMessage with the Appendix C ProxyFetchRequest/ProxyFetchResponse shapes and a 25s timeout (agent discretion on defaults)."
    - "Every rejection routes through debugLog with a canonical code (Golden Rule 9) — never a silent catch."
    - "RateLimiter and Requester import nothing from zustand/storage — they are dependency-free primitives (R-3/R-4-safe for later panel usage)."
  artifacts:
    - src/core/utils/RateLimiter.ts
    - src/core/http/Requester.ts
    - tests/core/utils/RateLimiter.test.ts
    - tests/core/http/Requester.test.ts
  key_links:
    - "Requester ↔ PROXY_FETCH message type (Appendix C/E — the background SW's proxy entry)"
    - "RateLimiter per-addonId instances ↔ ProviderRegistry per-instance keyed pattern (02-PATTERNS analog)"
---

<objective>
Ship the two functional primitives from the §18 Phase-2 create-list that Phase 8's add-ons will consume: `RateLimiter` (per-instance token bucket keyed by addonId — §10.7/§13) and `Requester` (PROXY_FETCH wrapper with timeout + retry defaults). These are NOT tied to a single STORAGE-N requirement — they are §18 create-list infrastructure (RESEARCH validation map: "§18 | RateLimiter per-instance token bucket | unit"), hence frontmatter `requirements: [§18]` (the ROADMAP STORAGE-01..05 IDs are all covered by the other ten plans).

Purpose: Phase 8 add-ons (ServiceNow, Write, TeamGQM) call the background SW's proxy through Requester and are throttled by RateLimiter; shipping them now keeps the §18 create-list complete and the phase green.
Output: RateLimiter + Requester + unit tests.

Flagged assumptions (specless probe):
- A-21: Token-bucket parameters (capacity/refill) and timeout/retry defaults are agent discretion per CONTEXT 'the agent's Discretion' — pinned in constants, documented in headers, adjustable by Phase 8 consumers without signature change.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md §10.7 (ProxyFetch + RateLimiter per-instance per-addon semantics, lines ~1507/1523/1645), Appendix C ProxyFetchRequest/ProxyFetchResponse (lines 4611-4626)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-CONTEXT.md 'the agent's Discretion' (RateLimiter/Requester internals)
@.planning/phases/02-storage-security-writejournal-workspace-persistence/02-PATTERNS.md (RateLimiter analog: ProviderRegistry per-instance class; Requester analog: BroadcastBus.emit sendMessage+catch→debugLog, lines 238-240)
@src/core/error/errorCodes.ts (STORAGE-* + network codes, from 02-01)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RateLimiter.ts — per-instance token bucket keyed by addonId</name>
  <read_first>
    - PRODUCT_SPEC §10.7 (RateLimiter per-instance per-addon semantics — §13 line 1794 area)
    - 02-CONTEXT.md 'the agent's Discretion' (exact token-bucket params are discretion)
    - 02-PATTERNS.md (RateLimiter analog: ProviderRegistry per-instance keyed class shape, lines 234-236)
    - src/core/ai/ProviderRegistry.ts (per-instance class + factory precedent)
  </read_first>
  <files>
    src/core/utils/RateLimiter.ts
  </files>
  <action>
    Create src/core/utils/RateLimiter.ts — a dependency-free per-instance token bucket (imports only from @/core/error, R-4-safe). Export:
    - `interface RateLimiterConfig { capacity: number; refillPerSecond: number }` — default constants `DEFAULT_CAPACITY = 10` and `DEFAULT_REFILL_PER_SECOND = 2` (agent discretion per CONTEXT; pinned as named constants with a header note that Phase 8 add-ons can pass their own config).
    - `class RateLimiter` with:
      - constructor(addonId: string, config?: RateLimiterConfig) — keyed by addonId (per-instance, per-addon semantics §10.7).
      - `tryAcquire(tokens?: number): boolean` — synchronous token check; on success deducts tokens and records a timestamp for refill; on failure returns false (no throw).
      - `waitForToken(tokens?: number, timeoutMs?: number): Promise<boolean>` — async variant that polls until a token is available or the timeout expires (default 5000ms).
      - private refill mechanics: tokens accrue by elapsed time at refillPerSecond, capped at capacity (classic token bucket).
    - `getRateLimiter(addonId: string): RateLimiter` — a module-level factory keeping a Map<addonId, RateLimiter> (ProviderRegistry-style lazy instance map; do NOT share instances across addonIds).
    - Header comment: §10.7 per-addon semantics + "Phase 8 consumers arrive later; params are constants, not magic numbers".
  </action>
  <acceptance_criteria>
    - `grep -c "class RateLimiter" src/core/utils/RateLimiter.ts` returns >= 1
    - `grep -c "tryAcquire\|waitForToken" src/core/utils/RateLimiter.ts` returns >= 2
    - `grep -c "getRateLimiter" src/core/utils/RateLimiter.ts` returns >= 1 (factory)
    - `grep -c "addonId" src/core/utils/RateLimiter.ts` returns >= 2 (per-addon keying)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "waitForToken" src/core/utils/RateLimiter.ts</automated>
  </verify>
  <done>RateLimiter is a per-addonId token bucket with sync/async acquire and a factory; dependency-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Requester.ts — PROXY_FETCH wrapper with 25s timeout</name>
  <read_first>
    - PRODUCT_SPEC Appendix C ProxyFetchRequest/ProxyFetchResponse (lines 4611-4626 — verbatim shapes)
    - 02-PATTERNS.md Requester analog (BroadcastBus.emit — runtime.sendMessage + .catch → debugLog, lines 238-240)
    - 02-CONTEXT.md 'the agent's Discretion' (timeout/retry defaults)
  </read_first>
  <files>
    src/core/http/Requester.ts
  </files>
  <action>
    Create src/core/http/Requester.ts — the PROXY_FETCH client wrapper (runs in panels; the background SW executes the actual fetch — R-3 boundary). Export:
    - `DEFAULT_TIMEOUT_MS = 25_000` and `DEFAULT_RETRIES = 1` consts (agent discretion, documented in a header comment).
    - `request(payload: ProxyFetchRequest): Promise<ProxyFetchResponse>` — validates the payload shape (addonId, url, method in the locked set 'GET'|'POST'|'PUT'|'PATCH'|'DELETE'), then chrome.runtime.sendMessage({ type: 'PROXY_FETCH', ...payload }) wrapped with a timeout (Promise.race against a 25s timer) and one bounded retry (retry-safe only — Appendix C: 'PROXY_FETCH | Never retried unless caller marks request retry-safe'; add a `retrySafe?: boolean` field to the request for opt-in retry).
    - On rejection/timeout: debugLog with a canonical code (e.g. CONNECT_FAILED or NETWORK_STATUS), then return a ProxyFetchResponse-shaped failure { ok: false, status: 0, body: '', error: message } — NEVER throw to the caller (Golden Rule 9).
    - On success: return the ProxyFetchResponse { ok, status, body, error? } passthrough.
    - Header comment: §10.7 proxy flow — Requester is the panel-side client; PROXY_FETCH executes in the background SW only (R-3).
  </action>
  <acceptance_criteria>
    - `grep -c "DEFAULT_TIMEOUT_MS" src/core/http/Requester.ts` returns >= 1
    - `grep -c "PROXY_FETCH" src/core/http/Requester.ts` returns >= 1
    - `grep -c "retrySafe" src/core/http/Requester.ts` returns >= 1 (opt-in retry contract)
    - `grep -c "ProxyFetchResponse" src/core/http/Requester.ts` returns >= 2
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && grep -c "DEFAULT_TIMEOUT_MS" src/core/http/Requester.ts && grep -c "retrySafe" src/core/http/Requester.ts</automated>
  </verify>
  <done>Requester wraps PROXY_FETCH with validation, 25s timeout, opt-in bounded retry, and never throws.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RateLimiter.test.ts + Requester.test.ts</name>
  <read_first>
    - 02-PATTERNS.md (BroadcastBus.emit analog — fakeBrowser sendMessage mock pattern)
    - tests/core/runtime/BroadcastBus.test.ts or similar (existing sendMessage test conventions; else tests/core/workspace/WorkspaceStore.test.ts)
    - The two modules under test
  </read_first>
  <files>
    tests/core/utils/RateLimiter.test.ts
    tests/core/http/Requester.test.ts
  </files>
  <action>
    Create tests/core/utils/RateLimiter.test.ts (per RESEARCH validation map — "RateLimiter per-instance token bucket"):
    1. Capacity: a limiter at capacity refills to full after the refill window (use a small capacity/refill config, e.g. capacity 3, refillPerSecond 0.05, and fake timers or short sleeps — determinism preferred via injected timestamps if the class allows; else use vi.useFakeTimers).
    2. Per-addon isolation: two addonIds get independent buckets via getRateLimiter — draining one does not affect the other.
    3. tryAcquire exhaustion: acquire until empty → further tryAcquire returns false without throwing.
    4. waitForToken timeout: with a depleted bucket and a short timeout → resolves false after the timeout, never hangs.

    Create tests/core/http/Requester.test.ts (per RESEARCH validation map — "Requester wraps PROXY_FETCH with 25s timeout (fakeBrowser sendMessage)"):
    1. Success passthrough: mock fakeBrowser.runtime.sendMessage to resolve { ok: true, status: 200, body: '…' } → request returns that response unchanged.
    2. Timeout: mock sendMessage to never settle; request with a short injected timeout (make the timeout injectable as an option param) → resolves { ok: false, status: 0, body: '', error } and debugLog called.
    3. Rejection: mock sendMessage to reject → failure-shaped response, no throw, debugLog with a canonical code.
    4. Method validation: an invalid method value is refused before sendMessage (locked set 'GET'|'POST'|'PUT'|'PATCH'|'DELETE').
  </action>
  <acceptance_criteria>
    - `pnpm vitest run tests/core/utils/RateLimiter.test.ts tests/core/http/Requester.test.ts --reporter=dot` passes (all cases)
    - `grep -c "fakeBrowser" tests/core/http/Requester.test.ts` returns >= 1 (sendMessage mocked)
    - `grep -c "getRateLimiter" tests/core/utils/RateLimiter.test.ts` returns >= 1 (per-addon isolation asserted)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/core/utils/RateLimiter.test.ts tests/core/http/Requester.test.ts --reporter=dot</automated>
  </verify>
  <done>Token-bucket capacity/isolation/timeout and PROXY_FETCH success/timeout/rejection/validation are all proven.</done>
</task>

</tasks>

## Artifacts this plan produces

| Symbol | Kind | Location |
|--------|------|----------|
| `RateLimiterConfig` / `DEFAULT_CAPACITY` / `DEFAULT_REFILL_PER_SECOND` | type/const | src/core/utils/RateLimiter.ts |
| `class RateLimiter` (tryAcquire, waitForToken) | class | src/core/utils/RateLimiter.ts |
| `getRateLimiter(addonId)` | factory | src/core/utils/RateLimiter.ts |
| `DEFAULT_TIMEOUT_MS` / `DEFAULT_RETRIES` | const | src/core/http/Requester.ts |
| `request(payload: ProxyFetchRequest): Promise<ProxyFetchResponse>` | fn | src/core/http/Requester.ts |
| RateLimiter + Requester tests (8 cases) | test | tests/core/utils/RateLimiter.test.ts, tests/core/http/Requester.test.ts |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel Requester → background SW (PROXY_FETCH) | Untrusted target-host responses cross here; the SW performs the real fetch |
| addon requests → RateLimiter | Throttle boundary before any network activity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-10-01 | Spoofing | invalid ProxyFetchRequest reaching the SW | medium | mitigate | request() validates method/addonId/url shape before sendMessage (test case 4) — malformed payloads never cross the boundary; the SW additionally validates sender.id (§16.2, Phase 1). |
| T-2-10-02 | Denial of Service | unbounded retries / hung proxy calls | medium | mitigate | One bounded retry, opt-in via retrySafe (Appendix C: PROXY_FETCH never retried unless marked); 25s timeout resolves to a failure response, never a hang (test cases 2-3). |
| T-2-10-03 | Information Disclosure | request/response bodies in debugLog | medium | mitigate | All logging routes through debugLog (TraceRedactor, 02-02); Requester logs codes + errors, never raw bodies — headers documented, callers must redact bodies before logging (R-10). |
| T-2-10-04 | Denial of Service | one addon exhausting proxy capacity | medium | mitigate | RateLimiter per-addonId buckets isolate traffic (test case 2) — Phase 8 wires them ahead of each addon's Requester usage. |
</threat_model>

<verification>
- `pnpm vitest run tests/core/utils/RateLimiter.test.ts tests/core/http/Requester.test.ts` green
- `pnpm typecheck` clean; eslint/prettier clean on touched files
- No zustand/storage imports in either module (dependency-free grep)
</verification>

<success_criteria>
- The §18 Phase-2 create-list is complete: RateLimiter and Requester ship as functional primitives.
- Neither module throws to callers; every failure routes through debugLog with canonical codes.
- Phase 8 can consume both without signature changes.
</success_criteria>

<output>
Create `.planning/phases/02-storage-security-writejournal-workspace-persistence/02-10-SUMMARY.md` when done.
</output>
