---
phase: 10-memory-governance-and-experience-candidates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/harness.ts
  - src/core/memory/MemoryRecord.ts
  - src/core/memory/MemoryGovernance.ts
  - src/core/memory/MemoryEngine.ts
  - src/core/storage/MemoryDB.ts
  - tests/core/memory/governance/MemoryRecord.test.ts
  - tests/core/memory/governance/MemoryGovernance.test.ts
  - tests/core/memory/governance/MemoryEngine.governance.test.ts
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - MemoryRecord type with source/confidence/lifecycle/sensitivity/verifiedAt fields exists in src/types/harness.ts
    - resolveConflict implements deterministic precedence: correction > verified > prior > inference
    - MemoryDB v5 migration creates memory_records + procedural_experiences stores idempotently
    - MemoryGovernance facade exposes all 9 controls (view/source/confidence/edit/pin/forget/disableType/export/cloudExclude)
    - MemoryEngine.retrieveMemoryHints filters procedural records with status !== 'approved'
  artifacts:
    - src/core/memory/MemoryRecord.ts
    - src/core/memory/MemoryGovernance.ts
    - tests/core/memory/governance/MemoryRecord.test.ts
    - tests/core/memory/governance/MemoryGovernance.test.ts
  key_links:
    - MemoryRecord type → MemoryDB.memory_records store (v5 migration)
    - MemoryGovernance → MemoryDB (all mutations via WriteJournal + isPrimaryWriter gate)
    - MemoryEngine.retrieveMemoryHints → governance status filter for procedural records
---

<objective>
MemoryRecord taxonomy, deterministic conflict resolution, v5 IDB migration, and the 9-control MemoryGovernance facade — the governance foundation over the Phase-8 memory system.

Purpose: MemoryRecords carry rich metadata (source/confidence/lifecycle/sensitivity/verified-at), conflicts resolve deterministically without LLM, and users can view/edit/pin/forget/disable/export/cloud-exclude their memories through a facade that Phase-15 UI will later wire to buttons.

Output: MemoryRecord types in harness.ts, MemoryRecord.ts with resolveConflict, MemoryGovernance.ts facade, MemoryDB v5 migration, MemoryEngine gating, and passing tests for MEM-01/02/03/04.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-memory-governance-and-experience-candidates/10-CONTEXT.md
@.planning/phases/10-memory-governance-and-experience-candidates/10-RESEARCH.md
@src/types/harness.ts
@src/core/memory/types.ts
@src/core/memory/MemoryEngine.ts
@src/core/memory/UserMemoryStore.ts
@src/core/storage/MemoryDB.ts
@src/core/storage/IndexedDBMigrator.ts
@src/core/storage/NotesDB.ts
@tests/core/storage/migrations/v4-notes-backup-config.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end MemoryRecord type + conflict resolution + v5 storage</name>
  <files>
    src/types/harness.ts
    src/core/memory/MemoryRecord.ts
    src/core/storage/MemoryDB.ts
    tests/core/memory/governance/MemoryRecord.test.ts
  </files>
  <read_first>
    src/types/harness.ts — canonical type home, existing WorkingMemory/ContextItem patterns
    src/core/memory/types.ts — UserMemoryFact shape (MemoryRecord extends this per D-126)
    src/core/storage/MemoryDB.ts — current v1 schema, MEMORY_DB_VERSION, openMemoryDB pattern
    src/core/storage/IndexedDBMigrator.ts — registerMigration pattern
    src/core/storage/NotesDB.ts — v4 migration precedent (registerMigration + populateNoteTypeDefaults)
    tests/core/storage/migrations/v4-notes-backup-config.test.ts — migration test pattern
  </read_first>
  <action>
    Wire ONE path through every layer: type definition → conflict resolver → IDB storage, proven by a passing conflict-precedence test.

    **Step 1 — Add canonical types to `src/types/harness.ts`** (per D-126, Appendix C.1 spec 4900-4915):
    Add after the WorkingMemory block (after line 117):
    - `MemoryKind = 'working' | 'episodic' | 'semantic' | 'preference' | 'procedural'` (MEM-01 taxonomy)
    - `MemoryRecord` interface extending `UserMemoryFact` (import from '../core/memory/types') with additive governance fields:
      - `kind: MemoryKind`
      - `source: { kind: 'extracted' | 'manual' | 'imported'; noteId?: string; conversationId?: string }` (per D-126)
      - `confidence: number` (already on UserMemoryFact, keep)
      - `lifecycle: { status: 'active' | 'pinned' | 'forgotten'; verifiedAt?: number; expiresAt?: number }`
      - `sensitivity: 'normal' | 'personal' | 'secret'`
      - `revisionChain?: Array<{ id: string; replacedAt: number }>` (audit trail per D-127)
    - `ProceduralExperience` interface: `{ id: string; title: string; description: string; steps: string[]; source: MemoryRecord['source']; confidence: number; status: 'proposed' | 'verified' | 'approved' | 'rejected'; verifiedAt?: number; approvedAt?: number; createdAt: number; updatedAt: number }` (MEM-05)
    - `KnowledgeEdgeSource = 'explicit' | 'imported' | 'suggested' | 'accepted'` (KNW-01)
    - Import `UserMemoryFact` from '../core/memory/types' for the extends clause.

    **Step 2 — Create `src/core/memory/MemoryRecord.ts`** (per D-127):
    - Export `CONFLICT_PRECEDENCE: readonly string[] = ['correction', 'verified', 'prior', 'inference']`
    - Export `resolveConflict(a: MemoryRecord, b: MemoryRecord): MemoryRecord` — pure function implementing:
      1. Map each record's source.kind to a precedence category: 'manual' → 'correction' if revisionChain non-empty else 'verified'; 'extracted' → 'prior'; 'imported' → 'inference'
      2. Compare precedence ranks (lower index wins)
      3. Tie-break: higher confidence → more recent lifecycle.verifiedAt → id asc
      4. Winner absorbs loser into revisionChain
    - Export `computeConflictKey(record: MemoryRecord): string` — normalized content (lowercase, trimmed) + sorted tags joined, for conflict detection
    - Export `detectConflicts(records: MemoryRecord[]): Array<[MemoryRecord, MemoryRecord]>` — pairs with matching conflict keys

    **Step 3 — Extend `src/core/storage/MemoryDB.ts` with v5 migration** (per D-131):
    - Bump `MEMORY_DB_VERSION` from 1 to 5
    - Import `registerMigration` from './IndexedDBMigrator'
    - Add `registerMigration('MemoryDB', { fromVersion: 1, toVersion: 5, description: 'Add memory_records + procedural_experiences stores (Phase 10 governance)', migrate: (db) => { if (!db.objectStoreNames.contains('memory_records')) { const s = db.createObjectStore('memory_records', { keyPath: 'id' }); s.createIndex('byKind', 'kind'); s.createIndex('byStatus', 'lifecycle.status'); s.createIndex('byConfidence', 'confidence'); } if (!db.objectStoreNames.contains('procedural_experiences')) { db.createObjectStore('procedural_experiences', { keyPath: 'id' }); } } })`
    - Update the `MemoryDBV1` interface to `MemoryDBV5` with the new stores typed
    - Update `openMemoryDB` generic to `MemoryDBV5`

    **Step 4 — Create `tests/core/memory/governance/MemoryRecord.test.ts`**:
    - Test MEM-01: MemoryKind union has exactly 5 values
    - Test MEM-02: MemoryRecord fixture has all required fields (source.kind, confidence, lifecycle.status, lifecycle.verifiedAt, sensitivity)
    - Test MEM-03: resolveConflict — correction beats verified, verified beats prior, prior beats inference; tie-break by confidence, then verifiedAt, then id
    - Test: revisionChain audit trail — winner absorbs loser's id
    - Test: computeConflictKey — same content + tags produce same key regardless of case/whitespace
    - Test: v5 migration — memory_records + procedural_experiences stores exist after openMemoryDB; idempotent on second open

    All new code is strict-clean (NP-STRICT ceiling = 0 per STATE.md decision 17). No `@ts-expect-error` markers.
  </action>
  <verify>
    <automated>cd /Users/george.li/Documents/workspaces/nowpilot && pnpm test -- tests/core/memory/governance/MemoryRecord.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - MemoryRecord interface in harness.ts has kind, source (object with kind/noteId/conversationId), lifecycle (object with status/verifiedAt/expiresAt), sensitivity, revisionChain fields
    - resolveConflict returns the higher-precedence record for all 6 pairwise combinations of correction/verified/prior/inference
    - MemoryDB.memory_records and MemoryDB.procedural_experiences stores exist after openMemoryDB()
    - Opening MemoryDB twice does not throw or duplicate stores (idempotent)
    - All tests pass with zero NP-STRICT markers
  </acceptance_criteria>
  <done>MemoryRecord type → resolveConflict → MemoryDB v5 storage works end-to-end; conflict precedence test passes (correction > verified > prior > inference); v5 migration is idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: MemoryGovernance facade — all 9 user lifecycle controls</name>
  <files>
    src/core/memory/MemoryGovernance.ts
    tests/core/memory/governance/MemoryGovernance.test.ts
  </files>
  <read_first>
    src/core/memory/MemoryRecord.ts — MemoryRecord type from Task 1
    src/core/storage/MemoryDB.ts — memory_records store interface from Task 1
    src/core/memory/UserMemoryStore.ts — upsertFact pattern (isPrimaryWriter gate, IDB put, metadata index)
    src/core/workspace/WorkspaceStore.ts — isPrimaryWriter() signature
    src/core/storage/WriteJournal.ts — journaled mutation pattern
    src/core/log/TraceRedactor.ts — redaction before export
    tests/core/memory/governance/MemoryRecord.test.ts — test patterns from Task 1
  </read_first>
  <action>
    Create the MemoryGovernance facade exposing all 9 user lifecycle controls as async functions over MemoryDB.memory_records, all gated by isPrimaryWriter() and journaled via WriteJournal (per D-128).

    **Step 1 — Create `src/core/memory/MemoryGovernance.ts`**:
    Export `MemoryGovernance` as an object-form namespace (ProviderRegistry/PageIndexBuilder convention) with these methods:
    - `async view(recordId: string): Promise<MemoryRecord | undefined>` — read from memory_records store
    - `async source(recordId: string): Promise<MemoryRecord['source'] | undefined>` — return source metadata only
    - `async confidence(recordId: string): Promise<number | undefined>` — return confidence only
    - `async edit(recordId: string, patch: Partial<MemoryRecord>): Promise<void>` — merge patch, update lifecycle.verifiedAt = Date.now(), gate on isPrimaryWriter(), journal via WriteJournal, put to memory_records
    - `async pin(recordId: string): Promise<void>` — set lifecycle.status = 'pinned', same gating
    - `async forget(recordId: string): Promise<void>` — set lifecycle.status = 'forgotten' (soft-delete), same gating
    - `async disableType(kind: MemoryKind): Promise<void>` — set lifecycle.status = 'forgotten' for ALL records where kind matches (bulk operation), same gating
    - `async export(filter?: (r: MemoryRecord) => boolean): Promise<string>` — serialize filtered records to JSON, apply TraceRedactor to redact sensitivity='secret' records, return JSON string
    - `async cloudExclude(recordId: string): Promise<void>` — set a cloudExclude flag (add `cloudExclude?: boolean` to MemoryRecord in harness.ts if not already present), same gating

    Every mutation method MUST:
    1. Check `isPrimaryWriter()` — return early (no-op) if false, with debugLog
    2. Open MemoryDB, read current record
    3. Apply mutation
    4. Journal via WriteJournal (operation: 'update-memory-record')
    5. Put updated record to memory_records store

    **Step 2 — Create `tests/core/memory/governance/MemoryGovernance.test.ts`**:
    - Mock isPrimaryWriter to return true
    - Test view: returns record from memory_records
    - Test source: returns source object
    - Test confidence: returns number in [0,1]
    - Test edit: patch applied, verifiedAt updated
    - Test pin: status → 'pinned'
    - Test forget: status → 'forgotten' (soft-delete, record still exists)
    - Test disableType: all records of given kind forgotten
    - Test export: returns valid JSON, redacts secret records
    - Test cloudExclude: flag set to true
    - Test non-primary: all mutations are no-ops when isPrimaryWriter returns false
  </action>
  <verify>
    <automated>cd /Users/george.li/Documents/workspaces/nowpilot && pnpm test -- tests/core/memory/governance/MemoryGovernance.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - MemoryGovernance object has all 9 methods: view, source, confidence, edit, pin, forget, disableType, export, cloudExclude
    - All mutation methods check isPrimaryWriter() and are no-ops for non-primary surfaces
    - edit() updates lifecycle.verifiedAt to current timestamp
    - forget() sets status to 'forgotten' but does NOT delete the record
    - disableType() bulk-updates all records of the given kind
    - export() returns JSON string with secret records redacted
    - All tests pass
  </acceptance_criteria>
  <done>MemoryGovernance facade exposes all 9 controls; mutations are single-writer gated and journaled; non-primary surfaces are read-only; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: MemoryEngine procedural gating — filter unapproved records</name>
  <files>
    src/core/memory/MemoryEngine.ts
    tests/core/memory/governance/MemoryEngine.governance.test.ts
  </files>
  <read_first>
    src/core/memory/MemoryEngine.ts — retrieveMemoryHints, retrieveUserMemory, upsert methods
    src/core/memory/MemoryRecord.ts — MemoryRecord type from Task 1
    src/core/memory/MemoryGovernance.ts — view method from Task 2
    src/core/storage/MemoryDB.ts — procedural_experiences store from Task 1
    tests/core/memory/MemoryEngine.test.ts — existing test patterns (isPrimaryWriter mock, beforeEach)
  </read_first>
  <action>
    Extend MemoryEngine to gate procedural experience records — only `status === 'approved'` records are returned by retrieveMemoryHints/retrieveUserMemory (per D-129, MEM-05).

    **Step 1 — Extend `src/core/memory/MemoryEngine.ts`**:
    - Import `openMemoryDB` from '../storage/MemoryDB'
    - Add `async retrieveProceduralExperience(query: string, opts?: { tier?: ModelContextTier; now?: number }): Promise<RetrievedMemory[]>` method:
      1. Open MemoryDB
      2. Get all records from procedural_experiences store
      3. Filter: `status === 'approved'` only
      4. Score by keyword match (reuse MemoryScorer.scoreMemory or simple term overlap)
      5. Map to RetrievedMemory shape
      6. Apply same top-k/token budget as retrieveUserMemory
    - Modify `retrieveMemoryHints` to optionally include procedural records: after retrieving user memory, call retrieveProceduralExperience, merge results, re-sort by score, trim to budget
    - Add `async submitProceduralExperience(record: Omit<ProceduralExperience, 'id' | 'createdAt' | 'updatedAt'>): Promise<string>` — insert into procedural_experiences store with status='proposed' (for Phase-15 UI to later verify/approve)

    **Step 2 — Create `tests/core/memory/governance/MemoryEngine.governance.test.ts`**:
    - Test: procedural record with status='proposed' is NOT returned by retrieveMemoryHints
    - Test: procedural record with status='approved' IS returned by retrieveMemoryHints
    - Test: procedural record with status='rejected' is NOT returned
    - Test: retrieveProceduralExperience returns only approved records
    - Test: submitProceduralExperience creates record with status='proposed'
    - Test: mixing user facts + procedural records — both returned when procedural is approved
  </action>
  <verify>
    <automated>cd /Users/george.li/Documents/workspaces/nowpilot && pnpm test -- tests/core/memory/governance/MemoryEngine.governance.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - retrieveMemoryHints does NOT return procedural records with status 'proposed' or 'rejected'
    - retrieveMemoryHints DOES return procedural records with status 'approved'
    - retrieveProceduralExperience returns only approved records
    - submitProceduralExperience creates a record with status='proposed'
    - Existing MemoryEngine tests still pass (no regression)
  </acceptance_criteria>
  <done>MemoryEngine gates procedural experience: only approved records reach chat context; proposed/rejected records are invisible to retrieveMemoryHints; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → MemoryGovernance | User actions (pin/forget/edit) cross from UI into core mutation logic |
| MemoryGovernance → MemoryDB | Governance facade writes to IDB via WriteJournal |
| MemoryEngine → procedural_experiences | Retrieval crosses from engine into procedural store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | MemoryGovernance mutation methods | high | mitigate | isPrimaryWriter() gate on all mutations; WriteJournal for crash-safety |
| T-10-02 | Information Disclosure | MemoryGovernance.export() | high | mitigate | TraceRedactor redacts sensitivity='secret' records before serialization |
| T-10-03 | Tampering | Procedural experience injection | medium | mitigate | status === 'approved' required for retrieval by retrieveMemoryHints |
| T-10-04 | Denial Service | MemoryDB v5 migration | low | accept | Idempotent migration; failure recorded to ErrorStore, degraded mode |
| T-10-SC | Tampering | npm installs | low | accept | No new packages in Phase 10; all deps already validated |
</threat_model>

<verification>
1. `pnpm test -- tests/core/memory/governance` — all 3 test files pass
2. `pnpm lint` — tsc --noEmit passes with zero NP-STRICT markers
3. Conflict precedence: correction > verified > prior > inference (MEM-03)
4. All 9 MemoryGovernance controls functional (MEM-04)
5. Procedural gating: only approved records retrieved (MEM-05 partial)
</verification>

<success_criteria>
- MemoryRecord type with full governance metadata exists in harness.ts
- resolveConflict implements deterministic precedence (no LLM)
- MemoryDB v5 migration creates both new stores idempotently
- MemoryGovernance facade exposes all 9 controls, single-writer gated
- MemoryEngine filters unapproved procedural records
- All tests pass, zero NP-STRICT markers
</success_criteria>

<output>
Create `.planning/phases/10-memory-governance-and-experience-candidates/10-01-SUMMARY.md` when done
</output>

<artifacts_this_phase_produces>
**Plan 10-01 artifacts:**
1. `src/types/harness.ts` — extended with MemoryRecord, MemoryKind, ProceduralExperience, KnowledgeEdgeSource types
2. `src/core/memory/MemoryRecord.ts` — NEW: resolveConflict, computeConflictKey, detectConflicts
3. `src/core/memory/MemoryGovernance.ts` — NEW: 9-control facade (view/source/confidence/edit/pin/forget/disableType/export/cloudExclude)
4. `src/core/memory/MemoryEngine.ts` — EXTENDED: retrieveProceduralExperience, submitProceduralExperience, procedural gating in retrieveMemoryHints
5. `src/core/storage/MemoryDB.ts` — EXTENDED: v5 migration (memory_records + procedural_experiences stores)
6. `tests/core/memory/governance/MemoryRecord.test.ts` — NEW: MEM-01/02/03 tests
7. `tests/core/memory/governance/MemoryGovernance.test.ts` — NEW: MEM-04 tests
8. `tests/core/memory/governance/MemoryEngine.governance.test.ts` — NEW: MEM-05 gating tests
</artifacts_this_phase_produces>
