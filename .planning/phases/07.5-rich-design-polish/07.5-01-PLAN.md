---
phase: 07.5-rich-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/memory/memoryTypes.ts
  - src/core/memory/PreferenceMemoryStore.ts
  - src/core/ai/persona/PersonaService.ts
  - src/core/context/contextTypes.ts
  - src/core/attachments/AttachmentModel.ts
autonomous: true
requirements:
  - RICH-R-04
  - RICH-H-07

must_haves:
  truths:
    - "PreferenceMemoryStore persists `balanced` as a valid `responseBrevity` alongside `concise` and `detailed`"
    - "PreferenceMemoryStore persists `aiTone` as a typed enum (`professional` | `professional_approachable`), not free-text"
    - "Zustand `migrate` transforms legacy free-text `aiTone` values into the new typed enum on first load"
    - "`PersonaService.getActiveProfile()` returns a `balanced` responseStyle when responseBrevity is `balanced`"
    - "`ContextOptimizerInput` accepts optional `referenceTokens` and `attachments` fields without breaking existing callers"
    - "`AttachmentModel.ts` exports typed discriminated unions for ImageAttachment, FileAttachment, VoiceTranscriptAttachment, and ClipboardTextAttachment"
  artifacts:
    - "src/core/memory/memoryTypes.ts — `responseBrevity` enum updated to `'concise' | 'balanced' | 'detailed'`; `aiTone` changed to `z.enum(['professional', 'professional_approachable']).optional()`"
    - "src/core/memory/PreferenceMemoryStore.ts — persist config includes `version: 1` and `migrate` function"
    - "src/core/ai/persona/PersonaService.ts — `'balanced'` case added to `getActiveProfile()`"
    - "src/core/context/contextTypes.ts — `referenceTokens?: ReferenceToken[]` and `attachments?: Attachment[]` added to `ContextOptimizerInput` interface"
    - "src/core/attachments/AttachmentModel.ts — discriminated union types + zod schemas for all 4 attachment types"
  key_links:
    - "PreferenceMemoryStore migrate idempotency — multi-surface Chrome extension must not double-transform"
    - "PersonaService `balanced` case mapping — exact wording for responseStyle must match PersonaProfile pattern"
    - "ContextOptimizerInput backward compat — optional fields must not break existing tests or callers"

---

<objective>
Establish the data foundation for Phase 7.5 — migrate PreferenceMemoryStore enums (D-09, D-10), update PersonaService for the new `balanced` brevity level, extend ContextOptimizerInput with ReferenceToken and Attachment fields, and define the unified Attachment model type hierarchy.

**Purpose:** Phase 7.5's 18 P2 requirements across 7 feature areas all depend on these type/store extensions. Getting the data layer right first avoids cascading rework in downstream UI plans.

**Output:** Updated memoryTypes/PreferenceMemoryStore/PersonaService/contextTypes, new AttachmentModel.ts with typed discriminated unions.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS_RICH_DESIGN.md
@.planning/phases/07.5-rich-design-polish/07.5-CONTEXT.md
@.planning/phases/07.5-rich-design-polish/07.5-RESEARCH.md

@src/core/memory/memoryTypes.ts
@src/core/memory/PreferenceMemoryStore.ts
@src/core/ai/persona/PersonaService.ts
@src/core/ai/persona/PersonaProfile.ts
@src/core/context/contextTypes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PreferenceMemoryStore Enum Migration (D-09, D-10)</name>
  <files>
    src/core/memory/memoryTypes.ts
    src/core/memory/PreferenceMemoryStore.ts
    tests/core/memory/PreferenceMemoryStore.test.ts
  </files>
  <behavior>
    - Test 1: Zustand `migrate` transforms legacy free-text `aiTone` (e.g. `'Professional + approachable — technically accurate, direct, practical, friendly'`) to `'professional_approachable'`
    - Test 2: `migrate` transforms `'Professional'` to `'professional'`, unknown strings default to `'professional_approachable'`
    - Test 3: `responseBrevity` old values (`'concise'`, `'detailed'`) pass through unchanged; `'balanced'` is accepted as valid
    - Test 4: migrate is idempotent — already-migrated `aiTone` enum values (`'professional'`, `'professional_approachable'`) pass through unchanged
    - Test 5: `version: 1` in persist config; second surface load skips migration (version already 1)
  </behavior>
  <action>
    **Step A — Update `memoryTypes.ts`:**
    Per D-09 and D-10: Change `responseBrevity` from `z.enum(['concise', 'detailed']).optional()` to `z.enum(['concise', 'balanced', 'detailed']).optional()`. Change `aiTone` from `z.string().optional()` to `z.enum(['professional', 'professional_approachable']).optional()`.

    **Step B — Add Zustand `migrate` to `PreferenceMemoryStore.ts`:**
    Add `version: 1` and a `migrate` function to the persist config (currently at lines 60-64, no version/migrate). The migrate function MUST be idempotent per RESEARCH.md Pitfall 3:
    - Guard aiTone mapping: check `typeof persisted.aiTone === 'string'` AND the value is NOT already `'professional'` or `'professional_approachable'` before transforming.
    - Map known strings: long DEFAULT_PERSONA_PROFILE string → `'professional_approachable'`, `'Professional + approachable'` → `'professional_approachable'`, `'Professional'` → `'professional'`. Unknown → `'professional_approachable'`.
    - Log a `console.warn` for unmapped aiTone values (helps debugging without blocking migration).
    - responseBrevity old values are forward-compatible — no transformation needed. `'balanced'` is the new value, not a migration target.

    **Step C — Add migration tests:**
    Extend `tests/core/memory/PreferenceMemoryStore.test.ts` with migration scenarios:
    - version 0 persisted state with free-text aiTone → after rehydrate, aiTone is typed enum
    - version 0 with `responseBrevity: 'concise'` → after rehydrate, still `'concise'`
    - version 1 state → no transformation (idempotency)
    - unknown aiTone string → defaults to `'professional_approachable'` with console.warn
  </action>
  <verify>
    <automated>npx vitest run tests/core/memory/PreferenceMemoryStore.test.ts</automated>
  </verify>
  <done>
    - responseBrevity enum accepts `'balanced'`; aiTone is typed `'professional' | 'professional_approachable' | undefined`
    - Zustand migrate transforms legacy free-text aiTone → typed enum on first load
    - Migration is idempotent (already-typed values pass through)
    - All existing preference tests still pass
    - New migration tests (4+ scenarios) pass
  </done>
</task>

<task type="auto">
  <name>Task 2: PersonaService.getActiveProfile() + ContextOptimizerInput + AttachmentModel</name>
  <files>
    src/core/ai/persona/PersonaService.ts
    src/core/context/contextTypes.ts
    src/core/attachments/AttachmentModel.ts
    tests/core/context/contextTypes.test.ts
    tests/core/ai/persona/PersonaService.test.ts
  </files>
  <action>
    **Step A — Add `'balanced'` case to `PersonaService.getActiveProfile()` (D-10):**
    At `PersonaService.ts:31-35`, the switch currently handles only `'concise'` and `'detailed'`. Add a `'balanced'` case:
    ```
    case 'balanced':
      responseStyle = 'Balanced — moderately detailed with context-appropriate expansion';
      break;
    ```
    Make `'balanced'` the default case (when responseBrevity is undefined or unrecognized), replacing the current `default: 'Concise...'` with the balanced value per D-10 ("Balanced = default").

    **Step B — Extend `ContextOptimizerInput` (D-04, D-37):**
    At `contextTypes.ts:69-87`, add two optional fields to the `ContextOptimizerInput` interface:
    - `referenceTokens?: ReferenceToken[]` — structured pointers to @mentioned resources (D-04, D-05). Import `ReferenceToken` type from a forward declaration or inline it minimally: `{ type: string; id: string; title: string; displayLabel: string }`.
    - `attachments?: Attachment[]` — structured attachment objects (D-37). Import `Attachment` from the new `AttachmentModel.ts`.

    Update the zod schema (lines 115-146) to include `.optional()` schemas for both new fields. For `referenceTokens`, validate each token has `type`, `id`, `title`, `displayLabel` (all strings). For `attachments`, use `z.discriminatedUnion('kind', [...])` once AttachmentModel is created.

    **Step C — Create `AttachmentModel.ts` (D-35, D-36, RICH-H-07 type foundation):**
    New file at `src/core/attachments/AttachmentModel.ts`. Define a discriminated union:
    - `ImageAttachment`: kind `'image'`, mimeType (string), dataUrl (string), fileName (string), sizeBytes (number)
    - `FileAttachment`: kind `'file'`, mimeType, name, sizeBytes, content? (string, optional — resolved on-demand)
    - `VoiceTranscriptAttachment`: kind `'voice_transcript'`, transcript (string), durationSeconds? (number)
    - `ClipboardTextAttachment`: kind `'clipboard_text'`, text (string), sourceUrl? (string)
    - `SuggestedFieldAction` (RICH-H-07 type only — no implementation): kind `'suggested_field'`, selector (string), suggestedValue (string), label (string)

    Export the union type `Attachment` and a zod discriminated union schema for validation. The model establishes one ingestion pipeline — ContextOptimizer decides budget per type (D-37).
  </action>
  <verify>
    <automated>npx vitest run tests/core/ai/persona/PersonaService.test.ts tests/core/context/contextTypes.test.ts</automated>
  </verify>
  <done>
    - PersonaService returns `'Balanced — moderately detailed with context-appropriate expansion'` for `responseBrevity: 'balanced'`
    - ContextOptimizerInput accepts optional `referenceTokens` and `attachments`; existing tests pass unchanged
    - AttachmentModel exports `Attachment` discriminated union with 5 subtypes + zod schema
    - All existing tests (contextTypes, PersonaService) still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → migrate function | Untrusted persisted state enters migrate — must validate shapes, not blindly trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.5-01 | Tampering | PreferenceMemoryStore migrate | medium | mitigate | migrate function validates aiTone typeof before transforming; unknown values default to safe enum; idempotency guard prevents double-transformation |
| T-07.5-02 | Information Disclosure | AttachmentModel dataUrl | medium | mitigate | ImageAttachment dataUrl is stored in-memory only (not persisted); ContextOptimizer strips dataUrls before sending to AI providers |
| T-07.5-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced in this phase — all dependencies pre-existing and audited |
</threat_model>

<verification>
- `npx vitest run tests/core/memory/PreferenceMemoryStore.test.ts tests/core/ai/persona/PersonaService.test.ts tests/core/context/contextTypes.test.ts` — all pass
- TypeScript compilation: `npx tsc --noEmit` — no errors
</verification>

<success_criteria>
- PreferenceMemoryStore auto-migrates legacy aiTone values on load; `responseBrevity` accepts `'balanced'`
- PersonaService correctly maps `'balanced'` to a responseStyle string
- ContextOptimizerInput extended with optional `referenceTokens` and `attachments` fields
- AttachmentModel provides typed foundation for all Phase 7.5 attachment features
</success_criteria>

<output>
Create `.planning/phases/07.5-rich-design-polish/07.5-01-SUMMARY.md` when done
</output>
