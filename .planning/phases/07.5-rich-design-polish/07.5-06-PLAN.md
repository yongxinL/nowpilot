---
phase: 07.5-rich-design-polish
plan: 06
type: execute
wave: 2
depends_on:
  - 07.5-01
  - 07.5-03
files_modified:
  - src/components/options/PersonaPage.tsx
  - src/components/options/MemoryPage.tsx
  - src/components/options/OptionsRoot.tsx
  - src/components/options/AdvancedSection.tsx
autonomous: true
requirements:
  - RICH-R-04
  - RICH-I-11

must_haves:
  truths:
    - "Options → Persona shows three cards: AI Name (text input), Tone (two radio cards with preview), Response Brevity (three-segment control with preview)"
    - "Persona changes are saved to PreferenceMemoryStore via `setPreferences()` and applied through existing `PersonaService.getActiveProfile()`"
    - "Each persona setting card includes a preview example showing how a sample response changes (D-12)"
    - "Options → Advanced → Memory shows three tabs: User Memory (facts with confidence indicators), Conversation Memory (summaries with expand/delete), Preferences (read-only)"
    - "User Memory facts show 🟢 High (>0.7), 🟡 Medium (0.4-0.7), 🔴 Low (<0.4) confidence badges with filtering controls"
    - "User Memory facts support Confirm, Reject, and Delete actions with Popconfirm dialogs"
  artifacts:
    - "src/components/options/PersonaPage.tsx — persona editor with 3 setting cards + previews"
    - "src/components/options/MemoryPage.tsx — memory browser with 3 tabs + confidence indicators"
    - "src/components/options/OptionsRoot.tsx — modified: 'persona' entry added to OPTIONS_SECTIONS"
    - "src/components/options/AdvancedSection.tsx — modified: Memory tab added"
  key_links:
    - "PersonaPage saves via `usePreferenceStore().setPreferences()` (existing store method)"
    - "PersonaService.getActiveProfile() consumes updated preferences (already handles applyOverrides flow)"
    - "MemoryPage reads from `UserMemoryStore` and `ConversationMemoryStore` (existing stores)"
    - "Memory confirm/reject/delete calls UserMemoryStore methods added in Plan 03 (Task 2)"

---

<objective>
Build two Options page sections: the Persona editor (AI name, tone, response brevity — RICH-R-04) and the Memory visibility browser (user facts, conversation summaries, preferences — D-31/D-34). Wire both into the existing Options navigation system.

**Purpose:** These are the user-facing control surfaces for persona customization and memory transparency. Both read from and write to existing data stores (PreferenceMemoryStore, UserMemoryStore) updated in Wave 1.

**Output:** Two new Options page components, two modified Options files with navigation updates.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-rich-design-polish/07.5-CONTEXT.md
@.planning/phases/07.5-rich-design-polish/07.5-RESEARCH.md
@.planning/phases/07.5-rich-design-polish/07.5-UI-SPEC.md

@src/components/options/OptionsRoot.tsx
@src/components/options/AdvancedSection.tsx
@src/core/memory/PreferenceMemoryStore.ts
@src/core/memory/UserMemoryStore.ts
@src/core/ai/persona/PersonaService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PersonaPage + OptionsRoot Navigation (RICH-R-04 UI, D-08, D-09, D-10, D-11, D-12)</name>
  <files>
    src/components/options/PersonaPage.tsx
    src/components/options/OptionsRoot.tsx
    tests/components/options/PersonaPage.test.tsx
    tests/shell/optionsShell.test.tsx
  </files>
  <action>
    **Step A — Create `PersonaPage.tsx` (D-08, D-11):**
    New Options section component. Three antd `Card` components in a `Flex` vertical layout (gap: `paddingMD`):

    **Card 1 — AI Name (D-08):**
    - antd `Input` with placeholder "Enter a name for your AI assistant"
    - Value: `aiName` from `usePreferenceStore()`
    - Preview below input: antd `Typography.Text` (type="secondary", italic): "Hi, I'm **{name}**. How can I help you today?" (D-12: preview example)

    **Card 2 — Tone (D-09):**
    - Two antd `Radio.Group` with `optionType="button"`, `buttonStyle="solid"`:
      - "Professional" — subtitle: "Direct, efficient, task-focused"
      - "Professional + Approachable" — subtitle: "Warm and supportive while staying focused"
    - Each option includes a preview example per UI-SPEC copywriting contract:
      - Professional preview: "Got it. Here's the summary."
      - Professional + Approachable preview: "I understand. Let me help you with that — here's what I found."
    - Value: `aiTone` from `usePreferenceStore()` (typed enum from Plan 01 migration)
    - Preview renders as `Typography.Paragraph` (type="secondary", italic, fontSize 12)

    **Card 3 — Response Brevity (D-10):**
    - antd `Segmented` (block, size="middle") with three options:
      - "Concise" — "Short, essential answers only"
      - "Balanced" — "Complete but not verbose (default)"
      - "Detailed" — "Thorough with examples and context"
    - Value: `responseBrevity` from `usePreferenceStore()` (now includes 'balanced')
    - Preview per D-12: show sample response text based on selected brevity (use UI-SPEC copywriting: concise/balanced/detailed preview texts)
    - "Balanced" is default per D-10 — show "(default)" badge on the balanced option

    **Save:** "Save Changes" antd `Button` (type="primary") at bottom of page. On click, calls `usePreferenceStore().setPreferences({ aiName, aiTone, responseBrevity })`. Show antd `message.success('Persona settings saved')`.
    - Changes applied through existing `PersonaService.getActiveProfile()` — no additional wiring needed (Plan 01 already added `'balanced'` case).

    **Step B — Modify `OptionsRoot.tsx` (D-11):**
    Add `'persona'` entry to `OPTIONS_SECTIONS` array (currently at L31-41, between 'notes' and 'addons'):
    ```
    { id: 'persona', title: 'Persona', description: 'AI identity, name, and communication style',
      icon: <RobotOutlined /> }
    ```
    Import `PersonaPage` and add route: `case 'persona': return <PersonaPage />;`

    **Step C — Tests:**
    - PersonaPage: renders 3 cards, inputs reflect store values, save dispatches setPreferences, preview text updates with selection changes, tone radio changes aiTone, brevity segmented changes responseBrevity
    - OptionsRoot: persona entry present in sections array, navigation to persona renders PersonaPage
  </action>
  <verify>
    <automated>npx vitest run tests/components/options/PersonaPage.test.tsx tests/shell/optionsShell.test.tsx</automated>
  </verify>
  <done>
    - Options → Persona page renders with 3 setting cards, preview examples, and save button
    - AI Name, Tone, and Brevity settings persist to PreferenceMemoryStore
    - Persona nav entry appears in Options sidebar between Notes and Addons
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: MemoryPage + AdvancedSection Integration (D-31, D-32, D-33, D-34)</name>
  <files>
    src/components/options/MemoryPage.tsx
    src/components/options/AdvancedSection.tsx
    tests/components/options/MemoryPage.test.tsx
  </files>
  <action>
    **Step A — Create `MemoryPage.tsx` (D-31):**
    Options → Advanced → Memory section with three tabs:

    **Tab 1 — User Memory (D-32):**
    - Fetch all facts from `userMemoryStore.getAllFacts()` (or `search('')`)
    - Filter bar: antd `Select` with options: "All", "High Confidence" (>0.7), "User Confirmed" (status='confirmed'), "Inferred" (status='inferred')
    - Fact list: antd `List` or `Card` list. Each fact card shows:
      - Content text (antd `Typography.Paragraph`, ellipsis expandable for long facts)
      - Confidence badge: 🟢/🟡/🔴 dot + label ("High"/"Medium"/"Low") per D-32
      - Status tag: "Confirmed" (colorSuccess), "Inferred" (colorDefault), "Rejected" (colorError)
      - Source and timestamp (antd `Typography.Text` type="secondary", fontSize 12)
      - Actions: `Button` "Confirm" (only if status !== 'confirmed'), `Button` "Reject" (only if status !== 'rejected'), `Button` "Delete" (danger type)
    - Confirm/Reject/Delete use antd `Popconfirm` with confirmation messages per UI-SPEC:
      - Confirm: "Boost this fact's confidence and mark as verified?"
      - Reject: "Reject this fact? It will no longer be used."
      - Delete: "Delete this memory fact? This cannot be undone."
    - On confirm → call `userMemoryStore.confirm(factId)` (Plan 03)
    - On reject → call `userMemoryStore.reject(factId)` (Plan 03)
    - On delete → call `userMemoryStore.delete(factId)` (Plan 03)
    - Empty state: "No memory facts yet. Facts are automatically extracted as you use NowPilot." (UI-SPEC copy)

    **Tab 2 — Conversation Memory (D-34):**
    - Fetch conversation summaries from `conversationMemoryStore` (existing store)
    - Each summary: antd `Card` (size="small") showing:
      - Title/topic (bold), key outcome (secondary text), date range
      - Expand toggle → full summary text (antd `Typography.Paragraph` with `ellipsis`)
      - Delete button per-summary; "Clear All" button at top (with Popconfirm per UI-SPEC)
    - Empty state: "No conversation summaries yet. Summaries are generated when conversations grow long enough."

    **Tab 3 — Preferences (D-31 read-only):**
    - Read-only display of current preferences from `usePreferenceStore()`
    - Fields: responseStyle, preferredLanguage, preferStructuredOutput, allowCloudFallbackFromLocal, defaultProviderId, toolAutonomy, displayName, aiName, aiTone, responseBrevity, themeMode, defaultSurface
    - Each field: label + value, with links: "Change in Persona settings" → navigates to persona section, "Change in General settings" → navigates to general section
    - Read-only note at top: antd `Alert` type="info": "Preferences are managed in their respective settings pages."

    **Step B — Modify `AdvancedSection.tsx` (D-31):**
    Add a "Memory" subsection or tab to the existing Advanced section. Import and render `<MemoryPage />` as a tab or nested section within Advanced.

    **Step C — Tests:**
    - MemoryPage: renders 3 tabs, User Memory tab shows facts with confidence badges, filter dropdown filters correctly, confirm/reject/delete actions trigger with Popconfirm, Conversation Memory tab shows summaries with expand, Preferences tab shows read-only values with links
  </action>
  <verify>
    <automated>npx vitest run tests/components/options/MemoryPage.test.tsx</automated>
  </verify>
  <done>
    - Options → Advanced → Memory renders with 3 tabs
    - User Memory facts display with confidence indicators and filter controls
    - Confirm/Reject/Delete actions work with confirmation dialogs
    - Conversation Memory summaries expandable and deletable
    - Preferences tab shows read-only values with settings navigation links
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserMemoryStore → MemoryPage | Fact content rendered in React — auto-escaped; no dangerous HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.5-12 | Tampering | MemoryPage delete all | medium | mitigate | Popconfirm dialog with clear warning before Clear All; per-fact delete also uses Popconfirm |
| T-07.5-13 | Repudiation | PersonaPage save | low | accept | Preference changes are non-destructive and revertible; no audit trail needed for user-controlled settings |
</threat_model>

<verification>
- `npx vitest run tests/components/options/PersonaPage.test.tsx tests/components/options/MemoryPage.test.tsx tests/shell/optionsShell.test.tsx` — all pass
- Manual: navigate to Options → Persona, change settings, verify applied in next chat message
- Manual: navigate to Options → Advanced → Memory, verify facts visible with confidence indicators
</verification>

<success_criteria>
- Persona editor saves AI name, tone, and brevity preferences; applied via PersonaService
- Memory browser shows facts with confidence badges, filtering, and confirm/reject/delete actions
- Both sections accessible from Options navigation
</success_criteria>

<output>
Create `.planning/phases/07.5-rich-design-polish/07.5-06-SUMMARY.md` when done
</output>
