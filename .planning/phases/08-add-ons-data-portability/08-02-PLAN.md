---
phase: 08-add-ons-data-portability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/addons/write/skills/writeSkills.ts
  - src/addons/write/components/WritePage.tsx
  - src/addons/write/registerWriteAddon.ts
  - tests/addons/write/writeSkills.test.ts
autonomous: true
requirements:
  - ADDON-06
  - ADDON-07

must_haves:
  truths:
    - "Six Write skills registered as PromptManager templates with displayCategory: 'Writing' (per D-07)"
    - "WritePage renders 6 quick-action buttons in a vertical Card layout in Side Panel"
    - "Clicking a skill button populates the Sender with the corresponding prompt template text"
    - "Write add-on registers its Side Panel page via sidepanelPageRegistry (Side Panel only per D-09)"
    - "WriteIcon from src/components/sider/icons.tsx used as nav icon"
  artifacts:
    - path: "src/addons/write/skills/writeSkills.ts"
      provides: "6 prompt template configs (Rewrite, Summarize, Draft Customer Update, Draft Internal Note, Explain, Action Plan)"
      exports: ["writeSkillTemplates", "registerWriteTemplates"]
    - path: "src/addons/write/components/WritePage.tsx"
      provides: "Side Panel page with vertical stack of 6 action buttons"
      exports: ["WritePage"]
      min_lines: 60
    - path: "src/addons/write/registerWriteAddon.ts"
      provides: "Registration function called from main.tsx at import time"
      exports: ["registerWriteAddon"]
    - path: "tests/addons/write/writeSkills.test.ts"
      provides: "Verifies 6 templates have expected shape"
      min_tests: 6
  key_links:
    - from: "src/addons/write/registerWriteAddon.ts"
      to: "src/core/registries/SidepanelPageRegistry.ts"
      via: "sidepanelPageRegistry.register({ id: 'write', ... })"
    - from: "src/addons/write/skills/writeSkills.ts"
      to: "src/core/prompts/PromptManager.ts"
      via: "promptManager.createTemplate() — 6 PromptTemplate registrations"
    - from: "src/addons/write/components/WritePage.tsx"
      to: "useWorkspaceStore"
      via: "Populates Sender via setDraft or direct workspace action"
---

<objective>
Build the Write add-on with 6 skill prompt templates and a Side Panel quick-action page. Skills are PromptManager templates (not Agent tools) per D-07/D-08. Each skill button populates the Sender with the structured prompt. Side Panel only — no Full App page (D-09). The existing WriteIcon is reused from icons.tsx.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-add-ons-data-portability/08-CONTEXT.md (D-07, D-08, D-09 — locked decisions)
@.planning/phases/08-add-ons-data-portability/08-PATTERNS.md (WritePage analog: ImportExportSection, PromptManager PromptTemplate type)
@.planning/phases/08-add-ons-data-portability/08-UI-SPEC.md (Write add-on interaction patterns, copywriting)
@.planning/phases/08-add-ons-data-portability/08-RESEARCH.md (Write add-on architecture)
@src/core/prompts/PromptManager.ts (PromptTemplate type: id, name, description, template, category, variables, isBuiltin, scopes, hidden, icon, order)
@src/core/registries/SidepanelPageRegistry.ts (PageDefinition: id, label, icon?, component, order?)
@src/components/sider/icons.tsx (WriteIcon already built, mapped as write: WriteIcon)
@src/components/options/ImportExportSection.tsx (Card-based layout analog)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Write skill prompt templates</name>
  <files>src/addons/write/skills/writeSkills.ts, tests/addons/write/writeSkills.test.ts</files>
  <behavior>
    - Test 1: writeSkillTemplates array has exactly 6 entries
    - Test 2: Each template has required fields: id, name, description, template, category, variables, isBuiltin: false
    - Test 3: All templates have category: 'Writing'
    - Test 4: Each template has at least one variable in `variables` array
    - Test 5: template strings are non-empty and contain the variable placeholders
    - Test 6: registerWriteTemplates() calls promptManager.createTemplate() for each skill
  </behavior>
  <action>
    Create `src/addons/write/skills/writeSkills.ts`. Export an array of 6 `PromptTemplate` configs (per D-07):

    | Skill | ID | Variables | Template Summary |
    |-------|-----|-----------|------------------|
    | Rewrite | `write-rewrite` | `{{content}}`, `{{style}}` | "Rewrite the following text in a {{style}} style:\n\n{{content}}" |
    | Summarize | `write-summarize` | `{{content}}`, `{{length}}` | "Summarize the following content in {{length}}:\n\n{{content}}" |
    | Draft Customer Update | `write-customer-update` | `{{case}}`, `{{status}}` | "Draft a customer update for case {{case}} with current status: {{status}}" |
    | Draft Internal Note | `write-internal-note` | `{{case}}`, `{{findings}}` | "Draft an internal work note for case {{case}} based on findings: {{findings}}" |
    | Explain | `write-explain` | `{{content}}`, `{{audience}}` | "Explain the following to a {{audience}} audience:\n\n{{content}}" |
    | Action Plan | `write-action-plan` | `{{goal}}`, `{{constraints}}` | "Create a step-by-step action plan to achieve: {{goal}}. Constraints: {{constraints}}" |

    Each template:
    - `category: 'Writing'` (displayCategory)
    - `isBuiltin: false`
    - `hidden: false`
    - `scopes: ['writing']` — scopes per PromptTemplate type

    Export `registerWriteTemplates()` function:
    - Iterates writeSkillTemplates
    - Calls `promptManager.createTemplate(template)` for each
    - Wraps in try/catch — if template already exists (idempotent), skip silently
    - Import promptManager from `../../../core/prompts/PromptManager`

    Create `tests/addons/write/writeSkills.test.ts`. Mock `promptManager.createTemplate` with vi.hoisted(). Assert:
    - 6 templates defined with correct ids
    - required fields present on each
    - category is 'Writing' for all
    - registerWriteTemplates calls createTemplate 6 times
    - RED phase: tests fail before module is created.
  </action>
  <verify>
    <automated>pnpm vitest tests/addons/write/writeSkills.test.ts --run</automated>
  </verify>
  <done>6 Write skill prompt templates defined. registerWriteTemplates() registers them with PromptManager. All 6 unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create WritePage Side Panel component + registration function</name>
  <files>src/addons/write/components/WritePage.tsx, src/addons/write/registerWriteAddon.ts</files>
  <action>
    **WritePage component** (`src/addons/write/components/WritePage.tsx`):

    Follow the ImportExportSection Card-based layout pattern (PATTERNS.md). Use Ant Design primitives only — no custom CSS.

    ```
    <div data-options-section="write" style={{ maxWidth: 720, padding: 16 }}>
      <Typography.Title level={4}>Write</Typography.Title>
      <Typography.Paragraph type="secondary">
        Select a writing action to populate the prompt.
      </Typography.Paragraph>

      <Card>
        {writeSkillTemplates.map((skill) => (
          <Button
            key={skill.id}
            block
            style={{ marginBottom: 8 }}
            onClick={() => handleSkillClick(skill)}
          >
            {skill.name}
          </Button>
        ))}
      </Card>
    </div>
    ```

    **handleSkillClick:**
    - Read the template from promptManager (or use the imported template config)
    - On click, the skill's template text should be loaded into the chat Sender
    - Since the Sender is owned by ChatPage (not WritePage directly), use workspaceStore to communicate:
      - `useWorkspaceStore.getState().setDraft('write', skill.template)` — store draft with key 'write'
      - Navigate to Chat page (via workspaceRouter or activeSurface change)
    - Per UI-SPEC: "User clicks a skill button → Sender is populated with prompt template → user must press Send to execute"

    **Imports:** Typography.Title, Typography.Paragraph, Card, Button from antd. writeSkillTemplates from '../skills/writeSkills'. useWorkspaceStore from workspaceStore. WriteIcon from icons for potential use.

    **Copywriting** (per UI-SPEC):
    - Page heading: "Write" (h4)
    - Page description: "Select a writing action to populate the prompt."
    - Button labels: "Rewrite", "Summarize", "Draft Customer Update", "Draft Internal Note", "Explain", "Action Plan"

    ---

    **registerWriteAddon function** (`src/addons/write/registerWriteAddon.ts`):

    Follow `registerNowPilotCorePages.ts` blueprint pattern (PATTERNS.md):

    ```typescript
    import { WriteIcon } from '../../components/sider/icons';
    import { WritePage } from './components/WritePage';
    import { sidepanelPageRegistry } from '../../core/registries/SidepanelPageRegistry';
    import { registerWriteTemplates } from './skills/writeSkills';

    export function registerWriteAddon(): void {
      // Register prompt templates
      registerWriteTemplates();

      // Register Side Panel page (Side Panel only per D-09)
      sidepanelPageRegistry.register({
        id: 'write',
        label: 'Write',
        icon: WriteIcon,
        component: WritePage,
        order: 10,
      });
    }
    ```

    Follow existing registerCorePages.ts pattern: import sidepanelPageRegistry singleton, call `.register()` with PageDefinition shape. Order 10 places it after core pages (1-4) but before other addons. Side Panel only — no standalonePageRegistry registration per D-09.
  </action>
  <verify>
    <automated>pnpm vitest related --run</automated>
  </verify>
  <done>WritePage renders 6 action buttons in a vertical Card. Clicking populates the sender via workspaceStore draft. registerWriteAddon registers page with sidepanelPageRegistry and templates with PromptManager. Side Panel only — no Full App page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Write addon → PromptManager | Write templates registered via promptManager.createTemplate() — templates are user-facing, no sensitive data |
| WritePage → workspaceStore | Draft populated via setDraft — draft content is user-visible, no threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Information Disclosure | WritePage draft | low | accept | Draft content is user-authored text — no credentials or session data. Accept. |
| T-08-05 | Spoofing | PromptTemplate injection | low | accept | Templates are hardcoded in source — no user-supplied template injection path. Accept. |
</threat_model>

<verification>
pnpm vitest tests/addons/write/writeSkills.test.ts passes with 6 tests. WritePage component renders with 6 action buttons. registerWriteAddon registers sidepanel page and prompt templates.
</verification>

<success_criteria>
Write add-on operational: 6 prompt templates registered with PromptManager under category 'Writing', WritePage renders in Side Panel with quick-action buttons, clicking a skill populates the Sender, sidepanelPageRegistry entry created with WriteIcon at order 10. No Full App page per D-09.
</success_criteria>

<output>
Create `.planning/phases/08-add-ons-data-portability/08-02-SUMMARY.md` when done
</output>
