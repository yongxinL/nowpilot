---
phase: 07.4-rich-design-enhance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/stores/RightPaneStore.ts
  - src/components/standalone/RightPane.tsx
  - src/components/standalone/PaneToggle.tsx
  - src/components/standalone/RightPaneTabs/ContextTab.tsx
  - src/components/standalone/RightPaneTabs/NotesTab.tsx
  - src/components/standalone/RightPaneTabs/ToolsTab.tsx
autonomous: true
requirements:
  - RICH-H-11
  - RICH-H-12

must_haves:
  truths:
    - "RightPaneStore persists activeTab/visible/width to np_right_pane key; transient searchQuery/selectedNoteId NOT persisted"
    - "RightPane renders three antd Tabs (Context, Notes, Tools) with correct content sourced from existing domain stores"
    - "PaneToggle cycles between compact (320px), expanded (45%), and collapsed (tab strip) on click per D-03"
    - "ContextTab reads currentPageContext + pinnedTabs from workspaceStore via selectors"
    - "NotesTab supports MiniSearch query + inline note preview (no full editor per D-05)"
    - "ToolsTab shows MCP tool status with expandable capability details"
  artifacts:
    - src/core/stores/RightPaneStore.ts
    - src/components/standalone/RightPane.tsx
    - src/components/standalone/PaneToggle.tsx
    - src/components/standalone/RightPaneTabs/ContextTab.tsx
    - src/components/standalone/RightPaneTabs/NotesTab.tsx
    - src/components/standalone/RightPaneTabs/ToolsTab.tsx
  key_links:
    - "RightPaneStore.partialize whitelist prevents transient state persistence (Pitfall 1)"
    - "StandaloneRoot (Plan 06) consumes RightPane + useRightPaneStore"
---

<objective>
Create the right pane system for the standalone split-pane layout: Zustand store with selective persistence, collapsible right pane container with three tabs (Context — pinned tabs, Notes — MiniSearch preview, Tools — MCP status), and a pane toggle button cycling between compact/expanded/collapsed states.

Purpose: Provide the auxiliary workspace surface (40% width) that Phase 7.3's standalone surface lacked, per RICH-H-11 and RICH-H-12 specification.

Output: 6 new files — RightPaneStore (Zustand), RightPane (container), PaneToggle (divider button), ContextTab, NotesTab, ToolsTab
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS_RICH_DESIGN.md
@.planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md
@.planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md
@.planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md
@.planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md
@src/core/stores/diagnosticsStore.ts
@src/core/stores/workspaceStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RightPaneStore with persist + partialize + transient state</name>
  <files>src/core/stores/RightPaneStore.ts</files>
  <read_first>
    - src/core/stores/diagnosticsStore.ts — persist + partialize + chrome.storage.local adapter pattern (lines 35-127)
    - src/core/stores/workspaceStore.ts — chrome.storage.local adapter (lines 41-74), np_ key prefix convention
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 41-97 — RightPaneStore exact pattern with partialize whitelist
  </read_first>
  <behavior>
    - Test 1: default state: activeTab='context', visible=true, width='compact'
    - Test 2: setActiveTab('notes') changes activeTab; setVisible(false) changes visible; toggleWidth cycles compact→expanded→compact
    - Test 3: persisted np_right_pane key contains only activeTab, visible, width — NOT searchQuery, selectedNoteId, expandedToolId
    - Test 4: reload → persisted fields restored; transient fields reset to defaults
    - Test 5: setSearchQuery persists in-memory only; lost on reload
  </behavior>
  <action>
    Create RightPaneStore following exact diagnosticsStore.ts pattern:
    - Interface RightPaneState: persisted (activeTab: 'context'|'notes'|'tools', visible: boolean, width: 'compact'|'expanded') + transient (searchQuery: string, selectedNoteId: string|null, expandedToolId: string|null)
    - Actions: setActiveTab, setVisible, toggleWidth (compact→expanded, expanded→compact), setSearchQuery, setSelectedNoteId, setExpandedToolId
    - Zustand create + persist with name: 'np_right_pane', storage: chromeLocalStorage adapter
    - partialize whitelist: only { activeTab, visible, width } — per Pitfall 1 prevention
    - Export useRightPaneStore hook
  </action>
  <verify>
    <automated>pnpm test tests/core/stores/RightPaneStore.test.ts</automated>
  </verify>
  <done>RightPaneStore persisted to chrome.storage.local under np_right_pane; 3 UI fields persisted, 3 transient fields not persisted; toggleWidth cycles compact→expanded→compact; all actions functional</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RightPane container, PaneToggle, and three tab components</name>
  <files>src/components/standalone/RightPane.tsx, src/components/standalone/PaneToggle.tsx, src/components/standalone/RightPaneTabs/ContextTab.tsx, src/components/standalone/RightPaneTabs/NotesTab.tsx, src/components/standalone/RightPaneTabs/ToolsTab.tsx</files>
  <read_first>
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 700-722 — RightPane layout pattern
    - .planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md — Empty states for each tab, typography contract (14px base, 16px heading), color contract
    - src/components/sidepanel/PinTabBar.tsx — pinned tabs display pattern (role-match for ContextTab)
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-01 through D-08 — all locked right-pane decisions
    - .planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md — anti-patterns (no domain data in RightPaneStore, no auto-show/hide)
  </read_first>
  <behavior>
    - Test 1: RightPane renders antd Tabs with Context/Notes/Tools tab labels
    - Test 2: ContextTab renders page title + URL from currentPageContext; renders pinnedTabs list from workspaceStore
    - Test 3: ContextTab empty state shows "No page context" + pin hint per UI-SPEC empty states
    - Test 4: NotesTab renders Input.Search + MiniSearch results; clicking a result shows inline preview (title + summary, no full editor)
    - Test 5: NotesTab empty state: "No notes yet" + "Create a note from any chat message" per UI-SPEC
    - Test 6: ToolsTab renders tool list with connected/disconnected status badges; expandable detail per tool
    - Test 7: ToolsTab empty state: "No tools configured" + "Connect MCP servers in Options" per UI-SPEC
    - Test 8: PaneToggle cycles width on click: compact→expanded→collapsed; collapsed shows narrow tab strip
    - Test 9: PaneToggle title attribute reflects current state ("Expand right pane" / "Collapse right pane")
  </behavior>
  <action>
    Per D-01 through D-08 decisions:

    **PaneToggle.tsx** — Narrow vertical divider strip with toggle icon button. On click: cycles between compact (320px), expanded (45% of available width), and collapsed (hidden + narrow tab strip visible). Use antd Button type="text" with MenuFoldOutlined/MenuUnfoldOutlined. Title attribute: "Expand right pane" or "Collapse right pane" per UI-SPEC accessibility.

    **RightPane.tsx** — Flex column container with left border divider (token.colorBorderSecondary). Contains antd Tabs with three tab panes: Context (ReadOutlined), Notes (FileTextOutlined), Tools (ToolOutlined). Width controlled by pane width state: compact=320, expanded=45%, collapsed=0 (hidden). When collapsed, render narrow tab strip (vertical Tabs with icon-only tabs) as re-expand affordance per D-02. Tab selection persisted via RightPaneStore setActiveTab.

    **ContextTab.tsx** — Read from workspaceStore: currentPageContext (page title + URL) and pinnedTabs. Display current page as compact card with favicon + title + URL. Pinned tabs list below. Empty state: "No page context" heading + "Pin a tab using the pin icon in the Side Panel to see context here" per UI-SPEC.

    **NotesTab.tsx** — Input.Search (SearchOutlined prefix) above results list. Use MiniSearch from notes system for search. Clicking a result shows inline preview (note title + summary excerpt, not full editor per D-05). Auto-switch to NotesTab when a wikilinked note is opened from chat (D-06). Empty state: "No notes yet" + "Create a note from any chat message using 'Save to note'" per UI-SPEC.

    **ToolsTab.tsx** — Read MCP configuration from chrome.storage or existing MCP config. Display tool list with status indicators (Badge: green="connected", red="disconnected"). Each tool expandable via Collapse/Accordion to show capabilities, input schemas. Empty state: "No tools configured" + "Connect MCP servers in Options to see tools here" per UI-SPEC.

    All tabs use antd theme.useToken() for styling — follow typography contract (14px body, 16px headings, 2 weights: 400/600). No 12px/13px/11px fonts.
  </action>
  <verify>
    <automated>pnpm test tests/components/standalone/RightPane.test.tsx</automated>
  </verify>
  <done>RightPane renders 3 tabs with correct labels/icons; ContextTab shows page context + pinned tabs or empty state; NotesTab supports search + inline preview; ToolsTab shows MCP status + expandable details; PaneToggle cycles 3 states correctly; collapsed state shows tab strip; all empty states match UI-SPEC copywriting</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RightPaneStore → chrome.storage.local | Persisted UI layout state (activeTab, visible, width); transient fields excluded via partialize |
| NotesTab → NotesDB/MiniSearch | Read-only queries over local IndexedDB; no modification capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.4-03 | Tampering | RightPaneStore persisted state | low | accept | partialize whitelist prevents transient-state injection into persisted fields; chrome.storage.local only accessible by extension |
</threat_model>

<verification>
## Wave 0 Requirements
- tests/core/stores/RightPaneStore.test.ts — persist/partialize behavior, toggleWidth cycle, transient state non-persistence
- tests/components/standalone/RightPane.test.tsx — tab rendering, content sourcing, empty states, PaneToggle behavior
</verification>

<success_criteria>
- [ ] RightPaneStore persists 3 UI fields via partialize whitelist; 3 transient fields reset on reload
- [ ] RightPane renders 3 tabs with correct content sourcing from domain stores
- [ ] PaneToggle cycles compact→expanded→collapsed with correct aria labels
- [ ] All tab empty states match UI-SPEC copywriting contract
- [ ] All styling uses antd tokens per typography/color/spacing contracts
</success_criteria>

<output>
Create `.planning/phases/07.4-rich-design-enhance/07.4-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `useRightPaneStore` | hook | RightPaneStore.ts | Zustand store hook for right pane UI state |
| `RightPaneState` | interface | RightPaneStore.ts | Type for persisted+transient pane state |
| `RightPane` | component | RightPane.tsx | Container with antd Tabs for 3 tab panels |
| `PaneToggle` | component | PaneToggle.tsx | Divider button cycling compact/expanded/collapsed |
| `ContextTab` | component | ContextTab.tsx | Displays currentPageContext + pinnedTabs |
| `NotesTab` | component | NotesTab.tsx | MiniSearch input + inline note preview |
| `ToolsTab` | component | ToolsTab.tsx | MCP tool status + expandable capability details |
