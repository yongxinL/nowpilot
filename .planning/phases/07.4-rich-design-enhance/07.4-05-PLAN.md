---
phase: 07.4-rich-design-enhance
plan: 05
type: execute
wave: 3
depends_on:
  - 07.4-01
  - 07.4-03
  - 07.4-04
files_modified:
  - src/core/onboarding/OnboardingModal.tsx
  - src/core/pages/AgentPage.tsx
  - src/components/chat/BrandedHeader.tsx
  - src/components/chat/ChatMessage.tsx
autonomous: true
requirements:
  - RICH-R-03
  - RICH-H-03
  - RICH-H-02
  - RICH-H-06
  - RICH-I-03

must_haves:
  truths:
    - "OnboardingModal Step 0 replaced with MeetNowPilotStep; total steps increase from 8→9; step circles updated from [1..8] to [1..9]"
    - "AgentPage ThoughtChain empty-state text shows 'NowPilot is working…' per D-29"
    - "BrandedHeader accepts userGreeting and contextualMessage props; renders dynamic greeting text when provided"
    - "ChatMessage shows BunnyAvatar + branded header on first assistant message only (D-28)"
    - "ChatMessage Save-to-note button always visible in Bubble footer (not hidden behind dropdown) per RICH-H-06"
  artifacts:
    - "Modified OnboardingModal.tsx — MeetNowPilotStep integration, 9-step circles"
    - "Modified AgentPage.tsx — ThoughtChain text change"
    - "Modified BrandedHeader.tsx — dynamic greeting props"
    - "Modified ChatMessage.tsx — first-message branding + visible Save-to-note"
  key_links:
    - "MeetNowPilotStep (Plan 04) → OnboardingModal step 0"
    - "useGreeting (Plan 03) → BrandedHeader greeting (Plan 07 ChatPage)"
---

<objective>
Integrate Wave 1 components into existing UI surfaces: replace OnboardingModal Step 0 (Welcome) with MeetNowPilotStep and adjust step count 8→9, change AgentPage ThoughtChain text to branded "NowPilot is working…", extend BrandedHeader with dynamic greeting props, and enhance ChatMessage with first-message branding and promoted Save-to-note button.

Purpose: Wire all persona-onboarding and brand-identity changes into their host pages and components.

Output: 4 files modified — OnboardingModal.tsx, AgentPage.tsx, BrandedHeader.tsx, ChatMessage.tsx
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md
@.planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md
@.planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md
@src/core/onboarding/OnboardingModal.tsx
@src/core/pages/AgentPage.tsx
@src/components/chat/BrandedHeader.tsx
@src/components/chat/ChatMessage.tsx
@src/components/onboarding/MeetNowPilotStep.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate MeetNowPilotStep into OnboardingModal Step 0 + adjust step count</name>
  <files>src/core/onboarding/OnboardingModal.tsx</files>
  <read_first>
    - src/core/onboarding/OnboardingModal.tsx — step 0 Welcome content (lines 395-429), step circles (lines 321-391, [1..8]), step content area (line 394), CTA logic
    - src/components/onboarding/MeetNowPilotStep.tsx — component created in Plan 04
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 1315-1338 — OnboardingModal step integration pattern
  </read_first>
  <behavior>
    - Test 1: step===0 renders MeetNowPilotStep instead of old Welcome content
    - Test 2: MeetNowPilotStep Continue calls setStep(1) (moves to Provider step)
    - Test 3: MeetNowPilotStep Skip calls handleComplete (completes onboarding)
    - Test 4: Step circles render 9 circles [1..9] instead of 8 [1..8]
    - Test 5: Existing steps 1-8 (Provider through Done) remain unchanged
    - Test 6: Step 0 is the first/initial step (initial step state is 0)
  </behavior>
  <action>
    Per D-09, D-10, D-12:

    **OnboardingModal.tsx** modifications:
    - Import MeetNowPilotStep from ../../components/onboarding/MeetNowPilotStep
    - Replace step===0 content block (lines 395-429): render MeetNowPilotStep with onContinue={() => setStep(1)} and onSkip={handleComplete}
    - Update step circle array: change [1,2,3,4,5,6,7,8] to [1,2,3,4,5,6,7,8,9] (line 322)
    - Update isActive condition: num === step + 1 still works correctly (step 0→circle 1 active)
    - All existing step===1 through step===7 content blocks remain UNCHANGED per D-12
    - No changes to continue/back navigation logic — existing flow handles new step count
    - Existing step labels array may need a 9th entry; if so, add "Meet NowPilot" as step 0 label
  </action>
  <verify>
    <automated>pnpm test tests/components/onboarding/OnboardingModal.test.tsx</automated>
  </verify>
  <done>OnboardingModal Step 0 shows MeetNowPilotStep; Continue advances to Provider step (1); Skip completes onboarding; 9 step circles rendered; Steps 1-8 unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update BrandedHeader with dynamic greeting props + AgentPage ThoughtChain text</name>
  <files>src/components/chat/BrandedHeader.tsx, src/core/pages/AgentPage.tsx</files>
  <read_first>
    - src/components/chat/BrandedHeader.tsx — current component (63 lines, static "NowPilot" name + "Your AI work co-pilot" tagline)
    - src/core/pages/AgentPage.tsx — current ThoughtChain integration
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 1046-1093 — BrandedHeader extension pattern, AgentPage lines 808-838
    - .planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md — typography contract (16px name→20px when displayName)
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-23, D-29
  </read_first>
  <behavior>
    - Test 1: BrandedHeader renders "NowPilot" when userGreeting prop not provided (backward compat)
    - Test 2: BrandedHeader renders userGreeting as name when provided (replaces "NowPilot" text)
    - Test 3: BrandedHeader renders contextualMessage as tagline when provided (replaces "Your AI work co-pilot")
    - Test 4: AgentPage ThoughtChain empty state shows "NowPilot is working…" text
    - Test 5: BrandedHeader fontSize changes to 20px when userGreeting provided (Display role per UI-SPEC)
  </behavior>
  <action>
    Per D-23, D-28, D-29:

    **BrandedHeader.tsx** — Extend:
    - Add optional props: userGreeting?: string, contextualMessage?: string to BrandedHeaderProps
    - Name text (line 43-44): render userGreeting || 'NowPilot'. When userGreeting is set, use fontSize: 20 (Display role per UI-SPEC typography contract), otherwise keep 16px heading
    - Tagline text (line 46-47): render contextualMessage || 'Your AI work co-pilot'. Keep fontSize 12 for secondary text
    - Update Text strong fontSize dynamically based on userGreeting presence
    - All existing styling (Flex, gap, padding, border, avatar, close button) unchanged

    **AgentPage.tsx** — Per D-29:
    - Find ThoughtChain empty-state text (likely "Send a message to start an agent workflow" or "Agent is processing…" in ThoughtChainView component)
    - Replace with "NowPilot is working…" per UI-SPEC
    - If ThoughtChainView.tsx has the text (PATTERNS.md line 837), change there; if AgentPage.tsx wrapper, change there
    - Minimal change — string replacement only
  </action>
  <verify>
    <automated>pnpm test tests/components/chat/BrandedHeader.test.tsx tests/core/pages/AgentPage.test.tsx</automated>
  </verify>
  <done>BrandedHeader renders dynamic greeting when props provided; backward-compatible when props absent; AgentPage shows "NowPilot is working…" text</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enhance ChatMessage with first-message branding + promoted Save-to-note button</name>
  <files>src/components/chat/ChatMessage.tsx</files>
  <read_first>
    - src/components/chat/ChatMessage.tsx — current component (52 lines, Bubble-based, optional footer with Save-to-note)
    - src/components/common/BunnyAvatar.tsx — bunny avatar component
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 1097-1156 — ChatMessage extension pattern
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-28 (first-message branding), RICH-H-06 (Save-to-note promotion)
    - .planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md — brand indicator spec, Bubble footer modifications
  </read_first>
  <behavior>
    - Test 1: When isFirstMessage=true and role='assistant', Bubble shows BunnyAvatar + branded header "NowPilot"
    - Test 2: When isFirstMessage=false, Bubble shows no avatar/header (standard rendering)
    - Test 3: Save-to-note button always visible in Bubble footer when onSaveToNote provided (not conditional on dropdown)
    - Test 4: Save-to-note button hidden during streaming
    - Test 5: StructuredOutputActions rendered in footer when content has markdown table
    - Test 6: User messages unaffected by isFirstMessage flag
  </behavior>
  <action>
    Per D-28 and RICH-H-06:

    **ChatMessage.tsx** — Extend ChatMessageProps:
    - Add isFirstMessage?: boolean (D-28)
    - Add showSaveToNote?: boolean (RICH-H-06 — controls visibility promotion)

    Bubble modifications:
    - Avatar: when isFirstMessage && role === 'assistant', render avatar prop as div with 2px solid primary border (32px), BunnyAvatar inside — per PATTERNS.md lines 1121-1125
    - Header: when isFirstMessage && role === 'assistant', render header prop as div with fontWeight 600, fontSize 14, primary color — "NowPilot" text per PATTERNS.md lines 1126-1128
    - Footer: restructure to always show Save-to-note Button when onSaveToNote is provided AND role='assistant' AND !streaming — no longer hidden behind conditional — per PATTERNS.md lines 1137-1153
    - Integrate StructuredOutputActions in footer: pass content and hasTable detection (content.includes('| ---'))
    - Footer shows: Save-to-note button (if applicable) + StructuredOutputActions (if has table)
    - All existing Bubble props (placement, content, streaming) unchanged

    Import BunnyAvatar from ../common/BunnyAvatar
    Import StructuredOutputActions from ./StructuredOutputActions
  </action>
  <verify>
    <automated>pnpm test tests/components/chat/ChatMessage.test.tsx</automated>
  </verify>
  <done>ChatMessage shows BunnyAvatar + branded header on first assistant message; Save-to-note button always visible in footer; StructuredOutputActions integrated; user messages unaffected; backward-compatible when new props omitted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| userGreeting prop → BrandedHeader | Prop sourced from GreetingService (pure function); rendered via antd Typography.Text (HTML-escaped) |
| MeetNowPilotStep → OnboardingModal | Static persona data display; no user input in this step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.4-08 | Tampering | BrandedHeader.userGreeting | low | accept | userGreeting rendered via antd Typography.Text which HTML-escapes content; sourced from generateGreeting pure function |
</threat_model>

<verification>
## Wave 0 Requirements
- tests/components/onboarding/OnboardingModal.test.tsx — extend with MeetNowPilotStep rendering, 9-step circles, navigation
- tests/components/chat/BrandedHeader.test.tsx — extend with userGreeting/contextualMessage prop rendering
- tests/core/pages/AgentPage.test.tsx — extend with ThoughtChain text assertion
- tests/components/chat/ChatMessage.test.tsx — extend with first-message branding, Save-to-note visibility
</verification>

<success_criteria>
- [ ] OnboardingModal Step 0 shows MeetNowPilotStep with correct navigation
- [ ] OnboardingModal shows 9 step circles; Steps 1-8 unchanged
- [ ] BrandedHeader renders dynamic greeting when props provided; backward-compatible
- [ ] AgentPage ThoughtChain shows branded "NowPilot is working…"
- [ ] ChatMessage shows first-message branding (avatar + header) on first assistant message
- [ ] Save-to-note button always visible in Bubble footer when applicable
- [ ] StructuredOutputActions integrated in Bubble footer
</success_criteria>

<output>
Create `.planning/phases/07.4-rich-design-enhance/07.4-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `userGreeting` (prop) | BrandedHeaderProps | BrandedHeader.tsx | Dynamic greeting text replacing static "NowPilot" |
| `contextualMessage` (prop) | BrandedHeaderProps | BrandedHeader.tsx | Optional contextual line (e.g., "You're working on: …") |
| `isFirstMessage` (prop) | ChatMessageProps | ChatMessage.tsx | Triggers first-message branding (avatar + header) |
| `showSaveToNote` (prop) | ChatMessageProps | ChatMessage.tsx | Controls Save-to-note button visibility promotion |
| `MeetNowPilotStep` (import) | OnboardingModal.tsx | OnboardingModal.tsx | Step 0 replacement for old Welcome step |
