---
phase: 07.4-rich-design-enhance
plan: 03
type: execute
wave: 1
depends_on:
  - 07.4-01
files_modified:
  - src/core/prompts/builtinTemplates.ts
  - src/core/prompts/TemplateBrowserService.ts
  - src/components/chat/TemplateBrowser.tsx
  - src/core/greeting/GreetingService.ts
  - src/hooks/useGreeting.ts
autonomous: true
requirements:
  - RICH-I-09
  - RICH-I-10
  - RICH-C-14
  - RICH-I-03
  - RICH-I-02
  - RICH-I-08

must_haves:
  truths:
    - "builtinTemplates exports 21 templates with displayCategory field assigned (Writing/Analysis/Research/Coding/Support)"
    - "TemplateBrowserService groups templates by displayCategory, tracks recent usage (max 5, in-memory), supports search by name/description"
    - "TemplateBrowser popover shows categorized sections + recently-used on top; selecting template inserts into Sender"
    - "GreetingService.generateGreeting() returns time-of-day greeting with optional displayName + pageTitle contextual line"
    - "useGreeting hook subscribes to PreferenceMemoryStore.displayName + workspaceStore.currentPageContext via individual selectors"
    - "RICH-I-02 (dynamic sort) and RICH-I-08 (intent classifier) already implemented in Phase 7.3 — no new work"
  artifacts:
    - src/core/prompts/TemplateBrowserService.ts
    - src/components/chat/TemplateBrowser.tsx
    - src/core/greeting/GreetingService.ts
    - src/hooks/useGreeting.ts
  key_links:
    - "TemplateBrowserService wraps promptManager — PromptManager remains source of truth (D-15)"
    - "GreetingService → useGreeting hook → BrandedHeader (Plan 05)"
    - "TemplateBrowser → onInsert callback → Sender prefix (Plan 07)"
---

<objective>
Build the template browsing and personalized greeting subsystems: extend all 21 built-in templates with displayCategory field, create TemplateBrowserService for category grouping/search/recent tracking, create TemplateBrowser popover component for the Sender toolbar, create GreetingService pure function for time-of-day logic, and create useGreeting hook subscribing to preferences + workspace context.

Purpose: Provide the data service + UI component for RICH-I-09/I-10 (template browser) and the greeting computation for RICH-C-14/RICH-I-03 (personalized empty-state).

Output: 5 files — builtinTemplates.ts (modified, add displayCategory), TemplateBrowserService.ts (new), TemplateBrowser.tsx (new), GreetingService.ts (new), useGreeting.ts (new)
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS_RICH_DESIGN.md
@.planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md
@.planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md
@.planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md
@.planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md
@src/core/prompts/builtinTemplates.ts
@src/core/prompts/PromptManager.ts
@src/core/ai/WelcomeCardService.ts
@src/hooks/useWorkspace.ts
@src/components/chat/QuickActionChips.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add displayCategory to builtinTemplates + create TemplateBrowserService</name>
  <files>src/core/prompts/builtinTemplates.ts, src/core/prompts/TemplateBrowserService.ts</files>
  <read_first>
    - src/core/prompts/builtinTemplates.ts — 21 templates (267 lines), BuiltinPromptTemplate interface (lines 1-12), all template definitions
    - src/core/prompts/PromptManager.ts — promptManager singleton, PromptTemplate interface
    - src/core/ai/WelcomeCardService.ts — service pattern: static definitions + read-from-store
  </read_first>
  <behavior>
    - Test 1: All 21 builtin templates have displayCategory field (Writing/Analysis/Research/Coding/Support)
    - Test 2: TemplateBrowserService.getByCategory() returns templates grouped by displayCategory, sorted by order
    - Test 3: TemplateBrowserService.search('summarize') returns matching templates by name/description
    - Test 4: TemplateBrowserService.trackRecentUse(id) adds to recent list (max 5); getRecentlyUsed() returns in LIFO order
    - Test 5: getByCategory() shows recently-used section first when recent templates exist
  </behavior>
  <action>
    Per D-14 and D-15:

    **builtinTemplates.ts** — Add displayCategory: string field to BuiltinPromptTemplate interface. Assign displayCategory to all 21 templates per PATTERNS.md lines 949-957:
    - Writing (18 templates): improve-writing, continue-writing, fix-spelling-grammar, translate, make-shorter, make-longer, simplify-language, change-tone, outline, brainstorm, blog-post, paragraph-about, social-media-post, press-release, creative-story, to-do-list, meeting-agenda, sales-email
    - Analysis (2 templates): summarize, testing
    - Research (1 template): explain
    - Coding (0): leave empty for future
    - Support (0): leave empty for future
    Keep existing category field unchanged (internal logic field). displayCategory is the user-facing grouping.

    **TemplateBrowserService.ts** (NEW) — Class + singleton export pattern matching WelcomeCardService:
    - Import promptManager from ./PromptManager, type PromptTemplate
    - Private recentTemplateIds: string[] (max 5, in-memory only — not persisted per D-15)
    - getByCategory(): returns CategorizedTemplates[] — group templates by displayCategory, include recently-used section first when non-empty
    - search(query: string): PromptTemplate[] — case-insensitive fuzzy match on name and description
    - trackRecentUse(templateId: string): void — prepend to recentTemplateIds, trim to MAX_RECENT=5
    - getRecentlyUsed(): PromptTemplate[] — resolve recent IDs to full template objects via promptManager
    - Export categorizedTemplates and templateBrowserService singleton
    - Error handling via debugLog, not throws (matching WelcomeCardService pattern)
  </action>
  <verify>
    <automated>pnpm test tests/core/prompts/TemplateBrowserService.test.ts</automated>
  </verify>
  <done>All 21 templates have displayCategory assigned; TemplateBrowserService groups by category, searches, tracks recent usage (max 5); getRecentlyUsed resolves from promptManager</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GreetingService + useGreeting hook for personalized time-of-day greeting</name>
  <files>src/core/greeting/GreetingService.ts, src/hooks/useGreeting.ts</files>
  <read_first>
    - src/hooks/useWorkspace.ts — individual selector pattern for Zustand stores (lines 23-45)
    - .planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md lines 606-646 — GreetingService exact implementation
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-21, D-22, D-23, D-24 — greeting decisions
    - .planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md — greeting copywriting contract (lines 147-156)
  </read_first>
  <behavior>
    - Test 1: generateGreeting({ displayName: 'George' }) at hour=9 returns greeting "Good morning, George" with timeOfDay='morning'
    - Test 2: generateGreeting({}) at hour=14 returns "Good afternoon! How can I help?" with no name
    - Test 3: generateGreeting({ displayName: 'A', pageTitle: 'INC001' }) at hour=19 returns evening greeting + contextualLine
    - Test 4: timeOfDay: hour<12→morning, hour<17→afternoon, hour≥17→evening
    - Test 5: useGreeting re-renders only when displayName or currentPageContext changes (individual selector)
    - Test 6: contextualLine is undefined when pageTitle is absent (per D-22 optional)
  </behavior>
  <action>
    Per D-21, D-22, D-24:

    **GreetingService.ts** (NEW — pure function module, no class):
    - Export GreetingInput interface: { displayName?: string, pageTitle?: string }
    - Export Greeting interface: { greeting: string, timeOfDay: 'morning'|'afternoon'|'evening', tagline: string, contextualLine?: string }
    - Export generateGreeting(input: GreetingInput): Greeting — exact implementation from RESEARCH.md lines 624-646:
      - const hour = new Date().getHours()
      - timeOfDay = hour < 12 ? 'morning' : hour < 17 ? 'afternoon' : 'evening'
      - timePrefix mapping: morning→'Good morning', afternoon→'Good afternoon', evening→'Good evening'
      - greeting = displayName ? `${timePrefix}, ${displayName}` : `${timePrefix}! How can I help?`
      - tagline = 'Your AI work co-pilot'
      - contextualLine = pageTitle ? `You're working on: ${pageTitle}` : undefined
    - Pure computation — no I/O, no side effects, no dependencies

    **useGreeting.ts** (NEW — React hook):
    - Import usePreferenceStore, useWorkspaceStore, generateGreeting, Greeting
    - Use individual Zustand selectors: displayName = usePreferenceStore(s => s.displayName), currentPageContext = useWorkspaceStore(s => s.currentPageContext)
    - Return generateGreeting({ displayName, pageTitle: currentPageContext?.title })
    - Individual selectors prevent re-renders from unrelated state changes (per useWorkspace pattern)
  </action>
  <verify>
    <automated>pnpm test tests/core/greeting/GreetingService.test.ts</automated>
  </verify>
  <done>generateGreeting produces correct time-of-day string for all 3 periods; displayName and contextualLine handled correctly; useGreeting subscribes to individual store fields; contextualLine undefined when no page context</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create TemplateBrowser popover component for Sender toolbar</name>
  <files>src/components/chat/TemplateBrowser.tsx</files>
  <read_first>
    - src/components/chat/QuickActionChips.tsx — existing chip component pattern
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 477-550 — TemplateBrowser exact component pattern
    - .planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md lines 379-429 — antd Popover + Input.Search pattern
    - .planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md — Template Browser interaction contract (lines 267-276), typography contract
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-13, D-14, D-15, D-16 — template browser decisions
  </read_first>
  <behavior>
    - Test 1: TemplateBrowser render shows TemplateOutlined icon button with aria-label="Browse templates"
    - Test 2: Clicking icon opens 360px Popover with Input.Search + categorized template sections
    - Test 3: Recently-used section appears first when recent templates exist; omitted when empty
    - Test 4: Clicking a template calls onInsert(template.template) and closes popover
    - Test 5: Typing in search filters results in real-time; search results shown as single category section
    - Test 6: Click outside or Escape closes popover (antd default behavior)
  </behavior>
  <action>
    Per D-13, D-16:

    Create TemplateBrowser component following PATTERNS.md lines 477-550 exact pattern:
    - Props: { onInsert: (template: string) => void }
    - State: open (boolean), search (string)
    - Compute: categories = search ? [{ category: 'Search results', templates: templateBrowserService.search(search) }] : templateBrowserService.getByCategory()
    - Render: Popover (trigger="click", open=open, onOpenChange=setOpen, placement="topLeft") containing trigger button (Button type="text", icon=<TemplateOutlined />, aria-label="Browse templates")
    - Popover content: 360px div with maxHeight 400, overflowY auto. Input (SearchOutlined prefix, allowClear) + categorized sections per PATTERNS.md lines 514-540
    - Each category section: uppercase category label (Text type="secondary" fontSize 11) + vertical Flex of template items
    - Each template item: strong name (13px) + description (11px secondary) — clickable with hover highlight
    - On template click: onInsert(tpl.template), setOpen(false)
    - Per D-16: templates with variables ({{placeholder}}) insert with placeholders as-is; user edits before sending
    - Per RESEARCH.md Pitfall 3: close popover on any Sender value change via onOpenChange
    - Use antd theme.useToken() for all styling
  </action>
  <verify>
    <automated>pnpm test tests/components/chat/TemplateBrowser.test.tsx</automated>
  </verify>
  <done>TemplateBrowser renders popover with categorized sections; recently-used shown first; search filters in real-time; selecting template fires onInsert and closes popover; aria-label on trigger button; closes on outside click</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template content → Sender input | Template text inserted as plain string into controlled React state — no HTML rendering |
| user search → TemplateBrowserService | Search query filtered against template names/descriptions; no external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.4-04 | Tampering | TemplateBrowser template insertion | low | accept | Template content inserted as plain text into Sender value (controlled React state); no dangerouslySetInnerHTML; variables like {{topic}} are literal strings |
| T-07.4-05 | Information Disclosure | GreetingService pageTitle | low | accept | pageTitle sourced from workspaceStore.currentPageContext.title — only shows what is already visible in the Side Panel pin tab bar; no new data exposure |
</threat_model>

<verification>
## Wave 0 Requirements
- tests/core/prompts/TemplateBrowserService.test.ts — category grouping, search, recent tracking
- tests/core/greeting/GreetingService.test.ts — time-of-day logic, displayName, contextualLine edge cases
- tests/components/chat/TemplateBrowser.test.tsx — popover rendering, search, selection, recent section
</verification>

<success_criteria>
- [ ] All 21 builtin templates have displayCategory assigned correctly
- [ ] TemplateBrowserService groups, searches, and tracks recent templates correctly
- [ ] GreetingService returns correct time-of-day greeting for all 3 periods
- [ ] useGreeting hook re-renders only on displayName/currentPageContext changes
- [ ] TemplateBrowser popover renders correctly with search, categories, recent section
- [ ] All existing tests pass without regression
</success_criteria>

<output>
Create `.planning/phases/07.4-rich-design-enhance/07.4-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `displayCategory` (field) | interface | builtinTemplates.ts | User-facing template category (Writing/Analysis/Research/Coding/Support) |
| `TemplateBrowserService` | class | TemplateBrowserService.ts | Category grouping, search, recent-usage tracking |
| `templateBrowserService` | singleton | TemplateBrowserService.ts | App-wide singleton instance |
| `CategorizedTemplates` | interface | TemplateBrowserService.ts | Category name + template list |
| `TemplateBrowser` | component | TemplateBrowser.tsx | Sender toolbar popover with categorized templates |
| `generateGreeting(input)` | function | GreetingService.ts | Returns time-of-day greeting with optional name + context |
| `GreetingInput` | interface | GreetingService.ts | Input type: displayName?, pageTitle? |
| `Greeting` | interface | GreetingService.ts | Output type: greeting, timeOfDay, tagline, contextualLine? |
| `useGreeting()` | hook | useGreeting.ts | React hook subscribing to prefs + workspace for greeting |
