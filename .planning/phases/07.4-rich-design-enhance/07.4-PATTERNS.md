# Phase 7.4: RICH Design Enhance - Pattern Map

**Mapped:** 2026-07-19
**Files classified:** 27 (16 new, 11 modified)
**Analogs found:** 27 / 27

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `src/core/stores/RightPaneStore.ts` | store | state-management | `src/core/stores/diagnosticsStore.ts` | exact (partialize) |
| `src/core/prompts/TemplateBrowserService.ts` | service | read/transform | `src/core/ai/WelcomeCardService.ts` | role-match |
| `src/core/greeting/GreetingService.ts` | utility | transform | Pure function module (no class) | partial |
| `src/core/confirmation/ConfirmationPolicy.ts` | utility | lookup/mapping | `src/core/ai/tools/PermissionService.ts` | partial |
| `src/hooks/useGreeting.ts` | hook | subscription+transform | `src/hooks/useWorkspace.ts` | exact |
| `src/hooks/useConversationClosure.ts` | hook | timer/state-machine | `src/hooks/useWorkspace.ts` | role-match |
| `src/components/standalone/RightPane.tsx` | component | presentation/container | `src/components/chat/FollowUpAction.tsx` | partial |
| `src/components/standalone/PaneToggle.tsx` | component | event-emitter | `src/components/standalone/StandaloneSider.tsx` | role-match |
| `src/components/standalone/RightPaneTabs/ContextTab.tsx` | component | read-from-store | `src/components/sidepanel/PinTabBar.tsx` | role-match |
| `src/components/standalone/RightPaneTabs/NotesTab.tsx` | component | read+search | `src/components/chat/ClarificationAction.tsx` | partial |
| `src/components/standalone/RightPaneTabs/ToolsTab.tsx` | component | read-only-display | `src/components/chat/ClarificationAction.tsx` | partial |
| `src/components/chat/InlineConfirmationCard.tsx` | component | event-emitter | `src/components/chat/ClarificationAction.tsx` | exact |
| `src/components/chat/ConversationClosure.tsx` | component | event-emitter | `src/components/chat/FollowUpAction.tsx` | exact |
| `src/components/chat/TemplateBrowser.tsx` | component | search+select | `src/components/chat/QuickActionChips.tsx` | role-match |
| `src/components/chat/StructuredOutputActions.tsx` | component | event-emitter | `src/components/chat/ChatMessage.tsx` | exact |
| `src/components/onboarding/MeetNowPilotStep.tsx` | component | presentation | `src/core/onboarding/OnboardingModal.tsx` step 0 | exact |
| `src/core/prompts/builtinTemplates.ts` [MODIFY] | model | data-definition | Same file (add field) | same-file |
| `src/core/ai/persona/PersonaService.ts` [MODIFY] | service | read/merge | Same file (add method) | same-file |
| `src/core/ai/tools/PermissionService.ts` [MODIFY] | service | permission-gate | Same file (add enum+method) | same-file |
| `src/core/memory/memoryTypes.ts` [MODIFY] | model | schema-extension | Same file (add fields) | same-file |
| `src/core/memory/PreferenceMemoryStore.ts` [MODIFY] | store | state-management | Same file (add fields) | same-file |
| `src/core/onboarding/OnboardingModal.tsx` [MODIFY] | component | wizard-flow | Same file (replace step 0) | same-file |
| `src/components/standalone/StandaloneRoot.tsx` [MODIFY] | component | layout-composition | Same file (add column) | same-file |
| `src/components/chat/BrandedHeader.tsx` [MODIFY] | component | presentation | Same file (add props) | same-file |
| `src/components/chat/ChatMessage.tsx` [MODIFY] | component | presentation | Same file (add branding+footer) | same-file |
| `src/core/pages/ChatPage.tsx` [MODIFY] | page | composition | Same file (integrate multiple) | same-file |
| `src/core/pages/AgentPage.tsx` [MODIFY] | page | composition | Same file (string change) | same-file |

## Pattern Assignments

### `src/core/stores/RightPaneStore.ts` (store, state-management)

**Analog:** `src/core/stores/diagnosticsStore.ts` + `src/core/stores/workspaceStore.ts`

**Imports pattern** (diagnosticsStore.ts lines 1-5):
```typescript
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
```

**chrome.storage.local adapter** (workspaceStore.ts lines 41-74, simplified variant from diagnosticsStore.ts lines 35-40):
```typescript
const chromeLocalStorage = createJSONStorage<RightPaneState>(() => ({
  getItem: (name: string) =>
    chrome.storage.local.get(name).then((result: Record<string, unknown>) => (result[name] as string) ?? null),
  setItem: (name: string, value: string) => chrome.storage.local.set({ [name]: value }),
  removeItem: (name: string) => chrome.storage.local.remove(name),
}));
```

**Core pattern — persist with partialize** (diagnosticsStore.ts lines 42-127):
```typescript
export const useRightPaneStore = create<RightPaneState>()(
  persist(
    (set) => ({
      // Persisted UI layout state
      activeTab: 'context',
      visible: true,
      width: 'compact',
      // Transient (not persisted)
      searchQuery: '',
      selectedNoteId: null,
      expandedToolId: null,
      // Actions
      setActiveTab: (tab) => set({ activeTab: tab }),
      setVisible: (v) => set({ visible: v }),
      toggleWidth: () => set((s) => ({ width: s.width === 'compact' ? 'expanded' : 'compact' })),
      setSearchQuery: (q) => set({ searchQuery: q }),
      setSelectedNoteId: (id) => set({ selectedNoteId: id }),
      setExpandedToolId: (id) => set({ expandedToolId: id }),
    }),
    {
      name: 'np_right_pane',
      storage: chromeLocalStorage,
      // Only persist UI layout state — search/selection is ephemeral
      partialize: (state) => ({
        activeTab: state.activeTab,
        visible: state.visible,
        width: state.width,
      }),
    },
  ),
);
```

**Key convention:** Prefer explicit `partialize` whitelist (diagnosticsStore.ts lines 121-124 pattern) — never rely on implicit default which persists everything.

---

### `src/core/prompts/TemplateBrowserService.ts` (service, read/transform)

**Analog:** `src/core/ai/WelcomeCardService.ts` (static definitions + read-from-store pattern)

**Imports pattern** (WelcomeCardService.ts lines 1-2):
```typescript
import { debugLog } from '../utils/debugLog';
import { useWorkspaceStore } from '../stores/workspaceStore';
```

**Core pattern — static definitions + methods** (WelcomeCardService.ts lines 20-169):
```typescript
import { debugLog } from '../utils/debugLog';
import { promptManager } from './PromptManager';
import type { PromptTemplate } from './PromptManager';

export interface CategorizedTemplates {
  category: string;
  templates: PromptTemplate[];
}

export class TemplateBrowserService {
  private recentTemplateIds: string[] = [];
  private readonly MAX_RECENT = 5;

  getByCategory(): CategorizedTemplates[] {
    // Group templates by displayCategory, sorted by order
    // ... group + sort logic
  }

  search(query: string): PromptTemplate[] {
    // Fuzzy search by name + description
    // ... search logic
  }

  trackRecentUse(templateId: string): void {
    // Track recently used templates (in-memory, not persisted)
    // ... tracking logic
  }

  getRecentlyUsed(): PromptTemplate[] {
    // Return recently used templates
    // ... retrieval logic
  }
}

export const templateBrowserService = new TemplateBrowserService();
```

**Error handling:** Services log via `debugLog()` not throws — same as WelcomeCardService.

---

### `src/core/greeting/GreetingService.ts` (utility, transform)

**Analog:** Pure function module — no class. REFERENCE: RESEARCH.md Code Examples lines 606-646.

**Pattern — exported interface + pure function (no class, no side effects):**
```typescript
export interface GreetingInput {
  displayName?: string;
  pageTitle?: string;
}

export interface Greeting {
  greeting: string;
  timeOfDay: 'morning' | 'afternoon' | 'evening';
  tagline: string;
  contextualLine?: string;
}

export function generateGreeting(input: GreetingInput): Greeting {
  const hour = new Date().getHours();
  const timeOfDay = hour < 12 ? 'morning' : hour < 17 ? 'afternoon' : 'evening';
  const timePrefix = timeOfDay === 'morning' ? 'Good morning'
    : timeOfDay === 'afternoon' ? 'Good afternoon'
    : 'Good evening';
  const greeting = input.displayName
    ? `${timePrefix}, ${input.displayName}`
    : `${timePrefix}! How can I help?`;
  const tagline = 'Your AI work co-pilot';
  const contextualLine = input.pageTitle
    ? `You're working on: ${input.pageTitle}`
    : undefined;
  return { greeting, timeOfDay, tagline, contextualLine };
}
```

**Error handling:** None needed — pure computation with no I/O.

---

### `src/core/confirmation/ConfirmationPolicy.ts` (utility, lookup/mapping)

**Analog:** `src/core/ai/tools/PermissionService.ts` (interface + enum pattern)

**Imports pattern** (PermissionService.ts line 1):
```typescript
import { debugLog } from '../utils/debugLog';
```

**Core pattern — enum + interface + static mapping:**
```typescript
import { debugLog } from '../utils/debugLog';

export enum ConfirmationLevel {
  AutoExecute = 'auto',
  ConfirmInline = 'inline',
  RequirePermission = 'require',
}

/**
 * Static tool → ConfirmationLevel policy mapping (D-18).
 * Agent's discretion: exact mapping per tool.
 */
export function getConfirmationLevel(toolName: string): ConfirmationLevel {
  const level = CONFIRMATION_POLICY[toolName];
  if (!level) {
    debugLog('info', `[ConfirmationPolicy] No policy for tool: ${toolName}, defaulting to RequirePermission`);
    return ConfirmationLevel.RequirePermission;
  }
  return level;
}

const CONFIRMATION_POLICY: Record<string, ConfirmationLevel> = {
  // Read-only local operations → auto-execute
  summarize_page: ConfirmationLevel.AutoExecute,
  extract_page: ConfirmationLevel.AutoExecute,
  // External lookups → inline confirm
  web_search: ConfirmationLevel.ConfirmInline,
  web_fetch: ConfirmationLevel.ConfirmInline,
  // Side-effect actions → require permission
  send_message: ConfirmationLevel.RequirePermission,
  create_record: ConfirmationLevel.RequirePermission,
  update_record: ConfirmationLevel.RequirePermission,
};
```

---

### `src/hooks/useGreeting.ts` (hook, subscription+transform)

**Analog:** `src/hooks/useWorkspace.ts` (lines 1-45)

**Imports pattern** (useWorkspace.ts lines 1-2):
```typescript
import { useWorkspaceStore } from '../core/stores/workspaceStore';
import type { Surface } from '../core/navigation/navigationTypes';
```

**Core pattern — hook extracting from Zustand stores with individual selectors** (useWorkspace.ts lines 23-45):
```typescript
import { usePreferenceStore } from '../core/memory/PreferenceMemoryStore';
import { useWorkspaceStore } from '../core/stores/workspaceStore';
import { generateGreeting } from '../core/greeting/GreetingService';
import type { Greeting } from '../core/greeting/GreetingService';

export function useGreeting(): Greeting {
  // Individual selectors — prevent re-renders from unrelated state changes
  const displayName = usePreferenceStore((s) => s.displayName);
  const currentPageContext = useWorkspaceStore((s) => s.currentPageContext);

  return generateGreeting({
    displayName,
    pageTitle: currentPageContext?.title,
  });
}
```

**Key convention:** Use individual selector functions (not `useStore()` with destructuring) to prevent re-renders — exactly the useWorkspace.ts pattern.

---

### `src/hooks/useConversationClosure.ts` (hook, timer/state-machine)

**Analog:** `src/hooks/useWorkspace.ts` (import structure) + timer management from useChat.ts pattern

**Imports pattern** (useWorkspace.ts line 1):
```typescript
import { useState, useEffect, useRef, useCallback } from 'react';
```

**Core pattern — ref-based timer management:**
```typescript
import { useState, useEffect, useRef, useCallback } from 'react';

export interface ClosureState {
  showPrompt: boolean;
  isComplete: boolean;
}

export function useConversationClosure(
  messagesLength: number,
  isStreaming: boolean,
  hasActiveClarifications: boolean,
): ClosureState {
  const [showPrompt, setShowPrompt] = useState(false);
  const [alreadyShown, setAlreadyShown] = useState(false);
  const idleTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  useEffect(() => {
    if (alreadyShown) return;

    // Reset timer on new activity
    if (idleTimerRef.current) {
      clearTimeout(idleTimerRef.current);
      idleTimerRef.current = null;
    }

    if (messagesLength === 0 || isStreaming || hasActiveClarifications) {
      setShowPrompt(false);
      return;
    }

    // Start idle timer
    idleTimerRef.current = setTimeout(() => {
      setShowPrompt(true);
      setAlreadyShown(true);
    }, 12_000); // 12s idle threshold (D-25)

    return () => {
      if (idleTimerRef.current) {
        clearTimeout(idleTimerRef.current);
      }
    };
  }, [messagesLength, isStreaming, hasActiveClarifications, alreadyShown]);

  const dismiss = useCallback(() => {
    setShowPrompt(false);
  }, []);

  return { showPrompt, isComplete: alreadyShown, dismiss };
}
```

**Error handling:** Cleanup via `useEffect` return — clear timer on unmount/rerun. Same pattern as useChat.ts timer cleanup.

---

### `src/components/chat/InlineConfirmationCard.tsx` (component, event-emitter)

**Analog:** `src/components/chat/ClarificationAction.tsx` (lines 1-48) + `src/components/chat/FollowUpAction.tsx` (lines 1-81)

**Imports pattern** (ClarificationAction.tsx lines 1-2):
```typescript
import { Typography, theme } from 'antd';
import { ActionChipGroup, type ChipAction } from '../common/ActionChipGroup';
```

**Core pattern — card with description + ActionChipGroup chips** (ClarificationAction.tsx lines 18-48):
```typescript
import { Typography, theme, Spin, Flex } from 'antd';
import { CheckCircleOutlined, CloseCircleOutlined } from '@ant-design/icons';
import { ActionChipGroup, type ChipAction } from '../common/ActionChipGroup';

const { Text } = Typography;

export interface InlineConfirmationCardProps {
  actionDescription: string;
  rationale?: string;
  onProceed: () => void;
  onCancel: () => void;
  state: 'pending' | 'executing' | 'completed' | 'cancelled';
  actionSummary?: string;
}

export function InlineConfirmationCard({
  actionDescription, rationale, onProceed, onCancel, state, actionSummary,
}: InlineConfirmationCardProps) {
  const { token } = theme.useToken();

  if (state === 'cancelled') {
    return <Text type="secondary" style={{ fontSize: 12 }}>Action cancelled.</Text>;
  }

  if (state === 'executing' || state === 'completed') {
    return (
      <Flex align="center" gap={token.marginXS}>
        <CheckCircleOutlined style={{ color: token.colorSuccess }} />
        <Text style={{ fontSize: 12 }}>{actionSummary}</Text>
        {state === 'executing' && <Spin size="small" />}
      </Flex>
    );
  }

  // Pending state — main confirmation card
  const chips: ChipAction[] = [
    { key: 'proceed', label: 'Proceed', value: 'proceed' },
    { key: 'cancel', label: "Don't proceed", value: 'cancel' },
  ];

  const handleSelect = (value: string) => {
    if (value === 'proceed') onProceed();
    else onCancel();
  };

  return (
    <div
      style={{
        border: `1px solid ${token.colorWarningBorder}`,
        borderRadius: token.borderRadiusLG,
        padding: token.paddingSM,
        marginTop: token.marginXS,
        background: token.colorWarningBg,
      }}
    >
      <Text strong style={{ fontSize: 13 }}>{actionDescription}</Text>
      {rationale && (
        <Text type="secondary" style={{ fontSize: 12, display: 'block', marginTop: 4 }}>
          {rationale}
        </Text>
      )}
      <div style={{ marginTop: token.marginSM }}>
        <ActionChipGroup actions={chips} onSelect={handleSelect} variant="default" />
      </div>
    </div>
  );
}
```

---

### `src/components/chat/ConversationClosure.tsx` (component, event-emitter)

**Analog:** `src/components/chat/FollowUpAction.tsx` (lines 1-81)

**Imports pattern** (FollowUpAction.tsx lines 1-4):
```typescript
import { useMemo, type CSSProperties } from 'react';
import { theme } from 'antd';
```

**Core pattern — lightweight prompt with icon buttons** (FollowUpAction.tsx lines 19-81):
```typescript
import { Flex, Button, Typography, theme } from 'antd';
import { LikeOutlined, DislikeOutlined } from '@ant-design/icons';

const { Text } = Typography;

export interface ConversationClosureProps {
  onFeedback: (helpful: boolean) => void;
}

export function ConversationClosure({ onFeedback }: ConversationClosureProps) {
  const { token } = theme.useToken();

  return (
    <Flex
      align="center"
      gap={token.marginXS}
      style={{
        padding: `${token.paddingXS}px ${token.paddingSM}px`,
        borderTop: `1px solid ${token.colorBorderSecondary}`,
      }}
    >
      <Text style={{ fontSize: 13, color: token.colorTextSecondary }}>
        Did this help?
      </Text>
      <Button
        type="text"
        size="small"
        icon={<LikeOutlined />}
        onClick={() => onFeedback(true)}
      />
      <Button
        type="text"
        size="small"
        icon={<DislikeOutlined />}
        onClick={() => onFeedback(false)}
      />
    </Flex>
  );
}
```

---

### `src/components/chat/TemplateBrowser.tsx` (component, search+select)

**Analog:** antd Popover pattern + `src/components/chat/QuickActionChips.tsx`

**Core pattern — controlled Popover with Input.Search + categorized list** (REFERENCE: RESEARCH.md lines 386-429):
```typescript
import { useState } from 'react';
import { Popover, Input, Button, Flex, Typography, theme } from 'antd';
import { TemplateOutlined, SearchOutlined } from '@ant-design/icons';
import { templateBrowserService, type CategorizedTemplates } from '../../core/prompts/TemplateBrowserService';
import type { PromptTemplate } from '../../core/prompts/PromptManager';

const { Text } = Typography;

export interface TemplateBrowserProps {
  onInsert: (template: string) => void;
}

export function TemplateBrowser({ onInsert }: TemplateBrowserProps) {
  const { token } = theme.useToken();
  const [open, setOpen] = useState(false);
  const [search, setSearch] = useState('');

  const categories = search
    ? [{ category: 'Search results', templates: templateBrowserService.search(search) }]
    : templateBrowserService.getByCategory();

  const content = (
    <div style={{ width: 360, maxHeight: 400, overflowY: 'auto' }}>
      <Input
        prefix={<SearchOutlined />}
        placeholder="Search templates..."
        value={search}
        onChange={(e) => setSearch(e.target.value)}
        allowClear
        style={{ marginBottom: token.marginSM }}
      />
      {categories.map((cat) => (
        <div key={cat.category} style={{ marginBottom: token.marginSM }}>
          <Text type="secondary" style={{ fontSize: 11, textTransform: 'uppercase', padding: `0 ${token.paddingXS}px` }}>
            {cat.category}
          </Text>
          <Flex vertical gap={2} style={{ marginTop: 4 }}>
            {cat.templates.map((tpl) => (
              <div
                key={tpl.id}
                onClick={() => { onInsert(tpl.template); setOpen(false); }}
                style={{
                  padding: `${token.paddingXS}px ${token.paddingSM}px`,
                  borderRadius: token.borderRadius,
                  cursor: 'pointer',
                }}
                onMouseEnter={(e) => (e.currentTarget.style.background = token.colorBgTextHover)}
                onMouseLeave={(e) => (e.currentTarget.style.background = 'transparent')}
              >
                <Text strong style={{ fontSize: 13 }}>{tpl.name}</Text>
                {tpl.description && (
                  <Text type="secondary" style={{ fontSize: 11, display: 'block' }}>{tpl.description}</Text>
                )}
              </div>
            ))}
          </Flex>
        </div>
      ))}
    </div>
  );

  return (
    <Popover content={content} trigger="click" open={open} onOpenChange={setOpen} placement="topLeft">
      <Button type="text" icon={<TemplateOutlined />} aria-label="Browse templates" />
    </Popover>
  );
}
```

---

### `src/components/chat/StructuredOutputActions.tsx` (component, event-emitter)

**Analog:** `src/components/chat/ChatMessage.tsx` footer button pattern (lines 41-49)

**Core pattern — footer with action buttons** (ChatMessage.tsx lines 41-49):
```typescript
import { Button, Flex, message, theme } from 'antd';
import { CopyOutlined, TableOutlined } from '@ant-design/icons';

export interface StructuredOutputActionsProps {
  content: string;
  hasTable: boolean;
}

export function StructuredOutputActions({ content, hasTable }: StructuredOutputActionsProps) {
  const { token } = theme.useToken();

  const handleCopyTable = () => {
    navigator.clipboard.writeText(content).then(() => message.success('Table copied to clipboard'));
  };

  const handleExportCSV = () => {
    // Extract markdown table rows → CSV
    const rows = content.split('\n').filter(line => line.startsWith('|'));
    const csv = rows.map(row =>
      row.split('|').filter(cell => cell.trim()).map(cell => `"${cell.trim()}"`).join(',')
    ).join('\n');
    const blob = new Blob([csv], { type: 'text/csv' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url; a.download = 'export.csv'; a.click();
    URL.revokeObjectURL(url);
  };

  if (!hasTable) return null;

  return (
    <Flex gap={4} style={{ marginTop: 4 }}>
      <Button type="text" size="small" icon={<CopyOutlined />} onClick={handleCopyTable}>
        Copy as table
      </Button>
      <Button type="text" size="small" icon={<TableOutlined />} onClick={handleExportCSV}>
        Export CSV
      </Button>
    </Flex>
  );
}
```

---

### `src/components/onboarding/MeetNowPilotStep.tsx` (component, presentation)

**Analog:** `src/core/onboarding/OnboardingModal.tsx` step 0 pattern (lines 395-520)

**Core pattern — step content with inline styles, no antd components** (OnboardingModal.tsx lines 395-520):
```typescript
import React from 'react';
import { Typography, theme, Avatar } from 'antd';
import { BunnyAvatar } from '../common/BunnyAvatar';

const { Text, Title } = Typography;

export interface MeetNowPilotStepProps {
  onContinue: () => void;
  onSkip: () => void;
}

export function MeetNowPilotStep({ onContinue, onSkip }: MeetNowPilotStepProps) {
  const { token } = theme.useToken();

  const capabilities = [
    { title: 'Summarize Pages', desc: 'Extract key insights from any page' },
    { title: 'Research Topics', desc: 'Search and synthesize information' },
    { title: 'Draft Responses', desc: 'Write professional replies' },
    { title: 'Explain Code', desc: 'Break down complex code/errors' },
  ];

  return (
    <div style={{ textAlign: 'center', padding: token.padding }}>
      {/* Bunny avatar */}
      <Avatar
        size={64}
        icon={<BunnyAvatar />}
        style={{ border: `2px solid ${token.colorPrimary}`, marginBottom: token.marginMD }}
      />
      {/* Identity */}
      <Title level={3} style={{ marginBottom: 4 }}>Meet NowPilot</Title>
      <Text type="secondary" style={{ display: 'block', marginBottom: token.marginMD }}>
        Your AI work co-pilot
      </Text>
      {/* Core values */}
      <Flex wrap="wrap" justify="center" gap={token.marginXS} style={{ marginBottom: token.marginLG }}>
        {['Privacy-first', 'Helpful', 'Precise', 'Humble'].map((v) => (
          <span key={v} style={{
            padding: '2px 10px', borderRadius: 12, fontSize: 11,
            background: token.colorFillSecondary, color: token.colorTextSecondary,
          }}>{v}</span>
        ))}
      </Flex>
      {/* Capability preview cards */}
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: token.marginXS, marginBottom: token.marginLG }}>
        {capabilities.map((c) => (
          <div key={c.title} style={{
            padding: token.paddingXS, borderRadius: token.borderRadius,
            background: token.colorBgLayout, border: `1px solid ${token.colorBorderSecondary}`,
            textAlign: 'left',
          }}>
            <Text strong style={{ fontSize: 12 }}>{c.title}</Text>
            <Text type="secondary" style={{ fontSize: 11, display: 'block' }}>{c.desc}</Text>
          </div>
        ))}
      </div>
      {/* CTA buttons */}
      <button
        onClick={onContinue}
        style={{
          width: '100%', background: '#e0582e', color: '#fff', padding: '10px 16px',
          borderRadius: 10, fontWeight: 'bold', fontSize: 14, border: 'none', cursor: 'pointer',
          marginBottom: token.marginSM,
        }}
      >
        Continue
      </button>
      <button
        onClick={onSkip}
        style={{
          background: 'transparent', border: 'none', color: token.colorTextQuaternary,
          fontSize: 13, cursor: 'pointer',
        }}
      >
        Skip for now
      </button>
    </div>
  );
}
```

---

### `src/components/standalone/StandaloneRoot.tsx` [MODIFY] (component, layout-composition)

**Analog:** Same file — add right pane column between chat content and right edge (lines 84-127)

**Core pattern — add flex column to layout** (StandaloneRoot.tsx lines 84-127):
```typescript
// Current layout: Sider → ContentWrapper (flex: 1)
// New layout: Sider → ContentWrapper (flex: 1) → RightPane (collapsible)

function StandaloneContentWrapper({ ... }) {
  const { token } = theme.useToken();
  const { visible, width } = useRightPaneStore();

  const rightPaneWidth = width === 'compact' ? 320 : Math.floor(containerWidth * 0.45);

  return (
    <div style={{ flex: 1, display: 'flex', minWidth: 0 }}>
      {/* Existing content column */}
      <div style={{ flex: 1, minWidth: 0, display: 'flex', flexDirection: 'column', ... }}>
        <StandaloneContent ...>
          {renderActivePage ? renderActivePage(...) : null}
        </StandaloneContent>
      </div>
      {/* NEW: Right pane column */}
      {visible && <RightPane width={rightPaneWidth} />}
    </div>
  );
}
```

---

### `src/core/pages/ChatPage.tsx` [MODIFY] (page, composition)

**Analog:** Same file — multiple integration points. See existing patterns sections.

**Key integration patterns:**

1. **Imports addition** (lines 1-70): Add new component imports following existing import block organization:
```typescript
// ... existing imports ...
import { TemplateBrowser } from '../../components/chat/TemplateBrowser';  // after existing component imports
import { InlineConfirmationCard } from '../../components/chat/InlineConfirmationCard';
import { ConversationClosure } from '../../components/chat/ConversationClosure';
import { StructuredOutputActions } from '../../components/chat/StructuredOutputActions';
import { useGreeting } from '../../hooks/useGreeting';
import { useConversationClosure } from '../../hooks/useConversationClosure';
import { useRightPaneStore } from '../../core/stores/RightPaneStore';
```

2. **Empty state — BrandedHeader with greeting** (lines 1217-1246): Replace static text with greeting props:
```typescript
// Current (lines 1220-1242):
{!welcomeCardsDismissed && <BrandedHeader onClose={handleDismissWelcomeCards} />}
// ...
<Text style={{ fontSize: 20, fontWeight: 600, color: token.colorText }}>
  Hi, I&apos;m NowPilot
</Text>

// New:
const greeting = useGreeting();
// ...
{!welcomeCardsDismissed && (
  <BrandedHeader
    userGreeting={greeting.greeting}
    contextualMessage={greeting.contextualLine}
    onClose={handleDismissWelcomeCards}
  />
)}
```

3. **Sender wrapper — TemplateBrowser icon + image paste** (lines 1839-1950):
```typescript
// Add TemplateBrowser to Sender prefix:
<Sender
  prefix={
    <TemplateBrowser
      onInsert={(tpl) => setDraft((prev) => prev + tpl)}
    />
  }
  onPasteFile={(files: FileList) => {
    const imageFiles = Array.from(files).filter(f => f.type.startsWith('image/'));
    if (imageFiles.length === 0) return;
    const remaining = 5 - attachedFiles.length;
    const toAdd = imageFiles.slice(0, remaining);
    const newAttachments = toAdd.map((file) => ({
      id: crypto.randomUUID(),
      name: file.name || `Pasted-${Date.now()}.png`,
      url: URL.createObjectURL(file),
      type: file.type,
      file,
    }));
    setAttachedFiles((prev) => [...prev, ...newAttachments]);
  }}
  // ... rest of existing props unchanged
/>
```

4. **Bubble rendering — InlineConfirmationCard + ConversationClosure** (bubble list area):
```typescript
// Inside the Bubble rendering loop:
{bubble.role === 'assistant' && bubble.metadata?.confirmation && (
  <InlineConfirmationCard
    actionDescription={bubble.metadata.confirmation.description}
    rationale={bubble.metadata.confirmation.rationale}
    onProceed={...}
    onCancel={...}
    state={bubble.metadata.confirmation.state}
    actionSummary={bubble.metadata.confirmation.summary}
  />
)}
{showClosurePrompt && <ConversationClosure onFeedback={handleClosureFeedback} />}
```

---

### `src/core/pages/AgentPage.tsx` [MODIFY] (page, composition)

**Analog:** Same file — simple string replacement (lines 66-69)

**Core pattern — ThoughtChain header text** (AgentPage.tsx lines 65-69):
```typescript
// Current (ThoughtChainView.tsx line 68):
Send a message to start an agent workflow

// Change to (D-29):
<ThoughtChain
  items={items}
  // ...
/>
// Replace empty state text in ThoughtChainView.tsx line 68:
// "Send a message to start an agent workflow"
// OR add a title prop to ThoughtChain that reads "NowPilot is working…"
```

**Actual change — ThoughtChainView.tsx line 68 or AgentPage.tsx wrapper** (AgentPage.tsx lines 96-98):
```typescript
{/* ThoughtChain area */}
<div style={thoughtChainStyle}>
  <ThoughtChainView steps={steps} />
</div>
// Option A: Pass title prop to ThoughtChainView
// Option B: Change empty-state text in ThoughtChainView.tsx line 68 to "NowPilot is working…"
```

---

### `src/core/memory/memoryTypes.ts` [MODIFY] (model, schema-extension)

**Analog:** Same file — extend `preferenceSchema` zod object (lines 84-91)

**Core pattern — add fields to zod schema** (memoryTypes.ts lines 84-91):
```typescript
export const preferenceSchema = z.object({
  // Existing 6 fields
  responseStyle: z.string(),
  preferredLanguage: z.string(),
  preferStructuredOutput: z.boolean(),
  allowCloudFallbackFromLocal: z.boolean(),
  defaultProviderId: z.string(),
  toolAutonomy: z.enum(['manual', 'allow-safe', 'allow-all']),
  // NEW fields (D-11, D-21)
  displayName: z.string().optional(),
  aiName: z.string().optional(),
  aiTone: z.string().optional(),
  responseBrevity: z.enum(['concise', 'detailed']).optional(),
});
export type PreferencePayload = z.infer<typeof preferenceSchema>;
```

---

### `src/core/memory/PreferenceMemoryStore.ts` [MODIFY] (store, state-management)

**Analog:** Same file — add default fields + pass through validation gate (lines 43-100)

**Core pattern — add defaults + validate in preferenceMemoryStore.get()** (PreferenceMemoryStore.ts lines 43-99):
```typescript
// In persist initial state (line 46-53):
persist(
  (set) => ({
    // Existing 6 fields
    responseStyle: 'concise',
    preferredLanguage: 'auto',
    preferStructuredOutput: false,
    allowCloudFallbackFromLocal: false,
    defaultProviderId: '',
    toolAutonomy: 'manual',
    // NEW fields — default undefined (optional)
    displayName: undefined,
    aiName: undefined,
    aiTone: undefined,
    responseBrevity: undefined,
    setPreferences: (partial: Partial<PreferencePayload>) => set(partial),
  }),
  { name: 'np_preferences', storage: chromeLocalStorage },
);

// In preferenceMemoryStore.get() validation gate (line 78-85):
preferenceSchema.parse({
  responseStyle: own.responseStyle,
  preferredLanguage: own.preferredLanguage,
  preferStructuredOutput: own.preferStructuredOutput,
  allowCloudFallbackFromLocal: own.allowCloudFallbackFromLocal,
  defaultProviderId: own.defaultProviderId,
  toolAutonomy: own.toolAutonomy,
  // NEW fields — zod optional fields pass through
  displayName: own.displayName,
  aiName: own.aiName,
  aiTone: own.aiTone,
  responseBrevity: own.responseBrevity,
});

return {
  // Existing 6 fields
  responseStyle: own.responseStyle,
  preferredLanguage: own.preferredLanguage,
  preferStructuredOutput: own.preferStructuredOutput,
  allowCloudFallbackFromLocal: own.allowCloudFallbackFromLocal,
  defaultProviderId: own.defaultProviderId,
  toolAutonomy: own.toolAutonomy,
  // External store reads
  themeMode,
  defaultSurface,
  // NEW fields
  displayName: own.displayName,
  aiName: own.aiName,
  aiTone: own.aiTone,
  responseBrevity: own.responseBrevity,
};
```

---

### `src/core/prompts/builtinTemplates.ts` [MODIFY] (model, data-definition)

**Analog:** Same file — add `displayCategory` field to interface + all 21 entries (lines 1-267)

**Core pattern — add field to all entries** (builtinTemplates.ts lines 1-12 interface):
```typescript
export interface BuiltinPromptTemplate {
  id: string;
  name: string;
  description: string;
  template: string;
  category: string;       // internal (kept)
  displayCategory: string; // NEW: Writing | Analysis | Research | Coding | Support
  variables: string[];
  scopes?: ('chat' | 'reading' | 'writing' | 'reply')[];
  hidden?: boolean;
  icon?: string;
  order?: number;
}

// Assign displayCategory per template:
// Writing: improve-writing, continue-writing, fix-spelling-grammar, translate,
//          make-shorter, make-longer, simplify-language, change-tone, outline,
//          brainstorm, blog-post, paragraph-about, social-media-post, press-release,
//          creative-story, to-do-list, meeting-agenda, sales-email
// Analysis: summarize, testing
// Research: explain  (re-categorized from builtin)
// Coding: none currently (future)
// Support: none currently (future)
```

---

### `src/core/ai/persona/PersonaService.ts` [MODIFY] (service, read/merge)

**Analog:** Same file — add `getActiveProfile()` method (lines 1-17)

**Core pattern — merge preference overrides onto DEFAULT_PERSONA_PROFILE** (PersonaService.ts lines 1-17):
```typescript
import { debugLog } from '../../utils/debugLog';
import { DEFAULT_PERSONA_PROFILE } from './PersonaProfile';
import type { PersonaProfile } from './PersonaProfile';
import { usePreferenceStore } from '../../core/memory/PreferenceMemoryStore';

export class PersonaService {
  #profile: PersonaProfile;

  constructor() {
    this.#profile = DEFAULT_PERSONA_PROFILE;
  }

  getProfile(): PersonaProfile {
    return this.#profile;
  }

  // NEW: Merge preference overrides onto canonical profile (D-11)
  getActiveProfile(): PersonaProfile {
    const prefs = usePreferenceStore.getState();
    const profile = { ...DEFAULT_PERSONA_PROFILE };

    // Apply identity overrides
    if (prefs.aiName) {
      profile.identity = { ...profile.identity, name: prefs.aiName };
    }
    // Apply language style overrides
    if (prefs.aiTone) {
      profile.languageStyle = { ...profile.languageStyle, tone: prefs.aiTone };
    }
    if (prefs.responseBrevity) {
      const style = prefs.responseBrevity === 'concise' ? 'Concise by default with task-aware expansion' : 'Detailed with thorough explanations';
      profile.languageStyle = { ...profile.languageStyle, responseStyle: style };
    }

    debugLog('info', '[PersonaService] getActiveProfile — merged preference overrides');
    return profile;
  }
}

export const personaService = new PersonaService();
```

**Key warning:** Per RESEARCH.md pitfall #4 — use explicit field-level assignment, NOT `{ ...default, ...overrides }` for nested objects. Deep merge with spread loses nested fields.

---

### `src/core/ai/tools/PermissionService.ts` [MODIFY] (service, permission-gate)

**Analog:** Same file — add `ConfirmationLevel` enum + `getConfirmationLevel()` method (lines 1-16)

**Core pattern — interface extension** (PermissionService.ts lines 1-16):
```typescript
import { debugLog } from '../../utils/debugLog';
import { getConfirmationLevel, ConfirmationLevel } from '../confirmation/ConfirmationPolicy';

export { ConfirmationLevel } from '../confirmation/ConfirmationPolicy';

export interface PermissionService {
  canExecute(toolName: string, toolInput: Record<string, unknown>): Promise<boolean>;
  getConfirmationLevel(toolName: string): ConfirmationLevel;  // NEW
}

export class DefaultPermissionService implements PermissionService {
  async canExecute(toolName: string, _toolInput: Record<string, unknown>): Promise<boolean> {
    debugLog('info', `[PermissionService] Permission denied for tool: ${toolName}`);
    return false;
  }

  // NEW: Delegate to ConfirmationPolicy
  getConfirmationLevel(toolName: string): ConfirmationLevel {
    return getConfirmationLevel(toolName);
  }
}

export const permissionService = new DefaultPermissionService();
```

---

### `src/components/chat/BrandedHeader.tsx` [MODIFY] (component, presentation)

**Analog:** Same file — extend props interface (lines 1-63)

**Core pattern — add optional greeting props** (BrandedHeader.tsx lines 9-63):
```typescript
export interface BrandedHeaderProps {
  userGreeting?: string;       // NEW: e.g., "Good morning, George"
  contextualMessage?: string;  // NEW: e.g., "You're working on: INC0012345"
  onClose?: () => void;
}

export function BrandedHeader({ userGreeting, contextualMessage, onClose }: BrandedHeaderProps) {
  const { token } = theme.useToken();

  return (
    <Flex
      align="center"
      gap={token.marginSM}
      style={{
        padding: `${token.paddingSM}px ${token.padding}px`,
        borderBottom: `1px solid ${token.colorBorderSecondary}`,
        background: token.colorBgElevated,
      }}
    >
      {/* Bunny avatar — unchanged */}
      <Avatar size={32} icon={<BunnyAvatar />}
        style={{ border: `2px solid ${token.colorPrimary}`, flexShrink: 0 }} />

      {/* Name + tagline — now dynamic */}
      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', minWidth: 0 }}>
        <Text strong style={{ fontSize: 16, lineHeight: '22px' }}>
          {userGreeting || 'NowPilot'}
        </Text>
        <Text type="secondary" style={{ fontSize: 12, lineHeight: '16px' }}>
          {contextualMessage || 'Your AI work co-pilot'}
        </Text>
      </div>

      {/* Close button — unchanged */}
      <Tooltip title="Hide this header">
        <Button type="text" size="small" icon={<CloseOutlined />}
          onClick={onClose} aria-label="Hide this header" />
      </Tooltip>
    </Flex>
  );
}
```

---

### `src/components/chat/ChatMessage.tsx` [MODIFY] (component, presentation)

**Analog:** Same file — add BunnyAvatar on first message + promote Save-to-note button (lines 1-52)

**Core pattern — first-message branding** (ChatMessage.tsx):
```typescript
import { BunnyAvatar } from '../common/BunnyAvatar';

export interface ChatMessageProps {
  content: string;
  role: 'user' | 'assistant';
  streaming: boolean;
  onSaveToNote?: () => void;
  isFirstMessage?: boolean;  // NEW: D-28 first-message branding
  showSaveToNote?: boolean;  // NEW: D-29 / RICH-H-06 promote to visible
}

export function ChatMessage({
  content, role, streaming, onSaveToNote, isFirstMessage, showSaveToNote,
}: ChatMessageProps) {
  return (
    <Bubble
      placement={role === 'user' ? 'end' : 'start'}
      // NEW: First-message avatar for branding (D-28)
      avatar={isFirstMessage && role === 'assistant' ? (
        <div style={{ border: '2px solid var(--ant-color-primary)', borderRadius: '50%', width: 32, height: 32 }}>
          <BunnyAvatar />
        </div>
      ) : undefined}
      header={isFirstMessage && role === 'assistant' ? (
        <div style={{ fontWeight: 600, fontSize: 14, color: 'var(--ant-color-primary)' }}>NowPilot</div>
      ) : undefined}
      content={
        role === 'assistant' ? (
          <XMarkdown content={content}
            streaming={{ hasNextChunk: streaming, enableAnimation: true }}
            openLinksInNewTab={true} />
        ) : content
      }
      footer={
        role === 'assistant' && !streaming ? (
          <div style={{ display: 'flex', gap: 4, marginTop: 4 }}>
            {/* Save-to-note always visible when applicable (RICH-H-06) */}
            {onSaveToNote && (
              <Button type="text" size="small" icon={<SaveOutlined />} onClick={onSaveToNote}>
                Save to note
              </Button>
            )}
            {/* Structured output actions when content has tables */}
            <StructuredOutputActions
              content={content}
              hasTable={content.includes('| ---') || content.includes('|---')}
            />
          </div>
        ) : undefined
      }
    />
  );
}
```

---

## Shared Patterns

### Zustand Store + chrome.storage.local Persist

**Source:** `src/core/stores/workspaceStore.ts` (lines 41-127) + `src/core/stores/diagnosticsStore.ts` (lines 35-127)
**Apply to:** `RightPaneStore.ts`, all new Zustand stores

```typescript
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';

const chromeLocalStorage = createJSONStorage<YourState>(() => ({
  getItem: (name: string) =>
    chrome.storage.local.get(name).then((result: Record<string, unknown>) => (result[name] as string) ?? null),
  setItem: (name: string, value: string) => chrome.storage.local.set({ [name]: value }),
  removeItem: (name: string) => chrome.storage.local.remove(name),
}));

export const useYourStore = create<YourState>()(
  persist(
    (set) => ({
      // state + actions
    }),
    {
      name: 'np_your_key',
      storage: chromeLocalStorage,
      partialize: (state) => ({ /* persisted fields only */ }),
    },
  ),
);
```

**Key conventions:**
- Storage key prefix: `np_` (e.g., `np_right_pane`, `np_preferences`, `np_workspace`)
- Actions are arrow functions in the `set` callback
- `partialize` whitelist pattern preferred (diagnosticsStore.ts lines 121-124)
- No `WriteJournal` for lightweight stores — only workspaceStore uses it

### React Hook — Store Subscription Pattern

**Source:** `src/hooks/useWorkspace.ts` (lines 1-45)
**Apply to:** `useGreeting.ts`, `useConversationClosure.ts`

```typescript
import { useWorkspaceStore } from '../core/stores/workspaceStore';

export function useMyHook(): ReturnType {
  // Individual selectors — prevent re-renders
  const field1 = useWorkspaceStore((s) => s.field1);
  const field2 = useWorkspaceStore((s) => s.field2);
  // ...
  return { field1, field2 };
}
```

### antd Token-Based Styling

**Source:** All components (BrandedHeader.tsx lines 19-29, ClarificationAction.tsx lines 23-26)
**Apply to:** All new UI components

```typescript
import { theme } from 'antd';

function MyComponent() {
  const { token } = theme.useToken();
  return (
    <div style={{ padding: token.paddingSM, background: token.colorBgElevated, borderRadius: token.borderRadius }}>
      {/* ... */}
    </div>
  );
}
```

### ActionChipGroup Reuse

**Source:** `src/components/common/ActionChipGroup.tsx` (lines 1-70)
**Apply to:** `InlineConfirmationCard.tsx`, any component with selectable chips

```typescript
import { ActionChipGroup, type ChipAction } from '../common/ActionChipGroup';

const chips: ChipAction[] = [
  { key: 'action-1', label: 'Action 1', value: 'action-1' },
  { key: 'action-2', label: 'Action 2', value: 'action-2' },
];
<ActionChipGroup actions={chips} onSelect={handleSelect} variant="default" />
```

### Test Pattern

**Source:** `tests/components/chat/BrandedHeader.test.tsx` (lines 1-58) + `tests/core/workspaceStore.test.ts` (lines 1-198)
**Apply to:** All new test files

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import { ConfigProvider } from 'antd';

const renderWithAntd = (ui: React.ReactElement) =>
  render(<ConfigProvider>{ui}</ConfigProvider>);

describe('ComponentName', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('renders correctly', () => {
    renderWithAntd(<ComponentName />);
    // assertions
  });
});
```

**Store test pattern** (workspaceStore.test.ts):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { useYourStore } from '../../src/core/stores/YourStore';

describe('YourStore', () => {
  beforeEach(() => {
    useYourStore.setState({ /* reset to defaults */ });
  });

  it('default state is correct', () => {
    const state = useYourStore.getState();
    expect(state.field).toBe(defaultValue);
  });
});
```

### PersonaService getActiveProfile — Deep Merge Safety

**Source:** RESEARCH.md Pitfall #4 (lines 527-535)
**Apply to:** `PersonaService.ts` modifications

```typescript
// CORRECT: Explicit field-level assignment
getActiveProfile(): PersonaProfile {
  const prefs = usePreferenceStore.getState();
  const profile: PersonaProfile = {
    ...DEFAULT_PERSONA_PROFILE,
    identity: { ...DEFAULT_PERSONA_PROFILE.identity },
    languageStyle: { ...DEFAULT_PERSONA_PROFILE.languageStyle },
  };
  if (prefs.aiName) profile.identity.name = prefs.aiName;
  if (prefs.aiTone) profile.languageStyle.tone = prefs.aiTone;
  // ...
  return profile;
}

// WRONG: Shallow spread loses nested fields
// const profile = { ...DEFAULT_PERSONA_PROFILE, ...overrides };
```

### OnboardingModal Step Pattern

**Source:** `src/core/onboarding/OnboardingModal.tsx` (lines 395-520)
**Apply to:** `MeetNowPilotStep` integration

```typescript
{/* Step indicator circles */}
<div style={{ display: 'flex', justifyContent: 'center', gap: '4px', marginBottom: '16px' }}>
  {[1, 2, 3, 4, 5, 6, 7, 8, 9].map((num) => {
    const isChecked = num < step + 1;
    const isActive = num === step + 1;
    // ... circle rendering logic (mirrors lines 322-390)
  })}
</div>

{/* Step content */}
<div style={{ flex: 1, display: 'flex', flexDirection: 'column', justifyContent: 'center' }}>
  {step === 0 && <MeetNowPilotStep onContinue={() => setStep(1)} onSkip={handleComplete} />}
  {step === 1 && (/* existing provider selection */)}
  {/* ... rest of steps unchanged */}
</div>
```

**Step renumbering:** Old step 0 (Welcome) becomes step 0 (MeetNowPilot). Old step 1 (Provider) becomes step 1. Total steps: 9 (was 8). Adjust step circle array from `[1..8]` to `[1..9]`.

---

## No Analog Found

All files have suitable analogs in the existing codebase. No files require fallback to RESEARCH.md-only patterns.

---

## Metadata

**Analog search scope:** `src/core/stores/*.ts`, `src/hooks/*.ts`, `src/components/chat/*.tsx`, `src/components/standalone/*.tsx`, `src/components/common/*.tsx`, `src/components/agent/*.tsx`, `src/core/onboarding/*.tsx`, `src/core/prompts/*.ts`, `src/core/memory/*.ts`, `src/core/ai/persona/*.ts`, `src/core/ai/tools/*.ts`, `src/core/pages/*.tsx`, `tests/**/*.test.{ts,tsx}`
**Files scanned:** 40+ source files + 100+ test files
**Pattern extraction date:** 2026-07-19
**Key libraries in use:** antd 6.5.0, @ant-design/x 2.8.0, zustand 5.0.14, motion 12.42.2, zod (for schemas), vitest 4.1.0
