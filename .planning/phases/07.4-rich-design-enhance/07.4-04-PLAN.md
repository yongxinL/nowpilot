---
phase: 07.4-rich-design-enhance
plan: 04
type: execute
wave: 1
depends_on:
  - 07.4-01
files_modified:
  - src/components/onboarding/MeetNowPilotStep.tsx
  - src/components/chat/InlineConfirmationCard.tsx
  - src/components/chat/ConversationClosure.tsx
  - src/hooks/useConversationClosure.ts
  - src/components/chat/StructuredOutputActions.tsx
autonomous: true
requirements:
  - RICH-R-03
  - RICH-C-12
  - RICH-C-09
  - RICH-H-05

must_haves:
  truths:
    - "MeetNowPilotStep renders bunny avatar, persona identity (name, tagline, core values), 4 capability preview cards, Continue CTA, and subtle Skip link per D-09/D-10"
    - "InlineConfirmationCard renders warning-border card with actionDescription, rationale, ActionChipGroup [Proceed][Don't proceed] in pending state"
    - "InlineConfirmationCard transisions to collapsed green-check + actionSummary + spinner in executing state"
    - "ConversationClosure renders 'Did this help?' text + thumbs up/down buttons with aria-labels"
    - "useConversationClosure manages 12s idle timer, resets on new messages, shows closure once per conversation per D-25/D-26"
    - "StructuredOutputActions detects markdown tables (pipe/separator-row) and renders Copy as table + Export CSV buttons"
  artifacts:
    - src/components/onboarding/MeetNowPilotStep.tsx
    - src/components/chat/InlineConfirmationCard.tsx
    - src/components/chat/ConversationClosure.tsx
    - src/hooks/useConversationClosure.ts
    - src/components/chat/StructuredOutputActions.tsx
  key_links:
    - "InlineConfirmationCard consumes ConfirmationLevel from Plan 01's ConfirmationPolicy"
    - "ConversationClosure → ChatPage (Plan 07) integration"
    - "MeetNowPilotStep → OnboardingModal (Plan 05) integration"
---

<objective>
Create the stateless chat-interaction widget components: MeetNowPilotStep for onboarding persona introduction, InlineConfirmationCard for chat-mode side-effect confirmations, ConversationClosure for end-of-conversation feedback, useConversationClosure hook for idle-timer management, and StructuredOutputActions for table export actions.

Purpose: Build all reusable chat-surface UI components that Plan 07 (ChatPage Integration) will wire together.

Output: 5 new files — MeetNowPilotStep, InlineConfirmationCard, ConversationClosure, useConversationClosure, StructuredOutputActions
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md
@.planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md
@.planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md
@.planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md
@src/components/chat/ClarificationAction.tsx
@src/components/chat/FollowUpAction.tsx
@src/components/common/ActionChipGroup.tsx
@src/components/common/BunnyAvatar.tsx
@src/core/onboarding/OnboardingModal.tsx
@src/hooks/useWorkspace.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create InlineConfirmationCard + ConversationClosure + useConversationClosure</name>
  <files>src/components/chat/InlineConfirmationCard.tsx, src/components/chat/ConversationClosure.tsx, src/hooks/useConversationClosure.ts</files>
  <read_first>
    - src/components/chat/ClarificationAction.tsx — card-with-description + ActionChipGroup pattern (lines 1-48)
    - src/components/chat/FollowUpAction.tsx — divider + chips pattern (lines 1-81)
    - src/components/common/ActionChipGroup.tsx — shared chip container (ChipAction type, variant support)
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 339-472 — exact component patterns
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-17 through D-27 — locked decisions
    - .planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md — interaction contracts, copywriting
  </read_first>
  <behavior>
    - Test 1: InlineConfirmationCard pending state renders warning-border card with actionDescription, rationale, ActionChipGroup [Proceed][Don't proceed]
    - Test 2: Clicking Proceed calls onProceed; card transitions to executing state with green check icon + actionSummary + Spin
    - Test 3: Clicking Don't proceed calls onCancel; card shows "Action cancelled." text
    - Test 4: ConversationClosure renders "Did this help?" + thumbs up/down buttons with correct aria-labels
    - Test 5: Clicking feedback button calls onFeedback with boolean; component shows "Thanks for your feedback!"
    - Test 6: useConversationClosure returns showPrompt=false when isStreaming=true or messagesLength=0
    - Test 7: useConversationClosure returns showPrompt=true after 12s idle (messagesLength>0, !isStreaming, !hasActiveClarifications)
    - Test 8: useConversationClosure returns showPrompt=false after dismiss() called; never shows again for same conversation
  </behavior>
  <action>
    Per D-17 through D-27:

    **InlineConfirmationCard.tsx** — Render per PATTERNS.md lines 339-417:
    - Props: actionDescription, rationale?, onProceed, onCancel, state ('pending'|'executing'|'completed'|'cancelled'), actionSummary?
    - States: 'cancelled' → "Action cancelled." Text type="secondary". 'executing'/'completed' → collapsed Flex: CheckCircleOutlined (token.colorSuccess) + actionSummary + Spin (if executing). 'pending' → warning-border card (token.colorWarningBorder, token.colorWarningBg, borderRadiusLG) with actionDescription (Text strong, 13px), rationale (Text secondary, 12px with opacity 0.85), and ActionChipGroup (variant="default") with [Proceed] and [Don't proceed] chips
    - Per D-17: distinct from Agent PermissionDialog — chat uses lighter-weight inline card; no "remember" checkbox per D-19

    **ConversationClosure.tsx** — Render per PATTERNS.md lines 422-473:
    - Props: onFeedback: (helpful: boolean) => void
    - Flex row with borderTop divider (token.colorBorderSecondary), "Did this help?" text, two icon-only buttons: Button type="text" size="small" with LikeOutlined (aria-label="This was helpful") and DislikeOutlined (aria-label="This was not helpful")
    - Per UI-SPEC accessibility: emoji-only buttons require aria-labels

    **useConversationClosure.ts** — Render per PATTERNS.md lines 282-333:
    - Params: messagesLength, isStreaming, hasActiveClarifications
    - Returns: { showPrompt, isComplete, dismiss }
    - Manages idle timer via useRef<setTimeout>: 12s idle after stream completion + no pending clarifications → showPrompt=true. Timer resets on new messages/stream. AlreadyShown flag prevents re-show. Cleanup via useEffect return.
  </action>
  <verify>
    <automated>pnpm test tests/components/chat/InlineConfirmationCard.test.tsx tests/hooks/useConversationClosure.test.ts</automated>
  </verify>
  <done>InlineConfirmationCard renders all 4 states correctly with Proceed/Cancel chips; ConversationClosure shows feedback UI with correct aria-labels; useConversationClosure manages 12s idle timer with once-per-conversation gating</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MeetNowPilotStep onboarding persona introduction component</name>
  <files>src/components/onboarding/MeetNowPilotStep.tsx</files>
  <read_first>
    - src/core/onboarding/OnboardingModal.tsx — step pattern (step circles at lines 321-391, step 0 content at lines 395-429, CTA button styles), existing Welcome step structure
    - src/components/common/BunnyAvatar.tsx — bunny avatar component
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 605-690 — MeetNowPilotStep exact component pattern
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-09, D-10, D-11 — locked persona onboarding decisions
    - .planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md — copywriting contract, typography contract (24px title, 16px heading)
  </read_first>
  <behavior>
    - Test 1: MeetNowPilotStep renders bunny avatar (64px, primary-color border ring)
    - Test 2: Renders persona name "Meet NowPilot" as 24px title and tagline "Your AI work co-pilot" as subtitle
    - Test 3: Renders 4 core value badges: Privacy-first, Helpful, Precise, Humble
    - Test 4: Renders 4 capability preview cards: Summarize Pages, Research Topics, Draft Responses, Explain Code
    - Test 5: Continue button (full width, #e0582e background, white text, borderRadius 10) calls onContinue
    - Test 6: Skip link (transparent, colorTextQuaternary) calls onSkip per D-10
  </behavior>
  <action>
    Per D-09, D-10:

    Create MeetNowPilotStep following PATTERNS.md lines 610-689 exact pattern:
    - Props: { onContinue: () => void, onSkip: () => void }
    - Use theme.useToken() for styling tokens
    - Layout: centered text, padding token.padding
    - Bunny avatar: Avatar size=64 icon=BunnyAvatar, 2px solid primary border, marginBottom
    - Identity: Title level={3} "Meet NowPilot" (24px per UI-SPEC title contract), tagline Text type="secondary"
    - Core values: Flex wrap justify="center" gap, 4 value badges (Privacy-first, Helpful, Precise, Humble) as span elements with 12px borderRadius, background colorFillSecondary, fontSize 11
    - Capability preview: 2x2 CSS grid, 4 cards each with title (12px strong) + description (11px secondary), padding token.paddingXS, borderRadius, border colorBorderSecondary, background colorBgLayout
    - CTA buttons: Continue button — full width, background '#e0582e', color '#fff', padding '10px 16px', borderRadius 10, fontWeight bold, fontSize 14, border none, cursor pointer. Skip button — transparent background, border none, color token.colorTextQuaternary, fontSize 13, cursor pointer
    - Match OnboardingModal existing CTA button styling pattern (inline styles, antd theme.useToken)
    - Per D-10: Skip link visible by default; Continue remains primary CTA
  </action>
  <verify>
    <automated>pnpm test tests/components/onboarding/OnboardingModal.test.tsx</automated>
  </verify>
  <done>MeetNowPilotStep renders bunny avatar, persona identity, 4 core values, 4 capability cards, Continue CTA, and Skip link; styling matches OnboardingModal pattern with inline styles + antd tokens</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create StructuredOutputActions for table export</name>
  <files>src/components/chat/StructuredOutputActions.tsx</files>
  <read_first>
    - src/components/chat/ChatMessage.tsx — Bubble footer button pattern (lines 41-49)
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 554-601 — StructuredOutputActions exact pattern
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md — agent discretion on RICH-H-05 implementation
  </read_first>
  <behavior>
    - Test 1: When hasTable=true, renders "Copy as table" and "Export CSV" buttons
    - Test 2: When hasTable=false, returns null (no render)
    - Test 3: Clicking "Copy as table" copies content to clipboard and shows success message
    - Test 4: Clicking "Export CSV" downloads CSV file with correct content derived from markdown table rows
  </behavior>
  <action>
    Per RICH-H-05 and agent's discretion:

    Create StructuredOutputActions following PATTERNS.md lines 560-601:
    - Props: { content: string, hasTable: boolean }
    - Detects markdown tables via content.includes('| ---') || content.includes('|---')
    - Copy as table: navigator.clipboard.writeText(content) → antd message.success('Table copied to clipboard')
    - Export CSV: parse markdown table rows (lines starting with '|'), convert to CSV with quoted cells, create Blob (type: 'text/csv'), trigger download via temporary anchor element
    - Render: Flex gap={4} with two Button type="text" size="small" elements with CopyOutlined and TableOutlined icons
    - Only render when hasTable is true
  </action>
  <verify>
    <automated>pnpm test tests/components/chat/StructuredOutputActions.test.tsx</automated>
  </verify>
  <done>StructuredOutputActions renders Copy/Export buttons when markdown table detected; clipboard copy works; CSV export downloads correctly; returns null when no table</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InlineConfirmationCard Proceed/Cancel → tool execution | Proceed triggers side-effect tool; Cancel prevents execution per D-19 (no persistent memory for chat confirmations) |
| StructuredOutputActions CSV → file download | CSV generated from AI response content; downloaded via Blob + temporary anchor — no server interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.4-06 | Tampering | InlineConfirmationCard state machine | low | accept | All 4 states (pending/executing/completed/cancelled) handled explicitly; no unhandled transitions; onCancel clears the card immediately |
| T-07.4-07 | Information Disclosure | ConversationClosure feedback | low | accept | D-27: feedback stored as anonymized conversation-level telemetry — no user-identifiable data in feedback payload |
</threat_model>

<verification>
## Wave 0 Requirements
- tests/components/chat/InlineConfirmationCard.test.tsx — all 4 states, Proceed/Cancel callbacks
- tests/components/chat/ConversationClosure.test.tsx — feedback callbacks, aria-labels
- tests/hooks/useConversationClosure.test.ts — idle timer, once-per-conversation gating
- tests/components/onboarding/OnboardingModal.test.tsx — MeetNowPilotStep rendering (extend existing)
- tests/components/chat/StructuredOutputActions.test.tsx — table detection, copy/export
</verification>

<success_criteria>
- [ ] InlineConfirmationCard handles all 4 states with correct visual treatment
- [ ] ConversationClosure renders with correct aria-labels and once-per-conversation behavior
- [ ] MeetNowPilotStep matches UI-SPEC and OnboardingModal styling patterns
- [ ] StructuredOutputActions detects markdown tables and provides Copy/Export actions
- [ ] All components use antd theme tokens; typography matches 14px/16px contract
</success_criteria>

<output>
Create `.planning/phases/07.4-rich-design-enhance/07.4-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `MeetNowPilotStep` | component | MeetNowPilotStep.tsx | Persona intro card for onboarding Step 1 (D-09) |
| `InlineConfirmationCard` | component | InlineConfirmationCard.tsx | Chat-mode inline confirmation with Proceed/Cancel (D-17) |
| `InlineConfirmationCardProps` | interface | InlineConfirmationCard.tsx | Props for actionDescription, rationale, state, callbacks |
| `ConversationClosure` | component | ConversationClosure.tsx | "Did this help?" feedback bar (D-25) |
| `useConversationClosure` | hook | useConversationClosure.ts | 12s idle-timer management (D-26) |
| `ClosureState` | interface | useConversationClosure.ts | showPrompt, isComplete, dismiss |
| `StructuredOutputActions` | component | StructuredOutputActions.tsx | Copy as table + Export CSV for markdown tables (RICH-H-05) |
