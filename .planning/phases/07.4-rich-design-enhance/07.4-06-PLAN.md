---
phase: 07.4-rich-design-enhance
plan: 06
type: execute
wave: 2
depends_on:
  - 07.4-02
files_modified:
  - src/components/standalone/StandaloneRoot.tsx
autonomous: true
requirements:
  - RICH-H-11
  - RICH-H-12

must_haves:
  truths:
    - "StandaloneRoot StandaloneContentWrapper adds right pane column between chat content and right edge per D-01"
    - "Right pane visible by default on Chat/Agent pages; hidden on Options/Diagnostics per D-08"
    - "Right pane width toggles between compact (320px) and expanded (45%) via RightPaneStore"
    - "Drawer overlay appears when available width < 720px (320px pane + 400px min chat) per D-04"
    - "Collapse state and active tab persist across sessions via RightPaneStore (D-02, D-06)"
  artifacts:
    - "Modified StandaloneRoot.tsx — three-column flex layout with right pane + responsive Drawer degradation"
  key_links:
    - "RightPane (Plan 02) + useRightPaneStore → StandaloneContentWrapper"
    - "ResizeObserver on content container for responsive breakpoint (RESEARCH.md Pitfall 5)"
---

<objective>
Integrate the right pane system into StandaloneRoot: add a third flex column between the existing StandaloneContentWrapper and the right edge, wire RightPane visibility/width from RightPaneStore, implement responsive Drawer degradation at the 720px breakpoint, and set page-availability rules (visible on Chat/Agent, hidden on Options/Diagnostics per D-08).

Purpose: Deliver the split-pane layout (RICH-H-11) with persistent collapsible right pane (RICH-H-12) to the standalone Full App surface.

Output: 1 file modified — StandaloneRoot.tsx (~60 lines added to StandaloneContentWrapper)
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md
@.planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md
@.planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md
@.planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md
@src/components/standalone/StandaloneRoot.tsx
@src/components/standalone/RightPane.tsx
@src/components/standalone/PaneToggle.tsx
@src/core/stores/RightPaneStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add right pane column to StandaloneContentWrapper with responsive Drawer degradation</name>
  <files>src/components/standalone/StandaloneRoot.tsx</files>
  <read_first>
    - src/components/standalone/StandaloneRoot.tsx — current StandaloneContentWrapper (lines 84-128, single flex column)
    - src/components/standalone/RightPane.tsx — RightPane component from Plan 02
    - src/components/standalone/PaneToggle.tsx — PaneToggle from Plan 02
    - src/core/stores/RightPaneStore.ts — useRightPaneStore hook
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 694-722 — StandaloneRoot three-column pattern
    - .planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md Pitfall 5 (lines 537-545) — breakpoint calculation
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-01 through D-08 — locked split-pane decisions
  </read_first>
  <behavior>
    - Test 1: StandaloneContentWrapper renders chat content + right pane in horizontal flex when visible=true and width>breakpoint
    - Test 2: Right pane width is 320px when RightPaneStore.width='compact'
    - Test 3: Right pane width is 45% of container width when RightPaneStore.width='expanded'
    - Test 4: When container width < 720px, right pane renders as antd Drawer overlay instead of inline column
    - Test 5: Right pane hidden when activeId is 'options' or 'diagnostics' per D-08
    - Test 6: Chat content column has margin right removed when right pane is visible (was margin: '12px 12px 12px 0')
    - Test 7: PaneToggle renders between chat content and right pane
  </behavior>
  <action>
    Per D-01 through D-08 and RESEARCH.md Pitfall 5:

    **StandaloneRoot.tsx** — Modify StandaloneContentWrapper function:

    - Import useRightPaneStore from ../../core/stores/RightPaneStore
    - Import RightPane from ./RightPane, PaneToggle from ./PaneToggle
    - Import Drawer from antd
    - Add useRef + ResizeObserver to track container width (not window.innerWidth per Pitfall 5)

    Container width calculation (Pitfall 5 — exact formula):
    - Use ResizeObserver on the outer wrapper div
    - Breakpoint: containerWidth < 720 (320px right pane compact + 400px minimum chat area)
    - When containerWidth < 720 → render right pane as antd Drawer (placement="right", size=320). Drawer open state mirrors RightPaneStore.visible.
    - When containerWidth >= 720 → render right pane as inline flex column

    Layout structure (per PATTERNS.md lines 709-721):
    - Outer div: flex 1, display flex, minWidth 0 (replaces current single-column flex)
    - Left column (existing): flex 1, minWidth 0, display flex, flexDirection column, borderRadius 16, overflow hidden, background colorBgContainer, boxShadow, margin: '12px 0 12px 0' when right pane visible (remove right margin), OR original margin when right pane hidden
    - PaneToggle: rendered as divider between left column and right pane (only when visible && width >= breakpoint)
    - Right column: when visible && width >= breakpoint → RightPane component with width from RightPaneStore

    Page availability (D-08):
    - Track which pages show right pane: Chat and Agent pages → visible. Options and Diagnostics pages → hidden.
    - Use activeId prop to determine: ['chat', 'agent'].includes(activeId) → pane available; else → pane hidden
    - When navigating away from Chat/Agent → RightPaneStore.setVisible(false). When returning → restore.

    Default visibility:
    - Right pane visible by default on Chat/Agent pages per D-02
    - Never auto-show/hide based on content availability per D-02
  </action>
  <verify>
    <automated>pnpm test tests/components/standalone/StandaloneRoot.test.tsx</automated>
  </verify>
  <done>StandaloneRoot renders three-column layout with right pane; compact=320px, expanded=45%; Drawer overlay below 720px breakpoint; right pane hidden on Options/Diagnostics per D-08; PaneToggle divider renders between columns</done>
</task>

<task type="auto">
  <name>Task 2: Verify right pane Integration and responsive behavior end-to-end</name>
  <files>src/components/standalone/StandaloneRoot.tsx</files>
  <read_first>
    - .planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md — Split-Pane Behavior interaction contract (lines 242-254)
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-01 through D-08 — verify all decisions implemented
  </read_first>
  <behavior>
    - Verify 1: Window resize below 720px triggers Drawer; above 720px restores inline column
    - Verify 2: Tab click switches active tab and persists selection
    - Verify 3: Collapse state persists across page navigation (Chat→Agent→Chat)
    - Verify 4: Right pane auto-switches only on explicit user action (opening wikilink note, clicking pinned tab) — never on background AI activity (D-06)
  </behavior>
  <action>
    Final integration verification:
    - Confirm all D-01 through D-08 decisions are satisfied
    - Confirm ResizeObserver cleanup on unmount
    - Confirm Drawer onClose calls RightPaneStore.setVisible(false), onOpen calls setVisible(true) — two-way binding
    - Confirm no right margin on chat content when pane visible — visual polish
    - Add debugLog entries for key state transitions (pane show/hide, breakpoint cross, Drawer toggle)
  </action>
  <verify>
    <automated>pnpm test tests/components/standalone/StandaloneRoot.test.tsx</automated>
  </verify>
  <done>All D-01 through D-08 satisfied; ResizeObserver cleaned up; Drawer two-way binding correct; responsive breakpoint working; pane visibility rules enforced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StandaloneRoot → RightPaneStore | Reads pane UI state (visible, width, activeTab); writes via setVisible on page navigation |
| ResizeObserver → Drawer toggle | DOM measurement only — no data exposure |

## STRIDE Threat Register

None — layout-only changes with no new data flows or input surfaces.
</threat_model>

<verification>
## Wave 0 Requirements
- tests/components/standalone/StandaloneRoot.test.tsx — three-column layout, width presets, responsive Drawer, page availability
</verification>

<success_criteria>
- [ ] Three-column flex layout renders correctly with right pane
- [ ] Compact (320px) and expanded (45%) widths work via toggle
- [ ] Drawer overlay activates below 720px container width
- [ ] Right pane hidden on Options/Diagnostics per D-08
- [ ] PaneToggle divider renders between columns
- [ ] Chat content margin adjusts correctly when pane visible
</success_criteria>

<output>
Create `.planning/phases/07.4-rich-design-enhance/07.4-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| Right pane column (layout) | DOM structure | StandaloneRoot.tsx | Third flex column in StandaloneContentWrapper |
| ResizeObserver + Drawer fallback | logic | StandaloneRoot.tsx | Responsive degradation at 720px breakpoint |
| Page availability gating | logic | StandaloneRoot.tsx | Pane visible on Chat/Agent, hidden on Options/Diagnostics |
