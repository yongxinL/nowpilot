---
phase: 07.4-rich-design-enhance
plan: 07
type: execute
wave: 4
depends_on:
  - 07.4-03
  - 07.4-04
  - 07.4-05
files_modified:
  - src/core/pages/ChatPage.tsx
autonomous: true
requirements:
  - RICH-C-14
  - RICH-I-10
  - RICH-H-16
  - RICH-C-12
  - RICH-C-09
  - RICH-H-05
  - RICH-I-03

must_haves:
  truths:
    - "ChatPage empty state renders BrandedHeader with personalized greeting from useGreeting hook + WelcomeCards below per D-23"
    - "Sender prefix area shows TemplateBrowser icon button with popover of categorized templates per D-13"
    - "Sender onPasteFile handler appends clipboard images to existing attachedFiles state; respects 5-file limit per D-31"
    - "InlineConfirmationCard renders in Bubble when tool metadata.confirmation is present per D-17"
    - "ConversationClosure renders after 12s idle once per conversation per D-25"
    - "ChatMessage receives isFirstMessage flag for first-message branding per D-28"
    - "StructuredOutputActions renders in Bubble footer for messages with markdown tables"
  artifacts:
    - "Modified ChatPage.tsx — 6 integration points: greeting, template browser, image paste, inline confirmation, conversation closure, structured output"
  key_links:
    - "useGreeting → BrandedHeader userGreeting + contextualMessage props"
    - "TemplateBrowser onInsert → setDraft callback"
    - "Sender onPasteFile → setAttachedFiles existing pipeline"
    - "InlineConfirmationCard + ConversationClosure → Bubble rendering area"
    - "StructuredOutputActions → ChatMessage footer (via ChatMessage.tsx modified in Plan 05)"
---

<objective>
Wire all Wave 1-2 chat components into ChatPage.tsx: integrate personalized greeting into the empty-state BrandedHeader, add TemplateBrowser icon as Sender prefix, add onPasteFile handler for clipboard image paste, render InlineConfirmationCard in Bubbles when tool confirmations are present, render ConversationClosure after idle timer, pass isFirstMessage to ChatMessage for first-message branding, and ensure StructuredOutputActions render in Bubble footers.

Purpose: Complete the ChatPage surface with all 6 RICH P1 enhancements — this is the primary integration plan that delivers the visible user experience.

Output: 1 file modified — ChatPage.tsx (~100 lines added across 6 integration points)
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md
@.planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md
@.planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md
@.planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md
@src/core/pages/ChatPage.tsx
@src/components/chat/BrandedHeader.tsx
@src/components/chat/ChatMessage.tsx
@src/components/chat/TemplateBrowser.tsx
@src/components/chat/InlineConfirmationCard.tsx
@src/components/chat/ConversationClosure.tsx
@src/hooks/useGreeting.ts
@src/hooks/useConversationClosure.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate personalized greeting + TemplateBrowser + image paste into ChatPage Sender area</name>
  <files>src/core/pages/ChatPage.tsx</files>
  <read_first>
    - src/core/pages/ChatPage.tsx — empty state (BrandedHeader + WelcomeCards rendering), Sender area (lines 1839-2054), attachedFiles state management, attachment pipeline
    - src/components/chat/BrandedHeader.tsx — updated with userGreeting/contextualMessage props (Plan 05)
    - src/components/chat/TemplateBrowser.tsx — onInsert callback interface (Plan 03)
    - src/hooks/useGreeting.ts — returns Greeting object (Plan 03)
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 726-806 — ChatPage integration patterns
    - .planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md lines 549-603 — image paste integration code example
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-21 through D-24, D-30 through D-33
    - .planning/phases/07.4-rich-design-enhance/07.4-UI-SPEC.md — Image Paste interaction contract (lines 278-288), Personalized Greeting contract (lines 290-300)
  </read_first>
  <behavior>
    - Test 1: Empty state BrandedHeader receives userGreeting and contextualMessage from useGreeting hook
    - Test 2: BrandedHeader greets with time-of-day + displayName when set; falls back to generic when not set
    - Test 3: WelcomeCards render below BrandedHeader per D-23; dismissing WelcomeCards does NOT remove greeting
    - Test 4: Sender prefix area renders TemplateBrowser component with onInsert callback updating draft
    - Test 5: Sender onPasteFile handler appends image files to attachedFiles state (up to 5 max)
    - Test 6: Pasted image thumbnails appear in attachment strip; focus stays in Sender per D-32
    - Test 7: Sender onPasteFile uses existing attachment pipeline — no separate paste pipeline per RESEARCH.md anti-pattern
  </behavior>
  <action>
    Per D-21 through D-24 and D-30 through D-33:

    **Imports to add:**
    - import { useGreeting } from '../../hooks/useGreeting'
    - import { TemplateBrowser } from '../../components/chat/TemplateBrowser'

    **Empty state integration (greeting):**
    - Call const greeting = useGreeting() at component level
    - In empty state area (around lines 1217-1246): pass userGreeting={greeting.greeting} and contextualMessage={greeting.contextualLine} to BrandedHeader
    - Keep existing WelcomeCards rendering below BrandedHeader — independent components per D-23
    - Per D-23: when WelcomeCards are dismissed, greeting remains visible in BrandedHeader

    **Sender prefix (TemplateBrowser):**
    - Add prefix prop to Sender: prefix={<TemplateBrowser onInsert={(tpl) => setDraft((prev) => prev + tpl)} />}
    - Per PATTERNS.md line 769-773 pattern
    - TemplateBrowser sits in Sender prefix area (left side of Sender toolbar) per RESEARCH.md state-of-the-art note

    **Sender onPasteFile (image paste):**
    - Add onPasteFile prop to Sender per RESEARCH.md code example lines 585-602:
      - Filter Array.from(files) for f.type.startsWith('image/')
      - Enforce existing limit: const remaining = 5 - attachedFiles.length; const toAdd = imageFiles.slice(0, remaining)
      - Excess silently dropped with debugLog per D-31
      - Map to attachment objects: { id: crypto.randomUUID(), name: file.name || `Pasted-${Date.now()}.png`, url: URL.createObjectURL(file), type: file.type, file }
      - setAttachedFiles((prev) => [...prev, ...newAttachments])
    - Feed into EXISTING attachedFiles state and attachmentHeader — per RESEARCH.md anti-pattern prohibition (do NOT create separate paste pipeline)
    - Per D-33: pasted images go through same resize pipeline as drag-drop (max 800px, JPEG quality 0.85)
  </action>
  <verify>
    <automated>pnpm test tests/core/pages/ChatPage.test.tsx</automated>
  </verify>
  <done>Empty state renders personalized BrandedHeader with greeting + WelcomeCards below; Sender prefix shows TemplateBrowser icon; onPasteFile appends clipboard images to attachment strip respecting 5-file limit</done>
</task>

<task type="auto">
  <name>Task 2: Integrate InlineConfirmationCard + ConversationClosure + first-message branding into ChatPage Bubble area</name>
  <files>src/core/pages/ChatPage.tsx</files>
  <read_first>
    - src/core/pages/ChatPage.tsx — Bubble list rendering area, assistant message metadata, conversation state management
    - src/components/chat/InlineConfirmationCard.tsx — component with state machine (Plan 04)
    - src/components/chat/ConversationClosure.tsx — feedback component (Plan 04)
    - src/components/chat/ChatMessage.tsx — updated with isFirstMessage prop (Plan 05)
    - src/hooks/useConversationClosure.ts — returns showPrompt/isComplete/dismiss (Plan 04)
    - src/components/chat/StructuredOutputActions.tsx — table actions (Plan 04)
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 792-806 — Bubble integration patterns
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-17 through D-20 (inline confirmation), D-25 through D-27 (conversation closure)
  </read_first>
  <behavior>
    - Test 1: When Bubble metadata.confirmation present, InlineConfirmationCard renders inside Bubble with actionDescription + Proceed/Cancel
    - Test 2: Clicking Proceed transitions card to executing state (collapsed + spinner); Cancel removes card with "Action cancelled." text
    - Test 3: ConversationClosure appears below last assistant Bubble after 12s idle and renders feedback buttons
    - Test 4: ConversationClosure hidden when user sends new message
    - Test 5: First assistant message in conversation receives isFirstMessage=true prop on ChatMessage
    - Test 6: StructuredOutputActions renders for messages with markdown tables (handled by ChatMessage.tsx — verify integration)
  </behavior>
  <action>
    Per D-17 through D-20 and D-25 through D-27:

    **Imports to add:**
    - import { InlineConfirmationCard } from '../../components/chat/InlineConfirmationCard'
    - import { ConversationClosure } from '../../components/chat/ConversationClosure'
    - import { useConversationClosure } from '../../hooks/useConversationClosure'

    **InlineConfirmationCard integration:**
    - In Bubble rendering loop: after assistant Bubble, check if bubble.metadata?.confirmation exists
    - If present, render InlineConfirmationCard below the Bubble with:
      - actionDescription={bubble.metadata.confirmation.description}
      - rationale={bubble.metadata.confirmation.rationale}
      - onProceed/onCancel: dispatch tool execution or cancellation
      - state: driven by tool execution lifecycle (pending→executing→completed/cancelled)
      - actionSummary={bubble.metadata.confirmation.summary}
    - Per D-17: only in Chat mode; Agent mode uses PermissionDialog modal (unchanged)

    **ConversationClosure integration:**
    - Call useConversationClosure(messages.length, isStreaming, hasActiveClarifications) at component level
    - When closureState.showPrompt is true, render ConversationClosure below the last assistant Bubble
    - Pass onFeedback: log to conversation metadata per D-27
    - Per D-26: timer resets on new message/stream activity; at most once per conversation per D-25

    **First-message branding:**
    - Track whether each assistant message is the first in the conversation
    - Pass isFirstMessage={messageIndex === 0} to ChatMessage for first assistant message
    - Per D-28: subsequent assistant messages rendered normally (no branding)

    **StructuredOutputActions:**
    - Already integrated in ChatMessage.tsx (Plan 05 Task 3) — verify ChatMessage receives correct props
    - ChatPage passes onSaveToNote callback through to ChatMessage (already existing behavior, now with promoted visibility per Plan 05)
  </action>
  <verify>
    <automated>pnpm test tests/core/pages/ChatPage.test.tsx</automated>
  </verify>
  <done>InlineConfirmationCard renders in Bubble when tool confirmation present; Proceed/Cancel transitions work; ConversationClosure appears after idle with feedback buttons; first assistant message branded; StructuredOutputActions renders for table content</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clipboard paste → attachedFiles | Image data from ClipboardEvent via Sender onPasteFile; files processed through existing resize pipeline (800px max, JPEG 0.85) |
| InlineConfirmationCard Proceed → tool execution | Per D-19: no persistent memory for chat confirmations; one-time, per-action only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.4-09 | Information Disclosure | Sender onPasteFile | low | accept | Sender onPasteFile uses ClipboardEvent.clipboardData.files (paste event only, no persistent clipboard permission); extension does not request clipboard-read permission |
| T-07.4-10 | Tampering | InlineConfirmationCard state machine | low | accept | All 4 states handled explicitly; Proceed/Cancel transitions are one-way; no persistent memory for chat confirmations per D-19 |
</threat_model>

<verification>
## Wave 0 Requirements
- tests/core/pages/ChatPage.test.tsx — extend with greeting integration, TemplateBrowser prefix, image paste handler, InlineConfirmationCard rendering, ConversationClosure rendering, first-message branding
</verification>

<success_criteria>
- [ ] Empty state shows personalized BrandedHeader greeting + WelcomeCards
- [ ] Sender prefix shows TemplateBrowser with working template insertion
- [ ] Image paste adds thumbnails to attachment strip respecting 5-file limit
- [ ] InlineConfirmationCard renders in Bubbles with working Proceed/Cancel
- [ ] ConversationClosure appears after 12s idle with feedback buttons
- [ ] First assistant message branded; subsequent messages normal
- [ ] StructuredOutputActions renders for table content
- [ ] Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/07.4-rich-design-enhance/07.4-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `useGreeting()` call | hook integration | ChatPage.tsx | Wires personalized greeting to BrandedHeader empty state |
| `TemplateBrowser` prefix | component integration | ChatPage.tsx | Template popover in Sender toolbar (RICH-I-10) |
| `onPasteFile` handler | event handler | ChatPage.tsx | Clipboard image paste → attachment pipeline (RICH-H-16) |
| `InlineConfirmationCard` rendering | component integration | ChatPage.tsx | Chat-mode side-effect confirmation (RICH-C-12) |
| `ConversationClosure` rendering | component integration | ChatPage.tsx | End-of-conversation feedback (RICH-C-09) |
| `isFirstMessage` pass-through | prop wiring | ChatPage.tsx | First-message branding on ChatMessage (RICH-H-02) |
