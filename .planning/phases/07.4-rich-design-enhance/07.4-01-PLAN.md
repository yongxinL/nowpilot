---
phase: 07.4-rich-design-enhance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/memory/memoryTypes.ts
  - src/core/memory/PreferenceMemoryStore.ts
  - src/core/ai/persona/PersonaService.ts
  - src/core/ai/tools/PermissionService.ts
  - src/core/confirmation/ConfirmationPolicy.ts
autonomous: true
requirements:
  - RICH-R-05
  - RICH-R-06
  - RICH-R-08
  - RICH-R-09
  - RICH-R-10
  - RICH-C-13

must_haves:
  truths:
    - "PreferenceMemoryStore accepts displayName, aiName, aiTone, responseBrevity optional fields (all undefined by default)"
    - "PersonaService.getActiveProfile() merges PreferenceMemoryStore overrides onto DEFAULT_PERSONA_PROFILE with explicit field-level assignment"
    - "ConfirmationPolicy maps tool names to ConfirmationLevel (AutoExecute/ConfirmInline/RequirePermission)"
    - "PermissionService interface extends with getConfirmationLevel(toolName)"
    - "Existing RICH-R-06/R-08/R-09/R-10 remain handled by Phase 7.3 PersonaInjector — no regressions introduced"
  artifacts:
    - src/core/confirmation/ConfirmationPolicy.ts
    - "ConfirmationLevel enum exported via PermissionService.ts"
    - "getActiveProfile method on PersonaService class"
  key_links:
    - "PreferenceMemoryStore → PersonaService.getActiveProfile() (vanilla getState())"
    - "ConfirmationPolicy → PermissionService.getConfirmationLevel()"
---

<objective>
Extend core data-layer types and services for Phase 7.4: add persona/displayName fields to PreferenceMemoryStore and preferenceSchema, implement PersonaService.getActiveProfile() with deep-merge of preference overrides onto DEFAULT_PERSONA_PROFILE, and introduce ConfirmationLevel tiered permission model via ConfirmationPolicy + PermissionService extension.

Purpose: Establish data contracts that all Wave 2/3 integration plans depend on — persona identity overrides, user display name, and chat-mode inline-confirmation gating.

Output: 5 files modified/created: memoryTypes.ts (4 new optional fields in preferenceSchema), PreferenceMemoryStore.ts (4 new store fields + validation gate pass-through), PersonaService.ts (getActiveProfile method), PermissionService.ts (ConfirmationLevel re-export + getConfirmationLevel), ConfirmationPolicy.ts (new — static tool-to-level mapping)
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS_RICH_DESIGN.md
@.planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md
@.planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md
@.planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md
@src/core/memory/memoryTypes.ts
@src/core/memory/PreferenceMemoryStore.ts
@src/core/ai/persona/PersonaService.ts
@src/core/ai/persona/PersonaProfile.ts
@src/core/ai/tools/PermissionService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PreferenceMemoryStore + preferenceSchema with persona/displayName fields</name>
  <files>src/core/memory/memoryTypes.ts, src/core/memory/PreferenceMemoryStore.ts</files>
  <read_first>
    - src/core/memory/memoryTypes.ts — current preferenceSchema (6 fields at lines 84-91)
    - src/core/memory/PreferenceMemoryStore.ts — current store (persist with 6 fields, validation gate at lines 77-85, return object at lines 87-98)
  </read_first>
  <behavior>
    - Test 1: preferenceSchema.parse() accepts object with only the 6 existing fields (backward compat)
    - Test 2: preferenceSchema.parse() accepts object with displayName, aiName, aiTone, responseBrevity as optional/undefined
    - Test 3: usePreferenceStore initial state includes displayName=undefined, aiName=undefined, aiTone=undefined, responseBrevity=undefined
    - Test 4: preferenceMemoryStore.get() passes new fields through validation gate and returns them in result
    - Test 5: setPreferences({ displayName: 'George' }) updates only displayName; other fields unchanged
  </behavior>
  <action>
    Per D-11 and D-21:

    **memoryTypes.ts** — extend preferenceSchema zod object (line 84) with 4 new optional fields:
    - displayName: z.string().optional() — user preferred display name (D-21)
    - aiName: z.string().optional() — custom AI name override (D-11)
    - aiTone: z.string().optional() — custom AI tone override (D-11)
    - responseBrevity: z.enum(['concise', 'detailed']).optional() — brevity preference (D-11)

    **PreferenceMemoryStore.ts** —
    - In persist initial state (line 46-53): add displayName: undefined, aiName: undefined, aiTone: undefined, responseBrevity: undefined
    - In preferenceMemoryStore.get() validation gate (line 78-85): pass the 4 new fields through preferenceSchema.parse()
    - In preferenceMemoryStore.get() return object (line 87-98): include displayName: own.displayName, aiName: own.aiName, aiTone: own.aiTone, responseBrevity: own.responseBrevity
    - The setPreferences partial setter already handles new fields automatically (accepts Partial<PreferencePayload>)
  </action>
  <verify>
    <automated>pnpm test tests/core/memory/PreferenceMemoryStore.test.ts</automated>
  </verify>
  <done>preferenceSchema accepts all 10 fields (6 existing + 4 new optional); usePreferenceStore initial state includes all 4 new fields as undefined; preferenceMemoryStore.get() returns all 10 fields through validation gate; setPreferences can set displayName/aiName/aiTone/responseBrevity individually</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PersonaService.getActiveProfile() with preference-override deep merge</name>
  <files>src/core/ai/persona/PersonaService.ts</files>
  <read_first>
    - src/core/ai/persona/PersonaService.ts — current class (17 lines, #profile field, getProfile())
    - src/core/ai/persona/PersonaProfile.ts — PersonaProfile interface (nested identity/languageStyle/emotionalAwareness objects, line 7-26), DEFAULT_PERSONA_PROFILE constant (line 67-101)
    - src/core/memory/PreferenceMemoryStore.ts — usePreferenceStore.getState() for vanilla access
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 961-1010 — getActiveProfile pattern with explicit field-level assignment
    - .planning/phases/07.4-rich-design-enhance/07.4-RESEARCH.md Pitfall #4 (lines 527-535) — deep merge safety
  </read_first>
  <behavior>
    - Test 1: getActiveProfile() returns DEFAULT_PERSONA_PROFILE when no preferences set
    - Test 2: getActiveProfile() with aiName='TestBot' overrides identity.name ONLY — other identity fields (tagline, domainExpertise) unchanged
    - Test 3: getActiveProfile() with aiTone='casual' overrides languageStyle.tone ONLY — responseStyle and vocabulary unchanged
    - Test 4: getActiveProfile() with responseBrevity='detailed' sets languageStyle.responseStyle to 'Detailed with thorough explanations'
    - Test 5: getActiveProfile() with responseBrevity='concise' sets languageStyle.responseStyle to 'Concise by default with task-aware expansion'
    - Test 6: getActiveProfile() returns a NEW object each call — no mutation of DEFAULT_PERSONA_PROFILE
    - Test 7: getProfile() still returns the original DEFAULT_PERSONA_PROFILE (backward compat)
  </behavior>
  <action>
    Per D-11 and RESEARCH.md Pitfall #4:

    Add getActiveProfile(): PersonaProfile method to PersonaService class:
    - Read usePreferenceStore.getState() to get current preference values (vanilla getState, no React hook)
    - Create a shallow copy of DEFAULT_PERSONA_PROFILE with explicit copies of nested objects: identity, languageStyle, emotionalAwareness
    - Apply overrides using explicit field-level assignment (NOT spread/merge of nested objects):
      - If prefs.aiName is set → profile.identity = { ...profile.identity, name: prefs.aiName }
      - If prefs.aiTone is set → profile.languageStyle = { ...profile.languageStyle, tone: prefs.aiTone }
      - If prefs.responseBrevity is set → map 'concise'/'detailed' to the corresponding responseStyle string, set via profile.languageStyle = { ...profile.languageStyle, responseStyle: ... }
    - Log via debugLog('info', '[PersonaService] getActiveProfile — merged preference overrides')
    - Return the merged profile object
    - getProfile() remains unchanged — returns this.#profile (DEFAULT_PERSONA_PROFILE)
    - The #profile private field stays as DEFAULT_PERSONA_PROFILE (never mutated)
  </action>
  <verify>
    <automated>pnpm test tests/core/ai/persona/PersonaService.test.ts</automated>
  </verify>
  <done>getActiveProfile() returns merged profile when aiName/aiTone/responseBrevity set; returns DEFAULT_PERSONA_PROFILE when no overrides; never mutates DEFAULT_PERSONA_PROFILE; getProfile() backward-compatible</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend PermissionService with ConfirmationLevel + create ConfirmationPolicy</name>
  <files>src/core/ai/tools/PermissionService.ts, src/core/confirmation/ConfirmationPolicy.ts</files>
  <read_first>
    - src/core/ai/tools/PermissionService.ts — current interface (canExecute) + DefaultPermissionService (16 lines)
    - .planning/phases/07.4-rich-design-enhance/07.4-PATTERNS.md lines 1013-1043 — PermissionService extension pattern with ConfirmationLevel enum
    - .planning/phases/07.4-rich-design-enhance/07.4-CONTEXT.md D-18, D-19 — ConfirmationLevel semantics (AutoExecute/ConfirmInline/RequirePermission)
  </read_first>
  <behavior>
    - Test 1: ConfirmationLevel enum has values AutoExecute='auto', ConfirmInline='inline', RequirePermission='require'
    - Test 2: getConfirmationLevel('summarize_page') returns ConfirmationLevel.AutoExecute
    - Test 3: getConfirmationLevel('web_search') returns ConfirmationLevel.ConfirmInline
    - Test 4: getConfirmationLevel('send_message') returns ConfirmationLevel.RequirePermission
    - Test 5: getConfirmationLevel('unknown_tool') returns ConfirmationLevel.RequirePermission (default-deny)
    - Test 6: DefaultPermissionService.getConfirmationLevel delegates to ConfirmationPolicy
    - Test 7: canExecute() still works unchanged (backward compat)
  </behavior>
  <action>
    Per D-18 and D-19:

    **ConfirmationPolicy.ts** (NEW — src/core/confirmation/ConfirmationPolicy.ts):
    - Export ConfirmationLevel enum with values: AutoExecute = 'auto', ConfirmInline = 'inline', RequirePermission = 'require'
    - Define static CONFIRMATION_POLICY: Record<string, ConfirmationLevel> mapping tool names to levels:
      - Read-only local: summarize_page, extract_page → AutoExecute
      - External lookups: web_search, web_fetch → ConfirmInline (triggers InlineConfirmationCard per D-17)
      - Side-effect actions: send_message, create_record, update_record → RequirePermission (delegates to Agent PermissionDialog)
    - Export getConfirmationLevel(toolName: string): ConfirmationLevel — look up in CONFIRMATION_POLICY, default to RequirePermission with debugLog warning for unknown tools
    - Import debugLog from ../../utils/debugLog

    **PermissionService.ts** — modify:
    - Re-export ConfirmationLevel: export { ConfirmationLevel } from '../confirmation/ConfirmationPolicy'
    - Add getConfirmationLevel(toolName: string): ConfirmationLevel to PermissionService interface
    - Implement getConfirmationLevel in DefaultPermissionService — delegate to getConfirmationLevel() from ConfirmationPolicy
    - Import getConfirmationLevel from ../confirmation/ConfirmationPolicy
    - canExecute method remains unchanged
  </action>
  <verify>
    <automated>pnpm test tests/core/ai/tools/PermissionService.test.ts</automated>
  </verify>
  <done>ConfirmationLevel enum with 3 tiers; ConfirmationPolicy maps known tools correctly; unknown tools default to RequirePermission; PermissionService.getConfirmationLevel delegates correctly; canExecute backward-compatible</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-input → PreferenceMemoryStore | User-customizable displayName stored in chrome.storage.local; rendered via antd Typography.Text (HTML-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.4-01 | Tampering | PreferenceMemoryStore.displayName | low | accept | displayName rendered via antd Typography.Text which escapes HTML; stored in chrome.storage.local only accessible by the extension |
| T-07.4-02 | Tampering | PersonaService.getActiveProfile | low | accept | getActiveProfile merges preference overrides via explicit field-level assignment; aiName/aiTone values injected into system prompts via PersonaInjector as plain-text blocks |
</threat_model>

<verification>
## Wave 0 Requirements

Before starting implementation, create or extend these test files:
- tests/core/memory/PreferenceMemoryStore.test.ts — extend with assertions for new fields (backward compat, set/get/undefined defaults)
- tests/core/ai/persona/PersonaService.test.ts — extend with getActiveProfile assertions (merge, no-mutation, no-override behavior)
- tests/core/ai/tools/PermissionService.test.ts — extend with ConfirmationLevel assertions (tier mapping, default-deny for unknowns)
</verification>

<success_criteria>
- [ ] preferenceSchema accepts all 4 new optional fields with backward-compatible validation
- [ ] usePreferenceStore initial state includes new fields as undefined
- [ ] preferenceMemoryStore.get() returns all 10 fields
- [ ] PersonaService.getActiveProfile() merges identity/languageStyle overrides without mutating DEFAULT_PERSONA_PROFILE
- [ ] ConfirmationLevel enum maps tools correctly (AutoExecute for read-only, ConfirmInline for external lookups, RequirePermission for side-effects)
- [ ] PermissionService interface extended without breaking existing canExecute consumers
- [ ] All existing tests pass; new tests cover all behaviors in behavior blocks
</success_criteria>

<output>
Create `.planning/phases/07.4-rich-design-enhance/07.4-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `displayName` (field) | PreferenceState | PreferenceMemoryStore.ts | User display name for personalized greeting |
| `aiName` (field) | PreferenceState | PreferenceMemoryStore.ts | Custom AI name override |
| `aiTone` (field) | PreferenceState | PreferenceMemoryStore.ts | Custom AI tone override |
| `responseBrevity` (field) | PreferenceState | PreferenceMemoryStore.ts | Response brevity preference (concise/detailed) |
| `getActiveProfile()` | method | PersonaService.ts | Returns merged persona profile with preference overrides |
| `ConfirmationLevel` | enum | ConfirmationPolicy.ts | AutoExecute/ConfirmInline/RequirePermission tiers |
| `getConfirmationLevel(toolName)` | function | ConfirmationPolicy.ts | Static tool-to-level policy lookup |
| `getConfirmationLevel(toolName)` | method | PermissionService.ts | Interface method on PermissionService |


