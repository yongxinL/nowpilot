---
phase: 07-trust-aware-context-and-receipts
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/core/context/trust/SkillDisclosure.ts
  - tests/core/context/trust/SkillDisclosure.test.ts
  - package.json
autonomous: true
requirements: [CTX-05, CTX-04]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - statement: "renderSkillDisclosure implements CTX-05 progressive disclosure: given N candidate skills with M active, the output carries M full instruction bodies + (N-M) trigger+one-line descriptions — irrelevant full instructions are ABSENT from the output (zero prompt tokens consumed, ROADMAP SC#4 / D-101)"
      verification: backstop
    - statement: "The disclosure mechanism is shaped against the ISkill contract (spec 1826-1856) as a declare-now seam — SkillDisclosureCandidate carries trigger/description/fullInstructions/active; real skill manifests + RICH catalog are Phase 15 (D-101 deferred-consumer boundary)"
      verification: backstop
    - statement: "The disclosure module is standalone — NOT wired into assemble or the live prompt (D-69 create-only discipline; would disturb the stable sections the 07-02 snapshots pin)"
      verification: backstop
    - statement: "verify:phase-7 is re-pointed to the §18 canonical gate string — tsc --noEmit && vitest run tests/core/context/trust tests/security/prompt-injection (spec 3611 verbatim, D-103 / D-92 analog) — so the CTX-04 snapshot and the CTX-02 fixtures actually run in the release block"
      verification: backstop
    - statement: "Both §18 test dirs contain ≥1 test file before the gate runs — vitest errors on empty dirs (RESEARCH Pitfall 3); the gate run is the phase's release-block proof"
      verification: backstop
  artifacts:
    - path: "src/core/context/trust/SkillDisclosure.ts"
      provides: "CTX-05 disclosure mechanism — SkillDisclosureCandidate interface (id/name/description/trigger/fullInstructions/active, shaped to ISkill spec 1829-1856) + renderSkillDisclosure(candidates) → { text, tokens }; active → name + fullInstructions, inactive → trigger — one-line description; token count via countTokensHeuristic (D-101)"
      exports: ["SkillDisclosureCandidate", "renderSkillDisclosure"]
    - path: "tests/core/context/trust/SkillDisclosure.test.ts"
      provides: "§18 — CTX-05 fixture proof: N skills / M active → M full bodies + N-M one-liners; token count equals tokens(active bodies) + tokens(one-liners); inactive full instructions consume zero tokens (absent from output); deterministic ordering"
      exports: []
    - path: "package.json"
      provides: "verify:phase-7 re-point — from the Phase-15/16 dirs (tests/hooks tests/components ...) to the spec 3611 canonical string (D-103); the ONLY package.json edit this phase (zero dependency changes, RESEARCH Package Legitimacy Audit)"
      exports: []
  key_links:
    - from: "src/core/context/trust/SkillDisclosure.ts"
      to: "src/core/context/TokenBudget.ts"
      via: "renderSkillDisclosure recounts the output text with countTokensHeuristic — the shipped accounting unit (STACK.md: no tokenizer library by design)"
      pattern: "countTokensHeuristic"
    - from: "package.json"
      to: "tests/core/context/trust"
      via: "verify:phase-7's vitest invocation targets tests/core/context/trust (trust policy + receipt + metrics + snapshots + disclosure) — the CTX-04 release block + CTX-05 proof (D-103)"
      pattern: "tests/core/context/trust"
    - from: "package.json"
      to: "tests/security/prompt-injection"
      via: "verify:phase-7's vitest invocation targets tests/security/prompt-injection — the CTX-02 adversarial fixtures (D-103)"
      pattern: "tests/security/prompt-injection"
  prohibitions:
    - "No real ISkill implementations, slash commands, or RICH catalog (D-101 — Phase 15 owns the concrete skills; this phase ships the disclosure contract + mechanism only)"
    - "Do NOT wire renderSkillDisclosure into assemble or the live prompt (D-69 create-only; would disturb the stable sections the 07-02 golden snapshots pin)"
    - "No new packages — the ONLY package.json edit is the verify:phase-7 script string (D-103; RESEARCH Package Legitimacy Audit is vacuous — a snapshot or test-util package would be scope creep)"
    - "Do NOT touch the verify:phase-1..6/8..19 scripts or verify:all — the re-point is scoped to verify:phase-7 exactly as spec 3611 states"
    - "Zero strict-suppression markers in all new files (STATE.md decision 17)"
    - "No invented error codes / requirement IDs (D-38)"
---

<objective>
Final expansion plan: ship CTX-05 (progressive skill disclosure — the declare-now mechanism whose Phase-15 consumers get the trigger + one-line description until a skill activates, so irrelevant full instructions consume zero prompt tokens) and execute D-103 (re-point `verify:phase-7` to the §18 canonical gate string) so the phase's release block actually runs the trust + prompt-injection suites — the CTX-04 snapshot diff gate and the CTX-02 adversarial fixtures become the gate's coverage.

Purpose: CTX-05 satisfies ROADMAP SC#4 ("irrelevant full skill instructions consume zero prompt tokens") with a standalone pure renderer proven by fixture — real `ISkill` implementations and the RICH catalog are Phase 15 (D-101 declared-now/populate-later). D-103 closes the phase: the mis-pointed gate (Phase 15/16 dirs) has blocked no one since Phase 6; re-pointing per spec 3611 makes `pnpm run verify:phase-7` the phase's true release block (D-68/D-78/D-92 precedent).

Output: the SkillDisclosure module + its §18 test, and the single package.json script-string edit, with the full phase gate run green as the closing proof.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  # Phase 7 block (225-238): SC#4 (irrelevant full skill instructions zero tokens), gate; D-103 verified
@.planning/STATE.md  # decision 17 (strict-clean), decision 18 (NP-STRICT ceiling 0), Phase 6 note (verify:phase-7 mis-pointing left for Phase-7)
@.planning/PRODUCT_SPEC_v0_1.md  # §18 Phase 7 (2646-2654), §28.3 CTX-05 (3953), §14.1 ISkill interface (1826-1856), §24 canonical gate (3611)
@.planning/phases/07-trust-aware-context-and-receipts/07-CONTEXT.md  # D-101 (declare-now disclosure), D-103 (gate re-point), D-98 (layer scoping)
@.planning/phases/07-trust-aware-context-and-receipts/07-RESEARCH.md  # D-101 section (declare-now mechanism), D-103 section (gate re-point), Code Example 5 (SkillDisclosure skeleton 402-422), Pitfall 3 (gate still mis-pointed — both dirs must exist), Package Legitimacy Audit (vacuous — no installs)
@.planning/phases/07-trust-aware-context-and-receipts/07-PATTERNS.md  # SkillDisclosure.ts section (124-137 — { text, tokens } return-shape precedent from ContextPack.pack / ContextCompressor.summarizeHistory) + no-analog note (296-299) + package.json section (248-258 — the exact re-point string) + shared patterns
@.planning/phases/07-trust-aware-context-and-receipts/07-UI-SPEC.md  # Contract D (disclosure mechanism → Phase 15 RICH catalog)
@.planning/phases/07-trust-aware-context-and-receipts/07-VALIDATION.md  # per-task map 7-03-01 (CTX-05) + 7-03-02 (gate); Wave 0 gaps (both dirs + re-point)
@src/core/context/TokenBudget.ts  # countTokensHeuristic (44-55) — the token accounting unit for renderSkillDisclosure
@src/core/context/ContextPack.ts  # pack() return shape { prompt, totalTokens } (36-51) — the { text, tokens } convention template
@src/core/ai/types.ts  # A8 PromptSection / StreamErrorCodeSchema — READ-ONLY; the ISkill shape-alignment reference
@package.json  # verify:phase-7 currently: "tsc --noEmit && vitest run tests/hooks tests/components tests/components/rich tests/core/intent tests/core/notes" — to re-point per D-103
@tests/core/context/ContextOptimizer.test.ts  # fixture-builder conventions for the disclosure fixture set
</context>

<tasks>

<task type="auto">
  <name>Task 1: SkillDisclosure — progressive disclosure with a zero-token proof (CTX-05)</name>
  <files>src/core/context/trust/SkillDisclosure.ts, tests/core/context/trust/SkillDisclosure.test.ts</files>
  <read_first>07-RESEARCH.md D-101 section + Code Example 5 (renderSkillDisclosure skeleton 402-422) + Architectural Responsibility Map row (disclosure standalone — not wired into the live prompt, D-69 discipline) · 07-PATTERNS.md SkillDisclosure.ts section (124-137 — { text, tokens } return shape mirrors ContextPack.pack 36-51; declare-now precedent mirrors Summarizer at types.ts:46-53) + no-analog note · 07-UI-SPEC.md Contract D · PRODUCT_SPEC §14.1 ISkill (1826-1856 — the shape SkillDisclosureCandidate aligns to: id/name/description/trigger semantics) · @src/core/context/TokenBudget.ts (countTokensHeuristic 44-55) · @src/core/context/ContextPack.ts (pack return shape 36-51)</read_first>
  <action>Create the CTX-05 disclosure mechanism. Strict-clean, header comment citing D-101 + spec 1829-1856 + the declare-now/populate-later precedent (Summarizer at src/core/context/types.ts:46-53).

1. `src/core/context/trust/SkillDisclosure.ts` — `export interface SkillDisclosureCandidate { id: string; name: string; description: string; trigger: string; fullInstructions: string; active: boolean; }` — shaped against the ISkill contract (spec 1829-1856: id/name/description + a trigger metadata field; fullInstructions is the body Phase 15's real skill manifests supply). `export function renderSkillDisclosure(candidates: SkillDisclosureCandidate[]): { text: string; tokens: number }` — pure, deterministic: ACTIVE candidates render as `name:\n<fullInstructions>`; INACTIVE candidates render as `<trigger> — <description>` (one line each); order = input order (the caller owns catalog ordering — Phase 15). Join sections with '\n\n' (the pack separator convention). `tokens` = countTokensHeuristic(outputText) — the shipped accounting unit, NO tokenizer library (STACK.md). The output contains ZERO bytes of any inactive candidate's fullInstructions — irrelevant full bodies consume zero prompt tokens (CTX-05 SC#4). Header comment documents: real ISkill implementations, slash commands, and the RICH catalog land Phase 15 (D-101); this module is the declared seam they consume.

2. `tests/core/context/trust/SkillDisclosure.test.ts` — fixture proof (makeInput-style builder): build N=4 candidates (2 active, 2 inactive) with distinct fullInstructions bodies containing unique marker strings (e.g. 'FULL_BODY_SKILL_ALPHA', 'FULL_BODY_SKILL_BETA' for the active ones; 'FULL_BODY_SKILL_GAMMA', 'FULL_BODY_SKILL_DELTA' for the inactive ones): (a) output contains both active full bodies verbatim; (b) output does NOT contain the inactive full bodies (grep the markers — they are absent, zero tokens); (c) output contains both inactive one-liners in the `<trigger> — <description>` shape; (d) token accounting: `renderSkillDisclosure(candidates).tokens === countTokensHeuristic(outputText)` AND equals tokens(active bodies) + tokens(one-liners) + separators (assert the sum decomposition — the SC#4 proof); (e) order preserved (input order in output); (f) all-inactive → output has one-liners only, no full body; (g) all-active → output has full bodies only; (h) empty array → { text: '', tokens: 0 }.</action>
  <acceptance_criteria>`npx vitest run tests/core/context/trust/SkillDisclosure.test.ts` passes; `pnpm run lint` passes strict-clean; `grep -n "renderSkillDisclosure" src/core/context/trust/SkillDisclosure.ts` shows the export; `grep -n "fullInstructions" src/core/context/trust/SkillDisclosure.ts` shows the candidate field; `grep -c "FULL_BODY_SKILL_GAMMA\|FULL_BODY_SKILL_DELTA" tests/core/context/trust/SkillDisclosure.test.ts` is >= 2 and the corresponding negative assertions exist (inactive bodies absent from output); `grep -rn "NP-STRICT" src/core/context/trust/ | wc -l` equals 0; `git status --porcelain src/core/context/ContextOptimizer.ts src/core/context/ContextPack.ts src/core/ai/types.ts | wc -l` equals 0 (no pipeline wiring — D-69 create-only; the disclosure module is standalone).</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/context/trust/SkillDisclosure.test.ts && test "$(grep -rn 'NP-STRICT' src/core/context/trust/ | wc -l)" -eq 0 && test "$(git status --porcelain src/core/context/ContextOptimizer.ts src/core/context/ContextPack.ts src/core/ai/types.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>renderSkillDisclosure ships the CTX-05 contract: N candidates / M active → M full bodies + (N-M) trigger+one-liners, token decomposition proven by the fixture (inactive full bodies absent — zero prompt tokens), standalone and unwired (D-69); the seam is declared for Phase 15's RICH catalog (D-101).</done>
  <reversibility rating="reversible">D-101 — additive declaration + fixture proof; Phase 15 consumes the seam additively (CONTEXT.md rating).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Re-point verify:phase-7 to the §18 canonical gate string and run it green (D-103)</name>
  <files>package.json</files>
  <read_first>07-RESEARCH.md D-103 section + Pitfall 3 (gate still mis-pointed — the two §18 dirs must contain ≥1 test file before the gate runs; vitest errors on empty dirs) + Validation Architecture (full suite command) · 07-PATTERNS.md package.json section (248-258 — the exact before/after script strings) · 07-CONTEXT.md D-103 · PRODUCT_SPEC §24 canonical gate (3611) · @package.json (verify:phase-7 current mis-pointed string + NP_STRICT_CEILING line 7 — do not touch anything else) · prior precedent: Phase 5 verify:phase-5 re-point (D-78), Phase 6 verify:phase-6 re-point (D-92) — same pattern</read_first>
  <action>Edit `package.json` — the ONLY edit is the `verify:phase-7` script string (D-103). BEFORE: `"verify:phase-7": "tsc --noEmit && vitest run tests/hooks tests/components tests/components/rich tests/core/intent tests/core/notes"` (Phase 15/16 territory — mis-pointed since Phase 6, STATE.md records). AFTER (spec 3611 VERBATIM, the D-68/D-78/D-92 precedent): `"verify:phase-7": "tsc --noEmit && vitest run tests/core/context/trust tests/security/prompt-injection"`. Do NOT touch any other script (verify:phase-1..6, verify:phase-8..19, verify:all, test, lint), any dependency, or NP_STRICT_CEILING — the RESEARCH Package Legitimacy Audit is vacuous and stays so (a dependency edit would be scope creep and would require a checkpoint). Confirm both §18 dirs contain test files first (`ls tests/core/context/trust/*.test.ts` — populated by 07-01/07-02/07-03 Task 1; `ls tests/security/prompt-injection/*.test.ts` — populated by 07-01 Task 3; RESEARCH Pitfall 3: vitest fails on empty dirs). Then run `pnpm run verify:phase-7` — it must exit 0 with tsc clean and the trust + prompt-injection suites executed (the release-block proof: CTX-04 snapshot diff gate + CTX-02 fixtures + CTX-05 proof + CTX-06 metrics + CTX-01/CTX-03 pipeline tests all run in the gate).</action>
  <acceptance_criteria>`node -e "const p=require('./package.json'); if(p.scripts['verify:phase-7']!=='tsc --noEmit && vitest run tests/core/context/trust tests/security/prompt-injection') process.exit(1)"` passes (exact spec-3611 string, D-103); `ls tests/core/context/trust/*.test.ts tests/security/prompt-injection/*.test.ts` lists test files in both dirs (≥1 each — no empty-dir vitest failure, Pitfall 3); `grep -c "tests/hooks\|tests/components\|tests/core/intent\|tests/core/notes" package.json` equals 0 within the verify:phase-7 string (re-pointed — the old Phase-15/16 targets are gone from the script); `pnpm run verify:phase-7` exits 0 and its vitest output reports tests from tests/core/context/trust AND tests/security/prompt-injection; `git diff package.json` shows ONLY the verify:phase-7 line changed (no dependency/other-script edits).</acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.scripts['verify:phase-7']!=='tsc --noEmit && vitest run tests/core/context/trust tests/security/prompt-injection') process.exit(1)" && pnpm run verify:phase-7</automated>
  </verify>
  <done>verify:phase-7 is the exact spec-3611 gate string, both §18 dirs are populated, and the full gate runs green — tsc strict-clean + the trust suite + the prompt-injection suite — making the CTX-04 snapshot diff and the CTX-02 fixtures real release blocks (D-103). The phase is gate-green end-to-end.</done>
  <reversibility rating="reversible">D-103 — package.json script-string edit (D-68/D-78/D-92 precedent); reverting restores the mis-pointed string (CONTEXT.md rating).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill metadata → rendered disclosure | Untrusted-ish skill descriptions cross into the prompt surface — only trigger + one-line for inactive skills; full bodies only for active (CTX-05/D-101) |
| test dirs → release gate | The §18 test dirs feed verify:phase-7 — empty dirs or a mis-pointed gate silently skip the phase's release-block coverage (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-08 | Information Disclosure | renderSkillDisclosure output | medium | mitigate | Inactive skills render trigger + one-line description ONLY — fullInstructions never enters the output (zero prompt tokens, CTX-05 SC#4); fixture asserts inactive full-body markers are absent |
| T-7-09 | Tampering | package.json verify:phase-7 script | medium | mitigate | Re-pointed to the spec-3611 verbatim string (D-103); node -e exact-string assertion + git diff scope assertion (only the one line changes) prevent a mis-pointed or over-wide gate; a silent gate would let the CTX-04 snapshot diff ship |
| T-7-10 | Spoofing | gate green with empty dirs | low | mitigate | Both §18 dirs asserted populated before the gate runs (ls check, RESEARCH Pitfall 3 — vitest errors on empty dirs rather than silently passing) |
| T-7-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO package installs this phase (RESEARCH Package Legitimacy Audit vacuous); the only package.json edit is the verify:phase-7 script string — any dependency addition would be scope creep and require a blocking checkpoint |
</threat_model>

<verification>
- **Per-task commits:** `pnpm run lint` + the affected trust test file (07-VALIDATION.md sampling rate).
- **Gate determinism note (wave deviation from 07-VALIDATION draft):** the draft's per-task map lists plan 03 at wave 2 — the planner waves it at 3 (depends_on 07-01 + 07-02) so the D-103 gate run executes AFTER every §18 test file exists (07-01 trust + prompt-injection, 07-02 metrics + snapshot, 07-03 disclosure). A wave-2 gate would race 07-02's in-flight tests and could "pass" with partial coverage — the release block must be deterministic.
- **Phase-close proof:** `pnpm run verify:phase-7` green at the end of this plan = the entire phase's release block (CTX-01/02/03 pipeline + CTX-04 snapshot diff + CTX-05 zero-token proof + CTX-06 metrics) is covered by the gate; `pnpm run verify:all` remains untouched (the re-point is scoped).
- Grep guards: zero strict-suppression markers in src/core/context/trust/ (STATE.md decision 17); no pipeline wiring of the disclosure module (D-69 — git status empty for ContextOptimizer/ContextPack/ai types in Task 1); single-line package.json diff (Task 2).
- D-103 precedent chain: D-68 (Phase 4) → D-78 (Phase 5) → D-92 (Phase 6) → D-103 (Phase 7) — same re-point pattern, spec 3611 verbatim.
</verification>

<success_criteria>
1. renderSkillDisclosure proves CTX-05: N skills / M active → M full bodies + (N-M) one-liners; inactive full instructions consume zero prompt tokens (SC#4) — fixture-decomposed token count.
2. The disclosure seam is standalone and unwired (D-69/D-101) — Phase 15's RICH catalog consumes it.
3. verify:phase-7 is the exact spec-3611 string and runs green with both §18 dirs populated — the CTX-04 snapshot diff and CTX-02 fixtures are live release blocks (D-103).
4. Zero new packages, single-line package.json diff, zero strict-suppression markers, no schema/contract edits anywhere in the phase.
</success_criteria>

<output>
Create `.planning/phases/07-trust-aware-context-and-receipts/07-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-7 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/types/harness.ts | TrustLevel / ContextItem / ContextReceiptEntry (C.1 verbatim) | 07-01 |
| src/core/context/trust/TrustPolicy.ts | AUTHORITY_BY_TRUST / applyTrustPolicy / isPolicyRedefinitionAttempt / raiseIfPolicyRedefinitionAttempt | 07-01 |
| src/core/context/trust/contextItems.ts | buildContextItems (D-94 item pipeline builder) | 07-01 |
| src/core/context/trust/ContextReceipt.ts | deriveContextReceipt → ContextReceiptSurface { entries, untrustedDataPresent } | 07-01 |
| src/core/context/trust/ContextQualityMetrics.ts | deriveContextQualityMetrics → ContextQualityMetrics (aggregates only, D-102) | 07-02 |
| src/core/context/trust/SkillDisclosure.ts | SkillDisclosureCandidate + renderSkillDisclosure (CTX-05 zero-token disclosure, D-101) | **[this plan]** |
| src/core/context/ContextOptimizer.ts | assemble() additive: item pipeline + originalTokens + rungs 1-2 + receipt + metrics attach | 07-01 + 07-02 |
| tests/core/context/trust/TrustPolicy.test.ts | §18 O.3 wrap/strip + guard | 07-01 |
| tests/core/context/trust/contextItems.test.ts | §18 D-94 tagging | 07-01 |
| tests/core/context/trust/ContextReceipt.test.ts | §18 derivation rules | 07-01 |
| tests/core/context/trust/assemble-trust.test.ts | §18 TRACER end-to-end | 07-01 |
| tests/core/context/trust/ContextQualityMetrics.test.ts | §18 CTX-06 aggregates + no-raw-text marker | 07-02 |
| tests/core/context/trust/stable-prefix.snapshot.test.ts + fixtures/stable-prefix.golden.txt | §18 CTX-04 golden + FNV-1a cross-check | 07-02 |
| tests/core/context/trust/SkillDisclosure.test.ts | §18 CTX-05 zero-token proof (N skills / M active) | **[this plan]** |
| tests/security/prompt-injection/policy-redefinition.test.ts | §18 adversarial fixtures (CTX-02) | 07-01 |
| package.json | verify:phase-7 re-pointed to spec 3611 verbatim (D-103) — the only edit this phase | **[this plan]** |

Layout note (locked): no barrel index.ts in src/core/context/trust/ — direct path imports only (src/core/ai flat convention). The disclosure module is standalone (D-69) — no imports from it into ContextOptimizer, ContextPack, or any AI-runtime file; the git-status assertions in Task 1 enforce this.