---
phase: 07-trust-aware-context-and-receipts
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/core/context/trust/ContextQualityMetrics.ts
  - src/core/context/ContextOptimizer.ts
  - tests/core/context/trust/ContextQualityMetrics.test.ts
  - tests/core/context/trust/stable-prefix.snapshot.test.ts
  - tests/core/context/trust/fixtures/stable-prefix.golden.txt
autonomous: true
requirements: [CTX-04, CTX-06]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - statement: "Golden snapshot fixtures of the packed stable prefix are committed under tests/core/context/trust/fixtures/ and a *.snapshot.* test asserts the assembled output is byte-identical (CTX-04 / D-100) — a system-prompt diff fails the test → blocks the release gate"
      verification: backstop
    - statement: "The stable-prefix snapshot cross-checks PromptCacheAdapter.hashStableSections (FNV-1a, spec 5747+) against a committed golden hash — stable sections are hash-stable; USER PREFERENCES stays stable:false (RESEARCH reconciliation 3 — the snapshot covers the deterministic packed output, not a stable-flag flip)"
      verification: backstop
    - statement: "deriveContextQualityMetrics returns AGGREGATES ONLY — section count, trust-mix counts per TrustLevel, truncation/omission counts, compression counts, token utilization ratio original→final, minimalMode flag — no section bodies, no raw sensitive text (CTX-06 / D-102 / UI-SPEC Contract B)"
      verification: backstop
    - statement: "The metrics surface attaches to OptimizedContext as an additive field (D-77 pattern) — assemble stays never-throw and the verbatim manifest/A8 contracts stay untouched"
      verification: backstop
    - statement: "The metrics test asserts no fixture body text appears in the serialized metrics output (the D-102 hard boundary, mirrored from the D-77 trace-surface discipline)"
      verification: backstop
  artifacts:
    - path: "src/core/context/trust/ContextQualityMetrics.ts"
      provides: "CTX-06 derived aggregate metrics — deriveContextQualityMetrics(manifest, receipt, items) → ContextQualityMetrics { sectionCount, trustMix, truncationCount, omissionCount, compressionCount, tokenUtilizationRatio, minimalMode }; aggregates only, no raw text (D-102, UI-SPEC Contract B)"
      exports: ["deriveContextQualityMetrics", "ContextQualityMetrics"]
    - path: "src/core/context/ContextOptimizer.ts"
      provides: "Additive metrics attach — assemble derives metrics from the manifest + receipt + items and adds the additive field to OptimizedContext (D-77 precedent; 07-01 added receipt, this adds metrics)"
      exports: ["assemble", "OptimizedContext"]
    - path: "tests/core/context/trust/ContextQualityMetrics.test.ts"
      provides: "§18 — CTX-06: aggregates correct for a fixture manifest/receipt/items; no section body text appears in the serialized metrics (D-102 boundary)"
      exports: []
    - path: "tests/core/context/trust/stable-prefix.snapshot.test.ts"
      provides: "§18 — CTX-04: canonical fixture → assemble → pack(context.sections).prompt byte-identical to the committed golden via toMatchFileSnapshot (async — awaited); hashStableSections FNV-1a equals the committed golden hash"
      exports: []
    - path: "tests/core/context/trust/fixtures/stable-prefix.golden.txt"
      provides: "CTX-04 committed golden — the deterministic packed output for the canonical fixture input (USER PREFERENCES included because rendering is deterministic for a fixed input, NOT because it is cache-stable — RESEARCH reconciliation 3); regeneration path documented (intentional prompt change → regenerate + commit)"
      exports: []
  key_links:
    - from: "tests/core/context/trust/stable-prefix.snapshot.test.ts"
      to: "src/core/context/ContextOptimizer.ts"
      via: "assemble(canonicalFixtureInput) → pack(result.context.sections).prompt — the deterministic packed output the golden snapshots (D-100; toMatchFileSnapshot is async — await it)"
      pattern: "toMatchFileSnapshot"
    - from: "tests/core/context/trust/stable-prefix.snapshot.test.ts"
      to: "src/core/ai/PromptCacheAdapter.ts"
      via: "hashStableSections(result.context.sections) === golden FNV-1a hash — the Appendix K stable-hash contract (spec 5747+; only TOOL SCHEMAS is stable:true in the shipped code)"
      pattern: "hashStableSections"
    - from: "src/core/context/ContextOptimizer.ts"
      to: "src/core/context/trust/ContextQualityMetrics.ts"
      via: "assemble → deriveContextQualityMetrics(manifest, receipt, items) → context.metrics (additive field; D-77 precedent; Phase 11 lifts it into PromptTrace)"
      pattern: "deriveContextQualityMetrics"
  prohibitions:
    - "Do NOT flip USER PREFERENCES to stable:true (RESEARCH reconciliation 3 — changes Phase-5/PromptCacheManager cache semantics; the snapshot covers the deterministic packed output instead)"
    - "No section bodies in metrics — deriveContextQualityMetrics returns counts/ratios only (D-102/UI-SPEC Contract B); a test asserts the serialized metrics contain no fixture body text"
    - "No edits to the manifest schema, A8 PromptSection, or CANONICAL_SECTION_ORDER (D-72/D-77/D-95) — metrics is a derived module like the receipt"
    - "assemble stays never-throw; the metrics derivation must not add a throw path (AssembleResult union contract)"
    - "Zero strict-suppression markers in all new/modified files (STATE.md decision 17)"
    - "No snapshot library — vitest's built-in toMatchFileSnapshot is the committed, diffable mechanism (RESEARCH Alternatives: don't hand-roll)"
    - "The golden file is committed — regenerating on an INTENTIONAL prompt change is a deliberate fixture update, documented in the test header, never an automatic silent refresh"
---

<objective>
Expansion plan: deliver CTX-04 (stable-prefix snapshot tests that gate releases) and CTX-06 (derived context-quality diagnostics with no raw text) on top of the 07-01 trust spine. The snapshot plan commits golden fixtures of the deterministic packed output for a canonical input and asserts byte-identity via vitest's file snapshots plus the `hashStableSections` FNV-1a cross-check — a system-prompt diff fails the test, blocking the release gate (D-100, CTX-04). The metrics plan ships `deriveContextQualityMetrics` — aggregates only (section count, trust mix, truncation/omission/compression counts, token utilization ratio, minimalMode) — attached additively to `OptimizedContext` (D-102, CTX-06), with a hard no-raw-text boundary proven by test.

Purpose: CTX-04 makes the stable-prefix contract testable — the Phase-7 home of the "system-prompt diff blocks release" guarantee, gated by `verify:phase-7` (D-103 lands in 07-03; no CI in this repo). CTX-06 gives Phase 11's DiagnosticsSection a raw-text-free quality surface over the receipt/manifest (the D-77 derived-trace pattern). Both are additive derived views — no schema edits, no live-pipeline adoption (D-69).

Output: the five files in frontmatter — the metrics module + its OptimizedContext attach, the snapshot test + committed golden, and the CTX-06 test — strict-clean, all proven by the §18 test files.
</objective>

<execution_context>
@/Users/george.li/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/george.li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  # Phase 7 block (225-238): SC#2 (stable-prefix snapshots block release), gate; SC#3 (Prompt Inspector)
@.planning/STATE.md  # decision 17 (strict-clean), decision 10 (ADR-SEC-01)
@.planning/PRODUCT_SPEC_v0_1.md  # §18 Phase 7 (2646-2654), §28.3 CTX-04/CTX-06 (3952/3954), §1.3 stable sections (331-350), Appendix K hashStableSections (5747-5821), §2.6 manifest (526-544), §24 gate (3611)
@.planning/phases/07-trust-aware-context-and-receipts/07-CONTEXT.md  # D-100 (golden snapshots + verify:phase-7 release block), D-102 (derived aggregates), D-98 (L5/L6 scope)
@.planning/phases/07-trust-aware-context-and-receipts/07-RESEARCH.md  # Pattern 4 (stable-prefix golden snapshots 227-231 + reconciliation: do NOT flip USER PREFERENCES), Pattern 2 (derived surface), Pitfall 5 (snapshot churn from USER PREFERENCES), Code Example 6 (snapshot skeleton 424-446), Security Domain (prompt-cache poisoning row)
@.planning/phases/07-trust-aware-context-and-receipts/07-PATTERNS.md  # ContextQualityMetrics.ts section (118-122), snapshot no-analog note (296-298: toMatchFileSnapshot is async — await it), derived-view precedent (manifestTruncatedSources ContextOptimizer.ts:441-445)
@.planning/phases/07-trust-aware-context-and-receipts/07-UI-SPEC.md  # Contract B (metrics aggregates + hard no-raw-text boundary), copy seeds for Phase 11
@src/core/context/ContextOptimizer.ts  # assemble (131-176) — the metrics attach point (after deriveContextReceipt from 07-01); OptimizedContext (60-71) — additive fields
@src/core/context/trust/ContextReceipt.ts  # ContextReceiptSurface (from 07-01) — metrics derives from receipt + manifest + items
@src/core/context/trust/contextItems.ts  # ContextItem[] (from 07-01) — trustMix derives from item.trust
@src/core/context/ContextProvenanceManifest.ts  # manifest schema + MANIFEST_KIND_MAP — READ-ONLY input to metrics
@src/core/ai/PromptCacheAdapter.ts  # hashStableSections FNV-1a (83-95) — the snapshot cross-check
@src/core/context/ContextPack.ts  # pack() (36-51) — the byte-identity materialization seam
@src/types/harness.ts  # ContextItem / ContextReceiptEntry / TrustLevel (from 07-01) — metrics input types
@tests/core/context/ContextOptimizer.test.ts  # makeInput fixture-builder conventions — the snapshot's canonical fixture mirrors this
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContextQualityMetrics — derived aggregates with a no-raw-text boundary (CTX-06)</name>
  <files>src/core/context/trust/ContextQualityMetrics.ts, src/core/context/ContextOptimizer.ts, tests/core/context/trust/ContextQualityMetrics.test.ts</files>
  <read_first>07-RESEARCH.md D-102 section (Context-quality diagnostics) + Security Domain (sensitive-text-leak row) + architecture diagram step 6 (deriveContextQualityMetrics) · 07-PATTERNS.md ContextQualityMetrics.ts section (118-122 — aggregate-only discipline mirrors manifestTruncatedSources ContextOptimizer.ts:441-445) + derived-view precedent · 07-UI-SPEC.md Contract B (shape + hard boundary: no raw section text) + copy seeds ("Trust mix", "Token utilization", "Minimal mode") · @src/core/context/trust/ContextReceipt.ts (the receipt surface this derives from — ContextReceiptEntry[] with originalTokens/finalTokens/omitReason) · @src/core/context/trust/contextItems.ts (ContextItem[] — the trustMix source) · @src/core/context/ContextProvenanceManifest.ts (manifest.sections records — READ-ONLY input) · @src/core/context/ContextOptimizer.ts (assemble 131-176 + OptimizedContext 60-71 — the additive attach point; manifestTruncatedSources 441-445 as the style template)</read_first>
  <action>Create the CTX-06 metrics surface. Strict-clean, header comment citing D-102 + UI-SPEC Contract B + the D-77 trace-surface precedent.

1. `src/core/context/trust/ContextQualityMetrics.ts` — `export interface ContextQualityMetrics` with AGGREGATE fields only: `sectionCount: number` (manifest.sections.length), `trustMix: Record<TrustLevel, number>` (counts per TrustLevel from the ContextItem[] — 0 for absent levels, all five keys present), `truncationCount: number` (manifest records with truncated:true, excluding the by-design system/task omission records — the manifestTruncatedSources filter precedent), `omissionCount: number` (receipt entries with included:false), `compressionCount: number` (records with compressionApplied present), `tokenUtilizationRatio: number` (Σ finalTokens / Σ originalTokens from the receipt entries, rounded to 4dp; 1 when no entries — never NaN, guard the zero-divisor), `minimalMode: boolean` (manifest.minimalMode). `export function deriveContextQualityMetrics(manifest: ContextProvenanceManifest, receipt: ContextReceiptSurface, items: ContextItem[]): ContextQualityMetrics` — pure, never throws. NO section bodies anywhere in the output shape — the aggregates are computed by iterating records/entries/items; a section's text is never read into the metrics (D-102 hard boundary).

2. `src/core/context/ContextOptimizer.ts` — additive attach (D-77 pattern, exactly as 07-01 did for receipt): in assemble, after the receipt is derived, compute `const metrics = deriveContextQualityMetrics(manifest, receipt, items)` (import from './trust/ContextQualityMetrics') and add the additive field `metrics: ContextQualityMetrics` to the OptimizedContext object literal. No signature change, no throw path, existing callers compile.

3. `tests/core/context/trust/ContextQualityMetrics.test.ts` — build a fixture manifest + receipt + items (makeInput-style builder or hand-built records): (a) sectionCount matches; (b) trustMix counts each TrustLevel correctly across a mixed item set (e.g. 1 system + 1 user + 2 retrieved + 1 untrusted + 0 tool → exact map); (c) truncationCount excludes system/task omissions but includes a genuinely truncated CONTEXT record; (d) omissionCount === receipt entries with included:false; (e) compressionCount counts only records carrying compressionApplied; (f) tokenUtilizationRatio = Σ final / Σ original (e.g. 400/500 → 0.8); ratio 1 for an empty receipt; (g) minimalMode mirrors the manifest; (h) THE D-102 BOUNDARY: `JSON.stringify(metrics)` does not contain any fixture section-body text (e.g. a unique marker string 'SECRET_PAGE_BODY_XYZ' placed in the fixture page content) — proves no raw text leaks into the diagnostics surface; (i) deriveContextQualityMetrics with an empty items array → trustMix all zeros, still a valid shape.</action>
  <acceptance_criteria>`npx vitest run tests/core/context/trust/ContextQualityMetrics.test.ts` passes; `pnpm run lint` passes strict-clean; `grep -n "deriveContextQualityMetrics" src/core/context/ContextOptimizer.ts` shows the attach; `grep -n "metrics" src/core/context/ContextOptimizer.ts` shows the additive OptimizedContext field; `grep -n "trustMix\|tokenUtilizationRatio\|minimalMode" src/core/context/trust/ContextQualityMetrics.ts` shows the aggregate fields; `grep -c "section.text\|\.text" src/core/context/trust/ContextQualityMetrics.ts` equals 0 (no section-body reads — D-102); `grep -rn "NP-STRICT" src/core/context/trust/ | wc -l` equals 0; `grep -c "SECRET_PAGE_BODY_XYZ" tests/core/context/trust/ContextQualityMetrics.test.ts` is >= 1 (the no-leak fixture marker exists).</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && npx vitest run tests/core/context/trust/ContextQualityMetrics.test.ts && test "$(grep -c 'section.text\|\.text' src/core/context/trust/ContextQualityMetrics.ts)" -eq 0 && test "$(grep -rn 'NP-STRICT' src/core/context/trust/ | wc -l)" -eq 0</automated>
  </verify>
  <done>deriveContextQualityMetrics ships aggregates only (sectionCount/trustMix/truncationCount/omissionCount/compressionCount/tokenUtilizationRatio/minimalMode), attaches to OptimizedContext additively, and the test proves no fixture body text appears in the serialized metrics — the CTX-06 no-raw-text boundary (D-102/Contract B).</done>
  <!-- planner-discipline-allow: SECRET_PAGE_BODY_XYZ -->
  <reversibility rating="reversible">D-102 — additive derived module + additive field; Phase 11 lifts it into PromptTrace additively (CONTEXT.md rating).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Stable-prefix golden snapshots + FNV-1a hash cross-check (CTX-04)</name>
  <files>tests/core/context/trust/stable-prefix.snapshot.test.ts, tests/core/context/trust/fixtures/stable-prefix.golden.txt</files>
  <read_first>07-RESEARCH.md D-100 section + Pattern 4 (stable-prefix golden snapshots 227-231 — byte-identity + hashStableSections cross-check) + reconciliation note (do NOT flip USER PREFERENCES stable:false; snapshot the deterministic packed output) + Pitfall 5 (snapshot churn from USER PREFERENCES — the golden regeneration path must be documented) + Code Example 6 (snapshot skeleton 424-446) · 07-PATTERNS.md snapshot no-analog note (296-298: zero toMatchFileSnapshot precedent in repo; toMatchFileSnapshot is async — await it) · 07-CONTEXT.md D-100 · @src/core/context/ContextOptimizer.ts (assemble — the assembled output the snapshot captures) · @src/core/context/ContextPack.ts (pack 36-51 — the byte-identity seam) · @src/core/ai/PromptCacheAdapter.ts (hashStableSections 83-95 — FNV-1a; stable-flag semantics: only TOOL SCHEMAS is stable:true in the shipped code) · @tests/core/context/ContextOptimizer.test.ts (makeInput fixture-builder — the canonical fixture mirrors its shape)</read_first>
  <action>Create the CTX-04 snapshot suite. The committed golden is the release-block artifact: any system-prompt change (tool-schema rendering, prefsCompact, separators, ordering) diffs it → the test fails → `verify:phase-7` blocks release (D-100 — this repo has no CI, the gate IS the release block).

1. Define a CANONICAL fixture input inline (documented in the test header, mirroring ContextOptimizer.test.ts makeInput shape): fixed operationId/model/modelContextWindow (a value that lands on a mid tier — e.g. 131072 for medium), userInput, conversationId, workspaceId, activeSurface 'sidepanel', a fixed pageContext (url/title/markdown), 3 fixed selectedToolSchemas (deterministic names/descriptions), 2 fixed memoryHints, fixed preferences (both model fields + personaOverrides with name/tone/brevity — so prefsCompact is deterministic). No debugSections/secondaryNotes (the snapshot covers the canonical stable input; debug/notes are D-97 caller-supplied and would churn the golden). No fabricated authority items (the wrap would alter section text — the canonical fixture is pipeline-correct).

2. `tests/core/context/trust/stable-prefix.snapshot.test.ts` — two tests: (a) `it('packed stable prefix is byte-identical to the committed golden (CTX-04)')` — `const result = assemble(canonicalFixtureInput); expect(result.ok).toBe(true); if (!result.ok) return;` then `await expect(pack(result.context.sections).prompt).toMatchFileSnapshot('./fixtures/stable-prefix.golden.txt')` — NOTE: toMatchFileSnapshot is ASYNC, the test must await it (PATTERNS note; no repo precedent — this is the first use). The first run WRITES the golden file (vitest snapshot semantics) — review the generated content for correctness (it must contain the TOOL SCHEMAS text, the prefsCompact output, the CONTEXT URL/TITLE/body, the memory lines, the user input — joined with the pack '\n\n' separator), then COMMIT it. (b) `it('stable-section FNV-1a hash matches the golden (cross-check, spec 5747+)')` — `expect(hashStableSections(result.context.sections)).toBe('<golden-hash>')` — the golden hash is computed from the FIRST run's actual output (run once, read the console/actual value, hard-code it as the golden — the same commit discipline as the golden file). Document in the test header: USER PREFERENCES is included in the packed golden because rendering is deterministic for a fixed input, NOT because it is cache-stable (it stays stable:false — RESEARCH reconciliation 3); regeneration path: an INTENTIONAL system-prompt change updates the golden deliberately (delete the golden + rerun + review + commit), never silently.

3. Do NOT touch ContextOptimizer.ts / ContextPack.ts / PromptCacheAdapter.ts / USER PREFERENCES stable flag in this task — the snapshot observes the shipped pipeline (07-01 integrated the trust layer; this task pins its deterministic output).</action>
  <acceptance_criteria>`npx vitest run tests/core/context/trust/stable-prefix.snapshot.test.ts` passes (byte-identity + hash cross-check); `test -s tests/core/context/trust/fixtures/stable-prefix.golden.txt` (golden committed, non-empty); `grep -n "toMatchFileSnapshot" tests/core/context/trust/stable-prefix.snapshot.test.ts` shows the awaited call; `grep -n "hashStableSections" tests/core/context/trust/stable-prefix.snapshot.test.ts` shows the cross-check; `git status --porcelain src/core/context/ContextOptimizer.ts src/core/context/ContextPack.ts src/core/ai/PromptCacheAdapter.ts src/core/ai/types.ts src/core/context/ContextProvenanceManifest.ts | wc -l` equals 0 (snapshot only observes — no source edits in this task); `grep -rn "NP-STRICT" tests/core/context/trust/ | wc -l` equals 0; the golden file contains the TOOL SCHEMAS text and the CONTEXT URL line (deterministic content review).</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/context/trust/stable-prefix.snapshot.test.ts && test -s tests/core/context/trust/fixtures/stable-prefix.golden.txt && test "$(git status --porcelain src/core/context/ContextOptimizer.ts src/core/context/ContextPack.ts src/core/ai/PromptCacheAdapter.ts src/core/ai/types.ts src/core/context/ContextProvenanceManifest.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>The committed golden snapshot (tests/core/context/trust/fixtures/stable-prefix.golden.txt) is byte-identical to the assembled packed output for the canonical fixture, cross-checked against the hashStableSections FNV-1a golden hash — CTX-04's release block is live: any system-prompt diff now fails verify:phase-7 (gated in 07-03 by D-103).</done>
  <reversibility rating="reversible">D-100 — golden fixtures; regenerating on an intentional prompt change is a fixture update (CONTEXT.md rating).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assemble() output → snapshot golden | The deterministic packed output crosses into the committed fixture — a system-prompt change diffs it and blocks release (CTX-04/D-100) |
| manifest/receipt/items → ContextQualityMetrics | Aggregate counts cross to the diagnostics surface — no section bodies may cross (D-102 hard boundary) |
| metrics → Phase 11 DiagnosticsSection | Forward contract — aggregates only, never raw sensitive text (UI-SPEC Contract B) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-05 | Tampering | stable-prefix snapshot / prompt cache | medium | mitigate | Committed golden + toMatchFileSnapshot byte-identity + hashStableSections FNV-1a cross-check (spec 5747+) gate the stable prefix — a system-prompt diff fails the test and blocks release via verify:phase-7 (D-100; this repo has no CI — the gate is the block) |
| T-7-06 | Information Disclosure | ContextQualityMetrics / receipt derivation | medium | mitigate | Aggregates only — no section bodies read into the metrics shape (grep-asserted), and the test asserts the serialized metrics contain no fixture body marker text (D-102 / UI-SPEC Contract B) |
| T-7-07 | Tampering | snapshot golden churn | low | mitigate | USER PREFERENCES stays stable:false (RESEARCH reconciliation 3 — no cache-semantics change); the golden regeneration path is documented in the test header (intentional prompt change → deliberate fixture update, never silent auto-refresh) |
| T-7-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO package installs this phase (RESEARCH Package Legitimacy Audit vacuous); vitest's built-in toMatchFileSnapshot replaces any snapshot-library temptation (RESEARCH Alternatives) |
</threat_model>

<verification>
- **Per-task commits:** `pnpm run lint` + the affected trust test file (07-VALIDATION.md sampling rate).
- CTX-04 release-block proof: the snapshot test runs inside `verify:phase-7` once 07-03 re-points the gate (D-103); this plan only creates the golden + test — the gate wiring is 07-03's Task 2.
- D-102 boundary proof: `grep -c "section.text\|\.text"` on ContextQualityMetrics.ts equals 0 AND the test's SECRET_PAGE_BODY_XYZ marker assertion — belt-and-braces that no raw text crosses into the diagnostics surface.
- Grep guards: zero strict-suppression markers in src/core/context/trust/ + tests (STATE.md decision 17); no source edits outside the two intended files in Task 2 (git status assertion).
- Snapshot discipline: the golden file is committed (test -s gate); a future intentional prompt change updates it deliberately — the regeneration path is documented in the test header.
</verification>

<success_criteria>
1. The committed golden snapshot is byte-identical to the deterministic packed output of a canonical fixture input (CTX-04/D-100); `hashStableSections` FNV-1a cross-checks against a committed golden hash.
2. A system-prompt change diffs the golden → the snapshot test fails → verify:phase-7 (re-pointed in 07-03) blocks release — the CTX-04 guarantee is live.
3. deriveContextQualityMetrics returns aggregates only and the no-raw-text boundary is proven (CTX-06/D-102/Contract B); the metrics field attaches to OptimizedContext additively.
4. No schema edits (manifest/A8/section-order untouched); assemble stays never-throw; zero strict-suppression markers; existing Phase-5 tests stay green.
</success_criteria>

<output>
Create `.planning/phases/07-trust-aware-context-and-receipts/07-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-7 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/types/harness.ts | TrustLevel / ContextItem / ContextReceiptEntry (C.1 verbatim) | 07-01 |
| src/core/context/trust/TrustPolicy.ts | AUTHORITY_BY_TRUST / applyTrustPolicy / isPolicyRedefinitionAttempt / raiseIfPolicyRedefinitionAttempt | 07-01 |
| src/core/context/trust/contextItems.ts | buildContextItems (D-94 item pipeline builder) | 07-01 |
| src/core/context/trust/ContextReceipt.ts | deriveContextReceipt → ContextReceiptSurface { entries, untrustedDataPresent } | 07-01 |
| src/core/context/trust/ContextQualityMetrics.ts | deriveContextQualityMetrics → ContextQualityMetrics (aggregates only, D-102) | **[this plan]** |
| src/core/context/trust/SkillDisclosure.ts | renderSkillDisclosure (CTX-05) | 07-03 |
| src/core/context/ContextOptimizer.ts | assemble() additive: item pipeline + originalTokens + rungs 1-2 (07-01) + receipt (07-01) + metrics attach (this plan) | 07-01 + **[this plan]** |
| tests/core/context/trust/TrustPolicy.test.ts | §18 O.3 wrap/strip + guard | 07-01 |
| tests/core/context/trust/contextItems.test.ts | §18 D-94 tagging | 07-01 |
| tests/core/context/trust/ContextReceipt.test.ts | §18 derivation rules | 07-01 |
| tests/core/context/trust/assemble-trust.test.ts | §18 TRACER end-to-end | 07-01 |
| tests/core/context/trust/ContextQualityMetrics.test.ts | §18 CTX-06 aggregates + no-raw-text marker assertion | **[this plan]** |
| tests/core/context/trust/stable-prefix.snapshot.test.ts | §18 CTX-04 byte-identity + FNV-1a cross-check | **[this plan]** |
| tests/core/context/trust/fixtures/stable-prefix.golden.txt | §18 committed golden (CTX-04 release block) | **[this plan]** |
| tests/core/context/trust/SkillDisclosure.test.ts | §18 CTX-05 zero-token proof | 07-03 |
| tests/security/prompt-injection/policy-redefinition.test.ts | §18 adversarial fixtures (CTX-02) | 07-01 |
| package.json | verify:phase-7 re-point (D-103, spec 3611) | 07-03 |