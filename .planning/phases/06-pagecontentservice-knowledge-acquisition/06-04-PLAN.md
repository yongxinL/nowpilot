---
phase: 06-pagecontentservice-knowledge-acquisition
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - src/core/extraction/PageIndexBuilder.ts
  - src/core/content/ContentScriptHost.ts
  - src/core/content/SPANavigationWatcher.ts
  - src/core/content/PageContextBridge.ts
  - src/core/runtime/RuntimeEnvelope.ts
  - entrypoints/content/core.content.ts
  - tests/core/extraction/PageIndexBuilder.test.ts
  - tests/core/content/ContentScriptHost.test.ts
  - tests/core/content/SPANavigationWatcher.test.ts
  - tests/core/content/PageContextBridge.test.ts
autonomous: true
requirements: []             # infra phase — no spec-native v1 IDs (ROADMAP Phase 6 note); acceptance = §18 DONE-when + D-84/D-85/D-87 + RESEARCH corrections 2
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - statement: "PageIndexBuilder builds a lazy, memoized, per-tab MiniSearch index over extracted markdown — chunked by heading (h1-h6) with preamble/paragraph-block/oversized-section rules; never persisted (D-87 / §26.5 — ROADMAP SC-4 'ephemeral per-tab MiniSearch index')"
      verification: backstop
    - statement: "When extracted tokens exceed the 2,000-token webpage budget (§22.2), selectRelevant(query) returns top-k chunks and records compressionApplied 'topk' — the Phase-5 CompressionType literal (D-87)"
      verification: backstop
    - statement: "The content script serializes a pre-stripped clone of document.documentElement (script/style/noscript/svg/cross-origin iframe + form action removed; text/headings/links/inputs kept), stamps the effective base URL, applies the PAGE_HTML_MAX_BYTES 2 MB cap with element-boundary truncation + truncated:true — the §26.6 serialization contract (ROADMAP SC-1, D-85)"
      verification: backstop
    - statement: "SPANavigationWatcher listens via the WXT content-script context: ctx.addEventListener(window, 'wxt:locationchange', …) — the namespaced event on window (RESEARCH correction 2; the scaffold's document-level listener was dead code) — plus the MutationObserver URL-diff fallback (ROADMAP SC-5, D-84/D-88)"
      verification: backstop
    - statement: "PageContextBridge handles EXTRACT_PAGE_CONTENT — mode 'default' responds with the serialized PageHtmlPayload, mode 'actionable' responds with the AxDomWalker RawNode tree; PAGE_LIVE_CONTEXT (title/url/meta) is sent on navigation; SPA_NAVIGATION feeds cache invalidation (D-84/D-89)"
      verification: backstop
    - statement: "The content bundle stays dependency-free: all src/core/content/* modules import only the runtime-envelope module (types) and siblings — never zod, never the panel-side layer — and the entry shell delegates instead of growing (D-85, Pitfall 8)"
      verification: backstop
  artifacts:
    - path: "src/core/extraction/PageIndexBuilder.ts"
      provides: "chunkMarkdown (heading/preamble/paragraph/oversized rules) + buildIndex (MiniSearch with synthesized id + tabId) + lazy per-tab memoized index + query() + selectRelevant() ('topk' record) + evict() wired into the 06-03 cache eviction-together hook"
      exports: ["PageIndexBuilder", "chunkMarkdown", "buildIndex", "PageChunk", "WEBPAGE_TOKEN_BUDGET", "PREAMBLE_CHUNK"]
    - path: "src/core/content/ContentScriptHost.ts"
      provides: "serializePage(budget?) — pre-stripped clone serialization + effective base URL stamp + 2 MB element-boundary truncation; sendHtmlPayload() — PAGE_HTML_PAYLOAD producer"
      exports: ["serializePage", "sendHtmlPayload", "stripForSerialization"]
    - path: "src/core/content/SPANavigationWatcher.ts"
      provides: "startWatcher(ctx, onNavigate) — ctx.addEventListener(window,'wxt:locationchange',…) + MutationObserver URL-diff fallback; emits SPA_NAVIGATION/PAGE_LIVE_CONTEXT"
      exports: ["startWatcher", "SPANavigationWatcher"]
    - path: "src/core/content/PageContextBridge.ts"
      provides: "initBridge(ctx, callbacks) — chrome.runtime.onMessage handler for EXTRACT_PAGE_CONTENT (default → ContentScriptHost payload; actionable → AxDomWalker tree); PAGE_LIVE_CONTEXT producer on navigation; typed response contract"
      exports: ["initBridge", "PageContextBridge", "ExtractRequestPayload", "ExtractResponse"]
    - path: "src/core/runtime/RuntimeEnvelope.ts"
      provides: "MODIFIED — adds PageLiveContextPayload { url, title, meta } interface following the frozen PageHtmlPayload pattern (D-89); MessageTypeValues untouched (D-15 types already declared)"
      exports: ["PageLiveContextPayload"]
    - path: "entrypoints/content/core.content.ts"
      provides: "MODIFIED — thin WXT shell keeps defineContentScript + delegates to SPANavigationWatcher/PageContextBridge/ContentScriptHost; dead document-level locationchange listener removed (RESEARCH correction 2)"
      exports: []
    - path: "tests/core/extraction/PageIndexBuilder.test.ts"
      provides: "§18 required test — heading chunking, preamble, no-heading paragraph blocks, oversized-section split (>500 tokens via countTokensHeuristic), MiniSearch hits, selectRelevant + 'topk', id synthesis, evict + cache-hook wiring, ephemerality"
      exports: []
    - path: "tests/core/content/ContentScriptHost.test.ts"
      provides: "serializer proof — pre-strip rules, base URL stamp, element-boundary truncation with parameterized budget + truncated flag, PAGE_HTML_PAYLOAD envelope shape"
      exports: []
    - path: "tests/core/content/SPANavigationWatcher.test.ts"
      provides: "watcher proof — ctx.addEventListener(window,'wxt:locationchange') registration (correction 2), onNavigate(newUrl, oldUrl) firing, MutationObserver fallback"
      exports: []
    - path: "tests/core/content/PageContextBridge.test.ts"
      provides: "bridge proof — EXTRACT_PAGE_CONTENT default → PageHtmlPayload response; actionable → RawNode tree response; PAGE_LIVE_CONTEXT emission; SPA_NAVIGATION invalidation signal"
      exports: []
  key_links:
    - from: "src/core/extraction/PageIndexBuilder.ts"
      to: "src/core/context/TokenBudget.ts"
      via: "countTokensHeuristic (Phase-5, D-71) sizes INDEX_CHUNK_MAX_TOKENS splits and the 2,000-token budget"
      pattern: "countTokensHeuristic"
    - from: "src/core/extraction/PageIndexBuilder.ts"
      to: "minisearch"
      via: "MiniSearch with fields [title,url,headingPath,sectionText], storeFields same, synthesized id (default idField is 'id' — Pitfall 5), searchOptions boost {title:3, headingPath:2}, prefix, fuzzy 0.2"
      pattern: "new MiniSearch"
    - from: "src/core/content/PageContextBridge.ts"
      to: "src/core/content/AxDomWalker.ts"
      via: "mode:'actionable' EXTRACT_PAGE_CONTENT → walkDom(document) → RawNode response (D-86 gating)"
      pattern: "walkDom"
    - from: "src/core/content/SPANavigationWatcher.ts"
      to: "src/core/extraction/PageContentCache.ts"
      via: "SPA_NAVIGATION envelope → cache init() listener invalidates the tab (06-03) — the ROADMAP SC-5 invalidation chain"
      pattern: "SPA_NAVIGATION"
    - from: "src/core/extraction/PageIndexBuilder.ts"
      to: "src/core/extraction/PageContentCache.ts"
      via: "eviction-together: builder.evict(tabId) registered into the cache's eviction hook — an index is never orphaned (§26.4a/D-88)"
      pattern: "evict"
  prohibitions:
    - "Content-side modules import ONLY the runtime-envelope module (types + createEnvelope) and sibling content-side modules — never the panel-side extraction/types layer, never zod (Pitfall 8; the 06-05 isolation grep is the hard enforcement)"
    - "No persistent index (D-87): PageIndexBuilder never touches IndexedDB/chrome.storage — grep-assertable; the persistent notes wrapper src/core/search/MiniSearchIndex.ts is Phase 8"
    - "No barrel index.ts (mirror src/core/ai layout)"
    - "Zero @ts-expect-error NP-STRICT markers (STATE.md decision 17)"
    - "No multi-envelope chunking (v0.1, §26.6): the serializer truncates at an element boundary and flags truncated:true — it does NOT split across envelopes"
    - "Do NOT register a BackgroundRouter handler for the extraction envelopes (D-84): the round-trip flows content-script → surface directly; BackgroundRouter stays stateless"
    - "No surface call-sites (D-81): the bridge/shells are proven by tests; live surface senders are Phase 7/15"
  security:
    - "The serializer strips form action attributes and never serializes password values (values are not serialized by the walker — D-86; the HTML payload itself may contain type=password elements but their values are user-typed runtime state, never in outerHTML)"
    - "cross-origin iframe markup is stripped at serialization (D-85) — no third-party DOM crosses into the panel"
---

<objective>
Expansion plan: build the phase's retrieval + producer side — `PageIndexBuilder` (lazy ephemeral per-tab MiniSearch over heading-chunked markdown, `selectRelevant` + 'topk' over the 2,000-token budget, eviction wired into the 06-03 cache hook) and the content-script shells (`ContentScriptHost` serializer, `SPANavigationWatcher` with the CORRECTED WXT event usage, `PageContextBridge` envelope producer/consumer) with the thin `core.content.ts` delegation and the `PageLiveContextPayload` envelope shape (D-89). Proven by the §18 `PageIndexBuilder.test.ts` plus three content-side shell tests.

Purpose: deliver the ROADMAP SC-4 retrieval guarantee ("PageIndexBuilder builds an ephemeral per-tab MiniSearch index, chunked by heading; never persisted"), the ROADMAP SC-1 serialization boundary ("content script only serializes a pre-stripped HTML clone with baseUrl + truncated"), the ROADMAP SC-5 invalidation signal source, and the D-84 producer/consumer wiring — with RESEARCH correction 2 (the namespaced `wxt:locationchange` event on `window` via the content-script context) embedded in the watcher.

Output: the ten files listed in frontmatter, strict-clean; the content bundle stays dependency-free by construction (imports limited to the envelope module + siblings).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md  # §26.5 MiniSearch integration (3763-3774 — chunking authoritative), §26.6 HTML payload + APC-lite depth (3776-3786), §26.4 canonical call (3718-3742), §22.2 webpage budget 2,000 tokens (3471-3504), §18 Phase 8 note (MiniSearchIndex is Phase 8, 2667)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-CONTEXT.md  # D-84 (envelope wiring), D-85 (content shells + serializer rules), D-87 (minisearch + PageIndexBuilder + no Phase-8 wrapper), D-89 (PAGE_LIVE_CONTEXT)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-RESEARCH.md  # Pattern 3 (heading-chunked index, 423-452), Pattern 6 (corrected SPA-nav usage, 454-472), Pitfalls 2/5/7/8, corrections 2, Open Question 2 (isolation mechanics — the negative grep lives in the 06-05 test)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-PATTERNS.md  # PageIndexBuilder.ts section (205-236), ContentScriptHost.ts section (314-323), PageContextBridge.ts section (325-336), SPANavigationWatcher.ts section (338-348), RuntimeEnvelope.ts section (293-306), core.content.ts section (308-312)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-VALIDATION.md  # 06-04-01 per-task map
@src/core/extraction/PageContentCache.ts  # the eviction-together hook (06-03) PageIndexBuilder wires into
@src/core/extraction/PageContentService.ts  # the panel-side orchestrator the bridge payloads feed (06-01)
@src/core/content/AxDomWalker.ts  # the walker the bridge invokes for mode:'actionable' (06-02)
@src/core/runtime/RuntimeEnvelope.ts  # D-15 declared types + PageHtmlPayload (40-46) — PageLiveContextPayload added here
@src/core/context/TokenBudget.ts  # countTokensHeuristic (44-56) — chunk sizing
@src/core/context/types.ts  # CompressionType ('topk' literal, line 50) — the selectRelevant record
@entrypoints/content/core.content.ts  # the thin shell being delegated (current 1-67)
@tests/setup.ts  # chrome mocks; __fireTabEvent (06-01)
@tests/isolation/cross-entrypoint-imports.test.ts  # the grep style the 06-05 isolation test extends (NOT this plan)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PageIndexBuilder — heading-chunked ephemeral per-tab MiniSearch + selectRelevant</name>
  <files>src/core/extraction/PageIndexBuilder.ts, tests/core/extraction/PageIndexBuilder.test.ts</files>
  <read_first>06-RESEARCH.md Pattern 3 (423-452 — chunkMarkdown rules + buildIndex options + id synthesis) + Pitfall 5 (id field mandatory) · PRODUCT_SPEC §26.5 (3763-3774 — chunking authoritative: preamble, paragraph fallback, oversized split, 2,000-token budget) + §22.2 (3471-3504) · 06-CONTEXT.md D-87 (minisearch ^7; no Phase-8 wrapper) · 06-PATTERNS.md PageIndexBuilder.ts section (205-236) · @src/core/context/TokenBudget.ts (countTokensHeuristic) · @src/core/context/types.ts (CompressionType 'topk') · @src/core/extraction/PageContentCache.ts (eviction hook to wire)</read_first>
  <behavior>
    - Test 1 (heading chunking): markdown with h1 + two h2 sections + preamble text → chunks = '(preamble)' chunk + one chunk per heading with headingPath breadcrumbs ('H1' → 'H1 > H2')
    - Test 2 (no-heading pages): markdown with zero headings → paragraph-block chunks (blank-line separated) under the page title
    - Test 3 (oversized sections): a heading section whose text exceeds INDEX_CHUNK_MAX_TOKENS (500, via countTokensHeuristic) → split into paragraph sub-chunks inheriting the same headingPath
    - Test 4 (id synthesis): every chunk carries a synthesized id ('{headingPath}:{index}') and the index-wide tabId — MiniSearch addAll does not throw (Pitfall 5)
    - Test 5 (query): MiniSearch returns hits with stored fields for a keyword query; boost favors title matches
    - Test 6 (selectRelevant): a tab whose extracted tokens exceed 2,000 → selectRelevant(query) returns top-k chunks and { compressionApplied: 'topk' } (the CompressionType literal)
    - Test 7 (lazy + memoized): the index is built on first query() for a tab and reused on the second (spy the chunker); never persisted (no storage imports)
    - Test 8 (evict + hook): evict(tabId) drops the memoized index; the builder's evict is registered into the 06-03 cache eviction hook so extraction+index are always evicted together
  </behavior>
  <action>Create `src/core/extraction/PageIndexBuilder.ts` — the ephemeral per-tab retrieval index (D-87). Test-first: write tests/core/extraction/PageIndexBuilder.test.ts (RED), then implement (GREEN).

`chunkMarkdown(markdown, tabId, title, url): PageChunk[]` — verbatim §26.5 rules: (1) split on /^#{1,6}\s/ heading lines tracking a breadcrumb headingPath per heading; (2) pre-heading text → synthetic '(preamble)' chunk (export const PREAMBLE_CHUNK = '(preamble)'); (3) no headings → blank-line-separated paragraph blocks under the page title; (4) a section exceeding INDEX_CHUNK_MAX_TOKENS (500 — import from strategies/IExtractionStrategy) per countTokensHeuristic → paragraph sub-chunks inheriting the same headingPath; (5) `PageChunk = { id: string; tabId: number; title: string; url: string; headingPath: string; sectionText: string }` with id = `${headingPath}:${index}` (Pitfall 5 — MiniSearch's default idField is 'id').

`buildIndex(chunks: PageChunk[]): MiniSearch<PageChunk>` — `new MiniSearch<PageChunk>({ fields: ['title','url','headingPath','sectionText'], storeFields: same, searchOptions: { boost: { title: 3, headingPath: 2 }, prefix: true, fuzzy: 0.2 } }).addAll(chunks)`.

`PageIndexBuilder` (module singleton, ProviderRegistry style): per-tab memoized lazy index — `getIndex(tabId, source: { markdown, title, url }): MiniSearch<PageChunk>` builds on first call (chunkMarkdown + buildIndex), memoizes, reuses thereafter; `query(tabId, q, source)` builds lazily then searches; `selectRelevant(tabId, q, source): { chunks: PageChunk[]; compressionApplied: CompressionType }` — used when the tab's extracted tokens exceed WEBPAGE_TOKEN_BUDGET = 2000 (exported const, §22.2): searches and returns the top-k chunk results with `compressionApplied: 'topk'` (the Phase-5 CompressionType literal imported from src/core/context/types — the manifest record is the Phase-7 receipt's consumer, D-87); `evict(tabId)` drops the memoized index. Wire the eviction-together invariant: on module load, register `evict` into the 06-03 PageContentCache eviction hook (one-directional import builder → cache; the cache never imports the builder) so an extraction eviction always drops the index too (§26.4a 'never orphan an index'). Header comment: ephemeral by §26.5 — never persisted; the persistent notes wrapper (src/core/search/MiniSearchIndex.ts) is Phase 8, NOT built here. Zero chrome/storage imports (grep-assertable).</action>
  <acceptance_criteria>`npx vitest run tests/core/extraction/PageIndexBuilder.test.ts` passes (all eight behavior groups green); `pnpm run lint` passes strict-clean; `grep -n "countTokensHeuristic" src/core/extraction/PageIndexBuilder.ts` shows the Phase-5 heuristic reuse; `grep -n "compressionApplied: 'topk'" src/core/extraction/PageIndexBuilder.ts` shows the CompressionType literal; `grep -c "chrome.storage\|indexedDB\|idb" src/core/extraction/PageIndexBuilder.ts` equals 0 (ephemeral — §26.5); `grep -rn "NP-STRICT" src/core/extraction/ | wc -l` equals 0.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/extraction/PageIndexBuilder.test.ts && pnpm run lint && test "$(grep -c 'chrome.storage\|indexedDB\|idb' src/core/extraction/PageIndexBuilder.ts)" -eq 0 && grep -q "compressionApplied: 'topk'" src/core/extraction/PageIndexBuilder.ts && test "$(grep -rn 'NP-STRICT' src/core/extraction/ | wc -l)" -eq 0</automated>
  </verify>
  <done>PageIndexBuilder builds lazy memoized per-tab MiniSearch indexes with the verbatim §26.5 chunking rules, selectRelevant returns top-k with the 'topk' compression record over the 2,000-token budget, evict is wired into the cache's eviction-together hook, and the index is ephemeral by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContentScriptHost serializer + SPANavigationWatcher (corrected WXT event) + PageLiveContextPayload</name>
  <files>src/core/content/ContentScriptHost.ts, src/core/content/SPANavigationWatcher.ts, src/core/runtime/RuntimeEnvelope.ts</files>
  <read_first>06-RESEARCH.md Pattern 6 (454-472 — ctx.addEventListener(window,'wxt:locationchange',…) translates the namespaced event AND starts the LocationWatcher) + Pitfall 2 (dead document listener) + §26.6 serializer rules (3776-3786) · 06-CONTEXT.md D-85 (pre-strip list, base URL, 2 MB cap, element-boundary truncation, no multi-envelope chunking) + D-89 (PAGE_LIVE_CONTEXT) · 06-PATTERNS.md ContentScriptHost.ts section (314-323) + SPANavigationWatcher.ts section (338-348) + RuntimeEnvelope.ts section (293-306) · @src/core/runtime/RuntimeEnvelope.ts (PageHtmlPayload pattern 40-46; MessageTypeValues untouched) · @entrypoints/content/core.content.ts (current shell — the MutationObserver URL-diff to keep as fallback)</read_first>
  <behavior>
    - Test 1 (ContentScriptHost pre-strip): a fixture DOM with script/style/noscript/svg/cross-origin iframe/form action + text/headings/links/inputs serializes with the strip list removed and the keep-list retained
    - Test 2 (ContentScriptHost base URL): the serialized payload's baseUrl equals document.baseURI (the effective base URL stamp)
    - Test 3 (ContentScriptHost truncation): with a parameterized budget (small), serialization truncates at an element boundary and sets truncated:true — never mid-element (invalid-HTML-producing cut)
    - Test 4 (ContentScriptHost envelope): sendHtmlPayload() sends a PAGE_HTML_PAYLOAD envelope via chrome.runtime.sendMessage with the frozen PageHtmlPayload shape
    - Test 5 (SPANavigationWatcher): startWatcher registers ctx.addEventListener(window, 'wxt:locationchange', handler) — the corrected namespaced usage — and the handler fires onNavigate(newUrl.href, oldUrl.href)
    - Test 6 (SPANavigationWatcher fallback): the MutationObserver URL-diff path detects a URL change and fires onNavigate (belt-and-braces)
  </behavior>
  <action>Test-first for both modules; the RuntimeEnvelope addition is a type-only edit (covered by tsc).

1. `src/core/content/ContentScriptHost.ts` — the serializer shell (D-85). `serializePage(budget: number = PAGE_HTML_MAX_BYTES): PageHtmlPayload` — (a) deep-clone document.documentElement (cloneNode(true)); (b) remove `script`, `style`, `noscript`, `svg`, and cross-origin `iframe` markup (same-origin iframes are kept per D-85's 'cross-origin iframe' wording — compare the iframe's src origin against location.origin) and strip `form action` attributes; (c) keep text, headings, links, and input controls; (d) compute the serialized string and if it exceeds `budget` (default PAGE_HTML_MAX_BYTES = 2_000_000 imported from strategies/IExtractionStrategy — a hard cap, §26.6), truncate at an ELEMENT boundary (walk the clone's elements in order, serialize children incrementally, stop before the next element that would overflow — never a mid-element cut, which produces invalid HTML that DOMParser must repair lossily, RESEARCH Pitfall) and set `truncated: true`; (e) return `{ html, baseUrl: document.baseURI, truncated }` — the frozen PageHtmlPayload shape (spec 40-46). Export `stripForSerialization(root: Element): Element` (the pre-strip, testable). `sendHtmlPayload(payload?)` — `chrome.runtime.sendMessage(createEnvelope('PAGE_HTML_PAYLOAD', payload, 'content')).catch(() => {})` (the producer round-trip shape, core.content.ts:24-32 precedent). Relative imports within src/core/content/ and to the envelope module (WXT does not typecheck content scripts through tsconfig — keep the entrypoint import style).

2. `src/core/content/SPANavigationWatcher.ts` — `startWatcher(ctx: ContentScriptContextLike, onNavigate: (newUrl: string, oldUrl: string) => void): () => void` — (a) the CORRECTED listener (RESEARCH correction 2 + Pitfall 2): `ctx.addEventListener(window, 'wxt:locationchange', ({ newUrl, oldUrl }) => onNavigate(newUrl.href, oldUrl.href))` — via the WXT content-script context, which translates the namespaced event name AND starts the LocationWatcher (Navigation API first, 1 s polling fallback); the scaffold's document-level listener (core.content.ts:49) was dead code — do not replicate it; (b) keep the MutationObserver URL-diff fallback (scaffold core.content.ts:36-43 pattern) for pre-0.20.27 parity — fires onNavigate when location.href changes; (c) returns a cleanup function. `ContentScriptContextLike = { addEventListener(target: Window, type: string, handler: (e: any) => void): void }` — the minimal structural type making the module testable with a stub ctx (jsdom cannot run the real WXT context). Export the type.

3. `src/core/runtime/RuntimeEnvelope.ts` — MODIFY: append the `PageLiveContextPayload { url: string; title: string; meta: Record<string, string> }` interface following the frozen PageHtmlPayload pattern (lines 40-46) with a comment naming D-89 (lightweight live context on navigation). Do NOT touch MessageTypeValues (the four extraction types are already declared, D-15) or createEnvelope/isEnvelope.</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `grep -n "PAGE_HTML_MAX_BYTES" src/core/content/ContentScriptHost.ts` shows the 2 MB cap import; `grep -n "document.baseURI" src/core/content/ContentScriptHost.ts` shows the base URL stamp; `grep -n "ctx.addEventListener(window, 'wxt:locationchange'" src/core/content/SPANavigationWatcher.ts` shows the corrected WXT usage (correction 2); `grep -n "PageLiveContextPayload" src/core/runtime/RuntimeEnvelope.ts` shows the added payload type; `grep -rn "NP-STRICT" src/core/content/ | wc -l` equals 0. (The dead document-level listener removal is asserted in Task 3, which edits the entry shell.)</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && grep -q "ctx.addEventListener(window, 'wxt:locationchange'" src/core/content/SPANavigationWatcher.ts && grep -q "PageLiveContextPayload" src/core/runtime/RuntimeEnvelope.ts && test "$(grep -rn 'NP-STRICT' src/core/content/ | wc -l)" -eq 0</automated>
  </verify>
  <done>ContentScriptHost serializes the pre-stripped clone with base URL stamp + element-boundary 2 MB truncation; SPANavigationWatcher uses the corrected ctx.addEventListener(window, 'wxt:locationchange') + MutationObserver fallback; RuntimeEnvelope carries the PageLiveContextPayload shape — all strict-clean with the dead listener removed from the entry shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PageContextBridge + core.content.ts delegation + the three content tests</name>
  <files>src/core/content/PageContextBridge.ts, entrypoints/content/core.content.ts, tests/core/content/ContentScriptHost.test.ts, tests/core/content/SPANavigationWatcher.test.ts, tests/core/content/PageContextBridge.test.ts</files>
  <read_first>06-PATTERNS.md PageContextBridge.ts section (325-336 — MessageBus init() handler + sendResponse pattern) + core.content.ts section (308-312 — thin shell + relative import convention) · 06-CONTEXT.md D-84 (producer/consumer wiring; EXTRACT_PAGE_CONTENT handling; BackgroundRouter stays stateless) + D-89 (live context always, full extraction on demand) · 06-RESEARCH.md Pattern 6 + System Architecture diagram (bridge block) · @src/core/runtime/RuntimeEnvelope.ts (envelope types + createEnvelope + isEnvelope) · @src/core/content/ContentScriptHost.ts + @src/core/content/SPANavigationWatcher.ts + @src/core/content/AxDomWalker.ts (the siblings the bridge orchestrates) · @entrypoints/content/core.content.ts (the shell to delegate)</read_first>
  <behavior>
    - Test 1 (bridge default mode): an EXTRACT_PAGE_CONTENT envelope with mode 'default' → the registered handler responds with { ok:true, payload } where payload is the frozen PageHtmlPayload shape (html/baseUrl/truncated)
    - Test 2 (bridge actionable mode): an EXTRACT_PAGE_CONTENT envelope with mode 'actionable' → responds with { ok:true, raw } where raw is a RawNode tree produced by walkDom on the test document (password values absent — D-86)
    - Test 3 (bridge unknown type): a non-extraction envelope → no response / ignored (does not crash)
    - Test 4 (bridge live context): on navigation the bridge sends a PAGE_LIVE_CONTEXT envelope with { url, title, meta } (chrome.runtime.sendMessage spy)
    - Test 5 (entry shell delegation): core.content.ts main(ctx) wires startWatcher + initBridge and no longer registers the document-level listener; CONTENT_SCRIPT_READY still sent
    - Test 6 (ContentScriptHost suite): the three behavior groups from Task 2's behavior block (pre-strip, base URL, truncation, envelope shape)
    - Test 7 (SPANavigationWatcher suite): the two behavior groups from Task 2 (ctx registration + MutationObserver fallback)
  </behavior>
  <action>Test-first: write the three content test files (RED), then implement PageContextBridge + the entry-shell delegation (GREEN).

1. `src/core/content/PageContextBridge.ts` — `initBridge(ctx: ContentScriptContextLike, callbacks: { onNavigate(newUrl, oldUrl): void }): () => void` — the RuntimeEnvelope producer/consumer (D-84). (a) Register a `chrome.runtime.onMessage` handler (MessageBus init() pattern — capture via addListener; respond via sendResponse): if the message is an EXTRACT_PAGE_CONTENT envelope (isEnvelope guard + type check), read the payload `{ tabId, url, mode }` (export the typed `ExtractRequestPayload { tabId: number; url: string; mode: 'default' | 'actionable' }`): mode 'default' → `const payload = serializePage()` from ContentScriptHost, respond `{ ok: true, payload }`; mode 'actionable' → `const raw = walkDom(document.documentElement)` from AxDomWalker (06-02), respond `{ ok: true, raw }` (typed `ExtractResponse = { ok: true; payload?: PageHtmlPayload; raw?: RawNode } | { ok: false; error: string }` — zod-free, the content bundle never imports zod); unknown message types → return false (pass through, no response). (b) Wire the SPANavigationWatcher: startWatcher(ctx, (newUrl, oldUrl) => { callbacks.onNavigate(newUrl, oldUrl); chrome.runtime.sendMessage(createEnvelope('SPA_NAVIGATION', { url: newUrl }, 'content')).catch(() => {}); chrome.runtime.sendMessage(createEnvelope('PAGE_LIVE_CONTEXT', { url: newUrl, title: document.title, meta: {} }, 'content')).catch(() => {}); }) — the lightweight live context (D-89) always on navigation; SPA_NAVIGATION feeds the 06-03 cache invalidation. (c) Return a cleanup function (removeListener + watcher cleanup). Comment: BackgroundRouter stays stateless — the round-trip flows content-script → surface directly via sendResponse / runtime messages (D-84); no background handler is registered.

2. `entrypoints/content/core.content.ts` — MODIFY (thin delegation, D-85): keep the defineContentScript shell + matches/runAt/world; in main(ctx): call `startWatcher(ctx, onNavigate)` + `initBridge(ctx, { onNavigate })` from the new shells (relative imports '../../src/core/content/…' — WXT does not typecheck content scripts through tsconfig); REMOVE the dead document-level locationchange listener (RESEARCH correction 2) and its cleanup; keep the MutationObserver URL-diff ONLY as the watcher's fallback (it now lives in SPANavigationWatcher — the entry shell does not duplicate it); keep the CONTENT_SCRIPT_READY send; return the combined cleanup.

3. Tests — `tests/core/content/ContentScriptHost.test.ts` (behavior groups 1-4 from Task 2's block — jsdom document fixture, chrome.runtime.sendMessage vi.fn spy), `tests/core/content/SPANavigationWatcher.test.ts` (groups 5-6: stub ctx capturing addEventListener calls; invoke the captured handler with { newUrl: new URL(...), oldUrl: new URL(...) }; MutationObserver fallback via a jsdom URL-diff simulation), `tests/core/content/PageContextBridge.test.ts` (groups 1-5: chrome.runtime.onMessage listener capture — test-local mock, do NOT edit tests/setup.ts again; EXTRACT_PAGE_CONTENT envelopes; walkDom on the jsdom document for the actionable path). All '@/' or relative imports per the content test location; no zod imports.</action>
<!-- planner-discipline-allow: wxt:locationchange -->
<!-- planner-discipline-allow: document.addEventListener('wxt:locationchange' -->
  <acceptance_criteria>`npx vitest run tests/core/content/ContentScriptHost.test.ts tests/core/content/SPANavigationWatcher.test.ts tests/core/content/PageContextBridge.test.ts` passes (all seven behavior groups green); `pnpm run lint` passes strict-clean; `grep -n "walkDom" src/core/content/PageContextBridge.ts` shows the actionable-path invocation (D-86 gating); `grep -n "EXTRACT_PAGE_CONTENT" src/core/content/PageContextBridge.ts` shows the handler; `grep -rn "NP-STRICT" src/core/content/ entrypoints/content/ | wc -l` equals 0; `grep -c "document.addEventListener('wxt:locationchange'" entrypoints/content/core.content.ts` equals 0 (dead listener removed — correction 2); `grep -rn "NP-STRICT" src/core/content/ | wc -l` equals 0.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/content/ContentScriptHost.test.ts tests/core/content/SPANavigationWatcher.test.ts tests/core/content/PageContextBridge.test.ts && pnpm run lint && grep -q "EXTRACT_PAGE_CONTENT" src/core/content/PageContextBridge.ts && test "$(grep -rn 'NP-STRICT' src/core/content/ entrypoints/content/ | wc -l)" -eq 0 && test "$(grep -c "document.addEventListener('wxt:locationchange'" entrypoints/content/core.content.ts)" -eq 0</automated>
  </verify>
  <done>PageContextBridge handles EXTRACT_PAGE_CONTENT (default → PageHtmlPayload, actionable → RawNode via AxDomWalker), emits PAGE_LIVE_CONTEXT + SPA_NAVIGATION on navigation, and the entry shell delegates to the new modules with the dead listener removed — proven by the three content test files, content-bundle-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-page DOM → ContentScriptHost serializer | Untrusted live markup is serialized here — stripped (script/style/svg/cross-origin iframe/form action) but never parsed or executed; ISOLATED world |
| content-script → panel envelope payloads | PageHtmlPayload / RawNode / PAGE_LIVE_CONTEXT cross into the surface — the 2 MB cap bounds them; the panel validates via zod (06-01/06-02) |
| surface → content-script EXTRACT_PAGE_CONTENT | A surface request crosses in — the bridge gates by mode and never responds to non-extraction messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-P6-03 | Tampering | content bundle dependencies | high | mitigate | All src/core/content/* modules import only the envelope module (types) + siblings — no zod, no panel-side layer, no defuddle-family libs (Pitfall 8); the built-bundle isolation grep (06-05) is the hard enforcement; this plan's greps assert the module-level boundary now |
| T-P6-15 | Information Disclosure | serialized HTML payload | high | mitigate | The serializer strips script/style/svg/noscript/cross-origin iframe markup + form action attributes; password VALUES are never serialized (input values are runtime state, absent from outerHTML; the walker omits them at capture — D-86); panel-side redaction (06-01, D-90) is the second layer |
| T-P6-16 | Denial of Service | oversized payload | medium | mitigate | PAGE_HTML_MAX_BYTES 2 MB hard cap with element-boundary truncation + truncated:true (§26.6); no multi-envelope chunking in v0.1 — the bound is hard |
| T-P6-17 | Tampering | cross-origin iframe content | medium | mitigate | cross-origin iframe markup removed at serialization (D-85) — third-party DOM never crosses into the panel; same-origin iframes kept per D-85 wording |
| T-P6-18 | Spoofing | host-page navigation events | low | accept | wxt:locationchange + MutationObserver signals drive invalidation only (never trusted as instructions); a spoofed signal at worst forces a re-extract — cache treats them as data (CTX-02 trust hardening is Phase 7) |
| T-P6-SC | Tampering | npm installs | high | mitigate | N/A this plan — zero new dependencies (deps landed in 06-01 behind the blocking-human gate; minisearch consumed here was [OK] in the audit) |
</threat_model>

<verification>
- Per-task commits: `pnpm run lint` + the affected test file.
- TDD discipline: all three tasks test-first with `<behavior>` blocks; every task has an automated `<verify>` (Nyquist holds).
- Grep guards: zero NP-STRICT in src/core/content/ + entrypoints/content/; dead document-level listener gone (correction 2); PageIndexBuilder has zero storage imports (ephemeral); content modules import only the envelope module + siblings (the exhaustive negative grep is the 06-05 isolation test — this plan asserts the positive boundary).
- Create-only (D-81): no surface call-sites — the bridge/shells are proven by tests; live senders are Phase 7/15.
- Cross-plan file discipline: RuntimeEnvelope.ts is the ONLY 06-01-adjacent file modified (additive PageLiveContextPayload interface — MessageTypeValues untouched); PageContentCache.ts (06-03) is consumed via its eviction hook, not modified.
</verification>

<success_criteria>
1. PageIndexBuilder builds lazy memoized per-tab MiniSearch indexes with the verbatim §26.5 chunking rules (preamble/paragraph/oversized), selectRelevant over the 2,000-token budget with the 'topk' record, and eviction wired into the cache hook — ephemeral by construction (ROADMAP SC-4).
2. ContentScriptHost serializes the pre-stripped clone with base URL stamp + element-boundary 2 MB truncation (ROADMAP SC-1).
3. SPANavigationWatcher uses the corrected ctx.addEventListener(window, 'wxt:locationchange') + MutationObserver fallback; PageContextBridge wires the D-84 producer/consumer with PAGE_LIVE_CONTEXT (D-89).
4. The entry shell delegates and the dead document-level listener is gone; the content bundle stays dependency-free.
5. `npx vitest run tests/core/extraction/PageIndexBuilder.test.ts tests/core/content/*.test.ts` green; `pnpm run lint` strict-clean.
</success_criteria>

<output>
Create `.planning/phases/06-pagecontentservice-knowledge-acquisition/06-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/extraction/PageIndexBuilder.ts | chunkMarkdown + buildIndex + lazy per-tab memoized MiniSearch + query + selectRelevant ('topk') + evict (cache-hook wired) + WEBPAGE_TOKEN_BUDGET/PREAMBLE_CHUNK | **[this plan]** |
| src/core/content/ContentScriptHost.ts | serializePage(budget?) + stripForSerialization + sendHtmlPayload — pre-strip + base URL + 2 MB element-boundary truncation | **[this plan]** |
| src/core/content/SPANavigationWatcher.ts | startWatcher(ctx, onNavigate) — corrected ctx.addEventListener(window,'wxt:locationchange') + MutationObserver fallback + ContentScriptContextLike | **[this plan]** |
| src/core/content/PageContextBridge.ts | initBridge + ExtractRequestPayload + ExtractResponse — EXTRACT_PAGE_CONTENT handler, PAGE_LIVE_CONTEXT/SPA_NAVIGATION producer | **[this plan]** |
| src/core/runtime/RuntimeEnvelope.ts | + PageLiveContextPayload interface (D-89) — MessageTypeValues untouched | **[this plan]** |
| entrypoints/content/core.content.ts | thin delegation shell — dead listener removed (correction 2) | **[this plan]** |
| tests/core/extraction/PageIndexBuilder.test.ts | §18 required test — chunking/preamble/paragraph/oversized/id/query/selectRelevant/evict | **[this plan]** |
| tests/core/content/ContentScriptHost.test.ts | serializer proof — pre-strip, base URL, truncation, envelope shape | **[this plan]** |
| tests/core/content/SPANavigationWatcher.test.ts | watcher proof — ctx registration + MutationObserver fallback | **[this plan]** |
| tests/core/content/PageContextBridge.test.ts | bridge proof — default/actionable responses, live context, unknown-type pass-through | **[this plan]** |
| (consumed) src/core/extraction/PageContentCache.ts | eviction-together hook | 06-03 (unchanged here) |