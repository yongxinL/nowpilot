---
phase: 06-pagecontentservice-knowledge-acquisition
plan: 05
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - tests/isolation/no-content-script-ui.test.ts
  - package.json
  - .planning/adr/ADR-P6-01-defuddle-panel-side.md
autonomous: true
requirements: []             # infra phase — no spec-native v1 IDs (ROADMAP Phase 6 note); acceptance = §18 DONE-when + D-79/D-92
user_setup: []

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - statement: "The isolation test is non-vacuous: it greps the forbidden dependency set (§24 rev 2026-08-12) across the content-script source modules AND the built .wxt bundle when present, rejects the File System Access API, and self-tests its own regex — a silently weakened gate trips its own self-test (ROADMAP SC-2 'no React/AntD/defuddle/yaml in the content bundle, <50 KB')"
      verification: backstop
    - statement: "verify:phase-6 is re-pointed to the §18 required test dirs — tests/core/extraction tests/core/content tests/isolation/no-content-script-ui.test.ts — and the stale phase-4a placeholder script is deleted (D-92, D-68/D-78 precedent)"
      verification: backstop
    - statement: "ADR-P6-01 Status flips to Accepted with the SPIKE-P6-01 outcome recorded — detached-doc fidelity acceptable (guarded getComputedStyle in defuddle; inline-only visibility in Readability 0.6.0), evidence = the DefuddleStrategy.test.ts real-engine tests from 06-01, no measurement pass needed (D-79)"
      verification: backstop
    - statement: "pnpm verify:phase-6 passes end-to-end — the phase gate is GREEN (ROADMAP SC-7 / §24)"
      verification: backstop
  artifacts:
    - path: "tests/isolation/no-content-script-ui.test.ts"
      provides: "§18/§24 isolation gate — greps entrypoints/content/** + src/core/content/** source imports AND the built .wxt content bundle (when present) for the forbidden list (React family, the panel-only extraction engine, yaml, math converters, the HTML→MD converter) + File System Access API; asserts the built bundle stays < 50 KB when measurable; non-vacuous self-test block (cross-entrypoint-imports.test.ts style)"
      exports: []
    - path: "package.json"
      provides: "verify:phase-6 re-pointed per D-92 (currently mis-pointed at Phase-11 dirs — telemetry/DiagnosticsSection); stale phase-4a script removed"
      exports: []
    - path: ".planning/adr/ADR-P6-01-defuddle-panel-side.md"
      provides: "Status → Accepted + SPIKE-P6-01 outcome record (D-79) — detached-doc fidelity evidence from the 06-01 tracer tests"
      exports: []
  key_links:
    - from: "tests/isolation/no-content-script-ui.test.ts"
      to: "entrypoints/content/** + src/core/content/**"
      via: "the isolation grep targets the very modules 06-02/06-04 created — the hard enforcement of the Pitfall-8 import boundary"
      pattern: "grep|execSync"
    - from: "package.json"
      to: "tests/core/extraction + tests/core/content + tests/isolation/no-content-script-ui.test.ts"
      via: "verify:phase-6 = 'tsc --noEmit && vitest run tests/core/extraction tests/core/content tests/isolation/no-content-script-ui.test.ts' — the D-92 re-point (D-68/D-78 precedent)"
      pattern: "verify:phase-6"
    - from: ".planning/adr/ADR-P6-01-defuddle-panel-side.md"
      to: "tests/core/extraction/DefuddleStrategy.test.ts"
      via: "the ADR's spike-outcome record cites the 06-01 real-engine detached-doc tests as the fidelity evidence (D-79)"
      pattern: "SPIKE-P6-01"
  prohibitions:
    - "Do NOT fix verify:phase-7's mis-pointing (package.json line 26 targets Phase-15 dirs) — out of Phase-6 scope (RESEARCH Open Question 3); flag it in the plan SUMMARY for the Phase-7 planner"
    - "No module edits in this plan — test file + package.json + ADR only; any red module surfaced by the gate is a bug for the owning plan's correction, not a gate weakening"
    - "Zero @ts-expect-error NP-STRICT markers in the new test file (STATE.md decision 17)"
    - "The isolation test must be non-vacuous — a self-test block proves its regex catches a planted violation; a gate that cannot fail is a gate that measures nothing (Pitfall 6 / #1479)"
  security:
    - "The isolation grep is the §24 enforcement point: it must reject the panel-side extraction engine AND its transitive math/HTML deps in the CONTENT bundle — bundlers hoist differently than source, so the built bundle is checked when present (Pitfall 7)"
---

<objective>
Final plan: close the phase's verification surface — the §18/§24 isolation gate (`tests/isolation/no-content-script-ui.test.ts`, non-vacuous built-bundle + source grep), the D-92 gate re-point (`verify:phase-6` → the §18 required test dirs; the stale phase-4a placeholder removed), and the D-79 ADR flip (`ADR-P6-01` → Accepted with the SPIKE-P6-01 outcome recorded from the 06-01 tracer evidence). Ends with `pnpm verify:phase-6` GREEN — the phase's definition of done.

Purpose: deliver ROADMAP SC-2 ("Content-script bundle contains no React, AntD, defuddle, or yaml, and stays < 50 KB" — enforced by test, not by hope), ROADMAP SC-7 ("pnpm run verify:phase-6 passes"), and close the ADR-P6-01 loop (Proposed → Accepted with evidence). The gate mechanics follow the RESEARCH Open Question 2 recommendation: source-import grep (fast, deterministic) + built-bundle grep + size assertion when the .wxt output is present (the gate must not depend on a build).

Output: the three files listed in frontmatter + a GREEN phase gate.
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md  # §18 Phase 6 DONE-when + Required tests (2608-2644), §24 isolation grep rev 2026-08-12 (3631), §22.1 50 KB bundle target (3471-3476)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-CONTEXT.md  # D-79 (ADR flip), D-92 (gate re-point + phase-4a reconcile)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-RESEARCH.md  # Pitfall 6 (vacuous gates), Pitfall 7 (grep the BUILT bundle), Open Question 2 (source vs built mechanics), Open Question 3 (phase-7 mis-pointing — flag only), Security Domain T-P6-03
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-PATTERNS.md  # metadata note 4 (verify:phase-6/phase-4a mis-pointing) + the no-content-script-ui test analog section
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-VALIDATION.md  # 06-05-01/06-05-02 per-task map + Sampling Rate (full suite at gate)
@.planning/adr/ADR-P6-01-defuddle-panel-side.md  # the ADR to flip (Status line 3 + Verification section)
@package.json  # lines 22 (phase-4a) and 25 (verify:phase-6) — the D-92 edits
@tests/isolation/cross-entrypoint-imports.test.ts  # the non-vacuous grep style to extend (self-test block pattern, 84-124)
@entrypoints/content/core.content.ts + @src/core/content/*  # the modules the isolation grep guards
@.planning/phases/05-context-adaptive-execution/05-02-SUMMARY.md  # the D-78 gate re-point precedent (verify:phase-5)
@.planning/phases/04-agent-reliability-and-evidence/04-01-SUMMARY.md  # the D-68 gate re-point precedent (verify:phase-4)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: no-content-script-ui.test.ts — the §18/§24 isolation gate (built-bundle + source grep, non-vacuous)</name>
  <files>tests/isolation/no-content-script-ui.test.ts</files>
  <read_first>06-RESEARCH.md Pitfall 7 (grep the BUILT bundle — transitive math deps resolve at build) + Pitfall 6 (vacuous-gate failure mode) + Open Question 2 (recommendation: source grep fast + bundle-size assertion when present) · PRODUCT_SPEC §24 rev 2026-08-12 (3631 — the forbidden set) + §22.1 (50 KB) · 06-CONTEXT.md D-92 (built-bundle grep + File System Access API + self-test) · @tests/isolation/cross-entrypoint-imports.test.ts (the grep + self-test style to extend — comment-strip discipline at 37-47, self-test block at 84-124) · @entrypoints/content/core.content.ts + @src/core/content/ (the guarded modules)</read_first>
  <behavior>
    - Test 1 (source-grep): a planted forbidden import in a scratch path is caught by the regex (the self-test proves non-vacuous)
    - Test 2 (source-grep real): the actual entrypoints/content/** + src/core/content/** modules contain zero forbidden imports (the §24 set + File System Access API)
    - Test 3 (built bundle): when .wxt/ content-script output exists, the same forbidden set is absent from the built bundle and the bundle size is under the 50 KB target; when no build artifact exists the check is skipped with a documented note (the gate must not depend on a build — Open Question 2)
    - Test 4 (self-test negative): a legit shared-infra import (the envelope module) is NOT flagged
  </behavior>
  <action>Create `tests/isolation/no-content-script-ui.test.ts` — the phase's isolation gate (cross-entrypoint-imports.test.ts style: execSync greps + a self-test block proving the regex is non-vacuous). Test-first: write the self-test cases first (they must fail against a regex that doesn't catch the forbidden set), then the real greps.

Structure: (a) a `FORBIDDEN_RE` regex matching the §24 rev 2026-08-12 forbidden set as import statements — the React family (react, react-dom, antd), the panel-only extraction engine (defuddle — including its transitive math/HTML deps mathml-to-latex, temml, turndown), yaml, plus the File System Access API (showDirectoryPicker / showOpenFilePicker); (b) source grep via execSync over `entrypoints/content/` + `src/core/content/` with the comment-strip discipline from cross-entrypoint-imports.test.ts:37-47 (strip 'path:line:' prefixes first, then leading '//' comments — an instructional comment must not trip the gate); (c) built-bundle grep: if `.wxt/` output exists, run the same regex over the built content-script chunks AND assert the content chunk stays under the 50 KB target (§22.1) — using a byte-count assertion on the relevant .wxt artifact; if no build artifact exists, `it.skip`-style skip with a comment (the gate stays fast — Open Question 2 recommendation; the executor MAY run `pnpm build:ext` once to materialize the bundle for the built check, but the gate's automated verify must pass without it); (d) the self-test block: assert FORBIDDEN_RE catches planted violations (e.g. a scratch line importing each forbidden package) and does NOT flag a legit envelope-module import — non-vacuous, exactly the cross-entrypoint-imports.test.ts:84-124 pattern. Zero NP-STRICT markers. The test is the hard enforcement of the Pitfall-8 boundary the 06-02/06-04 modules were built against.</action>
  <acceptance_criteria>`npx vitest run tests/isolation/no-content-script-ui.test.ts` passes (all four behavior groups green); `grep -c "describe(" tests/isolation/no-content-script-ui.test.ts` >= 2 (real-grep describe + self-test describe); `grep -n "execSync" tests/isolation/no-content-script-ui.test.ts` shows the grep mechanism; `grep -rn "NP-STRICT" tests/isolation/ | wc -l` equals 0; `pnpm run lint` passes.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/isolation/no-content-script-ui.test.ts && pnpm run lint && test "$(grep -rn 'NP-STRICT' tests/isolation/ | wc -l)" -eq 0</automated>
  </verify>
  <done>The isolation gate exists and is green: it greps the forbidden §24 set across content-script source AND the built bundle when present, asserts the <50 KB target when measurable, self-tests its own regex (non-vacuous), and skips the bundle check gracefully when no build artifact exists.</done>
</task>

<task type="auto">
  <name>Task 2: verify:phase-6 re-point + stale phase-4a removal (D-92)</name>
  <files>package.json</files>
  <read_first>06-CONTEXT.md D-92 (exact re-point target + phase-4a reconcile) · 06-RESEARCH.md Pitfall 6 (gate mis-pointing) + Open Question 3 (phase-7 left alone) · @package.json (line 22 phase-4a placeholder + line 25 verify:phase-6 mis-pointed at telemetry/DiagnosticsSection) · 05-02-SUMMARY.md / 04-01-SUMMARY.md (the D-78/D-68 re-point precedents)</read_first>
  <action>Edit package.json scripts (two scoped edits — use Edit, never rewrite the file): (1) replace the verify:phase-6 line — currently pointing at the Phase-11 dirs (telemetry/DiagnosticsSection) — with `"verify:phase-6": "tsc --noEmit && vitest run tests/core/extraction tests/core/content tests/isolation/no-content-script-ui.test.ts"` (D-92 verbatim — the §18 required test dirs + the new isolation test); (2) DELETE the stale verify:phase-4a script line entirely (it is a placeholder pointing at the same Phase-6 dirs — Phase 6 owns these dirs now; nothing references phase-4a). Do NOT touch verify:phase-7 (its mis-pointing at Phase-15 dirs is Phase-7's own gate reconciliation — RESEARCH Open Question 3; flag it in the SUMMARY). Leave all other scripts byte-identical.</action>
<!-- planner-discipline-allow: telemetry -->
  <acceptance_criteria>`grep -n '"verify:phase-6"' package.json` shows `tests/core/extraction tests/core/content tests/isolation/no-content-script-ui.test.ts`; `grep -c 'phase-4a' package.json` equals 0 (script deleted); `grep -c 'telemetry' package.json` equals 0 (Phase-11 dirs no longer referenced by the phase-6 gate); `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` succeeds (valid JSON).</acceptance_criteria>
  <verify>
    <automated>grep -q '"verify:phase-6": "tsc --noEmit && vitest run tests/core/extraction tests/core/content tests/isolation/no-content-script-ui.test.ts"' package.json && test "$(grep -c 'phase-4a' package.json)" -eq 0 && node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"</automated>
  </verify>
  <done>verify:phase-6 targets the §18 required test dirs (D-92); the stale phase-4a placeholder is gone; verify:phase-7 mis-pointing flagged for its own phase; package.json parses.</done>
  <!-- planner-discipline-allow: phase-4a -->
  <reversibility rating="reversible">D-92 script edit — a package.json one-line change, revertible by git (D-68/D-78 precedent).</reversibility>
</task>

<task type="auto">
  <name>Task 3: ADR-P6-01 flip to Accepted (D-79 spike outcome) + full phase gate GREEN</name>
  <files>.planning/adr/ADR-P6-01-defuddle-panel-side.md</files>
  <read_first>06-CONTEXT.md D-79 (spike outcome → ADR Accepted, no measurement pass) · 06-RESEARCH.md Pattern 1 spike outcome (guarded defaultView?. getComputedStyle; Readability 0.6.0 inline-only _isProbablyVisible — verified Readability.js:2694-2707; nexus #329 confirmation; the one known delta: stylesheet-driven display:none removal inert on detached docs) · 06-01-SUMMARY.md (the tracer evidence) · @.planning/adr/ADR-P6-01-defuddle-panel-side.md (the file to flip — Status line 3 + Verification section)</read_first>
  <action>Edit `.planning/adr/ADR-P6-01-defuddle-panel-side.md`: (1) replace the Status line (currently the spike-pending value) with `- **Status:** Accepted (2026-08-29 — SPIKE-P6-01 resolved by the Phase-6 tracer tests; no measurement pass)`; (2) append a `## Spike Outcome (SPIKE-P6-01)` section recording: detached-doc fidelity is ACCEPTABLE — defuddle 0.19.x guards computed-style access (`element.ownerDocument.defaultView?.getComputedStyle` — optional chaining, degrades rather than throws; independently confirmed by nexus issue #329) and Readability 0.6.0's `_isProbablyVisible` uses inline style/attribute checks only (verified Readability.js:2694-2707), so both engines run on a DOMParser document with `defaultView === null`; evidence = the real-engine detached-doc tests in `tests/core/extraction/DefuddleStrategy.test.ts` (06-01 tracer: no-throw + non-empty markdown on synthesized KB/portal fixtures with base-href injection); the one known fidelity delta (stylesheet-driven `display:none` removal is inert on detached docs; inline `display:none`/`hidden` still detected) degrades gracefully and is partially mitigated by the content-script pre-strip (06-04); conclusion: NO content-script measurement pass — placement stays panel-side per the ADR and §26.4. Then run the FULL phase gate: `pnpm verify:phase-6` must pass end-to-end (tsc + the §18 test dirs + the isolation test). If any test is red, fix the offending test/module in its owning plan's file (do NOT weaken the gate) and re-run.</action>
  <acceptance_criteria>`grep -n "Status:" .planning/adr/ADR-P6-01-defuddle-panel-side.md` shows Accepted; `grep -c "Spike Outcome" .planning/adr/ADR-P6-01-defuddle-panel-side.md` equals 1 (the outcome record exists); `grep -n "DefuddleStrategy.test.ts" .planning/adr/ADR-P6-01-defuddle-panel-side.md` shows the evidence citation; `pnpm verify:phase-6` exits 0 (the full phase gate GREEN — ROADMAP SC-7).</acceptance_criteria>
  <verify>
    <automated>pnpm verify:phase-6 && grep -q "Accepted" .planning/adr/ADR-P6-01-defuddle-panel-side.md && grep -q "Spike Outcome" .planning/adr/ADR-P6-01-defuddle-panel-side.md</automated>
  </verify>
  <done>ADR-P6-01 is Accepted with the SPIKE-P6-01 outcome recorded (evidence = the 06-01 tracer tests; no measurement pass — D-79), and `pnpm verify:phase-6` is GREEN — the phase gate passes per §24.</done>
  <reversibility rating="reversible">D-79 ADR status flip — a documentation status change; the panel-side placement itself was never in question (ADR + §26.4 fixed it).</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content-script bundle → the isolation grep | The built/source content modules are asserted against the §24 forbidden set — the boundary that keeps the <50 KB, dependency-free bundle intact |
| package.json scripts → the verify gate | The gate target is the enforcement point — a mis-pointed gate certifies nothing (Pitfall 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-P6-03 | Tampering | content bundle dependencies | critical | mitigate | The isolation gate greps the §24 forbidden set across content-script source AND the built bundle when present, with a non-vacuous self-test — a bundle regression (transitive math/HTML deps hoisted by the bundler, Pitfall 7) blocks the phase gate; the <50 KB assertion catches size regressions (§22.1) |
| T-P6-19 | Tampering | vacuous gate | high | mitigate | Self-test block (cross-entrypoint-imports.test.ts:84-124 style) proves FORBIDDEN_RE catches planted violations — a silently weakened regex trips its own test (Pitfall 6 / #1479: a gate that cannot fail measures nothing) |
| T-P6-20 | — | gate mis-pointing | medium | mitigate | D-92 re-points verify:phase-6 to the §18 dirs and removes the stale phase-4a placeholder; the full-gate run in Task 3 proves the gate is meaningful (exits 0 with the real test files present) |
| T-P6-SC | Tampering | npm installs | high | mitigate | N/A this plan — zero new dependencies; the isolation grep additionally proves the defuddle-family libs never reached the content bundle |
</threat_model>

<verification>
- Per-task commits: `pnpm run lint` + the affected test file (Task 1); grep + JSON.parse assertions (Task 2); `pnpm verify:phase-6` (Task 3).
- Nyquist: every task carries an automated `<verify>`; the final gate run is the phase-level proof.
- Grep guards: zero NP-STRICT in tests/isolation/; phase-4a gone from package.json; telemetry no longer referenced by the phase-6 gate.
- Out-of-scope flag: verify:phase-7's mis-pointing (Phase-15 dirs) is documented in the plan SUMMARY for the Phase-7 planner (RESEARCH Open Question 3) — NOT fixed here.
- Cross-plan file discipline: only the three listed files are touched; any red module surfaced by the gate is a bug for its owning plan's correction, never a gate weakening.
</verification>

<success_criteria>
1. `tests/isolation/no-content-script-ui.test.ts` is green and non-vacuous — the §24 forbidden set + File System Access API are rejected across content source and (when present) the built bundle; the <50 KB target is asserted when measurable (ROADMAP SC-2).
2. `verify:phase-6` targets `tests/core/extraction tests/core/content tests/isolation/no-content-script-ui.test.ts`; the stale phase-4a placeholder is removed (D-92).
3. ADR-P6-01 is Accepted with the SPIKE-P6-01 outcome recorded (D-79).
4. `pnpm verify:phase-6` exits 0 — the phase gate is GREEN (ROADMAP SC-7 / §24); the phase's DONE-when contract is met.
</success_criteria>

<output>
Create `.planning/phases/06-pagecontentservice-knowledge-acquisition/06-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| File | Provides / Exports | Plan |
|------|--------------------|------|
| tests/isolation/no-content-script-ui.test.ts | §18/§24 isolation gate — FORBIDDEN_RE (React family, defuddle + transitive math/HTML deps, yaml, File System Access API), source + built-bundle greps, <50 KB assertion, non-vacuous self-test | **[this plan]** |
| package.json | verify:phase-6 re-pointed to the §18 test dirs (D-92); phase-4a placeholder removed | **[this plan]** |
| .planning/adr/ADR-P6-01-defuddle-panel-side.md | Status → Accepted + SPIKE-P6-01 outcome record (D-79) | **[this plan]** |
| (consumed) all 06-01..06-04 modules + tests | the gate's subject matter — `pnpm verify:phase-6` GREEN at the end of this plan | 06-01..06-04 |