# Deferred Items — Phase 6

Out-of-scope discoveries logged during execution (per executor scope boundary:
only auto-fix issues directly caused by the current task's changes).

| Date | Plan | Item | Evidence | Owner |
|------|------|------|----------|-------|
| 2026-08-29 | 06-01 | `tests/core/workspace/journalingAdapter.test.ts` fails with ENOENT on `/Users/george.li/Documents/workspaces/nowpilot/src/core/workspace/journalingAdapter.ts` — the test hardcodes the original author's macOS path; unrelated to Phase 6 (pre-existing environment-specific failure, not caused by 06-01 changes). | Full-suite regression run at 06-01 Task 4: `1 failed \| 482 passed`; failure is `fs.readFileSync('/Users/george.li/...')` at journalingAdapter.test.ts:289. | **RESOLVED 2026-08-30 (commit 8b891a2)** — path now resolves relative to `process.cwd()`; suite green (559/559). |
| 2026-08-30 | 06-05 | **`entrypoints/content/core.content.ts` is NOT a valid WXT 0.20.27 entrypoint — the content script is never built or registered in the manifest.** The D-07a "directory form" path matches none of WXT's content-script globs (`content.[jt]s?(x)`, `content/index.[jt]s?(x)`, `*.content.[jt]s?(x)`, `*.content/index.[jt]s?(x)` — verified with picomatch). The built-bundle isolation check (Pitfall 7) therefore skips permanently until fixed, and the extension ships WITHOUT the content script (D-84 producer wiring is dead at build level). Discovered during 06-05 Task 1's `pnpm build:ext` materialization attempt; a transient `content/index.ts` scratch entrypoint built the bundle cleanly at 9.51 kB with zero forbidden ids — proving the Phase-6 modules are clean and the ONLY defect is the entrypoint shape. | `pnpm build:ext` at 06-05: manifest.json has no `content_scripts` entry; WXT 0.20.27 `find-entrypoints.mjs` `PATH_GLOB_TO_TYPE_MAP` (node_modules/wxt/dist/core/utils/building/find-entrypoints.mjs:259) does not match `content/core.content.ts`. | **RESOLVED 2026-08-30 (commit 45250b5)** — `entrypoints/content/index.ts` re-export added; `pnpm build:ext` registers `content-scripts/content.js` (9.51 kB, ISOLATED) in the manifest; built-bundle isolation checks now run green (verify:phase-6 94/94). |