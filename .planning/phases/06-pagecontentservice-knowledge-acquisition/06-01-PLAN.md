---
phase: 06-pagecontentservice-knowledge-acquisition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/setup.ts
  - src/core/content/PageContext.ts
  - src/core/context/types.ts
  - src/core/extraction/apcLite.types.ts
  - src/core/extraction/strategies/IExtractionStrategy.ts
  - src/core/extraction/strategies/DefuddleStrategy.ts
  - src/core/extraction/PageContentSerializer.ts
  - src/core/extraction/PageContentService.ts
  - tests/core/extraction/DefuddleStrategy.test.ts
  - tests/core/extraction/PageContentService.test.ts
# autonomous: false — Task 1 is a checkpoint:human-verify with gate=blocking-human (defuddle package legitimacy — never auto-approved)
autonomous: false
requirements: []             # infra phase — no spec-native v1 IDs (ROADMAP Phase 6 note); acceptance = §18 DONE-when + D-79..D-92
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - statement: "Defuddle runs panel-side: DefuddleStrategy lives under src/core/extraction/ and the content bundle never imports it — the §26.4 placement boundary (ROADMAP SC-1 / ADR-P6-01 / D-81)"
      verification: backstop
    - statement: "Every extraction records its producing engine: StrategyResult.source ∈ {'defuddle','readability','apc-lite','servicenow-api'} — Readability exists ONLY as DefuddleStrategy's internal low-confidence fallback, never as a separate strategy file; 'servicenow-api' stays reserved-unregistered (spec 4688-4693 two-enums note, D-80)"
      verification: backstop
    - statement: "The Defuddle call is privacy-explicit: useAsync:false + synchronous parse() in the strategy — the option defaults to TRUE in 0.19.x and omitting it would allow third-party API fetches (T-P6-05 / spec 3740)"
      verification: backstop
    - statement: "extract() returns a typed discriminated union — {ok:false, code:'CONTENT_EXTRACT_FAILED'} on every failure path (timeout, strategy throw, fallback exhaustion), never a silent empty result (D-91 / Appendix C.2 / §26.6)"
      verification: backstop
    - statement: "PageContext is canonical at src/core/content/PageContext.ts (verbatim spec 4345-4391) and src/core/context/types.ts re-exports it — ContextOptimizer's import keeps resolving, no parallel copy (D-83 / D-72 precedent)"
      verification: backstop
    - statement: "The panel-side round-trip is proven by tests: fixture HTML + stamped baseUrl → extract() → PageContext (url/origin/hostname/title/markdown/meta/extractedAt) — the §18 DONE-when 'Defuddle runs in the side panel/standalone; content script only serializes HTML' (ROADMAP SC-1, D-82)"
      verification: backstop
    - statement: "Dependencies land at spec-pinned versions — defuddle ^0.19 (≥0.19.2, CVE-2026-30830 patch floor — VAI-01 confirmed), @mozilla/readability ^0.6, turndown ^7, minisearch ^7 — re-verified at install (VAI-04); defuddle human-verified before install (package legitimacy gate)"
      verification: backstop
  artifacts:
    - path: "src/core/content/PageContext.ts"
      provides: "CANONICAL PageContext / TabContext / SNowCaseData / FileContext / NoteContext verbatim from spec 4345-4391 (D-83 supersession point)"
      exports: ["PageContext", "TabContext", "SNowCaseData", "FileContext", "NoteContext"]
    - path: "src/core/context/types.ts"
      provides: "D-83 re-export — local PageContext declaration deleted, re-exported from ../content/PageContext; RetrievedMemory/ToolSchemaRef/CompressionType/Summarizer untouched (Phase 8/18 own those supersession points)"
      exports: ["PageContext", "TabContext", "SNowCaseData", "FileContext", "NoteContext"]
    - path: "src/core/extraction/apcLite.types.ts"
      provides: "RawNode interface + GeometrySchema/InteractionSchema/FormControlSchema (password-value omission refine) + APCLiteNode/APCLiteNodeSchema (lazy recursive) + APCLiteDocumentSchema (source enum incl. 'dom'|'ax'|'hybrid'|'servicenow-api'|'defuddle'|'readability') — verbatim spec 4393-4448"
      exports: ["RawNode", "GeometrySchema", "InteractionSchema", "FormControlSchema", "APCLiteNode", "APCLiteNodeSchema", "APCLiteDocumentSchema", "APCLiteDocument"]
    - path: "src/core/extraction/strategies/IExtractionStrategy.ts"
      provides: "StrategyInput / StrategyResult / IExtractionStrategy contract verbatim spec 4667-4693 + the four tunables PAGE_CACHE_MAX_TABS=20 / PAGE_HTML_MAX_BYTES=2_000_000 / INDEX_CHUNK_MAX_TOKENS=500 / PAGE_EXTRACTION_TIMEOUT_MS=5_000 (spec 4696-4699)"
      exports: ["StrategyInput", "StrategyResult", "IExtractionStrategy", "PAGE_CACHE_MAX_TABS", "PAGE_HTML_MAX_BYTES", "INDEX_CHUNK_MAX_TOKENS", "PAGE_EXTRACTION_TIMEOUT_MS"]
    - path: "src/core/extraction/strategies/DefuddleStrategy.ts"
      provides: "PRIMARY read path — detached-doc Defuddle parse (DEFAULT import correction + base-href injection + useAsync:false) with internal Readability low-confidence fallback; source provenance 'defuddle'|'readability'; truncated propagation"
      exports: ["DefuddleStrategy", "defuddleStrategy", "DEFUDDLE_LOW_CONFIDENCE_WORD_COUNT"]
    - path: "src/core/extraction/PageContentSerializer.ts"
      provides: "StrategyResult → PageContext (serializeToPageContext, pure) + APCLiteNode tree → structural markdown (apcTreeToMarkdown)"
      exports: ["serializeToPageContext", "apcTreeToMarkdown"]
    - path: "src/core/extraction/PageContentService.ts"
      provides: "Per-surface module singleton orchestrator — extract() with strategy chain (canHandle by mode), single 5 s AbortController, typed ExtractResult union (CONTENT_EXTRACT_FAILED), panel-side redaction seam (D-90), metrics recording (durationMs/source/truncated/charCount)"
      exports: ["PageContentService", "ExtractResult", "ExtractionMetrics", "redactExtractedContent"]
    - path: "tests/core/extraction/DefuddleStrategy.test.ts"
      provides: "§18 required test — real-engine detached-doc fidelity (SPIKE-P6-01 host: no-throw on DOMParser doc, base-href relative-link resolution) + Readability fallback paths + truncated propagation"
      exports: []
    - path: "tests/core/extraction/PageContentService.test.ts"
      provides: "§18 required test — extract() → PageContext round-trip via fixture HTML + baseUrl; CONTENT_EXTRACT_FAILED on throw/timeout/fallback-exhaustion (never silent-empty); redaction seam; metrics"
      exports: []
    - path: "package.json"
      provides: "dependencies += defuddle ^0.19, @mozilla/readability ^0.6, turndown ^7, minisearch ^7"
      exports: []
    - path: "tests/setup.ts"
      provides: "chrome.tabs mock (onUpdated/onRemoved listener capture) — Wave-0 infra needed by cache/content tests (06-VALIDATION.md Wave 0)"
      exports: []
  key_links:
    - from: "src/core/extraction/PageContentService.ts"
      to: "src/core/extraction/strategies/DefuddleStrategy.ts"
      via: "extract() selects by canHandle({url, mode}); mode 'default' → defuddleStrategy.run(StrategyInput with additive baseUrl?)"
      pattern: "canHandle"
    - from: "src/core/extraction/strategies/DefuddleStrategy.ts"
      to: "src/core/extraction/PageContentSerializer.ts"
      via: "StrategyResult.source/markdown feed serializeToPageContext → PageContext (the §2.3 ContextOptimizerInput.pageContext consumer contract, D-82)"
      pattern: "serializeToPageContext"
    - from: "src/core/context/types.ts"
      to: "src/core/content/PageContext.ts"
      via: "export type { PageContext, TabContext, SNowCaseData, FileContext, NoteContext } from '../content/PageContext' — D-72 re-export precedent; ContextOptimizer.ts:36 import keeps resolving"
      pattern: "export type \\{ PageContext"
    - from: "src/core/extraction/strategies/DefuddleStrategy.ts"
      to: "defuddle/full"
      via: "import Defuddle from 'defuddle/full' (DEFAULT export — RESEARCH correction 1; spec 3721's named import fails TS2305) + { markdown:true, url:base, useAsync:false }.parse() (correction 3)"
      pattern: "useAsync: false"
  prohibitions:
    - "No ReadabilityStrategy or ServiceNow strategy file (D-80): Readability is provenance-only inside DefuddleStrategy; 'servicenow-api' stays in the StrategyResult.source union + reserved ordering, never registered"
    - "No edits to src/core/ai/*, src/components/*, AgentOrchestrator, WorkspaceStore, or ContextOptimizer.ts (D-81 create-only): the only Phase-5 contact is the PageContext re-export in src/core/context/types.ts — git-diff-assertable"
    - "No new error codes (D-38): CONTENT_EXTRACT_FAILED is the Appendix C.2 closed-set literal used directly; StreamErrorCodeSchema untouched"
    - "No barrel index.ts in src/core/extraction/ or src/core/content/ — direct path imports only (mirror src/core/ai layout)"
    - "Zero @ts-expect-error NP-STRICT markers in all new modules (STATE.md decision 17 — new code strict-clean)"
    - "No cache/index in this plan (D-88/D-87): PageContentCache and PageIndexBuilder land in 06-03/06-04; extract() is cache-free"
    - "No content-side shells in this plan (D-85/D-86: 06-04): only PageContext.ts (types) is created under src/core/content/ here"
    - "Do NOT apply the 80-char-truncating storage redaction primitive to the whole PageContext — it would destroy the page body; the phase-6 seam redacts secret-shaped meta/addonFields keys and passes markdown/html through (D-90 intent, documented below)"
---

<objective>
TRACER plan: install the four extraction dependencies (with the defuddle human-verify gate), create the canonical type spine (`PageContext.ts` supersession + `apcLite.types.ts` + the strategy contract), then wire ONE end-to-end panel-side extraction path — `PageContentService.extract()` → `DefuddleStrategy` (real defuddle on a detached doc with base-href) → `PageContentSerializer` → `PageContext` — proven by the two §18 test files (`DefuddleStrategy.test.ts` hosts the SPIKE-P6-01 detached-doc fidelity evidence; `PageContentService.test.ts` proves the fixture-HTML → PageContext round-trip and the never-silent-empty error contract).

Purpose: prove the phase's architecture end-to-end on the earliest context tokens — the extraction spine (payload → DOMParser + base-href → Defuddle → fallback → serializer → PageContext) type-checks and runs against the real defuddle engine before any expansion (ApcLiteStrategy 06-02, cache 06-03, index + content shells 06-04, isolation gate 06-05) builds on it. This is the thinnest vertical slice through every layer the phase introduces on the read path; the tracer's modules are production-quality (D-81 create-only — no pipeline wiring, no surface call-sites).

Output: the eleven files listed in frontmatter, strict-clean, with the locked D-79..D-84/D-90/D-91 semantics and the two RESEARCH corrections (default import + useAsync:false) embedded in the DefuddleStrategy code.
</objective>

<assumption_delta_decision>
  term: "fallback" (as in "Defuddle → Readability fallback")
  generalized_noun: "extraction strategy with ordered layering — StrategyResult.source records which engine produced the output"
  decision: "promote"
  rationale: "The layered strategy model is primary; Defuddle/Readability/APC-lite are details of one variant, matching the §26.2 extract(tabId, mode) dispatch and the IExtractionStrategy contract. Encoded as an invariant — every extraction records its source — not a new abstraction. Readability is DefuddleStrategy's internal low-confidence fallback (provenance only, never a separate strategy file — spec 4688-4693, D-80)."
</assumption_delta_decision>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  # Phase 6 block (191-205): goal, success criteria, gate, SPIKE-P6-01 → ADR-P6-01 note
@.planning/STATE.md  # decision 12 (ADR-P6-01 Proposed, spike pending), decision 17 (strict-clean, zero NP-STRICT), VAI-01/VAI-04 watch items
@.planning/PRODUCT_SPEC_v0_1.md  # §18 Phase 6 (2608-2644), §26.1-26.8 (3672-3794), §26.4 canonical call (3718-3742), §26.4a lifecycle (3744-3761), §26.6 reliability/privacy (3776-3786), Appendix C PageContext block (4345-4391), Appendix C apcLite block (4393-4448), extraction contract (4658-4699), Appendix C.2 closed codes (3420-3433), §2.3 ContextOptimizerInput.pageContext (463-489)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-CONTEXT.md  # D-79..D-92 locked decisions + discretion items
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-RESEARCH.md  # corrections 1-3, Patterns 1/2/4/5, Pitfalls 1/3/4/8, Assumptions A1/A2, Open Questions 1/4, Package Legitimacy Audit, Security Domain
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-PATTERNS.md  # module sections for PageContentService/apcLite.types/IExtractionStrategy/DefuddleStrategy/PageContentSerializer/PageContext.ts + shared patterns (module singleton, typed result union, redaction seam, debug logging)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-VALIDATION.md  # per-task verification map (06-01-01/02, 06-02-01, 06-03-01) + Wave 0 checklist
@.planning/adr/ADR-P6-01-defuddle-panel-side.md  # SPIKE-P6-01 directive — the spike evidence is produced by DefuddleStrategy.test.ts in this plan; ADR flip recorded in 06-05
@src/core/context/types.ts  # lines 17-29 = the PageContext supersession point to replace via re-export (D-83)
@src/core/runtime/RuntimeEnvelope.ts  # D-15 declared envelope types + frozen PageHtmlPayload (40-46) — the payload contract extract() consumes
@src/core/context/ContextOptimizer.ts  # line 36 import type { PageContext } from './types' + buildContextText (385-388) — the consumer contract that must keep resolving
@src/core/context/TokenBudget.ts  # countTokensHeuristic (44-56) — token estimation convention; Phase-6 strategies reuse the heuristic for approxTokens
@src/core/log/debugLog.ts  # debugLog(code, message, context?) — never raw console.log (codes EXTRACT_DONE/EXTRACT_FAILED)
@src/core/security/redactSensitive.ts  # 80-char truncation + secret-key regex (20-31) — the seam's semantics; DO NOT apply wholesale to PageContext (see prohibition)
@src/core/ai/ProviderRegistry.ts  # module-singleton + namespace-export + __test__ seams pattern (141-146, 434-459)
@tests/setup.ts  # chrome mocks — the chrome.tabs mock is added here (Task 2)
@entrypoints/content/core.content.ts  # the thin content shell that stays untouched this plan (content shells are 06-04)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify defuddle package legitimacy before install (package-legitimacy gate)</name>
  <what-built>defuddle package legitimacy verification — pre-install blocking gate (package-legitimacy protocol, T-P6-SC)</what-built>
  <how-to-verify>
    The research legitimacy audit flagged defuddle as [SUS] on publish-recency only (0.19.3 published 2026-08-22; the package is long-established). Confirm at https://www.npmjs.com/package/defuddle:
    1. Publisher/repo = kepano/defuddle (the Obsidian Web Clipper extraction engine, ~9.2k GitHub stars, MIT) — this is the expected provenance per RESEARCH.md Package Legitimacy Audit
    2. Latest version is 0.19.x and the ^0.19 range satisfies ≥ 0.19.2 (the CVE-2026-30830 patch floor — VAI-01)
    3. No postinstall script on the latest publish (npm view defuddle scripts.postinstall is null/absent)
    4. Weekly downloads in the hundreds of thousands (562K/wk at research time) — consistent with a mainstream package
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the dependency install (Task 2); describe any concern to stop</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the four extraction dependencies + chrome.tabs test mock</name>
  <files>package.json, tests/setup.ts</files>
  <read_first>06-RESEARCH.md Standard Stack + Installation (114-127) + Package Legitimacy Audit (131-141) + Environment Availability (522-537) · 06-PATTERNS.md metadata note 5 (install command) · @tests/setup.ts (chrome mock conventions — storage.local/sync/session maps; add chrome.tabs in the same style) · 06-VALIDATION.md Wave 0 checklist (dependency install + tabs mock rows)</read_first>
  <action>Run `pnpm add defuddle@^0.19 @mozilla/readability@^0.6 turndown@^7 minisearch@^7`. Before and after install, re-verify versions per VAI-04: `npm view defuddle version` (expect 0.19.x, ≥ 0.19.2 — VAI-01 CVE-2026-30830 floor), `npm view @mozilla/readability version` (0.6.x), `npm view turndown version` (7.x), `npm view minisearch version` (7.x); confirm none has a postinstall script (`npm view <pkg> scripts.postinstall` → null). If any version falls outside the pinned ranges, stop and report — do not silently accept a downgrade.

Then add a `chrome.tabs` mock to tests/setup.ts in the existing Map-backed style: `chrome.tabs.onUpdated` and `chrome.tabs.onRemoved` each expose `addListener(handler)` / `removeListener(handler)` capturing handlers into module-level arrays, plus a test helper `(globalThis as any).__fireTabEvent(type, ...args)` that invokes captured listeners (e.g. `__fireTabEvent('onUpdated', tabId, changeInfo, tab)` and `__fireTabEvent('onRemoved', tabId, removeInfo)`). This mock is Wave-0 infra (06-VALIDATION.md) consumed by the PageContentCache lifecycle tests in 06-03 and the content-shell tests in 06-04. Keep the existing mocks byte-identical otherwise.</action>
  <acceptance_criteria>`pnpm ls defuddle @mozilla/readability turndown minisearch --depth 0` shows all four installed under dependencies; `grep -n '"defuddle"\|"@mozilla/readability"\|"turndown"\|"minisearch"' package.json` shows the four dependency entries with the pinned ranges; `grep -n "onUpdated\|onRemoved\|__fireTabEvent" tests/setup.ts` shows the tabs mock; `pnpm run lint` (tsc --noEmit) still passes with the setup.ts addition.</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && grep -q "onUpdated" tests/setup.ts && grep -q '"defuddle"' package.json && grep -q '"@mozilla/readability"' package.json && grep -q '"turndown"' package.json && grep -q '"minisearch"' package.json</automated>
  </verify>
  <done>The four deps are installed at spec-pinned ranges (defuddle ≥ 0.19.2 per VAI-01; all re-verified per VAI-04, no postinstall); chrome.tabs onUpdated/onRemoved listener-capture mock + __fireTabEvent helper live in tests/setup.ts; tsc is green.</done>
</task>

<task type="auto">
  <name>Task 3: Type spine — canonical PageContext supersession + apcLite types + strategy contract</name>
  <files>src/core/content/PageContext.ts, src/core/context/types.ts, src/core/extraction/apcLite.types.ts, src/core/extraction/strategies/IExtractionStrategy.ts</files>
  <read_first>06-PATTERNS.md PageContext.ts section (verbatim spec 4345-4391 + supersession header convention from UserPreferences.ts:1-6) + context/types.ts re-export section (D-72 precedent) + apcLite.types.ts section (zod patterns: z.enum closed sets, z.lazy recursion, FormControlSchema.refine, output schema-parse) + IExtractionStrategy.ts section (contract verbatim + tunables style) · 06-CONTEXT.md D-80/D-83 + the agent's Discretion (extraction layout) · PRODUCT_SPEC Appendix C (4345-4448) + extraction contract (4658-4699) — copy verbatim · @src/core/context/ContextProvenanceManifest.ts (schema-first + z.infer export pattern 9-43) · @src/core/ai/ILLMProvider.ts (interface-only contract file shape)</read_first>
  <action>Create the four type files. All are strict-clean, schema-first, verbatim from the spec blocks — do not invent fields or rename.

1. `src/core/content/PageContext.ts` — header comment in the UserPreferences.ts supersession style: "Phase-6 canonical home — replaces the Phase-5 placeholder in src/core/context/types.ts (spec 4345)." Declare verbatim spec 4345-4391: PageContext { url, origin, hostname, title, html?, markdown?, meta: Record<string,string>, extractedAt: number, addonId?, addonFields? }, TabContext { tabId, windowId, page: PageContext, pinnedAt? }, SNowCaseData, FileContext, NoteContext. NOTE: this TabContext (Appendix C) is a DIFFERENT type from WorkspaceStore's local TabContext { tabId,title,url,pinned } (the pinnedTabs subscription signal, D-88) — add a comment naming the distinction so no executor conflates them; do NOT touch WorkspaceStore.

2. `src/core/context/types.ts` — MODIFY: delete the local PageContext declaration (current lines 17-29) and add `export type { PageContext, TabContext, SNowCaseData, FileContext, NoteContext } from '../content/PageContext';` (D-83, D-72 re-export precedent — ContextOptimizer.ts:36 keeps resolving). Keep RetrievedMemory, ToolSchemaRef, CompressionType, Summarizer byte-identical (Phase 8/18 own those supersession points).

3. `src/core/extraction/apcLite.types.ts` — verbatim spec 4393-4448: RawNode interface; GeometrySchema; InteractionSchema; FormControlSchema with the refine invariant (a password control must not carry a value — D-86); APCLiteNode type; recursive APCLiteNodeSchema via z.lazy; APCLiteDocumentSchema with source enum ['dom','ax','hybrid','servicenow-api','defuddle','readability']; APCLiteDocument = z.infer. Keep RawNode a plain serializable interface (the content-side walker imports it type-only — Pitfall 8; zod must never enter the content bundle).

4. `src/core/extraction/strategies/IExtractionStrategy.ts` — verbatim spec 4667-4693: StrategyInput { url, title, mode: 'default'|'actionable', html?, raw? } PLUS the documented additive field `baseUrl?: string` — required by the §26.4 canonical call (the panel injects the payload's effective base URL into the detached doc; falls back to `url` when absent). Comment the deviation: "spec 3726-3740 passes payload.baseUrl to Defuddle; StrategyInput carries it as the additive optional baseUrl". StrategyResult { source: 'defuddle'|'readability'|'apc-lite'|'servicenow-api', markdown?, root?, meta?, approxTokens, truncated }; IExtractionStrategy { id: StrategyResult['source'], canHandle(i:{url,mode}): boolean, run(i): Promise<StrategyResult> }. Copy the two-enums NOTE comment verbatim (spec 4688-4693) — Readability is provenance-only, servicenow-api reserved for Phase 17. Export the four tunables verbatim (spec 4696-4699): PAGE_CACHE_MAX_TABS=20, PAGE_HTML_MAX_BYTES=2_000_000, INDEX_CHUNK_MAX_TOKENS=500, PAGE_EXTRACTION_TIMEOUT_MS=5_000. No barrel index.ts — direct path imports only (mirror src/core/ai).</action>
  <acceptance_criteria>`pnpm run lint` passes strict-clean; `grep -n "export type { PageContext" src/core/context/types.ts` shows the re-export and `grep -c "interface PageContext" src/core/context/types.ts` equals 0 (no parallel copy — D-83); `grep -n "refine" src/core/extraction/apcLite.types.ts` shows the FormControlSchema password invariant (D-86); `grep -n "servicenow-api" src/core/extraction/apcLite.types.ts src/core/extraction/strategies/IExtractionStrategy.ts` shows the reserved id in both unions but NO strategy file registers it; `ls src/core/extraction/strategies/` lists exactly IExtractionStrategy.ts (no ReadabilityStrategy/ServiceNow file — D-80); `grep -rn "NP-STRICT" src/core/content/ src/core/extraction/ | wc -l` equals 0; `grep -n "PAGE_EXTRACTION_TIMEOUT_MS = 5_000" src/core/extraction/strategies/IExtractionStrategy.ts` shows the tunable.</acceptance_criteria>
  <verify>
    <automated>pnpm run lint && test "$(grep -c 'interface PageContext' src/core/context/types.ts)" -eq 0 && grep -q "refine" src/core/extraction/apcLite.types.ts && test "$(ls src/core/extraction/strategies/ | wc -l)" -eq 1 && test "$(grep -rn 'NP-STRICT' src/core/content/ src/core/extraction/ | wc -l)" -eq 0</automated>
  </verify>
  <done>The canonical PageContext types exist at src/core/content/PageContext.ts and the Phase-5 types.ts re-exports them (ContextOptimizer's import resolves — no parallel copy); apcLite.types.ts ships the verbatim Appendix C schemas with the password-omission refine; IExtractionStrategy.ts ships the verbatim contract + additive baseUrl? + four tunables with the two-enums note; no extra strategy files exist.</done>
  <reversibility rating="reversible">PageContext supersession (D-83) — a re-export; moving the canonical home later is an import edit.</reversibility>
</task>

<task type="tracer">
  <name>Task 4: TRACER — panel-side extraction spine: DefuddleStrategy + PageContentSerializer + PageContentService, proven by the two §18 test files</name>
  <files>src/core/extraction/strategies/DefuddleStrategy.ts, src/core/extraction/PageContentSerializer.ts, src/core/extraction/PageContentService.ts, tests/core/extraction/DefuddleStrategy.test.ts, tests/core/extraction/PageContentService.test.ts</files>
  <read_first>06-RESEARCH.md corrections 1-3 + Pattern 1 (corrected canonical Defuddle call, 387-407) + Pattern 2 (layered provenance + low-confidence heuristic) + Pattern 4 (Readability fallback, 409-421) + Pitfalls 1/3/4 + Assumptions A1/A2 + Open Question 1 (synthesized fixtures) + Open Question 4 (redaction call-site recommendation) + Security Domain (T-P6-04/05/07) · 06-PATTERNS.md DefuddleStrategy.ts section (145-171) + PageContentSerializer.ts section (183-201) + PageContentService.ts section (40-77) + shared patterns (module singleton, typed result union, redaction seam, debug logging) · 06-CONTEXT.md D-79/D-81/D-82/D-90/D-91 + discretion (singleton-vs-factory, redaction call-site, low-confidence threshold) · PRODUCT_SPEC §26.4 (3718-3742) + §26.6 (3776-3786) + extraction contract (4667-4693) + Appendix C.2 CONTENT_EXTRACT_FAILED (3420-3433) · @src/core/ai/ProviderRegistry.ts (module-singleton + __test__ seam 141-146, 434-459) · @src/core/context/ContextOptimizer.ts (typed-result union precedent 102-112) · @src/core/log/debugLog.ts · @src/core/security/redactSensitive.ts (20-31 secret-key regex semantics — for the redaction seam) · @src/core/context/TokenBudget.ts (countTokensHeuristic for approxTokens) · @tests/core/context/ContextOptimizer.test.ts (fixture-style test conventions)</read_first>
  <action>Test-first within this task: write the two §18 test files with inline synthesized fixtures FIRST (KB-article-shaped HTML, portal-record-shaped HTML — RESEARCH Open Question 1 recommends 2-3 representative fixtures inline; run them RED against the absent modules), then implement the three modules to GREEN. This is the phase's tracer — one real, verified path through every panel-side layer.

1. `src/core/extraction/strategies/DefuddleStrategy.ts` — implements IExtractionStrategy. `id: 'defuddle'`. canHandle({mode}) = mode === 'default'. run(input): (a) parse input.html with DOMParser; (b) inject `<base href>` when absent using `const base = input.baseUrl ?? input.url` (spec 3726-3740 canonical shape; comment the additive baseUrl? field); (c) `new Defuddle(doc, { url: base, markdown: true, useAsync: false }).parse()` — useAsync:false is EXPLICIT (privacy-critical, defaults true in 0.19.x — T-P6-05) and parse() is synchronous; (d) low-confidence branch (DEFUDDLE_LOW_CONFIDENCE_WORD_COUNT = 50, below defuddle's internal 200-word auto-retry — Research recommendation): if the result's markdown is empty/whitespace OR wordCount < 50 → Readability fallback: `new Readability(doc).parse()` (may mutate doc — the detached doc is disposable; clone first only if reuse were needed, it is not here) → null or empty textContent → return `{ source: 'readability', markdown: undefined, approxTokens: 0, truncated: true }` (caller surfaces the typed error — never silent); else TurndownService({ headingStyle: 'atx' }).turndown(article.content) → markdown; (e) confident path returns `{ source: 'defuddle', markdown: result.content, meta: { title: result.title, wordCount: String(result.wordCount) }, approxTokens: countTokensHeuristic(markdown), truncated: input.truncated }`; (f) any throw → catch → the fallback-failed shape with source 'readability' (never rethrow — the service decides the typed error). IMPORTANT import shape (RESEARCH correction 1): `import Defuddle from 'defuddle/full'` — the DEFAULT export; the spec's named-import shape fails TS2305; add a one-line comment noting the spec-literal deviation. Export the singleton `export const defuddleStrategy = new DefuddleStrategy()` (openaiProvider precedent).

2. `src/core/extraction/PageContentSerializer.ts` — pure functions, no side effects. `serializeToPageContext(input: { url, origin, hostname, title, extractedAt, strategyResult: StrategyResult, mode }): PageContext` — builds the spec 4346-4357 shape: url/origin/hostname/title verbatim from input, markdown from strategyResult.markdown (when present), meta merged from strategyResult.meta (string values only), extractedAt from input; html is NOT carried (the raw payload HTML stays in the cache layer, 06-03 — the serializer emits the page content contract only). `apcTreeToMarkdown(root: APCLiteNode): string` — structural markdown renderer for the actionable path (used from 06-02 onward): headings from textStyle.level (h1-h6), paragraph text lines, links as [text](href), form controls as labelled field lines, tables as pipe rows — deterministic, no turndown (the tree is not HTML).

3. `src/core/extraction/PageContentService.ts` — per-surface module singleton (ProviderRegistry pattern: module-level Map + namespace export + named exports + `__test__` reset seam). Register `defuddleStrategy` at module load (ApcLiteStrategy registers in 06-02 — mode 'actionable' before then has no handler → typed error, which is legitimate tracer behavior, not an architectural gap). Export types: `ExtractionMetrics = { durationMs: number; source: StrategyResult['source']; truncated: boolean; charCount: number }`; `ExtractResult = { ok: true; context: PageContext; metrics: ExtractionMetrics } | { ok: false; code: 'CONTENT_EXTRACT_FAILED'; message: string; cause?: unknown }` (Appendix C.2 literal — no invented codes, D-38; do NOT add to StreamErrorCodeSchema). `extract(input: { tabId: number; url: string; title: string; mode: 'default' | 'actionable'; html?: string; baseUrl?: string; raw?: RawNode; signal?: AbortSignal }): Promise<ExtractResult>` — (a) single AbortController per extraction: PAGE_EXTRACTION_TIMEOUT_MS=5_000 internal timeout merged with the caller signal (mirror the Requester single-controller pattern — both abort paths classify as timeout); (b) select the strategy by canHandle({url, mode}); no handler → ok:false CONTENT_EXTRACT_FAILED; (c) run the strategy with StrategyInput { url, title, mode, html, baseUrl, raw }; (d) redact panel-side via the module-local `redactExtractedContent(context: PageContext): PageContext` seam (D-90): deep-clone, empty `meta`/`addonFields` entries whose keys match the /key|token|secret|authorization/i pattern (same semantics as redactSensitive.ts:25), pass markdown/html/title/url through UNCHANGED (page content is the extraction's purpose; password values are already omitted at capture by 06-02's walker); header comment: superseded by the richer TraceRedactor (Phase 11); (e) serialize via PageContentSerializer; (f) record metrics + debugLog('EXTRACT_DONE'|'EXTRACT_FAILED') with REDACTED context only; (g) never return an empty ok:true — a strategy that yields no markdown and no tree surfaces the typed error. Export `redactExtractedContent` for testability.

4. `tests/core/extraction/DefuddleStrategy.test.ts` — the SPIKE-P6-01 host (fixture HTML inline: a KB-article-shaped fixture and a portal-record-shaped fixture, synthesized per RESEARCH Open Question 1): (a) real-engine no-throw on a detached DOMParser doc with base-href (A1/A2 gate — jsdom provides DOMParser; if `defuddle/full` fails to LOAD under jsdom at module import time, gate the real-engine describe behind a load-probe try/catch with a documented skip and note the `pnpm build:ext` + node-harness fallback in the test header — do not silently weaken the test); (b) markdown output non-empty and title extracted for the KB fixture; (c) base-href relative-link resolution: a fixture with a relative href resolves against the injected base (assert the strategy ran with the base and produced markdown containing the resolved-absolute link text); (d) low-confidence fixture → source 'readability' (fallback-success path — craft the fixture so the defuddle output is thin but the Readability path yields text; tune empirically); (e) fallback-exhaustion: content so thin that Readability returns null → the fallback-failed shape (source 'readability', markdown undefined, truncated true); (f) truncated propagation (input.truncated → result.truncated).

5. `tests/core/extraction/PageContentService.test.ts` — (a) round-trip: KB fixture html + baseUrl → extract({mode:'default'}) → ok:true with PageContext.url/origin/hostname/title/markdown non-empty/meta/extractedAt and metrics.source === 'defuddle' (D-82 — the §2.3 ContextOptimizerInput.pageContext consumer contract); (b) malformed/empty html → ok:false code CONTENT_EXTRACT_FAILED (never silent-empty); (c) strategy throw → ok:false CONTENT_EXTRACT_FAILED; (d) timeout: a strategy whose run() never resolves, with vi.useFakeTimers advancing 5_000ms → ok:false CONTENT_EXTRACT_FAILED (D-91); (e) redaction: meta containing an apiKey-shaped key → returned PageContext.meta has it emptied (D-90); (f) fallback-exhaustion end-to-end → ok:false (never an empty ok:true). Use the '@/' alias imports. No chrome mocks needed (service is pure orchestration — the envelope round-trip is 06-04).</action>
<!-- planner-discipline-allow: CONTENT_EXTRACT_FAILED -->
  <acceptance_criteria>`npx vitest run tests/core/extraction/DefuddleStrategy.test.ts tests/core/extraction/PageContentService.test.ts` passes (the tracer's end-to-end proof: real defuddle on a detached doc + extract() → PageContext round-trip + all CONTENT_EXTRACT_FAILED error paths); `pnpm run lint` passes strict-clean; `grep -n "import Defuddle from 'defuddle/full'" src/core/extraction/strategies/DefuddleStrategy.ts` shows the DEFAULT import (RESEARCH correction 1); `grep -n "useAsync: false" src/core/extraction/strategies/DefuddleStrategy.ts` shows the explicit privacy flag (correction 3); `grep -c "CONTENT_EXTRACT_FAILED" src/core/extraction/PageContentService.ts` is >= 1 and `grep -n "CONTENT_EXTRACT_FAILED" src/core/ai/types.ts | wc -l` equals 0 (closed-set literal used, StreamErrorCodeSchema untouched — D-38); `grep -rn "NP-STRICT" src/core/extraction/ | wc -l` equals 0; `ls src/core/extraction/strategies/` lists exactly IExtractionStrategy.ts + DefuddleStrategy.ts (no ReadabilityStrategy file — D-80); `grep -rn "ReadabilityStrategy\|ServiceNowStrategy" src/ | wc -l` equals 0; `git status --porcelain src/core/ai/ src/components/ src/core/workspace/ src/core/context/ContextOptimizer.ts | wc -l` equals 0 (create-only boundary, D-81); `git status --porcelain src/core/context/types.ts` is non-empty (the ONLY Phase-5 file deliberately modified — the D-83 re-export).</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/extraction/DefuddleStrategy.test.ts tests/core/extraction/PageContentService.test.ts && pnpm run lint && test "$(grep -rn 'NP-STRICT' src/core/extraction/ | wc -l)" -eq 0 && test "$(grep -rn 'ReadabilityStrategy\|ServiceNowStrategy' src/ | wc -l)" -eq 0 && test "$(git status --porcelain src/core/ai/ src/components/ src/core/workspace/ src/core/context/ContextOptimizer.ts | wc -l)" -eq 0</automated>
  </verify>
  <done>The tracer slice is green and committed: DefuddleStrategy runs the real defuddle engine panel-side on a detached doc (default import, explicit useAsync:false, Readability fallback with source provenance), PageContentSerializer emits the canonical PageContext, PageContentService extracts end-to-end with the 5 s timeout + typed CONTENT_EXTRACT_FAILED + redaction seam — proven by the two §18 test files. SPIKE-P6-01 evidence produced (detached-doc fidelity acceptable, no-throw); ADR flip recorded in 06-05.</done>
  <reversibility rating="reversible">D-79 spike outcome — the ADR status flip + optional additive measurement pass; panel-side placement itself is fixed by ADR-P6-01 and §26.4.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content-script payload → panel PageContentService | Untrusted host-page HTML crosses here — parsed on a detached DOMParser doc, never rendered (XSS surface moved off the host page, ADR-P6-01) |
| PageContentService → PageContext | The extraction output exits for Phase-7 ContextOptimizerInput.pageContext — must be redacted before indexing/logging (D-90) |
| DefuddleStrategy → third-party APIs | The useAsync option boundary — if left at its 0.19.x default (true), async extractors could fetch third-party APIs (FxTwitter) — prohibited (§0.2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-P6-04 | Tampering | DefuddleStrategy / extracted HTML | high | mitigate | defuddle pinned ^0.19 (≥0.19.2 — CVE-2026-30830 XSS fix, VAI-01 confirmed at install); extraction output is never rendered via raw innerHTML in this phase (no render path ships — create-only, D-81); markdown-renderer sanitization is a consumer-side concern (Phase-3 x-markdown), documented in RESEARCH Security Domain |
| T-P6-05 | Information Disclosure | DefuddleStrategy async extractors | high | mitigate | useAsync: false + synchronous parse() both explicit (belt-and-braces, spec 3740); acceptance greps assert the flag is present; the content script carries zero fetch( — existing Phase-1 isolation gate |
| T-P6-07 | Denial of Service | PageContentService.extract / payload parsing | high | mitigate | 2 MB hard cap + element-boundary truncation ship with the serializer (06-04); the single 5 s AbortController (PAGE_EXTRACTION_TIMEOUT_MS) classifies both caller-abort and timeout to CONTENT_EXTRACT_FAILED (D-91) — a hanging strategy can never stall the surface |
| T-P6-01 | Spoofing | extracted page text | medium | accept | Phase-6 extraction hygiene only — page text is data, never instructions; the full CTX-02 six-layer trust defense ships in Phase 7 (RESEARCH Security Domain row, out of Phase-6 scope) |
| T-P6-08 | Availability | detached-doc computed-style crash | medium | mitigate | defuddle guards defaultView?.getComputedStyle (degrades on detached docs — nexus #329 confirmation); Readability 0.6.0 _isProbablyVisible is inline-only; the spike fixture test (DefuddleStrategy.test.ts) asserts no-throw on the detached doc (A1/A2) |
| T-P6-SC | Tampering | npm installs | high | mitigate | package-legitimacy gate: checkpoint:human-verify (gate=blocking-human) before the defuddle install (Task 1) — never auto-approved; remaining three packages [OK] per RESEARCH audit; no postinstall scripts verified |
</threat_model>

<verification>
- **Per-task commits:** `pnpm run lint` + the affected test file (06-VALIDATION.md sampling rate).
- **Tracer discipline (documented acceptance — NOT an executor error):** the two §18 test files are created INSIDE Task 4 (test-first RED → module GREEN) rather than in a separate later plan, because the tracer's whole purpose is a real runnable end-to-end proof (extract() → real defuddle → PageContext) — a grep-only tracer would not prove the architecture. Every task carries an automated `<verify>`, so Nyquist holds; the expansion test files (ApcLiteStrategy/PageIndexBuilder/isolation) land with their owning plans (06-02/06-04/06-05).
- Grep guards: zero `@ts-expect-error NP-STRICT` in src/core/extraction/ + src/core/content/ (STATE.md decision 17); no ReadabilityStrategy/ServiceNowStrategy files (D-80); CONTENT_EXTRACT_FAILED used as a literal only — StreamErrorCodeSchema untouched (D-38).
- Create-only boundary (D-81): `git status --porcelain` empty for src/core/ai/, src/components/, src/core/workspace/, src/core/context/ContextOptimizer.ts; the single deliberate Phase-5 modification is the D-83 re-export in src/core/context/types.ts.
- SPIKE-P6-01 evidence: DefuddleStrategy.test.ts real-engine detached-doc tests green → ADR-P6-01 flips to Accepted in 06-05 Task 3 with this evidence recorded.
- Assumption gates: A1 (defuddle 0.19.x guards computed-style access — proven by the no-throw test) and A2 (jsdom executes defuddle/full — if the UMD bundle cannot load under jsdom, the real-engine describe is gated behind a documented load-probe skip and the spike moves to a `pnpm build:ext` + node harness, noted in the test header — never silently weakened).
</verification>

<success_criteria>
1. The four dependencies are installed at spec-pinned versions (defuddle ≥ 0.19.2 — VAI-01; all re-verified — VAI-04) after a blocking-human package-legitimacy gate; the chrome.tabs mock exists in tests/setup.ts.
2. The canonical type spine is verbatim: src/core/content/PageContext.ts (spec 4345-4391), apcLite.types.ts (spec 4393-4448 with the FormControlSchema password refine), IExtractionStrategy.ts (spec 4667-4699 + additive baseUrl? + two-enums note) — strict-clean, no parallel copies.
3. The tracer path is proven end-to-end by the two §18 test files: fixture HTML + baseUrl → extract() → PageContext, with CONTENT_EXTRACT_FAILED on every failure path (D-91) and the redaction seam applied before the output leaves the service (D-90).
4. DefuddleStrategy embeds both RESEARCH corrections: default import from defuddle/full (spec 3721's named import fails tsc) and explicit useAsync:false + sync parse() (privacy §0.2).
5. SPIKE-P6-01 evidence is produced (detached-doc fidelity acceptable — no measurement pass needed); the ADR flip is recorded in 06-05.
</success_criteria>

<output>
Create `.planning/phases/06-pagecontentservice-knowledge-acquisition/06-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full Phase-6 inventory (source-grounding contract for plan-review). `[this plan]` = created here.

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/content/PageContext.ts | CANONICAL PageContext / TabContext / SNowCaseData / FileContext / NoteContext (verbatim spec 4345-4391, D-83) | **[this plan]** |
| src/core/context/types.ts | D-83 re-export of the five PageContext types (Phase-5 placeholder replaced; RetrievedMemory/ToolSchemaRef/CompressionType/Summarizer untouched) | **[this plan]** |
| src/core/extraction/apcLite.types.ts | RawNode + GeometrySchema + InteractionSchema + FormControlSchema (password refine) + APCLiteNode(APCLiteNodeSchema lazy) + APCLiteDocumentSchema (source enum) | **[this plan]** |
| src/core/extraction/strategies/IExtractionStrategy.ts | StrategyInput (+additive baseUrl?) / StrategyResult / IExtractionStrategy + 4 tunables (PAGE_CACHE_MAX_TABS / PAGE_HTML_MAX_BYTES / INDEX_CHUNK_MAX_TOKENS / PAGE_EXTRACTION_TIMEOUT_MS) + two-enums note | **[this plan]** |
| src/core/extraction/strategies/DefuddleStrategy.ts | PRIMARY read strategy — detached-doc Defuddle (default import, useAsync:false) + Readability low-confidence fallback; source 'defuddle'\|'readability' | **[this plan]** |
| src/core/extraction/PageContentSerializer.ts | serializeToPageContext (StrategyResult → PageContext) + apcTreeToMarkdown (structural tree renderer) | **[this plan]** |
| src/core/extraction/PageContentService.ts | Per-surface singleton orchestrator — extract() + ExtractResult union (CONTENT_EXTRACT_FAILED) + 5 s AbortController + redactExtractedContent seam (D-90) + metrics | **[this plan]** |
| src/core/extraction/PageContentCache.ts | §26.4a lifecycle cache — LRU cap 20, invalidation, coalescing, subscription API (D-88/D-89) | 06-03 |
| src/core/extraction/PageIndexBuilder.ts | ephemeral per-tab MiniSearch index — heading chunking, selectRelevant + 'topk' (D-87) | 06-04 |
| src/core/content/AxDomWalker.ts | content-script RawNode walker — password omission, geometry unset (D-86) | 06-02 |
| src/core/content/PageContextBridge.ts | RuntimeEnvelope bridge — EXTRACT_PAGE_CONTENT handler, PAGE_HTML_PAYLOAD/PAGE_LIVE_CONTEXT producer (D-84) | 06-04 |
| src/core/content/ContentScriptHost.ts | serializer shell — pre-strip + baseUrl + 2 MB cap (D-85) | 06-04 |
| src/core/content/SPANavigationWatcher.ts | ctx.addEventListener(window,'wxt:locationchange') + MutationObserver fallback (D-84/D-88) | 06-04 |
| src/core/runtime/RuntimeEnvelope.ts | + PageLiveContextPayload shape (D-89) | 06-04 |
| entrypoints/content/core.content.ts | thin delegation shell (D-85) | 06-04 |
| tests/core/extraction/DefuddleStrategy.test.ts | §18 — real-engine detached-doc fidelity (SPIKE-P6-01 host) + fallback paths | **[this plan]** |
| tests/core/extraction/PageContentService.test.ts | §18 — extract() → PageContext round-trip + CONTENT_EXTRACT_FAILED paths + redaction + (06-03 adds cache lifecycle) | **[this plan]** (cache in 06-03) |
| tests/core/extraction/ApcLiteStrategy.test.ts | §18 — normalization + schema + password omission | 06-02 |
| tests/core/extraction/PageIndexBuilder.test.ts | §18 — chunking + selectRelevant + ephemerality | 06-04 |
| tests/isolation/no-content-script-ui.test.ts | §18/§24 — built-bundle isolation grep + self-test | 06-05 |
| tests/core/content/{ContentScriptHost,SPANavigationWatcher,PageContextBridge,AxDomWalker}.test.ts | content-shell behavior tests (D-84/85/86; feed the D-92 gate dir) | 06-02/06-04 |
| package.json | + 4 deps (this plan) · verify:phase-6 re-point + phase-4a reconcile (D-92) | **[this plan]** / 06-05 |
| tests/setup.ts | + chrome.tabs onUpdated/onRemoved mock + __fireTabEvent | **[this plan]** |
| .planning/adr/ADR-P6-01-defuddle-panel-side.md | Status Proposed → Accepted + spike outcome record (D-79) | 06-05 |

Layout note (locked): no barrel index.ts in src/core/extraction/ or src/core/content/ — direct path imports only, mirroring the existing src/core/ai layout. Content-side modules import ONLY src/core/runtime/RuntimeEnvelope (types + createEnvelope) and each other — never src/core/extraction/* or zod (Pitfall 8; enforced by the 06-05 isolation grep).