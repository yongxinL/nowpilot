---
phase: 06-pagecontentservice-knowledge-acquisition
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/core/content/AxDomWalker.ts
  - src/core/extraction/strategies/ApcLiteStrategy.ts
  - tests/core/extraction/ApcLiteStrategy.test.ts
  - tests/core/content/AxDomWalker.test.ts
autonomous: true
requirements: []             # infra phase — no spec-native v1 IDs (ROADMAP Phase 6 note); acceptance = §18 DONE-when + D-80/D-86/D-90
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - statement: "The actionable path runs only on request: AxDomWalker executes only when a mode:'actionable' extraction is requested — zero AX cost on the default read/summarize path (D-86 / §26.6)"
      verification: backstop
    - statement: "Passwords are never captured: the walker omits the value of any password control at capture (FormControlSchema.refine invariant, D-86/D-90 — ROADMAP SC-6 'passwords never captured')"
      verification: backstop
    - statement: "APCLiteNode.geometry stays unset in v0.1 (§26.6): the walker never populates geometry — if ever populated it must be read content-side against live layout, never in the panel's detached doc"
      verification: backstop
    - statement: "ApcLiteStrategy normalizes RawNode → APCLiteNode and validates the output with APCLiteDocumentSchema (source:'ax'); the StrategyResult records source:'apc-lite' — layered-fallback provenance (ROADMAP SC-3, D-80)"
      verification: backstop
    - statement: "The content bundle stays clean: AxDomWalker imports ONLY the runtime-envelope module (types) and sibling content modules — no zod, no panel-side modules (Pitfall 8 / §24)"
      verification: backstop
  artifacts:
    - path: "src/core/content/AxDomWalker.ts"
      provides: "Content-script-safe structural DOM+ARIA walker producing RawNode trees (roles/text/hierarchy/interaction flags/links/tables); password values omitted at capture; geometry? unset; runs only on mode:'actionable'"
      exports: ["walkDom", "isPasswordControl", "RawNode"]
    - path: "src/core/extraction/strategies/ApcLiteStrategy.ts"
      provides: "Actionable-path strategy — RawNode → APCLiteNode normalization + APCLiteDocumentSchema validation; canHandle(mode==='actionable'); StrategyResult.source 'apc-lite'; turndown-free (tree, not HTML)"
      exports: ["ApcLiteStrategy", "apcLiteStrategy", "normalizeRawNode"]
    - path: "tests/core/extraction/ApcLiteStrategy.test.ts"
      provides: "§18 required test — normalization correctness, APCLiteDocumentSchema validation (source:'ax'), password-value fixture rejected by the refine, stats (nodeCount/approxTokens/durationMs/truncated)"
      exports: []
    - path: "tests/core/content/AxDomWalker.test.ts"
      provides: "Content-side walker proof — jsdom doc → RawNode tree (roles/interaction/links/tables), password value omitted, geometry unset, no zod import"
      exports: []
  key_links:
    - from: "src/core/extraction/strategies/ApcLiteStrategy.ts"
      to: "src/core/extraction/apcLite.types.ts"
      via: "normalizeRawNode maps RawNode → APCLiteNode; output validated with APCLiteDocumentSchema.parse (source:'ax') — cross-boundary zod per CLAUDE.md"
      pattern: "APCLiteDocumentSchema"
    - from: "src/core/content/AxDomWalker.ts"
      to: "src/core/content/PageContextBridge.ts"
      via: "the bridge (06-04) invokes walkDom only for mode:'actionable' EXTRACT_PAGE_CONTENT requests and returns the RawNode tree over the envelope — D-86 gating"
      pattern: "walkDom"
    - from: "src/core/content/AxDomWalker.ts"
      to: "src/core/runtime/RuntimeEnvelope.ts"
      via: "type-only import (createEnvelope + envelope types) — the ONLY allowed content-side dependency (Pitfall 8)"
      pattern: "import type"
  prohibitions:
    - "No zod in AxDomWalker or any content-side module (Pitfall 8): RawNode stays a plain interface; FormControlSchema.refine runs where the walker builds the node and ApcLiteStrategy validates panel-side"
    - "No geometry population (v0.1, §26.6): the walker leaves geometry? unset — never read layout in the panel; a future measurement pass would read content-side"
    - "No ReadabilityStrategy/ServiceNow file (D-80): ApcLiteStrategy is the only new strategy here"
    - "No edits to 06-01 modules (DefuddleStrategy/PageContentService/PageContentSerializer stay byte-identical this plan)"
    - "Zero @ts-expect-error NP-STRICT markers (STATE.md decision 17)"
    - "No barrel index.ts (mirror src/core/ai layout)"
  security:
    - "Password omission is enforced at capture (D-86/D-90): the walker must not emit form.control.value when isPassword is true; the schema refine is the panel-side backstop — a fixture carrying a password value must FAIL validation"
---

<objective>
Expansion plan: build the phase's second strategy — the **actionable path** (mode:'actionable') — split across the two responsibility tiers per the Architectural Responsibility Map: `AxDomWalker` captures `RawNode` trees content-script-side (ISOLATED world, zero AX cost unless requested, password values omitted at capture), and `ApcLiteStrategy` normalizes `RawNode` → `APCLiteNode` and validates with `APCLiteDocumentSchema` panel-side. Proven by the §18 `ApcLiteStrategy.test.ts` plus the content-side `AxDomWalker.test.ts`.

Purpose: deliver the ROADMAP SC-3 layered-fallback second half (AX→DOM provenance) and the ROADMAP SC-6 password guarantee (isPassword ⇒ value omitted — enforced at capture, the only place live form values exist). D-86 is the normative contract; the walker runs only on `mode:'actionable'` requests (D-81 create-only — the request path is exercised via PageContextBridge + tests in 06-04, not a shipped surface call-site).

Output: the two strategy-side modules + two test files, strict-clean, with the walker/strategy split matching §26.6 (capture content-side, normalize + validate panel-side).
</objective>

<execution_context>
@/home/yongxin.Li/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/yongxin.Li/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRODUCT_SPEC_v0_1.md  # §26.2 layered strategy (3680-3706), §26.6 APC-lite depth + password omission (3776-3786), Appendix C apcLite block verbatim (4393-4448), extraction contract (4667-4693)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-CONTEXT.md  # D-80 (two strategies only), D-86 (walker content-side, actionable-only, password refine, geometry unset), D-90 (redaction panel-side; content script omits passwords only)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-RESEARCH.md  # Architectural Responsibility Map rows 3-4 (APC-lite split), Pattern 2 provenance, Security Domain (T-P6-06 password capture), Pitfall 8
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-PATTERNS.md  # ApcLiteStrategy.ts section (175-180) + AxDomWalker.ts section (350-352, NO ANALOG — spec Appendix C verbatim) + shared patterns (zod at cross-boundary, module singleton)
@.planning/phases/06-pagecontentservice-knowledge-acquisition/06-VALIDATION.md  # 06-02-01/06-02-02 per-task map
@src/core/extraction/apcLite.types.ts  # the RawNode interface + APCLiteNode + schemas under test (from 06-01)
@src/core/extraction/strategies/IExtractionStrategy.ts  # StrategyInput/StrategyResult contract ApcLiteStrategy implements
@src/core/extraction/PageContentService.ts  # registerStrategy seam (module-level; ApcLiteStrategy registers here at module load)
@src/core/runtime/RuntimeEnvelope.ts  # the type-only dependency allowed in content modules
@tests/core/context/ContextOptimizer.test.ts  # fixture-style test conventions
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AxDomWalker — content-script structural walker with password omission</name>
  <files>src/core/content/AxDomWalker.ts, tests/core/content/AxDomWalker.test.ts</files>
  <read_first>06-PATTERNS.md AxDomWalker.ts section (350-352 — NO ANALOG; spec Appendix C verbatim) · 06-RESEARCH.md Architectural Responsibility Map (APC-lite walk content-side) + Pitfall 8 (content import boundary) + Security Domain T-P6-06 · PRODUCT_SPEC §26.6 APC-lite depth (3776-3786) + RawNode (4393-4402) · @src/core/runtime/RuntimeEnvelope.ts (the type-only dependency allowed) · 06-CONTEXT.md D-86/D-90</read_first>
  <behavior>
    - Test 1: a jsdom document with a heading, a paragraph, a link, a button, and a two-cell table walks to a RawNode tree whose root role/text/interaction flags and children reflect the structure; link.href is captured
    - Test 2: a password input in the fixture produces a form.control node with isPassword:true and NO value key (value omitted at capture — the D-86/D-90 invariant)
    - Test 3: a text input produces form.control with the value captured (non-password values ARE captured)
    - Test 4: geometry is never present on any RawNode (v0.1, §26.6 — geometry? stays unset)
    - Test 5: the module imports nothing but type-only from the runtime-envelope module + sibling content modules (no zod, no panel-side imports — enforced by grep)
  </behavior>
  <action>Create `src/core/content/AxDomWalker.ts` — the content-script-safe structural DOM+ARIA walker (NO ANALOG in the codebase; spec Appendix C RawNode is the target shape). Header comment: content-side, ISOLATED world, extraction-only; imports ONLY the runtime-envelope module (type-only: envelope types) and sibling content modules — never zod, never the panel-side extraction layer (Pitfall 8). Test-first: write tests/core/content/AxDomWalker.test.ts (RED), then implement walkDom (GREEN).

`walkDom(root: Element, options?: { maxDepth?: number }): RawNode` — recursive structural walk producing the spec 4393-4402 RawNode shape: id (synthesized stable path like 'n1','n1.1','n1.2' — a deterministic DFS index), role (ARIA role when present else semantic role from the element: 'heading' for h1-h6 with textStyle level, 'link' for anchors, 'button', 'textbox', 'list'/'listitem', 'table'/'row'/'cell', 'region' default), text (textContent trimmed for leaf-ish nodes; heading text), interaction flags (clickable for button/a[href]/[role=button], editable for input/textarea/[contenteditable], focusable, disabled from the disabled attribute), link { href } for anchors (absolute via element.href — the live document resolves it), form.control for input/select/textarea { fieldName from name, fieldType from type, value ONLY when not a password, isPassword: boolean }, children for element nodes with non-empty descendants. IMPORTANT: password omission at capture — when the control is a password input (type=password OR isPasswordControl heuristic), the value key MUST NOT be emitted at all (D-86/D-90; the schema refine is the panel-side backstop, the walker is the enforcement point). The optional geometry field of RawNode stays UNSET (v0.1 §26.6 — never read layout in the walker; a future measurement pass would be additive content-side). Keep the walk bounded: maxDepth guard (default 32) + node cap (e.g. 5_000 nodes) to bound AX cost on pathological pages — a cap breach truncates the tree and the caller (bridge, 06-04) flags it. Export `isPasswordControl(el): boolean` (type=password or name/aria-label matching a password heuristic) for the omission logic. Runs ONLY when the bridge (06-04) invokes it for a mode:'actionable' request — the walker itself is a pure function, the gating lives at the call site.</action>
<!-- planner-discipline-allow: geometry -->
  <acceptance_criteria>`npx vitest run tests/core/content/AxDomWalker.test.ts` passes (all five behavior groups green); `pnpm run lint` passes strict-clean; `grep -rn "from '@/core/extraction\|from '../extraction\|from '../../extraction" src/core/content/AxDomWalker.ts | wc -l` equals 0 (no panel-side import — Pitfall 8); `grep -c "geometry" src/core/content/AxDomWalker.ts` is >= 1 (declared-unset) and `grep -c "geometry:" src/core/content/AxDomWalker.ts` equals 0 (never populated — v0.1 §26.6); `grep -rn "NP-STRICT" src/core/content/ | wc -l` equals 0.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/content/AxDomWalker.test.ts && pnpm run lint && test "$(grep -rn 'from .*extraction' src/core/content/AxDomWalker.ts | wc -l)" -eq 0 && test "$(grep -rn 'NP-STRICT' src/core/content/ | wc -l)" -eq 0</automated>
  </verify>
  <done>AxDomWalker walks a live jsdom document into RawNode trees with roles/text/interaction/links/tables; password values are omitted at capture (isPassword ⇒ no value key); geometry stays unset; the module imports only the envelope module type-only — content-bundle-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ApcLiteStrategy — RawNode → APCLiteNode normalization + schema validation</name>
  <files>src/core/extraction/strategies/ApcLiteStrategy.ts, tests/core/extraction/ApcLiteStrategy.test.ts</files>
  <read_first>06-PATTERNS.md ApcLiteStrategy.ts section (175-180 — interface-implementation + output schema-parsed, buildManifest convention) · 06-RESEARCH.md Pattern 2 (layered provenance) + Security Domain T-P6-06 · PRODUCT_SPEC §26.2 (3694-3706) + Appendix C apcLite block (4393-4448) + extraction contract (4667-4693) · 06-CONTEXT.md D-80/D-86 · @src/core/extraction/apcLite.types.ts (APCLiteNodeSchema/APCLiteDocumentSchema under test) · @src/core/extraction/strategies/IExtractionStrategy.ts (StrategyInput/StrategyResult) · @src/core/extraction/PageContentService.ts (registerStrategy — ApcLiteStrategy registers at module load)</read_first>
  <behavior>
    - Test 1: a RawNode fixture (heading/link/button/table with interaction flags) normalizes to an APCLiteNode tree with the same hierarchy, and APCLiteDocumentSchema.parse succeeds with source:'ax' and stats {nodeCount, approxTokens, durationMs, truncated:false}
    - Test 2: a RawNode fixture whose form.control carries isPassword:true AND a value REJECTS validation (the FormControlSchema refine — the D-86 backstop)
    - Test 3: canHandle({mode:'actionable'}) === true and canHandle({mode:'default'}) === false (zero AX cost on the read path)
    - Test 4: run() returns StrategyResult { source:'apc-lite', root, approxTokens, truncated:false } for a valid raw tree
    - Test 5: run() with input.raw undefined (actionable without the walker payload) yields a failed fallback shape that lets the service surface CONTENT_EXTRACT_FAILED — never a silent empty result
  </behavior>
  <action>Create `src/core/extraction/strategies/ApcLiteStrategy.ts` — the panel-side actionable strategy (interface-implementation shape per OpenAIProvider analog). Test-first: write tests/core/extraction/ApcLiteStrategy.test.ts (RED), then implement (GREEN).

`ApcLiteStrategy implements IExtractionStrategy` — `id: 'apc-lite'`; canHandle({mode}) = mode === 'actionable' (D-86: zero AX cost unless requested). `run(input: StrategyInput): Promise<StrategyResult>` — (a) input.raw absent → return `{ source: 'apc-lite', markdown: undefined, approxTokens: 0, truncated: true }` (caller surfaces the typed error — never silent); (b) normalize the RawNode tree → APCLiteNode tree via `normalizeRawNode(raw: RawNode): APCLiteNode` (exported for tests): map role/text/type verbatim, textStyle.level for headings (h1-h6 → 1-6), interaction flags from the raw interaction record, link/image/iframe fields, form { name, control } from raw.form.control (the password value is already absent at capture — the refine below is the backstop), children recursively; domNodeId left undefined; geometry left undefined; (c) build the APCLiteDocument: `APCLiteDocumentSchema.parse({ url: input.url, title: input.title, extractedAt: Date.now(), source: 'ax', root: normalizedRoot, stats: { nodeCount, approxTokens: countTokensHeuristic(serializedTree) or a node-count heuristic, durationMs, truncated: false } })` — schema-parse the OUTPUT (buildManifest convention, ContextProvenanceManifest.ts:82-93); a password-carrying fixture therefore throws at validation — catch and return the failed shape (source 'apc-lite', approxTokens 0, truncated true); (d) return `{ source: 'apc-lite', root: doc.root, meta: { nodeCount: String(doc.stats.nodeCount) }, approxTokens: doc.stats.approxTokens, truncated: doc.stats.truncated }`. Export the singleton instance and register it at module load into PageContentService's strategy set (import PageContentService and call its register seam — the service already registers defuddleStrategy the same way; the actionable path becomes live once registered). Note: `PageContentSerializer.apcTreeToMarkdown` (06-01) consumes the root for a markdown view — wire it only if the serializer export is consumed by a test here; otherwise leave the tree as the StrategyResult.root contract.</action>
<!-- planner-discipline-allow: apcLiteStrategy -->
  <acceptance_criteria>`npx vitest run tests/core/extraction/ApcLiteStrategy.test.ts` passes (all five behavior groups green); `pnpm run lint` passes strict-clean; `grep -n "APCLiteDocumentSchema.parse" src/core/extraction/strategies/ApcLiteStrategy.ts` shows the output schema-validation; `grep -c "apcLiteStrategy" src/core/extraction/strategies/ApcLiteStrategy.ts src/core/extraction/PageContentService.ts` >= 2 (singleton + registration); `grep -rn "NP-STRICT" src/core/extraction/ | wc -l` equals 0.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/core/extraction/ApcLiteStrategy.test.ts && pnpm run lint && test "$(grep -rn 'NP-STRICT' src/core/extraction/ | wc -l)" -eq 0</automated>
  </verify>
  <done>ApcLiteStrategy normalizes RawNode → APCLiteNode, validates the APCLiteDocument (source:'ax') with the password refine as backstop, gates on mode:'actionable', and is registered into PageContentService — the actionable path is live and proven by the §18 test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host-page DOM → AxDomWalker | Untrusted live page structure crosses here — the walker reads roles/text/controls in the ISOLATED world and must never execute page-provided strings |
| content-script RawNode → ApcLiteStrategy | Untrusted serialized tree crosses into the panel — zod-validated (APCLiteDocumentSchema) before use (V5 input validation) |
| form.control capture | The only place live form values exist — password values must never be emitted (D-86/D-90) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-P6-06 | Information Disclosure | AxDomWalker form capture | high | mitigate | Password omission enforced AT CAPTURE (the walker never emits value when isPassword) + FormControlSchema.refine backstop at panel-side validation (a password-carrying fixture fails APCLiteDocumentSchema.parse — asserted by test); D-90's panel-side redaction is the third layer |
| T-P6-09 | Tampering | RawNode tree content | medium | mitigate | All cross-boundary tree data is schema-validated panel-side (APCLiteDocumentSchema.parse) before consumption; text is data, never instructions (extraction hygiene — CTX-02 completes in Phase 7) |
| T-P6-10 | Denial of Service | unbounded DOM walk | medium | mitigate | walkDom bounds: maxDepth 32 + 5,000-node cap — a pathological page truncates instead of hanging the walker; the bridge flags truncation |
| T-P6-03 | Tampering | content bundle deps | high | mitigate | AxDomWalker imports only the envelope module type-only + siblings; no zod/panel-side imports (grep-asserted); the built-bundle isolation grep (06-05) enforces the §24 set |
| T-P6-SC | Tampering | npm installs | high | mitigate | N/A this plan — zero new dependencies (deps landed in 06-01 behind the blocking-human gate) |
</threat_model>

<verification>
- Per-task commits: `pnpm run lint` + the affected test file (06-VALIDATION.md sampling rate).
- TDD discipline: both tasks are test-first (RED test → GREEN module) — each carries a `<behavior>` block making test expectations explicit before implementation; every task has an automated `<verify>` (Nyquist holds).
- Grep guards: zero NP-STRICT in src/core/content/ + src/core/extraction/; no panel-side import in AxDomWalker (Pitfall 8); geometry declared-but-unset.
- No 06-01 module edits: `git status --porcelain src/core/extraction/strategies/DefuddleStrategy.ts src/core/extraction/PageContentService.ts src/core/extraction/PageContentSerializer.ts` — the ONLY 06-01 file ApcLiteStrategy touches is PageContentService.ts via its register seam (consumed via register-seam import, NOT listed in files_modified — behavior correct).
</verification>

<success_criteria>
1. AxDomWalker walks live DOM → RawNode trees with password values omitted at capture and geometry unset — content-bundle-clean (type-only envelope import).
2. ApcLiteStrategy normalizes RawNode → APCLiteNode, validates the APCLiteDocument (source:'ax'), gates on mode:'actionable', and registers into PageContentService.
3. The password invariant is proven at both layers: capture omission (walker test) and validation backstop (refine rejection test).
4. `npx vitest run tests/core/extraction/ApcLiteStrategy.test.ts tests/core/content/AxDomWalker.test.ts` green; `pnpm run lint` strict-clean.
</success_criteria>

<output>
Create `.planning/phases/06-pagecontentservice-knowledge-acquisition/06-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| File | Provides / Exports | Plan |
|------|--------------------|------|
| src/core/content/AxDomWalker.ts | content-script RawNode walker — roles/text/interaction/links/tables; password omission; geometry unset; bounded walk (maxDepth 32 / 5,000 nodes) | **[this plan]** |
| src/core/extraction/strategies/ApcLiteStrategy.ts | actionable-path strategy — normalizeRawNode (RawNode → APCLiteNode) + APCLiteDocumentSchema validation (source 'ax') + register into PageContentService | **[this plan]** |
| tests/core/extraction/ApcLiteStrategy.test.ts | §18 required test — normalization, schema, password refine rejection, canHandle gating, failed-shape on missing raw | **[this plan]** |
| tests/core/content/AxDomWalker.test.ts | walker proof — structure, password omission, non-password capture, geometry unset, import boundary | **[this plan]** |
| (consumed) PageContentService.ts | register seam extended with apcLiteStrategy | 06-01 (modified here) |